// ═══════════════════════════════════════════════════════════════════════════════
// Модуль формы внешней обработки MCPToolkit
// Клиент для подключения к MCP прокси-серверу
// Совместимость: 1С:Предприятие 8.2.13+ / 8.3.25
// ═══════════════════════════════════════════════════════════════════════════════

// ─────────────────────────────────────────────────────────────────────────────────
// ПЕРЕМЕННЫЕ МОДУЛЯ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Перем ФлагПодключения;

&НаКлиенте
Перем ИнтервалОпроса;

&НаКлиенте
Перем МаксимальноеКоличествоПопытокПереподключения;

&НаКлиенте
Перем ТекущееКоличествоПопытокПереподключения;

&НаКлиенте
Перем КэшТокеновМетаданныхРасширений;

&НаКлиенте
Перем Компонента; // AddIn.MCPHttp.MCPHttpTransport

&НаКлиенте
Перем КомпонентаТун; // AddIn.ToonConv.ToonConverter

&НаКлиенте
Перем КомпонентаLineage; // AddIn.Lineage.QueryLineageAnalyzer

&НаКлиенте
Перем МСПСессии; // Соответствие: ИдСессии → АдресВоВременномХранилище

&НаКлиенте
Перем SSEПотоки; // Соответствие: ИдСессии → requestId

&НаКлиенте
Перем ОжидающиеПодтверждения; // Соответствие: requestId → структура {ДанныеЗапроса, ТелоJSON, Ид}

&НаКлиенте
Перем КэшСхемИнструментов; // Структура — кэш для ПолучитьСхемыИнструментов()

&НаКлиенте
Перем ЛегасиССЕПотоки; // Соответствие: transportSessionId → requestId (GET-потока)

&НаКлиенте
Перем ИспользоватьАсинхронноеПодключениеКомпонент; // Булево, инициализируется в ПриОткрытии

&НаКлиенте
Перем ЗапускСервераВПроцессе; // защита от повторного нажатия во время async-цепочки

&НаКлиенте
Перем КэшМаппинговОтвет; // кэш последнего ответа для фильтрации без повторного запроса

&НаКлиенте
Перем МаппингАнонимизации;    // Соответствие: оригинал -> токен

&НаКлиенте
Перем МаппингДеанонимизации;  // Соответствие: ВРег(токен) -> оригинал

&НаКлиенте
Перем СчетчикиТокенов;        // Соответствие: категория -> счётчик

&НаКлиенте
Перем СловарьАнонимизации;    // Массив Структур {ЗначениеНижний, ЗначениеОриг, Категория, Длина}

&НаКлиенте
Перем БакетИндексСловаря;     // Соответствие: первые 3 символа (НРег) -> Массив записей словаря

&НаКлиенте
Перем СловарьПостроен;        // Булево

&НаКлиенте
Перем КомпонентаRegex;           // AddIn.RegexH.RegexHelper

&НаКлиенте
Перем КомпонентаRegexНедоступна; // Истина если попытка загрузки уже провалилась — не пробовать снова

&НаКлиенте
Перем КэшRegexПравил;            // Массив структур — сбрасывается после изменения настроек

&НаКлиенте
Перем КомпонентаSyntaxHelp;      // AddIn.SyntaxH.SyntaxHelpReader

&НаКлиенте
Перем КомпонентаScreenCapture;

&НаКлиенте
Перем КолвоТемBSL;

&НаКлиенте
Перем ПерезапускВОжидании;            // Булево
&НаКлиенте
Перем ИдКомандыПерезапуска;           // Строка — Команда.id для отложенного ответа
&НаКлиенте
Перем ПутьФайлаСигналаПерезапуска;    // Строка
&НаКлиенте
Перем ПутьФайлаПараметровПерезапуска; // Строка — путь к params JSON, удаляется при таймауте
&НаКлиенте
Перем ПутьНонсаПерезапуска;           // Строка — cancellation token, удаляется при таймауте
&НаКлиенте
Перем ПутьФайлаМаппинговПерезапуска;  // Строка — путь к mappings JSON, удаляется при таймауте
&НаКлиенте
Перем ТаймаутПерезапуска;             // Дата
&НаКлиенте
Перем ПутьФайлаСигналаАвтостарта;    // Строка — для новой сессии, используется в колбэках старта
&НаКлиенте
Перем МаппингиДляВосстановления;      // Строка — путь к файлу маппингов, читается после АнонимизаторОчистить()
&НаКлиенте
Перем ПутьКлеймаАвтостарта;           // Строка — claimed_path из params, удаляется в ЗавершитьАвтостартСОшибкой и error-paths
&НаКлиенте
Перем СессийДляВосстановления;        // Массив — inline данные МСПСессии из params JSON, читается после старта сервера
&НаКлиенте
Перем ЗавершениеПослеПерезапускаЗапущено; // Булево — idempotency guard для shutdown callback
&НаКлиенте
Перем ПутьКлеймаПерезапуска;              // Строка — claimed marker, наблюдается на таймауте
&НаКлиенте
Перем КлючШифрованияПерезапуска;          // Строка — hardcoded ключ XOR для пароля в params JSON
&НаКлиенте
Перем ДанныеЗапросаПерезапуска;       // ДанныеЗапроса из ОбработатьToolsCall
&НаКлиенте
Перем ИдЗапросаПерезапуска;           // Ид из ОбработатьToolsCall
&НаКлиенте
Перем ИдПотокаПерезапуска;            // ИдПотокаSSE из ОбработатьToolsCall
&НаКлиенте
Перем ЭтоRESTЗапросПерезапуска;       // Булево — признак REST-вызова, установлен только после duplicate guard
&НаКлиенте
Перем ИдRESTЗапросаПерезапуска;       // Строка — кэш ДанныеЗапроса.id для REST, безопасный повторный доступ
&НаКлиенте
Перем ЗакрытиеСессииВПроцессе;        // guard от параллельных close
&НаКлиенте
Перем ПутьФайлаСигналаХолодногоСтарта; // хранится между ПриОткрытии и местом записи сигнала

// ─────────────────────────────────────────────────────────────────────────────────
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Загрузка сохранённого адреса сервера
	ЗагрузитьНастройки();
	
	// Установка начального статуса
	Статус = "Отключено";
	Подключено = Ложь;
	
	// Значение по умолчанию для адреса сервера
	Если ПустаяСтрока(АдресСервера) Тогда
		АдресСервера = "http://localhost:6003";
	КонецЕсли;

	// Значение по умолчанию для порта встроенного сервера
	Если ПортВстроенногоСервера = 0 Тогда
		ПортВстроенногоСервера = 6003;
	КонецЕсли;

	ОбновитьСводкуПравилТочнойАнонимизацииНаСервере();

	НастройкиАнонИнит = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	ПодтверждениеАнонимизации = НастройкиАнонИнит.ПодтверждениеАнонимизации;

	// Установка подсказок инструментов
	Элементы.ПолеИнструментExecuteQuery.Подсказка     = СоединитьМассив(ОписаниеToolExecuteQuery(),               Символы.ПС);
	Элементы.ПолеИнструментExecuteCode.Подсказка      = СоединитьМассив(ОписаниеToolExecuteCode(),                Символы.ПС);
	Элементы.ПолеИнструментGetMetadata.Подсказка      = СоединитьМассив(ОписаниеToolGetMetadata(),                Символы.ПС);
	Элементы.ПолеИнструментGetEventLog.Подсказка      = СоединитьМассив(ОписаниеToolGetEventLog(),                Символы.ПС);
	Элементы.ПолеИнструментGetObjectByLink.Подсказка  = СоединитьМассив(ОписаниеToolGetObjectByLink(),            Символы.ПС);
	Элементы.ПолеИнструментGetLinkOfObject.Подсказка  = СоединитьМассив(ОписаниеToolGetLinkOfObject(),            Символы.ПС);
	Элементы.ПолеИнструментFindReferences.Подсказка   = СоединитьМассив(ОписаниеToolFindReferencesToObject(),     Символы.ПС);
	Элементы.ПолеИнструментGetAccessRights.Подсказка  = СоединитьМассив(ОписаниеToolGetAccessRights(),            Символы.ПС);
	Элементы.ПолеИнструментGetBslSyntaxHelp.Подсказка = СоединитьМассив(ОписаниеToolGetBslSyntaxHelp(),           Символы.ПС);
	Элементы.ПолеИнструментSubmitDeanon.Подсказка     = СоединитьМассив(ОписаниеToolSubmitForDeanonymization(),   Символы.ПС);
	Элементы.ПолеИнструментGetScreenshot.Подсказка = СоединитьМассив(ОписаниеToolGetScreenshot(),         Символы.ПС);
	Элементы.ПолеИнструментRestart1cSession.Подсказка = СоединитьМассив(ОписаниеToolRestart1cSession(),      Символы.ПС);
	Элементы.ПолеИнструментClose1cSession.Подсказка  = СоединитьМассив(ОписаниеToolClose1cSession(),       Символы.ПС);

	// Загрузка шаблона HTML просмотра markdown
	ДД = РеквизитФормыВЗначение("Объект").ПолучитьМакет("MarkdownViewer");
	Поток = ДД.ОткрытьПотокДляЧтения();
	ЧтениеТекста = Новый ЧтениеТекста(Поток, КодировкаТекста.UTF8);
	ШаблонMarkdownПросмотра = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();
	Поток.Закрыть();

КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)

	ИспользоватьАсинхронноеПодключениеКомпонент = ПроверитьНужноАсинхронноеПодключениеКомпонент();
	ЗапускСервераВПроцессе = Ложь;

	// Инициализация переменных
	ФлагПодключения = Ложь;
	ИнтервалОпроса = 1; // секунды
	МаксимальноеКоличествоПопытокПереподключения = 5;
	ТекущееКоличествоПопытокПереподключения = 0;
	
	// Клиентский кэш токенов для расширений (адреса ВременногоХранилища)
	КэшТокеновМетаданныхРасширений = Новый Соответствие;

	// Инициализация переменных встроенного сервера
	МСПСессии = Новый Соответствие;
	SSEПотоки = Новый Соответствие;
	ОжидающиеПодтверждения = Новый Соответствие;
	КэшСхемИнструментов = Неопределено;

	// Кэш маппингов анонимизации
	КэшМаппинговОтвет = Неопределено;

	// Инициализация переменных перезапуска сессии
	ПерезапускВОжидании               = Ложь;
	ИдКомандыПерезапуска              = "";
	ПутьФайлаСигналаПерезапуска       = "";
	ПутьФайлаПараметровПерезапуска    = "";
	ПутьНонсаПерезапуска              = "";
	ПутьФайлаМаппинговПерезапуска     = "";
	ПутьФайлаСигналаАвтостарта        = "";
	МаппингиДляВосстановления         = "";
	ПутьКлеймаАвтостарта              = "";
	СессийДляВосстановления           = Неопределено;
	ПутьКлеймаПерезапуска             = "";
	ДанныеЗапросаПерезапуска          = Неопределено;
	ИдЗапросаПерезапуска              = "";
	ИдПотокаПерезапуска               = "";
	ЗавершениеПослеПерезапускаЗапущено = Ложь;
	КлючШифрованияПерезапуска = "MCPToolkit-RestartKey-v1-x7Kq2mNp";
	ЭтоRESTЗапросПерезапуска            = Ложь;
	ИдRESTЗапросаПерезапуска            = "";
	ЗакрытиеСессииВПроцессе             = Ложь;
	ПутьФайлаСигналаХолодногоСтарта     = "";

	// Инициализация видимости элементов по текущему режиму
	ПриИзмененииРежимаПодключения(Неопределено);

	// Настройка доступности элементов перезапуска (зависит от типа платформы и прав пользователя)
	НастроитьДоступностьАутентификацииПерезапуска();

	// Автозапуск при передаче параметра /C "autostart;<timestamp>;<путь к params JSON>"
	// или coldstart;<timestamp>;<путь к params JSON> — запуск после close_1c_session
	// Если params JSON удалён или повреждён — закрываем новую autostart-сессию;
	// старая сессия дождётся timeout и продолжит работать.
	// coldstart при stale/битом timestamp — пропускается, форма открывается нормально.
	Если ПараметрЗапуска = "startup" ИЛИ Лев(ПараметрЗапуска, СтрДлина("startup;")) = "startup;" Тогда

		СтартапОшибка = "";

		// Фаза 1: разбор и валидация в локальные сентинелы (без сайд-эффектов)
		НовыйРежим         = Неопределено;
		НовыйАдрес         = Неопределено;
		НовыйКанал         = Неопределено;
		НовыйПорт          = Неопределено;
		НовыйРежимАутентиф = Неопределено;
		НовыйПароль        = Неопределено;

		Попытка
			Если ПараметрЗапуска <> "startup" Тогда
				ОстатокПараметра = Сред(ПараметрЗапуска, СтрДлина("startup;") + 1);
				Пока ЗначениеЗаполнено(ОстатокПараметра) И ПустаяСтрока(СтартапОшибка) Цикл
					РазделительПоз = Найти(ОстатокПараметра, ";");
					Если РазделительПоз > 0 Тогда
						ПараЗначение     = Лев(ОстатокПараметра, РазделительПоз - 1);
						ОстатокПараметра = Сред(ОстатокПараметра, РазделительПоз + 1);
					Иначе
						ПараЗначение     = ОстатокПараметра;
						ОстатокПараметра = "";
					КонецЕсли;

					РавноПоз = Найти(ПараЗначение, "=");
					Если РавноПоз > 0 Тогда
						Ключ     = Лев(ПараЗначение, РавноПоз - 1);
						Значение = URLDecode(Сред(ПараЗначение, РавноПоз + 1));
					Иначе
						Ключ     = ПараЗначение;
						Значение = "";
					КонецЕсли;

					Если Ключ = "mode" Тогда
						Если Значение = "proxy" Тогда
							НовыйРежим = 0;
						ИначеЕсли Значение = "embedded" Тогда
							НовыйРежим = 1;
						Иначе
							СтартапОшибка = "startup: unknown mode=" + Значение;
						КонецЕсли;
					ИначеЕсли Ключ = "url" Тогда
						Если ПустаяСтрока(Значение) Тогда
							СтартапОшибка = "startup: url cannot be empty";
						Иначе
							НовыйАдрес = Значение;
						КонецЕсли;
					ИначеЕсли Ключ = "channel" Тогда
						НовыйКанал = Значение;
					ИначеЕсли Ключ = "port" Тогда
						НомПорт = 0;
						Попытка НомПорт = Число(Значение); Исключение КонецПопытки;
						Если НомПорт >= 1 И НомПорт <= 65535 Тогда
							НовыйПорт = НомПорт;
						Иначе
							СтартапОшибка = "startup: invalid port=" + Значение + " (expected 1..65535)";
						КонецЕсли;
					ИначеЕсли Ключ = "auth" Тогда
						Если Значение = "windows" Тогда
							НовыйРежимАутентиф = 0;
							НовыйПароль = "";
						ИначеЕсли Значение = "password" Тогда
							НовыйРежимАутентиф = 1;
						Иначе
							СтартапОшибка = "startup: unknown auth=" + Значение;
						КонецЕсли;
					ИначеЕсли Ключ = "pwd" Тогда
						НовыйПароль = Значение;
					Иначе
						ДобавитьВЛог("startup: unknown key '" + Ключ + "' — ignored");
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		Исключение
			СтартапОшибка = ОписаниеОшибки();
		КонецПопытки;

		// Фаза 2: применить и сохранить только при полном успехе
		Если ПустаяСтрока(СтартапОшибка) Тогда
			Если НовыйРежим <> Неопределено Тогда
				РежимПодключения = НовыйРежим;
				СохранитьРежимПодключенияНаСервере(РежимПодключения);
			КонецЕсли;
			Если НовыйАдрес <> Неопределено Тогда
				АдресСервера = НовыйАдрес;
				СохранитьНастройкиНаСервере(АдресСервера);
			КонецЕсли;
			Если НовыйКанал <> Неопределено Тогда
				ИдентификаторКанала = НовыйКанал;
				СохранитьИдентификаторКаналаНаСервере(ИдентификаторКанала);
			КонецЕсли;
			Если НовыйПорт <> Неопределено Тогда
				ПортВстроенногоСервера = НовыйПорт;
				СохранитьПортВстроенногоСервераНаСервере(ПортВстроенногоСервера);
			КонецЕсли;
			Если НовыйРежимАутентиф <> Неопределено Тогда
				РежимАутентификацииПерезапуска = НовыйРежимАутентиф;
				СохранитьРежимАутентификацииПерезапускаНаСервере(РежимАутентификацииПерезапуска);
			КонецЕсли;
			Если НовыйПароль <> Неопределено Тогда
				ПарольПерезапуска = НовыйПароль;
			КонецЕсли;

			ПриИзмененииРежимаПодключения(Неопределено);
			НастроитьДоступностьАутентификацииПерезапуска();
			Подключиться(Неопределено);
		Иначе
			ДобавитьВЛог(СтартапОшибка);
		КонецЕсли;

	ИначеЕсли Лев(ПараметрЗапуска, СтрДлина("autostart;")) = "autostart;" Тогда
		ВладениеПолучено = Ложь;
		Попытка
			ОстатокПараметра = Сред(ПараметрЗапуска, СтрДлина("autostart;") + 1);
			РазделительПоз = Найти(ОстатокПараметра, ";");
			Если РазделительПоз = 0 Тогда
				// Формат без timestamp — игнорируем autostart, открываем форму нормально
				Возврат;
			КонецЕсли;
			МеткаВременниСтрока = Лев(ОстатокПараметра, РазделительПоз - 1);
			ФайлПараметров = Сред(ОстатокПараметра, РазделительПоз + 1);

			РазницаСек = ТекущаяДата() - Дата(МеткаВременниСтрока);
			Если РазницаСек > 300 Тогда
				// Повторное открытие обработки в той же сессии — игнорируем autostart
				Возврат;
			КонецЕсли;

			ЧТ = Новый ЧтениеТекста(ФайлПараметров, КодировкаТекста.UTF8);
			Попытка
				ТекстJSON = ЧТ.Прочитать();
			Исключение
				Попытка ЧТ.Закрыть(); Исключение КонецПопытки;
				ВызватьИсключение;
			КонецПопытки;
			ЧТ.Закрыть();
			ПараметрыАвтостарта = ПрочитатьJSON2(ТекстJSON);
			УдалитьФайлы(ФайлПараметров);

			ПутьФайлаСигналаАвтостарта = ПараметрыАвтостарта.signal_path;
			МаппингиДляВосстановления  = ПараметрыАвтостарта.mappings_path;
			ПутьКлеймаАвтостарта       = ПараметрыАвтостарта.claimed_path;
			СессийДляВосстановления    = ПараметрыАвтостарта.mcp_sessions;

			НонсФайл    = Новый Файл(ПараметрыАвтостарта.nonce_path);
			НонсВалиден = НонсФайл.Существует();

			Если НонсВалиден Тогда
				КлеймЗаписан = Истина;
				Если ПараметрыАвтостарта.claimed_path <> "" Тогда
					Попытка
						ЗТ = Новый ЗаписьТекста(ПараметрыАвтостарта.claimed_path, КодировкаТекста.UTF8);
						ЗТ.ЗаписатьСтроку("1");
						ЗТ.Закрыть();
					Исключение
						КлеймЗаписан = Ложь;
					КонецПопытки;
				КонецЕсли;

				Если НЕ КлеймЗаписан Тогда
					Попытка
						ЗТ = Новый ЗаписьТекста(ПараметрыАвтостарта.signal_path, КодировкаТекста.UTF8);
						ЗТ.ЗаписатьСтроку("failed:Cannot write claimed marker — ownership protocol broken");
						ЗТ.Закрыть();
					Исключение
					КонецПопытки;
					Если МаппингиДляВосстановления <> "" Тогда
						Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
					КонецЕсли;
					Попытка УдалитьФайлы(ПараметрыАвтостарта.nonce_path); Исключение КонецПопытки;
					Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
					ПутьФайлаСигналаАвтостарта = "";
					МаппингиДляВосстановления  = "";
					ПутьКлеймаАвтостарта       = "";
					СессийДляВосстановления    = Неопределено;
					ЗавершитьРаботуСистемы(Ложь);
					Возврат;
				Иначе
					НФАвтостарт = Новый Файл(ПараметрыАвтостарта.nonce_path);
					НонсЕщеВалиден = НФАвтостарт.Существует();
					Если НЕ НонсЕщеВалиден Тогда
						Попытка УдалитьФайлы(ПараметрыАвтостарта.claimed_path); Исключение КонецПопытки;
						Попытка УдалитьФайлы(ПараметрыАвтостарта.mappings_path); Исключение КонецПопытки;
						ПутьФайлаСигналаАвтостарта = "";
						МаппингиДляВосстановления  = "";
						ПутьКлеймаАвтостарта       = "";
						СессийДляВосстановления    = Неопределено;
						ЗавершитьРаботуСистемы(Ложь);
						Возврат;
					КонецЕсли;
					Попытка УдалитьФайлы(ПараметрыАвтостарта.nonce_path); Исключение КонецПопытки;
					ВладениеПолучено = Истина;

					// Восстанавливаем пароль ПОСЛЕ double-check nonce
					Если ЗначениеЗаполнено(ПараметрыАвтостарта.restart_password) Тогда
						Попытка
							ПарольПерезапуска = РасшифроватьПарольПерезапуска(ПараметрыАвтостарта.restart_password);
						Исключение
							Попытка
								ОшибкаСтрока = СтрЗаменить(ОписаниеОшибки(), Символы.ВК, " ");
								ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.ПС, " ");
								ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.Таб, " ");
								ЗТ = Новый ЗаписьТекста(ПараметрыАвтостарта.signal_path, КодировкаТекста.UTF8);
								ЗТ.ЗаписатьСтроку("failed:Password decrypt error: " + ОшибкаСтрока);
								ЗТ.Закрыть();
							Исключение
							КонецПопытки;
							Если МаппингиДляВосстановления <> Неопределено И МаппингиДляВосстановления <> "" Тогда
								Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
							КонецЕсли;
							Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
							ПутьФайлаСигналаАвтостарта = "";
							МаппингиДляВосстановления  = "";
							ПутьКлеймаАвтостарта       = "";
							СессийДляВосстановления    = Неопределено;
							ЗавершитьРаботуСистемы(Ложь);
							Возврат;
						КонецПопытки;
					КонецЕсли;

					// Запускаем сервер/прокси
					Если РежимПодключения = 1 Тогда
						ЗапуститьВстроенныйСервер();
					Иначе
						Подключиться(Неопределено);
					КонецЕсли;
				КонецЕсли;
			Иначе
				// Nonce недействителен — таймаут старой сессии. Закрываем новую autostart-сессию.
				ПутьФайлаСигналаАвтостарта = "";
				Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
				ПутьКлеймаАвтостарта       = "";
				СессийДляВосстановления    = Неопределено;
				Если МаппингиДляВосстановления <> "" Тогда
					Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
					МаппингиДляВосстановления = "";
				КонецЕсли;
				ЗавершитьРаботуСистемы(Ложь);
				Возврат;
			КонецЕсли;
		Исключение
			Если ВладениеПолучено Тогда
				Попытка
					ОшибкаСтрока = СтрЗаменить(ОписаниеОшибки(), Символы.ВК, " ");
					ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.ПС, " ");
					ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.Таб, " ");
					Если ПутьФайлаСигналаАвтостарта <> "" Тогда
						ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
						ЗТ.ЗаписатьСтроку("failed:" + ОшибкаСтрока);
						ЗТ.Закрыть();
					КонецЕсли;
				Исключение КонецПопытки;
				Если МаппингиДляВосстановления <> "" Тогда
					Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
				КонецЕсли;
				Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
				ПутьФайлаСигналаАвтостарта = "";
				МаппингиДляВосстановления  = "";
				ПутьКлеймаАвтостарта       = "";
				СессийДляВосстановления    = Неопределено;
				ЗавершитьРаботуСистемы(Ложь);
				Возврат;
			Иначе
				ЕстьСигнал = ПутьФайлаСигналаАвтостарта <> "";

				Если ЕстьСигнал Тогда
					Попытка
						ОшибкаСтрока = СтрЗаменить(ОписаниеОшибки(), Символы.ВК, " ");
						ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.ПС, " ");
						ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.Таб, " ");
						ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
						ЗТ.ЗаписатьСтроку("failed:" + ОшибкаСтрока);
						ЗТ.Закрыть();
					Исключение КонецПопытки;
				КонецЕсли;

				Если МаппингиДляВосстановления <> "" Тогда
					Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
				КонецЕсли;

				ПутьФайлаСигналаАвтостарта = "";
				МаппингиДляВосстановления  = "";
				ПутьКлеймаАвтостарта       = "";
				СессийДляВосстановления    = Неопределено;

				Если ЕстьСигнал Тогда
					Попытка УдалитьФайлы(ПараметрыАвтостарта.nonce_path); Исключение КонецПопытки;
					Попытка УдалитьФайлы(ПараметрыАвтостарта.claimed_path); Исключение КонецПопытки;
				КонецЕсли;

				ЗавершитьРаботуСистемы(Ложь);
				Возврат;
			КонецЕсли;
		КонецПопытки;
	ИначеЕсли Лев(ПараметрЗапуска, СтрДлина("coldstart;")) = "coldstart;" Тогда
		ОстатокПараметра = Сред(ПараметрЗапуска, СтрДлина("coldstart;") + 1);
		РазделительПоз = Найти(ОстатокПараметра, ";");
		Если РазделительПоз > 0 Тогда
			МеткаВремениСтрока = Лев(ОстатокПараметра, РазделительПоз - 1);
			ФайлПараметров = Сред(ОстатокПараметра, РазделительПоз + 1);

			МеткаВалидна = Ложь;
			Попытка
				РазницаСек = ТекущаяДата() - Дата(МеткаВремениСтрока);
				МеткаВалидна = Истина;
			Исключение
			КонецПопытки;

			Если МеткаВалидна И РазницаСек <= 300 Тогда
				Попытка
					ЧТ = Новый ЧтениеТекста(ФайлПараметров, КодировкаТекста.UTF8);
					Попытка
						ТекстJSON = ЧТ.Прочитать();
					Исключение
						Попытка ЧТ.Закрыть(); Исключение КонецПопытки;
						ВызватьИсключение;
					КонецПопытки;
					ЧТ.Закрыть();
					ПараметрыАвтостарта = ПрочитатьJSON2(ТекстJSON);

					// signal_path присваиваем ПЕРВЫМ — catch-блок может записать failed-сигнал
					// только если ПутьФайлаСигналаХолодногоСтарта уже установлен
					Если ПараметрыАвтостарта.Свойство("signal_path") Тогда
						ПутьФайлаСигналаХолодногоСтарта = ПараметрыАвтостарта.signal_path;
					КонецЕсли;

					УдалитьФайлы(ФайлПараметров);

					СессийДляВосстановления = ПараметрыАвтостарта.mcp_sessions;
					Если ПараметрыАвтостарта.Свойство("mappings_path") Тогда
						МаппингиДляВосстановления = ПараметрыАвтостарта.mappings_path;
					КонецЕсли;

					Если ЗначениеЗаполнено(ПараметрыАвтостарта.restart_password) Тогда
						ПарольПерезапуска = РасшифроватьПарольПерезапуска(ПараметрыАвтостарта.restart_password);
					КонецЕсли;

					Если РежимПодключения = 1 Тогда
						ЗапуститьВстроенныйСервер();
						// Сигнал пишется позже в ЗавершитьАвтостартПослеЗапускаВстроенногоСервера
					Иначе
						Подключиться(Неопределено);
						// Сигнал пишется в ЦиклОпроса на первом успешном poll
					КонецЕсли;
				Исключение
					ТекстОш = ОписаниеОшибки();
					ДобавитьВЛог("Cold start error: " + ТекстОш);
					Если ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
						Попытка
							ТекстОшСанит = СтрЗаменить(СтрЗаменить(ТекстОш, Символы.ВК, " "), Символы.ПС, " ");
							ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаХолодногоСтарта, КодировкаТекста.UTF8);
							ЗТ.ЗаписатьСтроку("failed:Cold start error: " + ТекстОшСанит);
							ЗТ.Закрыть();
						Исключение КонецПопытки;
						ПутьФайлаСигналаХолодногоСтарта = "";
					КонецЕсли;
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	// AUTO-START MCP SERVER ON FORM OPEN (emulate connect button)
	РазрешатьОпасныеОперацииСПодтверждением = Истина;
	АвтоРазрешитьЗаписать = Истина;
	АвтоРазрешитьПривилегированныйРежим = Истина;
	Подключиться(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(Отказ)

	ЗапускСервераВПроцессе = Ложь; // колбэки async-цепочки получат Ложь и выйдут без ошибок

	// Отключение при закрытии формы
	Если ФлагПодключения Тогда
		ОтключитьсяОтСервера();
	КонецЕсли;

	// Остановка встроенного сервера
	Если ВстроенныйСерверЗапущен Тогда
		ОстановитьВстроенныйСервер();
	КонецЕсли;

КонецПроцедуры


// ─────────────────────────────────────────────────────────────────────────────────
// КОМАНДЫ ФОРМЫ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура Подключиться(Команда)

	Если РежимПодключения = 1 Тогда
		// Режим встроенного сервера — сначала отключиться от прокси
		Если ФлагПодключения Тогда
			ОтключитьсяОтСервера();
		КонецЕсли;
		ЗапуститьВстроенныйСервер();
		Возврат;
	КонецЕсли;

	// Режим прокси — сначала остановить встроенный сервер
	Если ВстроенныйСерверЗапущен Тогда
		ОстановитьВстроенныйСервер();
	КонецЕсли;

	Если ПустаяСтрока(АдресСервера) Тогда
		ПоказатьПредупреждение(, "Укажите адрес сервера");
		Возврат;
	КонецЕсли;

	// Сохранение адреса сервера
	СохранитьНастройкиНаСервере(АдресСервера);

	// Инициализация переменных если не были инициализированы
	Если ИнтервалОпроса = Неопределено Тогда
		ИнтервалОпроса = 1;
	КонецЕсли;

	// Запуск подключения
	ФлагПодключения = Истина;
	ТекущееКоличествоПопытокПереподключения = 0;

	УстановитьСтатус("Подключение...", Ложь);
	ДобавитьВЛог("Подключение к серверу: " + АдресСервера);

	// Подключение SyntaxHelpReader
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияМакетаSyntaxHelpProxy", ЭтаФорма),
			"ВнешняяОбработка.MCPToolkit.Макет.SyntaxHelpReader", "SyntaxH");
	Иначе
		ПодключитьКомпонентуSyntaxHelpСинхронно();
	КонецЕсли;

	// Подключение ScreenCapture
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияМакетаScreenCaptureProxy", ЭтаФорма),
			"ВнешняяОбработка.MCPToolkit.Макет.ScreenCapture", "ScreenCapture");
	Иначе
		ПодключитьКомпонентуScreenCaptureСинхронно();
	КонецЕсли;

	// Запуск цикла опроса
	ПодключитьОбработчикОжидания("ЦиклОпроса", 1, Ложь);

КонецПроцедуры

&НаКлиенте
Процедура Отключиться(Команда)

	Если РежимПодключения = 1 Тогда
		ОстановитьВстроенныйСервер();
		Возврат;
	КонецЕсли;

	ОтключитьсяОтСервера();

КонецПроцедуры

&НаКлиенте
Процедура ОчиститьЖурнал(Команда)
	
	Лог = "";
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастройкиТочнойАнонимизации(Команда)

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПослеЗакрытияНастроекТочнойАнонимизации",
		ЭтаФорма);

	ПарамФормы = Новый Структура(
		"ИспользоватьАсинхронноеПодключениеКомпонент, РежимПодключения",
		ИспользоватьАсинхронноеПодключениеКомпонент,
		РежимПодключения);

	ОткрытьФорму(
		"ВнешняяОбработка.MCPToolkit.Форма.ФормаНастройкиТочнойАнонимизации",
		ПарамФормы,
		ЭтаФорма,
		,
		,
		,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);

КонецПроцедуры

&НаКлиенте
Процедура ПослеЗакрытияНастроекТочнойАнонимизации(НастройкиСохранены, ДополнительныеДанные) Экспорт

	Если НастройкиСохранены <> Истина Тогда
		Возврат;
	КонецЕсли;

	КэшRegexПравил = Неопределено; // инвалидировать кэш при изменении настроек

	ОбновитьСводкуПравилТочнойАнонимизацииНаСервере();

	НастройкиАнон = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	ПодтверждениеАнонимизации = НастройкиАнон.ПодтверждениеАнонимизации;

	Если ВстроенныйСерверЗапущен И ИспользоватьТочнуюАнонимизацию Тогда
		ПостроитьИЗаполнитьСловарьАнонимизации();
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОтключитьсяОтСервера()
	
	ФлагПодключения = Ложь;
	
	Попытка
		ОтключитьОбработчикОжидания("ЦиклОпроса");
	Исключение
		// Обработчик уже отключен
	КонецПопытки;
	
	УстановитьСтатус("Отключено", Ложь);
	ДобавитьВЛог("Отключено от сервера");
	
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ЦИКЛ ОПРОСА (LONG POLLING)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ЦиклОпроса()
	
	Если Не ФлагПодключения Тогда
		Возврат;
	КонецЕсли;
	
	// Получение команды с сервера
	РезультатЗапроса = ПолучитьКомандуССервера(АдресСервера);
	
	Если РезультатЗапроса.Успех Тогда

		// Сброс счётчика попыток при успешном подключении
		ТекущееКоличествоПопытокПереподключения = 0;

		Если Не Подключено Тогда
			УстановитьСтатус("Подключено", Истина);
			ДобавитьВЛог("Успешное подключение к серверу");
		КонецЕсли;

		// Первый успешный poll после autostart — записываем сигнал "ready"
		Если ПутьФайлаСигналаАвтостарта <> Неопределено И ПутьФайлаСигналаАвтостарта <> "" Тогда
			Попытка
				ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
				ЗТ.ЗаписатьСтроку("ready");
				ЗТ.Закрыть();
				ПутьФайлаСигналаАвтостарта = "";
			Исключение
				ПутьФайлаСигналаАвтостарта = "";
				Попытка ОтключитьсяОтСервера(); Исключение КонецПопытки;
				ЗавершитьРаботуСистемы(Ложь);
			КонецПопытки;
		КонецЕсли;

		// Первый успешный poll после coldstart (proxy) — записываем сигнал "ready"
		Если ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
			Попытка
				ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаХолодногоСтарта, КодировкаТекста.UTF8);
				ЗТ.ЗаписатьСтроку("ready");
				ЗТ.Закрыть();
			Исключение КонецПопытки;
			ПутьФайлаСигналаХолодногоСтарта = "";
		КонецЕсли;

		Если РезультатЗапроса.ЕстьКоманда Тогда
			// Выполнение команды
			ОбработатьКоманду(РезультатЗапроса.Команда);
		КонецЕсли;

	Иначе
		// Ошибка подключения
		ОбработатьОшибкуПодключения(РезультатЗапроса.ОписаниеОшибки);
	КонецЕсли;
	
КонецПроцедуры


&НаКлиенте
Процедура ОбработатьОшибкуПодключения(ОписаниеОшибки)
	
	// Инициализация если не были инициализированы
	Если ТекущееКоличествоПопытокПереподключения = Неопределено Тогда
		ТекущееКоличествоПопытокПереподключения = 0;
	КонецЕсли;
	Если МаксимальноеКоличествоПопытокПереподключения = Неопределено Тогда
		МаксимальноеКоличествоПопытокПереподключения = 5;
	КонецЕсли;
	
	ТекущееКоличествоПопытокПереподключения = ТекущееКоличествоПопытокПереподключения + 1;
	
	Если ТекущееКоличествоПопытокПереподключения >= МаксимальноеКоличествоПопытокПереподключения Тогда
		// Превышено количество попыток переподключения
		УстановитьСтатус("Ошибка: " + ОписаниеОшибки, Ложь);
		ДобавитьВЛог("Ошибка подключения: " + ОписаниеОшибки + ". Превышено количество попыток.");
		// В coldstart-режиме — failed: и закрыть новую сессию
		Если ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
			Попытка
				ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаХолодногоСтарта, КодировкаТекста.UTF8);
				ЗТ.ЗаписатьСтроку("failed:proxy connection failed");
				ЗТ.Закрыть();
			Исключение КонецПопытки;
			ПутьФайлаСигналаХолодногоСтарта = "";
			ЗавершитьРаботуСистемы(Ложь);
			Возврат;
		КонецЕсли;
		// В autostart-режиме — failed: и закрыть новую сессию
		Если ПутьФайлаСигналаАвтостарта <> Неопределено И ПутьФайлаСигналаАвтостарта <> "" Тогда
			Попытка
				ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
				ЗТ.ЗаписатьСтроку("failed:Proxy connection failed — new session could not connect");
				ЗТ.Закрыть();
			Исключение
			КонецПопытки;
			ПутьФайлаСигналаАвтостарта = "";
			Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
			ПутьКлеймаАвтостарта = "";
			ЗавершитьРаботуСистемы(Ложь);
			Возврат;
		КонецЕсли;
		ОтключитьсяОтСервера();
	Иначе
		// Попытка переподключения
		УстановитьСтатус("Переподключение (" + ТекущееКоличествоПопытокПереподключения + "/" + МаксимальноеКоличествоПопытокПереподключения + ")...", Ложь);
		ДобавитьВЛог("Ошибка подключения: " + ОписаниеОшибки + ". Попытка переподключения...");
	КонецЕсли;
	
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// КЭШ ДЛЯ МЕТАДАННЫХ РАСШИРЕНИЙ (клиент хранит токены, сервер хранит данные во ВременномХранилище)
// Внешний API не меняется: токены не уходят наружу (в ОтправитьРезультатНаСервер есть whitelist полей).
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция ПолучитьКэшТокеновМетаданныхРасширений()
	Если КэшТокеновМетаданныхРасширений = Неопределено Тогда
		КэшТокеновМетаданныхРасширений = Новый Соответствие;
	КонецЕсли;
	Возврат КэшТокеновМетаданныхРасширений;
КонецФункции

&НаКлиенте
Функция СформироватьКлючКэшаДеталейРасширения(Фильтр, Секции)
	Ключ = ?(Фильтр = Неопределено, "", Строка(Фильтр));
	
	Если Секции = Неопределено Тогда
		Возврат Ключ;
	КонецЕсли;
	
	Попытка
		Если ТипЗнч(Секции) = Тип("Массив") Тогда
			СтрСекции = "";
			Для Каждого Секция Из Секции Цикл
				Секция = СокрЛП(Строка(Секция));
				Если ПустаяСтрока(Секция) Тогда
					Продолжить;
				КонецЕсли;
				СтрСекции = СтрСекции + ?(ПустаяСтрока(СтрСекции), "", ",") + Секция;
			КонецЦикла;
			Если НЕ ПустаяСтрока(СтрСекции) Тогда
				Ключ = Ключ + "|" + СтрСекции;
			КонецЕсли;
		Иначе
			Ключ = Ключ + "|" + Строка(Секции);
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Возврат Ключ;
КонецФункции

&НаКлиенте
Функция ВыполнитьКомандуСУчетомКонтекста(Команда)

	Если Команда.tool = "execute_code" Тогда
		КонтекстВыполнения = "server";
		Если Команда.Свойство("params") И Команда.params <> Неопределено
			И Команда.params.Свойство("execution_context") Тогда
			КонтекстВыполнения = Команда.params.execution_context;
		КонецЕсли;
		Если КонтекстВыполнения = "client" Тогда
			Код = "";
			Если Команда.params.Свойство("code") Тогда Код = Команда.params.code; КонецЕсли;
			РезультатКлиент = ВыполнитьКодНаКлиенте(Код);
			Если ИспользоватьТочнуюАнонимизацию И РежимПодключения <> 0 Тогда
				Если ТипЗнч(РезультатКлиент) = Тип("Структура") И РезультатКлиент.Свойство("data") Тогда
					РезультатКлиент.Вставить("_original_data_json",        ЗаписатьJSON2(РезультатКлиент.data));
					РезультатКлиент.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				РегексКонтекст = Новый Структура(
					"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
					РезультатКлиент, "", "", Неопределено, Ложь, Истина);
				АнонимизироватьExecuteCodeВстроенный(РезультатКлиент, РегексКонтекст);
				Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
					Если КомпонентаRegex = Неопределено Тогда
						ПодключитьКомпонентуRegexСинхронно();
						// НЕ выставляем КомпонентаRegexНедоступна — client-path не должен
						// блокировать async-режим на весь сеанс при одном неудачном sync-load
					КонецЕсли;
					Если КомпонентаRegex <> Неопределено Тогда
						СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
							РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
						ПрименитьRegexТокеныExecuteCode(РегексКонтекст, СовпаденияJSON);
					КонецЕсли;
				КонецЕсли;
				ОбновитьТаблицуМаппинговВстроенный();
			КонецЕсли;
			Возврат РезультатКлиент;
		КонецЕсли;
	КонецЕсли;

	// submit_for_deanonymization выполняется на клиенте (доступ к форменным атрибутам)
	Если Команда.tool = "submit_for_deanonymization" Тогда
		// Защита: инструмент недоступен при выключенной анонимизации даже при прямом вызове
		Если НЕ ИспользоватьТочнуюАнонимизацию Тогда
			Возврат Новый Структура("success, error", Ложь, "Tool is not available: anonymization is disabled");
		КонецЕсли;
		Текст = Неопределено;
		Если Команда.Свойство("params") И Команда.params <> Неопределено Тогда
			Команда.params.Свойство("text", Текст);
		КонецЕсли;
		// Валидация: "text" обязателен (схема required: ["text"]), но built-in tools/call
		// не валидирует аргументы — проверяем тип здесь. Пустая строка допустима.
		// Возвращаем обычную структуру success/error, а не СформироватьToolsCallОшибку —
		// та делает готовую MCP-обёртку, которая будет двойно упакована на уровне выше.
		Если ТипЗнч(Текст) <> Тип("Строка") Тогда
			Возврат Новый Структура("success, error", Ложь, "Missing required parameter: text");
		КонецЕсли;
		Возврат ВыполнитьSubmitForDeanonymization(Текст);
	КонецЕсли;

	// get_bsl_syntax_help выполняется на клиенте (доступ к AddIn SyntaxHelpReader)
	Если Команда.tool = "get_bsl_syntax_help" Тогда
		ПараметрыBSL = Неопределено;
		Если Команда.Свойство("params") Тогда ПараметрыBSL = Команда.params; КонецЕсли;
		Возврат ПолучитьСправкуПоBSL(ПараметрыBSL);
	КонецЕсли;

	// get_screenshot выполняется на клиенте (доступ к AddIn ScreenCapture и ОткрытьФорму)
	Если Команда.tool = "get_screenshot" Тогда
		ПараметрыSnap = Неопределено;
		Если Команда.Свойство("params") Тогда ПараметрыSnap = Команда.params; КонецЕсли;
		Возврат ПолучитьСкриншот(ПараметрыSnap);
	КонецЕсли;

	Возврат ВыполнитьКомандуНаСервереСКэшированием(Команда);

КонецФункции

&НаКлиенте
Функция ВыполнитьКомандуНаСервереСКэшированием(Команда)
	ИмяИнструмента = "";
	Попытка
		ИмяИнструмента = СокрЛП(Команда.tool);
	Исключение
		ИмяИнструмента = "";
	КонецПопытки;
	
	// Важно: Команда может быть фиксированной структурой (read-only), поэтому НЕ модифицируем ее.
	// Всегда работаем с копией params в обычной Структуре.
	ПараметрыИсходные = Неопределено;
	Если Команда.Свойство("params") Тогда
		ПараметрыИсходные = Команда.params;
	КонецЕсли;
	
	// ВАЖНО: имя "Параметры" в модуле формы может конфликтовать с одноименным
	// системным/форменным свойством (read-only). Используем уникальное имя.
	ПараметрыКоманды = Новый Структура;
	Если ПараметрыИсходные <> Неопределено Тогда
		Попытка
			Для Каждого Пара Из ПараметрыИсходные Цикл
				ПараметрыКоманды.Вставить(Пара.Ключ, Пара.Значение);
			КонецЦикла;
		Исключение
			// Если params не коллекция/не итерируется - оставляем пустыми
		КонецПопытки;
	КонецЕсли;
	
	ИмяРасширения = Неопределено;
	Фильтр = "";
	Секции = Неопределено;
	
	Если ИмяИнструмента = "get_metadata" И ПараметрыКоманды <> Неопределено Тогда
		Если ПараметрыКоманды.Свойство("extension_name") Тогда
			ИмяРасширения = ПараметрыКоманды.extension_name;
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("filter") Тогда
			Фильтр = ПараметрыКоманды.filter;
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("sections") Тогда
			Секции = ПараметрыКоманды.sections;
		КонецЕсли;
	КонецЕсли;
	
	КлючДеталей = "";
	
	// Добавляем токены кэша только для режима "объекты расширения" (extension_name="Имя")
	Если ИмяИнструмента = "get_metadata"
		И ИмяРасширения <> Неопределено
		И ТипЗнч(ИмяРасширения) = Тип("Строка")
		И НЕ ПустаяСтрока(ИмяРасширения) Тогда
		
		Кэш = ПолучитьКэшТокеновМетаданныхРасширений();
		Запись = Кэш.Получить(ИмяРасширения);
		
		Если Запись = Неопределено Тогда
			Запись = Новый Структура(
				"hash, data_token, index_token, details, details_order",
				"",
				Неопределено,
				Неопределено,
				Новый Соответствие,
				Новый Массив
			);
			Кэш.Вставить(ИмяРасширения, Запись);
		КонецЕсли;
		
		Если Запись.hash <> "" Тогда
			ПараметрыКоманды.Вставить("mcp_cache_hash", Запись.hash);
		КонецЕсли;
		Если Запись.data_token <> Неопределено Тогда
			ПараметрыКоманды.Вставить("mcp_cache_data_token", Запись.data_token);
		КонецЕсли;
		Если Запись.index_token <> Неопределено Тогда
			ПараметрыКоманды.Вставить("mcp_cache_index_token", Запись.index_token);
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(Фильтр) Тогда
			КлючДеталей = СформироватьКлючКэшаДеталейРасширения(Фильтр, Секции);
			ТокенДеталей = Запись.details.Получить(КлючДеталей);
			Если ТокенДеталей <> Неопределено Тогда
				ПараметрыКоманды.Вставить("mcp_cache_details_token", ТокенДеталей);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// Передаем на сервер копию команды с измененными params (если добавили токены кэша)
	КомандаНаСервер = Новый Структура;
	Попытка
		Если Команда.Свойство("id") Тогда
			КомандаНаСервер.Вставить("id", Команда.id);
		КонецЕсли;
	Исключение
	КонецПопытки;
	АнонСхемаПринудительно = Ложь;
	Если РежимПодключения <> 0 И ИспользоватьТочнуюАнонимизацию
		И ИмяИнструмента = "execute_query" Тогда
		АнонАгентЗапросилСхему = ПараметрыКоманды.Свойство("include_schema")
			И ПараметрыКоманды.include_schema = Истина;
		Если НЕ АнонАгентЗапросилСхему Тогда
			ПараметрыКоманды.Вставить("include_schema", Истина);
			АнонСхемаПринудительно = Истина;
		КонецЕсли;
	КонецЕсли;
	КомандаНаСервер.Вставить("tool", ИмяИнструмента);
	КомандаНаСервер.Вставить("params", ПараметрыКоманды);

	Результат = ВыполнитьКомандуНаСервере(КомандаНаСервер);
	Если АнонСхемаПринудительно И Результат <> Неопределено
		И ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("success") И Результат.success
		И Результат.Свойство("schema") Тогда
		Результат.Вставить("_anon_schema_forced", Истина);
	КонецЕсли;
	Если НЕ ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		ТекстЗапросаLineage = "";
		Если ПараметрыКоманды.Свойство("query") Тогда
			ТекстЗапросаLineage = ПараметрыКоманды.query;
		КонецЕсли;
		Если НуженLineageДляРезультата(ИмяИнструмента, ТекстЗапросаLineage, Результат) Тогда
			Результат = ОбогатитьСхемуИсточниками(ТекстЗапросаLineage, Результат);
		КонецЕсли;
	КонецЕсли;
	Если НЕ ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		Если ИмяИнструмента = "execute_query" И ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0
				И ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			Результат = ПрименитьТочнуюPolicyКРезультатуExecuteQuery(Результат, РежимПодключения = 0);
			Если РежимПодключения <> 0 Тогда
				РегексКонтекст = Новый Структура;
				РегексКонтекст.Вставить("Результат",            Результат);
				РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
				РегексКонтекст.Вставить("ТекстыJSON",            "");
				РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
				РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
				АнонимизироватьРезультатВстроенный(Результат, РегексКонтекст);
				// Lazy-load и inline regex-фаза
				Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
					Если КомпонентаRegex = Неопределено Тогда
						ПодключитьКомпонентуRegexСинхронно();
						Если КомпонентаRegex = Неопределено Тогда
							КомпонентаRegexНедоступна = Истина;
						КонецЕсли;
					КонецЕсли;
					Если КомпонентаRegex <> Неопределено Тогда
						СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
							РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
						ПрименитьRegexТокены(РегексКонтекст, СовпаденияJSON);
					КонецЕсли;
				КонецЕсли;
				ОбновитьТаблицуМаппинговВстроенный(); // ПОСЛЕ regex-токенов
			КонецЕсли;
		ИначеЕсли ИмяИнструмента = "get_object_by_link" И ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0 Тогда
				Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
					Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
					Результат.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				Результат = ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат);
				РегексКонтекст = Новый Структура;
				РегексКонтекст.Вставить("Результат",            Результат);
				РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
				РегексКонтекст.Вставить("ТекстыJSON",            "");
				РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
				РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
				РегексКонтекст.Вставить("ЭтоОбъект",             Истина);
				АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст);
				Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
					Если КомпонентаRegex = Неопределено Тогда
						ПодключитьКомпонентуRegexСинхронно();
						Если КомпонентаRegex = Неопределено Тогда
							КомпонентаRegexНедоступна = Истина;
						КонецЕсли;
					КонецЕсли;
					Если КомпонентаRegex <> Неопределено Тогда
						СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
							РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
						ПрименитьRegexТокеныОбъект(РегексКонтекст, СовпаденияJSON);
					КонецЕсли;
				КонецЕсли;
				ОбновитьТаблицуМаппинговВстроенный();
				Попытка Результат.Удалить("schema");               Исключение КонецПопытки;
				Попытка Результат.Удалить("_anon_schema_forced");  Исключение КонецПопытки;
				// _original_data_json НЕ удаляем — ПолучитьТекстРезультата()
				// берёт его для журнала событий формы (отображаются НЕ анонимизированные данные).
				// Наружу (к AI) он не уйдёт — отфильтруется whitelist в финальном ответе.
				Попытка Результат.Удалить("_blind_anonymization"); Исключение КонецПопытки;
			КонецЕсли;
		ИначеЕсли ИмяИнструмента = "find_references_to_object" И ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0 Тогда
				Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
					Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
					Результат.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				РегексКонтекст = Новый Структура;
				РегексКонтекст.Вставить("Результат",            Результат);
				РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
				РегексКонтекст.Вставить("ТекстыJSON",            "");
				РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
				РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
				РегексКонтекст.Вставить("ЭтоFindReferences",     Истина);
				АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст);
				Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
					Если КомпонентаRegex = Неопределено Тогда
						ПодключитьКомпонентуRegexСинхронно();
						Если КомпонентаRegex = Неопределено Тогда
							КомпонентаRegexНедоступна = Истина;
						КонецЕсли;
					КонецЕсли;
					Если КомпонентаRegex <> Неопределено Тогда
						СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
							РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
						ПрименитьRegexТокеныFindReferences(РегексКонтекст, СовпаденияJSON);
					КонецЕсли;
				КонецЕсли;
				ОбновитьТаблицуМаппинговВстроенный();
				// _original_data_json НЕ удаляем - ПолучитьТекстРезультата()
				// берёт его для журнала событий формы (отображаются НЕ анонимизированные данные).
				// Наружу (к AI) он не уйдёт — отфильтруется whitelist в финальном ответе.
			КонецЕсли;
		ИначеЕсли ИмяИнструмента = "get_event_log" И ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0 Тогда
				Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
					Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
					Результат.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				РегексКонтекст = Новый Структура;
				РегексКонтекст.Вставить("Результат",            Результат);
				РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
				РегексКонтекст.Вставить("ТекстыJSON",            "");
				РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
				РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
				РегексКонтекст.Вставить("ЭтоGetEventLog",        Истина);
				АнонимизироватьРезультатGetEventLogВстроенный(Результат, РегексКонтекст);
				Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
					Если КомпонентаRegex = Неопределено Тогда
						ПодключитьКомпонентуRegexСинхронно();
						Если КомпонентаRegex = Неопределено Тогда
							КомпонентаRegexНедоступна = Истина;
						КонецЕсли;
					КонецЕсли;
					Если КомпонентаRegex <> Неопределено Тогда
						СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
							РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
						ПрименитьRegexТокеныGetEventLog(РегексКонтекст, СовпаденияJSON);
					КонецЕсли;
				КонецЕсли;
				ОбновитьТаблицуМаппинговВстроенный();
			КонецЕсли;
		ИначеЕсли ИмяИнструмента = "get_access_rights" И ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0 Тогда
				Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
					Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
					Результат.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				АнонимизироватьРезультатGetAccessRights(Результат);
				ОбновитьТаблицуМаппинговВстроенный();
				// _original_data_json НЕ удаляем — ПолучитьТекстРезультата()
				// берёт его для журнала событий формы (отображаются НЕ анонимизированные данные).
				// Наружу (к AI) он не уйдёт — отфильтруется whitelist в финальном ответе.
			КонецЕсли;
		ИначеЕсли ИмяИнструмента = "execute_code" И ИспользоватьТочнуюАнонимизацию И РежимПодключения <> 0 Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			РегексКонтекст = Новый Структура(
				"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст);
			Если КомпонентаRegexНедоступна <> Истина И РегексКонтекст.ЕстьТекстыДляRegex Тогда
				Если КомпонентаRegex = Неопределено Тогда
					ПодключитьКомпонентуRegexСинхронно();
					Если КомпонентаRegex = Неопределено Тогда КомпонентаRegexНедоступна = Истина; КонецЕсли;
				КонецЕсли;
				Если КомпонентаRegex <> Неопределено Тогда
					СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(
						РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
					ПрименитьRegexТокеныExecuteCode(РегексКонтекст, СовпаденияJSON);
				КонецЕсли;
			КонецЕсли;
			ОбновитьТаблицуМаппинговВстроенный();
		КонецЕсли;
	КонецЕсли;
	Если АнонСхемаПринудительно И НЕ ИспользоватьАсинхронноеПодключениеКомпонент
		И Результат <> Неопределено И ТипЗнч(Результат) = Тип("Структура") Тогда
		Попытка Результат.Удалить("schema"); Исключение КонецПопытки;
		Попытка Результат.Удалить("_anon_schema_forced"); Исключение КонецПопытки;
	КонецЕсли;

	// Обновляем кэш по внутренним полям результата (они не уходят наружу из-за whitelist)
	Если ИмяИнструмента = "get_metadata"
		И ИмяРасширения <> Неопределено
		И ТипЗнч(ИмяРасширения) = Тип("Строка")
		И НЕ ПустаяСтрока(ИмяРасширения) Тогда
		
		Попытка
			Если Результат <> Неопределено
				И ТипЗнч(Результат) = Тип("Структура") Тогда
				
				Кэш = ПолучитьКэшТокеновМетаданныхРасширений();
				Запись = Кэш.Получить(ИмяРасширения);
				Если Запись = Неопределено Тогда
					Запись = Новый Структура(
						"hash, data_token, index_token, details, details_order",
						"",
						Неопределено,
						Неопределено,
						Новый Соответствие,
						Новый Массив
					);
				КонецЕсли;
				
				НовХеш = "";
				Если Результат.Свойство("mcp_cache_hash") Тогда
					НовХеш = Строка(Результат.mcp_cache_hash);
				КонецЕсли;
				
				Если НЕ ПустаяСтрока(НовХеш) И Запись.hash <> "" И Запись.hash <> НовХеш Тогда
					Запись.hash = НовХеш;
					Запись.data_token = Неопределено;
					Запись.index_token = Неопределено;
					Запись.details = Новый Соответствие;
					Запись.details_order = Новый Массив;
				ИначеЕсли НЕ ПустаяСтрока(НовХеш) Тогда
					Запись.hash = НовХеш;
				КонецЕсли;
				
				Если Результат.Свойство("mcp_cache_data_token") Тогда
					Запись.data_token = Результат.mcp_cache_data_token;
				КонецЕсли;
				Если Результат.Свойство("mcp_cache_index_token") Тогда
					Запись.index_token = Результат.mcp_cache_index_token;
				КонецЕсли;
				
				Если Результат.Свойство("mcp_cache_details_token") И НЕ ПустаяСтрока(Фильтр) Тогда
					Если ПустаяСтрока(КлючДеталей) Тогда
						КлючДеталей = СформироватьКлючКэшаДеталейРасширения(Фильтр, Секции);
					КонецЕсли;
					ТокенДеталей = Результат.mcp_cache_details_token;
					Если ТокенДеталей <> Неопределено Тогда
						Запись.details.Вставить(КлючДеталей, ТокенДеталей);
						Запись.details_order.Добавить(КлючДеталей);
						
						// Ограничиваем размер кэша деталей, чтобы не раздувать память/ВременноеХранилище бесконечно
						МаксДеталей = 50;
						Если Запись.details_order.Количество() > МаксДеталей Тогда
							СтарыйКлюч = Запись.details_order[0];
							Запись.details_order.Удалить(0);
							Попытка
								Запись.details.Удалить(СтарыйКлюч);
							Исключение
							КонецПопытки;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
				
				Кэш.Вставить(ИмяРасширения, Запись);
				
				// Не светим токены в текстовых логах формы
				Если Результат.Свойство("mcp_cache_hash") Тогда Результат.Удалить("mcp_cache_hash"); КонецЕсли;
				Если Результат.Свойство("mcp_cache_data_token") Тогда Результат.Удалить("mcp_cache_data_token"); КонецЕсли;
				Если Результат.Свойство("mcp_cache_index_token") Тогда Результат.Удалить("mcp_cache_index_token"); КонецЕсли;
				Если Результат.Свойство("mcp_cache_details_token") Тогда Результат.Удалить("mcp_cache_details_token"); КонецЕсли;
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура ОбработатьКоманду(Команда)

	// restart/close обрабатываются до обычного flow — отложенный ответ
	Если Команда.tool = "restart_1c_session" Тогда
		ВыполнитьПерезапускСессии(Команда.id);
		Возврат;
	КонецЕсли;
	Если Команда.tool = "close_1c_session" Тогда
		ВыполнитьЗакрытиеСессии(Команда.id);
		Возврат;
	КонецЕсли;

	// Формируем информативное сообщение о команде
	ТекстКоманды = ПолучитьТекстКоманды(Команда);
	ДобавитьВЛог("Получена команда: " + Команда.tool + Символы.ПС + ТекстКоманды);

	Если РежимПодключения <> 0 Тогда
		ДеАнонимизироватьПараметрыКоманды(Команда);
	КонецЕсли;

	// Проверка необходимости подтверждения опасной операции
	Если Команда.Свойство("params") И Команда.params <> Неопределено Тогда
		ПараметрыКоманды = Команда.params;
		
		Если ПараметрыКоманды.Свойство("requires_approval") И ПараметрыКоманды.requires_approval = Истина Тогда
			
			// Получение массива опасных ключевых слов
			МассивОпасныхСлов = Новый Массив;
			ОпасныеСлова = "";
			Если ПараметрыКоманды.Свойство("dangerous_keywords") Тогда
				Для Каждого Слово Из ПараметрыКоманды.dangerous_keywords Цикл
					МассивОпасныхСлов.Добавить(Слово);
					ОпасныеСлова = ОпасныеСлова + ?(ПустаяСтрока(ОпасныеСлова), "", ", ") + Слово;
				КонецЦикла;
			КонецЕсли;
			
 			// Проверка автоматического разрешения
 			Если ВсеОперацииАвтоРазрешены(МассивОпасныхСлов) Тогда
 				ДобавитьВЛог("Операции автоматически разрешены: " + ОпасныеСлова);
				Попытка
					Результат = ВыполнитьКомандуСУчетомКонтекста(Команда);
				Исключение
					ТекстОшибки = ОписаниеОшибки();
					ДобавитьВЛог("Ошибка выполнения команды на сервере 1С: " + ТекстОшибки);
					Результат = Новый Структура(
						"success, error",
						Ложь,
						"Ошибка выполнения команды на сервере 1С: " + ТекстОшибки
						+ " / Error executing command in 1C server: " + ТекстОшибки
					);
				КонецПопытки;
				ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Команда);
				Если ИспользоватьАсинхронноеПодключениеКомпонент
					И НуженLineageДляРезультата(Команда.tool, ТекстЗапросаLineage, Результат) Тогда
					КонтекстLineage = Новый Структура("Режим, КомандаId, Результат, ТекстЗапроса",
						"proxy", Команда.id, Результат, ТекстЗапросаLineage);
					НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
					Возврат;
				КонецЕсли;
  				РезультатОтправки = ОтправитьРезультатНаСервер(АдресСервера, Команда.id, Результат);
 				
 				Если РезультатОтправки.Успех Тогда
 					ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
				Иначе
					ДобавитьВЛог("Ошибка отправки результата: " + РезультатОтправки.ОписаниеОшибки);
				КонецЕсли;
 				
				Возврат;
			КонецЕсли;

			// Получение кода
			Код = "";
			Если ПараметрыКоманды.Свойство("code") Тогда
				Код = ПараметрыКоманды.code;
			КонецЕсли;
			
			ДобавитьВЛог("Требуется подтверждение пользователя. Опасные слова: " + ОпасныеСлова);
			
			// Показ формы подтверждения
			ПараметрыФормы = Новый Структура;
			ПараметрыФормы.Вставить("ОпасныеСлова", ОпасныеСлова);
			ПараметрыФормы.Вставить("Код", Код);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОбработатьРезультатПодтверждения", 
				ЭтаФорма, 
				Новый Структура("Команда", Команда)
			);
			
			ОткрытьФорму(
				"ВнешняяОбработка.MCPToolkit.Форма.ФормаПодтверждения",
				ПараметрыФормы,
				ЭтаФорма,
				,
				,
				,
				ОписаниеОповещения,
				РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс
			);
			
			Возврат;
		КонецЕсли;
 	КонецЕсли;
 	
 	// Обычное выполнение без подтверждения
	Попытка
		Результат = ВыполнитьКомандуСУчетомКонтекста(Команда);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		ДобавитьВЛог("Ошибка выполнения команды на сервере 1С: " + ТекстОшибки);
		Результат = Новый Структура(
			"success, error",
			Ложь,
			"Ошибка выполнения команды на сервере 1С: " + ТекстОшибки
			+ " / Error executing command in 1C server: " + ТекстОшибки
		);
	КонецПопытки;
	ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Команда);
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И НуженLineageДляРезультата(Команда.tool, ТекстЗапросаLineage, Результат) Тогда
		КонтекстLineage = Новый Структура("Режим, КомандаId, Результат, ТекстЗапроса",
			"proxy", Команда.id, Результат, ТекстЗапросаLineage);
		НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
		Возврат;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И Команда.tool = "get_object_by_link" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json", ЗаписатьJSON2(Результат.data));
		КонецЕсли;
		Результат = ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат);
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоОбъект",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, КомандаId, Результат",
			"proxy", Команда.id, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И Команда.tool = "find_references_to_object" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json", ЗаписатьJSON2(Результат.data));
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоFindReferences",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, КомандаId, Результат",
			"proxy", Команда.id, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И Команда.tool = "get_event_log" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json", ЗаписатьJSON2(Результат.data));
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоGetEventLog",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатGetEventLogВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, КомандаId, Результат",
			"proxy", Команда.id, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И Команда.tool = "get_access_rights" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json", ЗаписатьJSON2(Результат.data));
		КонецЕсли;
		АнонимизироватьРезультатGetAccessRights(Результат);
		ОбновитьТаблицуМаппинговВстроенный();
		// _original_data_json НЕ удаляем — ПолучитьТекстРезультата()
		// берёт его для журнала событий формы (отображаются НЕ анонимизированные данные).
		// Наружу (к AI) он не уйдёт — отфильтруется whitelist в финальном ответе.
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И Команда.tool = "execute_code" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		УжеАнонимизирован = Ложь;
		Если ТипЗнч(Результат) = Тип("Структура") Тогда
			ВремАнон = Неопределено;
			УжеАнонимизирован = Результат.Свойство("_original_data_json", ВремАнон) И ЗначениеЗаполнено(ВремАнон);
		КонецЕсли;
		Если НЕ УжеАнонимизирован Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json", ЗаписатьJSON2(Результат.data));
			КонецЕсли;
			РегексКонтекст = Новый Структура(
				"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст);
			КонтекстLineage = Новый Структура("Режим, КомандаId, Результат",
				"proxy", Команда.id, Результат);
			РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
			ЗапуститьRegexФазу(РегексКонтекст);
			Возврат;
		КонецЕсли;
	КонецЕсли;

  	// Отправка результата на сервер
  	РезультатОтправки = ОтправитьРезультатНаСервер(АдресСервера, Команда.id, Результат);
	
	Если РезультатОтправки.Успех Тогда
		ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
	Иначе
		ДобавитьВЛог("Ошибка отправки результата: " + РезультатОтправки.ОписаниеОшибки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьРезультатПодтверждения(Разрешено, ДополнительныеПараметры) Экспорт
	
	Команда = ДополнительныеПараметры.Команда;
	
 	Если Разрешено = Истина Тогда
 		ДобавитьВЛог("Пользователь разрешил выполнение опасной операции");
		Попытка
			Результат = ВыполнитьКомандуСУчетомКонтекста(Команда);
		Исключение
			ТекстОшибки = ОписаниеОшибки();
			ДобавитьВЛог("Ошибка выполнения команды на сервере 1С: " + ТекстОшибки);
			Результат = Новый Структура(
				"success, error",
				Ложь,
				"Ошибка выполнения команды на сервере 1С: " + ТекстОшибки
				+ " / Error executing command in 1C server: " + ТекстОшибки
			);
		КонецПопытки;
		ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Команда);
		Если ИспользоватьАсинхронноеПодключениеКомпонент
			И НуженLineageДляРезультата(Команда.tool, ТекстЗапросаLineage, Результат) Тогда
			КонтекстLineage = Новый Структура("Режим, КомандаId, Результат, ТекстЗапроса",
				"proxy", Команда.id, Результат, ТекстЗапросаLineage);
			НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
			Возврат;
		КонецЕсли;
  	Иначе
  		ДобавитьВЛог("Пользователь отклонил выполнение опасной операции");
		
		// Формирование списка опасных слов для сообщения
		ОпасныеСлова = "";
		Если Команда.Свойство("params") И Команда.params <> Неопределено Тогда
			ПараметрыКоманды = Команда.params;
			Если ПараметрыКоманды.Свойство("dangerous_keywords") Тогда
				Для Каждого Слово Из ПараметрыКоманды.dangerous_keywords Цикл
					ОпасныеСлова = ОпасныеСлова + ?(ПустаяСтрока(ОпасныеСлова), "", ", ") + Слово;
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		
		Результат = Новый Структура("success, error", Ложь, 
			"Операция отклонена пользователем. Код содержит опасные ключевые слова: " + ОпасныеСлова + 
			" / Operation rejected by user. Code contains dangerous keywords: " + ОпасныеСлова);
	КонецЕсли;
	
	// Отправка результата на сервер
	РезультатОтправки = ОтправитьРезультатНаСервер(АдресСервера, Команда.id, Результат);
	
	Если РезультатОтправки.Успех Тогда
		ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
	Иначе
		ДобавитьВЛог("Ошибка отправки результата: " + РезультатОтправки.ОписаниеОшибки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ВсеОперацииАвтоРазрешены(МассивОпасныхСлов)
	
	// Соответствие ключевых слов и флагов автоматического разрешения
	СоответствиеФлагов = Новый Соответствие;
	СоответствиеФлагов.Вставить("ЗАПИСАТЬ", АвтоРазрешитьЗаписать);
	СоответствиеФлагов.Вставить("WRITE", АвтоРазрешитьЗаписать);
	СоответствиеФлагов.Вставить("УСТАНОВИТЬПРИВИЛЕГИРОВАННЫЙРЕЖИМ", АвтоРазрешитьПривилегированныйРежим);
	СоответствиеФлагов.Вставить("SETPRIVILEGEDMODE", АвтоРазрешитьПривилегированныйРежим);
	
	// Проверка каждого опасного слова
	Для Каждого Слово Из МассивОпасныхСлов Цикл
		СловоВерхнийРегистр = ВРег(Слово);
		ФлагРазрешения = СоответствиеФлагов.Получить(СловоВерхнийРегистр);
		
		Если ФлагРазрешения = Неопределено Или ФлагРазрешения = Ложь Тогда
			Возврат Ложь; // Есть неразрешённая операция
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина; // Все операции автоматически разрешены
	
КонецФункции

&НаКлиенте
Функция ПолучитьТекстКоманды(Команда)
	
	ТекстКоманды = "";
	Отступ = "    "; // 4 пробела
	
	Если Не Команда.Свойство("params") Или Команда.params = Неопределено Тогда
		Возврат Отступ + "(без параметров)";
	КонецЕсли;
	
	ПараметрыКоманды = Команда.params;
	
	Если Команда.tool = "execute_query" Тогда
		Если ПараметрыКоманды.Свойство("query") Тогда
			ТекстКоманды = ПараметрыКоманды.query;
		КонецЕсли;
	ИначеЕсли Команда.tool = "execute_code" Тогда
		Если ПараметрыКоманды.Свойство("code") Тогда
			ТекстКоманды = ПараметрыКоманды.code;
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_metadata" Тогда
		ЧастиТекста = Новый Массив;
		Если ПараметрыКоманды.Свойство("filter") И Не ПустаяСтрока(ПараметрыКоманды.filter) Тогда
			ЧастиТекста.Добавить("фильтр: " + ПараметрыКоманды.filter);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("meta_type") И Не ПустаяСтрока(ПараметрыКоманды.meta_type) Тогда
			ЧастиТекста.Добавить("тип: " + ПараметрыКоманды.meta_type);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("name_mask") И Не ПустаяСтрока(ПараметрыКоманды.name_mask) Тогда
			ЧастиТекста.Добавить("маска: " + ПараметрыКоманды.name_mask);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("attribute_mask") И Не ПустаяСтрока(ПараметрыКоманды.attribute_mask) Тогда
			ЧастиТекста.Добавить("маска атрибута: " + ПараметрыКоманды.attribute_mask);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("extension_name") И ПараметрыКоманды.extension_name <> Неопределено Тогда
			ЧастиТекста.Добавить("расширение: " + ?(ПустаяСтрока(ПараметрыКоманды.extension_name), "(конфигурация)", ПараметрыКоманды.extension_name));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("limit") И ЗначениеЗаполнено(ПараметрыКоманды.limit) Тогда
			ЧастиТекста.Добавить("лимит: " + ПараметрыКоманды.limit);
		КонецЕсли;
		Если ЧастиТекста.Количество() = 0 Тогда
			ТекстКоманды = "(все метаданные)";
		Иначе
			ТекстКоманды = СоединитьМассив(ЧастиТекста, ", ");
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_event_log" Тогда
		ЧастиТекста = Новый Массив;
		Если ПараметрыКоманды.Свойство("start_date") И ЗначениеЗаполнено(ПараметрыКоманды.start_date) Тогда
			ЧастиТекста.Добавить("с " + ПараметрыКоманды.start_date);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("end_date") И ЗначениеЗаполнено(ПараметрыКоманды.end_date) Тогда
			ЧастиТекста.Добавить("по " + ПараметрыКоманды.end_date);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("levels") И ПараметрыКоманды.levels <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.levels) И ПараметрыКоманды.levels.Количество() > 0 Тогда
			ЧастиТекста.Добавить("уровни: " + СоединитьМассив(ПараметрыКоманды.levels, ", "));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("limit") И ЗначениеЗаполнено(ПараметрыКоманды.limit) Тогда
			ЧастиТекста.Добавить("лимит: " + ПараметрыКоманды.limit);
		КонецЕсли;
		// Фильтры по объекту (приоритет: object_description > link > data)
		Если ПараметрыКоманды.Свойство("object_description") И ПараметрыКоманды.object_description <> Неопределено Тогда
			Если ТипЗнч(ПараметрыКоманды.object_description) = Тип("Структура") Тогда
				Если ПараметрыКоманды.object_description.Свойство("Представление") Тогда
					ЧастиТекста.Добавить("объект: " + ПараметрыКоманды.object_description.Представление);
				ИначеЕсли ПараметрыКоманды.object_description.Свойство("ТипОбъекта") Тогда
					ЧастиТекста.Добавить("объект (тип): " + ПараметрыКоманды.object_description.ТипОбъекта);
				Иначе
					ЧастиТекста.Добавить("объект: (описание объекта)");
				КонецЕсли;
			Иначе
				ЧастиТекста.Добавить("объект: (некорректное описание)");
			КонецЕсли;
		ИначеЕсли ПараметрыКоманды.Свойство("link") И ЗначениеЗаполнено(ПараметрыКоманды.link) Тогда
			ЧастиТекста.Добавить("ссылка: " + ПараметрыКоманды.link);
		ИначеЕсли ПараметрыКоманды.Свойство("data") И ЗначениеЗаполнено(ПараметрыКоманды.data) Тогда
			ЧастиТекста.Добавить("данные: " + ПараметрыКоманды.data);
		КонецЕсли;
		// Остальные фильтры
		Если ПараметрыКоманды.Свойство("metadata_type") И ПараметрыКоманды.metadata_type <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.metadata_type) И ПараметрыКоманды.metadata_type.Количество() > 0 Тогда
			ЧастиТекста.Добавить("метаданные: " + СоединитьМассив(ПараметрыКоманды.metadata_type, ", "));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("user") И ПараметрыКоманды.user <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.user) И ПараметрыКоманды.user.Количество() > 0 Тогда
			ЧастиТекста.Добавить("пользователи: " + СоединитьМассив(ПараметрыКоманды.user, ", "));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("session") И ПараметрыКоманды.session <> Неопределено Тогда
			Если ЭтоКоллекция(ПараметрыКоманды.session) И ПараметрыКоманды.session.Количество() > 0 Тогда
				МассивСеансов = Новый Массив;
				Для Каждого Сеанс Из ПараметрыКоманды.session Цикл
					МассивСеансов.Добавить(Строка(Сеанс));
				КонецЦикла;
				ЧастиТекста.Добавить("сеансы: " + СоединитьМассив(МассивСеансов, ", "));
			ИначеЕсли Не ЭтоКоллекция(ПараметрыКоманды.session) И ЗначениеЗаполнено(ПараметрыКоманды.session) Тогда
				ЧастиТекста.Добавить("сеанс: " + Строка(ПараметрыКоманды.session));
			КонецЕсли;
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("application") И ПараметрыКоманды.application <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.application) И ПараметрыКоманды.application.Количество() > 0 Тогда
			ЧастиТекста.Добавить("приложения: " + СоединитьМассив(ПараметрыКоманды.application, ", "));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("computer") И ЗначениеЗаполнено(ПараметрыКоманды.computer) Тогда
			ЧастиТекста.Добавить("компьютер: " + ПараметрыКоманды.computer);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("comment_contains") И ЗначениеЗаполнено(ПараметрыКоманды.comment_contains) Тогда
			ЧастиТекста.Добавить("комментарий содержит: " + ПараметрыКоманды.comment_contains);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("transaction_status") И ЗначениеЗаполнено(ПараметрыКоманды.transaction_status) Тогда
			ЧастиТекста.Добавить("статус транзакции: " + ПараметрыКоманды.transaction_status);
		КонецЕсли;
		Если ЧастиТекста.Количество() > 0 Тогда
			// Каждый параметр на новой строке с отступом в 4 пробела
			ТекстКоманды = "    " + СоединитьМассив(ЧастиТекста, Символы.ПС + "    ");
		Иначе
			ТекстКоманды = "(журнал регистрации)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_object_by_link" Тогда
		Если ПараметрыКоманды.Свойство("link") И ЗначениеЗаполнено(ПараметрыКоманды.link) Тогда
			ТекстКоманды = "ссылка: " + ПараметрыКоманды.link;
		Иначе
			ТекстКоманды = "(пустая ссылка)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_link_of_object" Тогда
		Если ПараметрыКоманды.Свойство("object_description") И ПараметрыКоманды.object_description <> Неопределено Тогда
			Если ТипЗнч(ПараметрыКоманды.object_description) = Тип("Структура") Тогда
				Если ПараметрыКоманды.object_description.Свойство("Представление") Тогда
					ТекстКоманды = "объект: " + ПараметрыКоманды.object_description.Представление;
				ИначеЕсли ПараметрыКоманды.object_description.Свойство("ТипОбъекта") Тогда
					ТекстКоманды = "тип: " + ПараметрыКоманды.object_description.ТипОбъекта;
				Иначе
					ТекстКоманды = "(описание объекта)";
				КонецЕсли;
			Иначе
				ТекстКоманды = "(некорректное описание)";
			КонецЕсли;
		Иначе
			ТекстКоманды = "(пустое описание)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "find_references_to_object" Тогда
		ЧастиТекста = Новый Массив;
		// Целевой объект
		Если ПараметрыКоманды.Свойство("target_object_description") И ПараметрыКоманды.target_object_description <> Неопределено Тогда
			Если ТипЗнч(ПараметрыКоманды.target_object_description) = Тип("Структура") Тогда
				Если ПараметрыКоманды.target_object_description.Свойство("Представление") Тогда
					ЧастиТекста.Добавить("цель: " + ПараметрыКоманды.target_object_description.Представление);
				ИначеЕсли ПараметрыКоманды.target_object_description.Свойство("ТипОбъекта") Тогда
					ЧастиТекста.Добавить("цель (тип): " + ПараметрыКоманды.target_object_description.ТипОбъекта);
				Иначе
					ЧастиТекста.Добавить("цель: (описание объекта)");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		// Области поиска
		Если ПараметрыКоманды.Свойство("search_scope") И ПараметрыКоманды.search_scope <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.search_scope) И ПараметрыКоманды.search_scope.Количество() > 0 Тогда
			ЧастиТекста.Добавить("области: " + СоединитьМассив(ПараметрыКоманды.search_scope, ", "));
		КонецЕсли;
		// Фильтр метаданных
		Если ПараметрыКоманды.Свойство("meta_filter") И ПараметрыКоманды.meta_filter <> Неопределено Тогда
			Если ТипЗнч(ПараметрыКоманды.meta_filter) = Тип("Структура") Тогда
				Если ПараметрыКоманды.meta_filter.Свойство("names") И ПараметрыКоманды.meta_filter.names <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.meta_filter.names) И ПараметрыКоманды.meta_filter.names.Количество() > 0 Тогда
					ЧастиТекста.Добавить("фильтр имён: " + СоединитьМассив(ПараметрыКоманды.meta_filter.names, ", "));
				ИначеЕсли ПараметрыКоманды.meta_filter.Свойство("name_mask") И ЗначениеЗаполнено(ПараметрыКоманды.meta_filter.name_mask) Тогда
					ЧастиТекста.Добавить("маска: " + ПараметрыКоманды.meta_filter.name_mask);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		// Лимиты
		Если ПараметрыКоманды.Свойство("limit_hits") И ЗначениеЗаполнено(ПараметрыКоманды.limit_hits) Тогда
			ЧастиТекста.Добавить("лимит: " + ПараметрыКоманды.limit_hits);
		КонецЕсли;
		Если ЧастиТекста.Количество() > 0 Тогда
			ТекстКоманды = "    " + СоединитьМассив(ЧастиТекста, Символы.ПС + "    ");
		Иначе
			ТекстКоманды = "(поиск ссылок на объект)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_access_rights" Тогда
		ЧастиТекста = Новый Массив;
		Если ПараметрыКоманды.Свойство("metadata_object") И ЗначениеЗаполнено(ПараметрыКоманды.metadata_object) Тогда
			ЧастиТекста.Добавить("объект: " + ПараметрыКоманды.metadata_object);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("user_name") И ЗначениеЗаполнено(ПараметрыКоманды.user_name) Тогда
			ЧастиТекста.Добавить("пользователь: " + ПараметрыКоманды.user_name);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("rights_filter") И ПараметрыКоманды.rights_filter <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.rights_filter) И ПараметрыКоманды.rights_filter.Количество() > 0 Тогда
			ЧастиТекста.Добавить("права: " + СоединитьМассив(ПараметрыКоманды.rights_filter, ", "));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("roles_filter") И ПараметрыКоманды.roles_filter <> Неопределено И ЭтоКоллекция(ПараметрыКоманды.roles_filter) И ПараметрыКоманды.roles_filter.Количество() > 0 Тогда
			ЧастиТекста.Добавить("роли: " + СоединитьМассив(ПараметрыКоманды.roles_filter, ", "));
		КонецЕсли;
		Если ЧастиТекста.Количество() > 0 Тогда
			ТекстКоманды = "    " + СоединитьМассив(ЧастиТекста, Символы.ПС + "    ");
		Иначе
			ТекстКоманды = "(права доступа)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "submit_for_deanonymization" Тогда
		Текст = "";
		Если ПараметрыКоманды.Свойство("text") Тогда Текст = ПараметрыКоманды.text; КонецЕсли;
		Если ЗначениеЗаполнено(Текст) Тогда
			ТекстКоманды = "текст: " + Лев(Текст, 80) + ?(СтрДлина(Текст) > 80, "…", "");
		Иначе
			ТекстКоманды = "(пустой текст)";
		КонецЕсли;
	ИначеЕсли Команда.tool = "get_screenshot" Тогда
		ЧастиТекста = Новый Массив;
		Если ПараметрыКоманды.Свойство("form_name") И ЗначениеЗаполнено(ПараметрыКоманды.form_name) Тогда
			ЧастиТекста.Добавить("форма: " + ПараметрыКоманды.form_name);
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("region") И ПараметрыКоманды.region <> Неопределено Тогда
			Р = ПараметрыКоманды.region;
			РегионСтр = "";
			Если ТипЗнч(Р) = Тип("Структура") Тогда
				Если Р.Свойство("x") Тогда РегионСтр = РегионСтр + "x=" + Р.x + " "; КонецЕсли;
				Если Р.Свойство("y") Тогда РегионСтр = РегионСтр + "y=" + Р.y + " "; КонецЕсли;
				Если Р.Свойство("width") Тогда РегионСтр = РегионСтр + "w=" + Р.width + " "; КонецЕсли;
				Если Р.Свойство("height") Тогда РегионСтр = РегионСтр + "h=" + Р.height; КонецЕсли;
			КонецЕсли;
			ЧастиТекста.Добавить("регион: " + СокрЛП(РегионСтр));
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("show_grid") И ПараметрыКоманды.show_grid = Истина Тогда
			ЧастиТекста.Добавить("сетка: да");
		КонецЕсли;
		Если ПараметрыКоманды.Свойство("scale_percent") И ЗначениеЗаполнено(ПараметрыКоманды.scale_percent) Тогда
			ЧастиТекста.Добавить("масштаб: " + ПараметрыКоманды.scale_percent + "%");
		КонецЕсли;
		Если ЧастиТекста.Количество() > 0 Тогда
			ТекстКоманды = СоединитьМассив(ЧастиТекста, ", ");
		Иначе
			ТекстКоманды = "(весь экран)";
		КонецЕсли;
	Иначе
		ТекстКоманды = "(неизвестная команда)";
	КонецЕсли;
	
	// Преобразуем экранированные символы
	ТекстКоманды = СтрЗаменить(ТекстКоманды, "\r", "");
	ТекстКоманды = СтрЗаменить(ТекстКоманды, "\t", "    ");
	ТекстКоманды = СтрЗаменить(ТекстКоманды, "\n", Символы.ПС);
	
	// Добавляем отступ к каждой строке
	ТекстКоманды = Отступ + СтрЗаменить(ТекстКоманды, Символы.ПС, Символы.ПС + Отступ);
	
	Возврат ТекстКоманды;
	
КонецФункции

&НаКлиенте
Функция ПолучитьТекстРезультата(Результат)
	
	Отступ = "    "; // 4 пробела
	ТекстРезультата = "";
	
	Если (Результат.Свойство("success") И Результат.success = Истина)
		Или (Результат.Свойство("received") И Результат.received = Истина) Тогда
		Если Результат.Свойство("_original_data_json") Тогда
			ТекстРезультата = Результат._original_data_json;
		ИначеЕсли Результат.Свойство("data") Тогда
			ТекстРезультата = ЗаписатьJSON2(Результат.data);
		ИначеЕсли Результат.Свойство("image_base64")
			И ЗначениеЗаполнено(Результат.image_base64) Тогда
			МимТип = "image/png";
			Если Результат.Свойство("mime_type") Тогда МимТип = Результат.mime_type; КонецЕсли;
			ТекстРезультата = "(изображение " + МимТип
				+ ", base64 длина " + СтрДлина(Результат.image_base64) + " символов)";
		Иначе
			ТекстРезультата = "(нет данных)";
		КонецЕсли;
	Иначе
		Если Результат.Свойство("error") Тогда
			ТекстРезультата = "Ошибка: " + Результат.error;
		Иначе
			ТекстРезультата = "(неизвестная ошибка)";
		КонецЕсли;
	КонецЕсли;
	
	// Преобразуем экранированные символы
	ТекстРезультата = СтрЗаменить(ТекстРезультата, "\r", "");
	ТекстРезультата = СтрЗаменить(ТекстРезультата, "\t", "    ");
	ТекстРезультата = СтрЗаменить(ТекстРезультата, "\n", Символы.ПС);
	
	// Добавляем отступ к каждой строке
	ТекстРезультата = Отступ + СтрЗаменить(ТекстРезультата, Символы.ПС, Символы.ПС + Отступ);
	
	Возврат ТекстРезультата;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// HTTP-ЗАПРОСЫ К СЕРВЕРУ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция СформироватьПутьЗапроса(БазовыйПуть, ПараметрыЗапроса)
	
	// ПараметрыЗапроса - Структура с параметрами
	// Возвращает путь вида "/1c/poll?channel=abc&timeout=30"
	
	Если ПараметрыЗапроса.Количество() = 0 Тогда
		Возврат БазовыйПуть;
	КонецЕсли;
	
	ЧастиПараметров = Новый Массив;
	Для Каждого КлючЗначение Из ПараметрыЗапроса Цикл
		Если Не ПустаяСтрока(КлючЗначение.Значение) Тогда
			ЧастиПараметров.Добавить(КлючЗначение.Ключ + "=" + КлючЗначение.Значение);
		КонецЕсли;
	КонецЦикла;
	
	Если ЧастиПараметров.Количество() = 0 Тогда
		Возврат БазовыйПуть;
	КонецЕсли;
	
	Возврат БазовыйПуть + "?" + СоединитьМассив(ЧастиПараметров, "&");
	
КонецФункции

&НаКлиенте
Функция ПолучитьКомандуССервера(Адрес)
	
	Результат = Новый Структура("Успех, ЕстьКоманда, Команда, ОписаниеОшибки", Ложь, Ложь, Неопределено, "");
	
	Попытка
		
		// Разбор URL
		СтруктураURL = РазобратьURL(Адрес);
		
		// Создание HTTP-соединения
		Если СтруктураURL.Протокол = "https" Тогда
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 30, Новый ЗащищенноеСоединениеOpenSSL());
		Иначе
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 30);
		КонецЕсли;
		
		// Формирование параметров запроса с channel
		ПараметрыЗапроса = Новый Структура;
		Если Не ПустаяСтрока(ИдентификаторКанала) Тогда
			ПараметрыЗапроса.Вставить("channel", СокрЛП(ИдентификаторКанала));
		КонецЕсли;
		
		ПутьЗапроса = СформироватьПутьЗапроса(СтруктураURL.Путь + "/1c/poll", ПараметрыЗапроса);
		
		// Формирование запроса
		Запрос = Новый HTTPЗапрос(ПутьЗапроса);
		Запрос.Заголовки.Вставить("Accept", "application/json");
		
		// Выполнение запроса
		Ответ = Соединение.Получить(Запрос);
		
		Если Ответ.КодСостояния = 200 Тогда
			// Есть команда
			ТелоОтвета = Ответ.ПолучитьТелоКакСтроку("UTF-8");
			Если Не ПустаяСтрока(ТелоОтвета) Тогда
				Команда = ПрочитатьJSON2(ТелоОтвета);
				Результат.Успех = Истина;
				Результат.ЕстьКоманда = Истина;
				Результат.Команда = Команда;
			Иначе
				Результат.Успех = Истина;
				Результат.ЕстьКоманда = Ложь;
			КонецЕсли;
		ИначеЕсли Ответ.КодСостояния = 204 Тогда
			// Нет команд (таймаут long polling)
			Результат.Успех = Истина;
			Результат.ЕстьКоманда = Ложь;
		Иначе
			Результат.ОписаниеОшибки = "HTTP " + Ответ.КодСостояния;
		КонецЕсли;
		
	Исключение
		Результат.ОписаниеОшибки = ОписаниеОшибки();
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции


&НаКлиенте
Функция ОтправитьРезультатНаСервер(Адрес, ИдКоманды, Результат)
	
	РезультатОтправки = Новый Структура("Успех, ОписаниеОшибки", Ложь, "");
	
	Попытка
		
		// Разбор URL
		СтруктураURL = РазобратьURL(Адрес);
		
		// Создание HTTP-соединения
		Если СтруктураURL.Протокол = "https" Тогда
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 30, Новый ЗащищенноеСоединениеOpenSSL());
		Иначе
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 30);
		КонецЕсли;
		
		// Формирование тела запроса
		ТелоЗапроса = Новый Структура;
		ТелоЗапроса.Вставить("id", ИдКоманды);
		
		Если Результат.Свойство("success") И Результат.success Тогда
			ТелоЗапроса.Вставить("success", Истина);
			
			// Безопасное получение data - может отсутствовать при ошибке
			Если Результат.Свойство("data") Тогда
				ТелоЗапроса.Вставить("data", Результат.data);
			КонецЕсли;
			
			// Схема результата (опционально, для execute_query include_schema=true)
			Если Результат.Свойство("schema") Тогда
				ТелоЗапроса.Вставить("schema", Результат.schema);
			КонецЕсли;
			
			// Дополнительные (опциональные) мета-поля результата (например, для постраничного вывода get_metadata)
			// Важно: whitelist, чтобы не прокидывать на сервер произвольные поля/мусор.
			ДопПоля = Новый Массив;
			ДопПоля.Добавить("truncated");
			ДопПоля.Добавить("limit");
			ДопПоля.Добавить("returned");
			ДопПоля.Добавить("count");
			ДопПоля.Добавить("offset");
			ДопПоля.Добавить("has_more");
			ДопПоля.Добавить("next_offset");
			ДопПоля.Добавить("last_date");
			ДопПоля.Добавить("next_same_second_offset");
			ДопПоля.Добавить("configuration");
			ДопПоля.Добавить("extension");
			ДопПоля.Добавить("received");
			ДопПоля.Добавить("image_base64");
			ДопПоля.Добавить("mime_type");
			ДопПоля.Добавить("window_rect");
			ДопПоля.Добавить("grid_coords");

			Для Каждого ИмяПоля Из ДопПоля Цикл
				Если Результат.Свойство(ИмяПоля) Тогда
					ЗначениеПоля = Результат[ИмяПоля];
					Если ЗначениеПоля <> Неопределено Тогда
						ТелоЗапроса.Вставить(ИмяПоля, ЗначениеПоля);
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
		Иначе
			ТелоЗапроса.Вставить("success", Ложь);
			
			// Безопасное получение error
			Если Результат.Свойство("error") Тогда
				ТелоЗапроса.Вставить("error", Результат.error);
			Иначе
				ТелоЗапроса.Вставить("error", "Неизвестная ошибка");
			КонецЕсли;
		КонецЕсли;
		
		ТелоJSON = ЗаписатьJSON2(ТелоЗапроса);
		
		// Формирование параметров запроса с channel
		ПараметрыЗапроса = Новый Структура;
		Если Не ПустаяСтрока(ИдентификаторКанала) Тогда
			ПараметрыЗапроса.Вставить("channel", СокрЛП(ИдентификаторКанала));
		КонецЕсли;
		
		ПутьЗапроса = СформироватьПутьЗапроса(СтруктураURL.Путь + "/1c/result", ПараметрыЗапроса);
		
		// Формирование запроса
		Запрос = Новый HTTPЗапрос(ПутьЗапроса);
		Запрос.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
		Запрос.УстановитьТелоИзСтроки(ТелоJSON, "UTF-8");
		
		// Выполнение запроса
		Ответ = Соединение.ОтправитьДляОбработки(Запрос);
		
		Если Ответ.КодСостояния = 200 Или Ответ.КодСостояния = 204 Тогда
			РезультатОтправки.Успех = Истина;
		Иначе
			РезультатОтправки.ОписаниеОшибки = "HTTP " + Ответ.КодСостояния;
		КонецЕсли;
		
	Исключение
		РезультатОтправки.ОписаниеОшибки = ОписаниеОшибки();
	КонецПопытки;
	
	Возврат РезультатОтправки;
	
КонецФункции

&НаКлиенте
Функция РазобратьURL(URL)
	
	Результат = Новый Структура("Протокол, Хост, Порт, Путь", "http", "", 80, "");
	
	АдресБезПротокола = URL;
	
	// Определение протокола
	Если НРег(Лев(URL, 8)) = "https://" Тогда
		Результат.Протокол = "https";
		Результат.Порт = 443;
		АдресБезПротокола = Сред(URL, 9);
	ИначеЕсли НРег(Лев(URL, 7)) = "http://" Тогда
		Результат.Протокол = "http";
		Результат.Порт = 80;
		АдресБезПротокола = Сред(URL, 8);
	КонецЕсли;
	
	// Разделение хоста и пути
	ПозицияСлеша = Найти(АдресБезПротокола, "/");
	Если ПозицияСлеша > 0 Тогда
		ХостСПортом = Лев(АдресБезПротокола, ПозицияСлеша - 1);
		Результат.Путь = Сред(АдресБезПротокола, ПозицияСлеша);
	Иначе
		ХостСПортом = АдресБезПротокола;
		Результат.Путь = "";
	КонецЕсли;
	
	// Разделение хоста и порта
	ПозицияДвоеточия = Найти(ХостСПортом, ":");
	Если ПозицияДвоеточия > 0 Тогда
		Результат.Хост = Лев(ХостСПортом, ПозицияДвоеточия - 1);
		Попытка
			Результат.Порт = Число(Сред(ХостСПортом, ПозицияДвоеточия + 1));
		Исключение
			// Оставляем порт по умолчанию
		КонецПопытки;
	Иначе
		Результат.Хост = ХостСПортом;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ДИСПЕТЧЕР КОМАНД
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ВыполнитьКомандуНаСервере(Команда)
	
	ИмяИнструмента = СокрЛП(Команда.tool);
	ПараметрыКоманды = Неопределено;
	
	Если Команда.Свойство("params") Тогда
		ПараметрыКоманды = Команда.params;
	КонецЕсли;
	
	// Диспетчеризация по имени инструмента
	Если ИмяИнструмента = "execute_query" Тогда
		
		ТекстЗапроса = "";
		ПараметрыЗапроса = Новый Соответствие;
		Лимит = 100;
		ВключитьСхему = Ложь;
		
		Если ПараметрыКоманды <> Неопределено Тогда
			Если ПараметрыКоманды.Свойство("query") Тогда
				ТекстЗапроса = ПараметрыКоманды.query;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("params") Тогда
				ПараметрыЗапроса = ПараметрыКоманды.params;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("limit") Тогда
				Лимит = ПараметрыКоманды.limit;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("include_schema") Тогда
				ВключитьСхему = (ПараметрыКоманды.include_schema = Истина);
			КонецЕсли;
		КонецЕсли;
		
		Возврат ВыполнитьЗапрос(ТекстЗапроса, ПараметрыЗапроса, Лимит, ВключитьСхему);
		
	ИначеЕсли ИмяИнструмента = "execute_code" Тогда
		
		Код = "";
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("code") Тогда
			Код = ПараметрыКоманды.code;
		КонецЕсли;
		
		Возврат ВыполнитьКод(Код);
		
	ИначеЕсли ИмяИнструмента = "get_metadata" Тогда

		Фильтр = "";
		ТипМетаданных = "";
		МаскаИмени = "";
		Лимит = 100;
		Смещение = 0;
		Секции = Неопределено;
		ИмяРасширения = Неопределено;  // КРИТИЧНО: Неопределено, НЕ ""
		КэшПараметры = Неопределено;
		МаскаАтрибута = "";

		Если ПараметрыКоманды <> Неопределено Тогда
			Если ПараметрыКоманды.Свойство("filter") Тогда
				Фильтр = ПараметрыКоманды.filter;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("meta_type") Тогда
				ТипМетаданных = НормализоватьМассивПараметра(ПараметрыКоманды.meta_type);
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("name_mask") Тогда
				МаскаИмени = ПараметрыКоманды.name_mask;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("limit") Тогда
				Лимит = ПараметрыКоманды.limit;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("offset") Тогда
				Смещение = ПараметрыКоманды.offset;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("sections") Тогда
				Секции = ПараметрыКоманды.sections;
			КонецЕсли;
			// extension_name: присваиваем ТОЛЬКО если свойство есть в JSON
			Если ПараметрыКоманды.Свойство("extension_name") Тогда
				ИмяРасширения = ПараметрыКоманды.extension_name;  // может быть "" (список расширений)
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("attribute_mask") Тогда
				// СокрЛП здесь для симметрии с Python-валидатором: whitespace-only = пусто = игнор
				МаскаАтрибута = СокрЛП(Строка(ПараметрыКоманды.attribute_mask));
			КонецЕсли;
			
			// Внутренние параметры кэша (токены ВременногоХранилища), добавляются на клиенте.
			Если ПараметрыКоманды.Свойство("mcp_cache_hash")
				Или ПараметрыКоманды.Свойство("mcp_cache_data_token")
				Или ПараметрыКоманды.Свойство("mcp_cache_index_token")
				Или ПараметрыКоманды.Свойство("mcp_cache_details_token") Тогда
				
				КэшПараметры = Новый Структура(
					"hash, data_token, index_token, details_token",
					?(ПараметрыКоманды.Свойство("mcp_cache_hash"), ПараметрыКоманды.mcp_cache_hash, ""),
					?(ПараметрыКоманды.Свойство("mcp_cache_data_token"), ПараметрыКоманды.mcp_cache_data_token, Неопределено),
					?(ПараметрыКоманды.Свойство("mcp_cache_index_token"), ПараметрыКоманды.mcp_cache_index_token, Неопределено),
					?(ПараметрыКоманды.Свойство("mcp_cache_details_token"), ПараметрыКоманды.mcp_cache_details_token, Неопределено)
				);
			КонецЕсли;
		КонецЕсли;

		Возврат ПолучитьМетаданные(Фильтр, ТипМетаданных, МаскаИмени, Лимит, Смещение, Секции, ИмяРасширения, КэшПараметры, МаскаАтрибута);
		
	ИначеЕсли ИмяИнструмента = "get_event_log" Тогда
		
		// Парсим даты из ISO формата
		ДатаНачала = Неопределено;
		ДатаОкончания = Неопределено;
		
		Если ПараметрыКоманды <> Неопределено Тогда
			Если ПараметрыКоманды.Свойство("start_date") И ЗначениеЗаполнено(ПараметрыКоманды.start_date) Тогда
				ДатаНачала = ПрочитатьДатуИзISO(ПараметрыКоманды.start_date);
			КонецЕсли;
			
			Если ПараметрыКоманды.Свойство("end_date") И ЗначениеЗаполнено(ПараметрыКоманды.end_date) Тогда
				ДатаОкончания = ПрочитатьДатуИзISO(ПараметрыКоманды.end_date);
			КонецЕсли;
		КонецЕсли;
		
		Уровни = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("levels") Тогда
			Уровни = НормализоватьМассивПараметра(ПараметрыКоманды.levels);
		КонецЕсли;

		События = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("events") Тогда
			События = НормализоватьМассивПараметра(ПараметрыКоманды.events);
		КонецЕсли;
		
		Лимит = 100;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("limit") Тогда
			Лимит = ПараметрыКоманды.limit;
		КонецЕсли;
		
		// Разрешение параметра фильтра по объекту (приоритет: object_description > link > data)
		ДанныеОбъекта = Неопределено;
		
		Если ПараметрыКоманды <> Неопределено Тогда
			// Приоритет 1: object_description
			Если ПараметрыКоманды.Свойство("object_description") И ПараметрыКоманды.object_description <> Неопределено Тогда
				Попытка
					ДанныеОбъекта = СформироватьНавигационнуюСсылку(ПараметрыКоманды.object_description);
				Исключение
					// Возвращаем явную ошибку при невозможности сформировать навигационную ссылку
					Возврат Новый Структура("success, error", Ложь, 
						"Ошибка формирования навигационной ссылки: " + ОписаниеОшибки() + 
						" / Error creating navigation link: " + ОписаниеОшибки());
				КонецПопытки;
			
			// Приоритет 2: link
			ИначеЕсли ПараметрыКоманды.Свойство("link") И ЗначениеЗаполнено(ПараметрыКоманды.link) Тогда
				ДанныеОбъекта = ПараметрыКоманды.link;
			
			// Приоритет 3: data (обратная совместимость)
			ИначеЕсли ПараметрыКоманды.Свойство("data") И ЗначениеЗаполнено(ПараметрыКоманды.data) Тогда
				ДанныеОбъекта = ПараметрыКоманды.data;
			КонецЕсли;
		КонецЕсли;
		
		ТипыМетаданных = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("metadata_type") Тогда
			ТипыМетаданных = НормализоватьМассивПараметра(ПараметрыКоманды.metadata_type);
		КонецЕсли;

		ФильтрПользователи = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("user") Тогда
			ФильтрПользователи = НормализоватьМассивПараметра(ПараметрыКоманды.user);
		КонецЕсли;

		Сеансы = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("session") Тогда
			Сеансы = НормализоватьМассивПараметра(ПараметрыКоманды.session);
		КонецЕсли;

		Приложения = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("application") Тогда
			Приложения = НормализоватьМассивПараметра(ПараметрыКоманды.application);
		КонецЕсли;
		
		Компьютер = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("computer") Тогда
			Компьютер = ПараметрыКоманды.computer;
		КонецЕсли;
		
		ПодстрокаКомментария = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("comment_contains") Тогда
			ПодстрокаКомментария = ПараметрыКоманды.comment_contains;
		КонецЕсли;
		
		СтатусТранзакции = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("transaction_status") Тогда
			СтатусТранзакции = ПараметрыКоманды.transaction_status;
		КонецЕсли;

		// Курсорная пагинация: смещение записей в пределах начальной секунды
		СмещениеВСекунде = 0;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("same_second_offset") И ЗначениеЗаполнено(ПараметрыКоманды.same_second_offset) Тогда
			СмещениеВСекунде = ПараметрыКоманды.same_second_offset;
		КонецЕсли;

		Возврат ПолучитьЖурналРегистрации(ДатаНачала, ДатаОкончания, Уровни, События, Лимит,
			ДанныеОбъекта, ТипыМетаданных, ФильтрПользователи, Сеансы, Приложения, Компьютер,
			ПодстрокаКомментария, СтатусТранзакции, СмещениеВСекунде);
		
	ИначеЕсли ИмяИнструмента = "get_object_by_link" Тогда
		
		Ссылка = "";
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("link") Тогда
			Ссылка = ПараметрыКоманды.link;
		КонецЕсли;
		
		Возврат ПолучитьОбъектПоНавигационнойСсылке(Ссылка);
		
	ИначеЕсли ИмяИнструмента = "get_link_of_object" Тогда

		ОписаниеОбъекта = Неопределено;
		Если ПараметрыКоманды <> Неопределено И ПараметрыКоманды.Свойство("object_description") Тогда
			ОписаниеОбъекта = ПараметрыКоманды.object_description;
		КонецЕсли;

		Возврат ПолучитьНавигационнуюСсылкуПоОписанию(ОписаниеОбъекта);

	ИначеЕсли ИмяИнструмента = "find_references_to_object" Тогда

		ОписаниеЦелевогоОбъекта = Неопределено;
		ОбластиПоиска = Неопределено;
		ФильтрМетаданных = Неопределено;
		ЛимитНайденных = 200;
		ЛимитНаМетуОбъект = 20;
		БюджетВремениСек = 30;

		Если ПараметрыКоманды <> Неопределено Тогда
			Если ПараметрыКоманды.Свойство("target_object_description") Тогда
				ОписаниеЦелевогоОбъекта = ПараметрыКоманды.target_object_description;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("search_scope") Тогда
				ОбластиПоиска = НормализоватьМассивПараметра(ПараметрыКоманды.search_scope);
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("meta_filter") Тогда
				ФильтрМетаданных = НормализоватьМассивПараметра(ПараметрыКоманды.meta_filter);
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("limit_hits") Тогда
				ЛимитНайденных = ПараметрыКоманды.limit_hits;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("limit_per_meta") Тогда
				ЛимитНаМетуОбъект = ПараметрыКоманды.limit_per_meta;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("timeout_budget_sec") Тогда
				БюджетВремениСек = ПараметрыКоманды.timeout_budget_sec;
			КонецЕсли;
		КонецЕсли;

		Возврат НайтиСсылкиНаОбъект(ОписаниеЦелевогоОбъекта, ОбластиПоиска, ФильтрМетаданных,
			ЛимитНайденных, ЛимитНаМетуОбъект, БюджетВремениСек);

	ИначеЕсли ИмяИнструмента = "get_access_rights" Тогда

		ИмяОбъектаМетаданных = "";
		ИмяПользователяИБ = Неопределено;
		ФильтрПрав = Неопределено;
		ФильтрРолей = Неопределено;

		Если ПараметрыКоманды <> Неопределено Тогда
			Если ПараметрыКоманды.Свойство("metadata_object") Тогда
				ИмяОбъектаМетаданных = ПараметрыКоманды.metadata_object;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("user_name") Тогда
				ИмяПользователяИБ = ПараметрыКоманды.user_name;
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("rights_filter") Тогда
				ФильтрПрав = НормализоватьМассивПараметра(ПараметрыКоманды.rights_filter);
			КонецЕсли;
			Если ПараметрыКоманды.Свойство("roles_filter") Тогда
				ФильтрРолей = НормализоватьМассивПараметра(ПараметрыКоманды.roles_filter);
			КонецЕсли;
		КонецЕсли;

		Возврат ПолучитьПраваДоступа(ИмяОбъектаМетаданных, ИмяПользователяИБ, ФильтрПрав, ФильтрРолей);

	Иначе

		Возврат Новый Структура("success, error", Ложь, "Неизвестный инструмент: " + ИмяИнструмента);

	КонецЕсли;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ВЫПОЛНЕНИЕ ЗАПРОСОВ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция БезопасноеЗначениеДляПередачи(Значение, Глубина = 0)
	
	Если Глубина > 5 Тогда
		Возврат "<max_depth>";
	КонецЕсли;
	
	Если Значение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТипЗначения = ТипЗнч(Значение);
	
	Если ТипЗначения = Тип("Строка")
		Или ТипЗначения = Тип("Число")
		Или ТипЗначения = Тип("Булево")
		Или ТипЗначения = Тип("Дата") Тогда
		Возврат Значение;
	КонецЕсли;
	
	Если ТипЗначения = Тип("УникальныйИдентификатор") Тогда
		Возврат Строка(Значение);
	КонецЕсли;
	
	Если ЭтоСсылка(ТипЗначения) Тогда
		Если ЗначениеЗаполнено(Значение) Тогда
			Возврат ОписаниеОбъектаПоСсылке(Значение);
		Иначе
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	Если ТипЗначения = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого Элемент Из Значение Цикл
			Результат.Добавить(БезопасноеЗначениеДляПередачи(Элемент, Глубина + 1));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// ФиксированныйМассив (FixedArray) - сериализуем как обычный массив
	Если ТипЗначения = Тип("ФиксированныйМассив") Тогда
		Результат = Новый Массив;
		Попытка
			Для Каждого Элемент Из Значение Цикл
				Результат.Добавить(БезопасноеЗначениеДляПередачи(Элемент, Глубина + 1));
			КонецЦикла;
		Исключение
			// Если по какой-то причине не итерируется - оставим пустой массив
		КонецПопытки;
		Возврат Результат;
	КонецЕсли;
	
	Если ТипЗначения = Тип("Структура") Тогда
		Результат = Новый Структура;
		Для Каждого Пара Из Значение Цикл
			Результат.Вставить(Пара.Ключ, БезопасноеЗначениеДляПередачи(Пара.Значение, Глубина + 1));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;
	
	Если ТипЗначения = Тип("Соответствие") Тогда
		Результат = Новый Массив;
		Для Каждого Пара Из Значение Цикл
			Результат.Добавить(Новый Структура(
				"key, value",
				БезопасноеЗначениеДляПередачи(Пара.Ключ, Глубина + 1),
				БезопасноеЗначениеДляПередачи(Пара.Значение, Глубина + 1)
			));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;
	
	Если ТипЗначения = Тип("ТаблицаЗначений") Тогда
		Результат = Новый Массив;
		Для Каждого СтрокаТаблицы Из Значение Цикл
			СтрокаРезультата = Новый Структура;
			Для Каждого Колонка Из Значение.Колонки Цикл
				СтрокаРезультата.Вставить(Колонка.Имя, БезопасноеЗначениеДляПередачи(СтрокаТаблицы[Колонка.Имя], Глубина + 1));
			КонецЦикла;
			Результат.Добавить(СтрокаРезультата);
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// ОписаниеТипов (TypeDescription) -> строковое перечисление типов
	// Строка(ТипЗнч(ОписаниеТипов)) возвращает "Описание типов" (не "ОписаниеТипов"), поэтому используем прямое сравнение типов
	Если ТипЗначения = Тип("ОписаниеТипов") Тогда
		Попытка
			Возврат ПолучитьСтроковоеПредставлениеОписанияТипов(Значение);
		Исключение
			// Если по какой-то причине не удалось - обработаем ниже как строку
		КонецПопытки;
	КонецЕсли;

	// СвязьПоТипу (TypeLink) -> объект {ПутьКДанным?, ЭлементСвязи?}
	// Важно: если связь не настроена, возвращаем пустой объект {}, чтобы ключ properties.СвязьПоТипу оставался,
	// но не превращался в неинформативную строку "СвязьПоТипу".
	Попытка
		Если ТипЗначения = Тип("СвязьПоТипу") Тогда
			Результат = Новый Структура;
			
			Попытка
				ПутьКДанным = Значение.ПутьКДанным;
				Если ПутьКДанным <> Неопределено И НЕ ПустаяСтрока(Строка(ПутьКДанным)) Тогда
					Результат.Вставить("ПутьКДанным", Строка(ПутьКДанным));
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Попытка
				ЭлементСвязи = Значение.ЭлементСвязи;
				Если ЭлементСвязи <> Неопределено И ЭлементСвязи <> 0 Тогда
					Результат.Вставить("ЭлементСвязи", ЭлементСвязи);
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Если тип недоступен в данной версии платформы - игнорируем
	КонецПопытки;

	// СвязьПараметраВыбора (ChoiceParameterLink) -> объект {Имя?, ПутьКДанным?, ИзменениеЗначения?}
	// Используется в СвязиПараметровВыбора (ChoiceParameterLinks): ФиксированныйМассив элементов этого типа.
	Попытка
		Если ТипЗначения = Тип("СвязьПараметраВыбора") Тогда
			Результат = Новый Структура;
			
			Попытка
				ИмяСвязи = Значение.Имя;
				Если ИмяСвязи <> Неопределено И НЕ ПустаяСтрока(Строка(ИмяСвязи)) Тогда
					Результат.Вставить("Имя", Строка(ИмяСвязи));
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Попытка
				ПутьКДанным = Значение.ПутьКДанным;
				Если ПутьКДанным <> Неопределено И НЕ ПустаяСтрока(Строка(ПутьКДанным)) Тогда
					Результат.Вставить("ПутьКДанным", Строка(ПутьКДанным));
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Попытка
				РежимИзменения = Значение.ИзменениеЗначения;
				Если РежимИзменения <> Неопределено Тогда
					Результат.Вставить("ИзменениеЗначения", Строка(РежимИзменения));
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Если тип недоступен в данной версии платформы - игнорируем
	КонецПопытки;
	
	// СоставОбщегоРеквизита (CommonAttributeContent) -> массив элементов состава
	// Каждый элемент: {Метаданные, Использование, УсловноеРазделение}
	Попытка
		Если ТипЗначения = Тип("СоставОбщегоРеквизита") Тогда
			Результат = Новый Массив;
			
			// Добавляем поле УсловноеРазделение только если оно реально встречается хотя бы у одного элемента.
			// Если встречается - ключ должен присутствовать у всех строк (у остальных будет null), чтобы табличный вывод не "терял" колонку.
			НужноПолеУсловноеРазделение = Ложь;
			Попытка
				Для Каждого ЭлементСостава Из Значение Цикл
					Попытка
						Если ЭлементСостава.УсловноеРазделение <> Неопределено Тогда
							НужноПолеУсловноеРазделение = Истина;
							Прервать;
						КонецЕсли;
					Исключение
					КонецПопытки;
				КонецЦикла;
			Исключение
			КонецПопытки;
			Попытка
				Для Каждого ЭлементСостава Из Значение Цикл
					ЭлементJSON = Новый Структура;
					
					// Метаданные (MetadataObject) -> ПолноеИмя() если возможно
					Попытка
						Мета = ЭлементСостава.Метаданные;
						Если Мета <> Неопределено Тогда
							ПолноеИмяМета = ПолноеИмяМетаданныхЕслиВозможно(Мета);
							Если ПолноеИмяМета = Неопределено Тогда
								Попытка
									ПолноеИмяМета = Мета.ПолноеИмя();
								Исключение
									ПолноеИмяМета = Строка(Мета);
								КонецПопытки;
							КонецЕсли;
							ЭлементJSON.Вставить("Метаданные", ПолноеИмяМета);
						КонецЕсли;
					Исключение
					КонецПопытки;
					
					// Использование (CommonAttributeUse) -> строка значения перечисления
					Попытка
						ЭлементJSON.Вставить("Использование", Строка(ЭлементСостава.Использование));
					Исключение
					КонецПопытки;
					
					// УсловноеРазделение (ConditionalSeparation): Константа/Булево/реквизит и т.д.
					Если НужноПолеУсловноеРазделение Тогда
						УсловиеJSON = Неопределено;
						Попытка
							Условие = ЭлементСостава.УсловноеРазделение;
							Если Условие <> Неопределено Тогда
								УсловиеJSON = ПолноеИмяМетаданныхЕслиВозможно(Условие);
								Если УсловиеJSON = Неопределено Тогда
									УсловиеJSON = БезопасноеЗначениеДляПередачи(Условие, Глубина + 1);
								КонецЕсли;
							КонецЕсли;
						Исключение
						КонецПопытки;
						ЭлементJSON.Вставить("УсловноеРазделение", УсловиеJSON);
					КонецЕсли;
					
					Если ЭлементJSON.Количество() > 0 Тогда
						Результат.Добавить(ЭлементJSON);
					КонецЕсли;
				КонецЦикла;
			Исключение
				// Если по какой-то причине не итерируется - оставим как пустой массив
			КонецПопытки;
			
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Если тип недоступен в данной версии платформы - игнорируем
	КонецПопытки;
	
	// СоставПланаОбмена (ExchangePlanContent) -> массив элементов состава
	// Каждый элемент: {Метаданные, АвтоРегистрация}
	Попытка
		Если ТипЗначения = Тип("СоставПланаОбмена") Тогда
			Результат = Новый Массив;
			Попытка
				Для Каждого ЭлементСостава Из Значение Цикл
					ЭлементJSON = Новый Структура;
					
					// Метаданные (MetadataObject) -> ПолноеИмя() если возможно
					ПолноеИмяМета = Неопределено;
					Попытка
						Мета = ЭлементСостава.Метаданные;
						Если Мета <> Неопределено Тогда
							ПолноеИмяМета = ПолноеИмяМетаданныхЕслиВозможно(Мета);
							Если ПолноеИмяМета = Неопределено Тогда
								Попытка
									ПолноеИмяМета = Мета.ПолноеИмя();
								Исключение
									ПолноеИмяМета = Строка(Мета);
								КонецПопытки;
							КонецЕсли;
						КонецЕсли;
					Исключение
					КонецПопытки;
					ЭлементJSON.Вставить("Метаданные", ПолноеИмяМета);
					
					// АвтоРегистрация (AutoChangeRecord) -> строка значения перечисления
					АвтоРегистрацияJSON = Неопределено;
					Попытка
						АвтоРегистрацияJSON = Строка(ЭлементСостава.АвтоРегистрация);
					Исключение
					КонецПопытки;
					ЭлементJSON.Вставить("АвтоРегистрация", АвтоРегистрацияJSON);
					
					Если ЭлементJSON.Количество() > 0 Тогда
						Результат.Добавить(ЭлементJSON);
					КонецЕсли;
				КонецЦикла;
			Исключение
				// Если по какой-то причине не итерируется - оставим как пустой массив
			КонецПопытки;
			
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Если тип недоступен в данной версии платформы - игнорируем
	КонецПопытки;
	
	// СоставФункциональнойОпции (FunctionalOptionContent) -> массив элементов состава
	// Каждый элемент: {Объект}
	Попытка
		Если ТипЗначения = Тип("СоставФункциональнойОпции") Тогда
			Результат = Новый Массив;
			Попытка
				Для Каждого ЭлементСостава Из Значение Цикл
					ЭлементJSON = Новый Структура;
					
					// Объект (MetadataObject) -> ПолноеИмя() если возможно
					ПолноеИмяОбъекта = Неопределено;
					Попытка
						ОбъектМета = ЭлементСостава.Объект;
						Если ОбъектМета <> Неопределено Тогда
							ПолноеИмяОбъекта = ПолноеИмяМетаданныхЕслиВозможно(ОбъектМета);
							Если ПолноеИмяОбъекта = Неопределено Тогда
								Попытка
									ПолноеИмяОбъекта = ОбъектМета.ПолноеИмя();
								Исключение
									ПолноеИмяОбъекта = Строка(ОбъектМета);
								КонецПопытки;
							КонецЕсли;
						КонецЕсли;
					Исключение
					КонецПопытки;
					ЭлементJSON.Вставить("Объект", ПолноеИмяОбъекта);
					
					Если ЭлементJSON.Количество() > 0 Тогда
						Результат.Добавить(ЭлементJSON);
					КонецЕсли;
				КонецЦикла;
			Исключение
				// Если по какой-то причине не итерируется - оставим как пустой массив
			КонецПопытки;
			
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Если тип недоступен в данной версии платформы - игнорируем
	КонецПопытки;
  	
	// ОписаниеЗащитыОтОпасныхДействий (UnsafeOperationProtectionDescription) -> Булево
	// Возвращаем флаг ПредупреждатьОбОпасныхДействиях, чтобы в JSON было true/false.
	Если Строка(ТипЗначения) = "ОписаниеЗащитыОтОпасныхДействий" Тогда
		Попытка
			Возврат (Значение.ПредупреждатьОбОпасныхДействиях = Истина);
		Исключение
			// Если свойство недоступно (например, платформа < 8.3.8) - вернём строковое представление
		КонецПопытки;
	КонецЕсли;
 	
	Попытка
		Возврат Строка(Значение);
	Исключение
		Возврат "<" + Строка(ТипЗначения) + ">";
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыполнитьЗапрос(ТекстЗапроса, ПараметрыЗапроса, Лимит, ВключитьСхему = Ложь)
	
	Попытка
		
		Запрос = Новый Запрос(ТекстЗапроса);
		
		// Установка параметров запроса
		Если ТипЗнч(ПараметрыЗапроса) = Тип("Соответствие") Тогда
			Для Каждого Параметр Из ПараметрыЗапроса Цикл
				ЗначениеПараметра = Параметр.Значение;
				// Конвертация ISO 8601 строки в Дату
				Если ТипЗнч(ЗначениеПараметра) = Тип("Строка") И СтрокаСоответствуетISO8601ДатаВремя(ЗначениеПараметра) Тогда
					Попытка
						ЗначениеПараметра = XMLЗначение(Тип("Дата"), ЗначениеПараметра);
					Исключение
					КонецПопытки;
				КонецЕсли;
				// Конвертация описания объекта обратно в ссылку
				ЗначениеПараметра = ОбъектИнформационнойБазыПоОписанию(ЗначениеПараметра);
				Запрос.УстановитьПараметр(Параметр.Ключ, ЗначениеПараметра);
			КонецЦикла;
		ИначеЕсли ТипЗнч(ПараметрыЗапроса) = Тип("Структура") Тогда
			Для Каждого Параметр Из ПараметрыЗапроса Цикл
				ЗначениеПараметра = Параметр.Значение;
				// Конвертация ISO 8601 строки в Дату
				Если ТипЗнч(ЗначениеПараметра) = Тип("Строка") И СтрокаСоответствуетISO8601ДатаВремя(ЗначениеПараметра) Тогда
					Попытка
						ЗначениеПараметра = XMLЗначение(Тип("Дата"), ЗначениеПараметра);
					Исключение
					КонецПопытки;
				КонецЕсли;
				// Конвертация описания объекта обратно в ссылку
				ЗначениеПараметра = ОбъектИнформационнойБазыПоОписанию(ЗначениеПараметра);
				Запрос.УстановитьПараметр(Параметр.Ключ, ЗначениеПараметра);
			КонецЦикла;
		КонецЕсли;
		
		РезультатЗапроса = Запрос.Выполнить();
		
		// Формирование схемы если запрошено
		Схема = Неопределено;
		Если ВключитьСхему Тогда
			Схема = СформироватьСхемуРезультата(РезультатЗапроса);
		КонецЕсли;
		
		Выборка = РезультатЗапроса.Выбрать();
		
		МассивРезультата = Новый Массив;
		Счетчик = 0;
		
		Пока Выборка.Следующий() И Счетчик < Лимит Цикл
			
			СтрокаРезультата = Новый Структура;
			
			Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
				Значение = Выборка[Колонка.Имя];
				СтрокаРезультата.Вставить(Колонка.Имя, БезопасноеЗначениеДляПередачи(Значение));
			КонецЦикла;
			
			МассивРезультата.Добавить(СтрокаРезультата);
			Счетчик = Счетчик + 1;
			
		КонецЦикла;
		
		// Возврат результата с учетом параметра ВключитьСхему
		Если ВключитьСхему Тогда
			Возврат Новый Структура("success, data, schema", Истина, МассивРезультата, Схема);
		Иначе
			Возврат Новый Структура("success, data", Истина, МассивРезультата);
		КонецЕсли;
		
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции

// Формирует схему результата запроса с информацией о типах колонок
//
// Параметры:
//  РезультатЗапроса - РезультатЗапроса - результат выполнения запроса
//
// Возвращаемое значение:
//  Структура - схема с полем columns (массив описаний колонок)
//    * columns - Массив - массив структур с описанием колонок:
//      ** name - Строка - имя колонки
//      ** types - Массив - массив строковых представлений типов (отсортирован, без дублей)
//
&НаСервереБезКонтекста
Функция СформироватьСхемуРезультата(РезультатЗапроса)
	
	МассивКолонок = Новый Массив;
	
	Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
		
		МассивТипов = Новый Массив;
		
		Для Каждого Тип Из Колонка.ТипЗначения.Типы() Цикл
			СтроковыйТип = ПолучитьСтроковоеПредставлениеТипаДляСхемы(Тип);
			МассивТипов.Добавить(СтроковыйТип);
		КонецЦикла;
		
		// Сортировка и удаление дублей
		МассивТипов = УдалитьДублиИСортировать(МассивТипов);
		
		ОписаниеКолонки = Новый Структура("name, types", Колонка.Имя, МассивТипов);
		МассивКолонок.Добавить(ОписаниеКолонки);
		
	КонецЦикла;
	
	Возврат Новый Структура("columns", МассивКолонок);
	
КонецФункции

// Получает строковое представление типа для схемы результата запроса
//
// Параметры:
//  Тип - Тип - тип для преобразования
//
// Возвращаемое значение:
//  Строка - строковое представление типа
//
&НаСервереБезКонтекста
Функция ПолучитьСтроковоеПредставлениеТипаДляСхемы(Тип)
	
	// Используем существующую функцию ЭтоСсылка для проверки ссылочного типа
	// и существующую СтроковоеПредставлениеТипа для получения полного имени
	
	Если ЭтоСсылка(Тип) Тогда
		// Для ссылочных типов используем СтроковоеПредставлениеТипа
		// Возвращает полное имя: "СправочникСсылка.Контрагенты"
		Возврат СтроковоеПредставлениеТипа(Тип);
	Иначе
		// Для примитивных типов используем Строка(Тип)
		// Возвращает: "Строка", "Число", "Дата", "Булево"
		Возврат Строка(Тип);
	КонецЕсли;
	
КонецФункции

// Формирует схему объекта (get_object_by_link) для анонимизации по метаданным
//
// Параметры:
//  МетаданныеОбъекта  - объект метаданных (МетаданныеОбъекта.НайтиПоПолномуИмени или Таблицы.Найти)
//  ПолноеИмяОбъекта   - Строка - полное имя, напр. "Справочник.Контрагенты"
//  ЭтоВнешняяТаблица  - Булево
//
// Возвращаемое значение:
//  Структура - {columns: Массив, tabular_sections: Соответствие}
//
&НаСервереБезКонтекста
Функция СформироватьСхемуОбъектаДляАнонимизации(МетаданныеОбъекта, ПолноеИмяОбъекта, ЭтоВнешняяТаблица)

	МассивКолонок = Новый Массив;

	// _presentation — только для обычных объектов.
	// Для внешних источников _presentation = "" — в схему не добавляем.
	// source = ПолноеИмяОбъекта + ".Ссылка" — чтобы exact source rules
	// (напр. Справочник.Контрагенты.Ссылка) работали и для _presentation.
	Если НЕ ЭтоВнешняяТаблица Тогда
		ЧастиИмени = РазделитьСтрокуПоРазделителю(ПолноеИмяОбъекта, ".");
		ТипСсылкиStr = "";
		Если ЧастиИмени.Количество() >= 2 Тогда
			ТипСсылкиStr = ЧастиИмени[0] + "Ссылка." + ЧастиИмени[1];
		КонецЕсли;
		МассивИсточников = Новый Массив;
		МассивИсточников.Добавить(ПолноеИмяОбъекта + ".Ссылка");
		МассивТипов = Новый Массив;
		МассивТипов.Добавить(ТипСсылкиStr);
		МассивКолонок.Добавить(Новый Структура("name, types, sources",
			"_presentation", МассивТипов, МассивИсточников));
	КонецЕсли;

	// Стандартные реквизиты (Код, Наименование, Дата, Номер) — только для обычных объектов
	Если НЕ ЭтоВнешняяТаблица Тогда
		Попытка
			Для Каждого СтдРеквизит Из МетаданныеОбъекта.СтандартныеРеквизиты Цикл
				МассивТипов = Новый Массив;
				Для Каждого Тип Из СтдРеквизит.Тип.Типы() Цикл
					МассивТипов.Добавить(ПолучитьСтроковоеПредставлениеТипаДляСхемы(Тип));
				КонецЦикла;
				МассивИсточников = Новый Массив;
				МассивИсточников.Добавить(ПолноеИмяОбъекта + "." + СтдРеквизит.Имя);
				МассивКолонок.Добавить(Новый Структура("name, types, sources",
					СтдРеквизит.Имя, МассивТипов, МассивИсточников));
			КонецЦикла;
		Исключение
		КонецПопытки;
	КонецЕсли;

	// Пользовательские реквизиты / поля внешней таблицы
	Если ЭтоВнешняяТаблица Тогда
		КоллекцияРеквизитов = МетаданныеОбъекта.Поля;
	Иначе
		КоллекцияРеквизитов = МетаданныеОбъекта.Реквизиты;
	КонецЕсли;
	Для Каждого Реквизит Из КоллекцияРеквизитов Цикл
		МассивТипов = Новый Массив;
		Попытка
			Для Каждого Тип Из Реквизит.Тип.Типы() Цикл
				МассивТипов.Добавить(ПолучитьСтроковоеПредставлениеТипаДляСхемы(Тип));
			КонецЦикла;
		Исключение
		КонецПопытки;
		МассивИсточников = Новый Массив;
		МассивИсточников.Добавить(ПолноеИмяОбъекта + "." + Реквизит.Имя);
		МассивКолонок.Добавить(Новый Структура("name, types, sources",
			Реквизит.Имя, МассивТипов, МассивИсточников));
	КонецЦикла;

	// Табличные части — только для обычных объектов
	СоответствиеТЧ = Новый Соответствие;
	Если НЕ ЭтоВнешняяТаблица Тогда
		Попытка
			Для Каждого ТЧ Из МетаданныеОбъекта.ТабличныеЧасти Цикл
				МассивКолТЧ = Новый Массив;
				Для Каждого Реквизит Из ТЧ.Реквизиты Цикл
					МассивТипов = Новый Массив;
					Для Каждого Тип Из Реквизит.Тип.Типы() Цикл
						МассивТипов.Добавить(ПолучитьСтроковоеПредставлениеТипаДляСхемы(Тип));
					КонецЦикла;
					МассивИсточников = Новый Массив;
					МассивИсточников.Добавить(ПолноеИмяОбъекта + "." + ТЧ.Имя + "." + Реквизит.Имя);
					МассивКолТЧ.Добавить(Новый Структура("name, types, sources",
						Реквизит.Имя, МассивТипов, МассивИсточников));
				КонецЦикла;
				СоответствиеТЧ.Вставить(ТЧ.Имя, Новый Структура("columns", МассивКолТЧ));
			КонецЦикла;
		Исключение
		КонецПопытки;
	КонецЕсли;

	Возврат Новый Структура("columns, tabular_sections", МассивКолонок, СоответствиеТЧ);

КонецФункции

// Удаляет дубликаты из массива строк и сортирует его
//
// Параметры:
//  МассивСтрок - Массив - массив строк для обработки
//
// Возвращаемое значение:
//  Массив - отсортированный массив уникальных строк
//
&НаСервереБезКонтекста
Функция УдалитьДублиИСортировать(МассивСтрок)
	
	// Используем соответствие для удаления дублей
	Соответствие = Новый Соответствие;
	Для Каждого Строка Из МассивСтрок Цикл
		Соответствие.Вставить(Строка, Истина);
	КонецЦикла;
	
	// Собираем уникальные значения
	УникальныеЗначения = Новый Массив;
	Для Каждого КлючЗначение Из Соответствие Цикл
		УникальныеЗначения.Добавить(КлючЗначение.Ключ);
	КонецЦикла;
	
	// Сортировка через СписокЗначений
	СписокДляСортировки = Новый СписокЗначений;
	Для Каждого Значение Из УникальныеЗначения Цикл
		СписокДляСортировки.Добавить(Значение);
	КонецЦикла;
	СписокДляСортировки.СортироватьПоЗначению();
	
	// Результат
	Результат = Новый Массив;
	Для Каждого Элемент Из СписокДляСортировки Цикл
		Результат.Добавить(Элемент.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ВЫПОЛНЕНИЕ ПРОИЗВОЛЬНОГО КОДА
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ВыполнитьКод(Код)
	
	Попытка
		
		Результат = Неопределено;
		Выполнить(Код);
		
		Возврат Новый Структура("success, data", Истина, БезопасноеЗначениеДляПередачи(Результат));
		
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции

// Упрощённый клиентский сериализатор результата для execute_code(execution_context="client").
// Не обрабатывает серверные типы метаданных (ОписаниеТипов и т.д.) — они не возникают
// при выполнении пользовательского кода на клиенте; для них используется fallback Строка().
&НаКлиенте
Функция БезопасноеЗначениеДляПередачиКлиент(Значение, Глубина = 0)

	Если Глубина > 5 Тогда
		Возврат "<max_depth>";
	КонецЕсли;

	Если Значение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	ТипЗначения = ТипЗнч(Значение);

	Если ТипЗначения = Тип("Строка")
		Или ТипЗначения = Тип("Число")
		Или ТипЗначения = Тип("Булево")
		Или ТипЗначения = Тип("Дата") Тогда
		Возврат Значение;
	КонецЕсли;

	Если ТипЗначения = Тип("УникальныйИдентификатор") Тогда
		Возврат Строка(Значение);
	КонецЕсли;

	Если ТипЗначения = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого Элемент Из Значение Цикл
			Результат.Добавить(БезопасноеЗначениеДляПередачиКлиент(Элемент, Глубина + 1));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	Если ТипЗначения = Тип("ФиксированныйМассив") Тогда
		Результат = Новый Массив;
		Попытка
			Для Каждого Элемент Из Значение Цикл
				Результат.Добавить(БезопасноеЗначениеДляПередачиКлиент(Элемент, Глубина + 1));
			КонецЦикла;
		Исключение
		КонецПопытки;
		Возврат Результат;
	КонецЕсли;

	Если ТипЗначения = Тип("Структура") Тогда
		Результат = Новый Структура;
		Для Каждого Пара Из Значение Цикл
			Результат.Вставить(Пара.Ключ, БезопасноеЗначениеДляПередачиКлиент(Пара.Значение, Глубина + 1));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	Если ТипЗначения = Тип("Соответствие") Тогда
		Результат = Новый Массив;
		Для Каждого Пара Из Значение Цикл
			Результат.Добавить(Новый Структура(
				"key, value",
				БезопасноеЗначениеДляПередачиКлиент(Пара.Ключ, Глубина + 1),
				БезопасноеЗначениеДляПередачиКлиент(Пара.Значение, Глубина + 1)
			));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	Попытка
		Если ТипЗначения = Тип("ТаблицаЗначений") Тогда
			Результат = Новый Массив;
			Для Каждого СтрокаТаблицы Из Значение Цикл
				СтрокаРезультата = Новый Структура;
				Для Каждого Колонка Из Значение.Колонки Цикл
					СтрокаРезультата.Вставить(Колонка.Имя,
						БезопасноеЗначениеДляПередачиКлиент(СтрокаТаблицы[Колонка.Имя], Глубина + 1));
				КонецЦикла;
				Результат.Добавить(СтрокаРезультата);
			КонецЦикла;
			Возврат Результат;
		КонецЕсли;
	Исключение
		// Тип недоступен в данном клиентском контексте — пропускаем
	КонецПопытки;

	// Всё остальное (ссылки, перечисления и т.д.) — строковое представление
	Попытка
		Возврат Строка(Значение);
	Исключение
		Возврат "<" + Строка(ТипЗначения) + ">";
	КонецПопытки;

КонецФункции

&НаКлиенте
Функция ВыполнитьКодНаКлиенте(Код)

	// Защита: блокировать код, обращающийся к чувствительным атрибутам деанонимизации.
	// Безусловно — данные могли быть записаны ранее, флаг анонимизации не имеет значения.
	// НРег: идентификаторы в 1С регистронезависимы, проверяем по нижнему регистру.
	КодНРег = НРег(Код);
	Если Найти(КодНРег, "текстдлядеанонимизации") > 0
		Или Найти(КодНРег, "полетекстдлядеанонимизации") > 0 Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Code execution blocked: access to protected attributes 'ТекстДляДеАнонимизации', 'ПолеТекстДляДеАнонимизации' is not allowed");
	КонецЕсли;

	Попытка
		Результат = Неопределено;
		Выполнить(Код);
		Возврат Новый Структура("success, data", Истина,
			БезопасноеЗначениеДляПередачиКлиент(Результат));
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ПОЛУЧЕНИЕ МЕТАДАННЫХ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ПолучитьМетаданные(Фильтр, ТипМетаданных = "", МаскаИмени = "", Лимит = 100, Смещение = 0, Секции = Неопределено, ИмяРасширения = Неопределено, КэшПараметры = Неопределено, МаскаАтрибута = "")

	Попытка

		// extension_name="" → список расширений (ВСЕГДА, независимо от других параметров)
		Если ИмяРасширения <> Неопределено И ПустаяСтрока(ИмяРасширения) Тогда
			Возврат ПолучитьСписокРасширений();
		КонецЕсли;

		// attribute_mask → режим поиска по атрибутам (после проверки extension_name="")
		Если НЕ ПустаяСтрока(МаскаАтрибута) Тогда
			// sections несовместимы с attribute_mask (тот же запрет что и в Python-proxy)
			Если Секции <> Неопределено И ТипЗнч(Секции) = Тип("Массив") И Секции.Количество() > 0 Тогда
				Возврат Новый Структура("success, error", Ложь,
					"attribute_mask is incompatible with sections. " +
					"Use round-trip: pass data[0][""ПолноеИмя""] as filter, then use sections.");
			КонецЕсли;
			Возврат ПолучитьМетаданныеПоАтрибуту(МаскаАтрибута, Фильтр,
				ТипМетаданных, МаскаИмени, Лимит, Смещение, ИмяРасширения, КэшПараметры);
		КонецЕсли;

		// Конкретное расширение (без attribute_mask)
		Если ИмяРасширения <> Неопределено Тогда
			Возврат ПолучитьМетаданныеРасширения(ИмяРасширения, Фильтр,
						ТипМетаданных, МаскаИмени, Лимит, Смещение, Секции, КэшПараметры);
		КонецЕсли;

		// Основная конфигурация
		Если ПустаяСтрока(Фильтр) Тогда
			ТипТипМетаданных = ТипЗнч(ТипМетаданных);
			НетТипаМетаданных = (ТипМетаданных = Неопределено) Или (ТипТипМетаданных = Тип("Строка") И ПустаяСтрока(ТипМетаданных));
			Если НетТипаМетаданных И ПустаяСтрока(МаскаИмени) Тогда
				Возврат ПолучитьСводкуМетаданных();
			Иначе
				Если НетТипаМетаданных Тогда
					ТипМетаданных = "*";
				КонецЕсли;
				Возврат ПолучитьСписокМетаданных(ТипМетаданных, МаскаИмени, Лимит, Смещение);
			КонецЕсли;
		Иначе
			Возврат ПолучитьДетальныеМетаданные(Фильтр, Секции);
		КонецЕсли;

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// РАБОТА С РАСШИРЕНИЯМИ КОНФИГУРАЦИИ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ПолучитьКоллекциюИзРасширения(ОбъектРасширения, ИмяКоллекции)
	// Унифицированный доступ к коллекциям метаданных внутри объекта расширения.
	// В разных версиях/контекстах платформа может по-разному предоставлять доступ:
	// - через свойство (ОбъектРасширения.<Коллекция>)
	// - через индексатор (ОбъектРасширения[<Коллекция>])
	
	Коллекция = Неопределено;
	
	Попытка
		Коллекция = Вычислить("ОбъектРасширения." + ИмяКоллекции);
	Исключение
		Коллекция = Неопределено;
	КонецПопытки;
	
	Если Коллекция = Неопределено Тогда
		Попытка
			Коллекция = ОбъектРасширения[ИмяКоллекции];
		Исключение
			Коллекция = Неопределено;
		КонецПопытки;
	КонецЕсли;
	
	Возврат Коллекция;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСписокРасширений()
	// Возвращает список всех подключённых расширений конфигурации

	Попытка
		// Платформенный API для расширений находится в глобальном контексте:
		//   РасширенияКонфигурации (ConfigurationExtensionsManager)
		// В рантайм-метаданных (Метаданные.…) этого поля может не быть.
		МассивРасширений = Неопределено;
		Попытка
			МассивРасширений = РасширенияКонфигурации.Получить();
		Исключение
			МассивРасширений = Неопределено;
		КонецПопытки;

		Если МассивРасширений = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Расширения конфигурации недоступны в данной версии платформы");
		КонецЕсли;

		СписокРасширений = Новый Массив;

		Для Каждого ТекРасширение Из МассивРасширений Цикл
			ИнфоРасширения = Новый Структура;
			
			// Возвращаем свойства объекта РасширениеКонфигурации (ConfigurationExtension).
			// Количество объектов расширения намеренно НЕ возвращаем: это отдельная задача
			// и может зависеть от способа анализа данных расширения.
			СвойстваРасширения = Новый Массив;
			СвойстваРасширения.Добавить("Имя");
			СвойстваРасширения.Добавить("Синоним");
			СвойстваРасширения.Добавить("Версия");
			СвойстваРасширения.Добавить("Активно");
			СвойстваРасширения.Добавить("БезопасныйРежим");
			СвойстваРасширения.Добавить("ГлавныйУзел");
			СвойстваРасширения.Добавить("ЗащитаОтОпасныхДействий");
			СвойстваРасширения.Добавить("ИспользоватьОсновныеРолиДляВсехПользователей");
			СвойстваРасширения.Добавить("ИспользуетсяВРаспределеннойИнформационнойБазе");
			СвойстваРасширения.Добавить("Назначение");
			СвойстваРасширения.Добавить("ОбластьДействия");
			СвойстваРасширения.Добавить("ОсновныеРоли");
			СвойстваРасширения.Добавить("УникальныйИдентификатор");
			СвойстваРасширения.Добавить("ХешСумма");
			
			Для Каждого ИмяСвойства Из СвойстваРасширения Цикл
				ЗначениеСвойства = Неопределено;
				Попытка
					ЗначениеСвойства = Вычислить("ТекРасширение." + ИмяСвойства);
				Исключение
					ЗначениеСвойства = Неопределено;
				КонецПопытки;
				
				// Спец. обработка: ЗащитаОтОпасныхДействий.
				// По справке это не булево, а объект ОписаниеЗащитыОтОпасныхДействий,
				// но в API мы хотим возвращать простое true/false.
				Если ИмяСвойства = "ЗащитаОтОпасныхДействий" Тогда
					БулевоЗначение = Неопределено;
					Попытка
						Если ЗначениеСвойства <> Неопределено Тогда
							БулевоЗначение = (ЗначениеСвойства.ПредупреждатьОбОпасныхДействиях = Истина);
						КонецЕсли;
					Исключение
						БулевоЗначение = Неопределено;
					КонецПопытки;
					ЗначениеСвойства = БулевоЗначение;
				КонецЕсли;
				
				// Вставляем ключ всегда, даже если значение Неопределено (будет null в JSON).
				ИнфоРасширения.Вставить(ИмяСвойства, БезопасноеЗначениеДляПередачи(ЗначениеСвойства));
			КонецЦикла;

			СписокРасширений.Добавить(ИнфоРасширения);
		КонецЦикла;

		Возврат Новый Структура("success, data", Истина, СписокРасширений);

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// КЭШ МЕТАДАННЫХ РАСШИРЕНИЙ (ОбъектМетаданныхКонфигурация)
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ПолучитьКэшМетаданныхРасширений()
	// В модуле формы (особенно при проверке "Толстый клиент (обычное приложение)")
	// серверные модульные переменные недоступны/нестабильны для &НаСервереБезКонтекста.
	// Поэтому кэш здесь отключён: возвращаем новый контейнер, который не сохраняется между вызовами.
	Возврат Новый Соответствие;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКлючКэшаРасширения(Расширение)
	Ключ = "";
	Попытка
		Если Расширение.УникальныйИдентификатор <> Неопределено Тогда
			Ключ = Строка(Расширение.УникальныйИдентификатор);
		КонецЕсли;
	Исключение
		Ключ = "";
	КонецПопытки;

	Если ПустаяСтрока(Ключ) Тогда
		Попытка
			Ключ = Расширение.Имя;
		Исключение
			Ключ = "";
		КонецПопытки;
	КонецЕсли;

	Возврат Ключ;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьХешРасширенияСтрокой(Расширение)
	Хеш = "";
	Попытка
		Хеш = Строка(Расширение.ХешСумма);
	Исключение
		Хеш = "";
	КонецПопытки;
	Возврат Хеш;
КонецФункции

&НаСервереБезКонтекста
Функция ПостроитьИндексМетаданныхРасширения(МетаданныеРасширения)
	// Возвращает ТаблицаЗначений со всеми объектами расширения (верхний уровень),
	// отсортированную по ПолноеИмя (через SortKey) для стабильной пагинации.
	Таблица = Новый ТаблицаЗначений;
	Таблица.Колонки.Добавить("Тип");
	Таблица.Колонки.Добавить("Имя");
	Таблица.Колонки.Добавить("Синоним");
	Таблица.Колонки.Добавить("ПолноеИмя");
	Таблица.Колонки.Добавить("SortKey");

	КорневыеТипы = ПолучитьКорневыеТипыМетаданных();
	Для Каждого ОписаниеТипа Из КорневыеТипы Цикл

		Коллекция = Неопределено;
		Попытка
			Коллекция = Вычислить("МетаданныеРасширения." + ОписаниеТипа.Коллекция);
		Исключение
			Коллекция = Неопределено;
		КонецПопытки;

		Если Коллекция = Неопределено Тогда
			Продолжить;
		КонецЕсли;

		Для Каждого ОбъектМетаданных Из Коллекция Цикл

			Имя = "";
			Синоним = "";
			ПолноеИмя = "";

			Попытка
				Имя = ОбъектМетаданных.Имя;
			Исключение
				Имя = "";
			КонецПопытки;

			Попытка
				Синоним = ОбъектМетаданных.Синоним;
			Исключение
				Синоним = "";
			КонецПопытки;

			Попытка
				ПолноеИмя = ОбъектМетаданных.ПолноеИмя();
			Исключение
				ПолноеИмя = "";
			КонецПопытки;

			Если ПустаяСтрока(ПолноеИмя) Тогда
				Если НЕ ПустаяСтрока(Имя) Тогда
					ПолноеИмя = ОписаниеТипа.Тип + "." + Имя;
				Иначе
					ПолноеИмя = ОписаниеТипа.Тип;
				КонецЕсли;
			КонецЕсли;

			Стр = Таблица.Добавить();
			Стр.Тип = ОписаниеТипа.Тип;
			Стр.Имя = Имя;
			Стр.Синоним = Синоним;
			Стр.ПолноеИмя = ПолноеИмя;
			Стр.SortKey = ВРег(ПолноеИмя);

		КонецЦикла;
	КонецЦикла;

	Таблица.Сортировать("SortKey, ПолноеИмя");
	Возврат Таблица;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьМетаданныеКонфигурацииРасширения(Расширение, КэшПараметры = Неопределено)
	// Возвращает структуру:
	// - success: Булево
	// - metadata: ОбъектМетаданныхКонфигурация | Неопределено
	// - index: ТаблицаЗначений | Неопределено
	// - is_empty: Булево (Истина если расширение пустое: ПолучитьДанные() вернуло Неопределено без ошибки)
	// - error: Строка (если success=Ложь)
	// - hash: Строка (ХешСумма расширения)
	// - data_token: адрес ВременногоХранилища для ДвоичныхДанных (best effort)
	Рез = Новый Структура("success, metadata, index, is_empty, error, hash, data_token",
		Ложь, Неопределено, Неопределено, Ложь, "", "", Неопределено);

	Если Расширение = Неопределено Тогда
		Рез.error = "Расширение не задано";
		Возврат Рез;
	КонецЕсли;

	Хеш = ПолучитьХешРасширенияСтрокой(Расширение);
	Рез.hash = Хеш;

	// Получаем бинарные данные расширения
	Данные = Неопределено;
	ОшибкаДанных = "";
	
	// 1) Попытка использовать токен кэша (ВременноеХранилище) если хеш совпадает
	Если КэшПараметры <> Неопределено Тогда
		Попытка
			Если КэшПараметры.Свойство("hash")
				И КэшПараметры.hash = Хеш
				И КэшПараметры.Свойство("data_token")
				И КэшПараметры.data_token <> Неопределено Тогда
				
				ТокенДанных = КэшПараметры.data_token;
				Попытка
					Данные = ПолучитьИзВременногоХранилища(ТокенДанных);
					Рез.data_token = ТокенДанных;
				Исключение
					Данные = Неопределено;
					Рез.data_token = Неопределено;
				КонецПопытки;
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	// 2) Если токена нет/протух, получаем данные через API расширения
	Попытка
		Если Данные = Неопределено Тогда
			Данные = Расширение.ПолучитьДанные();
		КонецЕсли;
	Исключение
		ОшибкаДанных = ОписаниеОшибки();
		Данные = Неопределено;
	КонецПопытки;

	Если Данные = Неопределено Тогда
		Если НЕ ПустаяСтрока(ОшибкаДанных) Тогда
			Рез.error = "Не удалось получить данные расширения: " + ОшибкаДанных;
			Возврат Рез;
		КонецЕсли;

		// Пустое расширение (GetData() вернуло Неопределено без ошибки)
		Рез.success = Истина;
		Рез.is_empty = Истина;

		Возврат Рез;
	КонецЕсли;
	
	// 3) Кладём ДвоичныеДанные во ВременноеХранилище (best effort), если ещё нет токена
	Если Рез.data_token = Неопределено Тогда
		Попытка
			Рез.data_token = ПоместитьВоВременноеХранилище(Данные);
		Исключение
			Рез.data_token = Неопределено;
		КонецПопытки;
	КонецЕсли;

	// Парсим метаданные расширения
	МетаданныеРасширения = Неопределено;
	ОшибкаПарсинга = "";
	Попытка
		МетаданныеРасширения = Новый ОбъектМетаданныхКонфигурация(Данные);
	Исключение
		ОшибкаПарсинга = ОписаниеОшибки();
		МетаданныеРасширения = Неопределено;
	КонецПопытки;

	Если МетаданныеРасширения = Неопределено Тогда
		Рез.error = "Не удалось прочитать метаданные расширения (ОбъектМетаданныхКонфигурация): " + ОшибкаПарсинга;
		Возврат Рез;
	КонецЕсли;

	Индекс = ПостроитьИндексМетаданныхРасширения(МетаданныеРасширения);

	Рез.success = Истина;
	Рез.metadata = МетаданныеРасширения;
	Рез.index = Индекс;

	Возврат Рез;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьМетаданныеРасширения(ИмяРасширения, Фильтр, ТипМетаданных, МаскаИмени, Лимит, Смещение, Секции, КэшПараметры = Неопределено)
	// Маршрутизатор для работы с метаданными конкретного расширения

	Попытка
		// 1. Проверяем доступность API расширений
		МассивРасширений = Неопределено;
		Попытка
			Отбор = Новый Структура("Имя", ИмяРасширения);
			МассивРасширений = РасширенияКонфигурации.Получить(Отбор);
		Исключение
			МассивРасширений = Неопределено;
		КонецПопытки;

		Если МассивРасширений = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Расширения конфигурации недоступны в данной версии платформы");
		КонецЕсли;

		// 2. Находим расширение по имени
		НайденноеРасширение = Неопределено;
		Если ТипЗнч(МассивРасширений) = Тип("Массив") И МассивРасширений.Количество() > 0 Тогда
			НайденноеРасширение = МассивРасширений[0];
		КонецЕсли;

		// 3. Обработка ошибки: расширение не найдено
		Если НайденноеРасширение = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Расширение не найдено: " + ИмяРасширения);
		КонецЕсли;

		// 4. Выбор режима: details или list
		Если НЕ ПустаяСтрока(Фильтр) Тогда
			Возврат ПолучитьДетальныеМетаданныеРасширения(НайденноеРасширение, Фильтр, Секции, КэшПараметры);
		Иначе
			// Новый контракт (симметрия с основной конфигурацией):
			// Если не задано filter/meta_type/name_mask — возвращаем summary по типам внутри расширения.
			ТипТипМетаданных = ТипЗнч(ТипМетаданных);
			НетТипаМетаданных = (ТипМетаданных = Неопределено)
				Или (ТипТипМетаданных = Тип("Строка") И ПустаяСтрока(ТипМетаданных));

			Если ТипТипМетаданных = Тип("Массив") Тогда
				Попытка
					Если ТипМетаданных.Количество() = 0 Тогда
						НетТипаМетаданных = Истина;
					КонецЕсли;
				Исключение
				КонецПопытки;
			КонецЕсли;

			Если НетТипаМетаданных И ПустаяСтрока(МаскаИмени) Тогда
				Возврат ПолучитьСводкуМетаданныхРасширения(НайденноеРасширение, КэшПараметры);
			КонецЕсли;

			Возврат ПолучитьСписокМетаданныхРасширения(НайденноеРасширение,
						ТипМетаданных, МаскаИмени, Лимит, Смещение, КэшПараметры);
		КонецЕсли;

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСводкуМетаданныхРасширения(Расширение, КэшПараметры = Неопределено)
	// Возвращает summary по корневым типам ВНУТРИ расширения:
	// {success: true, data: [{Тип, Количество}...], configuration: {...}, extension: "ИмяРасширения"}

	Попытка
		ТекущийХеш = ПолучитьХешРасширенияСтрокой(Расширение);
		ТокенДанных = Неопределено;
		ТокенИндекса = Неопределено;
		Индекс = Неопределено;

		// 1) Попытка использовать кэш индекса (ВременноеХранилище) если хеш совпадает
		Если КэшПараметры <> Неопределено Тогда
			Попытка
				Если КэшПараметры.Свойство("hash")
					И КэшПараметры.hash = ТекущийХеш Тогда

					Если КэшПараметры.Свойство("data_token") Тогда
						ТокенДанных = КэшПараметры.data_token;
					КонецЕсли;

					Если КэшПараметры.Свойство("index_token")
						И КэшПараметры.index_token <> Неопределено Тогда

						ТокенИндекса = КэшПараметры.index_token;
						КэшИндекс = Неопределено;
						Попытка
							КэшИндекс = ПолучитьИзВременногоХранилища(ТокенИндекса);
						Исключение
							КэшИндекс = Неопределено;
						КонецПопытки;

						Если КэшИндекс <> Неопределено
							И ТипЗнч(КэшИндекс) = Тип("ТаблицаЗначений") Тогда
							Индекс = КэшИндекс;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			Исключение
			КонецПопытки;
		КонецЕсли;

		// 2) Если индекса нет - строим/получаем заново (используя кэш ДвоичныхДанных, если он есть)
		Если Индекс = Неопределено Тогда
			РезМД = ПолучитьМетаданныеКонфигурацииРасширения(Расширение, КэшПараметры);
			Если НЕ РезМД.success Тогда
				Возврат Новый Структура("success, error", Ложь, РезМД.error);
			КонецЕсли;

			ТекущийХеш = РезМД.hash;
			ТокенДанных = РезМД.data_token;

			Если РезМД.is_empty Тогда
				// Пустое расширение: все типы по 0
				Счетчики = Новый Соответствие;
				Для Каждого ОписаниеТипа Из ПолучитьКорневыеТипыМетаданных() Цикл
					Счетчики.Вставить(ОписаниеТипа.Тип, 0);
				КонецЦикла;

				Сводка = Новый Массив;
				Для Каждого ОписаниеТипа Из ПолучитьКорневыеТипыМетаданных() Цикл
					Сводка.Добавить(Новый Структура("Тип, Количество", ОписаниеТипа.Тип, 0));
				КонецЦикла;

				ИнфоКонф = ПолучитьИнформациюОКонфигурации();
				Результат = Новый Структура("success, data, configuration, extension", Истина, Сводка, ИнфоКонф, Расширение.Имя);

				// Внутренние поля для обновления клиентского кэша (не уходят наружу из-за whitelist)
				Результат.Вставить("mcp_cache_hash", ТекущийХеш);
				Если ТокенДанных <> Неопределено Тогда
					Результат.Вставить("mcp_cache_data_token", ТокенДанных);
				КонецЕсли;

				Возврат Результат;
			КонецЕсли;

			Индекс = РезМД.index;
			Если Индекс = Неопределено Тогда
				Возврат Новый Структура("success, error", Ложь, "Не удалось построить индекс метаданных расширения");
			КонецЕсли;

			// Кэшируем индекс во ВременномХранилище (best effort)
			Попытка
				ТокенИндекса = ПоместитьВоВременноеХранилище(Индекс);
			Исключение
				ТокенИндекса = Неопределено;
			КонецПопытки;
		КонецЕсли;

		// 3) Подсчет по типам (одним проходом по индексу)
		Счетчики = Новый Соответствие;
		КорневыеТипы = ПолучитьКорневыеТипыМетаданных();
		Для Каждого ОписаниеТипа Из КорневыеТипы Цикл
			Счетчики.Вставить(ОписаниеТипа.Тип, 0);
		КонецЦикла;

		Для Каждого СтрИндекса Из Индекс Цикл
			ТипСтроки = "";
			Попытка
				ТипСтроки = Строка(СтрИндекса.Тип);
			Исключение
				ТипСтроки = "";
			КонецПопытки;

			Если ПустаяСтрока(ТипСтроки) Тогда
				Продолжить;
			КонецЕсли;

			Текущее = Счетчики.Получить(ТипСтроки);
			Если Текущее = Неопределено Тогда
				Счетчики.Вставить(ТипСтроки, 1);
			Иначе
				Счетчики.Вставить(ТипСтроки, Текущее + 1);
			КонецЕсли;
		КонецЦикла;

		Сводка = Новый Массив;
		Для Каждого ОписаниеТипа Из КорневыеТипы Цикл
			Кол = Счетчики.Получить(ОписаниеТипа.Тип);
			Если Кол = Неопределено Тогда
				Кол = 0;
			КонецЕсли;
			Сводка.Добавить(Новый Структура("Тип, Количество", ОписаниеТипа.Тип, Кол));
		КонецЦикла;

		ИнфоКонф = ПолучитьИнформациюОКонфигурации();
		Результат = Новый Структура("success, data, configuration, extension", Истина, Сводка, ИнфоКонф, Расширение.Имя);

		// Внутренние поля для обновления клиентского кэша (не уходят наружу из-за whitelist)
		Результат.Вставить("mcp_cache_hash", ТекущийХеш);
		Если ТокенДанных <> Неопределено Тогда
			Результат.Вставить("mcp_cache_data_token", ТокенДанных);
		КонецЕсли;
		Если ТокенИндекса <> Неопределено Тогда
			Результат.Вставить("mcp_cache_index_token", ТокенИндекса);
		КонецЕсли;

		Возврат Результат;

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСписокМетаданныхРасширения(Расширение, ТипМетаданных, МаскаИмени, Лимит, Смещение, КэшПараметры = Неопределено)
	// Возвращает список объектов метаданных внутри расширения (list-режим с пагинацией)

	Попытка
		// list-режим расширения: используем кэш индекса (ТаблицаЗначений) во ВременномХранилище, если доступен.
		ТекущийХеш = ПолучитьХешРасширенияСтрокой(Расширение);
		ТокенДанных = Неопределено;
		ТокенИндекса = Неопределено;
		Индекс = Неопределено;
		
		Если КэшПараметры <> Неопределено Тогда
			Попытка
				Если КэшПараметры.Свойство("hash")
					И КэшПараметры.hash = ТекущийХеш
					И КэшПараметры.Свойство("index_token")
					И КэшПараметры.index_token <> Неопределено Тогда
					
					ТокенИндекса = КэшПараметры.index_token;
					Попытка
						КэшИндекс = ПолучитьИзВременногоХранилища(ТокенИндекса);
					Исключение
						КэшИндекс = Неопределено;
					КонецПопытки;
					
					Если КэшИндекс <> Неопределено И ТипЗнч(КэшИндекс) = Тип("ТаблицаЗначений") Тогда
						Индекс = КэшИндекс;
					КонецЕсли;
				КонецЕсли;
			Исключение
			КонецПопытки;
		КонецЕсли;
		
		// Если индекса нет в кэше - строим заново (используя кэш ДвоичныхДанных, если он есть)
		Если Индекс = Неопределено Тогда
			РезМД = ПолучитьМетаданныеКонфигурацииРасширения(Расширение, КэшПараметры);
			Если НЕ РезМД.success Тогда
				Возврат Новый Структура("success, error", Ложь, РезМД.error);
			КонецЕсли;
			
			ТекущийХеш = РезМД.hash;
			ТокенДанных = РезМД.data_token;
			
			// Пустое расширение: объектов нет
			Если РезМД.is_empty Тогда
				Результат = Новый Структура;
				Результат.Вставить("success", Истина);
				Результат.Вставить("data", Новый Массив);
				Результат.Вставить("extension", Расширение.Имя);
				Результат.Вставить("truncated", Ложь);
				Результат.Вставить("limit", Лимит);
				Результат.Вставить("returned", 0);
				Результат.Вставить("count", 0);
				Результат.Вставить("offset", Смещение);
				Результат.Вставить("has_more", Ложь);
				Результат.Вставить("next_offset", Смещение);
				Результат.Вставить("mcp_cache_hash", ТекущийХеш);
				Если ТокенДанных <> Неопределено Тогда
					Результат.Вставить("mcp_cache_data_token", ТокенДанных);
				КонецЕсли;
				Возврат Результат;
			КонецЕсли;
			
			Индекс = РезМД.index;
			Если Индекс = Неопределено Тогда
				Возврат Новый Структура("success, error", Ложь, "Не удалось построить индекс метаданных расширения");
			КонецЕсли;
			
			// Кэшируем индекс во ВременномХранилище (best effort)
			Попытка
				ТокенИндекса = ПоместитьВоВременноеХранилище(Индекс);
			Исключение
				ТокенИндекса = Неопределено;
			КонецПопытки;
		Иначе
			// Индекс пришёл из кэша; попытаемся вернуть токен ДвоичныхДанных для обновления клиентского кэша (если передан)
			Если КэшПараметры <> Неопределено Тогда
				Попытка
					Если КэшПараметры.Свойство("hash") И КэшПараметры.hash = ТекущийХеш
						И КэшПараметры.Свойство("data_token") Тогда
						ТокенДанных = КэшПараметры.data_token;
					КонецЕсли;
				Исключение
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;

		КорневыеТипы = ПолучитьКорневыеТипыМетаданных();

		// ТипМетаданных может быть строкой ("Документ", "*") или массивом строк
		ТипыДляОбработки = Новый Массив;
		ТипТипМетаданных = ТипЗнч(ТипМетаданных);
		
		Если ТипМетаданных = Неопределено Или (ТипТипМетаданных = Тип("Строка") И ПустаяСтрока(ТипМетаданных)) Тогда
			ТипМетаданных = "*";
			ТипТипМетаданных = Тип("Строка");
		КонецЕсли;
		
		Если ТипТипМетаданных = Тип("Строка") Тогда
			
			Если ТипМетаданных = "*" Тогда
				ТипыДляОбработки = КорневыеТипы;
			Иначе
				ОписаниеЗапрошенногоТипа = Неопределено;
				Для Каждого ОписаниеТипа Из КорневыеТипы Цикл
					Если ОписаниеТипа.Тип = ТипМетаданных Тогда
						ОписаниеЗапрошенногоТипа = ОписаниеТипа;
						Прервать;
					КонецЕсли;
				КонецЦикла;
				
				Если ОписаниеЗапрошенногоТипа = Неопределено Тогда
					Возврат Новый Структура("success, error", Ложь, "Неизвестный тип метаданных: " + ТипМетаданных);
				КонецЕсли;
				
				ТипыДляОбработки.Добавить(ОписаниеЗапрошенногоТипа);
			КонецЕсли;
			
		ИначеЕсли ТипТипМетаданных = Тип("Массив") Тогда
			
			ЗапрошеныВсеТипы = Ложь;
			Уникальные = Новый Соответствие;
			
			Попытка
				Для Каждого ИмяТипа Из ТипМетаданных Цикл
					ИмяТипа = СокрЛП(Строка(ИмяТипа));
					Если ПустаяСтрока(ИмяТипа) Тогда
						Продолжить;
					КонецЕсли;
					Если ИмяТипа = "*" Тогда
						ЗапрошеныВсеТипы = Истина;
						Прервать;
					КонецЕсли;
					Уникальные.Вставить(ИмяТипа, Истина);
				КонецЦикла;
			Исключение
				Возврат Новый Структура("success, error", Ложь, "Некорректный формат meta_type (ожидается строка или массив строк)");
			КонецПопытки;
			
			Если ЗапрошеныВсеТипы Тогда
				ТипыДляОбработки = КорневыеТипы;
			Иначе
				Для Каждого Пара Из Уникальные Цикл
					ИмяТипа = Пара.Ключ;
					ОписаниеЗапрошенногоТипа = Неопределено;
					Для Каждого ОписаниеТипа Из КорневыеТипы Цикл
						Если ОписаниеТипа.Тип = ИмяТипа Тогда
							ОписаниеЗапрошенногоТипа = ОписаниеТипа;
							Прервать;
						КонецЕсли;
					КонецЦикла;
					
					Если ОписаниеЗапрошенногоТипа = Неопределено Тогда
						Возврат Новый Структура("success, error", Ложь, "Неизвестный тип метаданных: " + ИмяТипа);
					КонецЕсли;
					
					ТипыДляОбработки.Добавить(ОписаниеЗапрошенногоТипа);
				КонецЦикла;
			КонецЕсли;
			
		Иначе
			Возврат Новый Структура("success, error", Ложь, "Некорректный формат meta_type (ожидается строка или массив строк)");
		КонецЕсли;

		// Быстрый фильтр по типам (если не "*")
		РазрешенныеТипы = Неопределено;
		Если НЕ (ТипТипМетаданных = Тип("Строка") И ТипМетаданных = "*") Тогда
			РазрешенныеТипы = Новый Соответствие;
			Для Каждого ОписаниеТипа Из ТипыДляОбработки Цикл
				РазрешенныеТипы.Вставить(ОписаниеТипа.Тип, Истина);
			КонецЦикла;
		КонецЕсли;

		Если Смещение < 0 Тогда
			Смещение = 0;
		КонецЕсли;

		МаскаВерхнийРегистр = "";
		Если МаскаИмени <> Неопределено Тогда
			Попытка
				МаскаВерхнийРегистр = ВРег(МаскаИмени);
			Исключение
				МаскаВерхнийРегистр = "";
			КонецПопытки;
		КонецЕсли;

		РезультатСписок = Новый Массив;
		ВсегоПодходящих = 0;
		Возвращено = 0;
		ЕстьЕще = Ложь;

		Для Каждого СтрИндекса Из Индекс Цикл
			Если РазрешенныеТипы <> Неопределено Тогда
				Если РазрешенныеТипы.Получить(СтрИндекса.Тип) = Неопределено Тогда
					Продолжить;
				КонецЕсли;
			КонецЕсли;

			Если НЕ ПрошелФильтрПоМаске(СтрИндекса.Имя, СтрИндекса.Синоним, МаскаВерхнийРегистр) Тогда
				Продолжить;
			КонецЕсли;

			// ВАЖНО: count должен быть точным, поэтому не прерываем обход после набора страницы.
			// После достижения лимита перестаём добавлять элементы в data, но продолжаем считать ВсегоПодходящих.
			Если ВсегоПодходящих >= Смещение И Возвращено < Лимит Тогда
				РезультатСписок.Добавить(Новый Структура("ПолноеИмя, Синоним", СтрИндекса.ПолноеИмя, СтрИндекса.Синоним));
				Возвращено = Возвращено + 1;
			КонецЕсли;

			ВсегоПодходящих = ВсегоПодходящих + 1;
		КонецЦикла;

		ЕстьЕще = (Смещение + Возвращено) < ВсегоПодходящих;
		Обрезано = ЕстьЕще;
		СледующееСмещение = Смещение + Возвращено;

		Результат = Новый Структура;
		Результат.Вставить("success", Истина);
		Результат.Вставить("data", РезультатСписок);
		Результат.Вставить("extension", Расширение.Имя);
		Результат.Вставить("truncated", Обрезано);
		Результат.Вставить("limit", Лимит);
		Результат.Вставить("returned", Возвращено);
		Результат.Вставить("count", ВсегоПодходящих);
		Результат.Вставить("offset", Смещение);
		Результат.Вставить("has_more", Обрезано);
		Результат.Вставить("next_offset", СледующееСмещение);
		
		// Внутренние поля для обновления клиентского кэша (не уходят наружу из-за whitelist)
		Результат.Вставить("mcp_cache_hash", ТекущийХеш);
		Если ТокенДанных <> Неопределено Тогда
			Результат.Вставить("mcp_cache_data_token", ТокенДанных);
		КонецЕсли;
		Если ТокенИндекса <> Неопределено Тогда
			Результат.Вставить("mcp_cache_index_token", ТокенИндекса);
		КонецЕсли;
		
		Возврат Результат;

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьДетальныеМетаданныеРасширения(Расширение, Фильтр, Секции, КэшПараметры = Неопределено)
	// Возвращает детальную структуру объекта метаданных внутри расширения (details-режим)

	Попытка
		// Парсим Фильтр: "ТипМетаданных.ИмяОбъекта[.ОстатокПути]"
		ПозицияТочки = Найти(Фильтр, ".");
		Если ПозицияТочки = 0 Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Неверный формат фильтра: " + Фильтр + ". Ожидается 'ТипМетаданных.ИмяОбъекта'");
		КонецЕсли;

		ТипОбъекта = Лев(Фильтр, ПозицияТочки - 1);
		ОстатокПослеТипа = Сред(Фильтр, ПозицияТочки + 1);

		// Ищем вторую точку — она отделяет ИмяОбъекта от пути к элементу коллекции
		ПозицияВторойТочки = Найти(ОстатокПослеТипа, ".");
		Если ПозицияВторойТочки = 0 Тогда
			// Формат "Тип.Объект" — стандартное поведение
			ИмяОбъекта = ОстатокПослеТипа;
			ОстатокПутиЭлемента = "";
		Иначе
			ИмяОбъекта = Лев(ОстатокПослеТипа, ПозицияВторойТочки - 1);
			ОстатокПутиЭлемента = Сред(ОстатокПослеТипа, ПозицияВторойТочки + 1);
		КонецЕсли;

		// Находим коллекцию по типу
		КорневыеТипы = ПолучитьКорневыеТипыМетаданных();
		ИмяКоллекции = "";
		Для Каждого ОписаниеТипа Из КорневыеТипы Цикл
			Если ОписаниеТипа.Тип = ТипОбъекта Тогда
				ИмяКоллекции = ОписаниеТипа.Коллекция;
				Прервать;
			КонецЕсли;
		КонецЦикла;

		Если ПустаяСтрока(ИмяКоллекции) Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Неизвестный тип метаданных: " + ТипОбъекта);
		КонецЕсли;
		
		// Попытка взять details-ответ из кэша (ВременноеХранилище) если хеш расширения совпадает
		ТекущийХеш = ПолучитьХешРасширенияСтрокой(Расширение);
		ТокенДанных = Неопределено;
		
		Если КэшПараметры <> Неопределено Тогда
			Попытка
				Если КэшПараметры.Свойство("hash") И КэшПараметры.hash = ТекущийХеш Тогда
					Если КэшПараметры.Свойство("data_token") Тогда
						ТокенДанных = КэшПараметры.data_token;
					КонецЕсли;
					
					Если КэшПараметры.Свойство("details_token") И КэшПараметры.details_token <> Неопределено Тогда
						ТокенДеталей = КэшПараметры.details_token;
						КэшДетали = Неопределено;
						Попытка
							КэшДетали = ПолучитьИзВременногоХранилища(ТокенДеталей);
						Исключение
							КэшДетали = Неопределено;
						КонецПопытки;
						
						Если КэшДетали <> Неопределено И ТипЗнч(КэшДетали) = Тип("Структура") Тогда
							Если КэшДетали.Свойство("success") И КэшДетали.success Тогда
								// Лёгкая валидация: если в data есть ПолноеИмя, сверим с запрошенным
								Попытка
									Если КэшДетали.Свойство("data")
										И ТипЗнч(КэшДетали.data) = Тип("Структура")
										И КэшДетали.data.Свойство("ПолноеИмя")
										И КэшДетали.data.ПолноеИмя <> Фильтр Тогда
										КэшДетали = Неопределено;
									КонецЕсли;
								Исключение
								КонецПопытки;
							Иначе
								КэшДетали = Неопределено;
							КонецЕсли;
						Иначе
							КэшДетали = Неопределено;
						КонецЕсли;
						
						Если КэшДетали <> Неопределено Тогда
							КэшДетали.Вставить("mcp_cache_hash", ТекущийХеш);
							КэшДетали.Вставить("mcp_cache_details_token", ТокенДеталей);
							Если ТокенДанных <> Неопределено Тогда
								КэшДетали.Вставить("mcp_cache_data_token", ТокенДанных);
							КонецЕсли;
							Возврат КэшДетали;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			Исключение
			КонецПопытки;
		КонецЕсли;

		// Получаем метаданные расширения из бинарных данных (GetData) и коллекцию из них.
		// Это даёт корректный результат и совпадает по формату с основной конфигурацией.
		РезМД = ПолучитьМетаданныеКонфигурацииРасширения(Расширение, КэшПараметры);
		Если НЕ РезМД.success Тогда
			Возврат Новый Структура("success, error", Ложь, РезМД.error);
		КонецЕсли;
		
		ТекущийХеш = РезМД.hash;
		ТокенДанных = РезМД.data_token;

		Если РезМД.is_empty Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Объект не найден в расширении (расширение пустое): " + Фильтр);
		КонецЕсли;

		МетаданныеРасширения = РезМД.metadata;
		Если МетаданныеРасширения = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь, "Не удалось получить метаданные расширения");
		КонецЕсли;

		КоллекцияВРасширении = Неопределено;
		Попытка
			КоллекцияВРасширении = Вычислить("МетаданныеРасширения." + ИмяКоллекции);
		Исключение
			КоллекцияВРасширении = Неопределено;
		КонецПопытки;

		Если КоллекцияВРасширении = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Коллекция " + ИмяКоллекции + " недоступна в метаданных расширения " + Расширение.Имя);
		КонецЕсли;

		// Ищем объект по имени
		ОбъектМД = Неопределено;
		Для Каждого ТекОбъект Из КоллекцияВРасширении Цикл
			Если ТекОбъект.Имя = ИмяОбъекта Тогда
				ОбъектМД = ТекОбъект;
				Прервать;
			КонецЕсли;
		КонецЦикла;

		Если ОбъектМД = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Объект не найден в расширении: " + Фильтр);
		КонецЕсли;

		// Навигация к элементу коллекции (если filter указывает глубже Тип.Объект)
		Если НЕ ПустаяСтрока(ОстатокПутиЭлемента) Тогда
			ЭлементМД = НайтиЭлементВКоллекцииОбъекта(ОбъектМД, ОстатокПутиЭлемента);
			Если ЭлементМД = Неопределено Тогда
				Возврат Новый Структура("success, error", Ложь,
					"Элемент не найден в расширении: " + Фильтр);
			КонецЕсли;
			Возврат ПолучитьДетальныеМетаданныеЭлементаРасширения(ЭлементМД, Фильтр, Секции, Расширение, ТекущийХеш, ТокенДанных);
		КонецЕсли;

		// Формируем детальную структуру объекта
		ИнформацияОбОбъекте = Новый Структура;
		ИнформацияОбОбъекте.Вставить("ТипОбъектаМетаданных", ТипОбъекта);
		ИнформацияОбОбъекте.Вставить("Имя", ОбъектМД.Имя);
		ИнформацияОбОбъекте.Вставить("Синоним", ОбъектМД.Синоним);
		
		ПолноеИмяФакт = Фильтр;
		Попытка
			ПолноеИмяФакт = ОбъектМД.ПолноеИмя();
		Исключение
			ПолноеИмяФакт = Фильтр;
		КонецПопытки;
		ИнформацияОбОбъекте.Вставить("ПолноеИмя", ПолноеИмяФакт);

		// Маркеры расширений: берём ТОЛЬКО из объекта метаданных расширения (ОбъектМетаданныхКонфигурация),
		// чтобы показать "Заимствованный/Собственный" в контексте расширения, а не рантайм-смёрженные метаданные.
		МеткиРасширений = ПолучитьExtensionMarksИзМетаданныхРасширения(ОбъектМД);
		Если МеткиРасширений <> Неопределено Тогда
			ИнформацияОбОбъекте.Вставить("extension_marks", МеткиРасширений);
		КонецЕсли;

		// Реквизиты
		Реквизиты = Новый Массив;
		ЕстьКоллекцияРеквизитов = Ложь;

		// Стандартные реквизиты (Код, Наименование, Ссылка и т.д.)
		Попытка
			КоллекцияСтдРеквизитов = ОбъектМД.СтандартныеРеквизиты;
			ЕстьКоллекцияРеквизитов = Истина;
			Для Каждого СтдРеквизит Из КоллекцияСтдРеквизитов Цикл
				Попытка
					СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип, Стандартный",
						СтдРеквизит.Имя,
						СтдРеквизит.Синоним,
						ПолучитьСтроковоеПредставлениеОписанияТипов(СтдРеквизит.Тип),
						Истина);

					МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(СтдРеквизит);
					Если МеткиДочернего <> Неопределено Тогда
						СтрокаРеквизита.Вставить("extension_marks", МеткиДочернего);
					КонецЕсли;

					Реквизиты.Добавить(СтрокаРеквизита);
				Исключение
					// Пропускаем проблемный стандартный реквизит
				КонецПопытки;
			КонецЦикла;
		Исключение
			// У объекта нет коллекции СтандартныеРеквизиты
		КонецПопытки;

		// Пользовательские реквизиты
		Попытка
			КоллекцияРеквизитов = ОбъектМД.Реквизиты;
			ЕстьКоллекцияРеквизитов = Истина;
			Для Каждого Реквизит Из КоллекцияРеквизитов Цикл
				СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип, Стандартный",
					Реквизит.Имя,
					Реквизит.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип),
					Ложь);

				МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(Реквизит);
				Если МеткиДочернего <> Неопределено Тогда
					СтрокаРеквизита.Вставить("extension_marks", МеткиДочернего);
				КонецЕсли;

				Реквизиты.Добавить(СтрокаРеквизита);
			КонецЦикла;
		Исключение
			// У некоторых объектов нет пользовательских реквизитов
		КонецПопытки;

		Если ЕстьКоллекцияРеквизитов Тогда
			ИнформацияОбОбъекте.Вставить("Реквизиты", Реквизиты);
		КонецЕсли;

		// Реквизиты адресации (для задач)
		Если ТипОбъекта = "Задача" Тогда
			РеквизитыАдресации = Неопределено;
			Попытка
				РеквизитыАдресации = Новый Массив;
				Для Каждого РеквизитАдресации Из ОбъектМД.РеквизитыАдресации Цикл
					СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип",
						РеквизитАдресации.Имя,
						РеквизитАдресации.Синоним,
						ПолучитьСтроковоеПредставлениеОписанияТипов(РеквизитАдресации.Тип));

					МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(РеквизитАдресации);
					Если МеткиДочернего <> Неопределено Тогда
						СтрокаРеквизита.Вставить("extension_marks", МеткиДочернего);
					КонецЕсли;

					РеквизитыАдресации.Добавить(СтрокаРеквизита);
				КонецЦикла;
			Исключение
				// Секция не применима/недоступна
				РеквизитыАдресации = Неопределено;
			КонецПопытки;

			Если РеквизитыАдресации <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("РеквизитыАдресации", РеквизитыАдресации);
			КонецЕсли;
		КонецЕсли;

		// Табличные части
		Попытка
			ТабличныеЧасти = Новый Массив;
			Для Каждого ТабличнаяЧасть Из ОбъектМД.ТабличныеЧасти Цикл
				РеквизитыТЧ = Новый Массив;
				Для Каждого Реквизит Из ТабличнаяЧасть.Реквизиты Цикл
					СтрокаРеквизитаТЧ = Новый Структура("Имя, Синоним, Тип",
						Реквизит.Имя,
						Реквизит.Синоним,
						ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип));

					МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(Реквизит);
					Если МеткиДочернего <> Неопределено Тогда
						СтрокаРеквизитаТЧ.Вставить("extension_marks", МеткиДочернего);
					КонецЕсли;
					
					РеквизитыТЧ.Добавить(СтрокаРеквизитаТЧ);
				КонецЦикла;
				
				СтрокаТЧ = Новый Структура("Имя, Синоним, Реквизиты",
					ТабличнаяЧасть.Имя,
					ТабличнаяЧасть.Синоним,
					РеквизитыТЧ);
				
				МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(ТабличнаяЧасть);
				Если МеткиДочернего <> Неопределено Тогда
					СтрокаТЧ.Вставить("extension_marks", МеткиДочернего);
				КонецЕсли;
				
				ТабличныеЧасти.Добавить(СтрокаТЧ);
			КонецЦикла;
			ИнформацияОбОбъекте.Вставить("ТабличныеЧасти", ТабличныеЧасти);
		Исключение
			// У некоторых объектов нет табличных частей
		КонецПопытки;

		// Измерения (для регистров)
		Попытка
			Измерения = Новый Массив;
			Для Каждого Измерение Из ОбъектМД.Измерения Цикл
				СтрокаИзмерения = Новый Структура("Имя, Синоним, Тип",
					Измерение.Имя,
					Измерение.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(Измерение.Тип));

				МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(Измерение);
				Если МеткиДочернего <> Неопределено Тогда
					СтрокаИзмерения.Вставить("extension_marks", МеткиДочернего);
				КонецЕсли;
				
				Измерения.Добавить(СтрокаИзмерения);
			КонецЦикла;
			ИнформацияОбОбъекте.Вставить("Измерения", Измерения);
		Исключение
			// У некоторых объектов нет измерений
		КонецПопытки;

		// Ресурсы (для регистров)
		Попытка
			Ресурсы = Новый Массив;
			Для Каждого Ресурс Из ОбъектМД.Ресурсы Цикл
				СтрокаРесурса = Новый Структура("Имя, Синоним, Тип",
					Ресурс.Имя,
					Ресурс.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(Ресурс.Тип));

				МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(Ресурс);
				Если МеткиДочернего <> Неопределено Тогда
					СтрокаРесурса.Вставить("extension_marks", МеткиДочернего);
				КонецЕсли;
				
				Ресурсы.Добавить(СтрокаРесурса);
			КонецЦикла;
			ИнформацияОбОбъекте.Вставить("Ресурсы", Ресурсы);
		Исключение
			// У некоторых объектов нет ресурсов
		КонецПопытки;

		// Значения перечисления
		Если ТипОбъекта = "Перечисление" Тогда
			Попытка
				ЗначенияПеречисления = Новый Массив;
				Для Каждого ЗначениеПеречисления Из ОбъектМД.ЗначенияПеречисления Цикл
					ЗначенияПеречисления.Добавить(Новый Структура("Имя, Синоним",
						ЗначениеПеречисления.Имя,
						ЗначениеПеречисления.Синоним));
				КонецЦикла;
				ИнформацияОбОбъекте.Вставить("ЗначенияПеречисления", ЗначенияПеречисления);
			Исключение
			КонецПопытки;
		КонецЕсли;

		// Владельцы (для подчинённых справочников)
		Если ТипОбъекта = "Справочник" Тогда
			Попытка
				Если ОбъектМД.Владельцы.Количество() > 0 Тогда
					Владельцы = Новый Массив;
					Для Каждого Владелец Из ОбъектМД.Владельцы Цикл
						Владельцы.Добавить(Владелец.ПолноеИмя());
					КонецЦикла;
					ИнформацияОбОбъекте.Вставить("Владельцы", Владельцы);
				КонецЕсли;
			Исключение
				// У некоторых справочников нет владельцев
			КонецПопытки;
		КонецЕсли;

		// ПризнакиУчета и ПризнакиУчетаСубконто (только для планов счетов)
		Если ТипОбъекта = "ПланСчетов" Тогда
			Попытка
				КоллекцияПризнаков = ОбъектМД.ПризнакиУчета;
				Если КоллекцияПризнаков <> Неопределено Тогда
					МассивПризнаков = Новый Массив;
					Для Каждого Признак Из КоллекцияПризнаков Цикл
						МассивПризнаков.Добавить(Новый Структура("Имя, Синоним, Тип",
							Признак.Имя, Признак.Синоним,
							ПолучитьСтроковоеПредставлениеОписанияТипов(Признак.Тип)));
					КонецЦикла;
					Если МассивПризнаков.Количество() > 0 Тогда
						ИнформацияОбОбъекте.Вставить("ПризнакиУчета", МассивПризнаков);
					КонецЕсли;
				КонецЕсли;
			Исключение
			КонецПопытки;

			Попытка
				КоллекцияПризнаковСубконто = ОбъектМД.ПризнакиУчетаСубконто;
				Если КоллекцияПризнаковСубконто <> Неопределено Тогда
					МассивПризнаковСубконто = Новый Массив;
					Для Каждого Признак Из КоллекцияПризнаковСубконто Цикл
						МассивПризнаковСубконто.Добавить(Новый Структура("Имя, Синоним, Тип",
							Признак.Имя, Признак.Синоним,
							ПолучитьСтроковоеПредставлениеОписанияТипов(Признак.Тип)));
					КонецЦикла;
					Если МассивПризнаковСубконто.Количество() > 0 Тогда
						ИнформацияОбОбъекте.Вставить("ПризнакиУчетаСубконто", МассивПризнаковСубконто);
					КонецЕсли;
				КонецЕсли;
			Исключение
			КонецПопытки;
		КонецЕсли;

		// СтандартныеТабличныеЧасти (для ПланСчетов и ПланВидовРасчета)
		Если ТипОбъекта = "ПланСчетов" ИЛИ ТипОбъекта = "ПланВидовРасчета" Тогда
			Попытка
				СтандартныеТабличныеЧасти = Новый Массив;
				Для Каждого СтдТЧ Из ОбъектМД.СтандартныеТабличныеЧасти Цикл
					РеквизитыСтдТЧ = Новый Массив;
					Попытка
						Для Каждого СтдРеквизит Из СтдТЧ.СтандартныеРеквизиты Цикл
							Попытка
								РеквизитыСтдТЧ.Добавить(Новый Структура("Имя, Синоним, Тип",
									СтдРеквизит.Имя,
									СтдРеквизит.Синоним,
									ПолучитьСтроковоеПредставлениеОписанияТипов(СтдРеквизит.Тип)));
							Исключение
							КонецПопытки;
						КонецЦикла;
					Исключение
					КонецПопытки;

					СтрокаСтдТЧ = Новый Структура("Имя, Синоним, Реквизиты",
						СтдТЧ.Имя,
						СтдТЧ.Синоним,
						РеквизитыСтдТЧ);

					МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(СтдТЧ);
					Если МеткиДочернего <> Неопределено Тогда
						СтрокаСтдТЧ.Вставить("extension_marks", МеткиДочернего);
					КонецЕсли;

					СтандартныеТабличныеЧасти.Добавить(СтрокаСтдТЧ);
				КонецЦикла;
				Если СтандартныеТабличныеЧасти.Количество() > 0 Тогда
					ИнформацияОбОбъекте.Вставить("СтандартныеТабличныеЧасти", СтандартныеТабличныеЧасти);
				КонецЕсли;
			Исключение
			КонецПопытки;
		КонецЕсли;

		// Дополнительные секции детального ответа (управляемый "богатый" режим)
		Если СекцияЗапрошена(Секции, "properties") Тогда

			Свойства = Новый Структура;
			Кандидаты = Новый Массив;

			ДобавитьКандидатыСвойствПоТипу(ТипОбъекта, Кандидаты);
			
			Для Каждого ИмяСвойства Из Кандидаты Цикл
				ЗначениеСвойства = Неопределено;
				Попытка
					ЗначениеСвойства = Вычислить("ОбъектМД." + ИмяСвойства);
				Исключение
					ЗначениеСвойства = Неопределено;
				КонецПопытки;
				
				Если ЗначениеСвойства <> Неопределено Тогда
					// Сериализация properties должна совпадать с основной конфигурацией:
					// сначала пытаемся получить ПолноеИмя() (или массив полных имён), иначе fallback.
					ЗначениеДляJSON = ПолноеИмяМетаданныхЕслиВозможно(ЗначениеСвойства);
					Если ЗначениеДляJSON = Неопределено Тогда
						ЗначениеДляJSON = БезопасноеЗначениеДляПередачи(ЗначениеСвойства);
					КонецЕсли;
					Свойства.Вставить(ИмяСвойства, ЗначениеДляJSON);
				КонецЕсли;
			КонецЦикла;
			
			ИнформацияОбОбъекте.Вставить("properties", Свойства);
			
		КонецЕсли;

		// Формы (если запрошена секция)
		Если СекцияЗапрошена(Секции, "forms") Тогда
			КоллекцияФорм = Неопределено;
			Попытка
				КоллекцияФорм = ОбъектМД.Формы;
			Исключение
				КоллекцияФорм = Неопределено;
			КонецПопытки;
			
			// Контракт как в основной конфигурации: ключ добавляем только если коллекция реально получена.
			Если КоллекцияФорм <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("forms", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияФорм));
			КонецЕсли;
		КонецЕсли;

		// Команды (если запрошена секция)
		Если СекцияЗапрошена(Секции, "commands") Тогда
			КоллекцияКоманд = Неопределено;
			Попытка
				КоллекцияКоманд = ОбъектМД.Команды;
			Исключение
				КоллекцияКоманд = Неопределено;
			КонецПопытки;
			
			// Контракт как в основной конфигурации: ключ добавляем только если коллекция реально получена.
			Если КоллекцияКоманд <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("commands", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияКоманд));
			КонецЕсли;
		КонецЕсли;

		// Макеты (если запрошена секция)
		Если СекцияЗапрошена(Секции, "layouts") Тогда
			КоллекцияМакетов = Неопределено;
			Попытка
				КоллекцияМакетов = ОбъектМД.Макеты;
			Исключение
				КоллекцияМакетов = Неопределено;
			КонецПопытки;
			
			// Контракт как в основной конфигурации: ключ добавляем только если коллекция реально получена.
			Если КоллекцияМакетов <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("layouts", СекцияИзКоллекцииМакетов(КоллекцияМакетов));
			КонецЕсли;
		КонецЕсли;

		Если СекцияЗапрошена(Секции, "predefined") Тогда
			
			// 1) Если в рантайм-метаданных есть коллекция предопределенных - используем ее
			КоллекцияПредопределенных = Неопределено;
			Попытка
				КоллекцияПредопределенных = ОбъектМД.Предопределенные;
			Исключение
				КоллекцияПредопределенных = Неопределено;
			КонецПопытки;
			
			Если КоллекцияПредопределенных <> Неопределено Тогда
				Попытка
					ИнформацияОбОбъекте.Вставить("predefined", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияПредопределенных));
				Исключение
					// Если это не коллекция - игнорируем и пробуем fallback ниже
				КонецПопытки;
			КонецЕсли;
			
			// 2) Fallback для справочников: предопределенные данные хранятся как данные (отбор по Предопределенный)
			Если Не ИнформацияОбОбъекте.Свойство("predefined") И ТипОбъекта = "Справочник" Тогда
				ПредопределенныеДанные = ПолучитьПредопределенныеЭлементыСправочника(ИмяОбъекта, 200);
				Если ПредопределенныеДанные <> Неопределено Тогда
					ИнформацияОбОбъекте.Вставить("predefined", ПредопределенныеДанные);
				КонецЕсли;
			КонецЕсли;
			
		КонецЕсли;

		// Движения (только для документов — список регистров, в которых документ делает движения)
		Если СекцияЗапрошена(Секции, "movements") Тогда
			Если ТипОбъекта = "Документ" Тогда
				КоллекцияДвижений = Неопределено;
				Попытка
					КоллекцияДвижений = ОбъектМД.Движения;
				Исключение
					КоллекцияДвижений = Неопределено;
				КонецПопытки;

				Если КоллекцияДвижений <> Неопределено Тогда
					ИнформацияОбОбъекте.Вставить("movements", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияДвижений));
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		// Характеристики (описания характеристик, привязанных к объекту метаданных)
		Если СекцияЗапрошена(Секции, "characteristics") Тогда
			КоллекцияХарактеристик = Неопределено;
			Попытка
				КоллекцияХарактеристик = ОбъектМД.Характеристики;
			Исключение
				КоллекцияХарактеристик = Неопределено;
			КонецПопытки;

			Если КоллекцияХарактеристик <> Неопределено Тогда
				МассивХарактеристик = Новый Массив;
				Для Каждого ОписаниеХарактеристики Из КоллекцияХарактеристик Цикл
					ЭлементХарактеристики = Новый Структура;

					// ВидыХарактеристик — ссылка на ПланВидовХарактеристик → ПолноеИмя()
					Попытка
						ВидыХар = ОписаниеХарактеристики.ВидыХарактеристик;
						Если ВидыХар <> Неопределено Тогда
							Попытка
								ЭлементХарактеристики.Вставить("ВидыХарактеристик", ВидыХар.ПолноеИмя());
							Исключение
								ЭлементХарактеристики.Вставить("ВидыХарактеристик", Строка(ВидыХар));
							КонецПопытки;
						КонецЕсли;
					Исключение
					КонецПопытки;

					// ЗначенияХарактеристик — ссылка на регистр сведений → ПолноеИмя()
					Попытка
						ЗначенияХар = ОписаниеХарактеристики.ЗначенияХарактеристик;
						Если ЗначенияХар <> Неопределено Тогда
							Попытка
								ЭлементХарактеристики.Вставить("ЗначенияХарактеристик", ЗначенияХар.ПолноеИмя());
							Исключение
								ЭлементХарактеристики.Вставить("ЗначенияХарактеристик", Строка(ЗначенияХар));
							КонецПопытки;
						КонецЕсли;
					Исключение
					КонецПопытки;

					// ЗначениеОтбораВидов — произвольный тип
					Попытка
						ЗначениеОтбора = ОписаниеХарактеристики.ЗначениеОтбораВидов;
						Если ЗначениеОтбора <> Неопределено Тогда
							ЭлементХарактеристики.Вставить("ЗначениеОтбораВидов",
								БезопасноеЗначениеДляПередачи(ЗначениеОтбора));
						КонецЕсли;
					Исключение
					КонецПопытки;

					// Поля (строковые свойства) — через Вычислить с безопасным доступом
					ПоляСтроковые = Новый Структура;
					ПоляСтроковые.Вставить("ПолеВида",                                "ПолеВида");
					ПоляСтроковые.Вставить("ПолеЗначения",                            "ПолеЗначения");
					ПоляСтроковые.Вставить("ПолеИспользованияМножественныхЗначений",  "ПолеИспользованияМножественныхЗначений");
					ПоляСтроковые.Вставить("ПолеКлюча",                               "ПолеКлюча");
					ПоляСтроковые.Вставить("ПолеКлючаМножественныхЗначений",          "ПолеКлючаМножественныхЗначений");
					ПоляСтроковые.Вставить("ПолеОбъекта",                             "ПолеОбъекта");
					ПоляСтроковые.Вставить("ПолеОтбораВидов",                         "ПолеОтбораВидов");
					ПоляСтроковые.Вставить("ПолеПорядкаМножественныхЗначений",        "ПолеПорядкаМножественныхЗначений");
					ПоляСтроковые.Вставить("ПолеПутиКДанным",                         "ПолеПутиКДанным");

					Для Каждого КлючЗначение Из ПоляСтроковые Цикл
						Попытка
							ЗначениеПоля = Вычислить("ОписаниеХарактеристики." + КлючЗначение.Значение);
							Если ЗначениеПоля <> Неопределено И Не ПустаяСтрока(Строка(ЗначениеПоля)) Тогда
								ЭлементХарактеристики.Вставить(КлючЗначение.Ключ, Строка(ЗначениеПоля));
							КонецЕсли;
						Исключение
						КонецПопытки;
					КонецЦикла;

					// Пропускаем пустые элементы (все поля = Неопределено/пустые)
					Если ЭлементХарактеристики.Количество() > 0 Тогда
						МассивХарактеристик.Добавить(ЭлементХарактеристики);
					КонецЕсли;
				КонецЦикла;

				Если МассивХарактеристик.Количество() > 0 Тогда
					ИнформацияОбОбъекте.Вставить("characteristics", МассивХарактеристик);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		Результат = Новый Структура;
		Результат.Вставить("success", Истина);
		Результат.Вставить("data", ИнформацияОбОбъекте);
		Результат.Вставить("extension", Расширение.Имя);
		
		// Кэшируем готовый details-ответ (best effort) и возвращаем токен для клиентского кэша
		ТокенДеталей = Неопределено;
		Попытка
			ТокенДеталей = ПоместитьВоВременноеХранилище(Результат);
		Исключение
			ТокенДеталей = Неопределено;
		КонецПопытки;
		
		Результат.Вставить("mcp_cache_hash", ТекущийХеш);
		Если ТокенДанных <> Неопределено Тогда
			Результат.Вставить("mcp_cache_data_token", ТокенДанных);
		КонецЕсли;
		Если ТокенДеталей <> Неопределено Тогда
			Результат.Вставить("mcp_cache_details_token", ТокенДеталей);
		КонецЕсли;

		Возврат Результат;

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьДетальныеМетаданныеЭлементаРасширения(ОбъектМетаданных, ПолноеИмяФильтра, Секции, Расширение, ТекущийХеш, ТокенДанных)

	ИнформацияОбЭлементе = Новый Структура;
	ИнформацияОбЭлементе.Вставить("ПолноеИмя", ПолноеИмяФильтра);
	ИнформацияОбЭлементе.Вставить("Имя", ОбъектМетаданных.Имя);

	Попытка
		ИнформацияОбЭлементе.Вставить("Синоним", ОбъектМетаданных.Синоним);
	Исключение
	КонецПопытки;

	// Тип (описание типов) — есть у реквизитов, измерений, ресурсов
	Попытка
		ИнформацияОбЭлементе.Вставить("Тип",
			ПолучитьСтроковоеПредставлениеОписанияТипов(ОбъектМетаданных.Тип));
	Исключение
		// У некоторых элементов (ТабличнаяЧасть) нет свойства Тип
	КонецПопытки;

	// Для табличных частей — вернуть список реквизитов
	Попытка
		РеквизитыТЧ = Новый Массив;
		Для Каждого Реквизит Из ОбъектМетаданных.Реквизиты Цикл
			СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип",
				Реквизит.Имя,
				Реквизит.Синоним,
				ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип));

			МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(Реквизит);
			Если МеткиДочернего <> Неопределено Тогда
				СтрокаРеквизита.Вставить("extension_marks", МеткиДочернего);
			КонецЕсли;

			РеквизитыТЧ.Добавить(СтрокаРеквизита);
		КонецЦикла;
		ИнформацияОбЭлементе.Вставить("Реквизиты", РеквизитыТЧ);
	Исключение
		// Нет коллекции реквизитов (не ТабличнаяЧасть)
	КонецПопытки;

	// Extension marks
	МеткиДочернего = ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(ОбъектМетаданных);
	Если МеткиДочернего <> Неопределено Тогда
		ИнформацияОбЭлементе.Вставить("extension_marks", МеткиДочернего);
	КонецЕсли;

	// Properties — расширенные свойства элемента
	Если СекцияЗапрошена(Секции, "properties") Тогда
		Свойства = Новый Структура;
		Кандидаты = Новый Массив;
		ДобавитьКандидатыСвойствЭлементаКоллекции(Кандидаты);

		Для Каждого ИмяСвойства Из Кандидаты Цикл
			ЗначениеСвойства = Неопределено;
			Попытка
				ЗначениеСвойства = Вычислить("ОбъектМетаданных." + ИмяСвойства);
			Исключение
			КонецПопытки;
			Если ЗначениеСвойства <> Неопределено Тогда
				ЗначениеДляJSON = ПолноеИмяМетаданныхЕслиВозможно(ЗначениеСвойства);
				Если ЗначениеДляJSON = Неопределено Тогда
					ЗначениеДляJSON = БезопасноеЗначениеДляПередачи(ЗначениеСвойства);
				КонецЕсли;
				Свойства.Вставить(ИмяСвойства, ЗначениеДляJSON);
			КонецЕсли;
		КонецЦикла;

		ИнформацияОбЭлементе.Вставить("properties", Свойства);
	КонецЕсли;

	Результат = Новый Структура;
	Результат.Вставить("success", Истина);
	Результат.Вставить("data", ИнформацияОбЭлементе);
	Результат.Вставить("extension", Расширение.Имя);

	// Кэшируем (best effort)
	ТокенДеталей = Неопределено;
	Попытка
		ТокенДеталей = ПоместитьВоВременноеХранилище(Результат);
	Исключение
	КонецПопытки;

	Результат.Вставить("mcp_cache_hash", ТекущийХеш);
	Если ТокенДанных <> Неопределено Тогда
		Результат.Вставить("mcp_cache_data_token", ТокенДанных);
	КонецЕсли;
	Если ТокенДеталей <> Неопределено Тогда
		Результат.Вставить("mcp_cache_details_token", ТокенДеталей);
	КонецЕсли;

	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКорневыеТипыМетаданных()
	
	// Список корневых типов метаданных (как в дереве конфигуратора).
	// Формат: Массив структур {Тип, Коллекция}, где Коллекция - имя свойства Метаданные.<Коллекция>.
	Типы = Новый Массив;
	
	// Общие
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Подсистема", "Подсистемы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщийМодуль", "ОбщиеМодули"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПараметрСеанса", "ПараметрыСеанса"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Роль", "Роли"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщийРеквизит", "ОбщиеРеквизиты"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПланОбмена", "ПланыОбмена"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "КритерийОтбора", "КритерииОтбора"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПодпискаНаСобытие", "ПодпискиНаСобытия"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "РегламентноеЗадание", "РегламентныеЗадания"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Бот", "Боты"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ФункциональнаяОпция", "ФункциональныеОпции"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПараметрФункциональныхОпций", "ПараметрыФункциональныхОпций"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОпределяемыйТип", "ОпределяемыеТипы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ХранилищеНастроек", "ХранилищаНастроек"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщаяКоманда", "ОбщиеКоманды"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ГруппаКоманд", "ГруппыКоманд"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщаяФорма", "ОбщиеФормы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщийМакет", "ОбщиеМакеты"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ОбщаяКартинка", "ОбщиеКартинки"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "WebСервис", "WebСервисы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "HTTPСервис", "HTTPСервисы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "WSСсылка", "WSСсылки"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "WebSocketКлиент", "WebSocketКлиенты"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "СервисИнтеграции", "СервисыИнтеграции"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ЭлементСтиля", "ЭлементыСтиля"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Стиль", "Стили"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Язык", "Языки"));
	
	// Прикладные
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Константа", "Константы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Справочник", "Справочники"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Документ", "Документы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ЖурналДокументов", "ЖурналыДокументов"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Перечисление", "Перечисления"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Отчет", "Отчеты"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Обработка", "Обработки"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПланВидовХарактеристик", "ПланыВидовХарактеристик"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПланСчетов", "ПланыСчетов"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПланВидовРасчета", "ПланыВидовРасчета"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "РегистрСведений", "РегистрыСведений"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "РегистрНакопления", "РегистрыНакопления"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "РегистрБухгалтерии", "РегистрыБухгалтерии"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "РегистрРасчета", "РегистрыРасчета"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "БизнесПроцесс", "БизнесПроцессы"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "Задача", "Задачи"));
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ВнешнийИсточникДанных", "ВнешниеИсточникиДанных"));
	
	// Пользовательский тип (фиксируем по договорённости)
	Типы.Добавить(Новый Структура("Тип, Коллекция", "ПакетыXDTO", "ПакетыXDTO"));
	
	Возврат Типы;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСводкуМетаданных()
	
	Типы = ПолучитьКорневыеТипыМетаданных();
	Сводка = Новый Массив;
	
	Для Каждого ОписаниеТипа Из Типы Цикл
		
		Коллекция = Неопределено;
		Попытка
			Коллекция = Вычислить("Метаданные." + ОписаниеТипа.Коллекция);
		Исключение
			Коллекция = Неопределено;
		КонецПопытки;
		
		Количество = 0;
		Если Коллекция <> Неопределено Тогда
			Попытка
				Количество = Коллекция.Количество();
			Исключение
				Количество = 0;
			КонецПопытки;
		КонецЕсли;
		
		Сводка.Добавить(Новый Структура("Тип, Количество", ОписаниеТипа.Тип, Количество));
		
	КонецЦикла;
	
	ИнформацияОКонфигурации = ПолучитьИнформациюОКонфигурации();
	// Важно: data оставляем массивом (roots), чтобы в TOON-формате оставался табличный вывод.
	// Информацию о конфигурации возвращаем отдельным полем верхнего уровня.
	Возврат Новый Структура("success, data, configuration", Истина, Сводка, ИнформацияОКонфигурации);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьЗначениеПараметраСтрокиСоединения(СтрокаСоединения, Ключ)
	
	Если СтрокаСоединения = Неопределено Или ПустаяСтрока(СтрокаСоединения) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Пары = РазделитьСтрокуПоРазделителю(СтрокаСоединения, ";");
	
	Для Каждого Пара Из Пары Цикл
		
		Если ПустаяСтрока(Пара) Тогда
			Продолжить;
		КонецЕсли;
		
		ПозицияРавно = Найти(Пара, "=");
		Если ПозицияРавно <= 0 Тогда
			Продолжить;
		КонецЕсли;
		
		ИмяПараметра = СокрЛП(Лев(Пара, ПозицияРавно - 1));
		Если ИмяПараметра <> Ключ Тогда
			Продолжить;
		КонецЕсли;
		
		Значение = СокрЛП(Сред(Пара, ПозицияРавно + 1));
		
		// Убираем кавычки по краям, если есть
		Если СтрДлина(Значение) >= 2 Тогда
			Если Лев(Значение, 1) = Символ(34) И Прав(Значение, 1) = Символ(34) Тогда
				Значение = Сред(Значение, 2, СтрДлина(Значение) - 2);
			КонецЕсли;
		КонецЕсли;
		
		Если ПустаяСтрока(Значение) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Возврат Значение;
		
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИмяИБПоСтрокеСоединения(СтрокаСоединения)
	
	// Серверная ИБ: Srvr="...";Ref="MyBase";
	ИмяИБ = ПолучитьЗначениеПараметраСтрокиСоединения(СтрокаСоединения, "Ref");
	Если ИмяИБ <> Неопределено Тогда
		Возврат ИмяИБ;
	КонецЕсли;
	
	// Файловая ИБ: File="D:\Bases\MyBase";
	ПутьИБ = ПолучитьЗначениеПараметраСтрокиСоединения(СтрокаСоединения, "File");
	Если ПутьИБ = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// Возвращаем только последний сегмент пути (без раскрытия полного пути)
	РазделительПути = Символ(92); // обратный слеш
	ПутьИБ = СтрЗаменить(ПутьИБ, "/", РазделительПути);
	
	ПозицияПоследняя = 0;
	
	// В режиме совместимости 8.2 третий параметр Найти() может быть недоступен,
	// поэтому ищем последний разделитель простым проходом по строке.
	ДлинаПути = СтрДлина(ПутьИБ);
	Для Индекс = 1 По ДлинаПути Цикл
		Если Сред(ПутьИБ, Индекс, 1) = РазделительПути Тогда
			ПозицияПоследняя = Индекс;
		КонецЕсли;
	КонецЦикла;
	
	Если ПозицияПоследняя > 0 И ПозицияПоследняя < СтрДлина(ПутьИБ) Тогда
		ИмяИБ = Сред(ПутьИБ, ПозицияПоследняя + 1);
	Иначе
		ИмяИБ = ПутьИБ;
	КонецЕсли;
	
	Если ПустаяСтрока(ИмяИБ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ИмяИБ;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИнформациюОКонфигурации()
	
	Инфо = Новый Структура;
	
	// Версия платформы (очень полезно для диагностики)
	Попытка
		СисИнфо = Новый СистемнаяИнформация;
		Если СисИнфо <> Неопределено Тогда
			ВерсияПлатформы = СисИнфо.ВерсияПриложения;
			Если ВерсияПлатформы <> Неопределено И Не ПустаяСтрока(ВерсияПлатформы) Тогда
				Инфо.Вставить("platform_version", БезопасноеЗначениеДляПередачи(Строка(ВерсияПлатформы)));
			КонецЕсли;
		КонецЕсли;
	Исключение
		// нет
	КонецПопытки;
	
	// Имя ИБ (часто полезно как контекст; без путей/адресов)
	Попытка
		СтрСоед = Вычислить("СтрокаСоединенияИнформационнойБазы()");
		Если СтрСоед <> Неопределено И Не ПустаяСтрока(СтрСоед) Тогда
			ИмяИБ = ПолучитьИмяИБПоСтрокеСоединения(СтрСоед);
			Если ИмяИБ <> Неопределено И Не ПустаяСтрока(ИмяИБ) Тогда
				Инфо.Вставить("infobase_name", БезопасноеЗначениеДляПередачи(ИмяИБ));
			КонецЕсли;
		КонецЕсли;
	Исключение
		// нет
	КонецПопытки;
	
	// Пытаемся получить объект метаданных конфигурации (если доступен)
	ОбъектКонфигурации = Неопределено;
	Попытка
		ОбъектКонфигурации = Метаданные.Конфигурация;
	Исключение
		ОбъектКонфигурации = Неопределено;
	КонецПопытки;
	
	Если ОбъектКонфигурации = Неопределено Тогда
		// На некоторых версиях/режимах может быть доступен только корневой объект Метаданные
		ОбъектКонфигурации = Метаданные;
	КонецЕсли;
	
	Свойства = Новый Структура;
	Кандидаты = Новый Массив;
	
	// Базовые/ожидаемые свойства конфигурации (если доступны в рантайме)
	Кандидаты.Добавить("Имя");
	Кандидаты.Добавить("Синоним");
	Кандидаты.Добавить("Комментарий");
	Кандидаты.Добавить("Поставщик");
	Кандидаты.Добавить("Версия");
	Кандидаты.Добавить("ВерсияКонфигурации");	
	Кандидаты.Добавить("ОсновнойЯзык");
	Кандидаты.Добавить("ОсновнойРежимЗапуска");
	Кандидаты.Добавить("РежимСовместимости");
	Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
	Кандидаты.Добавить("РежимСовместимостиИнтерфейса");
	Кандидаты.Добавить("РежимИспользованияМодальности");
	Кандидаты.Добавить("РежимАвтонумерацииОбъектов");	
	Кандидаты.Добавить("РежимИспользованияБлочногоХраненияДвоичныхДанных");
	Кандидаты.Добавить("РежимИспользованияТабличныхПространствБазыДанных");
	Кандидаты.Добавить("ИспользоватьОбычныеФормыВУправляемомПриложении");
	Кандидаты.Добавить("ИспользоватьУправляемыеФормыВОбычномПриложении");

	Для Каждого ИмяСвойства Из Кандидаты Цикл
		ЗначениеСвойства = Неопределено;
		Попытка
			ЗначениеСвойства = Вычислить("ОбъектКонфигурации." + ИмяСвойства);
		Исключение
			ЗначениеСвойства = Неопределено;
		КонецПопытки;
		
		Если ЗначениеСвойства <> Неопределено Тогда
			Свойства.Вставить(ИмяСвойства, БезопасноеЗначениеДляПередачи(ЗначениеСвойства));
		КонецЕсли;
	КонецЦикла;
	
	Если Свойства.Количество() > 0 Тогда
		Инфо.Вставить("metadata", Свойства);
	КонецЕсли;
	
	Возврат Инфо;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСписокМетаданных(ТипМетаданных = "", МаскаИмени = "", Лимит = 100, Смещение = 0)
	
	// ТипМетаданных может быть строкой ("Документ", "*") или массивом строк (["Документ","РегистрСведений"])
	Типы = ПолучитьКорневыеТипыМетаданных();
	ТипыДляОбхода = Новый Массив;
	
	ТипТипМетаданных = ТипЗнч(ТипМетаданных);
	
	Если ТипМетаданных = Неопределено Или (ТипТипМетаданных = Тип("Строка") И ПустаяСтрока(ТипМетаданных)) Тогда
		ТипМетаданных = "*";
		ТипТипМетаданных = Тип("Строка");
	КонецЕсли;
	
	Если ТипТипМетаданных = Тип("Строка") Тогда
		
		Если ТипМетаданных = "*" Тогда
			ТипыДляОбхода = Типы;
		Иначе
			ОписаниеЗапрошенногоТипа = Неопределено;
			Для Каждого ОписаниеТипа Из Типы Цикл
				Если ОписаниеТипа.Тип = ТипМетаданных Тогда
					ОписаниеЗапрошенногоТипа = ОписаниеТипа;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			Если ОписаниеЗапрошенногоТипа = Неопределено Тогда
				Возврат Новый Структура("success, error", Ложь, "Неизвестный тип метаданных: " + ТипМетаданных);
			КонецЕсли;
			
			ТипыДляОбхода.Добавить(ОписаниеЗапрошенногоТипа);
		КонецЕсли;
		
	Иначе
		
		// Массив типов
		ЗапрошеныВсеТипы = Ложь;
		Уникальные = Новый Соответствие;
		
		Попытка
			Для Каждого ИмяТипа Из ТипМетаданных Цикл
				ИмяТипа = СокрЛП(Строка(ИмяТипа));
				Если ПустаяСтрока(ИмяТипа) Тогда
					Продолжить;
				КонецЕсли;
				Если ИмяТипа = "*" Тогда
					ЗапрошеныВсеТипы = Истина;
					Прервать;
				КонецЕсли;
				Уникальные.Вставить(ИмяТипа, Истина);
			КонецЦикла;
		Исключение
			Возврат Новый Структура("success, error", Ложь, "Некорректный формат meta_type (ожидается строка или массив строк)");
		КонецПопытки;
		
		Если ЗапрошеныВсеТипы Тогда
			ТипыДляОбхода = Типы;
		Иначе
			Для Каждого Пара Из Уникальные Цикл
				ИмяТипа = Пара.Ключ;
				ОписаниеЗапрошенногоТипа = Неопределено;
				Для Каждого ОписаниеТипа Из Типы Цикл
					Если ОписаниеТипа.Тип = ИмяТипа Тогда
						ОписаниеЗапрошенногоТипа = ОписаниеТипа;
						Прервать;
					КонецЕсли;
				КонецЦикла;
				
				Если ОписаниеЗапрошенногоТипа = Неопределено Тогда
					Возврат Новый Структура("success, error", Ложь, "Неизвестный тип метаданных: " + ИмяТипа);
				КонецЕсли;
				
				ТипыДляОбхода.Добавить(ОписаниеЗапрошенногоТипа);
			КонецЦикла;
		КонецЕсли;
		
	КонецЕсли;
	
	// Собираем все совпадения, сортируем по ПолноеИмя и делаем срез offset+limit
	Таблица = Новый ТаблицаЗначений;
	Таблица.Колонки.Добавить("ПолноеИмя");
	Таблица.Колонки.Добавить("Синоним");
	Таблица.Колонки.Добавить("SortKey");
	
	МаскаВерхнийРегистр = "";
	Если МаскаИмени <> Неопределено Тогда
		Попытка
			МаскаВерхнийРегистр = ВРег(МаскаИмени);
		Исключение
			МаскаВерхнийРегистр = "";
		КонецПопытки;
	КонецЕсли;
	
	Для Каждого ОписаниеТипа Из ТипыДляОбхода Цикл
		
		Коллекция = Неопределено;
		Попытка
			Коллекция = Вычислить("Метаданные." + ОписаниеТипа.Коллекция);
		Исключение
			Коллекция = Неопределено;
		КонецПопытки;
		
		Если Коллекция = Неопределено Тогда
			// В режиме "*" пропускаем недоступные коллекции, иначе считаем ошибкой (тип явно запросили)
			Если (ТипТипМетаданных = Тип("Строка") И ТипМетаданных = "*") Тогда
				Продолжить;
			Иначе
				Возврат Новый Структура("success, error", Ложь,
					"Коллекция метаданных не найдена/недоступна: Метаданные." + ОписаниеТипа.Коллекция);
			КонецЕсли;
		КонецЕсли;
		
		Для Каждого ОбъектМетаданных Из Коллекция Цикл
			
			Имя = "";
			Синоним = "";
			ПолноеИмя = "";
			
			Попытка
				Имя = ОбъектМетаданных.Имя;
			Исключение
				Имя = "";
			КонецПопытки;
			
			Попытка
				Синоним = ОбъектМетаданных.Синоним;
			Исключение
				Синоним = "";
			КонецПопытки;
			
			Если ПрошелФильтрПоМаске(Имя, Синоним, МаскаВерхнийРегистр) Тогда
				
				Попытка
					ПолноеИмя = ОбъектМетаданных.ПолноеИмя();
				Исключение
					ПолноеИмя = "";
				КонецПопытки;
				
				Если ПустаяСтрока(ПолноеИмя) Тогда
					Если Не ПустаяСтрока(Имя) Тогда
						ПолноеИмя = ОписаниеТипа.Тип + "." + Имя;
					Иначе
						ПолноеИмя = ОписаниеТипа.Тип;
					КонецЕсли;
				КонецЕсли;
				
				Стр = Таблица.Добавить();
				Стр.ПолноеИмя = ПолноеИмя;
				Стр.Синоним = Синоним;
				Стр.SortKey = ВРег(ПолноеИмя);
				
			КонецЕсли;
		КонецЦикла;
		
	КонецЦикла;
	
	Таблица.Сортировать("SortKey, ПолноеИмя");
	
	Всего = Таблица.Количество();
	Если Смещение < 0 Тогда
		Смещение = 0;
	КонецЕсли;
	
	СписокОбъектов = Новый Массив;
	Возвращено = 0;
	
	Если Смещение < Всего Тогда
		ИндексПоследний = Мин(Смещение + Лимит - 1, Всего - 1);
		Для Индекс = Смещение По ИндексПоследний Цикл
			СтрокаТаблицы = Таблица.Получить(Индекс);
			СписокОбъектов.Добавить(Новый Структура("ПолноеИмя, Синоним", СтрокаТаблицы.ПолноеИмя, СтрокаТаблицы.Синоним));
			Возвращено = Возвращено + 1;
		КонецЦикла;
	КонецЕсли;
	
	ЕстьЕще = (Смещение + Возвращено) < Всего;
	СледующееСмещение = Смещение + Возвращено;
	
	Возврат Новый Структура("success, data, truncated, limit, returned, count, offset, has_more, next_offset",
		Истина,
		СписокОбъектов,
		ЕстьЕще,
		Лимит,
		Возвращено,
		Всего,
		Смещение,
		ЕстьЕще,
		СледующееСмещение);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДобавитьСовпаденияАтрибутов(ОбъектМД, Префикс, МаскаВерхний, Совпадения)

	// СтандартныеРеквизиты → СтандартныйРеквизит
	Попытка
		Для Каждого А Из ОбъектМД.СтандартныеРеквизиты Цикл
			Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
				Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
					Префикс + ".СтандартныйРеквизит." + А.Имя, А.Синоним));
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;

	// Реквизиты → Реквизит
	Попытка
		Для Каждого А Из ОбъектМД.Реквизиты Цикл
			Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
				Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
					Префикс + ".Реквизит." + А.Имя, А.Синоним));
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;

	// РеквизитыАдресации → РеквизитАдресации (только Задача)
	Попытка
		Для Каждого А Из ОбъектМД.РеквизитыАдресации Цикл
			Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
				Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
					Префикс + ".РеквизитАдресации." + А.Имя, А.Синоним));
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;

	// ТабличныеЧасти.Реквизиты → ТабличнаяЧасть.ИмяТЧ.Реквизит
	Попытка
		Для Каждого ТЧ Из ОбъектМД.ТабличныеЧасти Цикл
			ПрефиксТЧ = Префикс + ".ТабличнаяЧасть." + ТЧ.Имя;
			Попытка
				Для Каждого А Из ТЧ.Реквизиты Цикл
					Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
						Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
							ПрефиксТЧ + ".Реквизит." + А.Имя, А.Синоним));
					КонецЕсли;
				КонецЦикла;
			Исключение КонецПопытки;
		КонецЦикла;
	Исключение КонецПопытки;

	// СтандартныеТабличныеЧасти (ПланСчетов/ПланВидовРасчета)
	Попытка
		Для Каждого ТЧ Из ОбъектМД.СтандартныеТабличныеЧасти Цикл
			ПрефиксСтдТЧ = Префикс + ".СтандартнаяТабличнаяЧасть." + ТЧ.Имя;
			Попытка
				Для Каждого А Из ТЧ.СтандартныеРеквизиты Цикл
					Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
						Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
							ПрефиксСтдТЧ + ".СтандартныйРеквизит." + А.Имя, А.Синоним));
					КонецЕсли;
				КонецЦикла;
			Исключение КонецПопытки;
		КонецЦикла;
	Исключение КонецПопытки;

	// Измерения → Измерение
	Попытка
		Для Каждого А Из ОбъектМД.Измерения Цикл
			Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
				Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
					Префикс + ".Измерение." + А.Имя, А.Синоним));
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;

	// Ресурсы → Ресурс
	Попытка
		Для Каждого А Из ОбъектМД.Ресурсы Цикл
			Если ПрошелФильтрПоМаске(А.Имя, А.Синоним, МаскаВерхний) Тогда
				Совпадения.Добавить(Новый Структура("ПолноеИмя, Синоним",
					Префикс + ".Ресурс." + А.Имя, А.Синоним));
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;

КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьМетаданныеПоАтрибуту(МаскаАтрибута, Фильтр = "", ТипМетаданных = "",
	МаскаИмени = "", Лимит = 100, Смещение = 0, ИмяРасширения = Неопределено, КэшПараметры = Неопределено)

	МаскаВерхний = ВРег(СокрЛП(МаскаАтрибута));
	Если ПустаяСтрока(МаскаВерхний) Тогда
		Возврат Новый Структура("success, error", Ложь, "attribute_mask cannot be empty");
	КонецЕсли;
	МаскаИмениВерхний = ВРег(СокрЛП(МаскаИмени));
	Совпадения = Новый Массив;
	НайденноеРасширение = Неопределено;

	// ──── Загрузка расширения (если нужно) ─────────────────────────────────────
	РезМД = Неопределено;
	Если ИмяРасширения <> Неопределено Тогда
		МассивРасширений = Неопределено;
		Попытка
			МассивРасширений = РасширенияКонфигурации.Получить(Новый Структура("Имя", ИмяРасширения));
		Исключение
			Возврат Новый Структура("success, error", Ложь,
				"Расширения конфигурации недоступны в данной версии платформы");
		КонецПопытки;
		// Защитная проверка типа — как в существующем коде
		Если МассивРасширений = Неопределено
			Или ТипЗнч(МассивРасширений) <> Тип("Массив")
			Или МассивРасширений.Количество() = 0 Тогда
			Возврат Новый Структура("success, error", Ложь, "Расширение не найдено: " + ИмяРасширения);
		КонецЕсли;
		НайденноеРасширение = МассивРасширений[0];
		РезМД = ПолучитьМетаданныеКонфигурацииРасширения(НайденноеРасширение, КэшПараметры);
		Если НЕ РезМД.success Тогда
			Возврат Новый Структура("success, error", Ложь, РезМД.error);
		КонецЕсли;
		// is_empty НЕ обрабатывается здесь — ранний возврат вынесен в case B.
		// В case A (filter задан) пустое расширение даст ОбъектМД = Неопределено
		// → "Объект не найден" — правильное поведение для конкретного объекта.
	КонецЕсли;

	// ──── Случай A: filter задан ────────────────────────────────────────────────
	Если НЕ ПустаяСтрока(Фильтр) Тогда

		// Валидация: filter должен быть формата Тип.Имя (ровно одна точка).
		// Паттерн через Найти — как в ПолучитьДетальныеМетаданныеРасширения строка 3489-3499.
		ПозТочки1 = Найти(Фильтр, ".");
		Если ПозТочки1 = 0 Тогда
			Возврат Новый Структура("success, error", Ложь,
				"attribute_mask + filter: filter must be a root object path (Type.Name)");
		КонецЕсли;
		// Пустой левый сегмент: ".Имя"
		Если ПозТочки1 = 1 Тогда
			Возврат Новый Структура("success, error", Ложь,
				"attribute_mask + filter: filter must be a root object path (Type.Name)");
		КонецЕсли;
		// Проверяем что после первой точки ещё точки нет, и правый сегмент не пустой
		ОстатокПослеТочки = Сред(Фильтр, ПозТочки1 + 1);
		Если ПустаяСтрока(ОстатокПослеТочки) Тогда
			// Пустой правый сегмент: "Тип."
			Возврат Новый Структура("success, error", Ложь,
				"attribute_mask + filter: filter must be a root object path (Type.Name)");
		КонецЕсли;
		Если Найти(ОстатокПослеТочки, ".") > 0 Тогда
			Возврат Новый Структура("success, error", Ложь,
				"attribute_mask + filter: filter must be a root object path (Type.Name), not a collection element path");
		КонецЕсли;

		ОбъектМД = Неопределено;
		Попытка
			Если РезМД <> Неопределено Тогда
				ОбъектМД = РезМД.metadata.НайтиПоПолномуИмени(Фильтр);
			Иначе
				ОбъектМД = Метаданные.НайтиПоПолномуИмени(Фильтр);
			КонецЕсли;
		Исключение
			Возврат Новый Структура("success, error", Ложь,
				"Ошибка при получении объекта: " + ОписаниеОшибки());
		КонецПопытки;

		Если ОбъектМД = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь, "Объект не найден: " + Фильтр);
		КонецЕсли;

		ДобавитьСовпаденияАтрибутов(ОбъектМД, Фильтр, МаскаВерхний, Совпадения);

	// ──── Случай B: итерируем все объекты ──────────────────────────────────────
	Иначе

		Типы = ПолучитьКорневыеТипыМетаданных();

		// Определяем допустимые типы и валидируем meta_type
		НетТипа = (ТипМетаданных = Неопределено)
			Или (ТипЗнч(ТипМетаданных) = Тип("Строка") И (ПустаяСтрока(ТипМетаданных) Или ТипМетаданных = "*"));

		РазрешённыеТипы = Неопределено;
		Если НЕ НетТипа Тогда
			РазрешённыеТипы = Новый Соответствие;
			МассивТипов = Неопределено;
			Если ТипЗнч(ТипМетаданных) = Тип("Строка") Тогда
				МассивТипов = Новый Массив;
				МассивТипов.Добавить(ТипМетаданных);
			ИначеЕсли ТипЗнч(ТипМетаданных) = Тип("Массив") Тогда
				МассивТипов = ТипМетаданных;
			Иначе
				Возврат Новый Структура("success, error", Ложь,
					"Некорректный формат meta_type (ожидается строка или массив строк)");
			КонецЕсли;
			Для Каждого ИмяТипа Из МассивТипов Цикл
				ИмяТипа = СокрЛП(Строка(ИмяТипа));
				НайденТип = Ложь;
				Для Каждого ОписаниеТипа Из Типы Цикл
					Если ОписаниеТипа.Тип = ИмяТипа Тогда
						НайденТип = Истина; Прервать;
					КонецЕсли;
				КонецЦикла;
				Если НЕ НайденТип Тогда
					Возврат Новый Структура("success, error", Ложь,
						"Неизвестный тип метаданных: " + ИмяТипа);
				КонецЕсли;
				РазрешённыеТипы.Вставить(ИмяТипа, Истина);
			КонецЦикла;
		КонецЕсли;

		// Пустое расширение в case B — возвращаем пустой список с полным cache-контрактом.
		// Делаем это здесь, а не в блоке загрузки, чтобы case A (filter задан) мог
		// провалиться до НайтиПоПолномуИмени и вернуть "Объект не найден".
		Если РезМД <> Неопределено И РезМД.is_empty Тогда
			Рез = Новый Структура(
				"success, data, truncated, limit, returned, count, offset, has_more, next_offset, extension",
				Истина, Новый Массив, Ложь, Лимит, 0, 0, Смещение, Ложь, Смещение, НайденноеРасширение.Имя);
			Рез.Вставить("mcp_cache_hash", РезМД.hash);
			Если РезМД.data_token <> Неопределено Тогда
				Рез.Вставить("mcp_cache_data_token", РезМД.data_token);
			КонецЕсли;
			Возврат Рез;
		КонецЕсли;

		// Итерируем коллекции.
		// ВАЖНО: для расширения переменная ОБЯЗАНА называться МетаданныеРасширения —
		// Вычислить("МетаданныеРасширения." + ...) обращается к локальной переменной по имени.
		// Это тот же паттерн что в ПостроитьИндексМетаданныхРасширения (строка 2873).
		Если РезМД <> Неопределено Тогда
			МетаданныеРасширения = РезМД.metadata;
		КонецЕсли;

		Для Каждого ОписаниеТипа Из Типы Цикл

			Если РазрешённыеТипы <> Неопределено
				И РазрешённыеТипы.Получить(ОписаниеТипа.Тип) = Неопределено Тогда
				Продолжить;
			КонецЕсли;

			Коллекция = Неопределено;
			Попытка
				Если РезМД <> Неопределено Тогда
					Коллекция = Вычислить("МетаданныеРасширения." + ОписаниеТипа.Коллекция);
				Иначе
					Коллекция = Вычислить("Метаданные." + ОписаниеТипа.Коллекция);
				КонецЕсли;
			Исключение
				Продолжить;
			КонецПопытки;
			Если Коллекция = Неопределено Тогда Продолжить; КонецЕсли;

			Для Каждого ОбъектМД Из Коллекция Цикл

				Если НЕ ПустаяСтрока(МаскаИмениВерхний) Тогда
					Попытка
						Если НЕ ПрошелФильтрПоМаске(ОбъектМД.Имя, ОбъектМД.Синоним, МаскаИмениВерхний) Тогда
							Продолжить;
						КонецЕсли;
					Исключение
						Продолжить;
					КонецПопытки;
				КонецЕсли;

				Попытка
					Префикс = ОписаниеТипа.Тип + "." + ОбъектМД.Имя;
				Исключение
					Продолжить;
				КонецПопытки;

				ДобавитьСовпаденияАтрибутов(ОбъектМД, Префикс, МаскаВерхний, Совпадения);

			КонецЦикла;
		КонецЦикла;

	КонецЕсли;

	// ──── Сортировка: двухключевая как в list mode (строка 2924) ────────────────
	ТаблицаСортировки = Новый ТаблицаЗначений;
	ТаблицаСортировки.Колонки.Добавить("SortKey");
	ТаблицаСортировки.Колонки.Добавить("ПолноеИмя");
	ТаблицаСортировки.Колонки.Добавить("Синоним");
	Для Каждого Эл Из Совпадения Цикл
		Стр = ТаблицаСортировки.Добавить();
		Стр.SortKey = ВРег(Эл.ПолноеИмя);
		Стр.ПолноеИмя = Эл.ПолноеИмя;
		Стр.Синоним = Эл.Синоним;
	КонецЦикла;
	ТаблицаСортировки.Сортировать("SortKey, ПолноеИмя");

	Всего = ТаблицаСортировки.Количество();
	СрезМассива = Новый Массив;
	Если Смещение < Всего Тогда
		ИндексПоследний = Мин(Смещение + Лимит - 1, Всего - 1);
		Для Индекс = Смещение По ИндексПоследний Цикл
			Стр = ТаблицаСортировки.Получить(Индекс);
			СрезМассива.Добавить(Новый Структура("ПолноеИмя, Синоним", Стр.ПолноеИмя, Стр.Синоним));
		КонецЦикла;
	КонецЕсли;
	Возвращено = СрезМассива.Количество();

	// next_offset = offset + returned (НЕ offset+limit)
	ЕстьЕще = (Смещение + Возвращено) < Всего;
	СледующееСмещение = Смещение + Возвращено;

	Рез = Новый Структура(
		"success, data, truncated, limit, returned, count, offset, has_more, next_offset",
		Истина, СрезМассива, ЕстьЕще, Лимит, Возвращено, Всего, Смещение, ЕстьЕще, СледующееСмещение);

	// extension-контракт (только если работали с расширением)
	Если НайденноеРасширение <> Неопределено Тогда
		Рез.Вставить("extension", НайденноеРасширение.Имя);
		Рез.Вставить("mcp_cache_hash", РезМД.hash);
		Если РезМД.data_token <> Неопределено Тогда
			Рез.Вставить("mcp_cache_data_token", РезМД.data_token);
		КонецЕсли;
	КонецЕсли;

	Возврат Рез;

КонецФункции

&НаСервереБезКонтекста
Функция ПрошелФильтрПоМаске(Имя, Синоним, МаскаВерхнийРегистр)
	
	Если ПустаяСтрока(МаскаВерхнийРегистр) Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Найти(ВРег(Имя), МаскаВерхнийРегистр) > 0 
		Или Найти(ВРег(Синоним), МаскаВерхнийРегистр) > 0;
	
КонецФункции


&НаСервереБезКонтекста
Функция СекцияЗапрошена(Секции, ИмяСекции)
	
	Если Секции = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Попытка
		Для Каждого Секция Из Секции Цикл
			Если ВРег(Строка(Секция)) = ВРег(ИмяСекции) Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
	Исключение
		// Некорректный формат sections
	КонецПопытки;
	
	Возврат Ложь;
	
КонецФункции


&НаСервереБезКонтекста
Функция ПолноеИмяМетаданныхЕслиВозможно(Значение, Глубина = 0)
	
	Если Значение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если Глубина > 5 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// Универсальный способ: пробуем метод ПолноеИмя() (для MetadataObject/ОбъектМетаданных).
	// Если метода нет или он недоступен — вернём Неопределено, и дальше будет fallback через БезопасноеЗначениеДляПередачи().
	ПолноеИмя = "";
	Попытка
		ПолноеИмя = Значение.ПолноеИмя();
	Исключение
		ПолноеИмя = "";
	КонецПопытки;
	
	Если НЕ ПустаяСтрока(ПолноеИмя) Тогда
		Возврат ПолноеИмя;
	КонецЕсли;

	// Если это коллекция (в т.ч. коллекция метаданных), попробуем обойти её и собрать ПолныеИмёна элементов.
	МассивПолныхИмен = Новый Массив;
	Попытка
		Для Каждого ЭлементКоллекции Из Значение Цикл
			ЗначениеЭлемента = ПолноеИмяМетаданныхЕслиВозможно(ЭлементКоллекции, Глубина + 1);
			Если ЗначениеЭлемента = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			Если ТипЗнч(ЗначениеЭлемента) = Тип("Массив") Тогда
				Для Каждого ЭлементМассива Из ЗначениеЭлемента Цикл
					Если ЭлементМассива <> Неопределено И НЕ ПустаяСтрока(Строка(ЭлементМассива)) Тогда
						МассивПолныхИмен.Добавить(ЭлементМассива);
					КонецЕсли;
				КонецЦикла;
			Иначе
				Если НЕ ПустаяСтрока(Строка(ЗначениеЭлемента)) Тогда
					МассивПолныхИмен.Добавить(ЗначениеЭлемента);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	Исключение
		МассивПолныхИмен = Неопределено;
	КонецПопытки;
	
	Если МассивПолныхИмен <> Неопределено И МассивПолныхИмен.Количество() > 0 Тогда
		Возврат МассивПолныхИмен;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции


&НаСервереБезКонтекста
Процедура ДобавитьКандидатыСвойствПоТипу(ТипОбъекта, Кандидаты)

	// Общие свойства (подставляются где доступны)
	Кандидаты.Добавить("Комментарий");

	Если ТипОбъекта = "ПодпискаНаСобытие" Тогда
		Кандидаты.Добавить("Источник");
		Кандидаты.Добавить("Обработчик");
		Кандидаты.Добавить("Событие");
	ИначеЕсли ТипОбъекта = "БизнесПроцесс" Тогда
		Кандидаты.Добавить("Автонумерация");
		Кандидаты.Добавить("ДлинаНомера");
		Кандидаты.Добавить("Задача");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("ПериодичностьНомера");
		Кандидаты.Добавить("ПривилегированныйРежимПриСозданииЗадач");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("ТипНомера");
	ИначеЕсли ТипОбъекта = "Задача" Тогда
		Кандидаты.Добавить("Автонумерация");
		Кандидаты.Добавить("АвтоПрефиксНомераЗадачи");
		Кандидаты.Добавить("Адресация");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ДлинаНомера");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("ОсновнойРеквизитАдресации");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("ТипНомера");
	ИначеЕсли ТипОбъекта = "Справочник" Тогда
		Кандидаты.Добавить("Иерархический");
		Кандидаты.Добавить("ВидИерархии");
		Кандидаты.Добавить("ИспользованиеПодчинения");
		Кандидаты.Добавить("Автонумерация");
		Кандидаты.Добавить("ДлинаКода");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ТипКода");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("ИсторияДанных");
	ИначеЕсли ТипОбъекта = "Документ" Тогда
		Кандидаты.Добавить("Автонумерация");
		Кандидаты.Добавить("ДлинаНомера");
		Кандидаты.Добавить("ТипНомера");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("Нумератор");
		Кандидаты.Добавить("Нумерация");
		Кандидаты.Добавить("ОперативноеПроведение");
		Кандидаты.Добавить("ПериодичностьНомера");
		Кандидаты.Добавить("ПривилегированныйРежимПриОтменеПроведения");
		Кандидаты.Добавить("ПривилегированныйРежимПриПроведении");
		Кандидаты.Добавить("Проведение");
		Кандидаты.Добавить("УдалениеДвижений");
		Кандидаты.Добавить("ЗаполнениеПоследовательностей");
		Кандидаты.Добавить("ЗаписьДвиженийПриПроведении");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("ИсторияДанных");
	ИначеЕсли ТипОбъекта = "РегистрСведений" Тогда
		Кандидаты.Добавить("Периодичность");
		Кандидаты.Добавить("РежимЗаписи");
		Кандидаты.Добавить("ПериодичностьРегистраСведений");
		Кандидаты.Добавить("ОсновнойОтборПоПериоду");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("РазрешитьИтогиСрезПервых");
		Кандидаты.Добавить("РазрешитьИтогиСрезПоследних");
	ИначеЕсли ТипОбъекта = "РегистрНакопления" Тогда
		Кандидаты.Добавить("ВидРегистра");
		Кандидаты.Добавить("РазрешитьРазделениеИтогов");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
	ИначеЕсли ТипОбъекта = "РегистрБухгалтерии" Тогда
		Кандидаты.Добавить("ПланСчетов");
		Кандидаты.Добавить("ДлинаУточненияПериода");
		Кандидаты.Добавить("Корреспонденция");
		Кандидаты.Добавить("РазрешитьРазделениеИтогов");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
	ИначеЕсли ТипОбъекта = "ПланСчетов" Тогда
		Кандидаты.Добавить("АвтоПорядокПоКоду");
		Кандидаты.Добавить("ВидыСубконто");
		Кандидаты.Добавить("ДлинаКода");
		Кандидаты.Добавить("ТипКода");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ДлинаПорядка");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("МаксКоличествоСубконто");
		Кандидаты.Добавить("МаскаКода");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("СерииКодов");
	ИначеЕсли ТипОбъекта = "РегистрРасчета" Тогда
		Кандидаты.Добавить("Периодичность");
		Кандидаты.Добавить("БазовыйПериод");
		Кандидаты.Добавить("График");
		Кандидаты.Добавить("ДатаГрафика");
		Кандидаты.Добавить("ЗначениеГрафика");
		Кандидаты.Добавить("ПериодДействия");
		Кандидаты.Добавить("ПланВидовРасчета");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
	ИначеЕсли ТипОбъекта = "ГруппаКоманд" Тогда
		Кандидаты.Добавить("Категория");
	ИначеЕсли ТипОбъекта = "ЖурналДокументов" Тогда
		Кандидаты.Добавить("Графы");
		Кандидаты.Добавить("РегистрируемыеДокументы");
	ИначеЕсли ТипОбъекта = "Константа" Тогда
		Кандидаты.Добавить("ВыделятьОтрицательные");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("Маска");
		Кандидаты.Добавить("МногострочныйРежим");
		Кандидаты.Добавить("ПараметрыВыбора");
		Кандидаты.Добавить("ПроверкаЗаполнения");
		Кандидаты.Добавить("РежимПароля");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("СвязиПараметровВыбора");
		Кандидаты.Добавить("СвязьПоТипу");
		Кандидаты.Добавить("Тип");
	ИначеЕсли ТипОбъекта = "КритерийОтбора" Тогда
		Кандидаты.Добавить("Состав");
		Кандидаты.Добавить("Тип");
	ИначеЕсли ТипОбъекта = "Нумератор" Тогда
		Кандидаты.Добавить("ДлинаНомера");
		Кандидаты.Добавить("ДопустимаяДлинаНомера");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("ПериодичностьНомера");
		Кандидаты.Добавить("ТипНомера");
	ИначеЕсли ТипОбъекта = "ОбщаяКоманда" Тогда
		Кандидаты.Добавить("Группа");
		Кандидаты.Добавить("ИзменяетДанные");
		Кандидаты.Добавить("Отображение");
		Кандидаты.Добавить("РежимИспользованияПараметра");
		Кандидаты.Добавить("ТипПараметраКоманды");
	ИначеЕсли ТипОбъекта = "ОбщийМодуль" Тогда
		Кандидаты.Добавить("ВнешнееСоединение");
		Кандидаты.Добавить("ВызовСервер");
		Кандидаты.Добавить("Глобальный");
		Кандидаты.Добавить("КлиентОбычноеПриложение");
		Кандидаты.Добавить("КлиентУправляемоеПриложение");
		Кандидаты.Добавить("ПовторноеИспользованиеВозвращаемыхЗначений");
		Кандидаты.Добавить("Привилегированный");
		Кандидаты.Добавить("Сервер");
	ИначеЕсли ТипОбъекта = "ОбщийРеквизит" Тогда
		Кандидаты.Добавить("АвтоИспользование");
		Кандидаты.Добавить("ВыделятьОтрицательные");
		Кандидаты.Добавить("ЗаполнятьИзДанныхЗаполнения");
		Кандидаты.Добавить("ЗначениеРазделенияДанных");
		Кандидаты.Добавить("ИспользованиеРазделенияДанных");
		Кандидаты.Добавить("ИспользованиеРазделяемыхДанных");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("Маска");
		Кандидаты.Добавить("МногострочныйРежим");
		Кандидаты.Добавить("ПроверкаЗаполнения");
		Кандидаты.Добавить("РазделениеАутентификации");
		Кандидаты.Добавить("РазделениеДанных");
		Кандидаты.Добавить("РазделениеПользователей");
		Кандидаты.Добавить("РазделениеРасширенийКонфигурации");
		Кандидаты.Добавить("РежимПароля");
		Кандидаты.Добавить("Состав");
		Кандидаты.Добавить("Тип");
		Кандидаты.Добавить("УсловноеРазделение");
	ИначеЕсли ТипОбъекта = "ОпределяемыйТип" Тогда
		Кандидаты.Добавить("Тип");
	ИначеЕсли ТипОбъекта = "Отчет" Тогда
		Кандидаты.Добавить("ОсновнаяСхемаКомпоновкиДанных");
		Кандидаты.Добавить("ХранилищеВариантов");
		Кандидаты.Добавить("ХранилищеНастроек");
	ИначеЕсли ТипОбъекта = "ПараметрФункциональныхОпций" Тогда
		Кандидаты.Добавить("Использование");
	ИначеЕсли ТипОбъекта = "ПланВидовРасчета" Тогда
		Кандидаты.Добавить("БазовыеВидыРасчета");
		Кандидаты.Добавить("ДлинаКода");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ДопустимаяДлинаКода");
		Кандидаты.Добавить("ЗависимостьОтВидовРасчета");
		Кандидаты.Добавить("ИспользованиеПериодаДействия");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("ТипКода");
	ИначеЕсли ТипОбъекта = "ПланВидовХарактеристик" Тогда
		Кандидаты.Добавить("Автонумерация");
		Кандидаты.Добавить("ГруппыСверху");
		Кандидаты.Добавить("ДлинаКода");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ДопустимаяДлинаКода");
		Кандидаты.Добавить("Иерархический");
		Кандидаты.Добавить("КонтрольУникальности");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("СерииКодов");
		Кандидаты.Добавить("Тип");
	ИначеЕсли ТипОбъекта = "ПланОбмена" Тогда
		Кандидаты.Добавить("ДлинаКода");
		Кандидаты.Добавить("ДлинаНаименования");
		Кандидаты.Добавить("ДопустимаяДлинаКода");
		Кандидаты.Добавить("ИсторияДанных");
		Кандидаты.Добавить("РежимУправленияБлокировкойДанных");
		Кандидаты.Добавить("Состав");
	ИначеЕсли ТипОбъекта = "Подсистема" Тогда
		Кандидаты.Добавить("ВключатьВКомандныйИнтерфейс");
		Кандидаты.Добавить("ИспользоватьОднуКоманду");
		Кандидаты.Добавить("Подсистемы");
		Кандидаты.Добавить("Состав");
	ИначеЕсли ТипОбъекта = "Последовательность" Тогда
		Кандидаты.Добавить("Движения");
		Кандидаты.Добавить("Документы");
		Кандидаты.Добавить("ПеремещениеГраницыПриПроведении");
	ИначеЕсли ТипОбъекта = "РегламентноеЗадание" Тогда
		Кандидаты.Добавить("ИмяМетода");
		Кандидаты.Добавить("ИнтервалПовтораПриАварийномЗавершении");
		Кандидаты.Добавить("Использование");
		Кандидаты.Добавить("Ключ");
		Кандидаты.Добавить("КоличествоПовторовПриАварийномЗавершении");
		Кандидаты.Добавить("Наименование");
		Кандидаты.Добавить("Предопределенное");
	ИначеЕсли ТипОбъекта = "ФункциональнаяОпция" Тогда
		Кандидаты.Добавить("ПривилегированныйРежимПриПолучении");
		Кандидаты.Добавить("Состав");
		Кандидаты.Добавить("Хранение");
	ИначеЕсли ТипОбъекта = "ОбщийМакет" Тогда
		Кандидаты.Добавить("Тип");
	ИначеЕсли ТипОбъекта = "HTTPСервис" Тогда
		Кандидаты.Добавить("ВремяЖизниСеанса");
		Кандидаты.Добавить("КорневойURL");
		Кандидаты.Добавить("ПовторноеИспользованиеСеансов");
		Кандидаты.Добавить("ШаблоныURL");
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДобавитьКандидатыСвойствЭлементаКоллекции(Кандидаты)
	Кандидаты.Добавить("БазовоеИзмерение");
	Кандидаты.Добавить("Балансовый");
	Кандидаты.Добавить("Ведущее");
	Кандидаты.Добавить("ВыделятьОтрицательные");
	Кандидаты.Добавить("ДанныеВедущихРегистров");
	Кандидаты.Добавить("ЗаполнятьИзДанныхЗаполнения");
	Кандидаты.Добавить("ЗапрещатьНезаполненныеЗначения");
	Кандидаты.Добавить("ИзмерениеАдресации");
	Кандидаты.Добавить("ИзмерениеРегистра");
	Кандидаты.Добавить("Использование");
	Кандидаты.Добавить("ИспользованиеВИтогах");
	Кандидаты.Добавить("ИсторияДанных");
	Кандидаты.Добавить("Маска");
	Кандидаты.Добавить("МногострочныйРежим");
	Кандидаты.Добавить("ОсновнойОтбор");
	Кандидаты.Добавить("ПараметрыВыбора");
	Кандидаты.Добавить("ПризнакУчета");
	Кандидаты.Добавить("ПризнакУчетаСубконто");
	Кандидаты.Добавить("ПроверкаЗаполнения");
	Кандидаты.Добавить("РежимПароля");
	Кандидаты.Добавить("СвязиПараметровВыбора");
	Кандидаты.Добавить("СвязьПоТипу");
	Кандидаты.Добавить("СоответствиеДвижениям");
	Кандидаты.Добавить("СоответствиеДокументам");
	Кандидаты.Добавить("ЗначениеЗаполнения");
КонецПроцедуры

&НаСервереБезКонтекста
Функция СекцияИзКоллекцииПолноеИмяСиноним(Коллекция)
	
	Результат = Новый Массив;
	
	Если Коллекция = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Для Каждого Элемент Из Коллекция Цикл
		
		ПолноеИмяЭлемента = "";
		СинонимЭлемента = "";
		
		Попытка
			ПолноеИмяЭлемента = Элемент.ПолноеИмя();
		Исключение
			ПолноеИмяЭлемента = "";
		КонецПопытки;
		
		Если ПустаяСтрока(ПолноеИмяЭлемента) Тогда
			Попытка
				ПолноеИмяЭлемента = Строка(Элемент);
			Исключение
				ПолноеИмяЭлемента = "";
			КонецПопытки;
		КонецЕсли;
		
		Попытка
			СинонимЭлемента = Элемент.Синоним;
		Исключение
			СинонимЭлемента = "";
		КонецПопытки;
		
		Результат.Добавить(Новый Структура("ПолноеИмя, Синоним", ПолноеИмяЭлемента, СинонимЭлемента));

	КонецЦикла;

	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция СекцияИзКоллекцииМакетов(Коллекция)

	Результат = Новый Массив;

	Если Коллекция = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;

	Для Каждого Элемент Из Коллекция Цикл

		ПолноеИмяЭлемента = "";
		СинонимЭлемента = "";
		ТипМакетаЭлемента = "";

		Попытка
			ПолноеИмяЭлемента = Элемент.ПолноеИмя();
		Исключение
			ПолноеИмяЭлемента = "";
		КонецПопытки;

		Если ПустаяСтрока(ПолноеИмяЭлемента) Тогда
			Попытка
				ПолноеИмяЭлемента = Строка(Элемент);
			Исключение
				ПолноеИмяЭлемента = "";
			КонецПопытки;
		КонецЕсли;

		Попытка
			СинонимЭлемента = Элемент.Синоним;
		Исключение
			СинонимЭлемента = "";
		КонецПопытки;

		Попытка
			ТипМакетаЭлемента = Строка(Элемент.ТипМакета);
		Исключение
			ТипМакетаЭлемента = "";
		КонецПопытки;

		Результат.Добавить(Новый Структура("ПолноеИмя, Синоним, ТипМакета",
			ПолноеИмяЭлемента, СинонимЭлемента, ТипМакетаЭлемента));

	КонецЦикла;

	Возврат Результат;

КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьПредопределенныеЭлементыСправочника(ИмяСправочника, Лимит = 200)
	
	Если ПустаяСтрока(ИмяСправочника) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = Новый Массив;
	
	Попытка
		
		ТекстЗапроса = 
			"ВЫБРАТЬ ПЕРВЫЕ " + Строка(Лимит) + Символы.ПС +
			"	ИмяПредопределенныхДанных КАК ИмяПредопределенныхДанных," + Символы.ПС +
			"	ПРЕДСТАВЛЕНИЕ(Ссылка) КАК Представление" + Символы.ПС +
			"ИЗ" + Символы.ПС +
			"	Справочник." + ИмяСправочника + Символы.ПС +
			"ГДЕ" + Символы.ПС +
			"	Предопределенный";
		
		Запрос = Новый Запрос(ТекстЗапроса);
		Таблица = Запрос.Выполнить().Выгрузить();
		
		Для Каждого СтрокаТаблицы Из Таблица Цикл
			Результат.Добавить(Новый Структура(
				"ИмяПредопределенныхДанных, Представление",
				СтрокаТаблицы.ИмяПредопределенныхДанных,
				СтрокаТаблицы.Представление
			));
		КонецЦикла;
		
		Возврат Результат;
		
	Исключение
		// Не удалось получить предопределенные данные (недоступно/нет прав/не поддерживается)
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьExtensionMarksЕслиНужно(МетаОбъект)
	// Возвращает структуру extension_marks для объекта метаданных, если объект
	// добавлен/заимствован/изменён расширениями конфигурации.
	// Если объект "обычный" (основная конфигурация, Собственный, нет изменений расширениями),
	// то возвращает Неопределено, чтобы не засорять ответ.
	
	Если МетаОбъект = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Принадлежность = Неопределено;
	Попытка
		Принадлежность = МетаОбъект.ПринадлежностьОбъекта;
	Исключение
		Принадлежность = Неопределено;
	КонецПопытки;
	
	Расширение = Неопределено;
	Попытка
		Расширение = МетаОбъект.РасширениеКонфигурации();
	Исключение
		Расширение = Неопределено;
	КонецПопытки;
	
	ЕстьИзменения = Неопределено;
	Попытка
		ЕстьИзменения = (МетаОбъект.ЕстьИзмененияРасширениямиКонфигурации() = Истина);
	Исключение
		ЕстьИзменения = Неопределено;
	КонецПопытки;
	
	Показать = Ложь;
	
	Если Расширение <> Неопределено Тогда
		Показать = Истина;
	КонецЕсли;
	
	Если ЕстьИзменения = Истина Тогда
		Показать = Истина;
	КонецЕсли;
	
	Если НЕ Показать И Принадлежность <> Неопределено Тогда
		// В некоторых окружениях (толстый клиент/режим совместимости) идентификатор
		// ПринадлежностьОбъекта.<Значение> может быть недоступен как глобальный.
		// Поэтому сравниваем по строковому представлению (best effort).
		СтрПринадлежность = Строка(Принадлежность);
		Если СтрПринадлежность <> "Собственный" И СтрПринадлежность <> "Native" Тогда
			Показать = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ Показать Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИнфоРасширения = Неопределено;
	Если Расширение <> Неопределено Тогда
		ИнфоРасширения = Новый Структура;
		Попытка
			ИнфоРасширения.Вставить("Имя", Расширение.Имя);
		Исключение
		КонецПопытки;
		Попытка
			ИнфоРасширения.Вставить("Синоним", Расширение.Синоним);
		Исключение
		КонецПопытки;
		Попытка
			ИнфоРасширения.Вставить("УникальныйИдентификатор", БезопасноеЗначениеДляПередачи(Расширение.УникальныйИдентификатор));
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	Метки = Новый Структура;
	Метки.Вставить("ПринадлежностьОбъекта", ?(Принадлежность = Неопределено, Неопределено, Строка(Принадлежность)));
	Метки.Вставить("РасширениеКонфигурации", ИнфоРасширения);
	Метки.Вставить("ЕстьИзмененияРасширениямиКонфигурации", ЕстьИзменения);
	
	Возврат Метки;
	
КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьExtensionMarksИзМетаданныхРасширения(МетаОбъектРасширения)
	// Маркеры "принадлежности" для объекта, полученного из ОбъектМетаданныхКонфигурация
	// (т.е. из бинарных метаданных расширения).
	//
	// ВАЖНО: метод ЕстьИзмененияРасширениямиКонфигурации() в этом контексте может существовать,
	// но семантически относится к рантайм-метаданным текущего сеанса и на объекте расширения
	// обычно бесполезен (часто всегда Ложь). Поэтому тут возвращаем только то, что точно
	// относится к метаданным расширения: ПринадлежностьОбъекта (Native/Adopted).
	
	Метки = Новый Структура;
	
	Принадлежность = Неопределено;
	Попытка
		Принадлежность = МетаОбъектРасширения.ПринадлежностьОбъекта;
	Исключение
		Принадлежность = Неопределено;
	КонецПопытки;
	
	Метки.Вставить("ПринадлежностьОбъекта", ?(Принадлежность = Неопределено, Неопределено, Строка(Принадлежность)));
	
	Возврат Метки;
КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьExtensionMarksДляДочернегоИзМетаданныхРасширения(МетаОбъектРасширения)
	// Для дочерних объектов (реквизиты/табличные части/измерения/ресурсы) показываем метки
	// только если ПринадлежностьОбъекта = "Собственный" (Native), чтобы не раздувать ответ.
	
	Метки = ПолучитьExtensionMarksИзМетаданныхРасширения(МетаОбъектРасширения);
	
	СтрПринадлежность = "";
	Попытка
		Если Метки <> Неопределено И Метки.Свойство("ПринадлежностьОбъекта") И Метки.ПринадлежностьОбъекта <> Неопределено Тогда
			СтрПринадлежность = Строка(Метки.ПринадлежностьОбъекта);
		КонецЕсли;
	Исключение
		СтрПринадлежность = "";
	КонецПопытки;
	
	Если СтрПринадлежность = "Собственный" Или СтрПринадлежность = "Native" Тогда
		Возврат Метки;
	КонецЕсли;
	
	Возврат Неопределено;
КонецФункции


&НаСервереБезКонтекста
Функция ПолучитьДетальныеМетаданные(ПолноеИмя, Секции = Неопределено)
	
	// Разбор полного имени (например, "Справочник.Номенклатура")
	ПозицияТочки = Найти(ПолноеИмя, ".");
	Если ПозицияТочки = 0 Тогда
		Возврат Новый Структура("success, error", Ложь, "Неверный формат имени объекта. Ожидается: Тип.Имя");
	КонецЕсли;
	
	ТипОбъекта = Лев(ПолноеИмя, ПозицияТочки - 1);
	ИмяОбъекта = Сред(ПолноеИмя, ПозицияТочки + 1);
	
	// Поиск объекта метаданных
	ОбъектМетаданных = Неопределено;
	
	// Основной способ: поиск по полному имени (поддерживает все типы, если доступно в платформе)
	Попытка
		ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ПолноеИмя);
	Исключение
		ОбъектМетаданных = Неопределено;
	КонецПопытки;
	
	// Fallback: поиск в коллекции по корневому типу
	Если ОбъектМетаданных = Неопределено Тогда
		
		ОписаниеТипа = Неопределено;
		Для Каждого Описание Из ПолучитьКорневыеТипыМетаданных() Цикл
			Если Описание.Тип = ТипОбъекта Тогда
				ОписаниеТипа = Описание;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ОписаниеТипа <> Неопределено Тогда
			Коллекция = Неопределено;
			Попытка
				Коллекция = Вычислить("Метаданные." + ОписаниеТипа.Коллекция);
			Исключение
				Коллекция = Неопределено;
			КонецПопытки;
			
			Если Коллекция <> Неопределено Тогда
				Попытка
					ОбъектМетаданных = Коллекция.Найти(ИмяОбъекта);
				Исключение
					ОбъектМетаданных = Неопределено;
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ОбъектМетаданных = Неопределено Тогда
		// Для путей с >1 точкой (элементы коллекций) пробуем найти через родителя
		// Нужно для стандартных реквизитов, которые НайтиПоПолномуИмени не находит
		КоличествоТочек = 0;
		Для Сч = 1 По СтрДлина(ПолноеИмя) Цикл
			Если Сред(ПолноеИмя, Сч, 1) = "." Тогда
				КоличествоТочек = КоличествоТочек + 1;
			КонецЕсли;
		КонецЦикла;

		Если КоличествоТочек > 1 Тогда
			// Извлекаем "Тип.Объект" (первые 2 сегмента) и "ОстатокПути"
			ПозПервойТочки = Найти(ПолноеИмя, ".");
			ОстатокПослеТипа = Сред(ПолноеИмя, ПозПервойТочки + 1);
			ПозВторойТочки = Найти(ОстатокПослеТипа, ".");
			// Защита от кривого filter (например "Тип.Объект." без имени элемента)
			Если ПозВторойТочки > 0 И ПозВторойТочки < СтрДлина(ОстатокПослеТипа) Тогда
				ИмяРодителя = Лев(ПолноеИмя, ПозПервойТочки + ПозВторойТочки - 1);
				ОстатокПутиЭлемента = Сред(ОстатокПослеТипа, ПозВторойТочки + 1);

				// Находим родительский объект
				РодительМД = Неопределено;
				Попытка
					РодительМД = Метаданные.НайтиПоПолномуИмени(ИмяРодителя);
				Исключение
				КонецПопытки;

				Если РодительМД <> Неопределено Тогда
					ЭлементМД = НайтиЭлементВКоллекцииОбъекта(РодительМД, ОстатокПутиЭлемента);
					Если ЭлементМД <> Неопределено Тогда
						Возврат ПолучитьДетальныеМетаданныеЭлемента(ЭлементМД, ПолноеИмя, Секции);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		Возврат Новый Структура("success, error", Ложь, "Объект не найден: " + ПолноеИмя);
	КонецЕсли;

	// Уточняем тип по фактическому полному имени (на случай, если исходная строка была нестандартной)
	ПолноеИмяФакт = ПолноеИмя;
	Попытка
		ПолноеИмяФакт = ОбъектМетаданных.ПолноеИмя();
	Исключение
		ПолноеИмяФакт = ПолноеИмя;
	КонецПопытки;
	
	ПозицияТочкиФакт = Найти(ПолноеИмяФакт, ".");
	Если ПозицияТочкиФакт > 0 Тогда
		ТипОбъекта = Лев(ПолноеИмяФакт, ПозицияТочкиФакт - 1);
	КонецЕсли;

	// Проверяем, является ли найденный объект элементом коллекции (Реквизит, Измерение и т.д.)
	// "Тип.Объект" = 1 точка, "Тип.Объект.Коллекция.Элемент" = 3 точки
	КоличествоТочекФакт = 0;
	Для Сч = 1 По СтрДлина(ПолноеИмяФакт) Цикл
		Если Сред(ПолноеИмяФакт, Сч, 1) = "." Тогда
			КоличествоТочекФакт = КоличествоТочекФакт + 1;
		КонецЕсли;
	КонецЦикла;

	Если КоличествоТочекФакт > 1 Тогда
		// Это элемент коллекции — возвращаем информацию только о нём
		Возврат ПолучитьДетальныеМетаданныеЭлемента(ОбъектМетаданных, ПолноеИмяФакт, Секции);
	КонецЕсли;
	
	// Формирование информации об объекте
	ИнформацияОбОбъекте = Новый Структура;
	ИнформацияОбОбъекте.Вставить("ТипОбъектаМетаданных", ТипОбъекта);
	ИнформацияОбОбъекте.Вставить("Имя", ОбъектМетаданных.Имя);
	ИнформацияОбОбъекте.Вставить("Синоним", ОбъектМетаданных.Синоним);
	ИнформацияОбОбъекте.Вставить("ПолноеИмя", ПолноеИмяФакт);
	
	// Маркеры расширений (показываем только если объект не "обычный")
	МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(ОбъектМетаданных);
	Если МеткиРасширений <> Неопределено Тогда
		ИнформацияОбОбъекте.Вставить("extension_marks", МеткиРасширений);
	КонецЕсли;
	
	// Реквизиты
	Реквизиты = Новый Массив;
	ЕстьКоллекцияРеквизитов = Ложь;

	// Стандартные реквизиты (Код, Наименование, Ссылка и т.д.)
	Попытка
		КоллекцияСтдРеквизитов = ОбъектМетаданных.СтандартныеРеквизиты;
		ЕстьКоллекцияРеквизитов = Истина;
		Для Каждого СтдРеквизит Из КоллекцияСтдРеквизитов Цикл
			Попытка
				СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип, Стандартный",
					СтдРеквизит.Имя,
					СтдРеквизит.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(СтдРеквизит.Тип),
					Истина);

				МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(СтдРеквизит);
				Если МеткиРасширений <> Неопределено Тогда
					СтрокаРеквизита.Вставить("extension_marks", МеткиРасширений);
				КонецЕсли;

				Реквизиты.Добавить(СтрокаРеквизита);
			Исключение
				// Пропускаем проблемный стандартный реквизит
			КонецПопытки;
		КонецЦикла;
	Исключение
		// У объекта нет коллекции СтандартныеРеквизиты
	КонецПопытки;

	// Пользовательские реквизиты
	Попытка
		КоллекцияРеквизитов = ОбъектМетаданных.Реквизиты;
		ЕстьКоллекцияРеквизитов = Истина;
		Для Каждого Реквизит Из КоллекцияРеквизитов Цикл
			СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип, Стандартный",
				Реквизит.Имя,
				Реквизит.Синоним,
				ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип),
				Ложь);

			МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(Реквизит);
			Если МеткиРасширений <> Неопределено Тогда
				СтрокаРеквизита.Вставить("extension_marks", МеткиРасширений);
			КонецЕсли;

			Реквизиты.Добавить(СтрокаРеквизита);
		КонецЦикла;
	Исключение
		// У некоторых объектов нет пользовательских реквизитов
	КонецПопытки;

	Если ЕстьКоллекцияРеквизитов Тогда
		ИнформацияОбОбъекте.Вставить("Реквизиты", Реквизиты);
	КонецЕсли;
	
	// Реквизиты адресации (для задач)
	Если ТипОбъекта = "Задача" Тогда
		РеквизитыАдресации = Неопределено;
		Попытка
			РеквизитыАдресации = Новый Массив;
			Для Каждого РеквизитАдресации Из ОбъектМетаданных.РеквизитыАдресации Цикл
				СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип",
					РеквизитАдресации.Имя,
					РеквизитАдресации.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(РеквизитАдресации.Тип));
				
				МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(РеквизитАдресации);
				Если МеткиРасширений <> Неопределено Тогда
					СтрокаРеквизита.Вставить("extension_marks", МеткиРасширений);
				КонецЕсли;
				
				РеквизитыАдресации.Добавить(СтрокаРеквизита);
			КонецЦикла;
		Исключение
			// Секция не применима/недоступна
			РеквизитыАдресации = Неопределено;
		КонецПопытки;
		
		Если РеквизитыАдресации <> Неопределено Тогда
			ИнформацияОбОбъекте.Вставить("РеквизитыАдресации", РеквизитыАдресации);
		КонецЕсли;
	КонецЕсли;
	
	// Табличные части
	ТабличныеЧасти = Неопределено;
	Попытка
		ТабличныеЧасти = Новый Массив;
		Для Каждого ТабличнаяЧасть Из ОбъектМетаданных.ТабличныеЧасти Цикл
			РеквизитыТЧ = Новый Массив;
			Для Каждого Реквизит Из ТабличнаяЧасть.Реквизиты Цикл
				СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип",
					Реквизит.Имя,
					Реквизит.Синоним,
					ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип));
				
				МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(Реквизит);
				Если МеткиРасширений <> Неопределено Тогда
					СтрокаРеквизита.Вставить("extension_marks", МеткиРасширений);
				КонецЕсли;
				
				РеквизитыТЧ.Добавить(СтрокаРеквизита);
			КонецЦикла;
			
			СтрокаТЧ = Новый Структура("Имя, Синоним, Реквизиты",
				ТабличнаяЧасть.Имя,
				ТабличнаяЧасть.Синоним,
				РеквизитыТЧ);
			
			МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(ТабличнаяЧасть);
			Если МеткиРасширений <> Неопределено Тогда
				СтрокаТЧ.Вставить("extension_marks", МеткиРасширений);
			КонецЕсли;
			
			ТабличныеЧасти.Добавить(СтрокаТЧ);
		КонецЦикла;
	Исключение
		// У некоторых объектов нет табличных частей (секция не применима)
		ТабличныеЧасти = Неопределено;
	КонецПопытки;
	Если ТабличныеЧасти <> Неопределено Тогда
		ИнформацияОбОбъекте.Вставить("ТабличныеЧасти", ТабличныеЧасти);
	КонецЕсли;
	
	// Измерения (для регистров)
	Измерения = Неопределено;
	Попытка
		Измерения = Новый Массив;
		Для Каждого Измерение Из ОбъектМетаданных.Измерения Цикл
			СтрокаИзмерения = Новый Структура("Имя, Синоним, Тип",
				Измерение.Имя,
				Измерение.Синоним,
				ПолучитьСтроковоеПредставлениеОписанияТипов(Измерение.Тип));
			
			МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(Измерение);
			Если МеткиРасширений <> Неопределено Тогда
				СтрокаИзмерения.Вставить("extension_marks", МеткиРасширений);
			КонецЕсли;
			
			Измерения.Добавить(СтрокаИзмерения);
		КонецЦикла;
	Исключение
		// У некоторых объектов нет измерений (секция не применима)
		Измерения = Неопределено;
	КонецПопытки;
	Если Измерения <> Неопределено Тогда
		ИнформацияОбОбъекте.Вставить("Измерения", Измерения);
	КонецЕсли;
	
	// Ресурсы (для регистров)
	Ресурсы = Неопределено;
	Попытка
		Ресурсы = Новый Массив;
		Для Каждого Ресурс Из ОбъектМетаданных.Ресурсы Цикл
			СтрокаРесурса = Новый Структура("Имя, Синоним, Тип",
				Ресурс.Имя,
				Ресурс.Синоним,
				ПолучитьСтроковоеПредставлениеОписанияТипов(Ресурс.Тип));
			
			МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(Ресурс);
			Если МеткиРасширений <> Неопределено Тогда
				СтрокаРесурса.Вставить("extension_marks", МеткиРасширений);
			КонецЕсли;
			
			Ресурсы.Добавить(СтрокаРесурса);
		КонецЦикла;
	Исключение
		// У некоторых объектов нет ресурсов (секция не применима)
		Ресурсы = Неопределено;
	КонецПопытки;
	Если Ресурсы <> Неопределено Тогда
		ИнформацияОбОбъекте.Вставить("Ресурсы", Ресурсы);
	КонецЕсли;
	
	// Значения перечисления
	Если ТипОбъекта = "Перечисление" Тогда
		ЗначенияПеречисления = Новый Массив;
		Для Каждого ЗначениеПеречисления Из ОбъектМетаданных.ЗначенияПеречисления Цикл
			ЗначенияПеречисления.Добавить(Новый Структура("Имя, Синоним", 
				ЗначениеПеречисления.Имя, 
				ЗначениеПеречисления.Синоним));
		КонецЦикла;
		ИнформацияОбОбъекте.Вставить("ЗначенияПеречисления", ЗначенияПеречисления);
	КонецЕсли;
	
	// Владельцы (для подчинённых справочников)
	Если ТипОбъекта = "Справочник" Тогда
		Попытка
			Если ОбъектМетаданных.Владельцы.Количество() > 0 Тогда
				Владельцы = Новый Массив;
				Для Каждого Владелец Из ОбъектМетаданных.Владельцы Цикл
					Владельцы.Добавить(Владелец.ПолноеИмя());
				КонецЦикла;
				ИнформацияОбОбъекте.Вставить("Владельцы", Владельцы);
			КонецЕсли;
		Исключение
			// У некоторых справочников нет владельцев
		КонецПопытки;
	КонецЕсли;

	// ПризнакиУчета и ПризнакиУчетаСубконто (только для планов счетов)
	Если ТипОбъекта = "ПланСчетов" Тогда
		Попытка
			КоллекцияПризнаков = ОбъектМетаданных.ПризнакиУчета;
			Если КоллекцияПризнаков <> Неопределено Тогда
				МассивПризнаков = Новый Массив;
				Для Каждого Признак Из КоллекцияПризнаков Цикл
					МассивПризнаков.Добавить(Новый Структура("Имя, Синоним, Тип",
						Признак.Имя, Признак.Синоним,
						ПолучитьСтроковоеПредставлениеОписанияТипов(Признак.Тип)));
				КонецЦикла;
				Если МассивПризнаков.Количество() > 0 Тогда
					ИнформацияОбОбъекте.Вставить("ПризнакиУчета", МассивПризнаков);
				КонецЕсли;
			КонецЕсли;
		Исключение
		КонецПопытки;

		Попытка
			КоллекцияПризнаковСубконто = ОбъектМетаданных.ПризнакиУчетаСубконто;
			Если КоллекцияПризнаковСубконто <> Неопределено Тогда
				МассивПризнаковСубконто = Новый Массив;
				Для Каждого Признак Из КоллекцияПризнаковСубконто Цикл
					МассивПризнаковСубконто.Добавить(Новый Структура("Имя, Синоним, Тип",
						Признак.Имя, Признак.Синоним,
						ПолучитьСтроковоеПредставлениеОписанияТипов(Признак.Тип)));
				КонецЦикла;
				Если МассивПризнаковСубконто.Количество() > 0 Тогда
					ИнформацияОбОбъекте.Вставить("ПризнакиУчетаСубконто", МассивПризнаковСубконто);
				КонецЕсли;
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;

	// СтандартныеТабличныеЧасти (для ПланСчетов и ПланВидовРасчета)
	Если ТипОбъекта = "ПланСчетов" ИЛИ ТипОбъекта = "ПланВидовРасчета" Тогда
		Попытка
			СтандартныеТабличныеЧасти = Новый Массив;
			Для Каждого СтдТЧ Из ОбъектМетаданных.СтандартныеТабличныеЧасти Цикл
				РеквизитыСтдТЧ = Новый Массив;
				Попытка
					Для Каждого СтдРеквизит Из СтдТЧ.СтандартныеРеквизиты Цикл
						Попытка
							РеквизитыСтдТЧ.Добавить(Новый Структура("Имя, Синоним, Тип",
								СтдРеквизит.Имя,
								СтдРеквизит.Синоним,
								ПолучитьСтроковоеПредставлениеОписанияТипов(СтдРеквизит.Тип)));
						Исключение
						КонецПопытки;
					КонецЦикла;
				Исключение
				КонецПопытки;

				СтрокаСтдТЧ = Новый Структура("Имя, Синоним, Реквизиты",
					СтдТЧ.Имя,
					СтдТЧ.Синоним,
					РеквизитыСтдТЧ);

				МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(СтдТЧ);
				Если МеткиРасширений <> Неопределено Тогда
					СтрокаСтдТЧ.Вставить("extension_marks", МеткиРасширений);
				КонецЕсли;

				СтандартныеТабличныеЧасти.Добавить(СтрокаСтдТЧ);
			КонецЦикла;
			Если СтандартныеТабличныеЧасти.Количество() > 0 Тогда
				ИнформацияОбОбъекте.Вставить("СтандартныеТабличныеЧасти", СтандартныеТабличныеЧасти);
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;

	// Дополнительные секции детального ответа (управляемый "богатый" режим)
	Если СекцияЗапрошена(Секции, "properties") Тогда

		Свойства = Новый Структура;
		Кандидаты = Новый Массив;
		
		ДобавитьКандидатыСвойствПоТипу(ТипОбъекта, Кандидаты);
		
		Для Каждого ИмяСвойства Из Кандидаты Цикл
			ЗначениеСвойства = Неопределено;
			Попытка
				ЗначениеСвойства = Вычислить("ОбъектМетаданных." + ИмяСвойства);
			Исключение
				ЗначениеСвойства = Неопределено;
			КонецПопытки;
			
			Если ЗначениеСвойства <> Неопределено Тогда
				ЗначениеДляJSON = ПолноеИмяМетаданныхЕслиВозможно(ЗначениеСвойства);
				Если ЗначениеДляJSON = Неопределено Тогда
					ЗначениеДляJSON = БезопасноеЗначениеДляПередачи(ЗначениеСвойства);
				КонецЕсли;
				Свойства.Вставить(ИмяСвойства, ЗначениеДляJSON);
			КонецЕсли;
		КонецЦикла;
		
		ИнформацияОбОбъекте.Вставить("properties", Свойства);
		
	КонецЕсли;
	
	Если СекцияЗапрошена(Секции, "forms") Тогда
		КоллекцияФорм = Неопределено;
		Попытка
			КоллекцияФорм = ОбъектМетаданных.Формы;
		Исключение
			КоллекцияФорм = Неопределено;
		КонецПопытки;
		
		Если КоллекцияФорм <> Неопределено Тогда
			ИнформацияОбОбъекте.Вставить("forms", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияФорм));
		КонецЕсли;
	КонецЕсли;
	
	Если СекцияЗапрошена(Секции, "commands") Тогда
		КоллекцияКоманд = Неопределено;
		Попытка
			КоллекцияКоманд = ОбъектМетаданных.Команды;
		Исключение
			КоллекцияКоманд = Неопределено;
		КонецПопытки;
		
		Если КоллекцияКоманд <> Неопределено Тогда
			ИнформацияОбОбъекте.Вставить("commands", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияКоманд));
		КонецЕсли;
	КонецЕсли;
	
	Если СекцияЗапрошена(Секции, "layouts") Тогда
		КоллекцияМакетов = Неопределено;
		Попытка
			КоллекцияМакетов = ОбъектМетаданных.Макеты;
		Исключение
			КоллекцияМакетов = Неопределено;
		КонецПопытки;
		
		Если КоллекцияМакетов <> Неопределено Тогда
			ИнформацияОбОбъекте.Вставить("layouts", СекцияИзКоллекцииМакетов(КоллекцияМакетов));
		КонецЕсли;
	КонецЕсли;
	
	Если СекцияЗапрошена(Секции, "predefined") Тогда
		
		// 1) Если в рантайм-метаданных есть коллекция предопределенных - используем ее
		КоллекцияПредопределенных = Неопределено;
		Попытка
			КоллекцияПредопределенных = ОбъектМетаданных.Предопределенные;
		Исключение
			КоллекцияПредопределенных = Неопределено;
		КонецПопытки;
		
		Если КоллекцияПредопределенных <> Неопределено Тогда
			Попытка
				ИнформацияОбОбъекте.Вставить("predefined", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияПредопределенных));
			Исключение
				// Если это не коллекция - игнорируем и пробуем fallback ниже
			КонецПопытки;
		КонецЕсли;
		
		// 2) Fallback для справочников: предопределенные данные хранятся как данные (отбор по Предопределенный)
		Если Не ИнформацияОбОбъекте.Свойство("predefined") И ТипОбъекта = "Справочник" Тогда
			ПредопределенныеДанные = ПолучитьПредопределенныеЭлементыСправочника(ИмяОбъекта, 200);
			Если ПредопределенныеДанные <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("predefined", ПредопределенныеДанные);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;

	// Движения (только для документов — список регистров, в которых документ делает движения)
	Если СекцияЗапрошена(Секции, "movements") Тогда
		Если ТипОбъекта = "Документ" Тогда
			КоллекцияДвижений = Неопределено;
			Попытка
				КоллекцияДвижений = ОбъектМетаданных.Движения;
			Исключение
				КоллекцияДвижений = Неопределено;
			КонецПопытки;

			Если КоллекцияДвижений <> Неопределено Тогда
				ИнформацияОбОбъекте.Вставить("movements", СекцияИзКоллекцииПолноеИмяСиноним(КоллекцияДвижений));
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	// Характеристики (описания характеристик, привязанных к объекту метаданных)
	Если СекцияЗапрошена(Секции, "characteristics") Тогда
		КоллекцияХарактеристик = Неопределено;
		Попытка
			КоллекцияХарактеристик = ОбъектМетаданных.Характеристики;
		Исключение
			КоллекцияХарактеристик = Неопределено;
		КонецПопытки;

		Если КоллекцияХарактеристик <> Неопределено Тогда
			МассивХарактеристик = Новый Массив;
			Для Каждого ОписаниеХарактеристики Из КоллекцияХарактеристик Цикл
				ЭлементХарактеристики = Новый Структура;

				// ВидыХарактеристик — ссылка на ПланВидовХарактеристик → ПолноеИмя()
				Попытка
					ВидыХар = ОписаниеХарактеристики.ВидыХарактеристик;
					Если ВидыХар <> Неопределено Тогда
						Попытка
							ЭлементХарактеристики.Вставить("ВидыХарактеристик", ВидыХар.ПолноеИмя());
						Исключение
							ЭлементХарактеристики.Вставить("ВидыХарактеристик", Строка(ВидыХар));
						КонецПопытки;
					КонецЕсли;
				Исключение
				КонецПопытки;

				// ЗначенияХарактеристик — ссылка на регистр сведений → ПолноеИмя()
				Попытка
					ЗначенияХар = ОписаниеХарактеристики.ЗначенияХарактеристик;
					Если ЗначенияХар <> Неопределено Тогда
						Попытка
							ЭлементХарактеристики.Вставить("ЗначенияХарактеристик", ЗначенияХар.ПолноеИмя());
						Исключение
							ЭлементХарактеристики.Вставить("ЗначенияХарактеристик", Строка(ЗначенияХар));
						КонецПопытки;
					КонецЕсли;
				Исключение
				КонецПопытки;

				// ЗначениеОтбораВидов — произвольный тип
				Попытка
					ЗначениеОтбора = ОписаниеХарактеристики.ЗначениеОтбораВидов;
					Если ЗначениеОтбора <> Неопределено Тогда
						ЭлементХарактеристики.Вставить("ЗначениеОтбораВидов",
							БезопасноеЗначениеДляПередачи(ЗначениеОтбора));
					КонецЕсли;
				Исключение
				КонецПопытки;

				// Поля (строковые свойства) — через Вычислить с безопасным доступом
				ПоляСтроковые = Новый Структура;
				ПоляСтроковые.Вставить("ПолеВида",                                "ПолеВида");
				ПоляСтроковые.Вставить("ПолеЗначения",                            "ПолеЗначения");
				ПоляСтроковые.Вставить("ПолеИспользованияМножественныхЗначений",  "ПолеИспользованияМножественныхЗначений");
				ПоляСтроковые.Вставить("ПолеКлюча",                               "ПолеКлюча");
				ПоляСтроковые.Вставить("ПолеКлючаМножественныхЗначений",          "ПолеКлючаМножественныхЗначений");
				ПоляСтроковые.Вставить("ПолеОбъекта",                             "ПолеОбъекта");
				ПоляСтроковые.Вставить("ПолеОтбораВидов",                         "ПолеОтбораВидов");
				ПоляСтроковые.Вставить("ПолеПорядкаМножественныхЗначений",        "ПолеПорядкаМножественныхЗначений");
				ПоляСтроковые.Вставить("ПолеПутиКДанным",                         "ПолеПутиКДанным");

				Для Каждого КлючЗначение Из ПоляСтроковые Цикл
					Попытка
						ЗначениеПоля = Вычислить("ОписаниеХарактеристики." + КлючЗначение.Значение);
						Если ЗначениеПоля <> Неопределено И Не ПустаяСтрока(Строка(ЗначениеПоля)) Тогда
							ЭлементХарактеристики.Вставить(КлючЗначение.Ключ, Строка(ЗначениеПоля));
						КонецЕсли;
					Исключение
					КонецПопытки;
				КонецЦикла;

				// Пропускаем пустые элементы (все поля = Неопределено/пустые)
				Если ЭлементХарактеристики.Количество() > 0 Тогда
					МассивХарактеристик.Добавить(ЭлементХарактеристики);
				КонецЕсли;
			КонецЦикла;

			Если МассивХарактеристик.Количество() > 0 Тогда
				ИнформацияОбОбъекте.Вставить("characteristics", МассивХарактеристик);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	Возврат Новый Структура("success, data", Истина, ИнформацияОбОбъекте);

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьДетальныеМетаданныеЭлемента(ОбъектМетаданных, ПолноеИмяМД, Секции)

	ИнформацияОбЭлементе = Новый Структура;
	ИнформацияОбЭлементе.Вставить("ПолноеИмя", ПолноеИмяМД);
	ИнформацияОбЭлементе.Вставить("Имя", ОбъектМетаданных.Имя);

	Попытка
		ИнформацияОбЭлементе.Вставить("Синоним", ОбъектМетаданных.Синоним);
	Исключение
	КонецПопытки;

	// Тип (описание типов) — есть у реквизитов, измерений, ресурсов
	Попытка
		ИнформацияОбЭлементе.Вставить("Тип",
			ПолучитьСтроковоеПредставлениеОписанияТипов(ОбъектМетаданных.Тип));
	Исключение
		// У некоторых элементов (ТабличнаяЧасть) нет свойства Тип
	КонецПопытки;

	// Для табличных частей — вернуть список реквизитов
	Попытка
		РеквизитыТЧ = Новый Массив;
		Для Каждого Реквизит Из ОбъектМетаданных.Реквизиты Цикл
			СтрокаРеквизита = Новый Структура("Имя, Синоним, Тип",
				Реквизит.Имя,
				Реквизит.Синоним,
				ПолучитьСтроковоеПредставлениеОписанияТипов(Реквизит.Тип));

			МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(Реквизит);
			Если МеткиРасширений <> Неопределено Тогда
				СтрокаРеквизита.Вставить("extension_marks", МеткиРасширений);
			КонецЕсли;

			РеквизитыТЧ.Добавить(СтрокаРеквизита);
		КонецЦикла;
		ИнформацияОбЭлементе.Вставить("Реквизиты", РеквизитыТЧ);
	Исключение
		// Нет коллекции реквизитов (не ТабличнаяЧасть)
	КонецПопытки;

	// Extension marks
	МеткиРасширений = ПолучитьExtensionMarksЕслиНужно(ОбъектМетаданных);
	Если МеткиРасширений <> Неопределено Тогда
		ИнформацияОбЭлементе.Вставить("extension_marks", МеткиРасширений);
	КонецЕсли;

	// Properties — расширенные свойства элемента
	Если СекцияЗапрошена(Секции, "properties") Тогда
		Свойства = Новый Структура;
		Кандидаты = Новый Массив;
		ДобавитьКандидатыСвойствЭлементаКоллекции(Кандидаты);

		Для Каждого ИмяСвойства Из Кандидаты Цикл
			ЗначениеСвойства = Неопределено;
			Попытка
				ЗначениеСвойства = Вычислить("ОбъектМетаданных." + ИмяСвойства);
			Исключение
			КонецПопытки;
			Если ЗначениеСвойства <> Неопределено Тогда
				ЗначениеДляJSON = ПолноеИмяМетаданныхЕслиВозможно(ЗначениеСвойства);
				Если ЗначениеДляJSON = Неопределено Тогда
					ЗначениеДляJSON = БезопасноеЗначениеДляПередачи(ЗначениеСвойства);
				КонецЕсли;
				Свойства.Вставить(ИмяСвойства, ЗначениеДляJSON);
			КонецЕсли;
		КонецЦикла;

		ИнформацияОбЭлементе.Вставить("properties", Свойства);
	КонецЕсли;

	Возврат Новый Структура("success, data", Истина, ИнформацияОбЭлементе);

КонецФункции

&НаСервереБезКонтекста
Функция НайтиЭлементВКоллекцииОбъекта(ОбъектМД, ОстатокПути)
	// ОстатокПути = "Реквизит.ИНН" или "ТабличнаяЧасть.Товары.Реквизит.Номенклатура"
	// Сегменты в единственном числе — формат ПолноеИмя() платформы
	Части = РазделитьСтрокуПоРазделителю(ОстатокПути, ".");

	Если Части.Количество() < 2 Тогда
		Возврат Неопределено;
	КонецЕсли;

	// Маппинг: единственное число из ПолноеИмя() → имя свойства объекта метаданных
	ИмяСегмента = Части[0];
	Если ИмяСегмента = "Реквизит" Тогда
		ИмяКоллекции = "Реквизиты";
	ИначеЕсли ИмяСегмента = "СтандартныйРеквизит" Тогда
		ИмяКоллекции = "СтандартныеРеквизиты";
	ИначеЕсли ИмяСегмента = "Измерение" Тогда
		ИмяКоллекции = "Измерения";
	ИначеЕсли ИмяСегмента = "Ресурс" Тогда
		ИмяКоллекции = "Ресурсы";
	ИначеЕсли ИмяСегмента = "РеквизитАдресации" Тогда
		ИмяКоллекции = "РеквизитыАдресации";
	ИначеЕсли ИмяСегмента = "ТабличнаяЧасть" Тогда
		ИмяКоллекции = "ТабличныеЧасти";
	Иначе
		ИмяКоллекции = ИмяСегмента;
	КонецЕсли;

	ИмяЭлемента = Части[1];

	// Получаем коллекцию
	Коллекция = Неопределено;
	Попытка
		Коллекция = Вычислить("ОбъектМД." + ИмяКоллекции);
	Исключение
		Возврат Неопределено;
	КонецПопытки;

	// Ищем элемент по имени
	Для Каждого Элемент Из Коллекция Цикл
		Если Элемент.Имя = ИмяЭлемента Тогда
			// Для вложенного пути (ТабличнаяЧасть.Товары.Реквизит.Номенклатура)
			Если Части.Количество() > 2 Тогда
				ВложенныйОстаток = "";
				Для Инд = 2 По Части.Количество() - 1 Цикл
					ВложенныйОстаток = ВложенныйОстаток
						+ ?(ВложенныйОстаток = "", "", ".") + Части[Инд];
				КонецЦикла;
				Возврат НайтиЭлементВКоллекцииОбъекта(Элемент, ВложенныйОстаток);
			КонецЕсли;
			Возврат Элемент;
		КонецЕсли;
	КонецЦикла;

	Возврат Неопределено;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ЖУРНАЛ РЕГИСТРАЦИИ
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ПолучитьЖурналРегистрации(ДатаНачала, ДатаОкончания, Уровни, СобытияЖурнала, Лимит,
	ДанныеОбъекта = Неопределено, ТипыМетаданных = Неопределено, Пользователи = Неопределено,
	Сеансы = Неопределено, Приложения = Неопределено, Компьютер = Неопределено,
	ПодстрокаКомментария = Неопределено, СтатусТранзакцииСтрока = Неопределено, СмещениеВСекунде = 0)
	
	Попытка
		// Формируем отбор
		Отбор = Новый Структура;
		
		Если ЗначениеЗаполнено(ДатаНачала) Тогда
			Отбор.Вставить("ДатаНачала", ДатаНачала);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ДатаОкончания) Тогда
			Отбор.Вставить("ДатаОкончания", ДатаОкончания);
		КонецЕсли;
		
		// Уровни важности
		Если Уровни <> Неопределено И ЭтоКоллекция(Уровни) Тогда
			МассивУровней = Новый Массив;
			Для Каждого УровеньСтрока Из Уровни Цикл
				Если ВРег(УровеньСтрока) = "INFORMATION" Или ВРег(УровеньСтрока) = "ИНФОРМАЦИЯ" Тогда
					МассивУровней.Добавить(УровеньЖурналаРегистрации.Информация);
				ИначеЕсли ВРег(УровеньСтрока) = "WARNING" Или ВРег(УровеньСтрока) = "ПРЕДУПРЕЖДЕНИЕ" Тогда
					МассивУровней.Добавить(УровеньЖурналаРегистрации.Предупреждение);
				ИначеЕсли ВРег(УровеньСтрока) = "ERROR" Или ВРег(УровеньСтрока) = "ОШИБКА" Тогда
					МассивУровней.Добавить(УровеньЖурналаРегистрации.Ошибка);
				ИначеЕсли ВРег(УровеньСтрока) = "NOTE" Или ВРег(УровеньСтрока) = "ПРИМЕЧАНИЕ" Тогда
					МассивУровней.Добавить(УровеньЖурналаРегистрации.Примечание);
				КонецЕсли;
			КонецЦикла;
			Если МассивУровней.Количество() > 0 Тогда
				Отбор.Вставить("Уровень", МассивУровней);
			КонецЕсли;
		КонецЕсли;
		
		// События
		Если СобытияЖурнала <> Неопределено И ЭтоКоллекция(СобытияЖурнала) Тогда
			Отбор.Вставить("Событие", СобытияЖурнала);
		КонецЕсли;
		
		// Фильтр по объекту данных (навигационная ссылка)
		Если ЗначениеЗаполнено(ДанныеОбъекта) Тогда
			СсылкаОбъекта = ПолучитьСсылкуИзСтроки(ДанныеОбъекта);
			Если СсылкаОбъекта = Неопределено Тогда
				// Возвращаем явную ошибку при невозможности применить фильтр по объекту
				Возврат Новый Структура("success,error", Ложь,
					"Не удалось применить фильтр по объекту: некорректная ссылка / Failed to apply object filter: invalid reference");
			КонецЕсли;
			Отбор.Вставить("Данные", СсылкаОбъекта);
		КонецЕсли;
		
		// Фильтр по типу метаданных
		Если ТипыМетаданных <> Неопределено И ЭтоКоллекция(ТипыМетаданных) Тогда
			МассивМетаданных = Новый Массив;
			Для Каждого ИмяМетаданных Из ТипыМетаданных Цикл
				ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ИмяМетаданных);
				Если ОбъектМетаданных <> Неопределено Тогда
					МассивМетаданных.Добавить(ОбъектМетаданных);
				КонецЕсли;
			КонецЦикла;
			Если МассивМетаданных.Количество() > 0 Тогда
				Отбор.Вставить("Метаданные", МассивМетаданных);
			КонецЕсли;
		КонецЕсли;
		
		// Фильтр по пользователю
		Если Пользователи <> Неопределено И ЭтоКоллекция(Пользователи) Тогда
			МассивПользователей = Новый Массив;
			Для Каждого ИмяПользователя Из Пользователи Цикл
				ЗначениеПользователя = СокрЛП(ИмяПользователя);
				ПользовательИБ = Неопределено;
				// Проверка формата UUID (два шага: длина, затем дефисы)
				ЭтоУИД = Ложь;
				Если СтрДлина(ЗначениеПользователя) = 36 Тогда
					ЭтоУИД = (Сред(ЗначениеПользователя, 9, 1) = "-")
						И (Сред(ЗначениеПользователя, 14, 1) = "-")
						И (Сред(ЗначениеПользователя, 19, 1) = "-")
						И (Сред(ЗначениеПользователя, 24, 1) = "-");
				КонецЕсли;
				Если ЭтоУИД Тогда
					Попытка
						ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоУникальномуИдентификатору(
							Новый УникальныйИдентификатор(ЗначениеПользователя));
					Исключение
					КонецПопытки;
				КонецЕсли;
				Если ПользовательИБ = Неопределено Тогда
					ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоИмени(ЗначениеПользователя);
				КонецЕсли;
				Если ПользовательИБ <> Неопределено Тогда
					МассивПользователей.Добавить(ПользовательИБ);
				Иначе
					// Удалённый пользователь или "" (пользователь по умолчанию) — передаём строкой
					МассивПользователей.Добавить(ЗначениеПользователя);
				КонецЕсли;
			КонецЦикла;
			Если МассивПользователей.Количество() > 0 Тогда
				Отбор.Вставить("Пользователь", МассивПользователей);
			КонецЕсли;
		КонецЕсли;
		
		// Фильтр по сеансу
		Если Сеансы <> Неопределено И ЭтоКоллекция(Сеансы) Тогда
			Отбор.Вставить("Сеанс", Сеансы);
		КонецЕсли;
		
		// Фильтр по приложению
		Если Приложения <> Неопределено И ЭтоКоллекция(Приложения) Тогда
			Отбор.Вставить("ИмяПриложения", Приложения);
		КонецЕсли;
		
		// Фильтр по компьютеру
		Если ЗначениеЗаполнено(Компьютер) Тогда
			Отбор.Вставить("Компьютер", Компьютер);
		КонецЕсли;
		
		// Статус транзакции - нативный фильтр (8.1+), через Вычислить() для совместимости с 8.2.13
		Если ЗначениеЗаполнено(СтатусТранзакцииСтрока) Тогда
			МассивСтатусов = Новый Массив;
			СтатусВерхний = ВРег(СтатусТранзакцииСтрока);
			ИмяЗначения = "";
			Если СтатусВерхний = "COMMITTED" Тогда
				ИмяЗначения = "СтатусТранзакцииЗаписиЖурналаРегистрации.Зафиксирована";
			ИначеЕсли СтатусВерхний = "ROLLEDBACK" Тогда
				ИмяЗначения = "СтатусТранзакцииЗаписиЖурналаРегистрации.Отменена";
			ИначеЕсли СтатусВерхний = "NOTAPPLICABLE" Тогда
				ИмяЗначения = "СтатусТранзакцииЗаписиЖурналаРегистрации.НетТранзакции";
			ИначеЕсли СтатусВерхний = "UNFINISHED" Тогда
				ИмяЗначения = "СтатусТранзакцииЗаписиЖурналаРегистрации.НеЗавершена";
			Иначе
				ИмяЗначения = "";
			КонецЕсли;
			Если ЗначениеЗаполнено(ИмяЗначения) Тогда
				Попытка
					МассивСтатусов.Добавить(Вычислить(ИмяЗначения));
					Отбор.Вставить("СтатусТранзакции", МассивСтатусов);
				Исключение
					Возврат Новый Структура("success,error", Ложь,
						"Фильтр по статусу транзакции недоступен на данной платформе: " + ОписаниеОшибки());
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
		
		// Фильтр по комментарию (нативный, поддерживает поиск по подстроке)
		Если ЗначениеЗаполнено(ПодстрокаКомментария) Тогда
			Отбор.Вставить("Комментарий", ПодстрокаКомментария);
		КонецЕсли;

		// Вычисляем лимит выгрузки с учётом смещения
		// Формула: Лимит + СмещениеВСекунде + 1 (для определения has_more)
		ЛимитВыгрузки = Лимит + СмещениеВСекунде + 1;

		// Выгружаем журнал
		ТаблицаЖурнала = Новый ТаблицаЗначений;
		ВыгрузитьЖурналРегистрации(ТаблицаЖурнала, Отбор, , , ЛимитВыгрузки);

		// КУРСОРНАЯ ПАГИНАЦИЯ: Явная сортировка с tie-breakers для стабильного порядка
		// Гарантирует детерминированный порядок записей с одинаковой секундой
		ТаблицаЖурнала.Сортировать("Дата, Сеанс, Событие, Пользователь, Метаданные, ПредставлениеДанных");

		// Вычисляем ключ секунды для start_date (для корректного сравнения)
		// В 1С дата может содержать доли секунды — сравнение = может не работать
		КлючСекундыНачала = "";
		Если ЗначениеЗаполнено(ДатаНачала) Тогда
			КлючСекундыНачала = Формат(ДатаНачала, "ДФ=yyyy-MM-ddTHH:mm:ss");
		КонецЕсли;

		// Преобразуем в массив для JSON с логикой курсорной пагинации
		Результат = Новый Массив;
		Счетчик = 0;
		ПропущеноВСекунде = 0;
		КлючПоследнейСекунды = "";
		КоличествоВПоследнейСекунде = 0;

		Для Каждого СтрокаТаблицы Из ТаблицаЖурнала Цикл
			// Ключ секунды текущей строки
			КлючСекундыСтроки = Формат(СтрокаТаблицы.Дата, "ДФ=yyyy-MM-ddTHH:mm:ss");

			// Пропускаем записи с начальной секундой если задан offset
			Если КлючСекундыНачала <> ""
			   И КлючСекундыСтроки = КлючСекундыНачала
			   И ПропущеноВСекунде < СмещениеВСекунде Тогда
				ПропущеноВСекунде = ПропущеноВСекунде + 1;
				Продолжить;
			КонецЕсли;

			Если Счетчик >= Лимит Тогда
				Прервать;
			КонецЕсли;

			ЗаписьЖурнала = Новый Структура;
			ЗаписьЖурнала.Вставить("date", КлючСекундыСтроки);
			ЗаписьЖурнала.Вставить("level", ПолучитьИмяУровня(СтрокаТаблицы.Уровень));
			ЗаписьЖурнала.Вставить("event", СтрокаТаблицы.Событие);
			ЗаписьЖурнала.Вставить("comment", СтрокаТаблицы.Комментарий);
			ЗаписьЖурнала.Вставить("user", Строка(СтрокаТаблицы.Пользователь));
			ЗаписьЖурнала.Вставить("metadata", Строка(СтрокаТаблицы.Метаданные));
			ЗаписьЖурнала.Вставить("data_presentation", СтрокаТаблицы.ПредставлениеДанных);
			ЗаписьЖурнала.Вставить("session", СтрокаТаблицы.Сеанс);
			ЗаписьЖурнала.Вставить("application", СтрокаТаблицы.ИмяПриложения);
			ЗаписьЖурнала.Вставить("computer", СтрокаТаблицы.Компьютер);
			ЗаписьЖурнала.Вставить("transaction_status", ПолучитьИмяСтатусаТранзакции(СтрокаТаблицы.СтатусТранзакции));
			Результат.Добавить(ЗаписьЖурнала);

			// Отслеживаем для курсора (накопительный счётчик) — по ключу секунды
			Если КлючПоследнейСекунды = КлючСекундыСтроки Тогда
				КоличествоВПоследнейСекунде = КоличествоВПоследнейСекунде + 1;
			Иначе
				КлючПоследнейСекунды = КлючСекундыСтроки;
				КоличествоВПоследнейСекунде = 1;
			КонецЕсли;

			Счетчик = Счетчик + 1;
		КонецЦикла;

		// Формируем ответ
		ОтветСтруктура = Новый Структура("success,data,count", Истина, Результат, Результат.Количество());

		// Курсорная пагинация - только если указаны даты
		ЕстьФильтрПоДате = ЗначениеЗаполнено(ДатаНачала) Или ЗначениеЗаполнено(ДатаОкончания);

		Если ЕстьФильтрПоДате И КлючПоследнейСекунды <> "" Тогда
			ОтветСтруктура.Вставить("last_date", КлючПоследнейСекунды);

			// КРИТИЧНО: возвращаем НАКОПИТЕЛЬНЫЙ offset для следующей страницы
			// Сравниваем по КЛЮЧУ СЕКУНДЫ, а не по Date-объектам
			Если КлючПоследнейСекунды = КлючСекундыНачала Тогда
				// Та же секунда что и на входе — накапливаем
				СледующийOffset = СмещениеВСекунде + КоличествоВПоследнейСекунде;
			Иначе
				// Новая секунда — offset только от записей с этой секундой
				СледующийOffset = КоличествоВПоследнейСекунде;
			КонецЕсли;
			ОтветСтруктура.Вставить("next_same_second_offset", СледующийOffset);

			// has_more: осталось больше записей чем limit после пропуска?
			ОсталосьЗаписей = ТаблицаЖурнала.Количество() - ПропущеноВСекунде;
			ЕстьЕще = ОсталосьЗаписей > Лимит;
			ОтветСтруктура.Вставить("has_more", ЕстьЕще);
		КонецЕсли;

		Возврат ОтветСтруктура;
		
	Исключение
		Возврат Новый Структура("success,error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСсылкуИзСтроки(СтрокаСсылки)
	
	Если Не ЗначениеЗаполнено(СтрокаСсылки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		// Пробуем как навигационную ссылку (e1cib/data/...)
		Если Найти(НРег(СтрокаСсылки), "e1cib") > 0 Тогда
			Возврат НайтиОбъектВручную(СтрокаСсылки);
		КонецЕсли;
		
		// Пробуем как UUID (8-4-4-4-12 формат)
		Если СтрДлина(СтрокаСсылки) = 36 И Найти(СтрокаСсылки, "-") > 0 Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Возврат Неопределено;
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИмяСтатусаТранзакции(Статус)
	Если Не ЗначениеЗаполнено(Статус) Тогда
		Возврат "";
	КонецЕсли;
	// XMLСтрока() возвращает английский идентификатор напрямую — используем его
	Попытка
		СтатусСтрока = XMLСтрока(Статус);
		Если СтатусСтрока = "Committed"
		  Или СтатусСтрока = "RolledBack"
		  Или СтатусСтрока = "NotApplicable"
		  Или СтатусСтрока = "Unfinished" Тогда
			Возврат СтатусСтрока;
		КонецЕсли;
	Исключение
	КонецПопытки;
	// Fallback: Строка() возвращает русский синоним, ВРег() для надёжности
	СтатусСтрокаВерх = ВРег(Строка(Статус));
	Если Найти(СтатусСтрокаВерх, "ЗАФИКСИРОВАН") > 0 Тогда
		Возврат "Committed";
	ИначеЕсли Найти(СтатусСтрокаВерх, "ОТМЕНЕНА") > 0 Тогда
		Возврат "RolledBack";
	ИначеЕсли Найти(СтатусСтрокаВерх, "НЕТТРАНЗАКЦИИ") > 0
	       Или Найти(СтатусСтрокаВерх, "НЕТ ТРАНЗАКЦИИ") > 0 Тогда
		Возврат "NotApplicable";
	ИначеЕсли Найти(СтатусСтрокаВерх, "НЕЗАВЕРШЕНА") > 0
	       Или Найти(СтатусСтрокаВерх, "НЕ ЗАВЕРШЕНА") > 0 Тогда
		Возврат "Unfinished";
	КонецЕсли;
	Возврат "";
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИмяУровня(Уровень)
	
	Если Уровень = УровеньЖурналаРегистрации.Информация Тогда
		Возврат "Information";
	ИначеЕсли Уровень = УровеньЖурналаРегистрации.Предупреждение Тогда
		Возврат "Warning";
	ИначеЕсли Уровень = УровеньЖурналаРегистрации.Ошибка Тогда
		Возврат "Error";
	ИначеЕсли Уровень = УровеньЖурналаРегистрации.Примечание Тогда
		Возврат "Note";
	Иначе
		Возврат Строка(Уровень);
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПрочитатьДатуИзISO(СтрокаДаты)
	
	Если Не ЗначениеЗаполнено(СтрокаДаты) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		// Формат ISO 8601: YYYY-MM-DDTHH:MM:SS
		// Убираем возможный суффикс Z
		СтрокаДаты = СтрЗаменить(СтрокаДаты, "Z", "");
		
		Год = Число(Лев(СтрокаДаты, 4));
		Месяц = Число(Сред(СтрокаДаты, 6, 2));
		День = Число(Сред(СтрокаДаты, 9, 2));
		
		Час = 0;
		Минута = 0;
		Секунда = 0;
		
		Если СтрДлина(СтрокаДаты) >= 19 Тогда
			Час = Число(Сред(СтрокаДаты, 12, 2));
			Минута = Число(Сред(СтрокаДаты, 15, 2));
			Секунда = Число(Сред(СтрокаДаты, 18, 2));
		КонецЕсли;
		
		Возврат Дата(Год, Месяц, День, Час, Минута, Секунда);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// СОХРАНЕНИЕ И ЗАГРУЗКА НАСТРОЕК
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервере
Процедура ЗагрузитьНастройки()
	
	// Загрузка адреса сервера из хранилища настроек
	Попытка
		СохраненныйАдрес = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "АдресСервера");
		Если СохраненныйАдрес <> Неопределено И Не ПустаяСтрока(СохраненныйАдрес) Тогда
			АдресСервера = СохраненныйАдрес;
		КонецЕсли;
	Исключение
		// Настройки не найдены или ошибка загрузки
	КонецПопытки;
	
	// Загрузка идентификатора канала из хранилища настроек
	Попытка
		СохранённыйКанал = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ИдентификаторКанала");
		Если СохранённыйКанал <> Неопределено Тогда
			ИдентификаторКанала = СохранённыйКанал;
		КонецЕсли;
	Исключение
		// Настройки не найдены или ошибка загрузки
	КонецПопытки;

	// Загрузка формата ответов встроенного сервера (по умолчанию TOON = 1)
	Попытка
		СохранённыйФормат = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ФорматОтветаВстроенный");
		Если СохранённыйФормат <> Неопределено Тогда
			ФорматОтветаВстроенный = СохранённыйФормат;
		Иначе
			ФорматОтветаВстроенный = 1; // TOON по умолчанию
		КонецЕсли;
	Исключение
		ФорматОтветаВстроенный = 1; // TOON по умолчанию
	КонецПопытки;

	// Загрузка флага точной анонимизации
	ИспользоватьТочнуюАнонимизацию = Истина; // дефолт до попытки загрузки
	Попытка
		СохранённыйФлаг = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ИспользоватьТочнуюАнонимизацию");
		Если СохранённыйФлаг <> Неопределено Тогда
			ИспользоватьТочнуюАнонимизацию = СохранённыйФлаг;
		КонецЕсли;
	Исключение
		// Ошибка чтения настроек — остаётся дефолт Истина
	КонецПопытки;

	// Загрузка порта встроенного сервера (по умолчанию 6003)
	Попытка
		СохраненныйПорт = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ПортВстроенногоСервера");
		Если ТипЗнч(СохраненныйПорт) = Тип("Число") И СохраненныйПорт > 0 Тогда
			ПортВстроенногоСервера = СохраненныйПорт;
		КонецЕсли;
	Исключение
	КонецПопытки;

	// Загрузка режима аутентификации при перезапуске (по умолчанию 0 = Windows)
	Попытка
		СохраненныйРежимАутентификации = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "РежимАутентификацииПерезапуска");
		Если ТипЗнч(СохраненныйРежимАутентификации) = Тип("Число") Тогда
			РежимАутентификацииПерезапуска = СохраненныйРежимАутентификации;
		КонецЕсли;
	Исключение
	КонецПопытки;

	// Загрузка пользовательского ключа шифрования для перезапуска (отображение; шифрование читает из хранилища напрямую)
	Попытка
		ЗначениеПользовательскогоКлюча = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ПользовательскийКлючШифрованияПерезапуска");
		Если ТипЗнч(ЗначениеПользовательскогоКлюча) = Тип("Строка") Тогда
			ПользовательскийКлючШифрования = Лев(СокрЛП(ЗначениеПользовательскогоКлюча), 20);
		Иначе
			НовыйПользовательскийКлюч = СгенерироватьПользовательскийКлюч();
			Попытка
				ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ПользовательскийКлючШифрованияПерезапуска", НовыйПользовательскийКлюч);
				ПользовательскийКлючШифрования = НовыйПользовательскийКлюч;
			Исключение
			КонецПопытки;
		КонецЕсли;
	Исключение
	КонецПопытки;

	// Загрузка режима подключения (по умолчанию — встроенный сервер = 1)
	Попытка
		СохранённыйРежим = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "РежимПодключения");
		Если СохранённыйРежим <> Неопределено Тогда
			РежимПодключения = СохранённыйРежим;
		Иначе
			РежимПодключения = 1;
		КонецЕсли;
	Исключение
		РежимПодключения = 1;
	КонецПопытки;

	// Загрузка настроек инструментов (по умолчанию все включены)
	ИнструментВключен_ExecuteQuery     = Истина;
	ИнструментВключен_ExecuteCode      = Истина;
	ИнструментВключен_GetMetadata      = Истина;
	ИнструментВключен_GetEventLog      = Истина;
	ИнструментВключен_GetObjectByLink  = Истина;
	ИнструментВключен_GetLinkOfObject  = Истина;
	ИнструментВключен_FindReferences   = Истина;
	ИнструментВключен_GetAccessRights  = Истина;
	ИнструментВключен_GetBslSyntaxHelp   = Истина;
	ИнструментВключен_SubmitDeanon       = Истина;
	ИнструментВключен_GetScreenshot  = Истина;
	ИнструментВключен_Restart1cSession = Истина;
	ИнструментВключен_Close1cSession  = Истина;
	Попытка
		НастройкиИнструментов = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "НастройкиИнструментов");
		Если ТипЗнч(НастройкиИнструментов) = Тип("Структура") Тогда
			Знач_ExecuteQuery     = Неопределено; НастройкиИнструментов.Свойство("ExecuteQuery",     Знач_ExecuteQuery);
			Знач_ExecuteCode      = Неопределено; НастройкиИнструментов.Свойство("ExecuteCode",      Знач_ExecuteCode);
			Знач_GetMetadata      = Неопределено; НастройкиИнструментов.Свойство("GetMetadata",      Знач_GetMetadata);
			Знач_GetEventLog      = Неопределено; НастройкиИнструментов.Свойство("GetEventLog",      Знач_GetEventLog);
			Знач_GetObjectByLink  = Неопределено; НастройкиИнструментов.Свойство("GetObjectByLink",  Знач_GetObjectByLink);
			Знач_GetLinkOfObject  = Неопределено; НастройкиИнструментов.Свойство("GetLinkOfObject",  Знач_GetLinkOfObject);
			Знач_FindReferences   = Неопределено; НастройкиИнструментов.Свойство("FindReferences",   Знач_FindReferences);
			Знач_GetAccessRights  = Неопределено; НастройкиИнструментов.Свойство("GetAccessRights",  Знач_GetAccessRights);
			Знач_GetBslSyntaxHelp      = Неопределено; НастройкиИнструментов.Свойство("GetBslSyntaxHelp",      Знач_GetBslSyntaxHelp);
			Знач_SubmitDeanon          = Неопределено; НастройкиИнструментов.Свойство("SubmitDeanon",          Знач_SubmitDeanon);
			Знач_GetScreenshot     = Неопределено; НастройкиИнструментов.Свойство("GetScreenshot",     Знач_GetScreenshot);
			Знач_Restart1cSession   = Неопределено; НастройкиИнструментов.Свойство("Restart1cSession",   Знач_Restart1cSession);
			Знач_Close1cSession     = Неопределено; НастройкиИнструментов.Свойство("Close1cSession",     Знач_Close1cSession);
			Если ТипЗнч(Знач_ExecuteQuery)     = Тип("Булево") Тогда ИнструментВключен_ExecuteQuery     = Знач_ExecuteQuery     КонецЕсли;
			Если ТипЗнч(Знач_ExecuteCode)      = Тип("Булево") Тогда ИнструментВключен_ExecuteCode      = Знач_ExecuteCode      КонецЕсли;
			Если ТипЗнч(Знач_GetMetadata)      = Тип("Булево") Тогда ИнструментВключен_GetMetadata      = Знач_GetMetadata      КонецЕсли;
			Если ТипЗнч(Знач_GetEventLog)      = Тип("Булево") Тогда ИнструментВключен_GetEventLog      = Знач_GetEventLog      КонецЕсли;
			Если ТипЗнч(Знач_GetObjectByLink)  = Тип("Булево") Тогда ИнструментВключен_GetObjectByLink  = Знач_GetObjectByLink  КонецЕсли;
			Если ТипЗнч(Знач_GetLinkOfObject)  = Тип("Булево") Тогда ИнструментВключен_GetLinkOfObject  = Знач_GetLinkOfObject  КонецЕсли;
			Если ТипЗнч(Знач_FindReferences)   = Тип("Булево") Тогда ИнструментВключен_FindReferences   = Знач_FindReferences   КонецЕсли;
			Если ТипЗнч(Знач_GetAccessRights)  = Тип("Булево") Тогда ИнструментВключен_GetAccessRights  = Знач_GetAccessRights  КонецЕсли;
			Если ТипЗнч(Знач_GetBslSyntaxHelp)     = Тип("Булево") Тогда ИнструментВключен_GetBslSyntaxHelp     = Знач_GetBslSyntaxHelp     КонецЕсли;
			Если ТипЗнч(Знач_SubmitDeanon)          = Тип("Булево") Тогда ИнструментВключен_SubmitDeanon          = Знач_SubmitDeanon          КонецЕсли;
			Если ТипЗнч(Знач_GetScreenshot)      = Тип("Булево") Тогда ИнструментВключен_GetScreenshot     = Знач_GetScreenshot     КонецЕсли;
		Если ТипЗнч(Знач_Restart1cSession)   = Тип("Булево") Тогда ИнструментВключен_Restart1cSession = Знач_Restart1cSession КонецЕсли;
		Если ТипЗнч(Знач_Close1cSession)     = Тип("Булево") Тогда ИнструментВключен_Close1cSession   = Знач_Close1cSession   КонецЕсли;
		КонецЕсли;
	Исключение
		// Ошибка чтения хранилища — остаются дефолты Истина
	КонецПопытки;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьРежимПодключенияНаСервере(Режим)
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "РежимПодключения", Режим);
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьНастройкиНаСервере(Адрес)
	
	// Сохранение адреса сервера в хранилище настроек
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "АдресСервера", Адрес);
	Исключение
		// Ошибка сохранения настроек
	КонецПопытки;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьИдентификаторКаналаНаСервере(Канал)
	
	// Сохранение идентификатора канала в хранилище настроек
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ИдентификаторКанала", Канал);
	Исключение
		// Ошибка сохранения настроек
	КонецПопытки;
	
КонецПроцедуры

&НаКлиенте
Процедура ИдентификаторКаналаПриИзменении(Элемент)

	// Сохраняем при изменении
	СохранитьИдентификаторКаналаНаСервере(ИдентификаторКанала);

	// Сброс кэша маппингов при смене канала
	КэшМаппинговОтвет = Неопределено;
	ТаблицаМаппингов.Очистить();
	Элементы.НадписьСтатистикаМаппингов.Заголовок = " ";

КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьФорматОтветаВстроенныйНаСервере(Формат)

	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ФорматОтветаВстроенный", Формат);
	Исключение
		// Ошибка сохранения настроек
	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ФорматОтветаВстроенныйПриИзменении(Элемент)

	СохранитьФорматОтветаВстроенныйНаСервере(ФорматОтветаВстроенный);

КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьТочнуюАнонимизациюПриИзменении(Элемент)

	ИспользоватьТочнуюАнонимизациюСохранитьНаСервере(ИспользоватьТочнуюАнонимизацию);
	КэшСхемИнструментов = Неопределено;

	Если ВстроенныйСерверЗапущен Тогда
		Если ИспользоватьТочнуюАнонимизацию Тогда
			ПостроитьИЗаполнитьСловарьАнонимизации();
		Иначе
			АнонимизаторОчистить();
			ОбновитьТаблицуМаппинговВстроенный();
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИспользоватьТочнуюАнонимизациюСохранитьНаСервере(Значение)

	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ИспользоватьТочнуюАнонимизацию", Значение);
	Исключение
		// Ошибка сохранения настроек
	КонецПопытки;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьНастройкиИнструментовНаСервере(Знач НастройкиИнструментов)

	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "НастройкиИнструментов", НастройкиИнструментов);
	Исключение
		// Best-effort — по образцу всех остальных сохранений.
		// При ошибке записи состояние не сохранится между сеансами,
		// но в текущем сеансе флаги и кэш уже обновлены корректно.
	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ИнструментПриИзменении(Элемент)

	КэшСхемИнструментов = Неопределено;
	НастройкиИнструментов = Новый Структура(
		"ExecuteQuery, ExecuteCode, GetMetadata, GetEventLog, GetObjectByLink, GetLinkOfObject, FindReferences, GetAccessRights, GetBslSyntaxHelp, SubmitDeanon, GetScreenshot, Restart1cSession, Close1cSession",
		ИнструментВключен_ExecuteQuery,     ИнструментВключен_ExecuteCode,
		ИнструментВключен_GetMetadata,      ИнструментВключен_GetEventLog,
		ИнструментВключен_GetObjectByLink,  ИнструментВключен_GetLinkOfObject,
		ИнструментВключен_FindReferences,   ИнструментВключен_GetAccessRights,
		ИнструментВключен_GetBslSyntaxHelp, ИнструментВключен_SubmitDeanon,
		ИнструментВключен_GetScreenshot, ИнструментВключен_Restart1cSession, ИнструментВключен_Close1cSession);
	СохранитьНастройкиИнструментовНаСервере(НастройкиИнструментов);

КонецПроцедуры

&НаСервере
Процедура ОбновитьСводкуПравилТочнойАнонимизацииНаСервере()

	ИсточникПравилТочнойАнонимизации = "Настройки по умолчанию";
	КоличествоПравил = ПолучитьКоличествоПравилТочнойАнонимизацииПоУмолчаниюНаСервере();

	Попытка
		НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v2");
		Если ТипЗнч(НаборПравил) <> Тип("Структура") Или НЕ НаборПравил.Свойство("Правила") Тогда
			НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v1");
		КонецЕсли;
		Если ТипЗнч(НаборПравил) = Тип("Структура") И НаборПравил.Свойство("Правила") Тогда
			КоличествоПравил = НаборПравил.Правила.Количество();
			Если КоличествоПравил = 0 Тогда
				ИсточникПравилТочнойАнонимизации = "Пустой набор";
			Иначе
				ИсточникПравилТочнойАнонимизации = "Сохраненные настройки";
			КонецЕсли;
		КонецЕсли;
	Исключение
	КонецПопытки;

	АгрессивныйРежим = Ложь;
	Попытка АгрессивныйРежим = НаборПравил.aggressive_mode; Исключение КонецПопытки;

	ИсточникПравилТочнойАнонимизации = ИсточникПравилТочнойАнонимизации + " (" + Формат(КоличествоПравил, "ЧГ=0") + " явных правил"
		+ ?(РежимПодключения <> 0 И АгрессивныйРежим, ", агрессивный режим", "") + ")";

КонецПроцедуры

&НаСервере
Функция ПолучитьКоличествоПравилТочнойАнонимизацииПоУмолчаниюНаСервере()

	Обработка = РеквизитФормыВЗначение("Объект");
	ДвоичныеДанные = Обработка.ПолучитьМакет("НастройкиТочнойАнонимизацииПоУмолчанию");
	ИмяФайла = ПолучитьИмяВременногоФайла("json");
	ДвоичныеДанные.Записать(ИмяФайла);

	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.Прочитать(ИмяФайла);
	Возврат КоличествоВхожденийПодстроки(ТекстовыйДокумент.ПолучитьТекст(), """source_path""");

КонецФункции

&НаСервереБезКонтекста
Функция КоличествоВхожденийПодстроки(ИсходныйТекст, Подстрока)

	Количество = 0;
	ПозицияПоиска = 1;
	Пока Истина Цикл
		ПозицияВхождения = Найти(Сред(ИсходныйТекст, ПозицияПоиска), Подстрока);
		Если ПозицияВхождения = 0 Тогда
			Прервать;
		КонецЕсли;
		ПозицияВхождения = ПозицияПоиска + ПозицияВхождения - 1;
		Количество = Количество + 1;
		ПозицияПоиска = ПозицияВхождения + СтрДлина(Подстрока);
	КонецЦикла;

	Возврат Количество;

КонецФункции

&НаКлиенте
Процедура СгенерироватьИдентификаторКанала(Команда)
	
	// Генерация нового UUID для идентификатора канала
	ИдентификаторКанала = Строка(Новый УникальныйИдентификатор());
	СохранитьИдентификаторКаналаНаСервере(ИдентификаторКанала);

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ПрименитьТочнуюPolicyКРезультатуExecuteQuery(Результат, ОтключитьАгрессивныйРежим = Ложь)

	Если Результат = Неопределено Или ТипЗнч(Результат) <> Тип("Структура") Тогда
		Возврат Результат;
	КонецЕсли;
	Если НЕ Результат.Свойство("schema") Тогда
		Возврат Результат;
	КонецЕсли;
	Если Результат.schema = Неопределено Или ТипЗнч(Результат.schema) <> Тип("Структура") Тогда
		Возврат Результат;
	КонецЕсли;

	НастройкиАнонимизации = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	ПравилаТочнойАнонимизации = НастройкиАнонимизации.Правила;
	АгрессивныйРежим = НастройкиАнонимизации.АгрессивныйРежим;
	Если ОтключитьАгрессивныйРежим Тогда
		АгрессивныйРежим = Ложь;
	КонецЕсли;
	КешРазыменования = Неопределено;
	Если НуженКешРазыменования(Результат.schema, ПравилаТочнойАнонимизации) Тогда
		КешРазыменования = ВычислитьКешРазыменованияДляSchema(Результат.schema, ПравилаТочнойАнонимизации);
	КонецЕсли;
	ПрименитьТочнуюPolicyКSchema(Результат.schema, ПравилаТочнойАнонимизации, КешРазыменования, АгрессивныйРежим);
	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат)

	Если Результат = Неопределено Или ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат Результат; КонецЕсли;
	Если НЕ Результат.Свойство("data") Или ТипЗнч(Результат.data) <> Тип("Структура") Тогда Возврат Результат; КонецЕсли;

	ТипОбъекта = "";
	Если НЕ Результат.data.Свойство("_type", ТипОбъекта) Тогда
		// Fail-closed: тип объекта неизвестен — нет схемы, слепая анонимизация строк
		Результат.Вставить("_blind_anonymization", Истина);
		Результат.Вставить("_anon_schema_forced",  Истина);
		Возврат Результат;
	КонецЕсли;

	ЭтоВнешняяТаблица = (НРег(Лев(ТипОбъекта, СтрДлина("внешнийисточникданных."))) = "внешнийисточникданных.");

	МетаданныеОбъекта = Неопределено;
	Попытка
		Если ЭтоВнешняяТаблица Тогда
			// Парсим "ВнешнийИсточникДанных.ИмяИсточника.Таблица.ИмяТаблицы"
			Части = РазделитьСтрокуПоРазделителю(ТипОбъекта, ".");
			Если Части.Количество() = 4 Тогда
				МетаИсточника = Метаданные.ВнешниеИсточникиДанных.Найти(Части[1]);
				Если МетаИсточника <> Неопределено Тогда
					МетаданныеОбъекта = МетаИсточника.Таблицы.Найти(Части[3]);
				КонецЕсли;
			КонецЕсли;
		Иначе
			МетаданныеОбъекта = Метаданные.НайтиПоПолномуИмени(ТипОбъекта);
		КонецЕсли;
	Исключение
	КонецПопытки;
	Если МетаданныеОбъекта = Неопределено Тогда
		// Fail-closed: метаданные недоступны — слепая анонимизация строк
		Результат.Вставить("_blind_anonymization", Истина);
		Результат.Вставить("_anon_schema_forced",  Истина);
		Возврат Результат;
	КонецЕсли;

	Схема = СформироватьСхемуОбъектаДляАнонимизации(МетаданныеОбъекта, ТипОбъекта, ЭтоВнешняяТаблица);

	НастройкиАнонимизации = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	Правила = НастройкиАнонимизации.Правила;
	АгрессивныйРежим = НастройкиАнонимизации.АгрессивныйРежим;

	// Основные колонки
	КешРазыменования = Неопределено;
	Если НуженКешРазыменования(Схема, Правила) Тогда
		КешРазыменования = ВычислитьКешРазыменованияДляSchema(Схема, Правила);
	КонецЕсли;
	ПрименитьТочнуюPolicyКSchema(Схема, Правила, КешРазыменования, АгрессивныйРежим);

	// Табличные части
	Для Каждого КВП Из Схема.tabular_sections Цикл
		СхемаТЧ = КВП.Значение;
		КешТЧ = Неопределено;
		Если НуженКешРазыменования(СхемаТЧ, Правила) Тогда
			КешТЧ = ВычислитьКешРазыменованияДляSchema(СхемаТЧ, Правила);
		КонецЕсли;
		ПрименитьТочнуюPolicyКSchema(СхемаТЧ, Правила, КешТЧ, АгрессивныйРежим);
	КонецЦикла;

	Результат.Вставить("_anon_schema_forced", Истина);
	Результат.Вставить("schema", Схема);
	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПравилаТочнойАнонимизацииИзХранилища()

	СоответствиеПравил = Новый Соответствие;
	Попытка
		НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v2");
		Если ТипЗнч(НаборПравил) <> Тип("Структура") Или НЕ НаборПравил.Свойство("Правила") Тогда
			НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v1");
		КонецЕсли;
		Если ТипЗнч(НаборПравил) = Тип("Структура") И НаборПравил.Свойство("Правила") Тогда
			Для Каждого СтрокаПравила Из НаборПравил.Правила Цикл
				ПутьИсточника = "";
				Action        = "";
				КатегорияТокена = "";
				Попытка ПутьИсточника  = СтрокаПравила.source_path;  Исключение КонецПопытки;
				Попытка Action          = СтрокаПравила.action;        Исключение КонецПопытки;
				Попытка КатегорияТокена = СтрокаПравила.token_category; Исключение КонецПопытки;
				Если КатегорияТокена = Неопределено Тогда КатегорияТокена = ""; КонецЕсли;
				Если ПустаяСтрока(ПутьИсточника) Тогда Продолжить; КонецЕсли;
				СоответствиеПравил.Вставить(ПутьИсточника,
					Новый Структура("action, token_category", Action, КатегорияТокена));
			КонецЦикла;
		КонецЕсли;
	Исключение
	КонецПопытки;

	Возврат СоответствиеПравил;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьНастройкиТочнойАнонимизацииИзХранилища()

	СоответствиеПравил = ПолучитьПравилаТочнойАнонимизацииИзХранилища();
	АгрессивныйРежим = Ложь;
	КонфирмРежим     = Ложь;
	Попытка
		НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v2");
		Если ТипЗнч(НаборПравил) = Тип("Структура") Тогда
			Попытка АгрессивныйРежим    = НаборПравил.aggressive_mode;  Исключение КонецПопытки;
			Попытка КонфирмРежим        = НаборПравил.confirmation_mode; Исключение КонецПопытки;
		КонецЕсли;
	Исключение
	КонецПопытки;
	Возврат Новый Структура("Правила, АгрессивныйРежим, ПодтверждениеАнонимизации",
		СоответствиеПравил, АгрессивныйРежим, КонфирмРежим);

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────
// HTML-генератор для окна подтверждения анонимизации
// ─────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция HTMLЭскейп(Знач Текст)
	Текст = СтрЗаменить(Текст, "&",  "&amp;");
	Текст = СтрЗаменить(Текст, "<",  "&lt;");
	Текст = СтрЗаменить(Текст, ">",  "&gt;");
	Текст = СтрЗаменить(Текст, """", "&quot;");
	Возврат Текст;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НормализоватьЗначениеДляHTML(Значение)
	Если Значение = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	ТипЗн = ТипЗнч(Значение);
	Если ТипЗн = Тип("Структура") Тогда
		Прес = "";
		Если НЕ Значение.Свойство("presentation", Прес) Тогда
			Если НЕ Значение.Свойство("Представление", Прес) Тогда
				Значение.Свойство("_presentation", Прес);
			КонецЕсли;
		КонецЕсли;
		Если ЗначениеЗаполнено(Прес) Тогда
			Возврат HTMLЭскейп(Строка(Прес));
		КонецЕсли;
		Возврат "<code>" + HTMLЭскейп(ЗаписатьJSON2(Значение)) + "</code>";
	КонецЕсли;
	Если ТипЗн = Тип("Соответствие") Тогда
		Прес = Значение["presentation"];
		Если Прес = Неопределено Тогда
			Прес = Значение["Представление"];
		КонецЕсли;
		Если Прес = Неопределено Тогда
			Прес = Значение["_presentation"];
		КонецЕсли;
		Если ЗначениеЗаполнено(Прес) Тогда
			Возврат HTMLЭскейп(Строка(Прес));
		КонецЕсли;
		Возврат "<code>" + HTMLЭскейп(ЗаписатьJSON2(Значение)) + "</code>";
	КонецЕсли;
	Если ТипЗн = Тип("Массив") Тогда
		Если Значение.Количество() = 0 Тогда
			Возврат "";
		КонецЕсли;
		Буфер = "<ul style=""margin:0;padding-left:16px"">";
		Для Каждого Эл Из Значение Цикл
			Буфер = Буфер + "<li>" + НормализоватьЗначениеДляHTML(Эл) + "</li>";
		КонецЦикла;
		Буфер = Буфер + "</ul>";
		Возврат Буфер;
	КонецЕсли;
	Возврат HTMLЭскейп(Строка(Значение));
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоОбъектБезPresentation(Значение)
	ТипЗн = ТипЗнч(Значение);
	Если ТипЗн = Тип("Структура") Тогда
		Прес = "";
		Если Значение.Свойство("presentation",  Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Если Значение.Свойство("Представление", Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Если Значение.Свойство("_presentation", Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Возврат Истина;
	ИначеЕсли ТипЗн = Тип("Соответствие") Тогда
		Прес = Значение["presentation"];
		Если Прес <> Неопределено И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Прес = Значение["Представление"];
		Если Прес <> Неопределено И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Прес = Значение["_presentation"];
		Если Прес <> Неопределено И ЗначениеЗаполнено(Прес) Тогда Возврат Ложь; КонецЕсли;
		Возврат Истина;
	КонецЕсли;
	Возврат Ложь;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьСкалярСтроку(Значение)
	Если Значение = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	ТипЗн = ТипЗнч(Значение);
	Если ТипЗн = Тип("Структура") Тогда
		Прес = "";
		Если Значение.Свойство("presentation",  Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Строка(Прес); КонецЕсли;
		Если Значение.Свойство("Представление", Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Строка(Прес); КонецЕсли;
		Если Значение.Свойство("_presentation", Прес) И ЗначениеЗаполнено(Прес) Тогда Возврат Строка(Прес); КонецЕсли;
		Возврат ЗаписатьJSON2(Значение);
	КонецЕсли;
	Если ТипЗн = Тип("Соответствие") Тогда
		Прес = Значение["presentation"];
		Если Прес = Неопределено Тогда Прес = Значение["Представление"]; КонецЕсли;
		Если Прес = Неопределено Тогда Прес = Значение["_presentation"];  КонецЕсли;
		Если ЗначениеЗаполнено(Прес) Тогда Возврат Строка(Прес); КонецЕсли;
		Возврат ЗаписатьJSON2(Значение);
	КонецЕсли;
	Если ТипЗн = Тип("Массив") Тогда
		Возврат ЗаписатьJSON2(Значение);
	КонецЕсли;
	Возврат Строка(Значение);
КонецФункции

// Разбить строку на токены (слова) + разделители (" ,:.;"{}[]")
// Возвращает массив Структура("Текст, ЭтоРазделитель")
&НаКлиентеНаСервереБезКонтекста
Функция ТокенизироватьСтроку(Текст)
	Разделители = " ,:.;""{[]}";
	Токены = Новый Массив;
	ТекущийТокен = "";
	Для Поз = 1 По СтрДлина(Текст) Цикл
		Символ = Сред(Текст, Поз, 1);
		Если Найти(Разделители, Символ) > 0 Тогда
			Если ЗначениеЗаполнено(ТекущийТокен) Тогда
				Токены.Добавить(Новый Структура("Текст, ЭтоРазделитель", ТекущийТокен, Ложь));
				ТекущийТокен = "";
			КонецЕсли;
			Токены.Добавить(Новый Структура("Текст, ЭтоРазделитель", Символ, Истина));
		Иначе
			ТекущийТокен = ТекущийТокен + Символ;
		КонецЕсли;
	КонецЦикла;
	Если ЗначениеЗаполнено(ТекущийТокен) Тогда
		Токены.Добавить(Новый Структура("Текст, ЭтоРазделитель", ТекущийТокен, Ложь));
	КонецЕсли;
	Возврат Токены;
КонецФункции

// LCS только по словам (не-разделителям).
// Возвращает отсортированный массив пар Структура("ИндОриг, ИндАнон") — полные индексы в массивах токенов.
&НаКлиентеНаСервереБезКонтекста
Функция ВычислитьLCSТокенов(ТокеныОриг, ТокеныАнон)
	СловаОриг = Новый Массив; // Структура("Текст, Инд")
	Для Нп = 0 По ТокеныОриг.Количество() - 1 Цикл
		Если НЕ ТокеныОриг[Нп].ЭтоРазделитель Тогда
			СловаОриг.Добавить(Новый Структура("Текст, Инд", ТокеныОриг[Нп].Текст, Нп));
		КонецЕсли;
	КонецЦикла;
	СловаАнон = Новый Массив;
	Для Нп = 0 По ТокеныАнон.Количество() - 1 Цикл
		Если НЕ ТокеныАнон[Нп].ЭтоРазделитель Тогда
			СловаАнон.Добавить(Новый Структура("Текст, Инд", ТокеныАнон[Нп].Текст, Нп));
		КонецЕсли;
	КонецЦикла;

	Н = СловаОриг.Количество();
	М = СловаАнон.Количество();

	DP = Новый Массив((Н + 1) * (М + 1));
	Для Кз = 0 По (Н + 1) * (М + 1) - 1 Цикл
		DP[Кз] = 0;
	КонецЦикла;
	Для Р = 1 По Н Цикл
		Для Кол = 1 По М Цикл
			Если СловаОриг[Р - 1].Текст = СловаАнон[Кол - 1].Текст Тогда
				DP[Р * (М + 1) + Кол] = DP[(Р - 1) * (М + 1) + (Кол - 1)] + 1;
			Иначе
				Прев1 = DP[(Р - 1) * (М + 1) + Кол];
				Прев2 = DP[Р * (М + 1) + (Кол - 1)];
				DP[Р * (М + 1) + Кол] = ?(Прев1 > Прев2, Прев1, Прев2);
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

	Пары = Новый Массив;
	Р = Н; Кол = М;
	Пока Р > 0 И Кол > 0 Цикл
		Если СловаОриг[Р - 1].Текст = СловаАнон[Кол - 1].Текст Тогда
			Пары.Вставить(0, Новый Структура("ИндОриг, ИндАнон",
				СловаОриг[Р - 1].Инд, СловаАнон[Кол - 1].Инд));
			Р = Р - 1; Кол = Кол - 1;
		ИначеЕсли DP[(Р - 1) * (М + 1) + Кол] > DP[Р * (М + 1) + (Кол - 1)] Тогда
			Р = Р - 1;
		Иначе
			Кол = Кол - 1;
		КонецЕсли;
	КонецЦикла;
	Возврат Пары;
КонецФункции

// Рендерит пару gap-ов с подсветкой: вычисляет общий prefix/suffix по тексту токенов,
// выводит их без подсветки, различающуюся середину - в span.
// Возвращает Структура("Оригинал, Анонимизированный").
&НаКлиентеНаСервереБезКонтекста
Функция СобратьHTMLПарыГэповСПодсветкой(ТокеныО, НачО, КонО, ТокеныА, НачА, КонА)
	ДлО = ?(НачО > КонО, 0, КонО - НачО + 1);
	ДлА = ?(НачА > КонА, 0, КонА - НачА + 1);

	// Общий префикс
	ОбщПреф = 0;
	Пока ОбщПреф < ДлО И ОбщПреф < ДлА Цикл
		Если ТокеныО[НачО + ОбщПреф].Текст = ТокеныА[НачА + ОбщПреф].Текст Тогда
			ОбщПреф = ОбщПреф + 1;
		Иначе
			Прервать;
		КонецЕсли;
	КонецЦикла;

	// Общий суффикс (без перекрытия с префиксом)
	ОбщСуф = 0;
	Пока (ОбщПреф + ОбщСуф < ДлО) И (ОбщПреф + ОбщСуф < ДлА) Цикл
		Если ТокеныО[НачО + ДлО - 1 - ОбщСуф].Текст = ТокеныА[НачА + ДлА - 1 - ОбщСуф].Текст Тогда
			ОбщСуф = ОбщСуф + 1;
		Иначе
			Прервать;
		КонецЕсли;
	КонецЦикла;

	// Префикс (общий, без подсветки)
	ПрефО = ""; ПрефА = "";
	Для Нп = 0 По ОбщПреф - 1 Цикл
		ПрефО = ПрефО + HTMLЭскейп(ТокеныО[НачО + Нп].Текст);
		ПрефА = ПрефА + HTMLЭскейп(ТокеныА[НачА + Нп].Текст);
	КонецЦикла;

	// Середина (различающаяся, в span)
	СерО = "";
	Для Нп = НачО + ОбщПреф По НачО + ДлО - 1 - ОбщСуф Цикл
		СерО = СерО + HTMLЭскейп(ТокеныО[Нп].Текст);
	КонецЦикла;
	СерА = "";
	Для Нп = НачА + ОбщПреф По НачА + ДлА - 1 - ОбщСуф Цикл
		СерА = СерА + HTMLЭскейп(ТокеныА[Нп].Текст);
	КонецЦикла;

	// Суффикс (общий, без подсветки)
	СуфО = ""; СуфА = "";
	Для Нп = 0 По ОбщСуф - 1 Цикл
		СуфО = СуфО + HTMLЭскейп(ТокеныО[НачО + ДлО - ОбщСуф + Нп].Текст);
		СуфА = СуфА + HTMLЭскейп(ТокеныА[НачА + ДлА - ОбщСуф + Нп].Текст);
	КонецЦикла;

	РезО = ПрефО + ?(СерО <> "", "<span class=""changed-orig"">" + СерО + "</span>", "") + СуфО;
	РезА = ПрефА + ?(СерА <> "", "<span class=""changed-anon"">" + СерА + "</span>", "") + СуфА;
	Возврат Новый Структура("Оригинал, Анонимизированный", РезО, РезА);
КонецФункции

// Построить HTML с inline-подсветкой изменённых токенов через парный обход гэпов
// ТекстОриг, ТекстАнон - чистые строки (НЕ экранированные)
// Возвращает Структура("Оригинал, Анонимизированный, ВесьЯчейка")
&НаКлиентеНаСервереБезКонтекста
Функция ПодсветитьДиффСтрок(ТекстОриг, ТекстАнон)
	Рез = Новый Структура("Оригинал, Анонимизированный, ВесьЯчейка", "", "", Ложь);
	Если ТекстОриг = ТекстАнон Тогда
		Рез.Оригинал         = HTMLЭскейп(ТекстОриг);
		Рез.Анонимизированный = HTMLЭскейп(ТекстАнон);
		Возврат Рез;
	КонецЕсли;

	ТокеныОриг = ТокенизироватьСтроку(ТекстОриг);
	ТокеныАнон = ТокенизироватьСтроку(ТекстАнон);
	Пары = ВычислитьLCSТокенов(ТокеныОриг, ТокеныАнон);
	Если Пары.Количество() = 0 Тогда
		Рез.Оригинал         = HTMLЭскейп(ТекстОриг);
		Рез.Анонимизированный = HTMLЭскейп(ТекстАнон);
		Рез.ВесьЯчейка        = Истина;
		Возврат Рез;
	КонецЕсли;

	БуфОриг = ""; БуфАнон = "";
	ПредО = -1;   ПредА = -1;

	Для Каждого Пара Из Пары Цикл
		ГэпОHTML = ""; ГэпОТекст = "";
		Для Нп = ПредО + 1 По Пара.ИндОриг - 1 Цикл
			ГэпОHTML  = ГэпОHTML  + HTMLЭскейп(ТокеныОриг[Нп].Текст);
			ГэпОТекст = ГэпОТекст + ТокеныОриг[Нп].Текст;
		КонецЦикла;
		ГэпАHTML = ""; ГэпАТекст = "";
		Для Нп = ПредА + 1 По Пара.ИндАнон - 1 Цикл
			ГэпАHTML  = ГэпАHTML  + HTMLЭскейп(ТокеныАнон[Нп].Текст);
			ГэпАТекст = ГэпАТекст + ТокеныАнон[Нп].Текст;
		КонецЦикла;
		Если ГэпОТекст <> ГэпАТекст Тогда
			ПараГэпов = СобратьHTMLПарыГэповСПодсветкой(ТокеныОриг, ПредО + 1, Пара.ИндОриг - 1, ТокеныАнон, ПредА + 1, Пара.ИндАнон - 1);
			БуфОриг = БуфОриг + ПараГэпов.Оригинал;
			БуфАнон = БуфАнон + ПараГэпов.Анонимизированный;
		Иначе
			БуфОриг = БуфОриг + ГэпОHTML;
			БуфАнон = БуфАнон + ГэпАHTML;
		КонецЕсли;
		БуфОриг = БуфОриг + HTMLЭскейп(ТокеныОриг[Пара.ИндОриг].Текст);
		БуфАнон = БуфАнон + HTMLЭскейп(ТокеныАнон[Пара.ИндАнон].Текст);
		ПредО = Пара.ИндОриг; ПредА = Пара.ИндАнон;
	КонецЦикла;

	// Хвостовые гэпы
	ГэпОТекст = ""; ГэпОHTML = ""; ГэпАТекст = ""; ГэпАHTML = "";
	Для Нп = ПредО + 1 По ТокеныОриг.Количество() - 1 Цикл
		ГэпОТекст = ГэпОТекст + ТокеныОриг[Нп].Текст;
		ГэпОHTML  = ГэпОHTML  + HTMLЭскейп(ТокеныОриг[Нп].Текст);
	КонецЦикла;
	Для Нп = ПредА + 1 По ТокеныАнон.Количество() - 1 Цикл
		ГэпАТекст = ГэпАТекст + ТокеныАнон[Нп].Текст;
		ГэпАHTML  = ГэпАHTML  + HTMLЭскейп(ТокеныАнон[Нп].Текст);
	КонецЦикла;
	Если ГэпОТекст <> ГэпАТекст Тогда
		ПараГэпов = СобратьHTMLПарыГэповСПодсветкой(
			ТокеныОриг, ПредО + 1, ТокеныОриг.Количество() - 1,
			ТокеныАнон, ПредА + 1, ТокеныАнон.Количество() - 1);
		БуфОриг = БуфОриг + ПараГэпов.Оригинал;
		БуфАнон = БуфАнон + ПараГэпов.Анонимизированный;
	Иначе
		БуфОриг = БуфОриг + ГэпОHTML;
		БуфАнон = БуфАнон + ГэпАHTML;
	КонецЕсли;

	Рез.Оригинал         = БуфОриг;
	Рез.Анонимизированный = БуфАнон;
	Возврат Рез;
КонецФункции

// Нормализовать пару значений ячейки с inline-подсветкой изменений
// Возвращает Структура("Оригинал, Анонимизированный")
&НаКлиентеНаСервереБезКонтекста
Функция НормализоватьПаруЗначенийСПодсветкой(ЗначОриг, ЗначАнон)
	Рез = Новый Структура("Оригинал, Анонимизированный, ВесьЯчейка", "", "", Ложь);
	ТипОриг = ТипЗнч(ЗначОриг);
	ТипАнон = ТипЗнч(ЗначАнон);

	// Несовместимые типы (один - массив, другой нет): whole-cell highlight
	Если (ТипОриг = Тип("Массив")) <> (ТипАнон = Тип("Массив")) Тогда
		Рез.Оригинал         = НормализоватьЗначениеДляHTML(ЗначОриг);
		Рез.Анонимизированный = НормализоватьЗначениеДляHTML(ЗначАнон);
		Рез.ВесьЯчейка        = Истина;
		Возврат Рез;
	КонецЕсли;

	// Оба - массивы: рекурсивно поэлементно, агрегируем ВесьЯчейка
	Если ТипОриг = Тип("Массив") И ТипАнон = Тип("Массив") Тогда
		КолОриг = ЗначОриг.Количество();
		КолАнон = ЗначАнон.Количество();
		Макс    = ?(КолОриг > КолАнон, КолОриг, КолАнон);
		// ВсеЯчейка = Истина только если длины равны, все элементы изменились и ни один не имеет inline diff
		ВсеЯчейка = (КолОриг = КолАнон);
		БуфОриг = "<ul style=""margin:0;padding-left:16px"">";
		БуфАнон = "<ul style=""margin:0;padding-left:16px"">";
		Для Нп = 0 По Макс - 1 Цикл
			ЭлОриг    = ?(Нп < КолОриг, ЗначОриг[Нп], Неопределено);
			ЭлАнон    = ?(Нп < КолАнон, ЗначАнон[Нп], Неопределено);
			ЗнОригHTML = НормализоватьЗначениеДляHTML(ЭлОриг);
			ЗнАнонHTML = НормализоватьЗначениеДляHTML(ЭлАнон);
			Если ЗнОригHTML = ЗнАнонHTML Тогда
				// элемент не изменился - inline не нужен, но ВсеЯчейка снимаем
				ВсеЯчейка = Ложь;
				БуфОриг = БуфОриг + "<li>" + ЗнОригHTML + "</li>";
				БуфАнон = БуфАнон + "<li>" + ЗнАнонHTML + "</li>";
			Иначе
				ПараЭл = НормализоватьПаруЗначенийСПодсветкой(ЭлОриг, ЭлАнон);
				Если НЕ ПараЭл.ВесьЯчейка Тогда
					ВсеЯчейка = Ложь;
					БуфОриг = БуфОриг + "<li>"                       + ПараЭл.Оригинал         + "</li>";
					БуфАнон = БуфАнон + "<li>"                       + ПараЭл.Анонимизированный + "</li>";
				Иначе
					БуфОриг = БуфОриг + "<li class=""changed-orig"">" + ПараЭл.Оригинал         + "</li>";
					БуфАнон = БуфАнон + "<li class=""changed-anon"">" + ПараЭл.Анонимизированный + "</li>";
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		Если ВсеЯчейка Тогда
			Рез.Оригинал         = НормализоватьЗначениеДляHTML(ЗначОриг);
			Рез.Анонимизированный = НормализоватьЗначениеДляHTML(ЗначАнон);
			Рез.ВесьЯчейка        = Истина;
		Иначе
			Рез.Оригинал         = БуфОриг + "</ul>";
			Рез.Анонимизированный = БуфАнон + "</ul>";
		КонецЕсли;
		Возврат Рез;
	КонецЕсли;

	// Скаляры и объекты: diff по строковому представлению
	СтрОриг = ПолучитьСкалярСтроку(ЗначОриг);
	СтрАнон = ПолучитьСкалярСтроку(ЗначАнон);
	ВОбъект = ЭтоОбъектБезPresentation(ЗначОриг) Или ЭтоОбъектБезPresentation(ЗначАнон);
	Дифф    = ПодсветитьДиффСтрок(СтрОриг, СтрАнон);
	Если ВОбъект Тогда
		Рез.Оригинал         = "<code>" + Дифф.Оригинал         + "</code>";
		Рез.Анонимизированный = "<code>" + Дифф.Анонимизированный + "</code>";
	Иначе
		Рез.Оригинал         = Дифф.Оригинал;
		Рез.Анонимизированный = Дифф.Анонимизированный;
	КонецЕсли;
	Рез.ВесьЯчейка = Дифф.ВесьЯчейка;
	Возврат Рез;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СобратьКлючиОбъектов(МассивОбъектов)
	// Возвращает массив ключей — объединение всех ключей всех строк
	КлючиСоответствие = Новый Соответствие;
	КлючиМассив = Новый Массив;
	Для Каждого Объект Из МассивОбъектов Цикл
		Если ТипЗнч(Объект) = Тип("Структура") Тогда
			Для Каждого КЗ Из Объект Цикл
				Если КлючиСоответствие[КЗ.Ключ] = Неопределено Тогда
					КлючиСоответствие[КЗ.Ключ] = Истина;
					КлючиМассив.Добавить(КЗ.Ключ);
				КонецЕсли;
			КонецЦикла;
		ИначеЕсли ТипЗнч(Объект) = Тип("Соответствие") Тогда
			Для Каждого КЗ Из Объект Цикл
				Если КлючиСоответствие[КЗ.Ключ] = Неопределено Тогда
					КлючиСоответствие[КЗ.Ключ] = Истина;
					КлючиМассив.Добавить(КЗ.Ключ);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	Возврат КлючиМассив;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьЗначениеОбъекта(Объект, Ключ)
	Если ТипЗнч(Объект) = Тип("Структура") Тогда
		Зн = Неопределено;
		Объект.Свойство(Ключ, Зн);
		Возврат Зн;
	ИначеЕсли ТипЗнч(Объект) = Тип("Соответствие") Тогда
		Возврат Объект[Ключ];
	КонецЕсли;
	Возврат Неопределено;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьHTMLТаблицуИзМассиваОбъектов(МассивОриг, МассивАнон, Колонки)
	ЕстьИзменения = Ложь;
	HTMLОриг = "<table><tr>";
	HTMLАнон = "<table><tr>";
	Для Каждого Кол Из Колонки Цикл
		HTMLОриг = HTMLОриг + "<th>" + HTMLЭскейп(Строка(Кол)) + "</th>";
		HTMLАнон = HTMLАнон + "<th>" + HTMLЭскейп(Строка(Кол)) + "</th>";
	КонецЦикла;
	HTMLОриг = HTMLОриг + "</tr>";
	HTMLАнон = HTMLАнон + "</tr>";

	КолВо = МассивОриг.Количество();
	Если КолВо > 200 Тогда КолВо = 200; КонецЕсли;

	Для НдкО = 0 По КолВо - 1 Цикл
		ОригСтрока = МассивОриг[НдкО];
		АнонСтрока = Неопределено;
		Если НдкО < МассивАнон.Количество() Тогда
			АнонСтрока = МассивАнон[НдкО];
		КонецЕсли;
		HTMLОриг = HTMLОриг + "<tr>";
		HTMLАнон = HTMLАнон + "<tr>";
		Для Каждого Кол Из Колонки Цикл
			РВОриг = ПолучитьЗначениеОбъекта(ОригСтрока, Кол);
			РВАнон = РВОриг;
			Если АнонСтрока <> Неопределено Тогда
				РВАнон = ПолучитьЗначениеОбъекта(АнонСтрока, Кол);
			КонецЕсли;
			ЗнОриг = НормализоватьЗначениеДляHTML(РВОриг);
			ЗнАнон = НормализоватьЗначениеДляHTML(РВАнон);
			Если ЗнОриг <> ЗнАнон Тогда
				ЕстьИзменения = Истина;
				ПараHTMLДифф = НормализоватьПаруЗначенийСПодсветкой(РВОриг, РВАнон);
				Если ПараHTMLДифф.ВесьЯчейка Тогда
					HTMLОриг = HTMLОриг + "<td class=""changed-orig"">" + ПараHTMLДифф.Оригинал         + "</td>";
					HTMLАнон = HTMLАнон + "<td class=""changed-anon"">" + ПараHTMLДифф.Анонимизированный + "</td>";
				Иначе
					HTMLОриг = HTMLОриг + "<td>" + ПараHTMLДифф.Оригинал         + "</td>";
					HTMLАнон = HTMLАнон + "<td>" + ПараHTMLДифф.Анонимизированный + "</td>";
				КонецЕсли;
			Иначе
				HTMLОриг = HTMLОриг + "<td>" + ЗнОриг + "</td>";
				HTMLАнон = HTMLАнон + "<td>" + ЗнАнон + "</td>";
			КонецЕсли;
		КонецЦикла;
		HTMLОриг = HTMLОриг + "</tr>";
		HTMLАнон = HTMLАнон + "</tr>";
	КонецЦикла;

	Если МассивОриг.Количество() > 200 Тогда
		Стр = "<tr><td colspan=""" + Колонки.Количество() + """>Показано 200 из "
			+ МассивОриг.Количество() + " строк</td></tr>";
		HTMLОриг = HTMLОриг + Стр;
		HTMLАнон = HTMLАнон + Стр;
	КонецЕсли;

	HTMLОриг = HTMLОриг + "</table>";
	HTMLАнон = HTMLАнон + "</table>";
	Возврат Новый Структура("Оригинал, Анонимизированный, ЕстьИзменения", HTMLОриг, HTMLАнон, ЕстьИзменения);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьHTMLТаблицуИзМассиваСкаляров(МассивОриг, МассивАнон)
	ЕстьИзменения = Ложь;
	HTMLОриг = "<ol>";
	HTMLАнон = "<ol>";
	КолВо = МассивОриг.Количество();
	Если КолВо > 200 Тогда КолВо = 200; КонецЕсли;
	Для НдкО = 0 По КолВо - 1 Цикл
		РВОриг = МассивОриг[НдкО];
		РВАнон = РВОриг;
		Если НдкО < МассивАнон.Количество() Тогда
			РВАнон = МассивАнон[НдкО];
		КонецЕсли;
		ЗнОриг = НормализоватьЗначениеДляHTML(РВОриг);
		ЗнАнон = НормализоватьЗначениеДляHTML(РВАнон);
		Если ЗнОриг <> ЗнАнон Тогда
			ЕстьИзменения = Истина;
			ПараHTMLДифф = НормализоватьПаруЗначенийСПодсветкой(РВОриг, РВАнон);
			Если ПараHTMLДифф.ВесьЯчейка Тогда
				HTMLОриг = HTMLОриг + "<li class=""changed-orig"">" + ПараHTMLДифф.Оригинал         + "</li>";
				HTMLАнон = HTMLАнон + "<li class=""changed-anon"">" + ПараHTMLДифф.Анонимизированный + "</li>";
			Иначе
				HTMLОриг = HTMLОриг + "<li>" + ПараHTMLДифф.Оригинал         + "</li>";
				HTMLАнон = HTMLАнон + "<li>" + ПараHTMLДифф.Анонимизированный + "</li>";
			КонецЕсли;
		Иначе
			HTMLОриг = HTMLОриг + "<li>" + ЗнОриг + "</li>";
			HTMLАнон = HTMLАнон + "<li>" + ЗнАнон + "</li>";
		КонецЕсли;
	КонецЦикла;
	Если МассивОриг.Количество() > 200 Тогда
		HTMLОриг = HTMLОриг + "<li>Показано 200 из " + МассивОриг.Количество() + " строк</li>";
		HTMLАнон = HTMLАнон + "<li>Показано 200 из " + МассивАнон.Количество() + " строк</li>";
	КонецЕсли;
	HTMLОриг = HTMLОриг + "</ol>";
	HTMLАнон = HTMLАнон + "</ol>";
	Возврат Новый Структура("Оригинал, Анонимизированный, ЕстьИзменения", HTMLОриг, HTMLАнон, ЕстьИзменения);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьHTMLТаблицуИзОбъекта(ОригОбъект, АнонОбъект)
	ЕстьИзменения = Ложь;
	HTMLОриг = "<table><tr><th>Ключ</th><th>Значение</th></tr>";
	HTMLАнон = "<table><tr><th>Ключ</th><th>Значение</th></tr>";
	Ключи = Новый Массив;
	Если ТипЗнч(ОригОбъект) = Тип("Структура") Тогда
		Для Каждого КЗ Из ОригОбъект Цикл Ключи.Добавить(КЗ.Ключ); КонецЦикла;
	ИначеЕсли ТипЗнч(ОригОбъект) = Тип("Соответствие") Тогда
		Для Каждого КЗ Из ОригОбъект Цикл Ключи.Добавить(КЗ.Ключ); КонецЦикла;
	КонецЕсли;
	Для Каждого Ключ Из Ключи Цикл
		РВОриг = ПолучитьЗначениеОбъекта(ОригОбъект, Ключ);
		РВАнон = ПолучитьЗначениеОбъекта(АнонОбъект, Ключ);
		ЗнОриг = НормализоватьЗначениеДляHTML(РВОриг);
		ЗнАнон = НормализоватьЗначениеДляHTML(РВАнон);
		Если ЗнОриг <> ЗнАнон Тогда
			ЕстьИзменения = Истина;
			ПараHTMLДифф = НормализоватьПаруЗначенийСПодсветкой(РВОриг, РВАнон);
			КлючHTML = HTMLЭскейп(Строка(Ключ));
			Если ПараHTMLДифф.ВесьЯчейка Тогда
				HTMLОриг = HTMLОриг + "<tr><td>" + КлючHTML + "</td><td class=""changed-orig"">" + ПараHTMLДифф.Оригинал         + "</td></tr>";
				HTMLАнон = HTMLАнон + "<tr><td>" + КлючHTML + "</td><td class=""changed-anon"">" + ПараHTMLДифф.Анонимизированный + "</td></tr>";
			Иначе
				HTMLОриг = HTMLОриг + "<tr><td>" + КлючHTML + "</td><td>" + ПараHTMLДифф.Оригинал         + "</td></tr>";
				HTMLАнон = HTMLАнон + "<tr><td>" + КлючHTML + "</td><td>" + ПараHTMLДифф.Анонимизированный + "</td></tr>";
			КонецЕсли;
		Иначе
			HTMLОриг = HTMLОриг + "<tr><td>" + HTMLЭскейп(Строка(Ключ)) + "</td><td>" + ЗнОриг + "</td></tr>";
			HTMLАнон = HTMLАнон + "<tr><td>" + HTMLЭскейп(Строка(Ключ)) + "</td><td>" + ЗнАнон + "</td></tr>";
		КонецЕсли;
	КонецЦикла;
	HTMLОриг = HTMLОриг + "</table>";
	HTMLАнон = HTMLАнон + "</table>";
	Возврат Новый Структура("Оригинал, Анонимизированный, ЕстьИзменения", HTMLОриг, HTMLАнон, ЕстьИзменения);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьHTMLДляСравненияАнонимизации(ОригиналJSON, АнонимJSON)
	ОбертываниеCSS = "<html><head><meta charset=""utf-8""><style>"
		+ "body{font-family:sans-serif;font-size:11px;margin:4px}"
		+ "table{border-collapse:collapse;width:100%}"
		+ "th{background:#f0f0f0;border:1px solid #ccc;padding:3px 6px}"
		+ "td{border:1px solid #ddd;padding:3px 6px;vertical-align:top}"
		+ ".changed-orig{background:#FFF3CD}"
		+ ".changed-anon{background:#D4EDDA}"
		+ "</style></head><body>";
	ОбертываниеКонец = "</body></html>";

	Рез = Новый Структура("Оригинал, Анонимизированный, ЕстьИзменения", "", "", Ложь);
	Попытка
		ОригДанные = ПрочитатьJSON2(ОригиналJSON);
		АнонДанные = ПрочитатьJSON2(АнонимJSON);
	Исключение
		ЗнОриг = HTMLЭскейп(ОригиналJSON);
		ЗнАнон = HTMLЭскейп(АнонимJSON);
		ЕстьИзм = (ЗнОриг <> ЗнАнон);
		Рез.Оригинал         = ОбертываниеCSS + "<pre>" + ЗнОриг + "</pre>" + ОбертываниеКонец;
		Рез.Анонимизированный = ОбертываниеCSS + "<pre>" + ЗнАнон + "</pre>" + ОбертываниеКонец;
		Рез.ЕстьИзменения    = ЕстьИзм;
		Возврат Рез;
	КонецПопытки;

	ТипОриг = ТипЗнч(ОригДанные);
	ТипАнон = ТипЗнч(АнонДанные);

	ЧастичныйРезультат = Неопределено;

	Если ТипОриг = Тип("Массив") И ТипАнон = Тип("Массив") Тогда
		// Таблица только если ВСЕ элементы обоих массивов — объекты (смешанный → список)
		ВсеОбъекты = Истина;
		Для Каждого Эл Из ОригДанные Цикл
			Если ТипЗнч(Эл) <> Тип("Структура") И ТипЗнч(Эл) <> Тип("Соответствие") Тогда
				ВсеОбъекты = Ложь;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Если ВсеОбъекты Тогда
			Для Каждого Эл Из АнонДанные Цикл
				Если ТипЗнч(Эл) <> Тип("Структура") И ТипЗнч(Эл) <> Тип("Соответствие") Тогда
					ВсеОбъекты = Ложь;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		Если ВсеОбъекты Тогда
			Колонки = СобратьКлючиОбъектов(ОригДанные);
			ДопКолонки = СобратьКлючиОбъектов(АнонДанные);
			Для Каждого К Из ДопКолонки Цикл
				НайденоВОсн = Ложь;
				Для Каждого КО Из Колонки Цикл
					Если КО = К Тогда НайденоВОсн = Истина; Прервать; КонецЕсли;
				КонецЦикла;
				Если НЕ НайденоВОсн Тогда Колонки.Добавить(К); КонецЕсли;
			КонецЦикла;
			ЧастичныйРезультат = СформироватьHTMLТаблицуИзМассиваОбъектов(ОригДанные, АнонДанные, Колонки);
		Иначе
			ЧастичныйРезультат = СформироватьHTMLТаблицуИзМассиваСкаляров(ОригДанные, АнонДанные);
		КонецЕсли;
	ИначеЕсли (ТипОриг = Тип("Структура") Или ТипОриг = Тип("Соответствие"))
		И (ТипАнон = Тип("Структура") Или ТипАнон = Тип("Соответствие")) Тогда
		ЧастичныйРезультат = СформироватьHTMLТаблицуИзОбъекта(ОригДанные, АнонДанные);
	Иначе
		ЗнОриг = HTMLЭскейп(ОригиналJSON);
		ЗнАнон = HTMLЭскейп(АнонимJSON);
		ЧастичныйРезультат = Новый Структура("Оригинал, Анонимизированный, ЕстьИзменения",
			"<pre>" + ЗнОриг + "</pre>",
			"<pre>" + ЗнАнон + "</pre>",
			ЗнОриг <> ЗнАнон);
	КонецЕсли;

	Рез.Оригинал          = ОбертываниеCSS + ЧастичныйРезультат.Оригинал + ОбертываниеКонец;
	Рез.Анонимизированный = ОбертываниеCSS + ЧастичныйРезультат.Анонимизированный + ОбертываниеКонец;
	Рез.ЕстьИзменения     = ЧастичныйРезультат.ЕстьИзменения;
	Возврат Рез;
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────
// Перехват и подтверждение анонимизации
// ─────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция ПроверитьПодтверждениеАнонимизации(Результат, ДанныеЗапроса, Ид, ИдПотока, Режим = "")
	Если НЕ ПодтверждениеАнонимизации Тогда
		Возврат Ложь;
	КонецЕсли;
	Если ТипЗнч(Результат) <> Тип("Структура") Тогда
		Возврат Ложь;
	КонецЕсли;
	Если НЕ Результат.Свойство("_original_data_anon_marker") Тогда
		Возврат Ложь;
	КонецЕсли;
	ДанныеРезультата = Неопределено;
	Если НЕ Результат.Свойство("data", ДанныеРезультата) Тогда
		Возврат Ложь;
	КонецЕсли;
	Если ДанныеРезультата = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	Если ТипЗнч(ДанныеРезультата) = Тип("Массив") И ДанныеРезультата.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;

	ОригиналJSON = "";
	Результат.Свойство("_original_data_json", ОригиналJSON);
	АнонимJSON = ЗаписатьJSON2(Результат.data);

	HTMLДанные = СформироватьHTMLДляСравненияАнонимизации(ОригиналJSON, АнонимJSON);

	КонтекстОтвета = Новый Структура(
		"Результат, ДанныеЗапроса, Ид, ИдПотока, Режим",
		Результат, ДанныеЗапроса, Ид, ИдПотока, Режим);

	ТекстЗапросаДляФормы = "";
	Результат.Свойство("_cmd_arguments_json", ТекстЗапросаДляФормы);
	ТекстЗапросаДляФормы = СтрЗаменить(ТекстЗапросаДляФормы, "\n", Символы.ПС);
	ТекстЗапросаДляФормы = СтрЗаменить(ТекстЗапросаДляФормы, "\t", Символы.Таб);

	ОткрытьФорму(
		"ВнешняяОбработка.MCPToolkit.Форма.ФормаПодтверждениеАнонимизации",
		Новый Структура("HTMLОригинал, HTMLАнонимизированный, ТекстЗапроса",
			HTMLДанные.Оригинал, HTMLДанные.Анонимизированный, ТекстЗапросаДляФормы),
		ЭтаФорма, , , ,
		Новый ОписаниеОповещения("ПослеПодтверждениеАнонимизации", ЭтаФорма, КонтекстОтвета),
		РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
	Возврат Истина;
КонецФункции

&НаКлиенте
Процедура ПослеПодтверждениеАнонимизации(РезультатФормы, КонтекстОтвета) Экспорт
	// REST-ветка: HTTP-ответ вместо SSE
	Если КонтекстОтвета.Режим = "rest" Тогда
		Если РезультатФормы = Истина Тогда
			Результат = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(КонтекстОтвета.Результат);
			Если ИспользоватьАсинхронноеПодключениеКомпонент И КомпонентаТун <> Неопределено
				И ФорматОтветаВстроенный = 1 И ТипЗнч(Результат) = Тип("Структура")
				И Результат.Свойство("success") И Результат.success И Результат.Свойство("data") Тогда
				КомпонентаТун.НачатьВызовJsonToToon(
					Новый ОписаниеОповещения("ПослеJsonToToonREST", ЭтаФорма,
						Новый Структура("ДанныеЗапроса, Результат",
							КонтекстОтвета.ДанныеЗапроса, Результат)),
					ЗаписатьJSON2(Результат.data));
				Возврат;
			КонецЕсли;
			ПрименитьТунКонвертацию(Результат);
			КомпонентаSendResponse(КонтекстОтвета.ДанныеЗапроса.id, 200,
				ЗаголовкиJSON(), ЗаписатьJSON2(Результат));
		Иначе
			ОтветОтказ = Новый Структура("success, error", Ложь,
				"Пользователь отклонил передачу данных агенту.");
			КомпонентаSendResponse(КонтекстОтвета.ДанныеЗапроса.id, 200,
				ЗаголовкиJSON(), ЗаписатьJSON2(ОтветОтказ));
		КонецЕсли;
		Возврат;
	КонецЕсли;
	// MCP/SSE-ветка
	Если РезультатФормы = Истина Тогда
		ТулКолРез = СформироватьToolsCallРезультат(
			КонтекстОтвета.Результат,
			КонтекстОтвета.ДанныеЗапроса,
			КонтекстОтвета.Ид,
			КонтекстОтвета.ИдПотока);
		Если ТулКолРез = Неопределено Тогда
			Возврат; // async TOON — уйдёт сам
		КонецЕсли;
		ОтветJSON = СформироватьJSONRPCОтвет(КонтекстОтвета.Ид, ТулКолРез);
	Иначе
		ОтветОтказ = Новый Структура("success, error", Ложь,
			"Пользователь отклонил передачу данных агенту.");
		ОтветJSON = СформироватьJSONRPCОтвет(КонтекстОтвета.Ид,
			СформироватьToolsCallРезультат(ОтветОтказ));
	КонецЕсли;

	Если ЗначениеЗаполнено(КонтекстОтвета.ИдПотока) Тогда
		КомпонентаSendSSEEvent(КонтекстОтвета.ИдПотока, ОтветJSON, "{}", "message");
	Иначе
		ОтправитьSSEОтвет(КонтекстОтвета.ДанныеЗапроса, ОтветJSON);
	КонецЕсли;
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ПрименитьТочнуюPolicyКSchema(Schema, ПравилаТочнойАнонимизации, КешРазыменования = Неопределено, АгрессивныйРежим = Ложь)

	Колонки = Неопределено;
	Если НЕ Schema.Свойство("columns", Колонки) Тогда
		Возврат;
	КонецЕсли;
	Если ТипЗнч(Колонки) <> Тип("Массив") Тогда
		Возврат;
	КонецЕсли;

	Для Каждого Колонка Из Колонки Цикл
		Если ТипЗнч(Колонка) <> Тип("Структура") Тогда
			Продолжить;
		КонецЕсли;

		Колонка.Вставить("anonymization_mode", "default");
		Колонка.Вставить("reference_presentation_mode", "default");
		Если ЭтоБулеваКолонкаСхемы(Колонка) Тогда
			Продолжить;
		КонецЕсли;
		Если ЭтоПеречислениеКолонкаСхемы(Колонка) Тогда
			Колонка.Вставить("anonymization_mode", "force_keep");
			Колонка.Вставить("reference_presentation_mode", "force_keep");
			Продолжить;
		КонецЕсли;

		Sources = ПолучитьМассивСтрокИзСвойства(Колонка, "sources");
		ТекущийРежим = ВычислитьРежимПоSources(Sources, ПравилаТочнойАнонимизации);
		Если ТекущийРежим = "default" И КешРазыменования <> Неопределено Тогда
			ТекущийРежим = ВычислитьРежимПоSourcesИзКеша(Sources, ПравилаТочнойАнонимизации, КешРазыменования);
		КонецЕсли;
		// Строковые колонки, чьи источники через метаданные ведут к ПланыСчетов —
		// защищаем по умолчанию (например, СчетДт.Представление, СчетДт.Код).
		Если КолонкаСтроковаяПоСхеме(Колонка)
			И ТекущийРежим <> "force_anonymize"
			И ИсточникиВедутКПланамСчетов(Sources) Тогда
			Колонка.Вставить("anonymization_mode", "force_keep");
			Колонка.Вставить("reference_presentation_mode", "force_keep");
			Продолжить;
		КонецЕсли;
		Если АгрессивныйРежим И ТекущийРежим = "default" И КолонкаСтроковаяПоСхеме(Колонка) Тогда
			ТекущийРежим = "force_anonymize";
		КонецЕсли;
		РежимПредставленияСсылки = ВычислитьРежимПредставленияСсылки(Колонка, Sources, ПравилаТочнойАнонимизации);
		// Планы счетов — нормативные данные без персональных данных: по умолчанию не анонимизируем.
		// Оба режима уже вычислены из явных правил. Если нет явного force_anonymize — защищаем.
		Если ЭтоПланСчетовКолонкаСхемы(Колонка)
			И ТекущийРежим <> "force_anonymize"
			И РежимПредставленияСсылки <> "force_anonymize" Тогда
			Колонка.Вставить("anonymization_mode", "force_keep");
			Колонка.Вставить("reference_presentation_mode", "force_keep");
			Продолжить;
		КонецЕсли;
		Если АгрессивныйРежим И РежимПредставленияСсылки = "default" И КолонкаСсылочнаяПоСхеме(Колонка) Тогда
			РежимПредставленияСсылки = "force_anonymize";
		КонецЕсли;

		Если КолонкаСтроковаяПоСхеме(Колонка) И РежимПредставленияСсылки <> "default" Тогда
			ТекущийРежим = ОбъединитьРежимыPolicy(ТекущийРежим, РежимПредставленияСсылки);
		Иначе
			Колонка.Вставить("reference_presentation_mode", РежимПредставленияСсылки);
		КонецЕсли;

		Колонка.Вставить("anonymization_mode", ТекущийРежим);
	КонецЦикла;

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция НуженКешРазыменования(Schema, ПравилаТочнойАнонимизации)

	Колонки = Неопределено;
	Если НЕ Schema.Свойство("columns", Колонки) Тогда
		Возврат Ложь;
	КонецЕсли;
	Если ТипЗнч(Колонки) <> Тип("Массив") Тогда
		Возврат Ложь;
	КонецЕсли;

	Для Каждого Колонка Из Колонки Цикл
		Sources = ПолучитьМассивСтрокИзСвойства(Колонка, "sources");
		Для Каждого SourcePath Из Sources Цикл
			Если ПравилаТочнойАнонимизации.Получить(SourcePath) <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			КоличествоТочек = СтрДлина(SourcePath) - СтрДлина(СтрЗаменить(SourcePath, ".", ""));
			Если КоличествоТочек >= 3 Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

	Возврат Ложь;

КонецФункции

&НаСервереБезКонтекста
Функция НайтиВКоллекцииПоИмени(Коллекция, ИмяПоля)
	Попытка
		Для Каждого Рекв Из Коллекция Цикл
			Если НРег(Рекв.Имя) = НРег(ИмяПоля) Тогда
				Возврат Рекв;
			КонецЕсли;
		КонецЦикла;
	Исключение КонецПопытки;
	Возврат Неопределено;
КонецФункции

&НаСервереБезКонтекста
Функция НайтиРеквизитВКоллекциях(ОбъектМД, ИмяПоля)

	Рекв = Неопределено;

	Попытка
		Рекв = НайтиВКоллекцииПоИмени(ОбъектМД.СтандартныеРеквизиты, ИмяПоля);
	Исключение КонецПопытки;
	Если Рекв <> Неопределено Тогда Возврат Рекв; КонецЕсли;

	Попытка
		Рекв = НайтиВКоллекцииПоИмени(ОбъектМД.Реквизиты, ИмяПоля);
	Исключение КонецПопытки;
	Если Рекв <> Неопределено Тогда Возврат Рекв; КонецЕсли;

	Попытка
		Рекв = НайтиВКоллекцииПоИмени(ОбъектМД.Измерения, ИмяПоля);
	Исключение КонецПопытки;
	Если Рекв <> Неопределено Тогда Возврат Рекв; КонецЕсли;

	Попытка
		Рекв = НайтиВКоллекцииПоИмени(ОбъектМД.Ресурсы, ИмяПоля);
	Исключение КонецПопытки;
	Если Рекв <> Неопределено Тогда Возврат Рекв; КонецЕсли;

	Попытка
		Рекв = НайтиВКоллекцииПоИмени(ОбъектМД.РеквизитыАдресации, ИмяПоля);
	Исключение КонецПопытки;
	Если Рекв <> Неопределено Тогда Возврат Рекв; КонецЕсли;

	Возврат Неопределено;

КонецФункции

&НаСервереБезКонтекста
Функция РазрешитьВиртуальныйОбъектРегистраБухгалтерии(ОбъМД, ИмяСегмента)
	// Если текущий объект — РегистрБухгалтерии И сегмент — одно из виртуальных
	// полей счёта (Счет, СчетДт, СчетКт), возвращает объект метаданных плана счетов.
	// В остальных случаях возвращает Неопределено.
	Если ОбъМД = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	ПолноеИмя = "";
	Попытка
		ПолноеИмя = ОбъМД.ПолноеИмя();
	Исключение
		Возврат Неопределено;
	КонецПопытки;

	Если Лев(ПолноеИмя, СтрДлина("РегистрБухгалтерии.")) <> "РегистрБухгалтерии." Тогда
		Возврат Неопределено;
	КонецЕсли;

	ИмяНиж = НРег(ИмяСегмента);
	Если ИмяНиж <> "счет" И ИмяНиж <> "счетдт" И ИмяНиж <> "счеткт" Тогда
		Возврат Неопределено;
	КонецЕсли;

	ПланМД = Неопределено;
	Попытка
		ПланМД = ОбъМД.ПланСчетов;
	Исключение
	КонецПопытки;

	Возврат ПланМД;

КонецФункции

&НаСервереБезКонтекста
Функция РазрешитьПутиРазыменования(SourcePath)

	Результат = Новый Массив;
	Части = РазделитьСтрокуПоРазделителю(SourcePath, ".");

	Если Части.Количество() <= 3 Тогда
		Возврат Результат;
	КонецЕсли;

	ИмяОбъекта = Части[0] + "." + Части[1];
	ТекущийОбъект = Неопределено;
	Попытка
		ТекущийОбъект = Метаданные.НайтиПоПолномуИмени(ИмяОбъекта);
	Исключение КонецПопытки;
	Если ТекущийОбъект = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;

	ТекущиеОбъекты = Новый Массив;
	ТекущиеОбъекты.Добавить(ТекущийОбъект);

	КоличествоЧастей = Части.Количество();
	Для Инд = 2 По КоличествоЧастей - 2 Цикл
		СледующиеОбъектыСет = Новый Соответствие;
		Для Каждого ОбъМД Из ТекущиеОбъекты Цикл
			ИмяСегмента = Части[Инд];
			Реквизит = НайтиРеквизитВКоллекциях(ОбъМД, ИмяСегмента);
			Если Реквизит <> Неопределено Тогда
				Попытка
					Для Каждого Тип Из Реквизит.Тип.Типы() Цикл
						СледОбъект = Неопределено;
						Попытка
							СледОбъект = Метаданные.НайтиПоТипу(Тип);
						Исключение КонецПопытки;
						Если СледОбъект <> Неопределено Тогда
							СледующиеОбъектыСет.Вставить(СледОбъект.ПолноеИмя(), СледОбъект);
						КонецЕсли;
					КонецЦикла;
				Исключение КонецПопытки;
			Иначе
				// Fallback: виртуальные поля счёта у РегистраБухгалтерии (СчетДт, СчетКт, Счет)
				ВиртуальныйОбъект = РазрешитьВиртуальныйОбъектРегистраБухгалтерии(ОбъМД, ИмяСегмента);
				Если ВиртуальныйОбъект <> Неопределено Тогда
					СледующиеОбъектыСет.Вставить(ВиртуальныйОбъект.ПолноеИмя(), ВиртуальныйОбъект);
				КонецЕсли;
			КонецЕсли;
			// Fallback: сегмент может быть табличной частью (Товары, Строки и т.п.)
			Попытка
				ТабЧасть = ОбъМД.ТабличныеЧасти.Найти(ИмяСегмента);
				Если ТабЧасть <> Неопределено Тогда
					СледующиеОбъектыСет.Вставить(ОбъМД.ПолноеИмя() + "." + ИмяСегмента, ТабЧасть);
				КонецЕсли;
			Исключение КонецПопытки;
			Попытка
				ТабЧасть = ОбъМД.СтандартныеТабличныеЧасти.Найти(ИмяСегмента);
				Если ТабЧасть <> Неопределено Тогда
					СледующиеОбъектыСет.Вставить(ОбъМД.ПолноеИмя() + "." + ИмяСегмента, ТабЧасть);
				КонецЕсли;
			Исключение КонецПопытки;
		КонецЦикла;
		СледующиеОбъекты = Новый Массив;
		Для Каждого КвП Из СледующиеОбъектыСет Цикл
			СледующиеОбъекты.Добавить(КвП.Значение);
		КонецЦикла;
		ТекущиеОбъекты = СледующиеОбъекты;
		Если ТекущиеОбъекты.Количество() = 0 Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЦикла;

	ИмяПоследнегоПоля = Части[КоличествоЧастей - 1];
	РезультатСет = Новый Соответствие;
	Для Каждого ОбъМД Из ТекущиеОбъекты Цикл
		Попытка
			РезультатСет.Вставить(ОбъМД.ПолноеИмя() + "." + ИмяПоследнегоПоля, Истина);
		Исключение КонецПопытки;
	КонецЦикла;
	Для Каждого КвП Из РезультатСет Цикл
		Результат.Добавить(КвП.Ключ);
	КонецЦикла;

	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ВычислитьКешРазыменованияДляSchema(Schema, ПравилаТочнойАнонимизации)

	Кеш = Новый Соответствие;
	Колонки = Неопределено;
	Если НЕ Schema.Свойство("columns", Колонки) Тогда
		Возврат Кеш;
	КонецЕсли;
	Если ТипЗнч(Колонки) <> Тип("Массив") Тогда
		Возврат Кеш;
	КонецЕсли;

	Для Каждого Колонка Из Колонки Цикл
		Sources = ПолучитьМассивСтрокИзСвойства(Колонка, "sources");
		Для Каждого SourcePath Из Sources Цикл
			Если Кеш.Получить(SourcePath) <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			Если ПравилаТочнойАнонимизации.Получить(SourcePath) <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			Части = РазделитьСтрокуПоРазделителю(SourcePath, ".");
			Если Части.Количество() <= 3 Тогда
				Продолжить;
			КонецЕсли;
			КандидатыВсе = РазрешитьПутиРазыменования(SourcePath);
			КандидатыПути = Новый Массив;
			Для Каждого КандидатПуть Из КандидатыВсе Цикл
				Если ПравилаТочнойАнонимизации.Получить(КандидатПуть) <> Неопределено Тогда
					КандидатыПути.Добавить(КандидатПуть);
				КонецЕсли;
			КонецЦикла;
			Кеш.Вставить(SourcePath, КандидатыПути);
		КонецЦикла;
	КонецЦикла;

	Возврат Кеш;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ВычислитьРежимПоSourcesИзКеша(Sources, ПравилаТочнойАнонимизации, КешРазыменования)

	ЕстьKeep = Ложь;
	Для Каждого SourcePath Из Sources Цикл
		КандидатыПути = КешРазыменования.Получить(SourcePath);
		Если КандидатыПути = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		Для Каждого КандидатПуть Из КандидатыПути Цикл
			Если ПравилаТочнойАнонимизации.Получить(КандидатПуть) <> Неопределено Тогда
				Action = ПравилаТочнойАнонимизации.Получить(КандидатПуть);
				Если Action.action = "anonymize" Тогда
					Возврат "force_anonymize";
				ИначеЕсли Action.action = "keep" Тогда
					ЕстьKeep = Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	Если ЕстьKeep Тогда Возврат "force_keep"; КонецЕсли;
	Возврат "default";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ВычислитьРежимПоSources(Sources, ПравилаТочнойАнонимизации)

	ЕстьKeep = Ложь;
	Для Каждого SourcePath Из Sources Цикл
		Если ПравилаТочнойАнонимизации.Получить(SourcePath) <> Неопределено Тогда
			Action = ПравилаТочнойАнонимизации.Получить(SourcePath);
			Если Action.action = "anonymize" Тогда
				Возврат "force_anonymize";
			ИначеЕсли Action.action = "keep" Тогда
				ЕстьKeep = Истина;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Если ЕстьKeep Тогда
		Возврат "force_keep";
	КонецЕсли;

	Возврат "default";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ВычислитьРежимПредставленияСсылки(Колонка, Sources, ПравилаТочнойАнонимизации)

	Для Каждого SourcePath Из Sources Цикл
		Если СтрДлина(SourcePath) >= 6 И Сред(SourcePath, СтрДлина(SourcePath) - 5, 6) = ".Ссылка" Тогда
			ПутьНаименования = ПолучитьПутьНаименованияПоИсточникуСсылки(SourcePath);
			Если ПравилаТочнойАнонимизации.Получить(ПутьНаименования) <> Неопределено Тогда
				Возврат ?(ПравилаТочнойАнонимизации.Получить(ПутьНаименования).action = "keep", "force_keep", "force_anonymize");
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Если КолонкаСсылочнаяПоСхеме(Колонка) Тогда
		ПутьНаименования = ПолучитьПутьНаименованияПоТипамКолонки(Колонка);
		Если ЗначениеЗаполнено(ПутьНаименования) И ПравилаТочнойАнонимизации.Получить(ПутьНаименования) <> Неопределено Тогда
			Возврат ?(ПравилаТочнойАнонимизации.Получить(ПутьНаименования).action = "keep", "force_keep", "force_anonymize");
		КонецЕсли;
	КонецЕсли;

	Возврат "default";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОбъединитьРежимыPolicy(ОсновнойРежим, ДополнительныйРежим)

	Если ОсновнойРежим = "force_anonymize" Или ДополнительныйРежим = "force_anonymize" Тогда
		Возврат "force_anonymize";
	КонецЕсли;
	Если ОсновнойРежим = "force_keep" Или ДополнительныйРежим = "force_keep" Тогда
		Возврат "force_keep";
	КонецЕсли;
	Возврат "default";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьМассивСтрокИзСвойства(СтруктураДанных, ИмяСвойства)

	Результат = Новый Массив;
	ЗначениеСвойства = Неопределено;
	Если НЕ СтруктураДанных.Свойство(ИмяСвойства, ЗначениеСвойства) Тогда
		Возврат Результат;
	КонецЕсли;
	Если ТипЗнч(ЗначениеСвойства) <> Тип("Массив") Тогда
		Возврат Результат;
	КонецЕсли;

	Для Каждого ЭлементМассива Из ЗначениеСвойства Цикл
		Если ТипЗнч(ЭлементМассива) = Тип("Строка") И НЕ ПустаяСтрока(ЭлементМассива) Тогда
			Результат.Добавить(ЭлементМассива);
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоБулеваКолонкаСхемы(Колонка)
	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Если ТипыКолонки.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если ТипКолонки <> "Булево" Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоПеречислениеКолонкаСхемы(Колонка)
	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Если ТипыКолонки.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если Найти(ТипКолонки, "ПеречислениеСсылка.") <> 1 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоПланСчетовКолонкаСхемы(Колонка)
	// Истина если ВСЕ типы колонки — ссылки на план счетов.
	// Планы счетов содержат нормативные данные (коды и наименования счетов) без персональных данных.
	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Если ТипыКолонки.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если Найти(ТипКолонки, "ПланСчетовСсылка.") <> 1 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

&НаСервереБезКонтекста
Функция ИсточникиВедутКПланамСчетов(Sources)
	// Истина если хотя бы один источник колонки после разыменования по метаданным
	// ведёт к объекту ПланыСчетов. Защищает строковые колонки типа СчетДт.Представление.
	Для Каждого SourcePath Из Sources Цикл
		Если Найти(SourcePath, "ПланСчетов.") = 1 Тогда
			Возврат Истина;
		КонецЕсли;
		РазрешённыеПути = РазрешитьПутиРазыменования(SourcePath);
		Для Каждого РазрешённыйПуть Из РазрешённыеПути Цикл
			Если Найти(РазрешённыйПуть, "ПланСчетов.") = 1 Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	Возврат Ложь;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция КолонкаСтроковаяПоСхеме(Колонка)
	Возврат НайтиТипВСпискеТиповКолонки(Колонка, "Строка");
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция КолонкаСсылочнаяПоСхеме(Колонка)

	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если Найти(ТипКолонки, "Ссылка.") > 0 Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;

	Возврат Ложь;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НайтиТипВСпискеТиповКолонки(Колонка, ИскомыйТип)

	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если ТипКолонки = ИскомыйТип Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;

	Возврат Ложь;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьПутьНаименованияПоИсточникуСсылки(SourcePath)

	ПозицияПоследнейТочки = НайтиПозициюПоследнейТочки(SourcePath);
	Если ПозицияПоследнейТочки = 0 Тогда
		Возврат "";
	КонецЕсли;

	Возврат Лев(SourcePath, ПозицияПоследнейТочки - 1) + ".Наименование";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьПутьНаименованияПоТипамКолонки(Колонка)

	ТипыКолонки = ПолучитьМассивСтрокИзСвойства(Колонка, "types");
	Для Каждого ТипКолонки Из ТипыКолонки Цикл
		Если Лев(ТипКолонки, 17) = "СправочникСсылка." Тогда
			Возврат "Справочник." + Сред(ТипКолонки, 18) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 15) = "ДокументСсылка." Тогда
			Возврат "Документ." + Сред(ТипКолонки, 16) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 28) = "ПланВидовХарактеристикСсылка." Тогда
			Возврат "ПланВидовХарактеристик." + Сред(ТипКолонки, 29) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 18) = "ПланСчетовСсылка." Тогда
			Возврат "ПланСчетов." + Сред(ТипКолонки, 19) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 22) = "ПланВидовРасчетаСсылка." Тогда
			Возврат "ПланВидовРасчета." + Сред(ТипКолонки, 23) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 20) = "БизнесПроцессСсылка." Тогда
			Возврат "БизнесПроцесс." + Сред(ТипКолонки, 21) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 13) = "ЗадачаСсылка." Тогда
			Возврат "Задача." + Сред(ТипКолонки, 14) + ".Наименование";
		ИначеЕсли Лев(ТипКолонки, 17) = "ПланОбменаСсылка." Тогда
			Возврат "ПланОбмена." + Сред(ТипКолонки, 18) + ".Наименование";
		КонецЕсли;
	КонецЦикла;

	Возврат "";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьПутьНаименованияПоТипуОбъекта(ТипОбъекта)

	Если Лев(ТипОбъекта, 17) = "СправочникСсылка." Тогда
		Возврат "Справочник." + Сред(ТипОбъекта, 18) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 15) = "ДокументСсылка." Тогда
		Возврат "Документ." + Сред(ТипОбъекта, 16) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 28) = "ПланВидовХарактеристикСсылка." Тогда
		Возврат "ПланВидовХарактеристик." + Сред(ТипОбъекта, 29) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 22) = "ПланВидовРасчетаСсылка." Тогда
		Возврат "ПланВидовРасчета." + Сред(ТипОбъекта, 23) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 20) = "БизнесПроцессСсылка." Тогда
		Возврат "БизнесПроцесс." + Сред(ТипОбъекта, 21) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 13) = "ЗадачаСсылка." Тогда
		Возврат "Задача." + Сред(ТипОбъекта, 14) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 17) = "ПланОбменаСсылка." Тогда
		Возврат "ПланОбмена." + Сред(ТипОбъекта, 18) + ".Наименование";
	ИначеЕсли Лев(ТипОбъекта, 18) = "ПланСчетовСсылка." Тогда
		Возврат "ПланСчетов." + Сред(ТипОбъекта, 19) + ".Наименование";
	КонецЕсли;
	Возврат "";

КонецФункции

&НаКлиенте
Функция ВычислитьРежимObjectRef(ТипОбъекта, ПравилаАнонимизации, АгрессивныйРежим)

	Если Найти(ТипОбъекта, "ПеречислениеСсылка.") = 1 Тогда Возврат "force_keep"; КонецЕсли;
	ПутьНаименования = ПолучитьПутьНаименованияПоТипуОбъекта(ТипОбъекта);
	Режим = "default";
	Если НЕ ПустаяСтрока(ПутьНаименования) Тогда
		Правило = ПравилаАнонимизации.Получить(ПутьНаименования);
		Если Правило <> Неопределено Тогда
			Режим = ?(Правило.action = "keep", "force_keep", "force_anonymize");
		КонецЕсли;
	КонецЕсли;
	// Планы счетов — force_keep если нет явного правила force_anonymize.
	// Проверка ДО агрессивного режима: aggressive mode не затрагивает планы счетов.
	Если Найти(ТипОбъекта, "ПланСчетовСсылка.") = 1 И Режим <> "force_anonymize" Тогда
		Возврат "force_keep";
	КонецЕсли;
	// Агрессивный режим: elevate default→force_anonymize для всех остальных типов
	Если Режим = "default" И АгрессивныйРежим Тогда
		Режим = "force_anonymize";
	КонецЕсли;
	Возврат Режим;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НайтиПозициюПоследнейТочки(СтрокаПути)

	ПозицияТочки = 0;
	ПозицияПоиска = 1;
	Пока Истина Цикл
		СледующаяПозиция = Найти(Сред(СтрокаПути, ПозицияПоиска), ".");
		Если СледующаяПозиция = 0 Тогда
			Прервать;
		КонецЕсли;
		СледующаяПозиция = ПозицияПоиска + СледующаяПозиция - 1;
		ПозицияТочки = СледующаяПозиция;
		ПозицияПоиска = СледующаяПозиция + 1;
	КонецЦикла;

	Возврат ПозицияТочки;

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(Результат)

	Попытка Результат.Удалить("_original_data_json");        Исключение КонецПопытки;
	Попытка Результат.Удалить("_original_data_anon_marker"); Исключение КонецПопытки;
	Попытка Результат.Удалить("_cmd_arguments_json");        Исключение КонецПопытки;

	Если ТипЗнч(Результат) <> Тип("Структура") Тогда
		Возврат Результат;
	КонецЕсли;
	Если НЕ Результат.Свойство("schema") Тогда
		Возврат Результат;
	КонецЕсли;
	Если Результат.schema = Неопределено Или ТипЗнч(Результат.schema) <> Тип("Структура") Тогда
		Возврат Результат;
	КонецЕсли;

	Колонки = Неопределено;
	Если НЕ Результат.schema.Свойство("columns", Колонки) Тогда
		Возврат Результат;
	КонецЕсли;
	Если ТипЗнч(Колонки) <> Тип("Массив") Тогда
		Возврат Результат;
	КонецЕсли;

	Для Каждого Колонка Из Колонки Цикл
		Если ТипЗнч(Колонка) <> Тип("Структура") Тогда
			Продолжить;
		КонецЕсли;
		Попытка
			Колонка.Удалить("anonymization_mode");
		Исключение
		КонецПопытки;
		Попытка
			Колонка.Удалить("reference_presentation_mode");
		Исключение
		КонецПопытки;
	КонецЦикла;

	Возврат Результат;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ — ТОКЕН-МАППЕР (ВСТРОЕННЫЙ РЕЖИМ)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура АнонимизаторИнициализировать()

	Если МаппингАнонимизации = Неопределено Тогда
		МаппингАнонимизации   = Новый Соответствие;
	КонецЕсли;
	Если МаппингДеанонимизации = Неопределено Тогда
		МаппингДеанонимизации = Новый Соответствие;
	КонецЕсли;
	Если СчетчикиТокенов = Неопределено Тогда
		СчетчикиТокенов       = Новый Соответствие;
	КонецЕсли;
	Если СловарьАнонимизации = Неопределено Тогда
		СловарьАнонимизации   = Новый Массив;
	КонецЕсли;
	Если БакетИндексСловаря = Неопределено Тогда
		БакетИндексСловаря    = Новый Соответствие;
	КонецЕсли;
	Если СловарьПостроен = Неопределено Тогда
		СловарьПостроен       = Ложь;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура АнонимизаторОчистить()

	МаппингАнонимизации   = Новый Соответствие;
	МаппингДеанонимизации = Новый Соответствие;
	СчетчикиТокенов       = Новый Соответствие;
	СловарьАнонимизации   = Новый Массив;
	БакетИндексСловаря    = Новый Соответствие;
	СловарьПостроен       = Ложь;
	Элементы.НадписьКоличествоЗаписейСловаря.Заголовок = "";

КонецПроцедуры

&НаКлиенте
Функция АнонимизаторТокенизировать(Значение, Категория)

	АнонимизаторИнициализировать();

	СуществующийТокен = МаппингАнонимизации.Получить(Значение);
	Если СуществующийТокен <> Неопределено Тогда
		Возврат СуществующийТокен;
	КонецЕсли;

	Счётчик = СчетчикиТокенов.Получить(Категория);
	Если Счётчик = Неопределено Тогда
		Счётчик = 0;
	КонецЕсли;
	Счётчик = Счётчик + 1;
	СчетчикиТокенов.Вставить(Категория, Счётчик);

	Токен = "[" + Категория + "-" + Формат(Счётчик, "ЧЦ=5; ЧВН=; ЧН=0; ЧГ=0") + "]";
	МаппингАнонимизации.Вставить(Значение, Токен);
	МаппингДеанонимизации.Вставить(ВРег(Токен), Значение);

	Возврат Токен;

КонецФункции

&НаКлиенте
Функция ЭтоВалидныйТокенФормат(Строка)

	// Формат: [CATEGORY-NNNNN] — буквы, дефис, минимум 5 цифр (счётчик > 99999 даёт > 5 знаков)
	Длина = СтрДлина(Строка);
	Если Длина < 9 Тогда
		Возврат Ложь;
	КонецЕсли;
	Если Лев(Строка, 1) <> "[" Или Прав(Строка, 1) <> "]" Тогда
		Возврат Ложь;
	КонецЕсли;
	Внутренняя = Сред(Строка, 2, Длина - 2);
	ПозицияДефиса = Найти(Внутренняя, "-");
	Если ПозицияДефиса < 2 Тогда
		Возврат Ложь;
	КонецЕсли;
	ЧастьКатегории = Лев(Внутренняя, ПозицияДефиса - 1);
	ЧастьНомера    = Сред(Внутренняя, ПозицияДефиса + 1);
	Если СтрДлина(ЧастьНомера) < 5 Тогда // минимум 5 цифр — счётчик > 99999 будет > 5 знаков
		Возврат Ложь;
	КонецЕсли;
	// категория — только буквы
	Для Инд = 1 По СтрДлина(ЧастьКатегории) Цикл
		СимВРег = ВРег(Сред(ЧастьКатегории, Инд, 1));
		КодСим  = КодСимвола(СимВРег);
		Если КодСим < 65 Или КодСим > 90 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	// номер — только цифры
	Для Инд = 1 По СтрДлина(ЧастьНомера) Цикл
		КодСим = КодСимвола(Сред(ЧастьНомера, Инд, 1));
		Если КодСим < 48 Или КодСим > 57 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;

	Возврат Истина;

КонецФункции

&НаКлиенте
Функция АнонимизаторДеТокенизироватьСтроку(Текст)

	АнонимизаторИнициализировать();

	Если МаппингДеанонимизации.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;

	Результат = "";
	Позиция   = 1;
	Длина     = СтрДлина(Текст);

	Пока Позиция <= Длина Цикл
		ПозОткр = Найти(Сред(Текст, Позиция), "[");
		Если ПозОткр = 0 Тогда
			Результат = Результат + Сред(Текст, Позиция);
			Прервать;
		КонецЕсли;
		ПозОткр = Позиция + ПозОткр - 1;
		Результат = Результат + Сред(Текст, Позиция, ПозОткр - Позиция);

		ПозЗакр = Найти(Сред(Текст, ПозОткр), "]");
		Если ПозЗакр = 0 Тогда
			Результат = Результат + Сред(Текст, ПозОткр);
			Прервать;
		КонецЕсли;
		ПозЗакр = ПозОткр + ПозЗакр - 1;

		Кандидат = Сред(Текст, ПозОткр, ПозЗакр - ПозОткр + 1);
		Если ЭтоВалидныйТокенФормат(Кандидат) Тогда
			Оригинал = МаппингДеанонимизации.Получить(ВРег(Кандидат));
			Если Оригинал <> Неопределено Тогда
				Результат = Результат + Оригинал;
			Иначе
				Результат = Результат + Кандидат;
			КонецЕсли;
		Иначе
			Результат = Результат + Кандидат;
		КонецЕсли;

		Позиция = ПозЗакр + 1;
	КонецЦикла;

	Возврат Результат;

КонецФункции

&НаКлиенте
Функция АнонимизаторАнонимизироватьСтроку(Текст)

	АнонимизаторИнициализировать();

	Если ПустаяСтрока(Текст) Тогда
		Возврат Текст;
	КонецЕсли;

	// Шаг 1: словарь — организации, физлица (с границами слов, создаёт новые токены)
	// Если ИспользоватьТочнуюАнонимизацию выключена — функция вернёт текст без изменений
	Результат = ПрименитьСловарьКСтроке(Текст);

	// Шаг 2: regex-паттерны (ИНН, телефон, email и т.д.)
	Если КомпонентаRegexНедоступна = Истина Тогда
		Возврат Результат;
	КонецЕсли;
	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") И Правило.enabled = Истина Тогда
			ВключённыеПравила.Добавить(Новый Структура("pattern, category",
				Правило.pattern, Правило.category));
		КонецЕсли;
	КонецЦикла;
	Если ВключённыеПравила.Количество() = 0 Тогда
		Возврат Результат;
	КонецЕсли;
	Если КомпонентаRegex = Неопределено Тогда
		ПодключитьКомпонентуRegexСинхронно();
	КонецЕсли;
	Если КомпонентаRegex = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	ПравилаJSON  = ЗаписатьJSON2(ВключённыеПравила);
	ТекстыМассив = Новый Массив;
	ТекстыМассив.Добавить(Результат);
	ТекстыJSON   = ЗаписатьJSON2(ТекстыМассив);
	СовпаденияJSON = КомпонентаRegex.НайтиСовпаденияВТекстах(ПравилаJSON, ТекстыJSON);

	Возврат ПрименитьRegexТокеныКСтроке(Результат, СовпаденияJSON);

КонецФункции

&НаКлиенте
Функция АнонимизаторДеТокенизироватьСтрокуВBSL(Текст)
	// Аналог АнонимизаторДеТокенизироватьСтроку, но при подстановке экранирует " → ""
	// чтобы замена токена не ломала строковый литерал BSL (актуально для params.code в execute_code)

	АнонимизаторИнициализировать();

	Если МаппингДеанонимизации.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;

	Результат = "";
	Позиция   = 1;
	Длина     = СтрДлина(Текст);

	Пока Позиция <= Длина Цикл
		ПозОткр = Найти(Сред(Текст, Позиция), "[");
		Если ПозОткр = 0 Тогда
			Результат = Результат + Сред(Текст, Позиция);
			Прервать;
		КонецЕсли;
		ПозОткр = Позиция + ПозОткр - 1;
		Результат = Результат + Сред(Текст, Позиция, ПозОткр - Позиция);

		ПозЗакр = Найти(Сред(Текст, ПозОткр), "]");
		Если ПозЗакр = 0 Тогда
			Результат = Результат + Сред(Текст, ПозОткр);
			Прервать;
		КонецЕсли;
		ПозЗакр = ПозОткр + ПозЗакр - 1;

		Кандидат = Сред(Текст, ПозОткр, ПозЗакр - ПозОткр + 1);
		Если ЭтоВалидныйТокенФормат(Кандидат) Тогда
			Оригинал = МаппингДеанонимизации.Получить(ВРег(Кандидат));
			Если Оригинал <> Неопределено Тогда
				Результат = Результат + СтрЗаменить(Оригинал, """", """""");
			Иначе
				Результат = Результат + Кандидат;
			КонецЕсли;
		Иначе
			Результат = Результат + Кандидат;
		КонецЕсли;

		Позиция = ПозЗакр + 1;
	КонецЦикла;

	Возврат Результат;

КонецФункции

&НаКлиенте
Функция АнонимизаторДеТокенизироватьСтрокуДляЗапроса(Текст)
	// Детокенизация текста 1С запроса с контекстным экранированием кавычек.
	// State-machine отслеживает текущий режим кавычек:
	//   0 = снаружи строкового литерала
	//   1 = внутри "..." (двойные кавычки)
	//   2 = внутри '...' (одинарные кавычки)
	// При нахождении токена — экранирование выбирается по текущему состоянию.

	АнонимизаторИнициализировать();
	Если МаппингДеанонимизации.Количество() = 0 Тогда Возврат Текст; КонецЕсли;

	Результат         = "";
	Позиция           = 1;
	Длина             = СтрДлина(Текст);
	СостояниеКавычек  = 0; // 0=снаружи, 1=двойные, 2=одинарные

	Пока Позиция <= Длина Цикл
		Символ = Сред(Текст, Позиция, 1);

		// Обновляем состояние кавычек
		Если СостояниеКавычек = 0 Тогда
			Если Символ = """" Тогда
				СостояниеКавычек = 1;
				Результат = Результат + Символ;
				Позиция = Позиция + 1;
				Продолжить;
			ИначеЕсли Символ = "'" Тогда
				СостояниеКавычек = 2;
				Результат = Результат + Символ;
				Позиция = Позиция + 1;
				Продолжить;
			КонецЕсли;
		ИначеЕсли СостояниеКавычек = 1 Тогда // внутри "..."
			Если Символ = """" Тогда
				Если Позиция < Длина И Сред(Текст, Позиция + 1, 1) = """" Тогда
					// "" — экранированная кавычка, оставляем как есть
					Результат = Результат + """""";
					Позиция = Позиция + 2;
					Продолжить;
				Иначе
					СостояниеКавычек = 0; // закрывающая кавычка
					Результат = Результат + Символ;
					Позиция = Позиция + 1;
					Продолжить;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли СостояниеКавычек = 2 Тогда // внутри '...'
			Если Символ = "'" Тогда
				Если Позиция < Длина И Сред(Текст, Позиция + 1, 1) = "'" Тогда
					// '' — экранированная кавычка, оставляем как есть
					Результат = Результат + "''";
					Позиция = Позиция + 2;
					Продолжить;
				Иначе
					СостояниеКавычек = 0; // закрывающая кавычка
					Результат = Результат + Символ;
					Позиция = Позиция + 1;
					Продолжить;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		// Проверяем начало токена
		Если Символ = "[" Тогда
			ПозЗакр = Найти(Сред(Текст, Позиция), "]");
			Если ПозЗакр > 0 Тогда
				ПозЗакр = Позиция + ПозЗакр - 1;
				Кандидат = Сред(Текст, Позиция, ПозЗакр - Позиция + 1);
				Если ЭтоВалидныйТокенФормат(Кандидат) Тогда
					Оригинал = МаппингДеанонимизации.Получить(ВРег(Кандидат));
					Если Оригинал <> Неопределено Тогда
						Если СостояниеКавычек = 1 Тогда
							Результат = Результат + СтрЗаменить(Оригинал, """", """""");
						ИначеЕсли СостояниеКавычек = 2 Тогда
							Результат = Результат + СтрЗаменить(Оригинал, "'", "''");
						Иначе
							Результат = Результат + Оригинал;
						КонецЕсли;
						Позиция = ПозЗакр + 1;
						Продолжить;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		Результат = Результат + Символ;
		Позиция = Позиция + 1;
	КонецЦикла;

	Возврат Результат;
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ — АВТО-ОПРЕДЕЛЕНИЕ КАТЕГОРИИ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция ОпределитьКатегориюТокена(ПолноеИмяПоля)

	Сегменты = РазложитьПутьНаСегменты(ПолноеИмяПоля);
	ИмяПоля  = ВРег(Сегменты[Сегменты.ВГраница()]);

	// По имени поля
	Если ИмяПоля = "ИНН"             Тогда Возврат "INN";       КонецЕсли;
	Если ИмяПоля = "КПП"             Тогда Возврат "KPP";       КонецЕсли;
	Если ИмяПоля = "ОГРН" Или ИмяПоля = "ОГРНИП"  Тогда Возврат "OGRN"; КонецЕсли;
	Если ИмяПоля = "СНИЛС"           Тогда Возврат "SNILS";     КонецЕсли;
	Если ИмяПоля = "БИК"             Тогда Возврат "BIK";       КонецЕсли;
	Если ИмяПоля = "СЧЕТ"            Тогда Возврат "ACC";       КонецЕсли;
	Если ИмяПоля = "АВТОР"           Или ИмяПоля = "ОТВЕТСТВЕННЫЙ" Или ИмяПоля = "ИСПОЛНИТЕЛЬ"
	  Или ИмяПоля = "ИЗМЕНИЛ"        Или ИмяПоля = "РЕДАКТОР"      Или ИмяПоля = "РЕДАКТИРУЕТ"
	  Или ИмяПоля = "ФАМИЛИЯ"        Или ИмяПоля = "ОТЧЕСТВО"
	  Или ИмяПоля = "ФИЗИЧЕСКОЕЛИЦО" Или ИмяПоля = "ФИЗЛИЦО"       Или ИмяПоля = "ФИО"           Тогда Возврат "PER"; КонецЕсли;
	Если ИмяПоля = "СТРАНА"          Или ИмяПоля = "ГОРОД"         Или ИмяПоля = "УЛИЦА"
	  Или ИмяПоля = "ДОМ"            Или ИмяПоля = "КВАРТИРА"                                     Тогда Возврат "LOC"; КонецЕсли;
	Если Найти(ИмяПоля, "ТЕЛЕФОН")     > 0 Или Найти(ИмяПоля, "PHONE")      > 0 Тогда Возврат "PHONE";    КонецЕсли;
	Если Найти(ИмяПоля, "EMAIL")       > 0 Или Найти(ИмяПоля, "ПОЧТА")      > 0 Тогда Возврат "EMAIL";    КонецЕсли;
	Если Найти(ИмяПоля, "ПАСПОРТ")     > 0 Или Найти(ИмяПоля, "PASSPORT")   > 0 Тогда Возврат "PASSPORT"; КонецЕсли;
	Если Найти(ИмяПоля, "ДАТА")        > 0 Или Найти(ИмяПоля, "DATE")       > 0 Тогда Возврат "DATE";     КонецЕсли;
	Если Найти(ИмяПоля, "АДРЕС")       > 0 Или Найти(ИмяПоля, "ADDRESS")    > 0 Тогда Возврат "LOC";      КонецЕсли;
	Если Найти(ИмяПоля, "РАСЧЕТНЫЙ")   > 0                                       Тогда Возврат "ACC";      КонецЕсли;
	Если Найти(ИмяПоля, "ОРГАНИЗАЦИЯ") > 0 Или Найти(ИмяПоля, "КОНТРАГЕНТ") > 0
	  Или Найти(ИмяПоля, "PARTNER")    > 0                                       Тогда Возврат "ORG";      КонецЕсли;
	Если Найти(ИмяПоля, "ФИЗЛИЦО")     > 0 Или Найти(ИмяПоля, "СОТРУДНИК")  > 0
	  Или Найти(ИмяПоля, "PERSON")     > 0                                       Тогда Возврат "PER";      КонецЕсли;

	// По имени объекта (второй сегмент)
	Если Сегменты.Количество() >= 2 Тогда
		ИмяОбъекта = ВРег(Сегменты[1]);
		Если Найти(ИмяОбъекта, "ФИЗЛИЦ")     > 0
		  Или Найти(ИмяОбъекта, "СОТРУДНИК") > 0
		  Или Найти(ИмяОбъекта, "PERSON")    > 0 Тогда
			Возврат "PER";
		КонецЕсли;
		Если Найти(ИмяОбъекта, "КОНТРАГЕНТ")  > 0
		  Или Найти(ИмяОбъекта, "ОРГАНИЗАЦИЯ") > 0
		  Или Найти(ИмяОбъекта, "PARTNER")    > 0 Тогда
			Возврат "ORG";
		КонецЕсли;
	КонецЕсли;

	Возврат "STR";

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция РазложитьПутьНаСегменты(Путь)

	Сегменты = Новый Массив;
	ОстатокПути = Путь;
	Пока Истина Цикл
		Поз = Найти(ОстатокПути, ".");
		Если Поз = 0 Тогда
			Сегменты.Добавить(ОстатокПути);
			Прервать;
		КонецЕсли;
		Сегменты.Добавить(Лев(ОстатокПути, Поз - 1));
		ОстатокПути = Сред(ОстатокПути, Поз + 1);
	КонецЦикла;
	Возврат Сегменты;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ — СЛОВАРНЫЙ ДВИЖОК
// ─────────────────────────────────────────────────────────────────────────────────

&НаСервереБезКонтекста
Функция ПостроитьСловарьАнонимизацииНаСервере()

	Результат = Новый Массив;
	Лог       = Новый Массив;

	Попытка
		НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v2");
		Если ТипЗнч(НаборПравил) <> Тип("Структура") Или НЕ НаборПравил.Свойство("Правила") Тогда
			НаборПравил = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ExecuteQueryFieldRules.v1");
		КонецЕсли;
		Если ТипЗнч(НаборПравил) <> Тип("Структура") Или НЕ НаборПравил.Свойство("Правила") Тогда
			Лог.Добавить("Словарь: правила не найдены в хранилище");
			Возврат Новый Структура("Данные, Лог", Результат, Лог);
		КонецЕсли;
	Исключение
		Лог.Добавить("Словарь: ошибка загрузки правил — " + ОписаниеОшибки());
		Возврат Новый Структура("Данные, Лог", Результат, Лог);
	КонецПопытки;

	Для Каждого СтрокаПравила Из НаборПравил.Правила Цикл

		ИспользоватьДляСловаря = Ложь;
		Попытка
			ИспользоватьДляСловаря = СтрокаПравила.use_for_dictionary;
		Исключение КонецПопытки;
		Если НЕ ИспользоватьДляСловаря Тогда
			Продолжить;
		КонецЕсли;

		ПутьИсточника = "";
		КатегорияПравила = "";
		Попытка
			ПутьИсточника = СтрокаПравила.source_path;
		Исключение КонецПопытки;
		Попытка
			КатегорияПравила = СтрокаПравила.token_category;
		Исключение КонецПопытки;
		Если КатегорияПравила = Неопределено Тогда КатегорияПравила = ""; КонецЕсли;

		Если ПустаяСтрока(ПутьИсточника) Тогда
			Лог.Добавить("Словарь: правило пропущено — пустой source_path");
			Продолжить;
		КонецЕсли;

		Сегменты = РазложитьПутьНаСегменты(ПутьИсточника);
		Если Сегменты.Количество() <> 3 Тогда
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: пропущено — ожидается 3 сегмента, найдено " + Сегменты.Количество());
			Продолжить;
		КонецЕсли;

		ИмяОбъекта = Сегменты[0] + "." + Сегменты[1];
		ИмяПоля    = Сегменты[2];

		ОбъектМД = Неопределено;
		Попытка
			ОбъектМД = Метаданные.НайтиПоПолномуИмени(ИмяОбъекта);
		Исключение КонецПопытки;
		Если ОбъектМД = Неопределено Тогда
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: пропущено — объект метаданных '" + ИмяОбъекта + "' не найден");
			Продолжить;
		КонецЕсли;

		РеквизитМД = НайтиРеквизитВКоллекциях(ОбъектМД, ИмяПоля);
		Если РеквизитМД = Неопределено Тогда
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: пропущено — реквизит '" + ИмяПоля + "' не найден в метаданных");
			Продолжить;
		КонецЕсли;

		ЕстьСтрока = Ложь;
		Попытка
			Для Каждого ТипПоля Из РеквизитМД.Тип.Типы() Цикл
				Если ТипПоля = Тип("Строка") Тогда
					ЕстьСтрока = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		Исключение КонецПопытки;
		Если НЕ ЕстьСтрока Тогда
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: пропущено — поле не является строковым");
			Продолжить;
		КонецЕсли;

		КатегорияТокена = КатегорияПравила;
		Если ПустаяСтрока(КатегорияТокена) Тогда
			КатегорияТокена = ОпределитьКатегориюТокена(ПутьИсточника);
		КонецЕсли;

		КоличествоДоЗапроса = Результат.Количество();
		Попытка
			ЗапросТекст =
				"ВЫБРАТЬ ПЕРВЫЕ 100000 РАЗЛИЧНЫЕ
				|	ВЫРАЗИТЬ(" + ИмяПоля + " КАК Строка(1000)) КАК Значение
				|ИЗ
				|	" + ИмяОбъекта + "
				|ГДЕ
				|	ВЫРАЗИТЬ(" + ИмяПоля + " КАК Строка(1000)) <> """"";
			ЗапросОбъект = Новый Запрос(ЗапросТекст);
			ВыборкаЗапроса = ЗапросОбъект.Выполнить().Выбрать();
			Пока ВыборкаЗапроса.Следующий() Цикл
				ЗначениеОриг = СокрЛП(ВыборкаЗапроса.Значение);
				Если ТипЗнч(ЗначениеОриг) <> Тип("Строка") Тогда
					Продолжить;
				КонецЕсли;
				Если СтрДлина(ЗначениеОриг) < 3 Тогда
					Продолжить;
				КонецЕсли;
				Запись = Новый Структура;
				Запись.Вставить("ЗначениеОриг",   ЗначениеОриг);
				Запись.Вставить("ЗначениеНижний", НРег(ЗначениеОриг));
				Запись.Вставить("Категория",      КатегорияТокена);
				Запись.Вставить("Длина",          СтрДлина(ЗначениеОриг));
				Результат.Добавить(Запись);
			КонецЦикла;
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: добавлено " + (Результат.Количество() - КоличествоДоЗапроса) + " значений");
		Исключение
			Лог.Добавить("Словарь [" + ПутьИсточника + "]: ошибка запроса — " + ОписаниеОшибки());
		КонецПопытки;

	КонецЦикла;

	// Сортировка по убыванию длины (сервер — ТаблицаЗначений доступна)
	ТаблицаВрем = Новый ТаблицаЗначений;
	ТаблицаВрем.Колонки.Добавить("Длина",          Новый ОписаниеТипов("Число"));
	ТаблицаВрем.Колонки.Добавить("ЗначениеОриг",   Новый ОписаниеТипов("Строка"));
	ТаблицаВрем.Колонки.Добавить("ЗначениеНижний", Новый ОписаниеТипов("Строка"));
	ТаблицаВрем.Колонки.Добавить("Категория",      Новый ОписаниеТипов("Строка"));
	Для Каждого Запись Из Результат Цикл
		Стр = ТаблицаВрем.Добавить();
		Стр.Длина          = Запись.Длина;
		Стр.ЗначениеОриг   = Запись.ЗначениеОриг;
		Стр.ЗначениеНижний = Запись.ЗначениеНижний;
		Стр.Категория      = Запись.Категория;
	КонецЦикла;
	ТаблицаВрем.Сортировать("Длина Убыв");
	Результат = Новый Массив;
	Для Каждого Стр Из ТаблицаВрем Цикл
		Запись = Новый Структура;
		Запись.Вставить("ЗначениеОриг",   Стр.ЗначениеОриг);
		Запись.Вставить("ЗначениеНижний", Стр.ЗначениеНижний);
		Запись.Вставить("Категория",      Стр.Категория);
		Запись.Вставить("Длина",          Стр.Длина);
		Результат.Добавить(Запись);
	КонецЦикла;

	Возврат Новый Структура("Данные, Лог", Результат, Лог);

КонецФункции

&НаКлиенте
Процедура ПостроитьИЗаполнитьСловарьАнонимизации()

	РезСловаря = ПостроитьСловарьАнонимизацииНаСервере();
	Для Каждого СтрЛог Из РезСловаря.Лог Цикл
		ДобавитьВЛог(СтрЛог);
	КонецЦикла;
	ЗаполнитьСловарьАнонимизации(РезСловаря.Данные);
	Элементы.НадписьКоличествоЗаписейСловаря.Заголовок = "Записей в словаре:  " + Формат(СловарьАнонимизации.Количество(), "ЧРГ= ");

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьСловарьАнонимизации(ДанныеСловаря)

	АнонимизаторИнициализировать();

	// Данные уже отсортированы сервером по убыванию длины
	СловарьАнонимизации = Новый Массив;
	БакетИндексСловаря  = Новый Соответствие;

	Для Каждого Запись Из ДанныеСловаря Цикл
		СловарьАнонимизации.Добавить(Запись);

		Если Запись.Длина >= 3 Тогда
			Бакет = Лев(Запись.ЗначениеНижний, 3);
			МассивЗаписей = БакетИндексСловаря.Получить(Бакет);
			Если МассивЗаписей = Неопределено Тогда
				МассивЗаписей = Новый Массив;
				БакетИндексСловаря.Вставить(Бакет, МассивЗаписей);
			КонецЕсли;
			МассивЗаписей.Добавить(Запись);
		КонецЕсли;
	КонецЦикла;

	СловарьПостроен = Истина;

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоБуквоЦифра(Символ)

	КодСим = КодСимвола(Символ);
	Если КодСим >= 48  И КодСим <= 57  Тогда Возврат Истина; КонецЕсли; // 0-9
	Если КодСим >= 65  И КодСим <= 90  Тогда Возврат Истина; КонецЕсли; // A-Z
	Если КодСим >= 97  И КодСим <= 122 Тогда Возврат Истина; КонецЕсли; // a-z
	Если КодСим >= 1040 И КодСим <= 1103 Тогда Возврат Истина; КонецЕсли; // А-я
	Если КодСим = 1025 Или КодСим = 1105 Тогда Возврат Истина; КонецЕсли; // Ё, ё
	Возврат Ложь;

КонецФункции

&НаКлиенте
Функция ПрименитьСловарьКСтроке(Текст)

	АнонимизаторИнициализировать();

	Если НЕ СловарьПостроен Или СловарьАнонимизации.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;

	Длина = СтрДлина(Текст);
	Если Длина = 0 Тогда
		Возврат Текст;
	КонецЕсли;

	// Собираем диапазоны уже существующих токенов для защиты
	ДиапазоныТокенов = Новый Массив;
	ПоискПоз = 1;
	Пока ПоискПоз <= Длина Цикл
		ПозОткр = Найти(Сред(Текст, ПоискПоз), "[");
		Если ПозОткр = 0 Тогда Прервать; КонецЕсли;
		ПозОткр = ПоискПоз + ПозОткр - 1;
		ПозЗакр = Найти(Сред(Текст, ПозОткр), "]");
		Если ПозЗакр = 0 Тогда Прервать; КонецЕсли;
		ПозЗакр = ПозОткр + ПозЗакр - 1;
		Кандидат = Сред(Текст, ПозОткр, ПозЗакр - ПозОткр + 1);
		Если ЭтоВалидныйТокенФормат(Кандидат) Тогда
			ДиапазоныТокенов.Добавить(Новый Структура("Начало, Конец", ПозОткр, ПозЗакр));
		КонецЕсли;
		ПоискПоз = ПозЗакр + 1;
	КонецЦикла;

	ТекстНижний = НРег(Текст);

	// Формируем список замен
	Замены       = Новый Массив;
	Позиция      = 1;
	ИндексТокена = 0;

	Пока Позиция <= Длина Цикл

		// Проверка: внутри существующего токена?
		ВнутриТокена = Ложь;
		КонецТокена  = 0;
		Для Инд = ИндексТокена По ДиапазоныТокенов.ВГраница() Цикл
			Диапазон = ДиапазоныТокенов[Инд];
			Если Позиция >= Диапазон.Начало И Позиция <= Диапазон.Конец Тогда
				ВнутриТокена = Истина;
				КонецТокена  = Диапазон.Конец;
				ИндексТокена = Инд;
				Прервать;
			КонецЕсли;
			Если Позиция < Диапазон.Начало Тогда Прервать; КонецЕсли;
		КонецЦикла;

		Если ВнутриТокена Тогда
			Позиция = КонецТокена + 1;
			Продолжить;
		КонецЕсли;

		Если Позиция + 2 > Длина Тогда
			Прервать;
		КонецЕсли;

		Бакет    = Сред(ТекстНижний, Позиция, 3);
		Кандидаты = БакетИндексСловаря.Получить(Бакет);
		Если Кандидаты = Неопределено Тогда
			Позиция = Позиция + 1;
			Продолжить;
		КонецЕсли;

		НашлиЗамену = Ложь;
		Для Каждого Кандидат Из Кандидаты Цикл
			ДлинаКандидата = Кандидат.Длина;
			Если ДлинаКандидата < 3 Тогда Продолжить; КонецЕсли;
			Если Позиция + ДлинаКандидата - 1 > Длина Тогда Продолжить; КонецЕсли;
			Если Сред(ТекстНижний, Позиция, ДлинаКандидата) <> Кандидат.ЗначениеНижний Тогда
				Продолжить;
			КонецЕсли;

			ГраницаОК = Истина;
			Если Позиция > 1 Тогда
				Если ЭтоБуквоЦифра(Сред(Текст, Позиция - 1, 1)) Тогда
					ГраницаОК = Ложь;
				КонецЕсли;
			КонецЕсли;
			Если ГраницаОК И Позиция + ДлинаКандидата <= Длина Тогда
				Если ЭтоБуквоЦифра(Сред(Текст, Позиция + ДлинаКандидата, 1)) Тогда
					ГраницаОК = Ложь;
				КонецЕсли;
			КонецЕсли;
			Если НЕ ГраницаОК Тогда Продолжить; КонецЕсли;

			Токен   = АнонимизаторТокенизировать(Кандидат.ЗначениеОриг, Кандидат.Категория);
			Замены.Добавить(Новый Структура("Начало, Длина, Токен", Позиция, ДлинаКандидата, Токен));
			Позиция = Позиция + ДлинаКандидата;
			НашлиЗамену = Истина;
			Прервать;
		КонецЦикла;

		Если НЕ НашлиЗамену Тогда
			Позиция = Позиция + 1;
		КонецЕсли;

	КонецЦикла;

	Если Замены.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;

	РезультатТекст  = "";
	ТекущаяПозиция  = 1;
	Для Каждого Замена Из Замены Цикл
		Если Замена.Начало > ТекущаяПозиция Тогда
			РезультатТекст = РезультатТекст + Сред(Текст, ТекущаяПозиция, Замена.Начало - ТекущаяПозиция);
		КонецЕсли;
		РезультатТекст = РезультатТекст + Замена.Токен;
		ТекущаяПозиция = Замена.Начало + Замена.Длина;
	КонецЦикла;
	Если ТекущаяПозиция <= Длина Тогда
		РезультатТекст = РезультатТекст + Сред(Текст, ТекущаяПозиция);
	КонецЕсли;

	Возврат РезультатТекст;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ — ДВИЖОК ОБРАБОТКИ РЕЗУЛЬТАТОВ И ДЕ-АНОНИМИЗАЦИЯ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция АнонимизироватьЗначениеВстроенный(Значение, МассивSources, ПравилаАнонимизации)

	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат Значение;
	КонецЕсли;

	// Ищем явную категорию в правилах
	Категория = "";
	Для Каждого SourcePath Из МассивSources Цикл
		Правило = ПравилаАнонимизации.Получить(SourcePath);
		Если Правило = Неопределено Тогда Продолжить; КонецЕсли;
		КатегорияПравила = "";
		Попытка
			Правило.Свойство("token_category", КатегорияПравила);
		Исключение КонецПопытки;
		Если НЕ ПустаяСтрока(КатегорияПравила) Тогда
			Категория = КатегорияПравила;
			Прервать;
		КонецЕсли;
	КонецЦикла;

	// Авто-определение если явная категория не задана
	Если ПустаяСтрока(Категория) Тогда
		НеSTR = "";
		Для Каждого SourcePath Из МассивSources Цикл
			АвтоКат = ОпределитьКатегориюТокена(SourcePath);
			Если АвтоКат <> "STR" Тогда
				НеSTR = АвтоКат;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Категория = ?(НЕ ПустаяСтрока(НеSTR), НеSTR, "STR");
	КонецЕсли;

	Возврат АнонимизаторТокенизировать(Значение, Категория);

КонецФункции

&НаКлиенте
Функция ЭтоБухгалтерскийСчет(Строка)
	Если ПустаяСтрока(Строка) Тогда Возврат Ложь; КонецЕсли;
	// Inline-разбор без серверного РазделитьСтрокуПоРазделителю
	ТекущийСегмент = "";
	НомерСегмента  = 0;
	Для СимИнд = 1 По СтрДлина(Строка) + 1 Цикл
		Если СимИнд > СтрДлина(Строка) Тогда
			Символ = ".";  // виртуальная точка для финального сегмента
		Иначе
			Символ = Сред(Строка, СимИнд, 1);
		КонецЕсли;
		Если Символ = "." Тогда
			Если СтрДлина(ТекущийСегмент) = 0 Тогда Возврат Ложь; КонецЕсли;
			МаксДлина = ?(НомерСегмента = 0, 2, 3);
			Если СтрДлина(ТекущийСегмент) > МаксДлина Тогда Возврат Ложь; КонецЕсли;
			НомерСегмента = НомерСегмента + 1;
			Если НомерСегмента > 4 Тогда Возврат Ложь; КонецЕсли;
			ТекущийСегмент = "";
		ИначеЕсли Найти("0123456789", Символ) = 0 Тогда
			Возврат Ложь;
		Иначе
			ТекущийСегмент = ТекущийСегмент + Символ;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ЭтоВалюта(Строка)
	Если ПустаяСтрока(Строка) Тогда Возврат Ложь; КонецЕсли;
	ВерхРегистр = ВРег(СокрЛП(Строка));
	ВалютыISO = "RUB,USD,EUR,GBP,CNY,JPY,CHF,KZT,BYN,BYR,UAH,TRY,AED,INR,CAD,AUD,"
		+ "SEK,NOK,DKK,PLN,CZK,HUF,SGD,HKD,NZD,MXN,ZAR,KGS,UZS,AMD,AZN,GEL,MDL,TJS,РУБ.";
	Если Найти("," + ВалютыISO + ",", "," + ВерхРегистр + ",") > 0 Тогда Возврат Истина; КонецЕсли;
	НижРегистр = НРег(СокрЛП(Строка));
	ВалютыРус = "российский рубль,доллар сша,евро,фунт стерлингов,китайский юань,"
		+ "японская иена,швейцарский франк,казахстанский тенге,белорусский рубль,"
		+ "украинская гривна,рубль,доллар,юань,иена,тенге,гривна";
	Если Найти("," + ВалютыРус + ",", "," + НижРегистр + ",") > 0 Тогда Возврат Истина; КонецЕсли;
	Возврат Ложь;
КонецФункции

&НаКлиенте
Функция ЭтоUUID(Строка)
	Если СтрДлина(Строка) <> 36 Тогда Возврат Ложь; КонецЕсли;
	Для СимИнд = 1 По 36 Цикл
		Символ = Сред(Строка, СимИнд, 1);
		Если СимИнд = 9 Или СимИнд = 14 Или СимИнд = 19 Или СимИнд = 24 Тогда
			Если Символ <> "-" Тогда Возврат Ложь; КонецЕсли;
		ИначеЕсли Найти("0123456789abcdefABCDEF", Символ) = 0 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ЭтоИсключениеАгрессивнойАнонимизации(Строка)
	Возврат ЭтоБухгалтерскийСчет(Строка) Или ЭтоВалюта(Строка) Или ЭтоUUID(Строка);
КонецФункции

&НаКлиенте
Функция ЭтоИзвестныйТокенАнонимизации(Строка)
	Если НЕ ЭтоВалидныйТокенФормат(Строка) Тогда
		Возврат Ложь;
	КонецЕсли;
	АнонимизаторИнициализировать();
	Возврат МаппингДеанонимизации.Получить(ВРег(Строка)) <> Неопределено;
КонецФункции

&НаКлиенте
Процедура АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст)
	Если Результат = Неопределено Тогда Возврат; КонецЕсли;
	РезультатУспех = Ложь;
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Результат.Свойство("success", РезультатУспех);
	КонецЕсли;
	Если НЕ РезультатУспех Тогда Возврат; КонецЕсли;
	НастройкиАнонимизации = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	АгрессивныйРежим = НастройкиАнонимизации.АгрессивныйРежим;
	Данные = Неопределено;
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data", Данные) Тогда
		НовыеДанные = АнонимизироватьExecuteCodeЗначение(Данные, 0, АгрессивныйРежим);
		Результат.Вставить("data", НовыеДанные);
	КонецЕсли;
	СобратьТекстыДляRegexExecuteCode(Результат, РегексКонтекст);
КонецПроцедуры

&НаКлиенте
Функция АнонимизироватьExecuteCodeЗначение(Значение, Глубина = 0, АгрессивныйРежим = Ложь, ВнутриКонтейнера = Ложь)
	Если Глубина > 10 Или Значение = Неопределено Тогда Возврат Значение; КонецЕсли;
	ТипЗначения = ТипЗнч(Значение);

	Если ТипЗначения = Тип("Строка") Тогда
		АнонимизаторИнициализировать();
		СуществующийТокен = МаппингАнонимизации.Получить(Значение);
		Если СуществующийТокен <> Неопределено Тогда Возврат СуществующийТокен; КонецЕсли;
		// Защита от повторной токенизации: только реально существующие токены текущего сеанса
		Если ЭтоИзвестныйТокенАнонимизации(Значение) Тогда Возврат Значение; КонецЕсли;
		// Словарь идёт первым: сохраняет точные категории (PER, ORG, ...)
		РезультатСловаря = ПрименитьСловарьКСтроке(Значение);
		// Агрессивный режим — только если строка является полем структуры или элементом массива
		Если АгрессивныйРежим И ВнутриКонтейнера
			И НЕ ПустаяСтрока(Значение)
			И НЕ ЭтоИсключениеАгрессивнойАнонимизации(Значение) Тогда
			// Словарь полностью токенизировал строку — возвращаем точный токен
			Если ЭтоВалидныйТокенФормат(РезультатСловаря) Тогда Возврат РезультатСловаря; КонецЕсли;
			// Частичная или нулевая замена — токенизируем по оригиналу (не РезультатСловаря),
			// чтобы МаппингДеанонимизации хранил исходную строку без вложенных токенов
			Возврат АнонимизаторТокенизировать(Значение, "STR");
		КонецЕсли;
		Возврат РезультатСловаря;

	ИначеЕсли ТипЗначения = Тип("Структура") Тогда
		ЭтоОбъектРеф = Ложь;
		Значение.Свойство("_objectRef", ЭтоОбъектРеф);
		НоваяСтруктура = Новый Структура;
		Для Каждого КВП Из Значение Цикл
			// Если это trusted-ссылка, технические поля копируем без анонимизации
			Если ЭтоОбъектРеф = Истина
				И (КВП.Ключ = "_objectRef"
					Или КВП.Ключ = "УникальныйИдентификатор"
					Или КВП.Ключ = "ТипОбъекта") Тогда
				НоваяСтруктура.Вставить(КВП.Ключ, КВП.Значение);
				Продолжить;
			КонецЕсли;
			// Все остальные поля (включая Представление и любые user-поля) — рекурсивно
			НоваяСтруктура.Вставить(КВП.Ключ,
				АнонимизироватьExecuteCodeЗначение(КВП.Значение, Глубина + 1, АгрессивныйРежим, Истина));
		КонецЦикла;
		Возврат НоваяСтруктура;

	ИначеЕсли ТипЗначения = Тип("Массив") Тогда
		НовыйМассив = Новый Массив;
		Для Каждого Элемент Из Значение Цикл
			НовыйМассив.Добавить(АнонимизироватьExecuteCodeЗначение(Элемент, Глубина + 1, АгрессивныйРежим, Истина));
		КонецЦикла;
		Возврат НовыйМассив;
	КонецЕсли;

	Возврат Значение;
КонецФункции

&НаКлиенте
Процедура СобратьТекстыДляRegexExecuteCode(Результат, РегексКонтекст)
	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") И Правило.enabled = Истина Тогда
			ВключённыеПравила.Добавить(Новый Структура("pattern, category",
				Правило.pattern, Правило.category));
		КонецЕсли;
	КонецЦикла;
	Если ВключённыеПравила.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь; Возврат;
	КонецЕсли;
	МассивПравилJSON = Новый Массив;
	Для Каждого П Из ВключённыеПравила Цикл
		МассивПравилJSON.Добавить(Новый Структура("pattern, category", П.pattern, П.category));
	КонецЦикла;
	РегексКонтекст.ВключённыеПравилаJSON = ЗаписатьJSON2(МассивПравилJSON);
	ТекстыМассив = Новый Массив;
	МаппингПутей = Новый Массив;
	Данные = Неопределено;
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data", Данные) Тогда
		НачальныйПуть = Новый Массив;
		НачальныйПуть.Добавить("data");
		СобратьСтрокиExecuteCodeРекурсивно(Данные, НачальныйПуть,
			ТекстыМассив, МаппингПутей, 0);
	КонецЕсли;
	Если ТекстыМассив.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь; Возврат;
	КонецЕсли;
	РегексКонтекст.ТекстыJSON         = ЗаписатьJSON2(ТекстыМассив);
	РегексКонтекст.МаппингПутей       = МаппингПутей;
	РегексКонтекст.ЕстьТекстыДляRegex = Истина;
КонецПроцедуры

&НаКлиенте
Процедура СобратьСтрокиExecuteCodeРекурсивно(Значение, ТекущийПуть,
	ТекстыМассив, МаппингПутей, Глубина)
	Если Глубина > 10 Или Значение = Неопределено Тогда Возврат; КонецЕсли;
	ТипЗначения = ТипЗнч(Значение);
	Если ТипЗначения = Тип("Строка") Тогда
		// Не отправлять реальные токены текущего сеанса в regex — широкие правила могут их сломать
		Если НЕ ЭтоИзвестныйТокенАнонимизации(Значение) Тогда
			ТекстыМассив.Добавить(Значение);
			КопияПути = Новый Массив;
			Для Каждого Эл Из ТекущийПуть Цикл КопияПути.Добавить(Эл); КонецЦикла;
			МаппингПутей.Добавить(КопияПути);
		КонецЕсли;
	ИначеЕсли ТипЗначения = Тип("Структура") Тогда
		ЭтоОбъектРеф = Ложь;
		Значение.Свойство("_objectRef", ЭтоОбъектРеф);
		Для Каждого КВП Из Значение Цикл
			// Технические поля trusted-ссылки не собираем
			Если ЭтоОбъектРеф = Истина
				И (КВП.Ключ = "_objectRef"
					Или КВП.Ключ = "УникальныйИдентификатор"
					Или КВП.Ключ = "ТипОбъекта") Тогда
				Продолжить;
			КонецЕсли;
			НовыйПуть = Новый Массив;
			Для Каждого Эл Из ТекущийПуть Цикл НовыйПуть.Добавить(Эл); КонецЦикла;
			НовыйПуть.Добавить(КВП.Ключ);
			СобратьСтрокиExecuteCodeРекурсивно(КВП.Значение, НовыйПуть,
				ТекстыМассив, МаппингПутей, Глубина + 1);
		КонецЦикла;
	ИначеЕсли ТипЗначения = Тип("Массив") Тогда
		Для Инд = 0 По Значение.Количество() - 1 Цикл
			НовыйПуть = Новый Массив;
			Для Каждого Эл Из ТекущийПуть Цикл НовыйПуть.Добавить(Эл); КонецЦикла;
			НовыйПуть.Добавить(Инд);
			СобратьСтрокиExecuteCodeРекурсивно(Значение[Инд], НовыйПуть,
				ТекстыМассив, МаппингПутей, Глубина + 1);
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьRegexТокеныExecuteCode(РегексКонтекст, СовпаденияJSON)
	Если НЕ ЗначениеЗаполнено(СовпаденияJSON) Тогда Возврат; КонецЕсли;
	РезСовпадений = Неопределено;
	Попытка РезСовпадений = ПрочитатьJSON2(СовпаденияJSON); Исключение Возврат; КонецПопытки;
	Если ТипЗнч(РезСовпадений) <> Тип("Массив") Тогда Возврат; КонецЕсли;
	МаппингПутей    = РегексКонтекст.МаппингПутей;
	КореньРезультат = РегексКонтекст.Результат;
	Для ТекстИнд = 0 По РезСовпадений.Количество() - 1 Цикл
		Если ТекстИнд >= МаппингПутей.Количество() Тогда Прервать; КонецЕсли;
		ЭлСовпадений = РезСовпадений[ТекстИнд];
		Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		Путь = МаппингПутей[ТекстИнд];
		Если Путь.Количество() = 0 Тогда Продолжить; КонецЕсли;
		// Обход от корня до родительского контейнера
		Контейнер = КореньРезультат;
		ОшибкаПути = Ложь;
		Для НавИнд = 0 По Путь.Количество() - 2 Цикл
			ЭлПути = Путь[НавИнд];
			Если ТипЗнч(Контейнер) = Тип("Массив") Тогда
				Контейнер = Контейнер[ЭлПути];
			ИначеЕсли ТипЗнч(Контейнер) = Тип("Структура") Тогда
				Вложенное = Неопределено;
				Контейнер.Свойство(ЭлПути, Вложенное);
				Контейнер = Вложенное;
			Иначе
				ОшибкаПути = Истина; Прервать;
			КонецЕсли;
			Если Контейнер = Неопределено Тогда ОшибкаПути = Истина; Прервать; КонецЕсли;
		КонецЦикла;
		Если ОшибкаПути Тогда Продолжить; КонецЕсли;
		Ключ = Путь[Путь.Количество() - 1];
		Если ТипЗнч(Контейнер) = Тип("Массив") Тогда
			ТекущийТекст = Контейнер[Ключ];
		ИначеЕсли ТипЗнч(Контейнер) = Тип("Структура") Тогда
			ТекущийТекст = Неопределено;
			Контейнер.Свойство(Ключ, ТекущийТекст);
		Иначе
			Продолжить;
		КонецЕсли;
		Если ТипЗнч(ТекущийТекст) <> Тип("Строка") Тогда Продолжить; КонецЕсли;
		НовыйТекст = ТекущийТекст;
		Для Каждого С Из ЭлСовпадений Цикл
			Категория = ""; МатчТекст = ""; Начало = 0; Длина = 0;
			Если ТипЗнч(С) = Тип("Структура") Тогда
				С.Свойство("category", Категория); С.Свойство("match", МатчТекст);
				С.Свойство("start", Начало);       С.Свойство("length", Длина);
			Иначе
				Категория = С["category"]; МатчТекст = С["match"];
				Начало = С["start"];       Длина = С["length"];
			КонецЕсли;
			Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
			Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
			Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
			Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
			Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
			Токен = АнонимизаторТокенизировать(МатчТекст, Категория);
			Если Начало >= 0 И (Начало + Длина) <= СтрДлина(НовыйТекст) Тогда
				НовыйТекст = Лев(НовыйТекст, Начало) + Токен + Сред(НовыйТекст, Начало + Длина + 1);
			КонецЕсли;
		КонецЦикла;
		Если ТипЗнч(Контейнер) = Тип("Массив") Тогда
			Контейнер[Ключ] = НовыйТекст;
		Иначе
			Контейнер.Вставить(Ключ, НовыйТекст);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Функция ПрименитьRegexТокеныКСтроке(Текст, СовпаденияJSON)
	// Упрощённый аналог ПрименитьRegexТокеныExecuteCode для одиночной строки.
	// Применяет замены справа налево, чтобы смещение позиций после замены не ломало последующие индексы.
	Если НЕ ЗначениеЗаполнено(СовпаденияJSON) Тогда Возврат Текст; КонецЕсли;
	РезСовпадений = Неопределено;
	Попытка РезСовпадений = ПрочитатьJSON2(СовпаденияJSON); Исключение Возврат Текст; КонецПопытки;
	Если ТипЗнч(РезСовпадений) <> Тип("Массив") Или РезСовпадений.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;
	ЭлСовпадений = РезСовпадений[0];
	Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда
		Возврат Текст;
	КонецЕсли;
	Замены = Новый Массив;
	Для Каждого С Из ЭлСовпадений Цикл
		Категория = ""; МатчТекст = ""; Начало = 0; Длина = 0;
		Если ТипЗнч(С) = Тип("Структура") Тогда
			С.Свойство("category", Категория); С.Свойство("match", МатчТекст);
			С.Свойство("start",    Начало);    С.Свойство("length", Длина);
		Иначе
			Категория = С["category"]; МатчТекст = С["match"];
			Начало    = С["start"];    Длина      = С["length"];
		КонецЕсли;
		Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
		Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
		Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
		Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
		Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
		Замены.Добавить(Новый Структура("Начало, Длина, МатчТекст, Категория",
			Начало, Длина, МатчТекст, Категория));
	КонецЦикла;
	// Пузырьковая сортировка по убыванию Начало (правые замены сначала)
	Для Инд1 = 0 По Замены.Количество() - 2 Цикл
		Для Инд2 = 0 По Замены.Количество() - 2 - Инд1 Цикл
			Если Замены[Инд2].Начало < Замены[Инд2 + 1].Начало Тогда
				Врем             = Замены[Инд2];
				Замены[Инд2]     = Замены[Инд2 + 1];
				Замены[Инд2 + 1] = Врем;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	НовыйТекст = Текст;
	Для Каждого Замена Из Замены Цикл
		Токен = АнонимизаторТокенизировать(Замена.МатчТекст, Замена.Категория);
		// start в regex — 0-based, в 1C Сред/Лев — 1-based
		Если Замена.Начало >= 0 И (Замена.Начало + Замена.Длина) <= СтрДлина(НовыйТекст) Тогда
			НовыйТекст = Лев(НовыйТекст, Замена.Начало)
				+ Токен
				+ Сред(НовыйТекст, Замена.Начало + Замена.Длина + 1);
		КонецЕсли;
	КонецЦикла;
	Возврат НовыйТекст;
КонецФункции

&НаКлиенте
Процедура АнонимизироватьРезультатВстроенный(Результат, РегексКонтекст = Неопределено)

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;

	РезультатУспех = Ложь;
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Результат.Свойство("success", РезультатУспех);
	КонецЕсли;
	Если НЕ РезультатУспех Тогда Возврат; КонецЕсли;

	ПравилаАнонимизации = ПолучитьПравилаТочнойАнонимизацииИзХранилища();

	// Строим карту политик колонок из уже проставленных полей schema
	СоответствиеPolicy = Новый Соответствие;
	Схема = Неопределено;
	Если НЕ Результат.Свойство("schema", Схема)
		Или ТипЗнч(Схема) <> Тип("Структура") Тогда
		Возврат;
	КонецЕсли;
	Колонки = Неопределено;
	Если НЕ Схема.Свойство("columns", Колонки)
		Или ТипЗнч(Колонки) <> Тип("Массив") Тогда
		Возврат;
	КонецЕсли;
	Для Каждого Колонка Из Колонки Цикл
		Если ТипЗнч(Колонка) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
		ИмяКолонки = "";
		Если НЕ Колонка.Свойство("name", ИмяКолонки) Тогда Продолжить; КонецЕсли;
		Режим = "default";
		Колонка.Свойство("anonymization_mode", Режим);
		РежимСсылки = "default";
		Колонка.Свойство("reference_presentation_mode", РежимСсылки);
		Sources = ПолучитьМассивСтрокИзСвойства(Колонка, "sources");
		СоответствиеPolicy.Вставить(ИмяКолонки,
			Новый Структура("mode, ref_mode, sources", Режим, РежимСсылки, Sources));
	КонецЦикла;

	// Обходим строки данных
	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Массив") Тогда
		Возврат;
	КонецЕсли;

	Для Каждого Строка Из Данные Цикл
		Если ТипЗнч(Строка) <> Тип("Соответствие")
			И ТипЗнч(Строка) <> Тип("Структура") Тогда
			Продолжить;
		КонецЕсли;

		Для Каждого PolicyЭлт Из СоответствиеPolicy Цикл
			ИмяКолонки = PolicyЭлт.Ключ;
			Policy      = PolicyЭлт.Значение;

			ЗначениеКолонки = Неопределено;
			Если ТипЗнч(Строка) = Тип("Структура") Тогда
				Если НЕ Строка.Свойство(ИмяКолонки, ЗначениеКолонки) Тогда Продолжить; КонецЕсли;
			Иначе
				ЗначениеКолонки = Строка[ИмяКолонки];
			КонецЕсли;
			Если ЗначениеКолонки = Неопределено Тогда Продолжить; КонецЕсли;

			Если ТипЗнч(ЗначениеКолонки) = Тип("Структура")
				И ЗначениеКолонки.Свойство("Представление") Тогда
				// Объектное значение — анонимизируем только Представление
				// Перечисления не анонимизируются никогда (в т.ч. при смешанном типе колонки)
				// Планы счетов по умолчанию не анонимизируются, если нет явного force_anonymize
				ТипОбъектаЗначения = "";
				ЗначениеКолонки.Свойство("ТипОбъекта", ТипОбъектаЗначения);
				Если Найти(ТипОбъектаЗначения, "ПеречислениеСсылка.") = 1 Тогда
					Продолжить;
				КонецЕсли;
				Если Найти(ТипОбъектаЗначения, "ПланСчетовСсылка.") = 1
					И Policy.mode <> "force_anonymize"
					И Policy.ref_mode <> "force_anonymize" Тогда
					Продолжить;
				КонецЕсли;
				Если Policy.mode = "force_keep" Тогда
					Продолжить;
				ИначеЕсли Policy.mode = "force_anonymize" Или Policy.ref_mode = "force_anonymize" Тогда
					ЗначениеКолонки.Вставить("Представление",
						АнонимизироватьЗначениеВстроенный(ЗначениеКолонки.Представление, Policy.sources, ПравилаАнонимизации));
				ИначеЕсли Policy.ref_mode = "force_keep" Тогда
					Продолжить;
				Иначе
					ЗначениеКолонки.Вставить("Представление",
						ПрименитьСловарьКСтроке(ЗначениеКолонки.Представление));
				КонецЕсли;

			ИначеЕсли ТипЗнч(ЗначениеКолонки) = Тип("Строка") Тогда
				// Строковое значение
				НовоеЗначение = Неопределено;
				Если Policy.mode = "force_anonymize" Тогда
					НовоеЗначение = АнонимизироватьЗначениеВстроенный(ЗначениеКолонки, Policy.sources, ПравилаАнонимизации);
				ИначеЕсли Policy.mode = "default" Тогда
					НовоеЗначение = ПрименитьСловарьКСтроке(ЗначениеКолонки);
				КонецЕсли;
				Если НовоеЗначение <> Неопределено Тогда
					Если ТипЗнч(Строка) = Тип("Структура") Тогда
						Строка.Вставить(ИмяКолонки, НовоеЗначение);
					Иначе
						Строка[ИмяКолонки] = НовоеЗначение;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;

		КонецЦикла;
	КонецЦикла;

	// Фаза regex: собрать тексты после словарной анонимизации
	Если РегексКонтекст <> Неопределено Тогда
		СобратьТекстыДляRegex(Результат, РегексКонтекст);
	КонецЕсли;

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ find_references_to_object
// ─────────────────────────────────────────────────────────────────────────────────

// Анонимизирует результат find_references_to_object: обходит hits и применяет
// трёхфазную анонимизацию (точные поля → словарь → regex) к чувствительным полям.
//
// Параметры:
//  Результат - Структура - результат инструмента {success, data: {hits, ...}}
//  РегексКонтекст - Структура, Неопределено - контекст для regex-фазы
//
&НаКлиенте
Процедура АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст = Неопределено)

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;

	РезультатУспех = Ложь;
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Результат.Свойство("success", РезультатУспех);
	КонецЕсли;
	Если НЕ РезультатУспех Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Хиты = Неопределено;
	Если НЕ Данные.Свойство("hits", Хиты)
		Или ТипЗнч(Хиты) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	НастройкиАнонимизации = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	ПравилаАнонимизации = НастройкиАнонимизации.Правила;
	АгрессивныйРежим = НастройкиАнонимизации.АгрессивныйРежим;

	Для Каждого Хит Из Хиты Цикл
		Если ТипЗнч(Хит) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		РежимFound = "default";
		SourcesFoundInObject = Новый Массив;

		// 1. found_in_object.Представление (документы/справочники)
		НайденВОбъекте = Неопределено;
		Если Хит.Свойство("found_in_object", НайденВОбъекте)
			И ТипЗнч(НайденВОбъекте) = Тип("Структура") Тогда
			РезОбъекта = АнонимизироватьПредставлениеObjectRef(НайденВОбъекте, ПравилаАнонимизации, АгрессивныйРежим);
			РежимFound = РезОбъекта.Режим;
			SourcesFoundInObject = РезОбъекта.Sources;
		КонецЕсли;

		// 2. record_key.*.Представление (objectRef-измерения регистров)
		ВсеИзмеренияЗащищены = Истина;
		ЕстьСсылочноеИзмерение = Ложь;
		ЕстьИзмерениеForceAnonymize = Ложь;
		КлючЗаписи = Неопределено;
		Если Хит.Свойство("record_key", КлючЗаписи)
			И ТипЗнч(КлючЗаписи) = Тип("Структура") Тогда
			Для Каждого КВП Из КлючЗаписи Цикл
				Если ТипЗнч(КВП.Значение) = Тип("Структура") Тогда
					РезКлюча = АнонимизироватьПредставлениеObjectRef(КВП.Значение, ПравилаАнонимизации, АгрессивныйРежим);
					ЕстьСсылочноеИзмерение = Истина;
					Если РезКлюча.Режим = "force_anonymize" Тогда
						ЕстьИзмерениеForceAnonymize = Истина;
					КонецЕсли;
					Если РезКлюча.Режим <> "force_keep" Тогда
						ВсеИзмеренияЗащищены = Ложь;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		ВсеИзмеренияЗащищены = ВсеИзмеренияЗащищены И ЕстьСсылочноеИзмерение;

		// 3. note
		Примечание = Неопределено;
		Если Хит.Свойство("note", Примечание)
			И ТипЗнч(Примечание) = Тип("Строка")
			И НЕ ПустаяСтрока(Примечание) Тогда
			Если НайденВОбъекте <> Неопределено Тогда
				// Документ/справочник: note следует режиму found_in_object
				Если РежимFound = "force_anonymize" Тогда
					Хит.Вставить("note", АнонимизироватьЗначениеВстроенный(Примечание, SourcesFoundInObject, ПравилаАнонимизации));
				ИначеЕсли РежимFound <> "force_keep" Тогда
					Хит.Вставить("note", ПрименитьСловарьКСтроке(Примечание));
				Иначе
				КонецЕсли;
			ИначеЕсли НЕ ВсеИзмеренияЗащищены Тогда
				// Регистр: обрабатываем если хотя бы одно измерение не force_keep
				Если АгрессивныйРежим Или ЕстьИзмерениеForceAnonymize Тогда
					Хит.Вставить("note", АнонимизаторТокенизировать(Примечание, "STR"));
				Иначе
					Хит.Вставить("note", ПрименитьСловарьКСтроке(Примечание));
				КонецЕсли;
			Иначе
			КонецЕсли;
		КонецЕсли;

	КонецЦикла;

	// Phase 3: сбор текстов для regex-токенизации
	Если РегексКонтекст <> Неопределено Тогда
		СобратьТекстыДляRegexFindReferences(Результат, РегексКонтекст, ПравилаАнонимизации, АгрессивныйРежим);
	КонецЕсли;

КонецПроцедуры

// Анонимизирует поле Представление в одном objectRef-описании (Phase 1 или Phase 2).
// Возвращает {Режим, Sources} для обработки note вызывающим кодом.
//
// Параметры:
//  ОписаниеОбъекта  - Структура - {_objectRef, ТипОбъекта, Представление, ...}
//  ПравилаАнонимизации - Соответствие - правила точной анонимизации
//  АгрессивныйРежим - Булево - признак агрессивного режима
//
&НаКлиенте
Функция АнонимизироватьПредставлениеObjectRef(ОписаниеОбъекта, ПравилаАнонимизации, АгрессивныйРежим)

	ПустойРезультат = Новый Структура("Режим, Sources", "default", Новый Массив);
	ЗащищённыйРезультат = Новый Структура("Режим, Sources", "force_keep", Новый Массив);

	ТипОбъекта = "";
	ОписаниеОбъекта.Свойство("ТипОбъекта", ТипОбъекта);
	Если Найти(ТипОбъекта, "ПеречислениеСсылка.") = 1 Тогда Возврат ЗащищённыйРезультат; КонецЕсли;

	Представление = Неопределено;
	Если НЕ ОписаниеОбъекта.Свойство("Представление", Представление) Тогда Возврат ПустойРезультат; КонецЕсли;
	Если ТипЗнч(Представление) <> Тип("Строка") Или ПустаяСтрока(Представление) Тогда Возврат ПустойРезультат; КонецЕсли;

	Режим = ВычислитьРежимObjectRef(ТипОбъекта, ПравилаАнонимизации, АгрессивныйРежим);

	Если Режим = "force_keep" Тогда
		Возврат Новый Структура("Режим, Sources", Режим, Новый Массив);
	ИначеЕсли Режим = "force_anonymize" Тогда
		ПутьНаименования = ПолучитьПутьНаименованияПоТипуОбъекта(ТипОбъекта);
		МассивSources = Новый Массив;
		МассивSources.Добавить(?(НЕ ПустаяСтрока(ПутьНаименования), ПутьНаименования, ТипОбъекта));
		ОписаниеОбъекта.Вставить("Представление",
			АнонимизироватьЗначениеВстроенный(Представление, МассивSources, ПравилаАнонимизации));
		Возврат Новый Структура("Режим, Sources", Режим, МассивSources);
	КонецЕсли;

	// Phase 2: нет правила, не агрессивный — словарная замена
	ОписаниеОбъекта.Вставить("Представление", ПрименитьСловарьКСтроке(Представление));
	Возврат Новый Структура("Режим, Sources", "default", Новый Массив);

КонецФункции

// Собирает тексты из hits для regex-фазы анонимизации find_references_to_object.
// Аналог СобратьТекстыДляRegex / СобратьТекстыДляRegexОбъект для hits-структуры.
// Пропускает поля с force_keep (аналог execute_query line 16934).
//
// Параметры:
//  Результат           - Структура - результат инструмента
//  РегексКонтекст      - Структура - контекст: ВключённыеПравилаJSON, ТекстыJSON,
//                                    МаппингИндексов, ЕстьТекстыДляRegex
//  ПравилаАнонимизации - Соответствие - правила точной анонимизации
//  АгрессивныйРежим    - Булево - признак агрессивного режима
//
&НаКлиенте
Процедура СобратьТекстыДляRegexFindReferences(Результат, РегексКонтекст, ПравилаАнонимизации, АгрессивныйРежим)

	// Получить включённые regex-правила
	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") Тогда
			Включено = Истина;
			Попытка Правило.Свойство("enabled", Включено); Исключение КонецПопытки;
			Если Включено Тогда
				ВключённыеПравила.Добавить(Новый Структура("pattern, category", Правило.pattern, Правило.category));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Если ВключённыеПравила.Количество() = 0 Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Хиты = Неопределено;
	Если НЕ Данные.Свойство("hits", Хиты)
		Или ТипЗнч(Хиты) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	ТекстыМассив = Новый Массив;
	МаппингИндексов = Новый Массив;

	Для ХитИндекс = 0 По Хиты.Количество() - 1 Цикл
		Хит = Хиты[ХитИндекс];
		Если ТипЗнч(Хит) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		// found_in_object.Представление
		НайденВОбъектеN = Неопределено;
		РежимFoundN = "default";
		Если Хит.Свойство("found_in_object", НайденВОбъектеN)
			И ТипЗнч(НайденВОбъектеN) = Тип("Структура") Тогда
			ТипОбъекта = "";
			НайденВОбъектеN.Свойство("ТипОбъекта", ТипОбъекта);
			РежимFoundN = ВычислитьРежимObjectRef(ТипОбъекта, ПравилаАнонимизации, АгрессивныйРежим);
			Если РежимFoundN <> "force_keep" Тогда
				Представление = Неопределено;
				Если НайденВОбъектеN.Свойство("Представление", Представление)
					И ТипЗнч(Представление) = Тип("Строка")
					И НЕ ПустаяСтрока(Представление) Тогда
					ТекстыМассив.Добавить(Представление);
					МаппингИндексов.Добавить(Новый Структура("ХитИндекс, Вид", ХитИндекс, "found_in_object"));
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;

		// record_key.*.Представление
		ВсеИзмеренияЗащищеныN = Истина;
		ЕстьСсылочноеИзмерениеN = Ложь;
		ЕстьИзмерениеForceAnonymizeN = Ложь;
		КлючЗаписи = Неопределено;
		Если Хит.Свойство("record_key", КлючЗаписи)
			И ТипЗнч(КлючЗаписи) = Тип("Структура") Тогда
			Для Каждого КВП Из КлючЗаписи Цикл
				Если ТипЗнч(КВП.Значение) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
				ТипОбъектаПоля = "";
				КВП.Значение.Свойство("ТипОбъекта", ТипОбъектаПоля);
				РежимПоля = ВычислитьРежимObjectRef(ТипОбъектаПоля, ПравилаАнонимизации, АгрессивныйРежим);
				ЕстьСсылочноеИзмерениеN = Истина;
				Если РежимПоля = "force_anonymize" Тогда
					ЕстьИзмерениеForceAnonymizeN = Истина;
				КонецЕсли;
				Если РежимПоля = "force_keep" Тогда Продолжить; КонецЕсли;
				ВсеИзмеренияЗащищеныN = Ложь;
				ПредставлениеПоля = Неопределено;
				Если КВП.Значение.Свойство("Представление", ПредставлениеПоля)
					И ТипЗнч(ПредставлениеПоля) = Тип("Строка")
					И НЕ ПустаяСтрока(ПредставлениеПоля) Тогда
					ТекстыМассив.Добавить(ПредставлениеПоля);
					МаппингИндексов.Добавить(Новый Структура("ХитИндекс, Вид, ИмяПоля",
						ХитИндекс, "record_key", КВП.Ключ));
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		ВсеИзмеренияЗащищеныN = ВсеИзмеренияЗащищеныN И ЕстьСсылочноеИзмерениеN;

		// note
		Примечание = Неопределено;
		ДобавитьNote = Ложь;
		Если Хит.Свойство("note", Примечание)
			И ТипЗнч(Примечание) = Тип("Строка")
			И НЕ ПустаяСтрока(Примечание) Тогда
			Если НайденВОбъектеN <> Неопределено Тогда
				ДобавитьNote = (РежимFoundN <> "force_keep");
			Иначе
				ДобавитьNote = НЕ ВсеИзмеренияЗащищеныN И НЕ ЕстьИзмерениеForceAnonymizeN;
			КонецЕсли;
		КонецЕсли;
		Если ДобавитьNote Тогда
			ТекстыМассив.Добавить(Примечание);
			МаппингИндексов.Добавить(Новый Структура("ХитИндекс, Вид", ХитИндекс, "note"));
		КонецЕсли;

	КонецЦикла;

	Если ТекстыМассив.Количество() = 0 Тогда Возврат; КонецЕсли;

	РегексКонтекст.Вставить("ВключённыеПравилаJSON", ЗаписатьJSON2(ВключённыеПравила));
	РегексКонтекст.Вставить("ТекстыJSON",            ЗаписатьJSON2(ТекстыМассив));
	РегексКонтекст.Вставить("МаппингИндексов",       МаппингИндексов);
	РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Истина);

КонецПроцедуры

// Применяет результаты regex-фазы к полям hits find_references_to_object.
// Аналог ПрименитьRegexТокены / ПрименитьRegexТокеныОбъект для hits-структуры.
//
// Параметры:
//  РегексКонтекст  - Структура - содержит Результат, МаппингИндексов
//  СовпаденияJSON  - Строка    - JSON-массив результатов regex
//
&НаКлиенте
Процедура ПрименитьRegexТокеныFindReferences(РегексКонтекст, СовпаденияJSON)

	Если ПустаяСтрока(СовпаденияJSON) Тогда Возврат; КонецЕсли;

	МаппингИндексов = Неопределено;
	Если НЕ РегексКонтекст.Свойство("МаппингИндексов", МаппингИндексов)
		Или МаппингИндексов = Неопределено Тогда Возврат; КонецЕсли;

	Результат = Неопределено;
	Если НЕ РегексКонтекст.Свойство("Результат", Результат)
		Или ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Хиты = Неопределено;
	Если НЕ Данные.Свойство("hits", Хиты)
		Или ТипЗнч(Хиты) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	Попытка
		Совпадения = ПрочитатьJSON2(СовпаденияJSON);
	Исключение
		Возврат;
	КонецПопытки;

	Если ТипЗнч(Совпадения) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	// РезСовпадений — массив по тем же индексам, что и МаппингИндексов / ТекстыМассив.
	// Каждый элемент — массив совпадений {category, match, start, length}, отсортированных по start DESC.
	РезСовпадений = Совпадения;
	Для ТекстИндекс = 0 По МаппингИндексов.Количество() - 1 Цикл

		Если ТекстИндекс >= РезСовпадений.Количество() Тогда Прервать; КонецЕсли;

		ЭлСовпадений = РезСовпадений[ТекстИндекс];
		Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда Продолжить; КонецЕсли;

		МаппингЭлт = МаппингИндексов[ТекстИндекс];
		Если ТипЗнч(МаппингЭлт) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		ХитИндекс = 0;
		МаппингЭлт.Свойство("ХитИндекс", ХитИндекс);
		Вид = "";
		МаппингЭлт.Свойство("Вид", Вид);

		Если ХитИндекс >= Хиты.Количество() Тогда Продолжить; КонецЕсли;
		Хит = Хиты[ХитИндекс];
		Если ТипЗнч(Хит) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		// Прочитать текущий текст из хита, сохранить ссылку для обратной записи
		ТекущийТекст    = "";
		СтруктураЗаписи = Неопределено; // структура, в которую пишем "Представление"
		ПрямаяЗапись    = Ложь;         // для "note" — пишем прямо в Хит

		Если Вид = "found_in_object" Тогда
			НайденВОбъекте = Неопределено;
			Если Хит.Свойство("found_in_object", НайденВОбъекте)
				И ТипЗнч(НайденВОбъекте) = Тип("Структура") Тогда
				НайденВОбъекте.Свойство("Представление", ТекущийТекст);
				СтруктураЗаписи = НайденВОбъекте;
			КонецЕсли;

		ИначеЕсли Вид = "record_key" Тогда
			ИмяПоля = "";
			МаппингЭлт.Свойство("ИмяПоля", ИмяПоля);
			КлючЗаписи = Неопределено;
			Если Хит.Свойство("record_key", КлючЗаписи)
				И ТипЗнч(КлючЗаписи) = Тип("Структура") Тогда
				ЗначениеПоля = Неопределено;
				Если КлючЗаписи.Свойство(ИмяПоля, ЗначениеПоля)
					И ТипЗнч(ЗначениеПоля) = Тип("Структура") Тогда
					ЗначениеПоля.Свойство("Представление", ТекущийТекст);
					СтруктураЗаписи = ЗначениеПоля;
				КонецЕсли;
			КонецЕсли;

		ИначеЕсли Вид = "note" Тогда
			Хит.Свойство("note", ТекущийТекст);
			ПрямаяЗапись = Истина;

		КонецЕсли;

		Если ТипЗнч(ТекущийТекст) <> Тип("Строка") Или ПустаяСтрока(ТекущийТекст) Тогда Продолжить; КонецЕсли;

		// Применить совпадения справа налево (отсортированы DESC по start)
		НовыйТекст = ТекущийТекст;
		Для Каждого С Из ЭлСовпадений Цикл
			Категория = "";
			МатчТекст = "";
			Начало    = 0;
			Длина     = 0;
			Если ТипЗнч(С) = Тип("Структура") Тогда
				С.Свойство("category", Категория);
				С.Свойство("match",    МатчТекст);
				С.Свойство("start",    Начало);
				С.Свойство("length",   Длина);
			ИначеЕсли ТипЗнч(С) = Тип("Соответствие") Тогда
				Категория = С["category"];
				МатчТекст = С["match"];
				Начало    = С["start"];
				Длина     = С["length"];
			КонецЕсли;
			Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
			Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
			Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
			Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
			Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
			Токен = АнонимизаторТокенизировать(МатчТекст, Категория);
			Если Начало >= 0 И (Начало + Длина) <= СтрДлина(НовыйТекст) Тогда
				НовыйТекст = Лев(НовыйТекст, Начало) + Токен + Сред(НовыйТекст, Начало + Длина + 1);
			КонецЕсли;
		КонецЦикла;

		// Записать результат обратно
		Если ПрямаяЗапись Тогда
			Хит.Вставить("note", НовыйТекст);
		ИначеЕсли СтруктураЗаписи <> Неопределено Тогда
			СтруктураЗаписи.Вставить("Представление", НовыйТекст);
		КонецЕсли;

	КонецЦикла;

КонецПроцедуры

// Возвращает Истина если строка выглядит как GUID (8-4-4-4-12, UUID-формат).
// Используется чтобы не анонимизировать системные UID в поле user журнала регистрации.
//
&НаКлиенте
Функция ЭтоGUID(Знач Строка)
	Возврат СтрДлина(Строка) = 36
		И Сред(Строка,  9, 1) = "-"
		И Сред(Строка, 14, 1) = "-"
		И Сред(Строка, 19, 1) = "-"
		И Сред(Строка, 24, 1) = "-";
КонецФункции

// Анонимизирует результат get_access_rights: user.name и user.full_name → PER-токены
//
&НаКлиенте
Процедура АнонимизироватьРезультатGetAccessRights(Результат)

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;
	Если ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат; КонецЕсли;
	Если НЕ Результат.Свойство("success") Или НЕ Результат.success Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные) Тогда Возврат; КонецЕсли;
	Если ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	ДанныеПользователя = Неопределено;
	Если НЕ Данные.Свойство("user", ДанныеПользователя) Тогда Возврат; КонецЕсли;
	Если ТипЗнч(ДанныеПользователя) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	// name → PER-токен
	ИмяПольз = "";
	Если ДанныеПользователя.Свойство("name", ИмяПольз)
		И ТипЗнч(ИмяПольз) = Тип("Строка")
		И НЕ ПустаяСтрока(ИмяПольз) Тогда
		ДанныеПользователя.Вставить("name", АнонимизаторТокенизировать(ИмяПольз, "PER"));
	КонецЕсли;

	// full_name → PER-токен (поле опциональное)
	ПолноеИмя = "";
	Если ДанныеПользователя.Свойство("full_name", ПолноеИмя)
		И ТипЗнч(ПолноеИмя) = Тип("Строка")
		И НЕ ПустаяСтрока(ПолноеИмя) Тогда
		ДанныеПользователя.Вставить("full_name", АнонимизаторТокенизировать(ПолноеИмя, "PER"));
	КонецЕсли;

КонецПроцедуры

// Анонимизирует результат get_event_log (data — Массив записей).
// user  → АнонимизаторТокенизировать("PER"), если не UUID
// computer → АнонимизаторТокенизировать("STR"), всегда
// comment, data_presentation → словарная замена + regex-фаза
//
&НаКлиенте
Процедура АнонимизироватьРезультатGetEventLogВстроенный(Результат, РегексКонтекст = Неопределено)

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;
	Если ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	РезультатУспех = Ложь;
	Результат.Свойство("success", РезультатУспех);
	Если НЕ РезультатУспех Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	НастройкиАнонимизации = ПолучитьНастройкиТочнойАнонимизацииИзХранилища();
	АгрессивныйРежим = НастройкиАнонимизации.АгрессивныйРежим;

	Для ЗаписьИндекс = 0 По Данные.Количество() - 1 Цикл
		Запись = Данные[ЗаписьИндекс];
		Если ТипЗнч(Запись) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		// user - токенизируем как персону, если не UUID
		Польз = "";
		Если Запись.Свойство("user", Польз)
			И ТипЗнч(Польз) = Тип("Строка")
			И НЕ ПустаяСтрока(Польз)
			И НЕ ЭтоGUID(Польз) Тогда
			Запись.Вставить("user", АнонимизаторТокенизировать(Польз, "PER"));
		КонецЕсли;

		// computer — всегда токенизируем как строку
		Комп = "";
		Если Запись.Свойство("computer", Комп)
			И ТипЗнч(Комп) = Тип("Строка")
			И НЕ ПустаяСтрока(Комп) Тогда
			Запись.Вставить("computer", АнонимизаторТокенизировать(Комп, "STR"));
		КонецЕсли;

		// comment — словарная замена (regex-фаза позже)
		Коммент = "";
		Если Запись.Свойство("comment", Коммент)
			И ТипЗнч(Коммент) = Тип("Строка")
			И НЕ ПустаяСтрока(Коммент) Тогда
			Запись.Вставить("comment", ПрименитьСловарьКСтроке(Коммент));
		КонецЕсли;

		// data_presentation — агрессивный режим: целиком; иначе — словарная замена (regex-фаза позже)
		ДатаПред = "";
		Если Запись.Свойство("data_presentation", ДатаПред)
			И ТипЗнч(ДатаПред) = Тип("Строка")
			И НЕ ПустаяСтрока(ДатаПред) Тогда
			Если АгрессивныйРежим Тогда
				Запись.Вставить("data_presentation", АнонимизаторТокенизировать(ДатаПред, "STR"));
			Иначе
				Запись.Вставить("data_presentation", ПрименитьСловарьКСтроке(ДатаПред));
			КонецЕсли;
		КонецЕсли;

	КонецЦикла;

	// Собрать тексты для regex-фазы
	Если РегексКонтекст <> Неопределено Тогда
		СобратьТекстыДляRegexGetEventLog(Результат, РегексКонтекст, АгрессивныйРежим);
	КонецЕсли;

КонецПроцедуры

// Собирает comment и data_presentation из записей журнала для regex-фазы.
//
&НаКлиенте
Процедура СобратьТекстыДляRegexGetEventLog(Результат, РегексКонтекст, АгрессивныйРежим = Ложь)

	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") Тогда
			Включено = Истина;
			Попытка Правило.Свойство("enabled", Включено); Исключение КонецПопытки;
			Если Включено Тогда
				ВключённыеПравила.Добавить(Новый Структура("pattern, category", Правило.pattern, Правило.category));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Если ВключённыеПравила.Количество() = 0 Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	ТекстыМассив  = Новый Массив;
	МаппингИндексов = Новый Массив;

	Для ЗаписьИндекс = 0 По Данные.Количество() - 1 Цикл
		Запись = Данные[ЗаписьИндекс];
		Если ТипЗнч(Запись) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		// comment (после словарной замены)
		Коммент = "";
		Если Запись.Свойство("comment", Коммент)
			И ТипЗнч(Коммент) = Тип("Строка")
			И НЕ ПустаяСтрока(Коммент) Тогда
			ТекстыМассив.Добавить(Коммент);
			МаппингИндексов.Добавить(Новый Структура("ЗаписьИндекс, ИмяПоля", ЗаписьИндекс, "comment"));
		КонецЕсли;

		// data_presentation (после словарной замены) — только если не агрессивный режим
		Если НЕ АгрессивныйРежим Тогда
			ДатаПред = "";
			Если Запись.Свойство("data_presentation", ДатаПред)
				И ТипЗнч(ДатаПред) = Тип("Строка")
				И НЕ ПустаяСтрока(ДатаПред) Тогда
				ТекстыМассив.Добавить(ДатаПред);
				МаппингИндексов.Добавить(Новый Структура("ЗаписьИндекс, ИмяПоля", ЗаписьИндекс, "data_presentation"));
			КонецЕсли;
		КонецЕсли;

	КонецЦикла;

	Если ТекстыМассив.Количество() = 0 Тогда Возврат; КонецЕсли;

	РегексКонтекст.Вставить("ВключённыеПравилаJSON", ЗаписатьJSON2(ВключённыеПравила));
	РегексКонтекст.Вставить("ТекстыJSON",            ЗаписатьJSON2(ТекстыМассив));
	РегексКонтекст.Вставить("МаппингИндексов",       МаппингИндексов);
	РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Истина);

КонецПроцедуры

// Применяет результаты regex-фазы к полям comment/data_presentation записей журнала.
//
&НаКлиенте
Процедура ПрименитьRegexТокеныGetEventLog(РегексКонтекст, СовпаденияJSON)

	Если ПустаяСтрока(СовпаденияJSON) Тогда Возврат; КонецЕсли;

	МаппингИндексов = Неопределено;
	Если НЕ РегексКонтекст.Свойство("МаппингИндексов", МаппингИндексов)
		Или МаппингИндексов = Неопределено Тогда Возврат; КонецЕсли;

	Результат = Неопределено;
	Если НЕ РегексКонтекст.Свойство("Результат", Результат)
		Или ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные)
		Или ТипЗнч(Данные) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	Попытка
		Совпадения = ПрочитатьJSON2(СовпаденияJSON);
	Исключение
		Возврат;
	КонецПопытки;

	Если ТипЗнч(Совпадения) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	РезСовпадений = Совпадения;
	Для ТекстИндекс = 0 По МаппингИндексов.Количество() - 1 Цикл

		Если ТекстИндекс >= РезСовпадений.Количество() Тогда Прервать; КонецЕсли;

		ЭлСовпадений = РезСовпадений[ТекстИндекс];
		Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда Продолжить; КонецЕсли;

		МаппингЭлт = МаппингИндексов[ТекстИндекс];
		Если ТипЗнч(МаппингЭлт) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		ЗаписьИндекс = 0;
		МаппингЭлт.Свойство("ЗаписьИндекс", ЗаписьИндекс);
		ИмяПоля = "";
		МаппингЭлт.Свойство("ИмяПоля", ИмяПоля);

		Если ЗаписьИндекс >= Данные.Количество() Тогда Продолжить; КонецЕсли;
		Запись = Данные[ЗаписьИндекс];
		Если ТипЗнч(Запись) <> Тип("Структура") Тогда Продолжить; КонецЕсли;

		ТекущийТекст = "";
		Запись.Свойство(ИмяПоля, ТекущийТекст);
		Если ТипЗнч(ТекущийТекст) <> Тип("Строка") Или ПустаяСтрока(ТекущийТекст) Тогда Продолжить; КонецЕсли;

		// Применить совпадения справа налево (отсортированы DESC по start)
		НовыйТекст = ТекущийТекст;
		Для Каждого С Из ЭлСовпадений Цикл
			Категория = "";
			МатчТекст = "";
			Начало    = 0;
			Длина     = 0;
			Если ТипЗнч(С) = Тип("Структура") Тогда
				С.Свойство("category", Категория);
				С.Свойство("match",    МатчТекст);
				С.Свойство("start",    Начало);
				С.Свойство("length",   Длина);
			ИначеЕсли ТипЗнч(С) = Тип("Соответствие") Тогда
				Категория = С["category"];
				МатчТекст = С["match"];
				Начало    = С["start"];
				Длина     = С["length"];
			КонецЕсли;
			Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
			Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
			Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
			Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
			Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
			Токен = АнонимизаторТокенизировать(МатчТекст, Категория);
			Если Начало >= 0 И (Начало + Длина) <= СтрДлина(НовыйТекст) Тогда
				НовыйТекст = Лев(НовыйТекст, Начало) + Токен + Сред(НовыйТекст, Начало + Длина + 1);
			КонецЕсли;
		КонецЦикла;

		Запись.Вставить(ИмяПоля, НовыйТекст);

	КонецЦикла;

КонецПроцедуры

// Анонимизирует результат get_object_by_link (data — Структура, не Массив).
// Поддерживает два режима:
//   - Schema mode: колонки берутся из schema, применяется policy
//   - Blind mode (_blind_anonymization = Истина): schema нет, токенизируем все строки
//
&НаКлиенте
Процедура АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст = Неопределено)

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;
	Если ТипЗнч(Результат) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	РезультатУспех = Ложь;
	Результат.Свойство("success", РезультатУспех);
	Если НЕ РезультатУспех Тогда Возврат; КонецЕсли;

	Данные = Неопределено;
	Если НЕ Результат.Свойство("data", Данные) Или ТипЗнч(Данные) <> Тип("Структура") Тогда
		Возврат;
	КонецЕсли;

	// — BLIND MODE —
	ЕстьСхема = Результат.Свойство("schema") И НЕ Результат.Свойство("_blind_anonymization");
	Если НЕ ЕстьСхема Тогда
		// Технические ключи, которые нельзя трогать
		СлужебныеКлючи = Новый Соответствие;
		СлужебныеКлючи.Вставить("_type",               Истина);
		СлужебныеКлючи.Вставить("_objectRef",          Истина);
		СлужебныеКлючи.Вставить("ТипОбъекта",          Истина);
		СлужебныеКлючи.Вставить("УникальныйИдентификатор", Истина);

		Для Каждого КВП Из Данные Цикл
			Если СлужебныеКлючи[КВП.Ключ] = Истина Тогда Продолжить; КонецЕсли;
			Значение = КВП.Значение;
			Если ТипЗнч(Значение) = Тип("Строка") И Значение <> "" Тогда
				Данные.Вставить(КВП.Ключ, АнонимизаторТокенизировать(Значение, "STR"));
			ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
				// _objectRef или другая структура — токенизируем Представление
				ПредставлениеОбъекта = "";
				Если Значение.Свойство("Представление", ПредставлениеОбъекта)
					И ТипЗнч(ПредставлениеОбъекта) = Тип("Строка")
					И ПредставлениеОбъекта <> "" Тогда
					Значение.Вставить("Представление", АнонимизаторТокенизировать(ПредставлениеОбъекта, "STR"));
				КонецЕсли;
			ИначеЕсли ТипЗнч(Значение) = Тип("Массив") Тогда
				// ТЧ или массив строк (ФиксированныйМассив → Массив после ПреобразоватьЗначениеДляJSON)
				Для ЭлтИнд = 0 По Значение.Количество() - 1 Цикл
					ЭлтМасс = Значение[ЭлтИнд];
					Если ТипЗнч(ЭлтМасс) = Тип("Строка") И ЭлтМасс <> "" Тогда
						Значение[ЭлтИнд] = АнонимизаторТокенизировать(ЭлтМасс, "STR");
					ИначеЕсли ТипЗнч(ЭлтМасс) = Тип("Структура") Тогда
						Для Каждого КВПСтр Из ЭлтМасс Цикл
							Если СлужебныеКлючи[КВПСтр.Ключ] = Истина Тогда Продолжить; КонецЕсли;
							ЗнчСтр = КВПСтр.Значение;
							Если ТипЗнч(ЗнчСтр) = Тип("Строка") И ЗнчСтр <> "" Тогда
								ЭлтМасс.Вставить(КВПСтр.Ключ, АнонимизаторТокенизировать(ЗнчСтр, "STR"));
							ИначеЕсли ТипЗнч(ЗнчСтр) = Тип("Структура") Тогда
								ПредстСтр = "";
								Если ЗнчСтр.Свойство("Представление", ПредстСтр)
									И ТипЗнч(ПредстСтр) = Тип("Строка") И ПредстСтр <> "" Тогда
									ЗнчСтр.Вставить("Представление", АнонимизаторТокенизировать(ПредстСтр, "STR"));
								КонецЕсли;
							КонецЕсли;
						КонецЦикла;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
		// Regex в blind mode не запускаем — все строки уже целиком токенизированы
		// РегексКонтекст.ЕстьТекстыДляRegex остаётся Ложь
		Возврат;
	КонецЕсли;

	// — SCHEMA MODE —
	ПравилаАнонимизации = ПолучитьПравилаТочнойАнонимизацииИзХранилища();

	Схема = Результат.schema;
	Колонки = Неопределено;
	Если НЕ Схема.Свойство("columns", Колонки) Или ТипЗнч(Колонки) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	// Карта политик основных колонок
	СоответствиеPolicy = Новый Соответствие;
	Для Каждого Колонка Из Колонки Цикл
		Если ТипЗнч(Колонка) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
		ИмяКолонки = "";
		Если НЕ Колонка.Свойство("name", ИмяКолонки) Тогда Продолжить; КонецЕсли;
		Режим = "default";      Колонка.Свойство("anonymization_mode", Режим);
		РежимСсылки = "default"; Колонка.Свойство("reference_presentation_mode", РежимСсылки);
		Sources = ПолучитьМассивСтрокИзСвойства(Колонка, "sources");
		СоответствиеPolicy.Вставить(ИмяКолонки,
			Новый Структура("mode, ref_mode, sources", Режим, РежимСсылки, Sources));
	КонецЦикла;

	// Карты политик табличных частей
	СоответствиеPolicyТЧ = Новый Соответствие; // ИмяТЧ -> Соответствие (имя колонки -> policy)
	ТабличныеЧасти = Неопределено;
	Если Схема.Свойство("tabular_sections", ТабличныеЧасти) Тогда
		Для Каждого КВПтч Из ТабличныеЧасти Цикл
			ИмяТЧ    = КВПтч.Ключ;
			СхемаТЧ  = КВПтч.Значение;
			PolicyТЧ = Новый Соответствие;
			КолонкиТЧ = Неопределено;
			Если СхемаТЧ.Свойство("columns", КолонкиТЧ) Тогда
				Для Каждого КолТЧ Из КолонкиТЧ Цикл
					Если ТипЗнч(КолТЧ) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
					ИмяКол = ""; Если НЕ КолТЧ.Свойство("name", ИмяКол) Тогда Продолжить; КонецЕсли;
					Реж = "default";      КолТЧ.Свойство("anonymization_mode", Реж);
					РежСсылки = "default"; КолТЧ.Свойство("reference_presentation_mode", РежСсылки);
					Src = ПолучитьМассивСтрокИзСвойства(КолТЧ, "sources");
					PolicyТЧ.Вставить(ИмяКол,
						Новый Структура("mode, ref_mode, sources", Реж, РежСсылки, Src));
				КонецЦикла;
			КонецЕсли;
			СоответствиеPolicyТЧ.Вставить(ИмяТЧ, PolicyТЧ);
		КонецЦикла;
	КонецЕсли;

	// Обходим поля data (Структура)
	Для Каждого КВП Из Данные Цикл
		ИмяПоля = КВП.Ключ;
		Значение = КВП.Значение;

		// Табличная часть?
		Если СоответствиеPolicyТЧ[ИмяПоля] <> Неопределено
			И ТипЗнч(Значение) = Тип("Массив") Тогда
			PolicyМапТЧ = СоответствиеPolicyТЧ[ИмяПоля];
			Для Каждого СтрокаТЧ Из Значение Цикл
				Если ТипЗнч(СтрокаТЧ) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
				Для Каждого PolicyЭлт Из PolicyМапТЧ Цикл
					ИмяКолонки = PolicyЭлт.Ключ;
					Policy      = PolicyЭлт.Значение;
					ЗначениеКолонки = Неопределено;
					Если НЕ СтрокаТЧ.Свойство(ИмяКолонки, ЗначениеКолонки) Тогда Продолжить; КонецЕсли;
					Если ЗначениеКолонки = Неопределено Тогда Продолжить; КонецЕсли;
					ОбработатьЗначениеОбъектаВстроенный(СтрокаТЧ, ИмяКолонки, ЗначениеКолонки, Policy, ПравилаАнонимизации);
				КонецЦикла;
			КонецЦикла;
			Продолжить;
		КонецЕсли;

		// Обычное поле
		Policy = СоответствиеPolicy[ИмяПоля];
		Если Policy = Неопределено Тогда Продолжить; КонецЕсли;
		ОбработатьЗначениеОбъектаВстроенный(Данные, ИмяПоля, Значение, Policy, ПравилаАнонимизации);
	КонецЦикла;

	// Фаза regex
	Если РегексКонтекст <> Неопределено Тогда
		СобратьТекстыДляRegexОбъект(Результат, РегексКонтекст);
	КонецЕсли;

КонецПроцедуры

// Вспомогательная — применяет policy к одному полю объекта/строки ТЧ
&НаКлиенте
Процедура ОбработатьЗначениеОбъектаВстроенный(Контейнер, ИмяПоля, Значение, Policy, ПравилаАнонимизации)

	Если ТипЗнч(Значение) = Тип("Структура")
		И Значение.Свойство("Представление") Тогда
		// objectRef — анонимизируем Представление по ref_mode
		ТипОбъектаЗначения = "";
		Значение.Свойство("ТипОбъекта", ТипОбъектаЗначения);
		Если Найти(ТипОбъектаЗначения, "ПеречислениеСсылка.") = 1 Тогда Возврат; КонецЕсли;
		Если Найти(ТипОбъектаЗначения, "ПланСчетовСсылка.") = 1
			И Policy.mode <> "force_anonymize" И Policy.ref_mode <> "force_anonymize" Тогда
			Возврат;
		КонецЕсли;
		Если Policy.mode = "force_keep" Тогда Возврат;
		ИначеЕсли Policy.mode = "force_anonymize" Или Policy.ref_mode = "force_anonymize" Тогда
			Значение.Вставить("Представление",
				АнонимизироватьЗначениеВстроенный(Значение.Представление, Policy.sources, ПравилаАнонимизации));
		ИначеЕсли Policy.ref_mode = "force_keep" Тогда Возврат;
		Иначе
			Значение.Вставить("Представление", ПрименитьСловарьКСтроке(Значение.Представление));
		КонецЕсли;

	ИначеЕсли ТипЗнч(Значение) = Тип("Строка") Тогда
		НовоеЗначение = Неопределено;
		Если Policy.mode = "force_anonymize" Тогда
			НовоеЗначение = АнонимизироватьЗначениеВстроенный(Значение, Policy.sources, ПравилаАнонимизации);
		ИначеЕсли Policy.mode = "default" Тогда
			НовоеЗначение = ПрименитьСловарьКСтроке(Значение);
		КонецЕсли;
		Если НовоеЗначение <> Неопределено Тогда
			Контейнер.Вставить(ИмяПоля, НовоеЗначение);
		КонецЕсли;
	ИначеЕсли ТипЗнч(Значение) = Тип("Массив") Тогда
		// Реквизит типа ФиксированныйМассив (строки или ссылки) после ПреобразоватьЗначениеДляJSON
		Если Policy.mode = "force_keep" Тогда Возврат; КонецЕсли;
		Для ЭлтИнд = 0 По Значение.Количество() - 1 Цикл
			Элт = Значение[ЭлтИнд];
			Если ТипЗнч(Элт) = Тип("Структура") И Элт.Свойство("Представление") Тогда
				ТипОбъектаЭлт = "";
				Элт.Свойство("ТипОбъекта", ТипОбъектаЭлт);
				Если Найти(ТипОбъектаЭлт, "ПеречислениеСсылка.") = 1 Тогда Продолжить; КонецЕсли;
				Если Найти(ТипОбъектаЭлт, "ПланСчетовСсылка.") = 1
					И Policy.mode <> "force_anonymize" И Policy.ref_mode <> "force_anonymize" Тогда
					Продолжить;
				КонецЕсли;
				Если Policy.ref_mode = "force_keep" Тогда Продолжить; КонецЕсли;
				Если Policy.mode = "force_anonymize" Или Policy.ref_mode = "force_anonymize" Тогда
					Элт.Вставить("Представление",
						АнонимизироватьЗначениеВстроенный(Элт.Представление, Policy.sources, ПравилаАнонимизации));
				Иначе
					Элт.Вставить("Представление", ПрименитьСловарьКСтроке(Элт.Представление));
				КонецЕсли;
			ИначеЕсли ТипЗнч(Элт) = Тип("Строка") Тогда
				НовЭлт = Неопределено;
				Если Policy.mode = "force_anonymize" Тогда
					НовЭлт = АнонимизироватьЗначениеВстроенный(Элт, Policy.sources, ПравилаАнонимизации);
				ИначеЕсли Policy.mode = "default" Тогда
					НовЭлт = ПрименитьСловарьКСтроке(Элт);
				КонецЕсли;
				Если НовЭлт <> Неопределено Тогда
					Значение[ЭлтИнд] = НовЭлт;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьТаблицуМаппинговВстроенный()

	АнонимизаторИнициализировать();
	ТаблицаМаппингов.Очистить();

	Если МаппингДеанонимизации = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ТекстПоиска = НРег(СокрЛП(ПоискМаппингов));
	Всего = 0;

	Для Каждого КвП Из МаппингДеанонимизации Цикл
		Токен           = КвП.Ключ;
		РеальноеЗначение = КвП.Значение;

		Категория = "";
		Если СтрДлина(Токен) > 2 И Лев(Токен, 1) = "[" Тогда
			Внутренняя = Сред(Токен, 2, СтрДлина(Токен) - 2);
			ПозДефиса  = Найти(Внутренняя, "-");
			Если ПозДефиса > 0 Тогда
				Категория = Лев(Внутренняя, ПозДефиса - 1);
			КонецЕсли;
		КонецЕсли;

		Всего = Всего + 1;

		Если Не ПустаяСтрока(ТекстПоиска) Тогда
			Если Найти(НРег(Токен), ТекстПоиска) = 0
				И Найти(НРег(РеальноеЗначение), ТекстПоиска) = 0
				И Найти(НРег(Категория), ТекстПоиска) = 0 Тогда
				Продолжить;
			КонецЕсли;
		КонецЕсли;

		НоваяСтрока = ТаблицаМаппингов.Добавить();
		НоваяСтрока.Токен           = Токен;
		НоваяСтрока.РеальноеЗначение = РеальноеЗначение;
		НоваяСтрока.Категория       = Категория;
	КонецЦикла;

	Количество = ТаблицаМаппингов.Количество();
	Если Не ПустаяСтрока(ТекстПоиска) Тогда
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Показано " + Количество + " из " + Всего;
	Иначе
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Всего записей: " + Всего;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Функция ДеТокенизироватьРекурсивно(Значение)

	Если ТипЗнч(Значение) = Тип("Строка") Тогда
		Возврат АнонимизаторДеТокенизироватьСтроку(Значение);

	ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
		Ключи = Новый Массив;
		НовыеЗначения = Новый Массив;
		Для Каждого КвП Из Значение Цикл
			Ключи.Добавить(КвП.Ключ);
			НовыеЗначения.Добавить(ДеТокенизироватьРекурсивно(КвП.Значение));
		КонецЦикла;
		Для Инд = 0 По Ключи.ВГраница() Цикл
			Значение.Вставить(Ключи[Инд], НовыеЗначения[Инд]);
		КонецЦикла;

	ИначеЕсли ТипЗнч(Значение) = Тип("Соответствие") Тогда
		Ключи = Новый Массив;
		НовыеЗначения = Новый Массив;
		Для Каждого КвП Из Значение Цикл
			Ключи.Добавить(КвП.Ключ);
			НовыеЗначения.Добавить(ДеТокенизироватьРекурсивно(КвП.Значение));
		КонецЦикла;
		Для Инд = 0 По Ключи.ВГраница() Цикл
			Значение.Вставить(Ключи[Инд], НовыеЗначения[Инд]);
		КонецЦикла;

	ИначеЕсли ТипЗнч(Значение) = Тип("Массив") Тогда
		Для Инд = 0 По Значение.ВГраница() Цикл
			Значение[Инд] = ДеТокенизироватьРекурсивно(Значение[Инд]);
		КонецЦикла;
	КонецЕсли;

	Возврат Значение;

КонецФункции

&НаКлиенте
Процедура ДеАнонимизироватьПараметрыКоманды(Команда)

	АнонимизаторИнициализировать();

	Если МаппингДеанонимизации = Неопределено
		Или МаппингДеанонимизации.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Если НЕ Команда.Свойство("params") Или Команда.params = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Поля с кодом/текстом запроса требуют специального экранирования кавычек
	КодовыеПоля = Новый Соответствие;
	КодовыеПоля.Вставить("execute_code",  "code");
	КодовыеПоля.Вставить("execute_query", "query");

	КодовоеПоле = КодовыеПоля.Получить(Команда.tool);
	Если КодовоеПоле <> Неопределено Тогда
		НовыеПараметры = Новый Структура;
		Для Каждого КвП Из Команда.params Цикл
			Если КвП.Ключ = КодовоеПоле Тогда
				Если Команда.tool = "execute_query" Тогда
					НовыеПараметры.Вставить(КвП.Ключ,
						АнонимизаторДеТокенизироватьСтрокуДляЗапроса(КвП.Значение));
				Иначе
					НовыеПараметры.Вставить(КвП.Ключ,
						АнонимизаторДеТокенизироватьСтрокуВBSL(КвП.Значение));
				КонецЕсли;
			Иначе
				НовыеПараметры.Вставить(КвП.Ключ, ДеТокенизироватьРекурсивно(КвП.Значение));
			КонецЕсли;
		КонецЦикла;
		Команда.Вставить("params", НовыеПараметры);
		Возврат;
	КонецЕсли;

	Команда.Вставить("params", ДеТокенизироватьРекурсивно(Команда.params));

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// АНОНИМИЗАЦИЯ — ПРОСМОТР МАППИНГОВ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбновитьМаппинги(Команда)
	ЗагрузитьМаппинги();
КонецПроцедуры

&НаКлиенте
Процедура ДеАнонимизироватьТекст(Команда)
	Если РежимПодключения = 0 Тогда
		// Прокси: синхронно обновить кэш с сервера (при ошибке — использует последний успешный кэш)
		ЗагрузитьМаппинги();
		// Пересоздать МаппингДеанонимизации из кэша (избежать смеси старых и новых данных)
		ЗаполнитьМаппингДеАнонимизацииИзКэша();
	КонецЕсли;
	// Встроенный режим: МаппингДеанонимизации уже актуален из текущей сессии
	ТекстДляДеАнонимизации = АнонимизаторДеТокенизироватьСтроку(ТекстДляДеАнонимизации);
	ОбновитьПросмотрMarkdown();
КонецПроцедуры

&НаКлиенте
Процедура АнонимизироватьТекст(Команда)
	// Только встроенный режим — в proxy-режиме кнопка скрыта, но защита на всякий случай
	Если РежимПодключения = 0 Тогда Возврат; КонецЕсли;
	ТекстДляДеАнонимизации = АнонимизаторАнонимизироватьСтроку(ТекстДляДеАнонимизации);
	ОбновитьТаблицуМаппинговВстроенный(); // синхронизировать список маппингов в UI
	ОбновитьПросмотрMarkdown();
КонецПроцедуры

&НаКлиенте
Функция ВыполнитьSubmitForDeanonymization(Текст)
	// Попытка герметизирует весь блок: даже при исключении агент получает только
	// фиксированную безопасную строку, а не ОписаниеОшибки() с деанонимизированными данными
	Попытка
		Если РежимПодключения = 0 Тогда
			ЗагрузитьМаппинги();
			ЗаполнитьМаппингДеАнонимизацииИзКэша();
		КонецЕсли;

		ДеАнонимизированный = АнонимизаторДеТокенизироватьСтроку(Текст);

		ВремяМетка = Формат(ТекущаяДата(), "ДФ='HH:mm:ss dd.MM.yyyy'");
		Разделитель = "═══════ [" + ВремяМетка + "] ═══════";

		Если ПустаяСтрока(ТекстДляДеАнонимизации) Тогда
			ТекстДляДеАнонимизации = Разделитель + Символы.ПС + Символы.ПС + ДеАнонимизированный;
		Иначе
			ТекстДляДеАнонимизации = ТекстДляДеАнонимизации
				+ Символы.ПС + Символы.ПС
				+ Разделитель + Символы.ПС + Символы.ПС
				+ ДеАнонимизированный;
		КонецЕсли;
		ОбновитьПросмотрMarkdown();

		// success=Истина нужен для ОтправитьРезультатНаСервер (proxy-режим);
		// received=Истина — содержательное поле ответа агенту
		Возврат Новый Структура("success, received", Истина, Истина);
	Исключение
		// Фиксированная строка — ОписаниеОшибки() не возвращаем, там могут быть реальные данные
		Возврат Новый Структура("success, error", Ложь, "submit_for_deanonymization failed");
	КонецПопытки;
КонецФункции

&НаКлиенте
Процедура ПолеТекстДляДеАнонимизацииПриИзменении(Элемент)
	ОбновитьПросмотрMarkdown();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьПросмотрMarkdown()
	Если ПустаяСтрока(ШаблонMarkdownПросмотра) Тогда
		Возврат;
	КонецЕсли;
	КонтентHTML = ЭкранироватьHTMLДляПросмотра(ТекстДляДеАнонимизации);
	ТекстДляДеАнонимизацииHTML = СтрЗаменить(ШаблонMarkdownПросмотра, "{{HTML_ESCAPED_CONTENT}}", КонтентHTML);
КонецПроцедуры

&НаКлиенте
Функция ЭкранироватьHTMLДляПросмотра(Знач Текст)
	Текст = СтрЗаменить(Текст, "&", "&amp;");
	Текст = СтрЗаменить(Текст, "<", "&lt;");
	Текст = СтрЗаменить(Текст, ">", "&gt;");
	Возврат Текст;
КонецФункции

&НаКлиенте
Процедура ПоискМаппинговПриИзменении(Элемент)
	ЗаполнитьТаблицуМаппингов(); // перефильтровка из кэша без HTTP-запроса
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьМаппинги()

	Если РежимПодключения <> 0 Тогда
		ЗаполнитьТаблицуМаппингов();
		Возврат;
	КонецЕсли;

	Если ПустаяСтрока(АдресСервера) Тогда
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Не указан адрес сервера";
		Возврат;
	КонецЕсли;

	Попытка
		СтруктураURL = РазобратьURL(АдресСервера);

		Если СтруктураURL.Протокол = "https" Тогда
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 10, Новый ЗащищенноеСоединениеOpenSSL());
		Иначе
			Соединение = Новый HTTPСоединение(СтруктураURL.Хост, СтруктураURL.Порт, , , , 10);
		КонецЕсли;

		ПараметрыЗапроса = Новый Структура;
		Если Не ПустаяСтрока(ИдентификаторКанала) Тогда
			ПараметрыЗапроса.Вставить("channel", СокрЛП(ИдентификаторКанала));
		КонецЕсли;

		ПутьЗапроса = СформироватьПутьЗапроса(СтруктураURL.Путь + "/1c/anonymization_mappings", ПараметрыЗапроса);

		Запрос = Новый HTTPЗапрос(ПутьЗапроса);
		Запрос.Заголовки.Вставить("Accept", "application/json");

		Ответ = Соединение.Получить(Запрос);

		Если Ответ.КодСостояния = 200 Тогда
			ТелоОтвета = Ответ.ПолучитьТелоКакСтроку("UTF-8");
			Если Не ПустаяСтрока(ТелоОтвета) Тогда
				КэшМаппинговОтвет = ПрочитатьJSON2(ТелоОтвета);
				ЗаполнитьТаблицуМаппингов();
			Иначе
				Элементы.НадписьСтатистикаМаппингов.Заголовок = "Пустой ответ от сервера";
			КонецЕсли;
		Иначе
			// Кэш НЕ очищаем — оставляем старые данные при ошибке
			Элементы.НадписьСтатистикаМаппингов.Заголовок = "Ошибка: HTTP " + Ответ.КодСостояния;
		КонецЕсли;

	Исключение
		// Кэш НЕ очищаем — оставляем старые данные при ошибке соединения
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Ошибка: " + ОписаниеОшибки();
	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьТаблицуМаппингов()

	Если РежимПодключения <> 0 Тогда
		ОбновитьТаблицуМаппинговВстроенный();
		Возврат;
	КонецЕсли;

	ТаблицаМаппингов.Очистить();

	Если КэшМаппинговОтвет = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Проверка: анонимизация отключена на сервере
	АнонВключена = Неопределено;
	Если ТипЗнч(КэшМаппинговОтвет) = Тип("Структура") Тогда
		КэшМаппинговОтвет.Свойство("enabled", АнонВключена);
	Иначе
		АнонВключена = КэшМаппинговОтвет["enabled"];
	КонецЕсли;

	Если АнонВключена = Ложь Тогда
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Анонимизация отключена на сервере";
		Возврат;
	КонецЕсли;

	// Получаем массив маппингов
	Маппинги = Неопределено;
	Если ТипЗнч(КэшМаппинговОтвет) = Тип("Структура") Тогда
		КэшМаппинговОтвет.Свойство("mappings", Маппинги);
	Иначе
		Маппинги = КэшМаппинговОтвет["mappings"];
	КонецЕсли;

	Если Маппинги = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Фильтр (регистронезависимый)
	ТекстПоиска = НРег(СокрЛП(ПоискМаппингов));
	Всего = Маппинги.Количество();

	Для Каждого Элт Из Маппинги Цикл
		Токен = "";
		РеальноеЗначение = "";
		Категория = "";

		Если ТипЗнч(Элт) = Тип("Структура") Тогда
			Элт.Свойство("token", Токен);
			Элт.Свойство("real_value", РеальноеЗначение);
			Элт.Свойство("category", Категория);
		Иначе
			Токен = Элт["token"];
			РеальноеЗначение = Элт["real_value"];
			Категория = Элт["category"];
		КонецЕсли;

		// Клиентская фильтрация
		Если Не ПустаяСтрока(ТекстПоиска) Тогда
			Если Найти(НРег(Токен), ТекстПоиска) = 0
				И Найти(НРег(РеальноеЗначение), ТекстПоиска) = 0
				И Найти(НРег(Категория), ТекстПоиска) = 0 Тогда
				Продолжить;
			КонецЕсли;
		КонецЕсли;

		НоваяСтрока = ТаблицаМаппингов.Добавить();
		НоваяСтрока.Токен = Токен;
		НоваяСтрока.РеальноеЗначение = РеальноеЗначение;
		НоваяСтрока.Категория = Категория;
	КонецЦикла;

	// Статистика
	Количество = ТаблицаМаппингов.Количество();
	Если Не ПустаяСтрока(ТекстПоиска) Тогда
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Показано " + Количество + " из " + Всего;
	Иначе
		Элементы.НадписьСтатистикаМаппингов.Заголовок = "Всего записей: " + Всего;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьМаппингДеАнонимизацииИзКэша()
	// Явно пересоздаём — не просто вставляем, чтобы не смешать со старыми данными
	МаппингДеанонимизации = Новый Соответствие;

	Если КэшМаппинговОтвет = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Проверяем флаг enabled — аналогично ЗаполнитьТаблицуМаппингов()
	АнонВключена = Неопределено;
	Если ТипЗнч(КэшМаппинговОтвет) = Тип("Структура") Тогда
		КэшМаппинговОтвет.Свойство("enabled", АнонВключена);
	Иначе
		АнонВключена = КэшМаппинговОтвет["enabled"];
	КонецЕсли;
	Если АнонВключена = Ложь Тогда
		Возврат; // Анонимизация отключена на сервере — маппинги не используем
	КонецЕсли;

	Маппинги = Неопределено;
	Если ТипЗнч(КэшМаппинговОтвет) = Тип("Структура") Тогда
		КэшМаппинговОтвет.Свойство("mappings", Маппинги);
	Иначе
		Маппинги = КэшМаппинговОтвет["mappings"];
	КонецЕсли;

	Если Маппинги = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Каждого Элт Из Маппинги Цикл
		Токен = "";
		РеальноеЗначение = "";
		Если ТипЗнч(Элт) = Тип("Структура") Тогда
			Элт.Свойство("token", Токен);
			Элт.Свойство("real_value", РеальноеЗначение);
		Иначе
			Токен = Элт["token"];
			РеальноеЗначение = Элт["real_value"];
		КонецЕсли;
		Если Не ПустаяСтрока(Токен) Тогда
			МаппингДеанонимизации.Вставить(ВРег(Токен), РеальноеЗначение);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ─────────────────────────────────────────────────────────────────────────────────

// Соединяет элементы массива в строку через разделитель
// Замена СоединитьМассив() для совместимости с 8.2.13
&НаКлиентеНаСервереБезКонтекста
Функция СоединитьМассив(Массив, Разделитель = ", ")
	
	Если Массив = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	// Проверяем, является ли значение коллекцией
	Попытка
		Количество = Массив.Количество();
	Исключение
		// Если не коллекция, возвращаем как строку
		Возврат Строка(Массив);
	КонецПопытки;
	
	Если Количество = 0 Тогда
		Возврат "";
	КонецЕсли;
	
	Результат = "";
	Первый = Истина;
	Для Каждого Элемент Из Массив Цикл
		Если Первый Тогда
			Первый = Ложь;
		Иначе
			Результат = Результат + Разделитель;
		КонецЕсли;
		Результат = Результат + Строка(Элемент);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Проверяет, является ли значение коллекцией (массив, фиксированный массив и т.п.)
&НаКлиентеНаСервереБезКонтекста
Функция ЭтоКоллекция(Значение)
	
	Если Значение = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Попытка
		Количество = Значение.Количество();
		Возврат Количество > 0;
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

&НаКлиенте
Процедура УстановитьСтатус(НовыйСтатус, НовоеСостояниеПодключения)
	
	Статус = НовыйСтатус;
	Подключено = НовоеСостояниеПодключения;
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьВЛог(Сообщение)
	
	ВремяСообщения = Формат(ТекущаяДата(), "ДФ='HH:mm:ss'");
	НоваяСтрока = ВремяСообщения + " " + Сообщение;
	
	Если ПустаяСтрока(Лог) Тогда
		Лог = НоваяСтрока;
	Иначе
		Лог = НоваяСтрока + Символы.ПС + Лог;
	КонецЕсли;
	
	// Ограничение размера лога
	Если СтрДлина(Лог) > 70000 Тогда
		Лог = Лев(Лог, 70000);
	КонецЕсли;
	
КонецПроцедуры


// ─────────────────────────────────────────────────────────────────────────────────
// НАВИГАЦИОННЫЕ ССЫЛКИ
// ─────────────────────────────────────────────────────────────────────────────────

// Разбирает навигационную ссылку 1С и извлекает её компоненты
// Формат ссылки: e1cib/data/ТипОбъекта.ИмяОбъекта?ref=HexGUID
// Пример: e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5
//
// Параметры:
//   НавигационнаяСсылка - Строка - навигационная ссылка для разбора
//
// Возвращаемое значение:
//   Структура - с полями:
//     * ТипОбъекта - Строка - тип объекта (например, "Справочник", "Документ")
//     * ИмяОбъекта - Строка - имя объекта (например, "Контрагенты")
//     * HexGUID - Строка - шестнадцатеричный идентификатор (32 символа)
//
// Исключения:
//   Вызывает исключение с описательным сообщением при ошибке парсинга
//
&НаСервереБезКонтекста
Функция РазобратьНавигационнуюСсылку(НавигационнаяСсылка)
	
	// Проверка на пустую строку
	Если Не ЗначениеЗаполнено(НавигационнаяСсылка) Тогда
		ВызватьИсключение "Навигационная ссылка не может быть пустой / Navigation link cannot be empty";
	КонецЕсли;
	
	// Удаление пробелов по краям
	НавигационнаяСсылка = СокрЛП(НавигационнаяСсылка);
	
	// Проверка префикса e1cib/data/
	Префикс = "e1cib/data/";
	Если НРег(Лев(НавигационнаяСсылка, СтрДлина(Префикс))) <> НРег(Префикс) Тогда
		ВызватьИсключение "Неверный формат ссылки: должна начинаться с 'e1cib/data/' / Invalid link format: must start with 'e1cib/data/'";
	КонецЕсли;
	
	// Извлечение части после префикса
	ЧастьДанных = Сред(НавигационнаяСсылка, СтрДлина(Префикс) + 1);
	
	// Поиск параметра ref
	ПозицияВопроса = Найти(ЧастьДанных, "?");
	Если ПозицияВопроса = 0 Тогда
		ВызватьИсключение "Неверный формат ссылки: отсутствует параметр '?ref=' / Invalid link format: missing '?ref=' parameter";
	КонецЕсли;
	
	// Извлечение полного имени объекта (ТипОбъекта.ИмяОбъекта)
	ПолноеИмяОбъекта = Лев(ЧастьДанных, ПозицияВопроса - 1);
	
	// Проверка наличия точки в имени объекта
	ПозицияТочки = Найти(ПолноеИмяОбъекта, ".");
	Если ПозицияТочки = 0 Тогда
		ВызватьИсключение "Неверный формат имени объекта: ожидается 'ТипОбъекта.ИмяОбъекта', получено '" + ПолноеИмяОбъекта + "' / Invalid object name format: expected 'ObjectType.ObjectName', got '" + ПолноеИмяОбъекта + "'";
	КонецЕсли;
	
	// Извлечение типа и имени объекта
	ТипОбъекта = Лев(ПолноеИмяОбъекта, ПозицияТочки - 1);
	ИмяОбъекта = Сред(ПолноеИмяОбъекта, ПозицияТочки + 1);
	
	// Проверка что тип и имя не пустые
	Если Не ЗначениеЗаполнено(ТипОбъекта) Тогда
		ВызватьИсключение "Тип объекта не может быть пустым / Object type cannot be empty";
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ИмяОбъекта) Тогда
		ВызватьИсключение "Имя объекта не может быть пустым / Object name cannot be empty";
	КонецЕсли;
	
	// Извлечение параметров после ?
	ЧастьПараметров = Сред(ЧастьДанных, ПозицияВопроса + 1);
	
	// Поиск параметра ref=
	ПозицияRef = Найти(НРег(ЧастьПараметров), "ref=");
	Если ПозицияRef = 0 Тогда
		ВызватьИсключение "Неверный формат ссылки: отсутствует параметр 'ref=' / Invalid link format: missing 'ref=' parameter";
	КонецЕсли;
	
	// Извлечение HexGUID
	HexGUID = Сред(ЧастьПараметров, ПозицияRef + 4);
	
	// Удаление возможных дополнительных параметров после &
	ПозицияАмперсанда = Найти(HexGUID, "&");
	Если ПозицияАмперсанда > 0 Тогда
		HexGUID = Лев(HexGUID, ПозицияАмперсанда - 1);
	КонецЕсли;
	
	// Удаление пробелов
	HexGUID = СокрЛП(HexGUID);
	
	// Проверка длины HexGUID (должно быть 32 символа)
	Если СтрДлина(HexGUID) <> 32 Тогда
		ВызватьИсключение "Неверная длина HexGUID: ожидается 32 символа, получено " + СтрДлина(HexGUID) + " / Invalid HexGUID length: expected 32 characters, got " + СтрДлина(HexGUID);
	КонецЕсли;
	
	// Проверка что HexGUID содержит только шестнадцатеричные символы
	ДопустимыеСимволы = "0123456789abcdefABCDEF";
	Для Позиция = 1 По 32 Цикл
		Символ = Сред(HexGUID, Позиция, 1);
		Если Найти(ДопустимыеСимволы, Символ) = 0 Тогда
			ВызватьИсключение "HexGUID содержит недопустимый символ '" + Символ + "' в позиции " + Позиция + ". Допускаются только шестнадцатеричные символы (0-9, a-f, A-F) / HexGUID contains invalid character '" + Символ + "' at position " + Позиция + ". Only hexadecimal characters (0-9, a-f, A-F) are allowed";
		КонецЕсли;
	КонецЦикла;
	
	// Формирование результата
	Результат = Новый Структура;
	Результат.Вставить("ТипОбъекта", ТипОбъекта);
	Результат.Вставить("ИмяОбъекта", ИмяОбъекта);
	Результат.Вставить("HexGUID", НРег(HexGUID)); // Приводим к нижнему регистру для единообразия
	
	Возврат Результат;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// КонвертироватьHexGUIDвUUID
// Преобразует 32-символьный HexGUID в стандартный формат UUID (8-4-4-4-12)
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   HexGUID - Строка - шестнадцатеричный идентификатор без дефисов (32 символа)
//                      Пример: "80c6cc1a7e58902811ebcda8cb07c0f5"
//
// Возвращаемое значение:
//   Строка - UUID в формате 8-4-4-4-12 с дефисами
//            Пример: "80c6cc1a-7e58-9028-11eb-cda8cb07c0f5"
//
// Исключения:
//   Вызывает исключение если длина HexGUID не равна 32 символам
//
&НаСервереБезКонтекста
Функция КонвертироватьHexGUIDвUUID(HexGUID)
	
	// Валидация длины входной строки
	Если СтрДлина(HexGUID) <> 32 Тогда
		ВызватьИсключение "Неверная длина HexGUID: ожидается 32 символа, получено " + СтрДлина(HexGUID) + " / Invalid HexGUID length: expected 32 characters, got " + СтрДлина(HexGUID);
	КонецЕсли;
	
	// HexGUID в навигационной ссылке 1С имеет особый порядок байтов!
	// Входной HexGUID: 80c6cc1a7e58902811ebcda8cb07c0f5
	// Нужно переставить байты для получения правильного UUID
	//
	// Формат HexGUID в ссылке: AABBCCDD EEFF GGHH IIJJ KKLLMMNNOOPP
	// Формат UUID в 1С:        DDCCBBAA FFEE HHGG IIJJ KKLLMMNNOOPP
	//
	// Первые 4 байта (8 символов) - reverse
	// Следующие 2 байта (4 символа) - reverse  
	// Следующие 2 байта (4 символа) - reverse
	// Остальные 8 байт (16 символов) - без изменений
	
	Часть1 = Сред(HexGUID, 7, 2) + Сред(HexGUID, 5, 2) + Сред(HexGUID, 3, 2) + Сред(HexGUID, 1, 2);
	Часть2 = Сред(HexGUID, 11, 2) + Сред(HexGUID, 9, 2);
	Часть3 = Сред(HexGUID, 15, 2) + Сред(HexGUID, 13, 2);
	Часть4 = Сред(HexGUID, 17, 4);
	Часть5 = Сред(HexGUID, 21, 12);
	
	UUID = Часть1 + "-" + Часть2 + "-" + Часть3 + "-" + Часть4 + "-" + Часть5;
	
	Возврат UUID;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// КонвертироватьUUIDвHexGUID
// Преобразует UUID в формате 8-4-4-4-12 в 32-символьный HexGUID для навигационной ссылки
// Обратная операция к КонвертироватьHexGUIDвUUID
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   UUID - Строка - уникальный идентификатор в формате 8-4-4-4-12 с дефисами
//                   Пример: "80c6cc1a-7e58-9028-11eb-cda8cb07c0f5"
//
// Возвращаемое значение:
//   Строка - HexGUID без дефисов (32 символа) для использования в навигационной ссылке
//            Пример: "1acc6c80589028e711ebcda8cb07c0f5"
//
// Примечание:
//   HexGUID в навигационной ссылке 1С имеет особый порядок байтов (обратный к UUID)
//
&НаСервереБезКонтекста
Функция КонвертироватьUUIDвHexGUID(UUID)
	
	// Удаляем дефисы из UUID
	УникальныйИдентификатор = СтрЗаменить(UUID, "-", "");
	
	// Валидация длины
	Если СтрДлина(УникальныйИдентификатор) <> 32 Тогда
		ВызватьИсключение "Неверная длина UUID: ожидается 32 символа без дефисов, получено " + СтрДлина(УникальныйИдентификатор);
	КонецЕсли;
	
	// Обратная операция к КонвертироватьHexGUIDвUUID
	// UUID формат:     AAAAAAAA-BBBB-CCCC-DDDD-EEEEEEEEEEEE
	// HexGUID формат:  DDDDEEEEEEEEEEEE CCCC BBBB AAAAAAAA
	//
	// Переставляем части в обратном порядке
	
	Часть1 = Сред(УникальныйИдентификатор, 1, 8);   // AAAAAAAA
	Часть2 = Сред(УникальныйИдентификатор, 9, 4);   // BBBB
	Часть3 = Сред(УникальныйИдентификатор, 13, 4);  // CCCC
	Часть4 = Сред(УникальныйИдентификатор, 17, 4);  // DDDD
	Часть5 = Сред(УникальныйИдентификатор, 21, 12); // EEEEEEEEEEEE
	
	// HexGUID = DDDD + EEEEEEEEEEEE + CCCC + BBBB + AAAAAAAA
	HexGUID = Часть4 + Часть5 + Часть3 + Часть2 + Часть1;
	
	Возврат НРег(HexGUID); // Приводим к нижнему регистру для единообразия
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// СформироватьНавигационнуюСсылку
// Формирует навигационную ссылку 1С по описанию объекта
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ОписаниеОбъекта - Структура - описание объекта с полями:
//     * _objectRef - Булево - признак описания объекта (должен быть true)
//     * УникальныйИдентификатор - Строка - UUID объекта
//     * ТипОбъекта - Строка - тип объекта (например, "СправочникСсылка.Контрагенты")
//
// Возвращаемое значение:
//   Строка - навигационная ссылка в формате e1cib/data/ТипОбъекта.ИмяОбъекта?ref=HexGUID
//            Пример: "e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5"
//
// Исключения:
//   Вызывает исключение если описание объекта некорректно
//
&НаСервереБезКонтекста
Функция СформироватьНавигационнуюСсылку(ОписаниеОбъекта)
	
	// Проверка что это описание объекта
	Если ТипЗнч(ОписаниеОбъекта) <> Тип("Структура") Тогда
		ВызватьИсключение "ОписаниеОбъекта должно быть структурой / objectDescription must be a structure";
	КонецЕсли;
	
	Если Не ОписаниеОбъекта.Свойство("_objectRef") Или ОписаниеОбъекта._objectRef <> Истина Тогда
		ВызватьИсключение "Некорректное описание объекта: отсутствует _objectRef = true / Invalid object description: missing _objectRef = true";
	КонецЕсли;
	
	Если Не ОписаниеОбъекта.Свойство("УникальныйИдентификатор") Тогда
		ВызватьИсключение "Некорректное описание объекта: отсутствует УникальныйИдентификатор / Invalid object description: missing УникальныйИдентификатор";
	КонецЕсли;
	
	Если Не ОписаниеОбъекта.Свойство("ТипОбъекта") Тогда
		ВызватьИсключение "Некорректное описание объекта: отсутствует ТипОбъекта / Invalid object description: missing ТипОбъекта";
	КонецЕсли;
	
	// Преобразуем тип объекта из формата "СправочникСсылка.Контрагенты" в "Справочник.Контрагенты"
	ТипОбъекта = ОписаниеОбъекта.ТипОбъекта;
	
	// Убираем "Ссылка" из типа
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "СправочникСсылка.", "Справочник.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ДокументСсылка.", "Документ.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ПеречислениеСсылка.", "Перечисление.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ПланВидовХарактеристикСсылка.", "ПланВидовХарактеристик.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ПланСчетовСсылка.", "ПланСчетов.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ПланВидовРасчетаСсылка.", "ПланВидовРасчета.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "БизнесПроцессСсылка.", "БизнесПроцесс.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ЗадачаСсылка.", "Задача.");
	ТипОбъекта = СтрЗаменить(ТипОбъекта, "ПланОбменаСсылка.", "ПланОбмена.");
	
	// Конвертируем UUID в HexGUID
	HexGUID = КонвертироватьUUIDвHexGUID(ОписаниеОбъекта.УникальныйИдентификатор);
	
	// Формируем навигационную ссылку
	НавигационнаяСсылка = "e1cib/data/" + ТипОбъекта + "?ref=" + HexGUID;
	
	Возврат НавигационнаяСсылка;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьНавигационнуюСсылкуПоОписанию
// Обёртка для формирования навигационной ссылки с обработкой ошибок
// Используется в MCP-инструменте get_link_of_object
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ОписаниеОбъекта - Структура - описание объекта из результатов execute_query
//
// Возвращаемое значение:
//   Структура - результат операции:
//     * success - Булево - признак успешного выполнения
//     * link    - Строка - навигационная ссылка (при success = Истина)
//     * error   - Строка - описание ошибки (при success = Ложь)
//
&НаСервереБезКонтекста
Функция ПолучитьНавигационнуюСсылкуПоОписанию(ОписаниеОбъекта)
	
	Попытка
		
		НавигационнаяСсылка = СформироватьНавигационнуюСсылку(ОписаниеОбъекта);
		
		Возврат Новый Структура("success, data", Истина, НавигационнаяСсылка);
		
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ВерсияПлатформыНеМеньше
// Проверяет, что версия текущей платформы не меньше указанной минимальной версии
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   МинимальнаяВерсия - Строка - минимальная требуемая версия платформы
//                                Пример: "8.3.3" или "8.2.13"
//
// Возвращаемое значение:
//   Булево - Истина, если текущая версия платформы >= минимальной версии
//            Ложь, если текущая версия платформы < минимальной версии
//
// Примечание:
//   Сравнение выполняется покомпонентно (8.3.25.1374 -> [8, 3, 25, 1374])
//   Не использует СтрРазделить для совместимости с 8.2.13
//
&НаСервереБезКонтекста
Функция ВерсияПлатформыНеМеньше(МинимальнаяВерсия)
	
	СистемнаяИнформация = Новый СистемнаяИнформация;
	ВерсияПлатформы = СистемнаяИнформация.ВерсияПриложения;
	
	// Разбор версий в массивы компонентов
	// Не используем СтрРазделить для совместимости с 8.2.13
	ЧастиТекущей = РазделитьСтрокуПоРазделителю(ВерсияПлатформы, ".");
	ЧастиМинимальной = РазделитьСтрокуПоРазделителю(МинимальнаяВерсия, ".");
	
	// Определяем минимальное количество компонентов для сравнения
	КоличествоТекущей = ЧастиТекущей.Количество();
	КоличествоМинимальной = ЧастиМинимальной.Количество();
	
	Если КоличествоТекущей < КоличествоМинимальной Тогда
		МинимальноеКоличество = КоличествоТекущей;
	Иначе
		МинимальноеКоличество = КоличествоМинимальной;
	КонецЕсли;
	
	// Сравнение версий по компонентам
	Для Индекс = 0 По МинимальноеКоличество - 1 Цикл
		ТекущаяЧасть = Число(ЧастиТекущей[Индекс]);
		МинимальнаяЧасть = Число(ЧастиМинимальной[Индекс]);
		
		Если ТекущаяЧасть > МинимальнаяЧасть Тогда
			Возврат Истина;
		ИначеЕсли ТекущаяЧасть < МинимальнаяЧасть Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// Все сравниваемые компоненты равны
	Возврат Истина;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// РазделитьСтрокуПоРазделителю
// Разделяет строку на массив подстрок по указанному разделителю
// Совместимость: 1С:Предприятие 8.2.13+ (замена СтрРазделить)
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   Строка - Строка - исходная строка для разделения
//   Разделитель - Строка - символ-разделитель
//
// Возвращаемое значение:
//   Массив - массив подстрок
//
&НаСервереБезКонтекста
Функция РазделитьСтрокуПоРазделителю(Строка, Разделитель)
	
	Результат = Новый Массив;
	ОстатокСтроки = Строка;
	
	ПозицияРазделителя = Найти(ОстатокСтроки, Разделитель);
	
	Пока ПозицияРазделителя > 0 Цикл
		Часть = Лев(ОстатокСтроки, ПозицияРазделителя - 1);
		Результат.Добавить(Часть);
		ОстатокСтроки = Сред(ОстатокСтроки, ПозицияРазделителя + СтрДлина(Разделитель));
		ПозицияРазделителя = Найти(ОстатокСтроки, Разделитель);
	КонецЦикла;
	
	// Добавляем последнюю часть (или всю строку, если разделитель не найден)
	Результат.Добавить(ОстатокСтроки);
	
	Возврат Результат;
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// НайтиОбъектВручную
// Находит объект 1С по навигационной ссылке без использования встроенного метода
// НайтиПоНавигационнойСсылке (для платформ < 8.3.3)
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   НавигационнаяСсылка - Строка - навигационная ссылка в формате
//                                  e1cib/data/ТипОбъекта.ИмяОбъекта?ref=HexGUID
//                                  Пример: "e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5"
//
// Возвращаемое значение:
//   СправочникСсылка, ДокументСсылка и т.д. - ссылка на найденный объект
//   Неопределено - если объект не найден
//
// Исключения:
//   Вызывает исключение при неверном формате ссылки или неподдерживаемом типе объекта
//
// Требования: 4.2
//
&НаСервереБезКонтекста
Функция НайтиОбъектВручную(НавигационнаяСсылка)

	// Парсинг навигационной ссылки с использованием существующей функции
	// Возвращает структуру с полями: ТипОбъекта, ИмяОбъекта, HexGUID
	РезультатПарсинга = РазобратьНавигационнуюСсылку(НавигационнаяСсылка);

	ТипОбъекта = РезультатПарсинга.ТипОбъекта;
	ИмяОбъекта = РезультатПарсинга.ИмяОбъекта;
	HexGUID = РезультатПарсинга.HexGUID;

	// Формируем полное имя типа для получения пустой ссылки
	ПолноеИмяТипа = ТипОбъекта + "." + ИмяОбъекта;

	// Получаем пустую ссылку нужного типа через предопределённое значение
	ПустаяСсылка = ПредопределенноеЗначение(ПолноеИмяТипа + ".ПустаяСсылка");

	// Преобразуем пустую ссылку во внутреннее строковое представление
	// Шаблон содержит строку с 32 нулями вместо GUID
	Шаблон = ЗначениеВСтрокуВнутр(ПустаяСсылка);

	// Заменяем нули на HexGUID из навигационной ссылки
	Значение = СтрЗаменить(Шаблон, "00000000000000000000000000000000", HexGUID);

	// Преобразуем обратно в ссылку 1С
	Возврат ЗначениеИзСтрокиВнутр(Значение);

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПроверитьФорматСсылкиВнешнегоИсточника
// Проверяет формат навигационной ссылки для внешнего источника данных
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   Ссылка - Строка - навигационная ссылка для проверки
//
// Возвращаемое значение:
//   Строка - описание ошибки или Неопределено если формат корректен
//
// Формат ссылки: e1cib/data/ВнешнийИсточникДанных.Источник.Таблица.ИмяТаблицы?Поле=Значение
//
&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьФорматСсылкиВнешнегоИсточника(Ссылка)

	Префикс = "e1cib/data/";
	Если Лев(Ссылка, СтрДлина(Префикс)) <> Префикс Тогда
		Возврат "Link must start with 'e1cib/data/'";
	КонецЕсли;

	ПозВопрос = Найти(Ссылка, "?");
	Если ПозВопрос = 0 Тогда
		Возврат "External data source link must contain '?' with key parameters";
	КонецЕсли;

	// Путь должен быть ровно 4 сегмента: ВнешнийИсточникДанных.Источник.Таблица.ИмяТаблицы
	ПутьОбъекта = Сред(Ссылка, СтрДлина(Префикс) + 1, ПозВопрос - СтрДлина(Префикс) - 1);
	Части = Новый Массив;
	Остаток = ПутьОбъекта;
	Пока НЕ ПустаяСтрока(Остаток) Цикл
		Поз = Найти(Остаток, ".");
		Если Поз = 0 Тогда
			Части.Добавить(Остаток);
			Остаток = "";
		Иначе
			Части.Добавить(Лев(Остаток, Поз - 1));
			Остаток = Сред(Остаток, Поз + 1);
		КонецЕсли;
	КонецЦикла;

	Если Части.Количество() <> 4 Тогда
		Возврат "External data source link path must have exactly 4 segments: ВнешнийИсточникДанных.Source.Таблица.TableName";
	КонецЕсли;

	Если НРег(Части[2]) <> "таблица" Тогда
		Возврат "External data source link path segment 3 must be 'Таблица'";
	КонецЕсли;

	СтрокаПараметров = Сред(Ссылка, ПозВопрос + 1);
	Если ПустаяСтрока(СтрокаПараметров) Или Найти(СтрокаПараметров, "=") = 0 Тогда
		Возврат "External data source link must contain key parameters after '?'";
	КонецЕсли;

	Возврат Неопределено;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// URLDecode
// Декодирует URL-encoded строку (%XX → символ, + → пробел)
// Поддерживает UTF-8 многобайтовые последовательности для кириллицы
// ─────────────────────────────────────────────────────────────────────────────────
//
&НаСервереБезКонтекста
Функция URLDecode(Строка)
	Если ПустаяСтрока(Строка) Тогда
		Возврат "";
	КонецЕсли;

	// Заменяем + на пробел
	Строка = СтрЗаменить(Строка, "+", " ");

	Результат = "";
	Индекс = 1;
	Длина = СтрДлина(Строка);
	БайтыUTF8 = Новый Массив;

	Пока Индекс <= Длина Цикл
		Симв = Сред(Строка, Индекс, 1);

		Если Симв = "%" И Индекс + 2 <= Длина Тогда
			// Пытаемся декодировать %XX
			Hex1 = НРег(Сред(Строка, Индекс + 1, 1));
			Hex2 = НРег(Сред(Строка, Индекс + 2, 1));

			// Проверяем что это hex
			Если ((Hex1 >= "0" И Hex1 <= "9") ИЛИ (Hex1 >= "a" И Hex1 <= "f"))
				И ((Hex2 >= "0" И Hex2 <= "9") ИЛИ (Hex2 >= "a" И Hex2 <= "f")) Тогда

				КодБайта = HexВЧисло(Hex1 + Hex2);
				БайтыUTF8.Добавить(КодБайта);
				Индекс = Индекс + 2;
			Иначе
				// Не hex - добавляем накопленные байты как символы и текущий символ
				Если БайтыUTF8.Количество() > 0 Тогда
					Результат = Результат + БайтыUTF8ВСтроку(БайтыUTF8);
					БайтыUTF8.Очистить();
				КонецЕсли;
				Результат = Результат + Симв;
			КонецЕсли;
		Иначе
			// Не % - добавляем накопленные байты и текущий символ
			Если БайтыUTF8.Количество() > 0 Тогда
				Результат = Результат + БайтыUTF8ВСтроку(БайтыUTF8);
				БайтыUTF8.Очистить();
			КонецЕсли;
			Результат = Результат + Симв;
		КонецЕсли;

		Индекс = Индекс + 1;
	КонецЦикла;

	// Добавляем оставшиеся байты
	Если БайтыUTF8.Количество() > 0 Тогда
		Результат = Результат + БайтыUTF8ВСтроку(БайтыUTF8);
	КонецЕсли;

	Возврат Результат;
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// HexВЧисло
// Преобразует 2-символьную hex-строку в число
// ─────────────────────────────────────────────────────────────────────────────────
//
&НаСервереБезКонтекста
Функция HexВЧисло(HexСтрока)
	// Преобразует 2-символьную hex-строку в число
	// Например "A1" → 161, "FF" → 255
	Результат = 0;
	Для Индекс = 1 ПО 2 Цикл
		Симв = ВРег(Сред(HexСтрока, Индекс, 1));
		Код = КодСимвола(Симв);
		Если Код >= КодСимвола("0") И Код <= КодСимвола("9") Тогда
			Значение = Код - КодСимвола("0");
		ИначеЕсли Код >= КодСимвола("A") И Код <= КодСимвола("F") Тогда
			Значение = Код - КодСимвола("A") + 10;
		Иначе
			Возврат 0;
		КонецЕсли;
		Результат = Результат * 16 + Значение;
	КонецЦикла;
	Возврат Результат;
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// БайтыUTF8ВСтроку
// Преобразует массив байтов UTF-8 в строку
// ─────────────────────────────────────────────────────────────────────────────────
//
&НаСервереБезКонтекста
Функция БайтыUTF8ВСтроку(Байты)
	// Преобразует массив байтов UTF-8 в строку
	// Поддерживает 1-2-3 байтовые последовательности
	// Continuation bytes должны быть в диапазоне 128..191 (0x80..0xBF)
	Результат = "";
	Индекс = 0;

	Пока Индекс < Байты.Количество() Цикл
		Байт1 = Байты[Индекс];

		Если Байт1 < 128 Тогда
			// ASCII - 1 байт
			Результат = Результат + Символ(Байт1);
			Индекс = Индекс + 1;
		ИначеЕсли Байт1 >= 192 И Байт1 < 224 Тогда
			// 2 байта
			Если Индекс + 1 < Байты.Количество() Тогда
				Байт2 = Байты[Индекс + 1];
				// Continuation byte: 128..191
				Если Байт2 >= 128 И Байт2 < 192 Тогда
					КодСимвола = ((Байт1 - 192) * 64) + (Байт2 - 128);
					Результат = Результат + Символ(КодСимвола);
				КонецЕсли;
			КонецЕсли;
			Индекс = Индекс + 2;
		ИначеЕсли Байт1 >= 224 И Байт1 < 240 Тогда
			// 3 байта (кириллица)
			Если Индекс + 2 < Байты.Количество() Тогда
				Байт2 = Байты[Индекс + 1];
				Байт3 = Байты[Индекс + 2];
				// Continuation bytes: 128..191
				Если Байт2 >= 128 И Байт2 < 192 И Байт3 >= 128 И Байт3 < 192 Тогда
					КодСимвола = ((Байт1 - 224) * 4096) + ((Байт2 - 128) * 64) + (Байт3 - 128);
					Результат = Результат + Символ(КодСимвола);
				КонецЕсли;
			КонецЕсли;
			Индекс = Индекс + 3;
		Иначе
			// Некорректная последовательность - пропускаем
			Индекс = Индекс + 1;
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// РазобратьСтрокуПараметровURL82
// Разбирает параметры из URL-строки без использования СтрРазделить (совместимость 8.2)
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   СтрокаПараметров - Строка - строка параметров после ? (например, "ID=5&Type=A")
//
// Возвращаемое значение:
//   Массив - массив структур с полями Имя и Значение
//
// ПРИМЕЧАНИЕ: URL-decoding ПОДДЕРЖИВАЕТСЯ (%20, %2B и т.д.) через функцию URLDecode
//
&НаСервереБезКонтекста
Функция РазобратьСтрокуПараметровURL82(СтрокаПараметров)

	Результат = Новый Массив;
	Остаток = СтрокаПараметров;

	Пока НЕ ПустаяСтрока(Остаток) Цикл
		ПозА = Найти(Остаток, "&");
		Если ПозА = 0 Тогда
			Фрагмент = Остаток;
			Остаток = "";
		Иначе
			Фрагмент = Лев(Остаток, ПозА - 1);
			Остаток = Сред(Остаток, ПозА + 1);
		КонецЕсли;

		Если ПустаяСтрока(Фрагмент) Тогда
			Продолжить;
		КонецЕсли;

		ПозР = Найти(Фрагмент, "=");
		Если ПозР = 0 Тогда
			Продолжить;
		КонецЕсли;

		Пар = Новый Структура("Имя, Значение",
			URLDecode(Лев(Фрагмент, ПозР - 1)),    // Декодируем имя параметра
			URLDecode(Сред(Фрагмент, ПозР + 1)));  // Декодируем значение параметра

		Если НЕ ПустаяСтрока(Пар.Имя) Тогда
			Результат.Добавить(Пар);
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьТипыКлючевыхПолейВнешнейТаблицы
// Получает типы ключевых полей таблицы внешнего источника данных из метаданных
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ПолноеИмяТаблицы - Строка - полное имя таблицы (ВнешнийИсточникДанных.Источник.Таблица.Имя)
//
// Возвращаемое значение:
//   Структура - отображение ИмяПоля -> ОписаниеТипов, или Неопределено если таблица не найдена
//
&НаСервереБезКонтекста
Функция ПолучитьТипыКлючевыхПолейВнешнейТаблицы(ПолноеИмяТаблицы)

	// Разбираем путь, требуем ровно 4 сегмента
	Части = Новый Массив;
	Остаток = ПолноеИмяТаблицы;
	Пока НЕ ПустаяСтрока(Остаток) Цикл
		Поз = Найти(Остаток, ".");
		Если Поз = 0 Тогда
			Части.Добавить(Остаток);
			Остаток = "";
		Иначе
			Части.Добавить(Лев(Остаток, Поз - 1));
			Остаток = Сред(Остаток, Поз + 1);
		КонецЕсли;
	КонецЦикла;

	Если Части.Количество() <> 4 Тогда
		Возврат Неопределено;
	КонецЕсли;

	ИмяИсточника = Части[1];
	ИмяТаблицы   = Части[3];

	// Паттерн .Найти() <> Неопределено — как в остальном коде модуля
	МетаИсточника = Метаданные.ВнешниеИсточникиДанных.Найти(ИмяИсточника);
	Если МетаИсточника = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	МетаТаблицы = МетаИсточника.Таблицы.Найти(ИмяТаблицы);
	Если МетаТаблицы = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Результат = Новый Структура;
	// ПоляКлюча возвращает коллекцию объектов типа Поле, не строк
	Для Каждого МетаКлючПоле Из МетаТаблицы.ПоляКлюча Цикл
		ИмяКлючевогоПоля = МетаКлючПоле.Имя;
		// Находим поле в .Поля чтобы получить тип
		МетаПоля = МетаТаблицы.Поля.Найти(ИмяКлючевогоПоля);
		Если МетаПоля <> Неопределено Тогда
			Результат.Вставить(ИмяКлючевогоПоля, МетаПоля.Тип);
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьПоляКлючаВнешнейТаблицы
// Возвращает массив имён ключевых полей таблицы внешнего источника данных
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ПолноеИмяТаблицы - Строка - полное имя таблицы (ВнешнийИсточникДанных.Source.Таблица.TableName)
//
// Возвращаемое значение:
//   Массив - массив имён ключевых полей, или Неопределено если таблица не найдена
//
&НаСервереБезКонтекста
Функция ПолучитьПоляКлючаВнешнейТаблицы(ПолноеИмяТаблицы)
	Части = Новый Массив;
	Остаток = ПолноеИмяТаблицы;
	Пока НЕ ПустаяСтрока(Остаток) Цикл
		Поз = Найти(Остаток, ".");
		Если Поз = 0 Тогда Части.Добавить(Остаток); Остаток = "";
		Иначе Части.Добавить(Лев(Остаток, Поз - 1)); Остаток = Сред(Остаток, Поз + 1);
		КонецЕсли;
	КонецЦикла;
	Если Части.Количество() <> 4 Тогда
		Возврат Неопределено;
	КонецЕсли;

	ИмяИсточника = Части[1];
	ИмяТаблицы   = Части[3];

	МетаИсточника = Метаданные.ВнешниеИсточникиДанных.Найти(ИмяИсточника);
	Если МетаИсточника = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	МетаТаблицы = МетаИсточника.Таблицы.Найти(ИмяТаблицы);
	Если МетаТаблицы = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Результат = Новый Массив;
	Для Каждого МетаКлючПоле Из МетаТаблицы.ПоляКлюча Цикл
		Результат.Добавить(МетаКлючПоле.Имя);
	КонецЦикла;

	Возврат Результат;
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПривестиЗначениеКТипуПоляВнешнегоИсточника
// Приводит строковое значение к типу поля внешнего источника данных
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ЗначениеСтрокой - Строка - значение в строковом представлении
//   ОписаниеТипов   - ОписаниеТипов - описание типов поля из метаданных
//
// Возвращаемое значение:
//   Произвольный - значение приведённое к соответствующему типу
//
&НаСервереБезКонтекста
Функция ПривестиЗначениеКТипуПоляВнешнегоИсточника(ЗначениеСтрокой, ОписаниеТипов)

	Если ОписаниеТипов = Неопределено Тогда
		Возврат ЗначениеСтрокой;
	КонецЕсли;

	// Приоритет приведения: Число → Дата → UUID → Булево → Строка
	// Это предотвращает случайное превращение "1" в Истина

	// 1. Число (приоритет чтобы "1" не стало Истина)
	Если ОписаниеТипов.СодержитТип(Тип("Число")) Тогда
		Попытка
			Возврат Число(ЗначениеСтрокой);
		Исключение
		КонецПопытки;
	КонецЕсли;

	// 2. Дата
	Если ОписаниеТипов.СодержитТип(Тип("Дата")) Тогда
		Попытка
			Возврат Дата(ЗначениеСтрокой);
		Исключение
		КонецПопытки;
	КонецЕсли;

	// 3. UUID
	Если ОписаниеТипов.СодержитТип(Тип("УникальныйИдентификатор")) Тогда
		Попытка
			Возврат Новый УникальныйИдентификатор(ЗначениеСтрокой);
		Исключение
		КонецПопытки;
	КонецЕсли;

	// 4. Булево (только explicit true/false)
	Если ОписаниеТипов.СодержитТип(Тип("Булево")) Тогда
		Попытка
			Если НРег(ЗначениеСтрокой) = "true" Тогда
				Возврат Истина;
			КонецЕсли;
			Если НРег(ЗначениеСтрокой) = "false" Тогда
				Возврат Ложь;
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;

	// 5. Строка (fallback)
	Если ОписаниеТипов.СодержитТип(Тип("Строка")) Тогда
		Возврат ЗначениеСтрокой;
	КонецЕсли;

	// Если ничего не подошло, возвращаем как есть
	Возврат ЗначениеСтрокой;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьЗаписьВнешнегоИсточника
// Получает данные записи внешнего источника данных по навигационной ссылке
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   НавигационнаяСсылка - Строка - навигационная ссылка формата
//                               e1cib/data/ВнешнийИсточникДанных.Источник.Таблица.Имя?Поле=Значение
//
// Возвращаемое значение:
//   Структура - с полями:
//     * success - Булево - признак успешного выполнения
//     * data    - Структура - данные записи (при success = Истина)
//     * error   - Строка - описание ошибки (при success = Ложь)
//
&НаСервереБезКонтекста
Функция ПолучитьЗаписьВнешнегоИсточника(НавигационнаяСсылка)

	Попытка
		Префикс = "e1cib/data/";
		ХвостБезПрефикса = Сред(НавигационнаяСсылка, СтрДлина(Префикс) + 1);
		ПозВопрос = Найти(ХвостБезПрефикса, "?");
		ПолноеИмяТаблицы = Лев(ХвостБезПрефикса, ПозВопрос - 1);
		СтрокаПараметров = Сред(ХвостБезПрефикса, ПозВопрос + 1);

		ПараметрыКлюча = РазобратьСтрокуПараметровURL82(СтрокаПараметров);
		Если ПараметрыКлюча.Количество() = 0 Тогда
			Возврат Новый Структура("success, error", Ложь, "Не найдены параметры ключа в ссылке");
		КонецЕсли;

		// Проверяем дубликаты имён параметров
		ИменаПараметров = Новый Структура;
		Для Каждого Пар Из ПараметрыКлюча Цикл
			Если ИменаПараметров.Свойство(Пар.Имя) Тогда
				Возврат Новый Структура("success, error", Ложь,
					"Дублирующийся параметр ключа: " + Пар.Имя);
			КонецЕсли;
			ИменаПараметров.Вставить(Пар.Имя, Истина);
		КонецЦикла;

		// === Маппинг ref для объектных таблиц ===
		// Если параметр один и его имя = "ref" — это системный параметр для объектных таблиц
		Если ПараметрыКлюча.Количество() = 1 И ПараметрыКлюча[0].Имя = "ref" Тогда
			// Получаем список ключевых полей из метаданных
			ИменаКлючевыхПолей = ПолучитьПоляКлючаВнешнейТаблицы(ПолноеИмяТаблицы);
			Если ИменаКлючевыхПолей = Неопределено ИЛИ ИменаКлючевыхПолей.Количество() = 0 Тогда
				Возврат Новый Структура("success, error", Ложь,
					"Таблица внешнего источника не найдена или не имеет ключевых полей: " + ПолноеИмяТаблицы);
			КонецЕсли;

			// Маппим только если у таблицы РОВНО ОДНО ключевое поле
			Если ИменаКлючевыхПолей.Количество() = 1 Тогда
				ИмяРеальногоКлюча = ИменаКлючевыхПолей[0];
				ЗначениеРеф = ПараметрыКлюча[0].Значение;
				// Пересоздаём элемент массива с корректным именем
				ПараметрыКлюча[0] = Новый Структура("Имя, Значение", ИмяРеальногоКлюча, ЗначениеРеф);
			Иначе
				Возврат Новый Структура("success, error", Ложь,
					"Параметр 'ref' используется только для таблиц с одним ключевым полем, " +
					"а таблица имеет " + ИменаКлючевыхПолей.Количество() + " ключевых полей");
			КонецЕсли;
		КонецЕсли;

		// Получаем типы ключевых полей из метаданных
		ОписаниеКлючей = ПолучитьТипыКлючевыхПолейВнешнейТаблицы(ПолноеИмяТаблицы);
		Если ОписаниеКлючей = Неопределено ИЛИ ОписаниеКлючей.Количество() = 0 Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Не удалось получить описание ключевых полей таблицы: " + ПолноеИмяТаблицы);
		КонецЕсли;

		// БЕЗОПАСНОСТЬ: проверяем что параметры из URL соответствуют ключевым полям
		// 1. Нет лишних параметров
		Для Каждого Пар Из ПараметрыКлюча Цикл
			Если НЕ ОписаниеКлючей.Свойство(Пар.Имя) Тогда
				Возврат Новый Структура("success, error", Ложь,
					"Параметр '" + Пар.Имя + "' не является ключевым полем таблицы");
			КонецЕсли;
		КонецЦикла;

		// 2. Нет пропущенных ключевых полей
		Для Каждого КлючПоле Из ОписаниеКлючей Цикл
			НайденоВПараметрах = Ложь;
			Для Каждого Пар Из ПараметрыКлюча Цикл
				Если Пар.Имя = КлючПоле.Ключ Тогда
					НайденоВПараметрах = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если НЕ НайденоВПараметрах Тогда
				Возврат Новый Структура("success, error", Ложь,
					"Отсутствует обязательное ключевое поле: " + КлючПоле.Ключ);
			КонецЕсли;
		КонецЦикла;

		// Строим запрос
		ТекстЗапроса =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|   Таблица.*
		|ИЗ
		|   " + ПолноеИмяТаблицы + " КАК Таблица
		|ГДЕ";

		ПервоеУсловие = Истина;
		Для Каждого Пар Из ПараметрыКлюча Цикл
			Если ПервоеУсловие Тогда
				ПервоеУсловие = Ложь;
			Иначе
				ТекстЗапроса = ТекстЗапроса + "
|   И";
			КонецЕсли;
			ТекстЗапроса = ТекстЗапроса + "
|   Таблица." + Пар.Имя + " = &" + Пар.Имя;
		КонецЦикла;

		Запрос = Новый Запрос;
		Запрос.Текст = ТекстЗапроса;

		Для Каждого Пар Из ПараметрыКлюча Цикл
			Запрос.УстановитьПараметр(Пар.Имя,
				ПривестиЗначениеКТипуПоляВнешнегоИсточника(Пар.Значение, ОписаниеКлючей[Пар.Имя]));
		КонецЦикла;

		// ВАЖНО: колонки из РезультатЗапроса, не из Выборка
		РезультатЗапроса = Запрос.Выполнить();
		Выборка = РезультатЗапроса.Выбрать();
		Если НЕ Выборка.Следующий() Тогда
			Возврат Новый Структура("success, error", Ложь, "Запись не найдена / Record not found");
		КонецЕсли;

		ДанныеОбъекта = Новый Структура;
		ДанныеОбъекта.Вставить("_type", ПолноеИмяТаблицы);
		ДанныеОбъекта.Вставить("_presentation", "");

		Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
			ДанныеОбъекта.Вставить(Колонка.Имя,
				ПреобразоватьЗначениеДляJSON(Выборка[Колонка.Имя]));
		КонецЦикла;

		Возврат Новый Структура("success, data", Истина, ДанныеОбъекта);

	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;

КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьСсылкуПоТипуИUUID
// Получает ссылку на объект 1С по типу объекта, имени и UUID
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   ТипОбъекта - Строка - тип объекта метаданных (Справочник, Документ и т.д.)
//                         Поддерживаются русские и английские имена типов
//   ИмяОбъекта - Строка - имя объекта метаданных (например, "Контрагенты")
//   UUID       - Строка - уникальный идентификатор в формате 8-4-4-4-12
//
// Возвращаемое значение:
//   СправочникСсылка, ДокументСсылка и т.д. - ссылка на объект
//
// Поддерживаемые типы объектов:
//   Русские: Справочник, Документ, ПланВидовХарактеристик, ПланСчетов,
//            ПланВидовРасчета, ПланОбмена, БизнесПроцесс, Задача
//   Английские: Catalog, Document, ChartOfCharacteristicTypes, ChartOfAccounts,
//               ChartOfCalculationTypes, ExchangePlan, BusinessProcess, Task
//
// Исключения:
//   Вызывает исключение при неподдерживаемом типе объекта
//
// Требования: 4.3, 6.2
//
&НаСервереБезКонтекста
Функция ПолучитьСсылкуПоТипуИUUID(ТипОбъекта, ИмяОбъекта, UUID)
	
	УникальныйИдентификатор = Новый УникальныйИдентификатор(UUID);
	
	Если ТипОбъекта = "Справочник" Или ТипОбъекта = "Catalog" Тогда
		Возврат Справочники[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "Документ" Или ТипОбъекта = "Document" Тогда
		Возврат Документы[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "ПланВидовХарактеристик" Или ТипОбъекта = "ChartOfCharacteristicTypes" Тогда
		Возврат ПланыВидовХарактеристик[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "ПланСчетов" Или ТипОбъекта = "ChartOfAccounts" Тогда
		Возврат ПланыСчетов[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "ПланВидовРасчета" Или ТипОбъекта = "ChartOfCalculationTypes" Тогда
		Возврат ПланыВидовРасчета[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "БизнесПроцесс" Или ТипОбъекта = "BusinessProcess" Тогда
		Возврат БизнесПроцессы[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "Задача" Или ТипОбъекта = "Task" Тогда
		Возврат Задачи[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	ИначеЕсли ТипОбъекта = "ПланОбмена" Или ТипОбъекта = "ExchangePlan" Тогда
		Возврат ПланыОбмена[ИмяОбъекта].ПолучитьСсылку(УникальныйИдентификатор);
		
	Иначе
		ВызватьИсключение "Неподдерживаемый тип объекта: " + ТипОбъекта + ". "
			+ "Поддерживаемые типы: Справочник (Catalog), Документ (Document), "
			+ "ПланВидовХарактеристик (ChartOfCharacteristicTypes), ПланСчетов (ChartOfAccounts), "
			+ "ПланВидовРасчета (ChartOfCalculationTypes), ПланОбмена (ExchangePlan), "
			+ "БизнесПроцесс (BusinessProcess), Задача (Task)";
	КонецЕсли;
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьДанныеОбъекта
// Получает все данные объекта 1С (реквизиты и табличные части) по ссылке
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   Ссылка - СправочникСсылка, ДокументСсылка и т.д. - ссылка на объект 1С
//
// Возвращаемое значение:
//   Структура - результат операции:
//     * success - Булево - признак успешного выполнения
//     * data    - Структура - данные объекта (при success = Истина):
//       * _type         - Строка - полное имя типа объекта (например, "Справочник.Контрагенты")
//       * _presentation - Строка - строковое представление ссылки
//       * Код           - Строка - код объекта (если есть)
//       * Наименование  - Строка - наименование объекта (если есть)
//       * Дата          - Дата - дата документа (если есть)
//       * Номер         - Строка - номер документа (если есть)
//       * <Реквизит>    - Произвольный - значения пользовательских реквизитов
//       * <ТабличнаяЧасть> - Массив - строки табличных частей
//     * error   - Строка - описание ошибки (при success = Ложь)
//
// Требования: 5.1, 5.2, 5.3, 5.4, 5.5
//
&НаСервереБезКонтекста
Функция ПолучитьДанныеОбъекта(Ссылка)
	
	Попытка
		Объект = Ссылка.ПолучитьОбъект();
		
		Если Объект = Неопределено Тогда
			// Отладка: показываем информацию о ссылке
			Возврат Новый Структура("success, error, debug", Ложь, 
				"Объект не найден или недостаточно прав доступа / Object not found or insufficient access rights",
				Новый Структура("СсылкаСтрока, СсылкаПустая", Строка(Ссылка), Ссылка.Пустая()));
		КонецЕсли;
		
		ДанныеОбъекта = Новый Структура;
		МетаданныеОбъекта = Ссылка.Метаданные();
		
		// Базовая информация
		ДанныеОбъекта.Вставить("_type", МетаданныеОбъекта.ПолноеИмя());
		ДанныеОбъекта.Вставить("_presentation", ОписаниеОбъектаПоСсылке(Ссылка));
		
		// Стандартные реквизиты (Код, Наименование, Дата, Номер)
		// Используем Попытка/Исключение, т.к. не все объекты имеют эти реквизиты
		Попытка
			ДанныеОбъекта.Вставить("Код", Объект.Код);
		Исключение
		КонецПопытки;
		
		Попытка
			ДанныеОбъекта.Вставить("Наименование", Объект.Наименование);
		Исключение
		КонецПопытки;
		
		Попытка
			ДанныеОбъекта.Вставить("Дата", Объект.Дата);
		Исключение
		КонецПопытки;
		
		Попытка
			ДанныеОбъекта.Вставить("Номер", Объект.Номер);
		Исключение
		КонецПопытки;
		
		// Пользовательские реквизиты из метаданных
		Для Каждого Реквизит Из МетаданныеОбъекта.Реквизиты Цикл
			Значение = Объект[Реквизит.Имя];
			ДанныеОбъекта.Вставить(Реквизит.Имя, ПреобразоватьЗначениеДляJSON(Значение));
		КонецЦикла;
		
		// Табличные части
		Для Каждого ТабличнаяЧасть Из МетаданныеОбъекта.ТабличныеЧасти Цикл
			МассивСтрок = Новый Массив;
			
			Для Каждого СтрокаТЧ Из Объект[ТабличнаяЧасть.Имя] Цикл
				ДанныеСтроки = Новый Структура;
				ДанныеСтроки.Вставить("НомерСтроки", СтрокаТЧ.НомерСтроки);
				
				Для Каждого Реквизит Из ТабличнаяЧасть.Реквизиты Цикл
					Значение = СтрокаТЧ[Реквизит.Имя];
					ДанныеСтроки.Вставить(Реквизит.Имя, ПреобразоватьЗначениеДляJSON(Значение));
				КонецЦикла;
				
				МассивСтрок.Добавить(ДанныеСтроки);
			КонецЦикла;
			
			ДанныеОбъекта.Вставить(ТабличнаяЧасть.Имя, МассивСтрок);
		КонецЦикла;
		
		Возврат Новый Структура("success, data", Истина, ДанныеОбъекта);
		
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПреобразоватьЗначениеДляJSON
// Преобразует значение 1С в формат, совместимый с JSON
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   Значение - Произвольный - значение для преобразования
//
// Возвращаемое значение:
//   Произвольный - преобразованное значение:
//     * Ссылки (СправочникСсылка, ДокументСсылка и т.д.) -> Строка (представление)
//     * Дата -> Дата (без изменений, JSON сериализатор обработает)
//     * Строка, Число, Булево -> без изменений
//     * Неопределено -> Неопределено (JSON сериализатор преобразует в null)
//     * Прочие типы -> Строка (строковое представление)
//
// Требования: 5.4
//
&НаСервереБезКонтекста
Функция ПреобразоватьЗначениеДляJSON(Значение)
	
	// Неопределено -> оставляем как есть (JSON сериализатор преобразует в null)
	Если Значение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТипЗначения = ТипЗнч(Значение);
	
	// Примитивные типы - оставляем без изменений
	Если ТипЗначения = Тип("Строка") Тогда
		Возврат Значение;
	ИначеЕсли ТипЗначения = Тип("Число") Тогда
		Возврат Значение;
	ИначеЕсли ТипЗначения = Тип("Булево") Тогда
		Возврат Значение;
	ИначеЕсли ТипЗначения = Тип("Дата") Тогда
		// Дата - оставляем как есть, JSON сериализатор обработает
		Возврат Значение;
	КонецЕсли;
	
	// Перечисления - преобразуем в строку (проверяем ДО общего ссылочного блока,
	// т.к. перечисления тоже имеют Пустая() и иначе ушли бы в ссылочную ветку)
	ОписаниеТипа = Строка(ТипЗначения);
	Если Найти(ОписаниеТипа, "ПеречислениеСсылка") > 0 Тогда
		Возврат Строка(Значение);
	КонецЕсли;

	// Проверка на ссылочные типы
	// Ссылки имеют метод Пустая() и Метаданные()
	Попытка
		Если Значение.Пустая() Тогда
			// Пустая ссылка - возвращаем пустую строку
			Возврат "";
		КонецЕсли;
		// Непустая ссылка - возвращаем objectRef-структуру для анонимизации
		Возврат ОписаниеОбъектаПоСсылке(Значение);
	Исключение
		// Не ссылочный тип - продолжаем проверку
	КонецПопытки;
	
	// ФиксированныйМассив - преобразуем в обычный Массив
	Если ТипЗначения = Тип("ФиксированныйМассив") Тогда
		РезультатМассив = Новый Массив;
		Для Каждого Элемент Из Значение Цикл
			РезультатМассив.Добавить(ПреобразоватьЗначениеДляJSON(Элемент));
		КонецЦикла;
		Возврат РезультатМассив;
	КонецЕсли;
	
	// ОписаниеЗащитыОтОпасныхДействий (UnsafeOperationProtectionDescription) -> Булево
	// Возвращаем флаг ПредупреждатьОбОпасныхДействиях, чтобы в JSON было true/false
	Если ОписаниеТипа = "ОписаниеЗащитыОтОпасныхДействий" Тогда
		Попытка
			Возврат Значение.ПредупреждатьОбОпасныхДействиях;
		Исключение
			Возврат Строка(Значение);
		КонецПопытки;
	КонецЕсли;
	
	// Все остальные типы - преобразуем в строку
	Возврат Строка(Значение);
	
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// ПолучитьОбъектПоНавигационнойСсылке
// Главная функция для получения данных объекта 1С по навигационной ссылке
// Совместимость: 1С:Предприятие 8.2.13+
// ─────────────────────────────────────────────────────────────────────────────────
//
// Параметры:
//   НавигационнаяСсылка - Строка - навигационная ссылка в формате
//                                  e1cib/data/ТипОбъекта.ИмяОбъекта?ref=HexGUID
//                                  Пример: "e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5"
//
// Возвращаемое значение:
//   Структура - результат операции:
//     * success - Булево - признак успешного выполнения
//     * data    - Структура - данные объекта (при success = Истина):
//       * _type         - Строка - полное имя типа объекта
//       * _presentation - Строка - строковое представление ссылки
//       * <Реквизиты>   - Произвольный - значения реквизитов объекта
//       * <ТабличныеЧасти> - Массив - строки табличных частей
//     * error   - Строка - описание ошибки (при success = Ложь)
//
// Алгоритм работы:
//   1. Использует ручной парсинг через НайтиОбъектВручную (универсальный метод)
//   2. Получает данные объекта через ПолучитьДанныеОбъекта
//   3. Обрабатывает ошибки и возвращает результат в стандартном формате
//
// Примечание:
//   Встроенный метод НайтиПоНавигационнойСсылке недоступен в контексте
//   &НаСервереБезКонтекста, поэтому используется универсальный ручной парсинг,
//   который работает на всех версиях платформы и во всех контекстах.
//
// Требования: 3.2, 3.3, 6.3, 6.4
//
&НаСервереБезКонтекста
Функция ПолучитьОбъектПоНавигационнойСсылке(НавигационнаяСсылка)
	
	Попытка
		// Определяем тип по пути — до '?', независимо от наличия '?'
		Префикс = "e1cib/data/";
		ХвостБезПрефикса = Сред(НавигационнаяСсылка, СтрДлина(Префикс) + 1);
		ПозВопрос = Найти(ХвостБезПрефикса, "?");
		ПутьОбъекта = ?(ПозВопрос > 0, Лев(ХвостБезПрефикса, ПозВопрос - 1), ХвостБезПрефикса);

		Если НРег(Лев(ПутьОбъекта, СтрДлина("внешнийисточникданных."))) = "внешнийисточникданных." Тогда
			// Внешний источник данных — используем специализированную функцию
			Возврат ПолучитьЗаписьВнешнегоИсточника(НавигационнаяСсылка);
		КонецЕсли;

		// Существующая логика для обычных объектов
		// Парсинг ссылки для отладки
		РезультатПарсинга = РазобратьНавигационнуюСсылку(НавигационнаяСсылка);
		ТипОбъекта = РезультатПарсинга.ТипОбъекта;
		ИмяОбъекта = РезультатПарсинга.ИмяОбъекта;
		HexGUID = РезультатПарсинга.HexGUID;

		// Используем метод через внутреннее представление ссылки
		Ссылка = НайтиОбъектВручную(НавигационнаяСсылка);

		// Проверка результата поиска
		Если Ссылка = Неопределено Тогда
			Возврат Новый Структура("success, error, debug", Ложь,
				"Объект не найден по указанной ссылке / Object not found by specified link",
				Новый Структура("ТипОбъекта, ИмяОбъекта, HexGUID", ТипОбъекта, ИмяОбъекта, HexGUID));
		КонецЕсли;

		// Проверка на пустую ссылку
		Попытка
			Если Ссылка.Пустая() Тогда
				Возврат Новый Структура("success, error, debug", Ложь,
					"Ссылка пустая / Reference is empty",
					Новый Структура("ТипОбъекта, ИмяОбъекта, HexGUID", ТипОбъекта, ИмяОбъекта, HexGUID));
			КонецЕсли;
		Исключение
			// Если метод Пустая() недоступен, продолжаем
		КонецПопытки;

		// Получение данных объекта
		Возврат ПолучитьДанныеОбъекта(Ссылка);
		
	Исключение
		Возврат Новый Структура("success, error", Ложь, ОписаниеОшибки());
	КонецПопытки;
	
КонецФункции


// ═══════════════════════════════════════════════════════════════════════════════
// JSON ПАРСЕР И СЕРИАЛИЗАТОР
// Встроенный модуль для совместимости с режимом 8.2.13
// Основан на модуле Александра Переверзева (MIT License)
// ═══════════════════════════════════════════════════════════════════════════════

// JSON парсер
&НаКлиентеНаСервереБезКонтекста
Функция ПрочитатьJSON2(Значение, Стандарт = Истина, ПредставленияСсылок = Ложь)
	
	Возврат jsonПрочитатьИнициализация(Значение, Стандарт, ПредставленияСсылок);
	
КонецФункции

// JSON сериализатор
&НаКлиентеНаСервереБезКонтекста
Функция ЗаписатьJSON2(Значение, Стандарт = Истина, ПредставленияСсылок = Ложь)

	Возврат jsonЗаписатьИнициализация(Значение, Стандарт, ПредставленияСсылок);

КонецФункции

// Нормализация параметра-массива: строка вида ["X","Y"] → Массив.
// REST- или MCP-клиент может передать JSON-массив сериализованным в строку.
// Python-прокси обрабатывал это через Pydantic; здесь делаем то же самое.
&НаКлиентеНаСервереБезКонтекста
Функция НормализоватьМассивПараметра(Значение)

	Если ТипЗнч(Значение) = Тип("Строка") И Лев(СокрЛП(Значение), 1) = "[" Тогда
		Попытка
			Разобранный = ПрочитатьJSON2(Значение);
			Если ТипЗнч(Разобранный) = Тип("Массив") Тогда
				Возврат Разобранный;
			КонецЕсли;
		Исключение
			// Оставляем как есть — ошибка выйдет позднее с понятным сообщением
		КонецПопытки;
	КонецЕсли;

	Возврат Значение;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// НАСТРОЙКИ JSON
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция НастройкаАвтоматическоеПриведениеОбъектаКСтруктуре()
	Возврат Истина;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НастройкаПолноеМаскированиеСимволов()
	Возврат Истина;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НастройкаМаскированиеКириллицы()
	Возврат Ложь;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НастройкаНеявноеПриведениеПримитивныхЗначенийКлюча()
	Возврат Ложь;
КонецФункции


// ─────────────────────────────────────────────────────────────────────────────────
// JSON ПАРСЕР
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция jsonПрочитатьИнициализация(Значение, Знач Стандарт, Знач ПредставленияСсылок)
	
	Альтернативный = (Стандарт = Неопределено) Или (Не Стандарт);
	Стандарт = (Стандарт = Неопределено) Или Стандарт;
	ПредставленияСсылок = (ПредставленияСсылок = Неопределено) Или ПредставленияСсылок;
	
	// Схема подстановок шестнадцатиричной системы
	СхемаПодстановок = Новый Соответствие;
	ШестнадцатиричнаяСистема = "0123456789abcdef";
	ДесятичноеЧисло = 0;
	Для ВторойРазряд = 1 По 16 Цикл
		Для ПервыйРазряд = 1 По 16 Цикл
			СхемаПодстановок.Вставить(Сред(ШестнадцатиричнаяСистема, ВторойРазряд, 1) + Сред(ШестнадцатиричнаяСистема, ПервыйРазряд, 1), ДесятичноеЧисло);
			ДесятичноеЧисло = ДесятичноеЧисло + 1;
		КонецЦикла;
	КонецЦикла;
	
	ВспомогательныеДанные = Новый Структура("ТипСтроки,СхемаПодстановок,АвтоматическиПриводитьКСтруктуре",
		Тип("Строка"),
		СхемаПодстановок,
		(НастройкаАвтоматическоеПриведениеОбъектаКСтруктуре() = Истина));
	
	Индекс = 1;
	Длина = СтрДлина(Значение);
	СимволыФорматирования = " " + Символы.ВК + Символы.ПС + Символы.Таб;
	
	Если Стандарт Тогда
		jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
	КонецЕсли;
	
	Если (Индекс > Длина) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат jsonПрочитать(Значение, Стандарт, Альтернативный, ПредставленияСсылок, Индекс, Длина, ВспомогательныеДанные, СимволыФорматирования, Истина);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования)
	Пока (Индекс <= Длина) И Найти(СимволыФорматирования, Сред(Значение, Индекс, 1)) Цикл
		Индекс = Индекс + 1;
	КонецЦикла;
КонецПроцедуры


&НаКлиентеНаСервереБезКонтекста
Функция jsonПрочитать(Значение, Стандарт, Альтернативный, ПредставленияСсылок, Индекс, Длина, ВспомогательныеДанные, СимволыФорматирования, ПервыйУровень)
	
	Символ = Сред(Значение, Индекс, 1);
	
	Если (Символ = """") Или (Символ = "'") Тогда
		// Строка
		Начало = Индекс;
		Пока Истина Цикл
			Подстрока = Сред(Значение, Индекс + 1, 384);
			Позиция = Найти(Подстрока, Символ);
			
			Если (Позиция > 0) Тогда
				Индекс = Индекс + Позиция;
				Откат = Индекс - 1;
				Маскировка = Ложь;
				Пока (Сред(Значение, Откат, 1) = "\") И Откат Цикл
					Маскировка = Не Маскировка;
					Откат = Откат - 1;
				КонецЦикла;
				Если НЕ Маскировка Тогда
					Прервать;
				КонецЕсли;
			Иначе
				Индекс = Индекс + 384;
				Если Индекс >= Длина Тогда
					Возврат Неопределено;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		Результат = jsonПрочитатьСтроку(Сред(Значение, Начало + 1, Индекс - Начало - 1), Стандарт, Начало, ВспомогательныеДанные.СхемаПодстановок, (Символ = "'"));
		Индекс = Индекс + 1;
		
	ИначеЕсли (Символ = "[") Тогда
		// Массив
		Результат = Новый Массив;
		Индекс = Индекс + 1;
		Если Стандарт Тогда
			jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
		КонецЕсли;
		
		Символ = Сред(Значение, Индекс, 1);
		Если (Символ = "]") Тогда
			Индекс = Индекс + 1;
		Иначе
			Пока (Индекс <= Длина) Цикл
				Результат.Добавить(jsonПрочитать(Значение, Стандарт, Альтернативный, ПредставленияСсылок, Индекс, Длина, ВспомогательныеДанные, СимволыФорматирования, Ложь));
				
				Символ = Сред(Значение, Индекс, 1);
				Если (Символ = "]") Тогда
					Индекс = Индекс + 1;
					Прервать;
				ИначеЕсли (Символ = ",") Тогда
					Индекс = Индекс + 1;
					Если Стандарт Тогда
						jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
					КонецЕсли;
				Иначе
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
	ИначеЕсли (Символ = "{") Тогда
		// Объект
		Индекс = Индекс + 1;
		Если Стандарт Тогда
			jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
		КонецЕсли;
		
		Символ = Сред(Значение, Индекс, 1);
		Если (Символ = "}") Тогда
			Индекс = Индекс + 1;
			Если ВспомогательныеДанные.АвтоматическиПриводитьКСтруктуре Тогда
				Результат = Новый Структура;
			Иначе
				Результат = Новый Соответствие;
			КонецЕсли;
		Иначе
			Результат = Новый Соответствие;
			ТипСтроки = ВспомогательныеДанные.ТипСтроки;
			
			Пока (Индекс <= Длина) Цикл
				КлючЭлемента = jsonПрочитать(Значение, Стандарт, Альтернативный, ПредставленияСсылок, Индекс, Длина, ВспомогательныеДанные, СимволыФорматирования, Ложь);
				
				Символ = Сред(Значение, Индекс, 1);
				Если (Символ = ":") Тогда
					Индекс = Индекс + 1;
					Если Стандарт Тогда
						jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
					КонецЕсли;
				Иначе
					Прервать;
				КонецЕсли;
				
				ЗначениеЭлемента = jsonПрочитать(Значение, Стандарт, Альтернативный, ПредставленияСсылок, Индекс, Длина, ВспомогательныеДанные, СимволыФорматирования, Ложь);
				Результат.Вставить(КлючЭлемента, ЗначениеЭлемента);
				
				Символ = Сред(Значение, Индекс, 1);
				Если (Символ = "}") Тогда
					Индекс = Индекс + 1;
					Прервать;
				ИначеЕсли (Символ = ",") Тогда
					Индекс = Индекс + 1;
					Если Стандарт Тогда
						jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
					КонецЕсли;
				Иначе
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			// Преобразование в структуру
			Если ВспомогательныеДанные.АвтоматическиПриводитьКСтруктуре Тогда
				РезультатСтруктура = Новый Структура;
				Для Каждого Элемент Из Результат Цикл
					Попытка
						РезультатСтруктура.Вставить(Элемент.Ключ, Элемент.Значение);
					Исключение
						РезультатСтруктура = Результат;
						Прервать;
					КонецПопытки;
				КонецЦикла;
				Результат = РезультатСтруктура;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		// Примитивные типы
		Если (Символ = "n") Тогда
			Если (Сред(Значение, Индекс, 4) = "null") Тогда
				Индекс = Индекс + 4;
				Результат = Null;
			Иначе
				Результат = Неопределено;
			КонецЕсли;
		ИначеЕсли (Символ = "t") Тогда
			Если (Сред(Значение, Индекс, 4) = "true") Тогда
				Индекс = Индекс + 4;
				Результат = Истина;
			Иначе
				Результат = Неопределено;
			КонецЕсли;
		ИначеЕсли (Символ = "f") Тогда
			Если (Сред(Значение, Индекс, 5) = "false") Тогда
				Индекс = Индекс + 5;
				Результат = Ложь;
			Иначе
				Результат = Неопределено;
			КонецЕсли;
		Иначе
			// Число
			Начало = Индекс;
			Пока Найти("-+0123456789.eE", Символ) И (Индекс <= Длина) Цикл
				Индекс = Индекс + 1;
				Символ = Сред(Значение, Индекс, 1);
			КонецЦикла;
			
			Попытка
				Результат = Число(Сред(Значение, Начало, Индекс - Начало));
			Исключение
				Результат = 0;
			КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	
	Если Стандарт Тогда
		jsonПрочитатьПропуститьФорматирование(Значение, Стандарт, Индекс, Длина, СимволыФорматирования);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции


&НаКлиентеНаСервереБезКонтекста
Функция jsonПрочитатьСтроку(Значение, Стандарт, Индекс, СхемаПодстановок, ОдинарнаяКавычка)
	
	ПоследоавтельностьПеремаскировки = "\" + Символ(65535);
	
	Результат = СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(Значение,
		"\\", ПоследоавтельностьПеремаскировки),
		"\/", "/"),
		"\b", Символ(008)),
		"\t", Символы.Таб),
		"\n", Символы.ПС),
		"\f", Символы.ПФ),
		"\r", Символы.ВК),
		"\""", """");
	
	// Демаскирование Юникод символов
	Позиция = Найти(Результат, "\u");
	Пока Позиция Цикл
		СтаршийБайт = СхемаПодстановок[НРег(Сред(Результат, Позиция + 2, 2))];
		МладшийБайт = СхемаПодстановок[НРег(Сред(Результат, Позиция + 4, 2))];
		Если (СтаршийБайт = Неопределено) Или (МладшийБайт = Неопределено) Тогда
			Прервать;
		КонецЕсли;
		Результат = СтрЗаменить(Результат, Сред(Результат, Позиция, 6), Символ(256 * СтаршийБайт + МладшийБайт));
		Позиция = Найти(Результат, "\u");
	КонецЦикла;
	
	Если ОдинарнаяКавычка Тогда
		Результат = СтрЗаменить(Результат, "\'", "'");
	КонецЕсли;
	
	Возврат СтрЗаменить(Результат, ПоследоавтельностьПеремаскировки, "\");
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// JSON СЕРИАЛИЗАТОР
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция jsonЗаписатьИнициализация(Значение, Знач Стандарт, Знач ПредставленияСсылок)
	
	ВспомогательныеДанные = Новый Структура("ПримитивныеТипы,Массивы,Структуры,Соответсвия,ТипДопустимыхКлючей",
		Новый ОписаниеТипов("Null,Булево,Число,Дата,УникальныйИдентификатор"),
		Новый ОписаниеТипов("Массив,ФиксированныйМассив"),
		Новый ОписаниеТипов("Структура,ФиксированнаяСтруктура"),
		Новый ОписаниеТипов("Структура,ФиксированнаяСтруктура,Соответствие,ФиксированноеСоответствие"),
		Новый ОписаниеТипов("Строка"));
	
	Если Стандарт Тогда
		Смещение = "";
		Отступ = " ";
		Табуляция = "    ";
		ПереносСтроки = Символы.ПС;
	Иначе
		Смещение = "";
		Отступ = "";
		Табуляция = "";
		ПереносСтроки = "";
	КонецЕсли;
	
	Возврат jsonЗаписать(Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, Смещение, Отступ, Табуляция, ПереносСтроки, (НастройкаПолноеМаскированиеСимволов() = Истина), (НастройкаМаскированиеКириллицы() = Истина));
	
КонецФункции


&НаКлиентеНаСервереБезКонтекста
Функция jsonЗаписать(Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Колонки, Смещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы)
	
	Тип = ТипЗнч(Значение);
	
	// Строка
	Если (Тип = Тип("Строка")) Тогда
		Возврат jsonЗаписатьСтроку(Значение, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
	КонецЕсли;
	
	// Неопределено и примитивные типы
	Если ВспомогательныеДанные.ПримитивныеТипы.СодержитТип(Тип) Тогда
		Если (Значение = Null) Или (Значение = Неопределено) Тогда
			Возврат "null";
		ИначеЕсли (Тип = Тип("Дата")) Тогда
			Возврат """" + Формат(Значение, "ДФ=yyyy-MM-ddTHH:mm:ss; ДП=") + "Z""";
		ИначеЕсли (Тип = Тип("УникальныйИдентификатор")) Тогда
			Возврат """" + Значение + """";
		Иначе
			Возврат Формат(Значение, "ЧРД=.; ЧН=; ЧГ=; БЛ=false; БИ=true");
		КонецЕсли;
	КонецЕсли;
	
	// Структуры
	Если ВспомогательныеДанные.Структуры.СодержитТип(Тип) Тогда
		Если Значение.Количество() Тогда
			СледующееСмещение = Смещение + Табуляция;
			Первый = Истина;
			Для Каждого Элемент Из Значение Цикл
				Если Первый Тогда
					Результат = ПереносСтроки + СледующееСмещение + """" + Элемент.Ключ + """:" + Отступ + jsonЗаписать(Элемент.Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
					Первый = Ложь;
				Иначе
					Результат = Результат + "," + ПереносСтроки + СледующееСмещение + """" + Элемент.Ключ + """:" + Отступ + jsonЗаписать(Элемент.Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
				КонецЕсли;
			КонецЦикла;
			Возврат "{" + Результат + ПереносСтроки + Смещение + "}";
		Иначе
			Возврат "{}";
		КонецЕсли;
	КонецЕсли;
	
	// Соответствия
	Если ВспомогательныеДанные.Соответсвия.СодержитТип(Тип) Тогда
		Если Значение.Количество() Тогда
			СледующееСмещение = Смещение + Табуляция;
			ТипДопустимыхКлючей = ВспомогательныеДанные.ТипДопустимыхКлючей;
			Первый = Истина;
			Для Каждого Элемент Из Значение Цикл
				Ключ = Элемент.Ключ;
				Если ТипДопустимыхКлючей.СодержитТип(ТипЗнч(Ключ)) Тогда
					Ключ = jsonЗаписатьСтроку(Строка(Ключ), ПолноеМаскированиеСимволов, МаскированиеКириллицы);
				Иначе
					Ключ = """" + Ключ + """";
				КонецЕсли;
				Если Первый Тогда
					Результат = ПереносСтроки + СледующееСмещение + Ключ + ":" + Отступ + jsonЗаписать(Элемент.Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
					Первый = Ложь;
				Иначе
					Результат = Результат + "," + ПереносСтроки + СледующееСмещение + Ключ + ":" + Отступ + jsonЗаписать(Элемент.Значение, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
				КонецЕсли;
			КонецЦикла;
			Возврат "{" + Результат + ПереносСтроки + Смещение + "}";
		Иначе
			Возврат "{}";
		КонецЕсли;
	КонецЕсли;
	
	// Массивы
	Если ВспомогательныеДанные.Массивы.СодержитТип(Тип) Тогда
		Если Значение.Количество() Тогда
			СледующееСмещение = Смещение + Табуляция;
			Первый = Истина;
			Для Каждого Элемент Из Значение Цикл
				Если Первый Тогда
					Результат = ПереносСтроки + СледующееСмещение + jsonЗаписать(Элемент, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
					Первый = Ложь;
				Иначе
					Результат = Результат + "," + ПереносСтроки + СледующееСмещение + jsonЗаписать(Элемент, Стандарт, ПредставленияСсылок, ВспомогательныеДанные, Неопределено, СледующееСмещение, Отступ, Табуляция, ПереносСтроки, ПолноеМаскированиеСимволов, МаскированиеКириллицы);
				КонецЕсли;
			КонецЦикла;
			Возврат "[" + Результат + ПереносСтроки + Смещение + "]";
		Иначе
			Возврат "[]";
		КонецЕсли;
	КонецЕсли;
	
	// Неподдерживаемые типы - преобразование в строку
	Возврат jsonЗаписатьСтроку(Строка(Значение), ПолноеМаскированиеСимволов, МаскированиеКириллицы);
	
КонецФункции


&НаКлиентеНаСервереБезКонтекста
Функция jsonЗаписатьСтроку(Значение, ПолноеМаскированиеСимволов, МаскированиеКириллицы)
	
	// Маскирование служебных символов
	Результат = СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(Значение,
		"\", "\\"),
		Символ(008), "\b"),
		Символы.Таб, "\t"),
		Символы.ПС, "\n"),
		Символы.ПФ, "\f"),
		Символы.ВК, "\r"),
		"""", "\""");
	
	// Маскирование специальных символов
	Результат = СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(Результат,
		Символ(00), "\u0000"),
		Символ(01), "\u0001"),
		Символ(02), "\u0002"),
		Символ(03), "\u0003"),
		Символ(04), "\u0004"),
		Символ(05), "\u0005"),
		Символ(06), "\u0006"),
		Символ(07), "\u0007"),
		Символ(11), "\u000b"),
		Символ(14), "\u000e"),
		Символ(15), "\u000f"),
		Символ(16), "\u0010"),
		Символ(17), "\u0011"),
		Символ(18), "\u0012"),
		Символ(19), "\u0013"),
		Символ(20), "\u0014"),
		Символ(21), "\u0015"),
		Символ(22), "\u0016"),
		Символ(23), "\u0017"),
		Символ(24), "\u0018"),
		Символ(25), "\u0019"),
		Символ(26), "\u001a"),
		Символ(27), "\u001b"),
		Символ(28), "\u001c"),
		Символ(29), "\u001d"),
		Символ(30), "\u001e"),
		Символ(31), "\u001f");
	
	Возврат """" + Результат + """";
	
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// КОНВЕРТАЦИЯ ССЫЛОЧНЫХ ТИПОВ
// ─────────────────────────────────────────────────────────────────────────────────

// Проверяет, является ли тип ссылочным типом данных
//
// Параметры:
//  ПроверяемыйТип - Тип - тип для проверки
//
// Возвращаемое значение:
//  Булево - Истина, если это ссылочный тип
//
&НаСервереБезКонтекста
Функция ЭтоСсылка(ПроверяемыйТип)
	
	Если ПроверяемыйТип = Тип("Неопределено") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Справочники.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или Документы.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или Перечисления.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или ПланыВидовХарактеристик.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или ПланыСчетов.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или ПланыВидовРасчета.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или БизнесПроцессы.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или Задачи.ТипВсеСсылки().СодержитТип(ПроверяемыйТип)
		Или ПланыОбмена.ТипВсеСсылки().СодержитТип(ПроверяемыйТип);
	
КонецФункции

// Возвращает строковое представление типа ссылки
//
// Параметры:
//  Тип - Тип - тип для преобразования
//
// Возвращаемое значение:
//  Строка - строковое представление типа (например, "СправочникСсылка.Контрагенты")
//
&НаСервереБезКонтекста
Функция СтроковоеПредставлениеТипа(Тип)
	
	Если Не ЭтоСсылка(Тип) Тогда
		Возврат Строка(Тип);
	КонецЕсли;
	
	ПолноеИмя = Метаданные.НайтиПоТипу(Тип).ПолноеИмя();
	ЧастиИмени = РазделитьСтрокуПоРазделителю(ПолноеИмя, ".");
	
	Если ЧастиИмени.Количество() < 2 Тогда
		Возврат Строка(Тип);
	КонецЕсли;
	
	ИмяОбъекта = ЧастиИмени[1];
	
	Если Справочники.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "СправочникСсылка." + ИмяОбъекта;
	ИначеЕсли Документы.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ДокументСсылка." + ИмяОбъекта;
	ИначеЕсли Перечисления.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ПеречислениеСсылка." + ИмяОбъекта;
	ИначеЕсли ПланыВидовХарактеристик.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ПланВидовХарактеристикСсылка." + ИмяОбъекта;
	ИначеЕсли ПланыСчетов.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ПланСчетовСсылка." + ИмяОбъекта;
	ИначеЕсли ПланыВидовРасчета.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ПланВидовРасчетаСсылка." + ИмяОбъекта;
	ИначеЕсли БизнесПроцессы.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "БизнесПроцессСсылка." + ИмяОбъекта;
	ИначеЕсли Задачи.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ЗадачаСсылка." + ИмяОбъекта;
	ИначеЕсли ПланыОбмена.ТипВсеСсылки().СодержитТип(Тип) Тогда
		Возврат "ПланОбменаСсылка." + ИмяОбъекта;
	Иначе
		Возврат Строка(Тип);
	КонецЕсли;
	
КонецФункции

// Возвращает программное имя типа прикладного объекта/менеджера (если возможно),
// например: "СправочникОбъект.Организации" или "СправочникМенеджер.Организации".
// Для примитивных типов и неподдерживаемых типов возвращает Строка(ПроверяемыйТип).
//
// Параметры:
//  ПроверяемыйТип - Тип - тип для преобразования
//
// Возвращаемое значение:
//  Строка - программное имя или строковое представление платформы
//
&НаСервереБезКонтекста
Функция СтроковоеПредставлениеПрикладногоТипа(ПроверяемыйТип)
	
	Если ПроверяемыйТип = Тип("Неопределено") Тогда
		Возврат Строка(ПроверяемыйТип);
	КонецЕсли;
	
	ПолноеИмя = "";
	Попытка
		ПолноеИмя = Метаданные.НайтиПоТипу(ПроверяемыйТип).ПолноеИмя();
	Исключение
		ПолноеИмя = "";
	КонецПопытки;
	
	Если ПустаяСтрока(ПолноеИмя) Тогда
		Возврат Строка(ПроверяемыйТип);
	КонецЕсли;
	
	ЧастиИмени = РазделитьСтрокуПоРазделителю(ПолноеИмя, ".");
	Если ЧастиИмени.Количество() < 2 Тогда
		Возврат Строка(ПроверяемыйТип);
	КонецЕсли;
	
	Префикс = ЧастиИмени[0];
	ИмяОбъекта = ЧастиИмени[1];
	
	ИмяТипаОбъекта = Префикс + "Объект." + ИмяОбъекта;
	Попытка
		Если Тип(ИмяТипаОбъекта) = ПроверяемыйТип Тогда
			Возврат ИмяТипаОбъекта;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	ИмяТипаМенеджера = Префикс + "Менеджер." + ИмяОбъекта;
	Попытка
		Если Тип(ИмяТипаМенеджера) = ПроверяемыйТип Тогда
			Возврат ИмяТипаМенеджера;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Возврат Строка(ПроверяемыйТип);
	
КонецФункции

// Возвращает полное строковое представление ОписанияТипов реквизита
// с учётом квалификаторов и программных имён ссылочных типов
//
// Параметры:
//  ОписаниеТипов - ОписаниеТипов - описание типов реквизита (.Тип)
//
// Возвращаемое значение:
//  Строка - строковое представление типов, например:
//    "Число(15, 3)", "Строка(100)", "Дата(ДатаВремя)", "Булево",
//    "СправочникСсылка.Номенклатура",
//    "Число(10, 0), СправочникСсылка.Номенклатура, ДокументСсылка.Заказ"
//
&НаСервереБезКонтекста
Функция ПолучитьСтроковоеПредставлениеОписанияТипов(ОписаниеТипов)

	МассивТипов = ОписаниеТипов.Типы();

	Если МассивТипов.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли;

	МассивПредставлений = Новый Массив;

	Для Каждого ТекущийТип Из МассивТипов Цикл

		Если ТекущийТип = Тип("Число") Тогда
			КвЧисла = ОписаниеТипов.КвалификаторыЧисла;
			ПредставлениеЧисла = "Число(" + КвЧисла.Разрядность + ", " + КвЧисла.РазрядностьДробнойЧасти;
			Если КвЧисла.ДопустимыйЗнак = ДопустимыйЗнак.Неотрицательный Тогда
				ПредставлениеЧисла = ПредставлениеЧисла + ", Неотрицательный";
			КонецЕсли;
			МассивПредставлений.Добавить(ПредставлениеЧисла + ")");

		ИначеЕсли ТекущийТип = Тип("Строка") Тогда
			КвСтроки = ОписаниеТипов.КвалификаторыСтроки;
			Если КвСтроки.ДопустимаяДлина = ДопустимаяДлина.Фиксированная Тогда
				ВидДлины = "Фиксированная";
			Иначе
				ВидДлины = "Переменная";
			КонецЕсли;
			МассивПредставлений.Добавить("Строка(" + КвСтроки.Длина + ", " + ВидДлины + ")");

		ИначеЕсли ТекущийТип = Тип("Дата") Тогда
			КвДаты = ОписаниеТипов.КвалификаторыДаты;
			Если КвДаты.ЧастиДаты = ЧастиДаты.Время Тогда
				МассивПредставлений.Добавить("Дата(Время)");
			ИначеЕсли КвДаты.ЧастиДаты = ЧастиДаты.Дата Тогда
				МассивПредставлений.Добавить("Дата(Дата)");
			Иначе
				МассивПредставлений.Добавить("Дата(ДатаВремя)");
			КонецЕсли;

		ИначеЕсли ЭтоСсылка(ТекущийТип) Тогда
			МассивПредставлений.Добавить(СтроковоеПредставлениеТипа(ТекущийТип));

		Иначе
			МассивПредставлений.Добавить(СтроковоеПредставлениеПрикладногоТипа(ТекущийТип));

		КонецЕсли;

	КонецЦикла;

	Возврат СоединитьМассив(МассивПредставлений, ", ");

КонецФункции

// Преобразует ссылку на объект в описание объекта для передачи через MCP
//
// Параметры:
//  СсылкаНаОбъект - ЛюбаяСсылка - ссылка на объект информационной базы
//
// Возвращаемое значение:
//  Структура - описание объекта с полями:
//    * _objectRef - Булево - признак описания объекта (всегда Истина)
//    * УникальныйИдентификатор - Строка - уникальный идентификатор объекта
//    * ТипОбъекта - Строка - строковое представление типа
//    * Представление - Строка - строковое представление объекта
//
&НаСервереБезКонтекста
Функция ОписаниеОбъектаПоСсылке(СсылкаНаОбъект)
	
	УникальныйИдентификатор = XMLСтрока(СсылкаНаОбъект);
	ТипОбъекта = СтроковоеПредставлениеТипа(ТипЗнч(СсылкаНаОбъект));
	Представление = Строка(СсылкаНаОбъект);
	
	ОписаниеОбъекта = Новый Структура;
	ОписаниеОбъекта.Вставить("_objectRef", Истина);
	ОписаниеОбъекта.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	ОписаниеОбъекта.Вставить("ТипОбъекта", ТипОбъекта);
	ОписаниеОбъекта.Вставить("Представление", Представление);
	
	Возврат ОписаниеОбъекта;
	
КонецФункции

// Возвращает менеджер объекта по ссылке
//
// Параметры:
//  Ссылка - ЛюбаяСсылка - ссылка на объект
//
// Возвращаемое значение:
//  СправочникМенеджер, ДокументМенеджер и т.д. - менеджер объекта
//
&НаСервереБезКонтекста
Функция МенеджерОбъектаПоСсылке(Ссылка)
	
	ИмяОбъекта = Ссылка.Метаданные().Имя;
	ТипСсылки = ТипЗнч(Ссылка);
	
	Если Справочники.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат Справочники[ИмяОбъекта];
	ИначеЕсли Документы.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат Документы[ИмяОбъекта];
	ИначеЕсли Перечисления.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат Перечисления[ИмяОбъекта];
	ИначеЕсли ПланыВидовХарактеристик.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат ПланыВидовХарактеристик[ИмяОбъекта];
	ИначеЕсли ПланыСчетов.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат ПланыСчетов[ИмяОбъекта];
	ИначеЕсли ПланыВидовРасчета.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат ПланыВидовРасчета[ИмяОбъекта];
	ИначеЕсли БизнесПроцессы.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат БизнесПроцессы[ИмяОбъекта];
	ИначеЕсли Задачи.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат Задачи[ИмяОбъекта];
	ИначеЕсли ПланыОбмена.ТипВсеСсылки().СодержитТип(ТипСсылки) Тогда
		Возврат ПланыОбмена[ИмяОбъекта];
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

// Преобразует описание объекта обратно в ссылку на объект информационной базы
//
// Параметры:
//  ОписаниеОбъекта - Структура, Произвольный - описание объекта или любое другое значение
//
// Возвращаемое значение:
//  ЛюбаяСсылка, Произвольный - ссылка на объект или исходное значение, если это не описание объекта
//
&НаСервереБезКонтекста
Функция ОбъектИнформационнойБазыПоОписанию(ОписаниеОбъекта)
	
	// Если это не структура - возвращаем как есть
	Если ТипЗнч(ОписаниеОбъекта) <> Тип("Структура") Тогда
		Возврат ОписаниеОбъекта;
	КонецЕсли;
	
	// Проверяем, что это описание объекта
	Если Не ОписаниеОбъекта.Свойство("_objectRef") 
		Или ОписаниеОбъекта._objectRef <> Истина Тогда
		Возврат ОписаниеОбъекта;
	КонецЕсли;
	
	// Проверяем наличие обязательных полей
	Если Не ОписаниеОбъекта.Свойство("УникальныйИдентификатор") 
		Или Не ОписаниеОбъекта.Свойство("ТипОбъекта") Тогда
		Возврат ОписаниеОбъекта;
	КонецЕсли;
	
	// Спецобработка перечислений: XMLСтрока возвращает имя значения, XMLЗначение — обратный вызов
	Если Лев(ОписаниеОбъекта.ТипОбъекта, 19) = "ПеречислениеСсылка." Тогда
		Попытка
			Возврат XMLЗначение(Тип(ОписаниеОбъекта.ТипОбъекта), ОписаниеОбъекта.УникальныйИдентификатор);
		Исключение
			Возврат ОписаниеОбъекта;
		КонецПопытки;
	КонецЕсли;

	Попытка
		// Создаём описание типа и получаем пустое значение нужного типа
		ОписаниеТипа = Новый ОписаниеТипов(ОписаниеОбъекта.ТипОбъекта);
		ПустоеЗначение = ОписаниеТипа.ПривестиЗначение();
		
		// Получаем менеджер объекта
		МенеджерОбъекта = МенеджерОбъектаПоСсылке(ПустоеЗначение);
		
		Если МенеджерОбъекта = Неопределено Тогда
			Возврат ОписаниеОбъекта;
		КонецЕсли;
		
		// Получаем ссылку по уникальному идентификатору
		Ссылка = МенеджерОбъекта.ПолучитьСсылку(
			Новый УникальныйИдентификатор(ОписаниеОбъекта.УникальныйИдентификатор)
		);
		
		Возврат Ссылка;
		
	Исключение
		// Если не удалось преобразовать - возвращаем исходное значение
		Возврат ОписаниеОбъекта;
	КонецПопытки;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ПОИСК ССЫЛОК НА ОБЪЕКТ (find_references_to_object)
// ─────────────────────────────────────────────────────────────────────────────────

// Главный обработчик инструмента find_references_to_object
//
// Параметры:
//  ОписаниеЦелевогоОбъекта - Структура - описание целевого объекта (_objectRef, УникальныйИдентификатор, ТипОбъекта)
//  ОбластиПоиска - Массив - массив строк: "documents", "catalogs", "information_registers" и т.д.
//  ФильтрМетаданных - Структура, Неопределено - {names?: Массив, name_mask?: Строка}
//  ЛимитНайденных - Число - максимум находок
//  ЛимитНаМетуОбъект - Число - максимум находок на один объект метаданных
//  БюджетВремениСек - Число - бюджет времени в секундах
//
// Возвращаемое значение:
//  Структура - результат с полями success, data или error
//
&НаСервереБезКонтекста
Функция НайтиСсылкиНаОбъект(ОписаниеЦелевогоОбъекта, ОбластиПоиска, ФильтрМетаданных,
	ЛимитНайденных, ЛимитНаМетуОбъект, БюджетВремениСек)

	// Валидация входных параметров
	Если ОписаниеЦелевогоОбъекта = Неопределено Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Не указан целевой объект (target_object_description) / Target object description is required");
	КонецЕсли;

	Если ОбластиПоиска = Неопределено Или ОбластиПоиска.Количество() = 0 Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Не указаны области поиска (search_scope) / Search scope is required");
	КонецЕсли;

	// Конвертация описания объекта в ссылку
	ЦелеваяСсылка = ОбъектИнформационнойБазыПоОписанию(ОписаниеЦелевогоОбъекта);

	// Проверка, что удалось конвертировать
	Если ТипЗнч(ЦелеваяСсылка) = Тип("Структура") Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Не удалось определить объект по описанию / Could not resolve object from description");
	КонецЕсли;

	// Проверка, что это ссылочный тип
	ТипЦелевойСсылки = ТипЗнч(ЦелеваяСсылка);
	Если Не ЭтоСсылка(ТипЦелевойСсылки) Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Объект не является ссылочным типом / Object is not a reference type");
	КонецЕсли;

	// Этап 1 — поиск кандидатов (полей, которые могут содержать ссылку нужного типа)
	ПропущенныеИмена = Новый Массив;
	Кандидаты = НайтиКандидатовПолей(ТипЦелевойСсылки, ОбластиПоиска, ФильтрМетаданных, ПропущенныеИмена);

	// Этап 2 — выполнение запросов по кандидатам
	Находки = Новый Массив;
	КандидатовПроверено = 0;
	ТаймаутПревышен = Ложь;
	ВремяНачала = ТекущаяУниверсальнаяДатаВМиллисекундах();
	БюджетМиллисекунд = БюджетВремениСек * 1000;

	Для Каждого Кандидат Из Кандидаты Цикл

		// Проверка таймаута
		Если (ТекущаяУниверсальнаяДатаВМиллисекундах() - ВремяНачала) >= БюджетМиллисекунд Тогда
			ТаймаутПревышен = Истина;
			Прервать;
		КонецЕсли;

		// Проверка общего лимита
		Если Находки.Количество() >= ЛимитНайденных Тогда
			Прервать;
		КонецЕсли;

		// Оставшийся лимит для этого кандидата
		ОстатокОбщий = ЛимитНайденных - Находки.Количество();
		ЛимитДляКандидата = Мин(ЛимитНаМетуОбъект, ОстатокОбщий);

		// Запрос ссылок в кандидате
		НовыеНаходки = ЗапроситьСсылкиВКандидате(ЦелеваяСсылка, Кандидат, ЛимитДляКандидата);

		Для Каждого Находка Из НовыеНаходки Цикл
			Если Находки.Количество() >= ЛимитНайденных Тогда
				Прервать;
			КонецЕсли;
			Находки.Добавить(Находка);
		КонецЦикла;

		КандидатовПроверено = КандидатовПроверено + 1;

	КонецЦикла;

	// Формирование результата
	Данные = Новый Структура;
	Данные.Вставить("hits", Находки);
	Данные.Вставить("total_hits", Находки.Количество());
	Данные.Вставить("candidates_checked", КандидатовПроверено);
	Данные.Вставить("timeout_exceeded", ТаймаутПревышен);
	Данные.Вставить("skipped_names", ПропущенныеИмена);

	Возврат Новый Структура("success, data", Истина, Данные);

КонецФункции

// Этап 1 — обход метаданных и поиск полей-кандидатов, содержащих нужный тип ссылки
//
// Параметры:
//  ТипЦелевойСсылки - Тип - тип ссылки целевого объекта
//  ОбластиПоиска - Массив - массив строк областей поиска
//  ФильтрМетаданных - Структура, Неопределено - фильтр метаданных {names, name_mask}
//  ПропущенныеИмена - Массив - (выходной) массив пропущенных имён {name, reason}
//
// Возвращаемое значение:
//  Массив - массив структур-кандидатов {meta_name, field_name, field_kind, tabular_section, has_ref}
//
&НаСервереБезКонтекста
Функция НайтиКандидатовПолей(ТипЦелевойСсылки, ОбластиПоиска, ФильтрМетаданных, ПропущенныеИмена)

	Кандидаты = Новый Массив;

	// Формирование соответствия scope → коллекция метаданных
	Для Каждого Область Из ОбластиПоиска Цикл

		Если Область = "documents" Тогда
			ПроверитьКоллекциюМетаданных(Метаданные.Документы, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты, Истина);

		ИначеЕсли Область = "catalogs" Тогда
			ПроверитьКоллекциюМетаданных(Метаданные.Справочники, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты, Истина);

		ИначеЕсли Область = "information_registers" Тогда
			ПроверитьКоллекциюРегистров(Метаданные.РегистрыСведений, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты);

		ИначеЕсли Область = "accumulation_registers" Тогда
			ПроверитьКоллекциюРегистров(Метаданные.РегистрыНакопления, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты);

		ИначеЕсли Область = "accounting_registers" Тогда
			ПроверитьКоллекциюРегистров(Метаданные.РегистрыБухгалтерии, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты);

		ИначеЕсли Область = "calculation_registers" Тогда
			ПроверитьКоллекциюРегистров(Метаданные.РегистрыРасчета, ТипЦелевойСсылки,
				ФильтрМетаданных, ПропущенныеИмена, Кандидаты);
		КонецЕсли;

	КонецЦикла;

	// Проверка skipped_names: какие имена из meta_filter.names не найдены или не соответствуют scope
	Если ФильтрМетаданных <> Неопределено
		И ТипЗнч(ФильтрМетаданных) = Тип("Структура")
		И ФильтрМетаданных.Свойство("names")
		И ФильтрМетаданных.names <> Неопределено
		И ТипЗнч(ФильтрМетаданных.names) = Тип("Массив")
		И ФильтрМетаданных.names.Количество() > 0 Тогда

		// Соответствие типов метаданных областям поиска
		СоответствиеТиповОбластям = Новый Соответствие;
		СоответствиеТиповОбластям.Вставить("Документ", "documents");
		СоответствиеТиповОбластям.Вставить("Справочник", "catalogs");
		СоответствиеТиповОбластям.Вставить("РегистрСведений", "information_registers");
		СоответствиеТиповОбластям.Вставить("РегистрНакопления", "accumulation_registers");
		СоответствиеТиповОбластям.Вставить("РегистрБухгалтерии", "accounting_registers");
		СоответствиеТиповОбластям.Вставить("РегистрРасчета", "calculation_registers");

		// Множество запрошенных областей для быстрой проверки
		МножествоОбластей = Новый Соответствие;
		Для Каждого Область Из ОбластиПоиска Цикл
			МножествоОбластей.Вставить(Область, Истина);
		КонецЦикла;

		Для Каждого ИмяФильтра Из ФильтрМетаданных.names Цикл
			// Проверяем существование объекта в метаданных
			НайденныйОбъект = Метаданные.НайтиПоПолномуИмени(ИмяФильтра);

			Если НайденныйОбъект = Неопределено Тогда
				Пропуск = Новый Структура("name, reason", ИмяФильтра,
					"не найден в метаданных / not found in metadata");
				ПропущенныеИмена.Добавить(Пропуск);
				Продолжить;
			КонецЕсли;

			// Проверяем соответствие типа метаданных областям поиска
			ЧастиИмени = РазделитьСтрокуПоРазделителю(ИмяФильтра, ".");
			Если ЧастиИмени.Количество() >= 1 Тогда
				ПрефиксТипа = ЧастиИмени[0];
				ОбластьДляТипа = СоответствиеТиповОбластям.Получить(ПрефиксТипа);

				Если ОбластьДляТипа <> Неопределено
					И МножествоОбластей.Получить(ОбластьДляТипа) = Неопределено Тогда
					Пропуск = Новый Структура("name, reason", ИмяФильтра,
						"не соответствует области поиска (search_scope) / does not match search_scope");
					ПропущенныеИмена.Добавить(Пропуск);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	Возврат Кандидаты;

КонецФункции

// Проверяет коллекцию метаданных документов/справочников на наличие полей с нужным типом ссылки
//
// Параметры:
//  КоллекцияМетаданных - КоллекцияОбъектовМетаданных - коллекция (Метаданные.Документы и т.п.)
//  ТипЦелевойСсылки - Тип - тип ссылки целевого объекта
//  ФильтрМетаданных - Структура, Неопределено - фильтр метаданных
//  ПропущенныеИмена - Массив - (выходной) массив пропущенных имён
//  Кандидаты - Массив - (выходной) массив найденных кандидатов
//  ЕстьСсылка - Булево - имеет ли объект поле Ссылка
//
&НаСервереБезКонтекста
Процедура ПроверитьКоллекциюМетаданных(КоллекцияМетаданных, ТипЦелевойСсылки,
	ФильтрМетаданных, ПропущенныеИмена, Кандидаты, ЕстьСсылка)

	Для Каждого ОбъектМетаданных Из КоллекцияМетаданных Цикл

		// Проверка фильтра метаданных
		Если Не ПрошелФильтрМетаданныхПоиска(ОбъектМетаданных, ФильтрМетаданных, ПропущенныеИмена) Тогда
			Продолжить;
		КонецЕсли;

		ПолноеИмяМета = ОбъектМетаданных.ПолноеИмя();

		// Проверка реквизитов шапки
		Для Каждого Реквизит Из ОбъектМетаданных.Реквизиты Цикл
			Если Реквизит.Тип.СодержитТип(ТипЦелевойСсылки) Тогда
				Кандидаты.Добавить(Новый Структура(
					"meta_name, field_name, field_kind, tabular_section, has_ref",
					ПолноеИмяМета, Реквизит.Имя, "attribute", "", ЕстьСсылка));
			КонецЕсли;
		КонецЦикла;

		// Проверка табличных частей
		Для Каждого ТабличнаяЧасть Из ОбъектМетаданных.ТабличныеЧасти Цикл
			Для Каждого Реквизит Из ТабличнаяЧасть.Реквизиты Цикл
				Если Реквизит.Тип.СодержитТип(ТипЦелевойСсылки) Тогда
					Кандидаты.Добавить(Новый Структура(
						"meta_name, field_name, field_kind, tabular_section, has_ref",
						ПолноеИмяМета, Реквизит.Имя, "tabular_section", ТабличнаяЧасть.Имя, ЕстьСсылка));
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;

	КонецЦикла;

КонецПроцедуры

// Проверяет коллекцию метаданных регистров на наличие полей с нужным типом ссылки
//
// Параметры:
//  КоллекцияМетаданных - КоллекцияОбъектовМетаданных - коллекция регистров
//  ТипЦелевойСсылки - Тип - тип ссылки целевого объекта
//  ФильтрМетаданных - Структура, Неопределено - фильтр метаданных
//  ПропущенныеИмена - Массив - (выходной) массив пропущенных имён
//  Кандидаты - Массив - (выходной) массив найденных кандидатов
//
&НаСервереБезКонтекста
Процедура ПроверитьКоллекциюРегистров(КоллекцияМетаданных, ТипЦелевойСсылки,
	ФильтрМетаданных, ПропущенныеИмена, Кандидаты)

	Для Каждого ОбъектМетаданных Из КоллекцияМетаданных Цикл

		// Проверка фильтра метаданных
		Если Не ПрошелФильтрМетаданныхПоиска(ОбъектМетаданных, ФильтрМетаданных, ПропущенныеИмена) Тогда
			Продолжить;
		КонецЕсли;

		ПолноеИмяМета = ОбъектМетаданных.ПолноеИмя();

		// Проверка измерений
		Для Каждого Измерение Из ОбъектМетаданных.Измерения Цикл
			Если Измерение.Тип.СодержитТип(ТипЦелевойСсылки) Тогда
				Кандидаты.Добавить(Новый Структура(
					"meta_name, field_name, field_kind, tabular_section, has_ref",
					ПолноеИмяМета, Измерение.Имя, "dimension", "", Ложь));
			КонецЕсли;
		КонецЦикла;

		// Проверка ресурсов
		Для Каждого Ресурс Из ОбъектМетаданных.Ресурсы Цикл
			Если Ресурс.Тип.СодержитТип(ТипЦелевойСсылки) Тогда
				Кандидаты.Добавить(Новый Структура(
					"meta_name, field_name, field_kind, tabular_section, has_ref",
					ПолноеИмяМета, Ресурс.Имя, "resource", "", Ложь));
			КонецЕсли;
		КонецЦикла;

		// Проверка реквизитов регистра
		Для Каждого Реквизит Из ОбъектМетаданных.Реквизиты Цикл
			Если Реквизит.Тип.СодержитТип(ТипЦелевойСсылки) Тогда
				Кандидаты.Добавить(Новый Структура(
					"meta_name, field_name, field_kind, tabular_section, has_ref",
					ПолноеИмяМета, Реквизит.Имя, "requisite", "", Ложь));
			КонецЕсли;
		КонецЦикла;

	КонецЦикла;

КонецПроцедуры

// Проверяет, проходит ли объект метаданных фильтр поиска (meta_filter)
//
// Параметры:
//  ОбъектМетаданных - ОбъектМетаданных - проверяемый объект метаданных
//  ФильтрМетаданных - Структура, Неопределено - фильтр {names?: Массив, name_mask?: Строка}
//  ПропущенныеИмена - Массив - (выходной) массив пропущенных имён (не используется при маске)
//
// Возвращаемое значение:
//  Булево - Истина, если объект прошёл фильтр
//
&НаСервереБезКонтекста
Функция ПрошелФильтрМетаданныхПоиска(ОбъектМетаданных, ФильтрМетаданных, ПропущенныеИмена)

	// Если фильтр не задан — проходит всё
	Если ФильтрМетаданных = Неопределено Тогда
		Возврат Истина;
	КонецЕсли;

	Если ТипЗнч(ФильтрМетаданных) <> Тип("Структура") Тогда
		Возврат Истина;
	КонецЕсли;

	ПолноеИмяОбъекта = ОбъектМетаданных.ПолноеИмя();

	// Приоритет 1: names (точное совпадение, регистронезависимо)
	Если ФильтрМетаданных.Свойство("names") И ФильтрМетаданных.names <> Неопределено
		И ТипЗнч(ФильтрМетаданных.names) = Тип("Массив") И ФильтрМетаданных.names.Количество() > 0 Тогда

		ПолноеИмяВерх = ВРег(ПолноеИмяОбъекта);

		Для Каждого ИмяФильтра Из ФильтрМетаданных.names Цикл
			Если ВРег(ИмяФильтра) = ПолноеИмяВерх Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;

		// Не найдено в списке имён
		Возврат Ложь;
	КонецЕсли;

	// Приоритет 2: name_mask (подстрока в имени или синониме)
	Если ФильтрМетаданных.Свойство("name_mask") И ЗначениеЗаполнено(ФильтрМетаданных.name_mask) Тогда
		МаскаВерхнийРегистр = ВРег(ФильтрМетаданных.name_mask);
		Возврат ПрошелФильтрПоМаске(ОбъектМетаданных.Имя, ОбъектМетаданных.Синоним, МаскаВерхнийРегистр);
	КонецЕсли;

	// Ни names, ни name_mask не заданы — проходит всё
	Возврат Истина;

КонецФункции

// Этап 2 — выполнение запроса для одного кандидата и формирование находок
//
// Параметры:
//  ЦелеваяСсылка - ЛюбаяСсылка - ссылка на целевой объект
//  Кандидат - Структура - описание кандидата {meta_name, field_name, field_kind, tabular_section, has_ref}
//  Лимит - Число - максимальное количество находок для этого кандидата
//
// Возвращаемое значение:
//  Массив - массив структур-находок
//
&НаСервереБезКонтекста
Функция ЗапроситьСсылкиВКандидате(ЦелеваяСсылка, Кандидат, Лимит)

	Находки = Новый Массив;

	Попытка

		ИмяМета = Кандидат.meta_name;
		ИмяПоля = Кандидат.field_name;
		ВидПоля = Кандидат.field_kind;
		ТабличнаяЧасть = Кандидат.tabular_section;
		ЕстьСсылка = Кандидат.has_ref;
		ЛимитСтрока = Формат(Лимит, "ЧГ=");

		Если ВидПоля = "attribute" И ЕстьСсылка Тогда
			// Реквизит шапки документа/справочника
			ТекстЗапроса = "ВЫБРАТЬ ПЕРВЫЕ " + ЛимитСтрока
				+ " Т.Ссылка КАК Ссылка"
				+ " ИЗ " + ИмяМета + " КАК Т"
				+ " ГДЕ Т." + ИмяПоля + " = &ЦельПоиска";

			Запрос = Новый Запрос(ТекстЗапроса);
			Запрос.УстановитьПараметр("ЦельПоиска", ЦелеваяСсылка);
			Результат = Запрос.Выполнить();
			Выборка = Результат.Выбрать();

			Пока Выборка.Следующий() Цикл
				Находка = Новый Структура;
				Находка.Вставить("found_in_meta", ИмяМета);
				Находка.Вставить("found_in_object", ОписаниеОбъектаПоСсылке(Выборка.Ссылка));
				Находка.Вставить("path", ИмяПоля);
				Находка.Вставить("match_kind", "attribute");
				Находка.Вставить("note", Строка(Выборка.Ссылка));
				Находки.Добавить(Находка);
			КонецЦикла;

		ИначеЕсли ВидПоля = "tabular_section" И ЕстьСсылка Тогда
			// Реквизит табличной части документа/справочника
			ТекстЗапроса = "ВЫБРАТЬ ПЕРВЫЕ " + ЛимитСтрока
				+ " РАЗЛИЧНЫЕ Т.Ссылка КАК Ссылка"
				+ " ИЗ " + ИмяМета + "." + ТабличнаяЧасть + " КАК Т"
				+ " ГДЕ Т." + ИмяПоля + " = &ЦельПоиска";

			Запрос = Новый Запрос(ТекстЗапроса);
			Запрос.УстановитьПараметр("ЦельПоиска", ЦелеваяСсылка);
			Результат = Запрос.Выполнить();
			Выборка = Результат.Выбрать();

			Пока Выборка.Следующий() Цикл
				Находка = Новый Структура;
				Находка.Вставить("found_in_meta", ИмяМета);
				Находка.Вставить("found_in_object", ОписаниеОбъектаПоСсылке(Выборка.Ссылка));
				Находка.Вставить("path", ТабличнаяЧасть + "." + ИмяПоля);
				Находка.Вставить("match_kind", "tabular_section");
				Находка.Вставить("note", Строка(Выборка.Ссылка));
				Находки.Добавить(Находка);
			КонецЦикла;

		Иначе
			// Регистр (dimension, resource, requisite)
			// Определяем объект метаданных регистра для построения списка полей выборки
			ОбъектМетаданныхРегистра = Метаданные.НайтиПоПолномуИмени(ИмяМета);

			Если ОбъектМетаданныхРегистра = Неопределено Тогда
				Возврат Находки;
			КонецЕсли;

			// Формируем список полей для выборки: все измерения
			ПоляВыборки = Новый Массив;
			ИменаИзмерений = Новый Массив;

			Для Каждого Измерение Из ОбъектМетаданныхРегистра.Измерения Цикл
				ПоляВыборки.Добавить("Т." + Измерение.Имя + " КАК " + Измерение.Имя);
				ИменаИзмерений.Добавить(Измерение.Имя);
			КонецЦикла;

			// Определяем тип регистра
			ЕстьПериод = Ложь;
			ПолноеИмяЧасти = РазделитьСтрокуПоРазделителю(ИмяМета, ".");
			ТипРегистра = "";
			Если ПолноеИмяЧасти.Количество() >= 1 Тогда
				ТипРегистра = ПолноеИмяЧасти[0];
			КонецЕсли;

			// Период — для периодических регистров сведений
			Если ТипРегистра = "РегистрСведений" Тогда
				Попытка
					Если ОбъектМетаданныхРегистра.ПериодичностьРегистраСведений
						<> Метаданные.СвойстваОбъектов.ПериодичностьРегистраСведений.Непериодический Тогда
						ПоляВыборки.Добавить("Т.Период КАК Период");
						ЕстьПериод = Истина;
					КонецЕсли;
				Исключение
					// Не удалось определить периодичность — пропускаем Период
				КонецПопытки;

			// Период — для регистров накопления и бухгалтерии (всегда есть)
			ИначеЕсли ТипРегистра = "РегистрНакопления"
				Или ТипРегистра = "РегистрБухгалтерии" Тогда
				ПоляВыборки.Добавить("Т.Период КАК Период");
				ЕстьПериод = Истина;
			КонецЕсли;

			// Регистратор — для регистров накопления, бухгалтерии, расчёта
			ЕстьРегистратор = Ложь;
			Если ТипРегистра = "РегистрНакопления"
				Или ТипРегистра = "РегистрБухгалтерии"
				Или ТипРегистра = "РегистрРасчета" Тогда
				ПоляВыборки.Добавить("Т.Регистратор КАК Регистратор");
				ЕстьРегистратор = Истина;
			КонецЕсли;

			Если ПоляВыборки.Количество() = 0 Тогда
				Возврат Находки;
			КонецЕсли;

			ТекстЗапроса = "ВЫБРАТЬ ПЕРВЫЕ " + ЛимитСтрока + " "
				+ СоединитьМассив(ПоляВыборки, ", ")
				+ " ИЗ " + ИмяМета + " КАК Т"
				+ " ГДЕ Т." + ИмяПоля + " = &ЦельПоиска";

			Запрос = Новый Запрос(ТекстЗапроса);
			Запрос.УстановитьПараметр("ЦельПоиска", ЦелеваяСсылка);
			Результат = Запрос.Выполнить();
			Выборка = Результат.Выбрать();

			Пока Выборка.Следующий() Цикл

				// Формируем record_key
				КлючЗаписи = Новый Структура;
				ЧастиПримечания = Новый Массив;

				Для Каждого ИмяИзмерения Из ИменаИзмерений Цикл
					ЗначениеИзмерения = Выборка[ИмяИзмерения];
					ТипЗначенияИзм = ТипЗнч(ЗначениеИзмерения);

					Если ЭтоСсылка(ТипЗначенияИзм) И ЗначениеЗаполнено(ЗначениеИзмерения) Тогда
						КлючЗаписи.Вставить(ИмяИзмерения, ОписаниеОбъектаПоСсылке(ЗначениеИзмерения));
					Иначе
						КлючЗаписи.Вставить(ИмяИзмерения, БезопасноеЗначениеДляПередачи(ЗначениеИзмерения));
					КонецЕсли;

					ЧастиПримечания.Добавить(ИмяИзмерения + "=" + Строка(ЗначениеИзмерения));
				КонецЦикла;

				// Период
				Если ЕстьПериод Тогда
					ЗначениеПериода = Выборка.Период;
					КлючЗаписи.Вставить("Период", БезопасноеЗначениеДляПередачи(ЗначениеПериода));
					ЧастиПримечания.Добавить("Период=" + Строка(ЗначениеПериода));
				КонецЕсли;

				// Регистратор
				Если ЕстьРегистратор Тогда
					ЗначениеРегистратора = Выборка.Регистратор;
					Если ЗначениеЗаполнено(ЗначениеРегистратора) Тогда
						КлючЗаписи.Вставить("Регистратор", ОписаниеОбъектаПоСсылке(ЗначениеРегистратора));
					Иначе
						КлючЗаписи.Вставить("Регистратор", Неопределено);
					КонецЕсли;
					ЧастиПримечания.Добавить("Регистратор=" + Строка(ЗначениеРегистратора));
				КонецЕсли;

				Находка = Новый Структура;
				Находка.Вставить("found_in_meta", ИмяМета);
				Находка.Вставить("found_in_object", Неопределено);
				Находка.Вставить("record_key", КлючЗаписи);
				Находка.Вставить("path", ИмяПоля);
				Находка.Вставить("match_kind", ВидПоля);
				Находка.Вставить("note", СоединитьМассив(ЧастиПримечания, "; "));
				Находки.Добавить(Находка);

			КонецЦикла;

		КонецЕсли;

	Исключение
		// Ошибка выполнения запроса — пропускаем этого кандидата
	КонецПопытки;

	Возврат Находки;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// СКРИНШОТ (get_screenshot)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция ПолучитьСкриншот(ПараметрыSnap)
	Если КомпонентаScreenCapture = Неопределено Тогда
		Возврат Новый Структура("success,error", Ложь, "ScreenCapture AddIn не загружен");
	КонецЕсли;

	НазваниеФормы = "";
	МасштабПроц = 100;
	ПоказатьСетку = Ложь;
	Если ПараметрыSnap <> Неопределено И ТипЗнч(ПараметрыSnap) = Тип("Структура") Тогда
		Если ПараметрыSnap.Свойство("form_name") Тогда
			НазваниеФормы = СокрЛП(ПараметрыSnap.form_name);
		КонецЕсли;
		Если ПараметрыSnap.Свойство("scale_percent")
			И ТипЗнч(ПараметрыSnap.scale_percent) = Тип("Число") Тогда
			МасштабПроц = Макс(10, Мин(200, Цел(ПараметрыSnap.scale_percent)));
		КонецЕсли;
		Если ПараметрыSnap.Свойство("show_grid") Тогда
			ПоказатьСетку = ПараметрыSnap.show_grid; // уже Булево после нормализации в валидаторе
		КонецЕсли;
	КонецЕсли;

	РегионX = -1;
	РегионY = -1;
	РегионШирина = -1;
	РегионВысота = -1;
	Если ПараметрыSnap <> Неопределено И ТипЗнч(ПараметрыSnap) = Тип("Структура")
		И ПараметрыSnap.Свойство("region") Тогда
		РегЗн = ПараметрыSnap.region;
		Если ТипЗнч(РегЗн) = Тип("Структура") Тогда
			РегионX      = Цел(РегЗн.x);
			РегионY      = Цел(РегЗн.y);
			РегионШирина = Цел(РегЗн.width);
			РегионВысота = Цел(РегЗн.height);
		КонецЕсли;
	КонецЕсли;

	СтрокаРектов = "";
	Если ПараметрыSnap <> Неопределено И ТипЗнч(ПараметрыSnap) = Тип("Структура")
		И ПараметрыSnap.Свойство("highlight_rects")
		И ТипЗнч(ПараметрыSnap.highlight_rects) = Тип("Массив") Тогда
		Для Каждого Рект Из ПараметрыSnap.highlight_rects Цикл
			Если ЗначениеЗаполнено(СтрокаРектов) Тогда
				СтрокаРектов = СтрокаРектов + ";";
			КонецЕсли;
			СтрокаРектов = СтрокаРектов
				+ Формат(Цел(Рект.x), "ЧГ=") + ","
				+ Формат(Цел(Рект.y), "ЧГ=") + ","
				+ Формат(Цел(Рект.width), "ЧГ=") + ","
				+ Формат(Цел(Рект.height), "ЧГ=");
		КонецЦикла;
	КонецЕсли;

	// Если указано имя формы — открываем и ждём рендеринга
	Если ЗначениеЗаполнено(НазваниеФормы) Тогда
		ОткрытаяФорма = Неопределено;
		Попытка
			ОткрытаяФорма = ОткрытьФорму(НазваниеФормы, Новый Структура);
		Исключение
			Возврат Новый Структура("success,error", Ложь,
				"Ошибка открытия формы: " + ОписаниеОшибки());
		КонецПопытки;
		Если ОткрытаяФорма = Неопределено Тогда
			Возврат Новый Структура("success,error", Ложь, "Форма не открылась: " + НазваниеФормы);
		КонецЕсли;

	КонецЕсли;

	// Захват активного окна процесса по PID
	Base64PNG = "";
	ОкноРект = Неопределено;
	ЧастиРект = Неопределено;
	ПоследнееСостояние = "окно не найдено";
	Попытка
		Ответ = КомпонентаScreenCapture.CaptureWindow(МасштабПроц, ПоказатьСетку,
			РегионX, РегионY, РегионШирина, РегионВысота, СтрокаРектов);
	Исключение
		Если ЗначениеЗаполнено(НазваниеФормы) И ОткрытаяФорма <> Неопределено Тогда
			Попытка ОткрытаяФорма.Закрыть(); Исключение КонецПопытки;
		КонецЕсли;
		Возврат Новый Структура("success,error", Ложь,
			"Ошибка захвата: " + ОписаниеОшибки());
	КонецПопытки;

	Если ЗначениеЗаполнено(НазваниеФормы) И ОткрытаяФорма <> Неопределено Тогда
		Попытка ОткрытаяФорма.Закрыть(); Исключение КонецПопытки;
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(Ответ) Тогда
		ПоследнееСостояние = "главное окно не найдено";
	ИначеЕсли Лев(Ответ, 7) = "RETRY:w" Тогда
		ПоследнееСостояние = "окно найдено, размер 0×0";
	ИначеЕсли Лев(Ответ, 7) = "RETRY:p" Тогда
		ПоследнееСостояние = "PrintWindow вернул 0";
	ИначеЕсли Ответ = "ERROR:region_oob" Тогда
		Возврат Новый Структура("success,error", Ложь,
			"Указанный регион выходит за пределы клиентской области окна");
	ИначеЕсли Ответ = "ERROR:highlight_fmt" Тогда
		Возврат Новый Структура("success,error", Ложь,
			"Внутренняя ошибка: некорректный формат highlight_rects в строке AddIn");
	ИначеЕсли Лев(Ответ, 20) = "ERROR:highlight_oob:" Тогда
		НомерРект = Сред(Ответ, 21);
		Возврат Новый Структура("success,error", Ложь,
			"Rectangle " + НомерРект + " is outside the captured area");
	Иначе
		// Форматы ответа компоненты:
		//   show_grid=false: "left|top|width|height|<base64>"
		//   show_grid=true:  "left|top|width|height|gridX|gridY|<base64>"
		//                    gridX/gridY — координаты нарисованных линий через запятую (могут быть пустыми)
		// Если '|' нет — старый бинарник, обратная совместимость
		Если Найти(Ответ, "|") > 0 Тогда
			Остаток = Ответ;
			ЧастиРект = Новый Массив;
			КорректныйФормат = Истина;
			// Читаем до 6 полей: первые 4 обязательные (left/top/width/height),
			// поля 5 и 6 опциональные (gridX/gridY, только новая DLL с show_grid=true)
			Для Инд = 1 По 6 Цикл
				Поз = Найти(Остаток, "|");
				Если Поз = 0 Тогда
					Если Инд <= 4 Тогда КорректныйФормат = Ложь; КонецЕсли;
					Прервать;
				КонецЕсли;
				ЧастиРект.Добавить(Лев(Остаток, Поз - 1));
				Остаток = Сред(Остаток, Поз + 1);
			КонецЦикла;
			Если КорректныйФормат Тогда
				Попытка
					ОкноРект = Новый Структура("left,top,width,height",
						Число(ЧастиРект[0]),
						Число(ЧастиРект[1]),
						Число(ЧастиРект[2]),
						Число(ЧастиРект[3]));
					Base64PNG = Остаток;
				Исключение
					// Число("") или Число("abc") — нечисловые coordinate fields
					Возврат Новый Структура("success,error", Ложь,
						"Неожиданный формат ответа компоненты ScreenCapture");
				КонецПопытки;
			Иначе
				// Недостаточно разделителей
				Возврат Новый Структура("success,error", Ложь,
					"Неожиданный формат ответа компоненты ScreenCapture");
			КонецЕсли;
		Иначе
			Base64PNG = Ответ;
		КонецЕсли;
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(Base64PNG) Тогда
		Возврат Новый Структура("success,error", Ложь,
			"Не удалось захватить окно (" + ПоследнееСостояние + ")");
	КонецЕсли;

	// Возвращаем image_base64 на верхнем уровне, НЕ под "data"
	// — обходит format_tool_result в Python (TOON-режим заменяет data на строку)
	Рез = Новый Структура("success,image_base64,mime_type",
		Истина, Base64PNG, "image/png");
	Если ОкноРект <> Неопределено Тогда
		Рез.Вставить("window_rect", ОкноРект);
	КонецЕсли;
	// Поля 5 и 6 — координаты сетки от новой DLL (только при show_grid=true)
	Если ЧастиРект <> Неопределено
		И ЧастиРект.Количество() >= 6
		И (ЗначениеЗаполнено(ЧастиРект[4]) ИЛИ ЗначениеЗаполнено(ЧастиРект[5])) Тогда
		КолонкиX = РазобратьСписокКоординат(ЧастиРект[4]);
		СтрокиY   = РазобратьСписокКоординат(ЧастиРект[5]);
		Рез.Вставить("grid_coords", Новый Структура("grid_x,grid_y", КолонкиX, СтрокиY));
	КонецЕсли;
	Возврат Рез;

КонецФункции

&НаКлиенте
// Разбирает строку "100,200,300" в массив чисел [100, 200, 300].
// Пустая строка → пустой массив (явная проверка, т.к. СтрРазделить("") на некоторых
// платформах возвращает массив с одним пустым элементом, что упадёт на Число("")).
Функция РазобратьСписокКоординат(Строка)
	Рез = Новый Массив;
	Если НЕ ЗначениеЗаполнено(Строка) Тогда
		Возврат Рез;
	КонецЕсли;
	Остаток = СокрЛП(Строка);
	Пока ЗначениеЗаполнено(Остаток) Цикл
		Поз = Найти(Остаток, ",");
		Если Поз = 0 Тогда
			Рез.Добавить(Число(СокрЛП(Остаток)));
			Прервать;
		Иначе
			Рез.Добавить(Число(СокрЛП(Лев(Остаток, Поз - 1))));
			Остаток = Сред(Остаток, Поз + 1);
		КонецЕсли;
	КонецЦикла;
	Возврат Рез;
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// СПРАВКА BSL (get_bsl_syntax_help)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция ПолучитьСправкуПоBSL(ЗапросBSL)
	Если КомпонентаSyntaxHelp = Неопределено Тогда
		Возврат Новый Структура("success,error", Ложь, "SyntaxHelpReader не загружен");
	КонецЕсли;

	Если ЗапросBSL = Неопределено ИЛИ ТипЗнч(ЗапросBSL) <> Тип("Структура") Тогда
		Возврат Новый Структура("success,error", Ложь, "params must be an object");
	КонецЕсли;

	// Валидация keywords
	КлючевыеСлова = Неопределено;
	Если ЗапросBSL.Свойство("keywords") Тогда
		КлючевыеСлова = ЗапросBSL.keywords;
	КонецЕсли;
	Если НЕ (ТипЗнч(КлючевыеСлова) = Тип("Массив") ИЛИ ТипЗнч(КлючевыеСлова) = Тип("ФиксированныйМассив")) ИЛИ КлючевыеСлова.Количество() = 0 Тогда
		Возврат Новый Структура("success,error", Ложь, "keywords: must be non-empty array");
	КонецЕсли;
	ОчищенныеКлючи = Новый Массив;
	Для Каждого КС Из КлючевыеСлова Цикл
		Если ТипЗнч(КС) = Тип("Строка") И ЗначениеЗаполнено(СокрЛП(КС)) Тогда
			ОчищенныеКлючи.Добавить(СокрЛП(КС));
		КонецЕсли;
	КонецЦикла;
	Если ОчищенныеКлючи.Количество() = 0 Тогда
		Возврат Новый Структура("success,error", Ложь, "keywords: must contain at least one non-empty string");
	КонецЕсли;
	КлючевыеСлова = ОчищенныеКлючи;

	// Валидация match
	РежимСтрока = "all";
	Если ЗапросBSL.Свойство("match") И ЗначениеЗаполнено(ЗапросBSL.match) Тогда
		Если ЗапросBSL.match = "all" ИЛИ ЗапросBSL.match = "any" Тогда
			РежимСтрока = ЗапросBSL.match;
		Иначе
			Возврат Новый Структура("success,error", Ложь, "match: must be 'all' or 'any'");
		КонецЕсли;
	КонецЕсли;
	РежимВсе = (РежимСтрока = "all");

	КлючевыеСловаJSON = ЗаписатьJSON2(КлючевыеСлова);

	РезультатJSON = КомпонентаSyntaxHelp.Поиск(КлючевыеСловаJSON, РежимВсе);

	// ПрочитатьJSON2 принимает строку напрямую и возвращает Структура,
	// т.к. оба ключа JSON ("candidates", "content") — валидные идентификаторы 1C
	Данные = ПрочитатьJSON2(РезультатJSON);

	// Применяем пагинацию к массиву кандидатов
	// Читаем limit/offset с дефолтами (нормализатор не всегда вызывается — в proxy-режиме нет)
	ВсеКандидаты = Данные.candidates;
	Лимит = 100;
	Если ЗапросBSL.Свойство("limit") И ТипЗнч(ЗапросBSL.limit) = Тип("Число") И ЗапросBSL.limit >= 1 Тогда
		Лимит = Цел(ЗапросBSL.limit);
	КонецЕсли;
	Смещение = 0;
	Если ЗапросBSL.Свойство("offset") И ТипЗнч(ЗапросBSL.offset) = Тип("Число") И ЗапросBSL.offset >= 0 Тогда
		Смещение = Цел(ЗапросBSL.offset);
	КонецЕсли;

	Итого = ВсеКандидаты.Количество();
	СтраницаКандидатов = Новый Массив;
	ИндексКонца = Мин(Смещение + Лимит, Итого);
	Для Инд = Смещение По ИндексКонца - 1 Цикл
		СтраницаКандидатов.Добавить(ВсеКандидаты[Инд]);
	КонецЦикла;

	ЕстьЕще = (Смещение + Лимит) < Итого;

	// Подавляем content при offset > 0
	Контент = Данные.content;
	Если Смещение > 0 Тогда
		Контент = Null;
	КонецЕсли;

	// Читаем content_page (нормализатор не всегда вызывается — в proxy-режиме нет)
	НомерСтраницыКонтента = 1;
	Если ЗапросBSL.Свойство("content_page") И ТипЗнч(ЗапросBSL.content_page) = Тип("Число")
		И ЗапросBSL.content_page >= 1 Тогда
		НомерСтраницыКонтента = Цел(ЗапросBSL.content_page);
	КонецЕсли;

	// Локальные переменные для полей ответа (заполняются только если Контент не Null)
	КонтентФактНомер = Неопределено;
	КонтентВсегоСтраниц = Неопределено;
	КонтентЕстьЕще = Неопределено;

	Если ЗначениеЗаполнено(Контент) И ТипЗнч(Контент) = Тип("Строка") Тогда
		РазмерСтраницыКонтента = 4000; // фиксирован, агент не управляет
		МассивСтраниц = Новый Массив;
		ТекущаяСтраница = "";

		// Разбиваем на строки вручную (СтрРазделить недоступна в режиме совместимости)
		// Логика эквивалентна РазделитьСтрокуПоРазделителю, но inline на клиенте.
		// Пустые строки сохраняются — важно для Markdown (разрывы абзацев, code blocks).
		МассивСтрок = Новый Массив;
		ОстатокКонтента = Контент;
		ПозПС = Найти(ОстатокКонтента, Символы.ПС);
		Пока ПозПС > 0 Цикл
			МассивСтрок.Добавить(Лев(ОстатокКонтента, ПозПС - 1));
			ОстатокКонтента = Сред(ОстатокКонтента, ПозПС + 1);
			ПозПС = Найти(ОстатокКонтента, Символы.ПС);
		КонецЦикла;
		МассивСтрок.Добавить(ОстатокКонтента);

		Для Каждого Строка Из МассивСтрок Цикл
			// Страница непустая И добавление строки превысит лимит → фиксируем страницу
			// Исключение: страница пустая (строка длиннее лимита) — всё равно добавляем
			Если ЗначениеЗаполнено(ТекущаяСтраница)
				И (СтрДлина(ТекущаяСтраница) + СтрДлина(Строка) + 1) > РазмерСтраницыКонтента Тогда
				// Убираем синтетический завершающий Символы.ПС (не СокрП — она портит Markdown)
				ЗафиксированнаяСтраница = ТекущаяСтраница;
				Если Прав(ЗафиксированнаяСтраница, 1) = Символы.ПС Тогда
					ЗафиксированнаяСтраница = Лев(ЗафиксированнаяСтраница, СтрДлина(ЗафиксированнаяСтраница) - 1);
				КонецЕсли;
				МассивСтраниц.Добавить(ЗафиксированнаяСтраница);
				ТекущаяСтраница = Строка + Символы.ПС;
			Иначе
				ТекущаяСтраница = ТекущаяСтраница + Строка + Символы.ПС;
			КонецЕсли;
		КонецЦикла;
		// Последняя страница
		Если ЗначениеЗаполнено(ТекущаяСтраница) Тогда
			ЗафиксированнаяСтраница = ТекущаяСтраница;
			Если Прав(ЗафиксированнаяСтраница, 1) = Символы.ПС Тогда
				ЗафиксированнаяСтраница = Лев(ЗафиксированнаяСтраница, СтрДлина(ЗафиксированнаяСтраница) - 1);
			КонецЕсли;
			МассивСтраниц.Добавить(ЗафиксированнаяСтраница);
		КонецЕсли;

		КонтентВсегоСтраниц = Макс(МассивСтраниц.Количество(), 1);
		КонтентФактНомер = Мин(НомерСтраницыКонтента, КонтентВсегоСтраниц);
		КонтентЕстьЕще = КонтентФактНомер < КонтентВсегоСтраниц;
		Контент = МассивСтраниц[КонтентФактНомер - 1]; // обновляем Контент нужной страницей
	КонецЕсли;

	РезультатДанные = Новый Структура;
	РезультатДанные.Вставить("candidates", СтраницаКандидатов);
	РезультатДанные.Вставить("total", Итого);
	РезультатДанные.Вставить("offset", Смещение);
	РезультатДанные.Вставить("limit", Лимит);
	РезультатДанные.Вставить("has_more", ЕстьЕще);
	РезультатДанные.Вставить("content", Контент);
	Если КонтентФактНомер <> Неопределено Тогда
		РезультатДанные.Вставить("content_page", КонтентФактНомер);
		РезультатДанные.Вставить("content_total_pages", КонтентВсегоСтраниц);
		РезультатДанные.Вставить("content_has_more", КонтентЕстьЕще);
	КонецЕсли;

	Возврат Новый Структура("success,data", Истина, РезультатДанные);
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ПРАВА ДОСТУПА (get_access_rights)
// ─────────────────────────────────────────────────────────────────────────────────

// Главная функция инструмента get_access_rights
//
// Параметры:
//  ИмяОбъектаМетаданных - Строка - полное имя объекта метаданных (например, "Справочник.Контрагенты")
//  ИмяПользователяИБ - Строка, Неопределено - имя пользователя для эффективных прав
//  ФильтрПрав - Массив, Неопределено - список прав для отображения
//  ФильтрРолей - Массив, Неопределено - список ролей для отображения
//
// Возвращаемое значение:
//  Структура - результат с полями success, data или error
//
&НаСервереБезКонтекста
Функция ПолучитьПраваДоступа(ИмяОбъектаМетаданных, ИмяПользователяИБ, ФильтрПрав, ФильтрРолей)

	// 1. Проверка привилегированного режима
	Если ПривилегированныйРежим() Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Инструмент не работает в привилегированном режиме / Tool does not work in privileged mode");
	КонецЕсли;

	// 2. Валидация обязательных параметров
	Если Не ЗначениеЗаполнено(ИмяОбъектаМетаданных) Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Не указан объект метаданных (metadata_object) / Metadata object is required");
	КонецЕсли;

	// 3. Поиск объекта метаданных
	ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ИмяОбъектаМетаданных);
	Если ОбъектМетаданных = Неопределено Тогда
		Возврат Новый Структура("success, error", Ложь,
			"Объект метаданных не найден: " + ИмяОбъектаМетаданных +
			" / Metadata object not found: " + ИмяОбъектаМетаданных);
	КонецЕсли;

	// 4. Определение типа метаданных
	ТипМетаданных = ОпределитьТипМетаданных(ОбъектМетаданных);
	Если ТипМетаданных = "Unknown" Тогда
		// Для поддержки всех типов метаданных не заваливаемся ошибкой.
		// В этом случае возвращаем тип как префикс из metadata_object и используем права по умолчанию.
		ТипМетаданных = ПолучитьПрефиксМетаданныхИзИмени(ИмяОбъектаМетаданных);
		Если ТипМетаданных = "Unknown" Тогда
			ТипМетаданных = "Другое";
		КонецЕсли;
	КонецЕсли;

	// 5. Получение применимых прав
	ПрименимыеПрава = ПолучитьПраваПоТипам();
	ПраваПоТипу = ПрименимыеПрава.Получить(ТипМетаданных);
	Если ПраваПоТипу = Неопределено Тогда
		// Права не определены для этого типа — используем универсальный список по умолчанию
		ПраваПоТипу = ПолучитьПраваПоУмолчанию();
	КонецЕсли;

	// 6. Применение фильтра прав
	ПраваДляПроверки = Новый Массив;
	Если ФильтрПрав <> Неопределено И ЭтоКоллекция(ФильтрПрав) И ФильтрПрав.Количество() > 0 Тогда
		Для Каждого ПравоИзФильтра Из ФильтрПрав Цикл
			ПравоИзФильтра = СокрЛП(ПравоИзФильтра);
			Если ПравоИзФильтра <> "" Тогда
				ПраваДляПроверки.Добавить(ПравоИзФильтра);
			КонецЕсли;
		КонецЦикла;
	Иначе
		// Используем дефолтный список по типу
		Для Каждого Право Из ПраваПоТипу Цикл
			ПраваДляПроверки.Добавить(Право);
		КонецЦикла;
	КонецЕсли;

	// 6.1 Предварительная проверка админ-прав
	//
	// Вызовы ПравоДоступа() с 3-м параметром (роль/пользователь) требуют административных прав.
	// Чтобы не "тихо" получить все false (из-за Исключение), проверяем это заранее на заведомо
	// корректном праве из дефолтного списка.
	ПроверочноеПраво = "";
	Если ПраваПоТипу <> Неопределено И ЭтоКоллекция(ПраваПоТипу) И ПраваПоТипу.Количество() > 0 Тогда
		ПроверочноеПраво = ПраваПоТипу[0];
	ИначеЕсли ПраваДляПроверки <> Неопределено И ПраваДляПроверки.Количество() > 0 Тогда
		ПроверочноеПраво = ПраваДляПроверки[0];
	КонецЕсли;

	Если ЗначениеЗаполнено(ПроверочноеПраво) Тогда
		ПерваяРоль = Неопределено;
		Для Каждого Роль0 Из Метаданные.Роли Цикл
			ПерваяРоль = Роль0;
			Прервать;
		КонецЦикла;

		Если ПерваяРоль <> Неопределено Тогда
			ПравоКорректно = Истина;
			Попытка
				// Без 3-го параметра исключений быть не должно (проверка на корректность права/объекта)
				ПравоДоступа(ПроверочноеПраво, ОбъектМетаданных);
			Исключение
				ПравоКорректно = Ложь;
			КонецПопытки;

			Если ПравоКорректно Тогда
				Попытка
					// Проверка наличия админ-прав (с 3-м параметром)
					ПравоДоступа(ПроверочноеПраво, ОбъектМетаданных, ПерваяРоль);
				Исключение
					Возврат Новый Структура("success, error", Ложь,
						"Требуются административные права / Admin rights required");
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	// 7. Получение прав ролей
	МассивРолей = Новый Массив;
	ИспользованФильтрРолей = (ФильтрРолей <> Неопределено И ЭтоКоллекция(ФильтрРолей) И ФильтрРолей.Количество() > 0);
	ЕстьЯвноеЗначениеРолейФильтр = Ложь;

	Для Каждого РольМетаданных Из Метаданные.Роли Цикл
		ИмяРоли = РольМетаданных.Имя;

		// Применение фильтра ролей
		Если ИспользованФильтрРолей Тогда
			НайденоВФильтре = Ложь;
			Для Каждого ИмяИзФильтра Из ФильтрРолей Цикл
				Если ВРег(СокрЛП(ИмяИзФильтра)) = ВРег(ИмяРоли) Тогда
					НайденоВФильтре = Истина;
					ЕстьЯвноеЗначениеРолейФильтр = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если Не НайденоВФильтре Тогда
				Продолжить;
			КонецЕсли;
		КонецЕсли;

		// Проверка прав роли на объект
		ПраваРоли = Новый Структура;
		ЕстьХотяБыОдноПраво = Ложь;
		РольИмеетПраваНаОбъект = Ложь;

		Для Каждого ИмяПрава Из ПраваДляПроверки Цикл
			ЕстьПравоНаМетаданные = Ложь;
			Попытка
				// Проверка права для роли (требует админ-прав!)
				ЕстьПравоНаМетаданные = ПравоДоступа(ИмяПрава, ОбъектМетаданных, РольМетаданных);
				ПраваРоли.Вставить(ИмяПрава, ЕстьПравоНаМетаданные);
				Если ЕстьПравоНаМетаданные Тогда
					ЕстьХотяБыОдноПраво = Истина;
					РольИмеетПраваНаОбъект = Истина;
				КонецЕсли;
			Исключение
				// Любая ошибка проверки конкретного права трактуется как отсутствие права
				// (в т.ч. неприменимое право или неизвестное имя права).
				ПраваРоли.Вставить(ИмяПрава, Ложь);
			КонецПопытки;
		КонецЦикла;

		// Добавление роли в результат
		// Без фильтра: только роли с правами
		// С фильтром: все роли из фильтра (даже если все false)
		Если ЕстьХотяБыОдноПраво Или (ИспользованФильтрРолей И ЕстьЯвноеЗначениеРолейФильтр) Тогда
			СтруктураРоли = Новый Структура;
			СтруктураРоли.Вставить("name", ИмяРоли);
			СтруктураРоли.Вставить("synonym", РольМетаданных.Синоним);
			СтруктураРоли.Вставить("rights", ПраваРоли);
			МассивРолей.Добавить(СтруктураРоли);
		КонецЕсли;
	КонецЦикла;

	// 8. Подсчёт статистики
	ВсегоРолей = МассивРолей.Количество();
	РолейСПравами = 0;
	Для Каждого ДанныеРоли Из МассивРолей Цикл
		Для Каждого КлючЗначение Из ДанныеРоли.rights Цикл
			Если КлючЗначение.Значение = Истина Тогда
				РолейСПравами = РолейСПравами + 1;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

	// 9. Сортировка ролей по имени
	МассивРолей = СортироватьМассивСтруктурПоПолю(МассивРолей, "name");

	// 10. Формирование результата
	Данные = Новый Структура;
	Данные.Вставить("metadata_object", ИмяОбъектаМетаданных);
	Данные.Вставить("metadata_type", ТипМетаданных);
	// applicable_rights соответствует ключам в rights/effective_rights (с учётом rights_filter)
	Данные.Вставить("applicable_rights", ПраваДляПроверки);
	Данные.Вставить("roles", МассивРолей);
	Данные.Вставить("total_roles", ВсегоРолей);
	Данные.Вставить("roles_with_rights", РолейСПравами);

	// 11. Права пользователя (если указан)
	Если ЗначениеЗаполнено(ИмяПользователяИБ) Тогда
		ПользовательИБ = НайтиПользователяИБПоИмени(ИмяПользователяИБ);
		Если ПользовательИБ = Неопределено Тогда
			Возврат Новый Структура("success, error", Ложь,
				"Пользователь не найден / User not found in infobase");
		КонецЕсли;

		// Роли пользователя
		РолиПользователя = Новый Массив;
		Для Каждого РольИБ Из ПользовательИБ.Роли Цикл
			РолиПользователя.Добавить(РольИБ.Имя);
		КонецЦикла;

		// Эффективные права через пользователя
		ЭффективныеПрава = Новый Структура;
		Для Каждого ИмяПрава Из ПраваДляПроверки Цикл
			Попытка
				// Проверка права для пользователя (требует админ-прав!)
				ЕстьПраво = ПравоДоступа(ИмяПрава, ОбъектМетаданных, ПользовательИБ);
				ЭффективныеПрава.Вставить(ИмяПрава, ЕстьПраво);
			Исключение
				ЭффективныеПрава.Вставить(ИмяПрава, Ложь);
			КонецПопытки;
		КонецЦикла;

		// Данные о пользователе
		ДанныеПользователя = Новый Структура;
		ДанныеПользователя.Вставить("name", ИмяПользователяИБ);

		// Полное имя (если доступно)
		ПолноеИмя = "";
		Попытка
			ПолноеИмя = ПользовательИБ.ПолноеИмя;
		Исключение
			// Полное имя может быть недоступно
		КонецПопытки;
		Если ЗначениеЗаполнено(ПолноеИмя) Тогда
			ДанныеПользователя.Вставить("full_name", ПолноеИмя);
		КонецЕсли;

		ДанныеПользователя.Вставить("roles", РолиПользователя);
		ДанныеПользователя.Вставить("effective_rights", ЭффективныеПрава);

		Данные.Вставить("user", ДанныеПользователя);
	КонецЕсли;

	Возврат Новый Структура("success, data", Истина, Данные);

КонецФункции

// Определение типа метаданных (на русском)
//
// Параметры:
//  ОбъектМетаданных - ОбъектМетаданных - объект метаданных
//
// Возвращаемое значение:
//  Строка - тип метаданных на русском
//
&НаСервереБезКонтекста
Функция ОпределитьТипМетаданных(ОбъектМетаданных)

	Если Метаданные.Справочники.Содержит(ОбъектМетаданных) Тогда
		Возврат "Справочник";
	ИначеЕсли Метаданные.Документы.Содержит(ОбъектМетаданных) Тогда
		Возврат "Документ";
	ИначеЕсли Метаданные.Перечисления.Содержит(ОбъектМетаданных) Тогда
		Возврат "Перечисление";
	ИначеЕсли Метаданные.РегистрыСведений.Содержит(ОбъектМетаданных) Тогда
		Возврат "РегистрСведений";
	ИначеЕсли Метаданные.РегистрыНакопления.Содержит(ОбъектМетаданных) Тогда
		Возврат "РегистрНакопления";
	ИначеЕсли Метаданные.РегистрыБухгалтерии.Содержит(ОбъектМетаданных) Тогда
		Возврат "РегистрБухгалтерии";
	ИначеЕсли Метаданные.РегистрыРасчета.Содержит(ОбъектМетаданных) Тогда
		Возврат "РегистрРасчета";
	ИначеЕсли Метаданные.Обработки.Содержит(ОбъектМетаданных) Тогда
		Возврат "Обработка";
	ИначеЕсли Метаданные.Отчеты.Содержит(ОбъектМетаданных) Тогда
		Возврат "Отчет";
	ИначеЕсли Метаданные.ПланыВидовХарактеристик.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПланВидовХарактеристик";
	ИначеЕсли Метаданные.ПланыСчетов.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПланСчетов";
	ИначеЕсли Метаданные.ПланыВидовРасчета.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПланВидовРасчета";
	ИначеЕсли Метаданные.ПланыОбмена.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПланОбмена";
	ИначеЕсли Метаданные.БизнесПроцессы.Содержит(ОбъектМетаданных) Тогда
		Возврат "БизнесПроцесс";
	ИначеЕсли Метаданные.Задачи.Содержит(ОбъектМетаданных) Тогда
		Возврат "Задача";
	ИначеЕсли Метаданные.ГруппыКоманд.Содержит(ОбъектМетаданных) Тогда
		Возврат "ГруппаКоманд";
	ИначеЕсли Метаданные.ЖурналыДокументов.Содержит(ОбъектМетаданных) Тогда
		Возврат "ЖурналДокументов";
	ИначеЕсли Метаданные.Константы.Содержит(ОбъектМетаданных) Тогда
		Возврат "Константа";
	ИначеЕсли Метаданные.КритерииОтбора.Содержит(ОбъектМетаданных) Тогда
		Возврат "КритерийОтбора";
	ИначеЕсли Метаданные.НумераторыДокументов.Содержит(ОбъектМетаданных) Тогда
		Возврат "Нумератор";
	ИначеЕсли Метаданные.ОбщиеКоманды.Содержит(ОбъектМетаданных) Тогда
		Возврат "ОбщаяКоманда";
	ИначеЕсли Метаданные.ОбщиеМодули.Содержит(ОбъектМетаданных) Тогда
		Возврат "ОбщийМодуль";
	ИначеЕсли Метаданные.ОбщиеРеквизиты.Содержит(ОбъектМетаданных) Тогда
		Возврат "ОбщийРеквизит";
	ИначеЕсли Метаданные.ОпределяемыеТипы.Содержит(ОбъектМетаданных) Тогда
		Возврат "ОпределяемыйТип";
	ИначеЕсли Метаданные.ПараметрыФункциональныхОпций.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПараметрФункциональныхОпций";
	ИначеЕсли Метаданные.Подсистемы.Содержит(ОбъектМетаданных) Тогда
		Возврат "Подсистема";
	ИначеЕсли Метаданные.Последовательности.Содержит(ОбъектМетаданных) Тогда
		Возврат "Последовательность";
	ИначеЕсли Метаданные.РегламентныеЗадания.Содержит(ОбъектМетаданных) Тогда
		Возврат "РегламентноеЗадание";
	ИначеЕсли Метаданные.ФункциональныеОпции.Содержит(ОбъектМетаданных) Тогда
		Возврат "ФункциональнаяОпция";
	ИначеЕсли Метаданные.HTTPСервисы.Содержит(ОбъектМетаданных) Тогда
		Возврат "HTTPСервис";
	ИначеЕсли Метаданные.WebСервисы.Содержит(ОбъектМетаданных) Тогда
		Возврат "WebСервис";
	ИначеЕсли Метаданные.WSСсылки.Содержит(ОбъектМетаданных) Тогда
		Возврат "WSСсылка";
	ИначеЕсли Метаданные.WebSocketКлиенты.Содержит(ОбъектМетаданных) Тогда
		Возврат "WebSocketКлиент";
	ИначеЕсли Метаданные.ВнешниеИсточникиДанных.Содержит(ОбъектМетаданных) Тогда
		Возврат "ВнешнийИсточникДанных";
	ИначеЕсли Метаданные.ПараметрыСеанса.Содержит(ОбъектМетаданных) Тогда
		Возврат "ПараметрСеанса";
	ИначеЕсли Метаданные.Роли.Содержит(ОбъектМетаданных) Тогда
		Возврат "Роль";
	ИначеЕсли Метаданные.ХранилищаНастроек.Содержит(ОбъектМетаданных) Тогда
		Возврат "ХранилищеНастроек";
	ИначеЕсли Метаданные.ОбщиеФормы.Содержит(ОбъектМетаданных) Тогда
		Возврат "ОбщаяФорма";
	ИначеЕсли Метаданные.СервисыИнтеграции.Содержит(ОбъектМетаданных) Тогда
		Возврат "СервисИнтеграции";
	ИначеЕсли Метаданные.Боты.Содержит(ОбъектМетаданных) Тогда
		Возврат "Бот";
	Иначе
		Возврат "Unknown";
	КонецЕсли;

КонецФункции

// Получить префикс типа метаданных из полного имени объекта (часть до первой точки).
//
// Параметры:
//  ПолноеИмя - Строка - например "Справочник.Контрагенты"
//
// Возвращаемое значение:
//  Строка - префикс (например "Справочник") или "Unknown"
//
&НаСервереБезКонтекста
Функция ПолучитьПрефиксМетаданныхИзИмени(ПолноеИмя)

	Если Не ЗначениеЗаполнено(ПолноеИмя) Тогда
		Возврат "Unknown";
	КонецЕсли;

	ПолноеИмя = СокрЛП(ПолноеИмя);
	ПозицияТочки = Найти(ПолноеИмя, ".");
	Если ПозицияТочки <= 0 Тогда
		Возврат "Unknown";
	КонецЕсли;

	Префикс = Лев(ПолноеИмя, ПозицияТочки - 1);
	Префикс = СокрЛП(Префикс);
	Если Префикс = "" Тогда
		Возврат "Unknown";
	КонецЕсли;

	Возврат Префикс;

КонецФункции

// Универсальный список прав по умолчанию для типов метаданных,
// для которых нет специфического списка в ПолучитьПраваПоТипам().
//
// Возвращаемое значение:
//  Массив - список прав для проверки
//
&НаСервереБезКонтекста
Функция ПолучитьПраваПоУмолчанию()

	Права = Новый Массив;

	// Базовые права, которые чаще всего применимы к большинству объектов
	Права.Добавить("Чтение");
	Права.Добавить("Просмотр");
	Права.Добавить("Использование");
	Права.Добавить("Добавление");
	Права.Добавить("Изменение");
	Права.Добавить("Удаление");
	Права.Добавить("Редактирование");

	// Часто встречающиеся "служебные" права
	Права.Добавить("Получение");
	Права.Добавить("Установка");
	Права.Добавить("Вывод");
	Права.Добавить("ВводПоСтроке");
	Права.Добавить("СохранениеДанныхПользователя");
	Права.Добавить("ИзменениеСтандартнойАутентификации");

	// Документные права (для большинства типов будут просто false)
	Права.Добавить("Проведение");
	Права.Добавить("ОтменаПроведения");
	Права.Добавить("ИнтерактивноеПроведение");
	Права.Добавить("ИнтерактивноеПроведениеНеОперативное");
	Права.Добавить("ИнтерактивнаяОтменаПроведения");
	Права.Добавить("ИнтерактивноеИзменениеПроведенных");

	// Интерактивные права редактирования (для многих типов будут false)
	Права.Добавить("ИнтерактивноеДобавление");
	Права.Добавить("ИнтерактивноеУдаление");
	Права.Добавить("ИнтерактивнаяПометкаУдаления");
	Права.Добавить("ИнтерактивноеСнятиеПометкиУдаления");
	Права.Добавить("ИнтерактивноеУдалениеПомеченных");
	Права.Добавить("ИнтерактивноеУдалениеПредопределенныхДанных");
	Права.Добавить("ИнтерактивнаяПометкаУдаленияПредопределенныхДанных");
	Права.Добавить("ИнтерактивноеСнятиеПометкиУдаленияПредопределенныхДанных");
	Права.Добавить("ИнтерактивноеУдалениеПомеченныхПредопределенныхДанных");
	Права.Добавить("ИнтерактивноеОткрытиеВнешнихОбработок");
	Права.Добавить("ИнтерактивноеОткрытиеВнешнихОтчетов");

	// Права для отдельных подсистем (для большинства типов будут просто false)
	Права.Добавить("УправлениеИтогами");
	Права.Добавить("Администрирование");
	Права.Добавить("АдминистрированиеДанных");
	Права.Добавить("РежимВсеФункции");
	Права.Добавить("Старт");
	Права.Добавить("ИнтерактивныйСтарт");
	Права.Добавить("Выполнение");
	Права.Добавить("ИнтерактивноеВыполнение");

	Возврат Права;

КонецФункции

// Получить справочник прав по типам метаданных
//
// Возвращаемое значение:
//  Соответствие - тип метаданных -> массив прав
//
&НаСервереБезКонтекста
Функция ПолучитьПраваПоТипам()

	ПраваПоТипам = Новый Соответствие;

	// Справочники
	ПраваСправочника = Новый Массив;
	ПраваСправочника.Добавить("Чтение");
	ПраваСправочника.Добавить("Добавление");
	ПраваСправочника.Добавить("Изменение");
	ПраваСправочника.Добавить("Удаление");
	ПраваСправочника.Добавить("Просмотр");
	ПраваСправочника.Добавить("ИнтерактивноеДобавление");
	ПраваСправочника.Добавить("Редактирование");
	ПраваСправочника.Добавить("ИнтерактивноеУдаление");
	ПраваСправочника.Добавить("ИнтерактивнаяПометкаУдаления");
	ПраваСправочника.Добавить("ИнтерактивноеСнятиеПометкиУдаления");
	ПраваСправочника.Добавить("ИнтерактивноеУдалениеПомеченных");
	ПраваСправочника.Добавить("ИнтерактивноеУдалениеПредопределенныхДанных");
	ПраваСправочника.Добавить("ИнтерактивнаяПометкаУдаленияПредопределенныхДанных");
	ПраваСправочника.Добавить("ИнтерактивноеСнятиеПометкиУдаленияПредопределенныхДанных");
	ПраваСправочника.Добавить("ИнтерактивноеУдалениеПомеченныхПредопределенныхДанных");
	ПраваПоТипам.Вставить("Справочник", ПраваСправочника);
	ПраваПоТипам.Вставить("ПланВидовХарактеристик", ПраваСправочника);
	ПраваПоТипам.Вставить("ПланСчетов", ПраваСправочника);
	ПраваПоТипам.Вставить("ПланВидовРасчета", ПраваСправочника);
	ПраваПоТипам.Вставить("ПланОбмена", ПраваСправочника);

	// Документы
	ПраваДокумента = Новый Массив;
	Для Каждого Право Из ПраваСправочника Цикл
		ПраваДокумента.Добавить(Право);
	КонецЦикла;
	ПраваДокумента.Добавить("Проведение");
	ПраваДокумента.Добавить("ОтменаПроведения");
	ПраваДокумента.Добавить("ИнтерактивноеПроведение");
	ПраваДокумента.Добавить("ИнтерактивноеПроведениеНеОперативное");
	ПраваДокумента.Добавить("ИнтерактивнаяОтменаПроведения");
	ПраваДокумента.Добавить("ИнтерактивноеИзменениеПроведенных");
	ПраваДокумента.Добавить("ИнтерактивноеУдалениеПомеченных");
	ПраваПоТипам.Вставить("Документ", ПраваДокумента);

	// Бизнес-процессы: документные права + старт
	ПраваБизнесПроцесса = Новый Массив;
	Для Каждого ПравоБП Из ПраваДокумента Цикл
		ПраваБизнесПроцесса.Добавить(ПравоБП);
	КонецЦикла;
	ПраваБизнесПроцесса.Добавить("Старт");
	ПраваБизнесПроцесса.Добавить("ИнтерактивныйСтарт");
	ПраваПоТипам.Вставить("БизнесПроцесс", ПраваБизнесПроцесса);

	// Задачи: документные права + выполнение
	ПраваЗадачи = Новый Массив;
	Для Каждого ПравоЗад Из ПраваДокумента Цикл
		ПраваЗадачи.Добавить(ПравоЗад);
	КонецЦикла;
	ПраваЗадачи.Добавить("Выполнение");
	ПраваЗадачи.Добавить("ИнтерактивноеВыполнение");
	ПраваПоТипам.Вставить("Задача", ПраваЗадачи);

	// Регистры
	ПраваРегистраБазовые = Новый Массив;
	ПраваРегистраБазовые.Добавить("Чтение");
	ПраваРегистраБазовые.Добавить("Изменение");
	ПраваРегистраБазовые.Добавить("Просмотр");
	ПраваРегистраБазовые.Добавить("Редактирование");
	ПраваПоТипам.Вставить("РегистрСведений", ПраваРегистраБазовые);
	ПраваПоТипам.Вставить("РегистрРасчета", ПраваРегистраБазовые);

	// УправлениеИтогами применимо к регистрам накопления и бухгалтерии
	ПраваРегистраСИтогами = Новый Массив;
	Для Каждого ПравоРег Из ПраваРегистраБазовые Цикл
		ПраваРегистраСИтогами.Добавить(ПравоРег);
	КонецЦикла;
	ПраваРегистраСИтогами.Добавить("УправлениеИтогами");
	ПраваПоТипам.Вставить("РегистрНакопления", ПраваРегистраСИтогами);
	ПраваПоТипам.Вставить("РегистрБухгалтерии", ПраваРегистраСИтогами);

	// Обработки и отчёты
	ПраваОбработки = Новый Массив;
	ПраваОбработки.Добавить("Использование");
	ПраваОбработки.Добавить("Просмотр");
	ПраваПоТипам.Вставить("Обработка", ПраваОбработки);
	ПраваПоТипам.Вставить("Отчет", ПраваОбработки);

	Возврат ПраваПоТипам;

КонецФункции

// Найти пользователя ИБ по имени (с case-insensitive fallback)
//
// Параметры:
//  ИмяПользователя - Строка - имя пользователя для поиска
//
// Возвращаемое значение:
//  ПользовательИнформационнойБазы - пользователь или Неопределено
//
&НаСервереБезКонтекста
Функция НайтиПользователяИБПоИмени(ИмяПользователя)

	ИмяПользователя = СокрЛП(ИмяПользователя);
	Если Не ЗначениеЗаполнено(ИмяПользователя) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// 1. Пытаемся найти через НайтиПоИмени (прямой поиск по имени пользователя ИБ)
	ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоИмени(ИмяПользователя);
	Если ПользовательИБ <> Неопределено Тогда
		Возврат ПользовательИБ;
	КонецЕсли;

	// 2. Поиск через справочник Пользователи (переданное имя может быть наименованием из справочника)
	Если Метаданные.Справочники.Найти("Пользователи") <> Неопределено Тогда
		Попытка
			ЭлементСправочника = Справочники.Пользователи.НайтиПоНаименованию(ИмяПользователя, Истина);
			Если ЭлементСправочника <> Справочники.Пользователи.ПустаяСсылка() Тогда
				ИдПользователяИБ = ЭлементСправочника.ИдентификаторПользователяИБ;
				Если ТипЗнч(ИдПользователяИБ) = Тип("Строка") Тогда
					ИдПользователяИБ = Новый УникальныйИдентификатор(СокрЛП(ИдПользователяИБ));
				КонецЕсли;
				Если ЗначениеЗаполнено(ИдПользователяИБ) Тогда
					ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоУникальномуИдентификатору(ИдПользователяИБ);
					Если ПользовательИБ <> Неопределено Тогда
						Возврат ПользовательИБ;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		Исключение
			// Справочник Пользователи существует, но реквизит ИдентификаторПользователяИБ
			// может отсутствовать или быть недоступен — пропускаем этот шаг.
		КонецПопытки;
	КонецЕсли;

	// 3. Fallback: ручной перебор с case-insensitive сравнением
	ИмяПользователяВерх = ВРег(ИмяПользователя);
	// В некоторых режимах/версиях платфомы менеджер ПользователиИнформационнойБазы
	// не является итерируемой коллекцией (ошибка "Итератор для значения не определен").
	// Поэтому перебор делаем в Попытка/Исключение, чтобы не падать инструментом целиком.
	Попытка
		Для Каждого Пользователь Из ПользователиИнформационнойБазы Цикл
			Если ВРег(Пользователь.Имя) = ИмяПользователяВерх Тогда
				Возврат Пользователь;
			КонецЕсли;
		КонецЦикла;
	Исключение
		// Fallback перебора пользователей недоступен в текущем окружении.
		// Возвращаем Неопределено, чтобы вызывающий код вернул понятную ошибку "Пользователь не найден".
	КонецПопытки;

	Возврат Неопределено;

КонецФункции

// Сортировать массив структур по полю
//
// Параметры:
//  МассивСтруктур - Массив - массив структур для сортировки
//  ИмяПоля - Строка - имя поля для сортировки
//
// Возвращаемое значение:
//  Массив - отсортированный массив
//
&НаСервереБезКонтекста
Функция СортироватьМассивСтруктурПоПолю(МассивСтруктур, ИмяПоля)

	// Простой алгоритм сортировки пузырьком
	Результат = Новый Массив;
	Для Каждого Элемент Из МассивСтруктур Цикл
		Результат.Добавить(Элемент);
	КонецЦикла;

	Если Результат.Количество() <= 1 Тогда
		Возврат Результат;
	КонецЕсли;

	Для н = 0 По Результат.Количество() - 2 Цикл
		Для м = 0 По Результат.Количество() - 2 - н Цикл
			Элемент1 = Результат[м];
			Элемент2 = Результат[м + 1];

			Значение1 = Неопределено;
			Значение2 = Неопределено;

			Если Элемент1.Свойство(ИмяПоля) Тогда
				Значение1 = Элемент1[ИмяПоля];
			КонецЕсли;
			Если Элемент2.Свойство(ИмяПоля) Тогда
				Значение2 = Элемент2[ИмяПоля];
			КонецЕсли;

			// Сравнение с учётом типа
			Сравнение = 0;
			Если ЗначениеЗаполнено(Значение1) И ЗначениеЗаполнено(Значение2) Тогда
				Строка1 = Строка(Значение1);
				Строка2 = Строка(Значение2);
				Если Строка1 > Строка2 Тогда
					Сравнение = 1;
				ИначеЕсли Строка1 < Строка2 Тогда
					Сравнение = -1;
				Иначе
					Сравнение = 0;
				КонецЕсли;
			ИначеЕсли Не ЗначениеЗаполнено(Значение1) И ЗначениеЗаполнено(Значение2) Тогда
				Сравнение = 1; // Пустые в конце
			ИначеЕсли ЗначениеЗаполнено(Значение1) И Не ЗначениеЗаполнено(Значение2) Тогда
				Сравнение = -1; // Пустые в конце
			КонецЕсли;

			Если Сравнение > 0 Тогда
				Результат.Установить(м, Элемент2);
				Результат.Установить(м + 1, Элемент1);
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

	Возврат Результат;

КонецФункции

// ═══════════════════════════════════════════════════════════════════════════════
// ВСТРОЕННЫЙ HTTP-СЕРВЕР (Native API компонента MCPHttpTransport)
// MCP Streamable HTTP + REST API
// ═══════════════════════════════════════════════════════════════════════════════

// ─────────────────────────────────────────────────────────────────────────────────
// ПЕРЕКЛЮЧЕНИЕ РЕЖИМА (UI)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ПриИзмененииРежимаПодключения(Элемент)

	ПроксиРежим = (РежимПодключения = 0);

	// Видимость групп
	Элементы.ГруппаПроксиНастройки.Видимость = ПроксиРежим;
	Элементы.ГруппаВстроенныйСервер.Видимость = НЕ ПроксиРежим;
	Элементы.СтраницаАнонимизация.Видимость = Истина;
	Элементы.КнопкаОбновитьМаппинги.Видимость = ПроксиРежим;
	Элементы.КнопкаАнонимизировать.Видимость = НЕ ПроксиРежим;

	// Заголовки кнопок
	Если ПроксиРежим Тогда
		Элементы.КнопкаПодключиться.Заголовок = "Подключиться";
		Элементы.КнопкаОтключиться.Заголовок = "Отключиться";
	Иначе
		Элементы.КнопкаПодключиться.Заголовок = "Запустить сервер";
		Элементы.КнопкаОтключиться.Заголовок = "Остановить сервер";
	КонецЕсли;

	СохранитьРежимПодключенияНаСервере(РежимПодключения);
	ОбновитьСводкуПравилТочнойАнонимизацииНаСервере();

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ЗАПУСК / ОСТАНОВКА ВСТРОЕННОГО СЕРВЕРА
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ЗапуститьВстроенныйСервер()
	СохранитьПортВстроенногоСервераНаСервере(ПортВстроенногоСервера);
	Если ВстроенныйСерверЗапущен ИЛИ ЗапускСервераВПроцессе Тогда
		ДобавитьВЛог("Сервер уже запущен или запускается");
		Возврат;
	КонецЕсли;

	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		ЗапускСервераВПроцессе = Истина;
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияМакетаМСП_ЗапускСервера", ЭтаФорма),
			"ВнешняяОбработка.MCPToolkit.Макет.MCPHttpTransport", "MCPHttp");
	Иначе
		ЗапуститьВстроенныйСерверСинхронно(); // 8.2.x / "Использовать"
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура НачатьАсинхронноеПодключениеКомпоненты(ОписаниеОповещения, Местоположение, Имя)
	Выполнить("НачатьПодключениеВнешнейКомпоненты(ОписаниеОповещения, Местоположение, Имя, ТипВнешнейКомпоненты.Native)");
КонецПроцедуры

// Колбэки async-цепочки запуска сервера

&НаКлиенте
Процедура ПослеПодключенияМакетаМСП_ЗапускСервера(Подключено, ДопПар) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		ЗапускСервера_ПодключитьТун();
	Иначе
		// Fallback: получить .dll с сервера, записать во временный файл
		ДвоичныеДанные = ПолучитьМакетНаСервере();
		ИмяФайла = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанные.Записать(ИмяФайла);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаМСП_ЗапускСервера", ЭтаФорма),
			ИмяФайла, "MCPHttp");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаМСП_ЗапускСервера(Подключено, ДопПар) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если НЕ Подключено Тогда
		ЗапускСервераВПроцессе = Ложь;
		ДобавитьВЛог("Не удалось подключить внешнюю компоненту MCPHttp");
		ЗавершитьАвтостартСОшибкой("Server add-in load failed");
		Возврат;
	КонецЕсли;
	ЗапускСервера_ПодключитьТун();
КонецПроцедуры

&НаКлиенте
Процедура ЗапускСервера_ПодключитьТун()
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Компонента = Новый("AddIn.MCPHttp.MCPHttpTransport");
	НачатьАсинхронноеПодключениеКомпоненты(
		Новый ОписаниеОповещения("ПослеПодключенияМакетаТун_ЗапускСервера", ЭтаФорма),
		"ВнешняяОбработка.MCPToolkit.Макет.ToonConverter", "ToonConv");
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаТун_ЗапускСервера(Подключено, ДопПар) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		ЗапускСервера_ПодключитьSyntaxHelp(Истина);
	Иначе
		// Fallback: ToonConverter из файла
		ДвоичныеДанные = ПолучитьМакетТунКонвертераНаСервере();
		ИмяФайла = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанные.Записать(ИмяФайла);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаТун_ЗапускСервера", ЭтаФорма),
			ИмяФайла, "ToonConv");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаТун_ЗапускСервера(Подключено, ДопПар) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	ЗапускСервера_ПодключитьSyntaxHelp(Подключено); // ToonConverter некритична: даже если нет — продолжаем
КонецПроцедуры

&НаКлиенте
Процедура ЗапускСервера_ПодключитьSyntaxHelp(ТунПодключён)
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	НачатьАсинхронноеПодключениеКомпоненты(
		Новый ОписаниеОповещения("ПослеПодключенияМакетаSyntaxHelp_Запуск", ЭтаФорма, ТунПодключён),
		"ВнешняяОбработка.MCPToolkit.Макет.SyntaxHelpReader", "SyntaxH");
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаSyntaxHelp_Запуск(Подключено, ТунПодключён) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		СоздатьОбъектSyntaxHelpИИнициализировать();
		ЗапускСервера_ПодключитьScreenCapture(ТунПодключён);
		Возврат;
	КонецЕсли;
	// Fallback через файл
	Попытка
		ДвоичныеДанные = ПолучитьМакетSyntaxHelpНаСервере();
		ВремФайл = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанные.Записать(ВремФайл);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаSyntaxHelp_Запуск", ЭтаФорма, ТунПодключён),
			ВремФайл, "SyntaxH");
	Исключение
		ЗапускСервера_ПодключитьScreenCapture(ТунПодключён); // SyntaxHelp некритична — продолжаем
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаSyntaxHelp_Запуск(Подключено, ТунПодключён) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		СоздатьОбъектSyntaxHelpИИнициализировать();
	КонецЕсли;
	ЗапускСервера_ПодключитьScreenCapture(ТунПодключён);
КонецПроцедуры

&НаКлиенте
Процедура ЗапускСервера_ПодключитьScreenCapture(ТунПодключён)
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	НачатьАсинхронноеПодключениеКомпоненты(
		Новый ОписаниеОповещения("ПослеПодключенияМакетаScreenCapture_Запуск", ЭтаФорма, ТунПодключён),
		"ВнешняяОбработка.MCPToolkit.Макет.ScreenCapture", "ScreenCapture");
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаScreenCapture_Запуск(Подключено, ТунПодключён) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		КомпонентаScreenCapture = Новый("AddIn.ScreenCapture.ScreenCapture");
		КэшСхемИнструментов = Неопределено;
		ЗапускСервера_Финал(ТунПодключён);
		Возврат;
	КонецЕсли;
	Попытка
		ДД = ПолучитьМакетScreenCaptureНаСервере();
		ВремФайл = ПолучитьИмяВременногоФайла("dll");
		ДД.Записать(ВремФайл);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаScreenCapture_Запуск", ЭтаФорма, ТунПодключён),
			ВремФайл, "ScreenCapture");
	Исключение
		ЗапускСервера_Финал(ТунПодключён); // некритична
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаScreenCapture_Запуск(Подключено, ТунПодключён) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если Подключено Тогда
		КомпонентаScreenCapture = Новый("AddIn.ScreenCapture.ScreenCapture");
		КэшСхемИнструментов = Неопределено;
	КонецЕсли;
	ЗапускСервера_Финал(ТунПодключён);
КонецПроцедуры

&НаКлиенте
Процедура ЗапускСервера_Финал(ТунПодключён)
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если ТунПодключён Тогда
		КомпонентаТун = Новый("AddIn.ToonConv.ToonConverter");
	КонецЕсли;
	Компонента.НачатьВызовStart(
		Новый ОписаниеОповещения("ПослеЗапускаВстроенногоСервера", ЭтаФорма),
		ПортВстроенногоСервера);
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗапускаВстроенногоСервера(РезВызова, ПарВызова, ДопПар) Экспорт
	Если НЕ ЗапускСервераВПроцессе Тогда Возврат; КонецЕсли;
	Если РезВызова = Истина Тогда
		ВстроенныйСерверЗапущен = Истина;
		МСПСессии = Новый Соответствие;
		SSEПотоки = Новый Соответствие;
		ЛегасиССЕПотоки = Новый Соответствие;
		ОжидающиеПодтверждения = Новый Соответствие;
		ЗапускСервераВПроцессе = Ложь;
		УстановитьСтатус("Сервер запущен (порт " + Формат(ПортВстроенногоСервера, "ЧГ=") + ")", Истина);
		ДобавитьВЛог("Встроенный HTTP-сервер запущен на порту " + Формат(ПортВстроенногоСервера, "ЧГ="));
		АнонимизаторОчистить();
		Если ИспользоватьТочнуюАнонимизацию Тогда
			ПостроитьИЗаполнитьСловарьАнонимизации();
		КонецЕсли;
		ЗавершитьАвтостартПослеЗапускаВстроенногоСервера();
	Иначе
		ЗапускСервераВПроцессе = Ложь;
		Компонента = Неопределено;
		ДобавитьВЛог("Не удалось запустить сервер на порту " + Формат(ПортВстроенногоСервера, "ЧГ="));
		ЗавершитьАвтостартСОшибкой("Embedded server start failed");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ЗапуститьВстроенныйСерверСинхронно()

	Если ВстроенныйСерверЗапущен Тогда
		ДобавитьВЛог("Сервер уже запущен");
		Возврат;
	КонецЕсли;

	// Подключение компоненты
	Попытка
		Результат = ПодключитьВнешнююКомпоненту(
			"ВнешняяОбработка.MCPToolkit.Макет.MCPHttpTransport",
			"MCPHttp", ТипВнешнейКомпоненты.Native);
		Если НЕ Результат Тогда
			// Fallback: загрузка из макета через файл
			ДвоичныеДанные = ПолучитьМакетНаСервере();
			ИмяФайла = ПолучитьИмяВременногоФайла("dll");
			ДвоичныеДанные.Записать(ИмяФайла);
			ПодключитьВнешнююКомпоненту(ИмяФайла, "MCPHttp", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		Компонента = Новый("AddIn.MCPHttp.MCPHttpTransport");
	Исключение
		ДобавитьВЛог("Ошибка подключения компоненты: " + ОписаниеОшибки());
		ЗавершитьАвтостартСОшибкой("Server add-in load failed (sync): " + ОписаниеОшибки());
		Возврат;
	КонецПопытки;

	// Подключение ToonConverter
	КомпонентаТун = Неопределено;
	Попытка
		РезТун = ПодключитьВнешнююКомпоненту(
			"ВнешняяОбработка.MCPToolkit.Макет.ToonConverter",
			"ToonConv", ТипВнешнейКомпоненты.Native);
		Если НЕ РезТун Тогда
			ДвоичныеДанные = ПолучитьМакетТунКонвертераНаСервере();
			ИмяФайла = ПолучитьИмяВременногоФайла("dll");
			ДвоичныеДанные.Записать(ИмяФайла);
			ПодключитьВнешнююКомпоненту(ИмяФайла, "ToonConv", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		КомпонентаТун = Новый("AddIn.ToonConv.ToonConverter");
	Исключение
		ДобавитьВЛог("Предупреждение: ToonConverter не загружена. TOON-формат недоступен. " + ОписаниеОшибки());
	КонецПопытки;

	// Подключение SyntaxHelpReader
	ПодключитьКомпонентуSyntaxHelpСинхронно();

	// Подключение ScreenCapture
	ПодключитьКомпонентуScreenCaptureСинхронно();

	// Запуск HTTP-сервера
	Если Компонента.Start(ПортВстроенногоСервера) Тогда
		ВстроенныйСерверЗапущен = Истина;
		МСПСессии = Новый Соответствие;
		SSEПотоки = Новый Соответствие;
		ЛегасиССЕПотоки = Новый Соответствие;
		ОжидающиеПодтверждения = Новый Соответствие;
		УстановитьСтатус("Сервер запущен (порт " + Формат(ПортВстроенногоСервера, "ЧГ=") + ")", Истина);
		ДобавитьВЛог("Встроенный HTTP-сервер запущен на порту " + Формат(ПортВстроенногоСервера, "ЧГ="));
		АнонимизаторОчистить();
		Если ИспользоватьТочнуюАнонимизацию Тогда
			ПостроитьИЗаполнитьСловарьАнонимизации();
		КонецЕсли;
		ЗавершитьАвтостартПослеЗапускаВстроенногоСервера();
	Иначе
		ДобавитьВЛог("Не удалось запустить сервер на порту " + Формат(ПортВстроенногоСервера, "ЧГ="));
		Компонента = Неопределено;
		ЗавершитьАвтостартСОшибкой("Embedded server start failed (sync)");
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура СоздатьОбъектSyntaxHelpИИнициализировать()
	Попытка
		КомпонентаSyntaxHelp = Новый("AddIn.SyntaxH.SyntaxHelpReader");
		КолвоТемBSL = КомпонентаSyntaxHelp.Инициализировать(КаталогПрограммы());
		Если КолвоТемBSL < 0 Тогда
			ДобавитьВЛог("SyntaxHelpReader: ошибка парсинга .hbk файлов (код " + КолвоТемBSL + ")");
			КомпонентаSyntaxHelp = Неопределено;
		ИначеЕсли КолвоТемBSL = 0 Тогда
			ДобавитьВЛог("SyntaxHelpReader: тем не найдено. Проверьте наличие shcntx_ru.hbk и shquery_ru.hbk в " + КаталогПрограммы());
		Иначе
			ДобавитьВЛог("SyntaxHelpReader загружен, тем: " + КолвоТемBSL);
			КэшСхемИнструментов = Неопределено; // сбросить кэш — инструмент теперь доступен
		КонецЕсли;
	Исключение
		КомпонентаSyntaxHelp = Неопределено;
		ДобавитьВЛог("SyntaxHelpReader: исключение при инициализации: " + ОписаниеОшибки());
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПодключитьКомпонентуSyntaxHelpСинхронно()
	Попытка
		Если НЕ ПодключитьВнешнююКомпоненту(
				"ВнешняяОбработка.MCPToolkit.Макет.SyntaxHelpReader",
				"SyntaxH", ТипВнешнейКомпоненты.Native) Тогда
			ДвоичныеДанные = ПолучитьМакетSyntaxHelpНаСервере();
			ВремФайл = ПолучитьИмяВременногоФайла("dll");
			ДвоичныеДанные.Записать(ВремФайл);
			ПодключитьВнешнююКомпоненту(ВремФайл, "SyntaxH", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		СоздатьОбъектSyntaxHelpИИнициализировать();
	Исключение
		КомпонентаSyntaxHelp = Неопределено;
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаSyntaxHelpProxy(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		СоздатьОбъектSyntaxHelpИИнициализировать();
		Возврат;
	КонецЕсли;
	Попытка
		ДвоичныеДанные = ПолучитьМакетSyntaxHelpНаСервере();
		ВремФайл = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанные.Записать(ВремФайл);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаSyntaxHelpProxy", ЭтаФорма),
			ВремФайл, "SyntaxH");
	Исключение
		// SyntaxHelp некритична — тихо игнорируем
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаSyntaxHelpProxy(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		СоздатьОбъектSyntaxHelpИИнициализировать();
	КонецЕсли;
КонецПроцедуры

&НаСервере
Функция ПолучитьМакетНаСервере()
	Обработка = РеквизитФормыВЗначение("Объект");
	Возврат Обработка.ПолучитьМакет("MCPHttpTransport");
КонецФункции

&НаСервере
Функция ПолучитьМакетТунКонвертераНаСервере()
	Обработка = РеквизитФормыВЗначение("Объект");
	Возврат Обработка.ПолучитьМакет("ToonConverter");
КонецФункции

&НаСервере
Функция ПолучитьМакетSyntaxHelpНаСервере()
	Возврат РеквизитФормыВЗначение("Объект").ПолучитьМакет("SyntaxHelpReader");
КонецФункции

&НаКлиенте
Процедура ПослеПодключенияМакетаScreenCaptureProxy(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		КомпонентаScreenCapture = Новый("AddIn.ScreenCapture.ScreenCapture");
		КэшСхемИнструментов = Неопределено;
		Возврат;
	КонецЕсли;
	Попытка
		ДД = ПолучитьМакетScreenCaptureНаСервере();
		ВремФайл = ПолучитьИмяВременногоФайла("dll");
		ДД.Записать(ВремФайл);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаScreenCaptureProxy", ЭтаФорма),
			ВремФайл, "ScreenCapture");
	Исключение
		// ScreenCapture некритична
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаScreenCaptureProxy(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		КомпонентаScreenCapture = Новый("AddIn.ScreenCapture.ScreenCapture");
		КэшСхемИнструментов = Неопределено;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПодключитьКомпонентуScreenCaptureСинхронно()
	Попытка
		Если НЕ ПодключитьВнешнююКомпоненту(
				"ВнешняяОбработка.MCPToolkit.Макет.ScreenCapture",
				"ScreenCapture", ТипВнешнейКомпоненты.Native) Тогда
			ДД = ПолучитьМакетScreenCaptureНаСервере();
			ВремФайл = ПолучитьИмяВременногоФайла("dll");
			ДД.Записать(ВремФайл);
			ПодключитьВнешнююКомпоненту(ВремФайл, "ScreenCapture", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		КомпонентаScreenCapture = Новый("AddIn.ScreenCapture.ScreenCapture");
		КэшСхемИнструментов = Неопределено;
	Исключение
		КомпонентаScreenCapture = Неопределено;
	КонецПопытки;
КонецПроцедуры

&НаСервере
Функция ПолучитьМакетScreenCaptureНаСервере()
	Возврат РеквизитФормыВЗначение("Объект").ПолучитьМакет("ScreenCapture");
КонецФункции

&НаСервере
Функция ПолучитьМакетLineageАнализатораНаСервере()
	Обработка = РеквизитФормыВЗначение("Объект");
	Возврат Обработка.ПолучитьМакет("QueryLineageAnalyzer");
КонецФункции

&НаСервере
Функция ПроверитьНужноАсинхронноеПодключениеКомпонент()
	// Читаем настройку через Выполнить, чтобы избежать ошибки компиляции на 8.2.x:
	// прямое обращение Метаданные.РежимИспользованияСинхронныхВызовов
	// может не компилироваться на платформах, где это свойство не определено.
	//
	// Значения РежимИспользованияСинхронныхВызовов:
	//   "Использовать"                  — sync ОК, async не нужен
	//   "ИспользоватьСПредупреждениями" — sync пишет предупреждения → лучше async
	//   "НеИспользовать"               — sync ЗАБЛОКИРОВАН → async обязателен
	// Полное имя свойства (из документации ОбъектМетаданныхКонфигурация):
	// РежимИспользованияСинхронныхВызововРасширенийПлатформыИВнешнихКомпонент
	// Доступность: Сервер, толстый клиент, внешнее соединение.
	// Через Выполнить — чтобы не дать compile error на 8.2.x.
	Попытка
		Рез = "";
		Выполнить("Рез = Строка(Метаданные.РежимИспользованияСинхронныхВызововРасширенийПлатформыИВнешнихКомпонент)");
		Возврат Рез <> "Использовать";
	Исключение
		Возврат Ложь; // 8.2.x или старые 8.3.x: свойства нет → синхронный вызов безопасен
	КонецПопытки;
КонецФункции

&НаКлиенте
Функция ПолучитьТекстЗапросаLineageИзКоманды(Команда)
	Если Команда = Неопределено Или ТипЗнч(Команда) <> Тип("Структура") Тогда
		Возврат "";
	КонецЕсли;
	Если НЕ Команда.Свойство("params") Тогда
		Возврат "";
	КонецЕсли;
	ПараметрыКомандыLineage = Команда.params;
	Если ПараметрыКомандыLineage = Неопределено Или ТипЗнч(ПараметрыКомандыLineage) <> Тип("Структура") Тогда
		Возврат "";
	КонецЕсли;
	Если ПараметрыКомандыLineage.Свойство("query") Тогда
		Возврат ПараметрыКомандыLineage.query;
	КонецЕсли;
	Возврат "";
КонецФункции

&НаКлиенте
Функция НуженLineageДляРезультата(ИмяИнструмента, ТекстЗапроса, Результат)
	Если СокрЛП(ИмяИнструмента) <> "execute_query" Тогда
		Возврат Ложь;
	КонецЕсли;
	Если ПустаяСтрока(ТекстЗапроса) Тогда
		Возврат Ложь;
	КонецЕсли;
	Если Результат = Неопределено Или ТипЗнч(Результат) <> Тип("Структура") Тогда
		Возврат Ложь;
	КонецЕсли;
	Если НЕ Результат.Свойство("success") Или НЕ Результат.success Тогда
		Возврат Ложь;
	КонецЕсли;
	Если НЕ Результат.Свойство("schema") Тогда
		Возврат Ложь;
	КонецЕсли;
	Если Результат.schema = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ПодключитьКомпонентуLineageСинхронно()
	Если КомпонентаLineage <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	Попытка
		РезLineage = ПодключитьВнешнююКомпоненту(
			"ВнешняяОбработка.MCPToolkit.Макет.QueryLineageAnalyzer",
			"Lineage", ТипВнешнейКомпоненты.Native);
		Если НЕ РезLineage Тогда
			ДвоичныеДанныеLineage = ПолучитьМакетLineageАнализатораНаСервере();
			ИмяФайлаLineage = ПолучитьИмяВременногоФайла("dll");
			ДвоичныеДанныеLineage.Записать(ИмяФайлаLineage);
			ПодключитьВнешнююКомпоненту(ИмяФайлаLineage, "Lineage", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		КомпонентаLineage = Новый("AddIn.Lineage.QueryLineageAnalyzer");
		Возврат Истина;
	Исключение
		ДобавитьВЛог("Предупреждение: QueryLineageAnalyzer не загружена. " + ОписаниеОшибки());
		КомпонентаLineage = Неопределено;
		Возврат Ложь;
	КонецПопытки;
КонецФункции

&НаКлиенте
Функция ОбогатитьСхемуИсточниками(ТекстЗапроса, Результат)
	Если НЕ НуженLineageДляРезультата("execute_query", ТекстЗапроса, Результат) Тогда
		Возврат Результат;
	КонецЕсли;
	Если НЕ ПодключитьКомпонентуLineageСинхронно() Тогда
		Возврат Результат;
	КонецЕсли;
	Попытка
		СхемаJSONLineage = ЗаписатьJSON2(Результат.schema);
		ОбогащеннаяСхемаJSON = КомпонентаLineage.AnalyzeSources(ТекстЗапроса, СхемаJSONLineage);
		Если ЗначениеЗаполнено(ОбогащеннаяСхемаJSON) Тогда
			Результат.schema = ПрочитатьJSON2(ОбогащеннаяСхемаJSON);
		КонецЕсли;
	Исключение
		ДобавитьВЛог("Предупреждение: ошибка enrichment schema lineage: " + ОписаниеОшибки());
	КонецПопытки;
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура НачатьПодключениеКомпонентыLineage(КонтекстLineage)
	НачатьАсинхронноеПодключениеКомпоненты(
		Новый ОписаниеОповещения("ПослеПодключенияМакетаLineage", ЭтаФорма, КонтекстLineage),
		"ВнешняяОбработка.MCPToolkit.Макет.QueryLineageAnalyzer", "Lineage");
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаLineage(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		Попытка
			КомпонентаLineage = Новый("AddIn.Lineage.QueryLineageAnalyzer");
			ЗапуститьAsyncLineageАнализ(ДопПар);
			Возврат;
		Исключение
		КонецПопытки;
	КонецЕсли;

	Попытка
		ДвоичныеДанныеLineage = ПолучитьМакетLineageАнализатораНаСервере();
		ИмяФайлаLineage = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанныеLineage.Записать(ИмяФайлаLineage);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаLineage", ЭтаФорма, ДопПар),
			ИмяФайлаLineage, "Lineage");
	Исключение
		ПродолжитьПослеLineage(ДопПар);
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаLineage(Подключено, ДопПар) Экспорт
	Если Подключено Тогда
		Попытка
			КомпонентаLineage = Новый("AddIn.Lineage.QueryLineageAnalyzer");
			ЗапуститьAsyncLineageАнализ(ДопПар);
			Возврат;
		Исключение
		КонецПопытки;
	КонецЕсли;
	ПродолжитьПослеLineage(ДопПар);
КонецПроцедуры

&НаКлиенте
Процедура НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage)
	Если КомпонентаLineage = Неопределено Тогда
		НачатьПодключениеКомпонентыLineage(КонтекстLineage);
	Иначе
		ЗапуститьAsyncLineageАнализ(КонтекстLineage);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ЗапуститьAsyncLineageАнализ(КонтекстLineage)
	Попытка
		СхемаJSONLineage = ЗаписатьJSON2(КонтекстLineage.Результат.schema);
		КомпонентаLineage.НачатьВызовAnalyzeSources(
			Новый ОписаниеОповещения("ПослеAnalyzeSourcesОбщий", ЭтаФорма, КонтекстLineage),
			КонтекстLineage.ТекстЗапроса, СхемаJSONLineage);
	Исключение
		ПродолжитьПослеLineage(КонтекстLineage);
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеAnalyzeSourcesОбщий(РезВызова, ПарВызова, ДопПар) Экспорт
	Попытка
		Если ЗначениеЗаполнено(РезВызова) Тогда
			ДопПар.Результат.schema = ПрочитатьJSON2(РезВызова);
		КонецЕсли;
	Исключение
	КонецПопытки;
	ПродолжитьПослеLineage(ДопПар);
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьПослеLineage(КонтекстLineage)
	Если КонтекстLineage.Свойство("Результат") Тогда
		Если ИспользоватьТочнуюАнонимизацию Тогда
			Если РежимПодключения <> 0
				И ТипЗнч(КонтекстLineage.Результат) = Тип("Структура")
				И КонтекстLineage.Результат.Свойство("data") Тогда
				КонтекстLineage.Результат.Вставить("_original_data_json",
					ЗаписатьJSON2(КонтекстLineage.Результат.data));
				КонтекстLineage.Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			КонтекстLineage.Результат = ПрименитьТочнуюPolicyКРезультатуExecuteQuery(КонтекстLineage.Результат, РежимПодключения = 0);
			Если РежимПодключения <> 0 Тогда
				РегексКонтекст = Новый Структура;
				РегексКонтекст.Вставить("Результат",            КонтекстLineage.Результат);
				РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
				РегексКонтекст.Вставить("ТекстыJSON",            "");
				РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
				РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
				РегексКонтекст.Вставить("КонтекстLineage",       КонтекстLineage);
				АнонимизироватьРезультатВстроенный(КонтекстLineage.Результат, РегексКонтекст);
				ЗапуститьRegexФазу(РегексКонтекст);
				Возврат; // продолжение в ПродолжитьПослеRegex
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	// Если анонимизация не нужна или режим = 0 — сразу финальная обработка
	ПродолжитьПослеRegexБезКонтекста(КонтекстLineage);
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьПослеRegexБезКонтекста(КонтекстLineage)
	Если КонтекстLineage.Свойство("Результат")
		И ТипЗнч(КонтекстLineage.Результат) = Тип("Структура") Тогда
		Если КонтекстLineage.Результат.Свойство("_anon_schema_forced")
			И КонтекстLineage.Результат._anon_schema_forced Тогда
			Попытка КонтекстLineage.Результат.Удалить("schema");               Исключение КонецПопытки;
			Попытка КонтекстLineage.Результат.Удалить("_anon_schema_forced");  Исключение КонецПопытки;
			// _original_data_json НЕ удаляем здесь — нужен для ДобавитьВЛог в proxy-пути (L13001).
			// Downstream-функции (ОтправитьРезультатНаСервер whitelist, ОчиститьВнутренниеПоляPolicy)
			// уберут его до отправки к AI.
			Попытка КонтекстLineage.Результат.Удалить("_blind_anonymization"); Исключение КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	РежимLineage = "";
	Если КонтекстLineage.Свойство("Режим", РежимLineage) = Ложь Тогда
		Возврат;
	КонецЕсли;

	Если РежимLineage = "proxy" Тогда
		ПродолжитьОтправкуРезультатаПроксиПослеLineage(КонтекстLineage);
	ИначеЕсли РежимLineage = "rest" Тогда
		ПродолжитьRESTОтветПослеLineage(КонтекстLineage);
	ИначеЕсли РежимLineage = "mcp" Тогда
		ПродолжитьMCPОтветПослеLineage(КонтекстLineage);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьОтправкуРезультатаПроксиПослеLineage(КонтекстLineage)
	РезультатОтправкиLineage = ОтправитьРезультатНаСервер(АдресСервера, КонтекстLineage.КомандаId, КонтекстLineage.Результат);
	Если РезультатОтправкиLineage.Успех Тогда
		ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(КонтекстLineage.Результат));
	Иначе
		ДобавитьВЛог("Ошибка отправки результата: " + РезультатОтправкиLineage.ОписаниеОшибки);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьRESTОтветПослеLineage(КонтекстLineage)
	// Точка перехвата: confirm анонимизации до async Toon — иначе при ФорматОтветаВстроенный=1 обходится
	Если ПроверитьПодтверждениеАнонимизации(КонтекстLineage.Результат,
		КонтекстLineage.ДанныеЗапроса, "", "", "rest") Тогда
		Возврат;
	КонецЕсли;

	Если ИспользоватьАсинхронноеПодключениеКомпонент И КомпонентаТун <> Неопределено
		И ФорматОтветаВстроенный = 1 И ТипЗнч(КонтекстLineage.Результат) = Тип("Структура")
		И КонтекстLineage.Результат.Свойство("success") И КонтекстLineage.Результат.success
		И КонтекстLineage.Результат.Свойство("data") Тогда
		КомпонентаТун.НачатьВызовJsonToToon(
			Новый ОписаниеОповещения("ПослеJsonToToonREST", ЭтаФорма,
				Новый Структура("ДанныеЗапроса, Результат", КонтекстLineage.ДанныеЗапроса, КонтекстLineage.Результат)),
			ЗаписатьJSON2(КонтекстLineage.Результат.data));
		Возврат;
	КонецЕсли;

	КонтекстLineage.Результат = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(КонтекстLineage.Результат);
	ПрименитьТунКонвертацию(КонтекстLineage.Результат);
	КомпонентаSendResponse(КонтекстLineage.ДанныеЗапроса.id, 200,
		ЗаголовкиJSON(), ЗаписатьJSON2(КонтекстLineage.Результат));
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьMCPОтветПослеLineage(КонтекстLineage)
	// Точка перехвата C: после lineage/regex async
	Если ПроверитьПодтверждениеАнонимизации(КонтекстLineage.Результат,
		КонтекстLineage.ДанныеЗапроса, КонтекстLineage.Ид, КонтекстLineage.ИдПотока) Тогда
		Возврат;
	КонецЕсли;

	ТулКолРезLineage = СформироватьToolsCallРезультат(
		КонтекстLineage.Результат, КонтекстLineage.ДанныеЗапроса,
		КонтекстLineage.Ид, КонтекстLineage.ИдПотока);
	Если ТулКолРезLineage = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ОтветJSONLineage = СформироватьJSONRPCОтвет(КонтекстLineage.Ид, ТулКолРезLineage);
	Если ЗначениеЗаполнено(КонтекстLineage.ИдПотока) Тогда
		КомпонентаSendSSEEvent(КонтекстLineage.ИдПотока, ОтветJSONLineage, "{}", "message");
	Иначе
		ОтправитьSSEОтвет(КонтекстLineage.ДанныеЗапроса, ОтветJSONLineage);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОстановитьВстроенныйСервер()

	Если НЕ ВстроенныйСерверЗапущен Тогда
		Возврат;
	КонецЕсли;

	// Весь teardown-код ДО вызова Stop
	SSEПотоки = Новый Соответствие;
	ЛегасиССЕПотоки = Новый Соответствие;
	МСПСессии = Новый Соответствие;
	ОжидающиеПодтверждения = Новый Соответствие;
	ВстроенныйСерверЗапущен = Ложь;
	АнонимизаторОчистить();
	ОбновитьТаблицуМаппинговВстроенный();
	УстановитьСтатус("Отключено", Ложь);
	ДобавитьВЛог("Встроенный HTTP-сервер остановлен");

	Если Компонента <> Неопределено Тогда
		Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
			Компонента.НачатьВызовStop(
				Новый ОписаниеОповещения("ИгнорироватьРезультатКомпоненты", ЭтаФорма));
		Иначе
			Компонента.Stop();
		КонецЕсли;
	КонецЕсли;

	Компонента = Неопределено;
	КомпонентаТун = Неопределено;

КонецПроцедуры

// Пустой колбэк для fire-and-forget вызовов компоненты
&НаКлиенте
Процедура ИгнорироватьРезультатКомпоненты(РезВызова, ПарВызова, ДопПар) Экспорт
КонецПроцедуры

// Обёртка SendResponse
&НаКлиенте
Процедура КомпонентаSendResponse(РзId, КодОтвета, Заголовки, Тело)
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		Компонента.НачатьВызовSendResponse(
			Новый ОписаниеОповещения("ИгнорироватьРезультатКомпоненты", ЭтаФорма),
			РзId, КодОтвета, Заголовки, Тело);
	Иначе
		Компонента.SendResponse(РзId, КодОтвета, Заголовки, Тело);
	КонецЕсли;
КонецПроцедуры

// Обёртка SendSSEEvent
&НаКлиенте
Процедура КомпонентаSendSSEEvent(РзId, Данные, Заголовки, ТипСобытия = "message")
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		Компонента.НачатьВызовSendSSEEvent(
			Новый ОписаниеОповещения("ИгнорироватьРезультатКомпоненты", ЭтаФорма),
			РзId, Данные, Заголовки, ТипСобытия);
	Иначе
		Компонента.SendSSEEvent(РзId, Данные, Заголовки, ТипСобытия);
	КонецЕсли;
КонецПроцедуры

// Обёртка CloseSSEStream
&НаКлиенте
Процедура КомпонентаCloseSSEStream(РзId)
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		Компонента.НачатьВызовCloseSSEStream(
			Новый ОписаниеОповещения("ИгнорироватьРезультатКомпоненты", ЭтаФорма),
			РзId);
	Иначе
		Компонента.CloseSSEStream(РзId);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьТунКонвертацию(Результат)
	Если ТипЗнч(Результат) <> Тип("Структура") Тогда
		Возврат;
	КонецЕсли;
	Если ФорматОтветаВстроенный <> 1 Или КомпонентаТун = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Если НЕ Результат.Свойство("success") Или НЕ Результат.success Тогда
		Возврат;
	КонецЕсли;
	Если НЕ Результат.Свойство("data") Тогда
		Возврат;
	КонецЕсли;
	ДанныеТун = КомпонентаТун.JsonToToon(ЗаписатьJSON2(Результат.data));
	Если ЗначениеЗаполнено(ДанныеТун) Тогда
		Результат.data = ДанныеТун;
	КонецЕсли;
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ОБРАБОТКА ВНЕШНИХ СОБЫТИЙ (диспетчер)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбработкаВнешнегоСобытия(Источник, Событие, Данные)

	Если Источник <> "MCPHttpTransport" Тогда Возврат КонецЕсли;

	ДанныеЗапроса = ПрочитатьJSON2(Данные);

	// SSE_CLOSED обрабатываем ДО bodyTruncated — у SSE_CLOSED нет этого поля
	Если Событие = "SSE_CLOSED" Тогда
		// Удаляем из SSEПотоки по requestId (идемпотентно)
		Для Каждого КлючЗначение Из SSEПотоки Цикл
			Если КлючЗначение.Значение = ДанныеЗапроса.id Тогда
				SSEПотоки.Удалить(КлючЗначение.Ключ);
				Прервать;
			КонецЕсли;
		КонецЦикла;
		// Удаляем из ЛегасиССЕПотоки по requestId GET-потока
		Для Каждого КлючЗначение Из ЛегасиССЕПотоки Цикл
			Если КлючЗначение.Значение = ДанныеЗапроса.id Тогда
				ЛегасиССЕПотоки.Удалить(КлючЗначение.Ключ);
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Возврат;
	КонецЕсли;

	// bodyTruncated — только для HTTP-запросов (REQUEST, MCP_POST, SSE_CONNECT)
	// Сохраняем Событие в ДанныеЗапроса для передачи в async-колбэк
	ДанныеЗапроса.Вставить("_событие", Событие);

	БольшоеТело = Ложь;
	Если ДанныеЗапроса.Свойство("bodyTruncated", БольшоеТело) И БольшоеТело = Истина Тогда
		Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
			// async-ветка: получаем тело через колбэк, маршрутизация в колбэке
			Компонента.НачатьВызовGetRequestBody(
				Новый ОписаниеОповещения("ПослеПолученияТелаЗапроса", ЭтаФорма, ДанныеЗапроса),
				ДанныеЗапроса.id);
			Возврат;
		Иначе
			ДанныеЗапроса.body = Компонента.GetRequestBody(ДанныеЗапроса.id);
		КонецЕсли;
	КонецЕсли;

	ПродолжитьОбработкуЗапроса(ДанныеЗапроса);

КонецПроцедуры

&НаКлиенте
Процедура ПослеПолученияТелаЗапроса(РезВызова, ПарВызова, ДопПар) Экспорт
	// ДопПар = ДанныеЗапроса (Структура/Соответствие)
	// НЕ использовать ЗапускСервераВПроцессе — это не startup chain!
	ДопПар.body = РезВызова;
	ПродолжитьОбработкуЗапроса(ДопПар);
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьОбработкуЗапроса(ДанныеЗапроса)
	Событие = "";
	ДанныеЗапроса.Свойство("_событие", Событие);
	Если Событие = "SSE_CONNECT" Тогда
		ОбработатьSSEConnect(ДанныеЗапроса);
	ИначеЕсли Событие = "SSE_LEGACY_CONNECT" Тогда
		ОбработатьSSELegacyConnect(ДанныеЗапроса);
	ИначеЕсли Событие = "SSE_LEGACY_MESSAGE" Тогда
		ОбработатьSSELegacyMessage(ДанныеЗапроса);
	ИначеЕсли Событие = "MCP_POST" Тогда
		ОбработатьMCPPost(ДанныеЗапроса);
	Иначе // "REQUEST"
		ОбработатьОбычныйЗапрос(ДанныеЗапроса);
	КонецЕсли;
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ОБРАБОТКА ОБЫЧНЫХ ЗАПРОСОВ (REST, health, DELETE /mcp)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбработатьОбычныйЗапрос(ДанныеЗапроса)

	Путь = ДанныеЗапроса.path;

	Если Путь = "/health" Тогда
		КомпонентаSendResponse(ДанныеЗапроса.id, 200,
			ЗаголовкиJSON(), СформироватьОтветHealth());

	ИначеЕсли Лев(Путь, 5) = "/api/" Тогда
		Ответ = ОбработатьRESTЗапрос(ДанныеЗапроса);
		Если Ответ <> Неопределено Тогда
			КомпонентаSendResponse(ДанныеЗапроса.id,
				Ответ.КодОтвета, ЗаголовкиJSON(), Ответ.Тело);
		КонецЕсли;

	ИначеЕсли Путь = "/mcp" И ДанныеЗапроса.method = "DELETE" Тогда
		ОбработатьDeleteСессии(ДанныеЗапроса);

	Иначе
		КомпонентаSendResponse(ДанныеЗапроса.id, 404,
			ЗаголовкиJSON(), "{""error"":""Not Found""}");
	КонецЕсли;

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// MCP POST /mcp (Streamable HTTP)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбработатьMCPPost(ДанныеЗапроса)

	// 1. Content-Type
	КонтентТип = ДанныеЗапроса.headers["content-type"];
	Если КонтентТип = Неопределено
		ИЛИ НЕ (Лев(НРег(КонтентТип), 16) = "application/json") Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 415,
			"Unsupported Media Type: Content-Type must be application/json",
			-32600);
		Возврат;
	КонецЕсли;

	// 2. Accept: оба application/json И text/event-stream
	Если НЕ ПроверитьAcceptPOST(ДанныеЗапроса) Тогда Возврат КонецЕсли;

	// 4. Парсинг JSON body
	Попытка
		ТелоJSON = ПрочитатьJSON2(ДанныеЗапроса.body);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 400,
			"Parse error: " + ТекстОшибки,
			-32700);
		Возврат;
	КонецПопытки;

	Если ТипЗнч(ТелоJSON) <> Тип("Структура") Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 400,
			"Validation error: JSON body must be an object",
			-32602);
		Возврат;
	КонецЕсли;

	// 5. Определяем тип JSON-RPC: Request / Notification / Response
	Метод = Неопределено;
	Ид = Неопределено;
	ЕстьМетод = ТелоJSON.Свойство("method", Метод);
	ЕстьИд = ТелоJSON.Свойство("id", Ид) И (Ид <> Null);

	// 6. Notification или Response → 202
	Если НЕ ЕстьМетод ИЛИ НЕ ЕстьИд Тогда
		// Для non-request сообщений требуется валидная сессия/версия протокола
		// (initialize сюда не попадает, т.к. initialize всегда request).
		Если НЕ ПроверитьПротоколВерсию(ДанныеЗапроса) Тогда Возврат КонецЕсли;
		Если НЕ ПроверитьСессию(ДанныеЗапроса) Тогда Возврат КонецЕсли;
		КомпонентаSendResponse(ДанныеЗапроса.id, 202, ЗаголовкиJSON(), "");
		Возврат;
	КонецЕсли;

	// === 7. JSON-RPC Request (method + id) → SSE-ответ ===

	// 8. mcp-session-id / mcp-protocol-version:
	// В streamable_http initialize договаривается по protocolVersion в params,
	// поэтому версию из заголовка для initialize НЕ валидируем.
	Если Метод <> "initialize" Тогда
		Если НЕ ПроверитьПротоколВерсию(ДанныеЗапроса) Тогда Возврат КонецЕсли;
		Если НЕ ПроверитьСессию(ДанныеЗапроса) Тогда Возврат КонецЕсли;
	КонецЕсли;

	Если Метод = "initialize" Тогда
		РезультатInit = ОбработатьInitialize(ТелоJSON, ДанныеЗапроса);
		ОтправитьSSEОтвет(ДанныеЗапроса,
			СформироватьJSONRPCОтвет(Ид, РезультатInit.Результат),
			РезультатInit.ИдСессии);
		Возврат;

	ИначеЕсли Метод = "tools/list" Тогда
		Результат = ПолучитьСхемыИнструментов();

	ИначеЕсли Метод = "tools/call" Тогда
		Результат = ОбработатьToolsCall(ДанныеЗапроса, ТелоJSON, Ид);
		Если Результат = Неопределено Тогда Возврат КонецЕсли; // async approval

	ИначеЕсли Метод = "ping" Тогда
		Результат = Новый Структура;

	Иначе
		ОтправитьSSEОтвет(ДанныеЗапроса,
			СформироватьJSONRPCОшибку(Ид, -32601, "Method not found"));
		Возврат;
	КонецЕсли;

	ОтправитьSSEОтвет(ДанныеЗапроса,
		СформироватьJSONRPCОтвет(Ид, Результат));

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// GET /mcp (SSE notification stream)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбработатьSSEConnect(ДанныеЗапроса)

	Если НЕ ПроверитьAcceptSSE(ДанныеЗапроса) Тогда Возврат КонецЕсли;
	Если НЕ ПроверитьПротоколВерсию(ДанныеЗапроса) Тогда Возврат КонецЕсли;

	// mcp-session-id опционален: клиент открывает SSE до initialize (до получения session ID)
	ИдСессии = ДанныеЗапроса.headers["mcp-session-id"];

	Если ИдСессии <> Неопределено Тогда
		// Session ID предоставлен — проверяем валидность
		Если НЕ ВалидныйФорматСессии(ИдСессии) ИЛИ МСПСессии.Получить(ИдСессии) = Неопределено Тогда
			ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 404,
				"Not Found: Invalid or expired session ID",
				-32600);
			Возврат;
		КонецЕсли;
		// Проверка: один GET stream на сессию
		Если SSEПотоки.Получить(ИдСессии) <> Неопределено Тогда
			ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 409,
				"Conflict: Only one SSE stream is allowed per session",
				-32600);
			Возврат;
		КонецЕсли;
	КонецЕсли;

	// Стартуем SSE-поток (пустой eventData = только заголовки)
	КомпонентаSendSSEEvent(ДанныеЗапроса.id, "",
		СформироватьSSEЗаголовки(ДанныеЗапроса));

	// Ключ: session ID если есть, иначе request ID (очистка по SSE_CLOSED работает по значению)
	КлючПотока = ?(ИдСессии <> Неопределено, ИдСессии, ДанныеЗапроса.id);
	SSEПотоки.Вставить(КлючПотока, ДанныеЗапроса.id);

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ОБРАБОТКА LEGACY SSE (MCP 2024-11-05 transport type = "sse")
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Процедура ОбработатьSSELegacyConnect(ДанныеЗапроса)
	Если НЕ ПроверитьAcceptSSE(ДанныеЗапроса) Тогда Возврат КонецЕсли;

	// Открываем SSE-поток (Content-Type выставит C++ через set_chunked_content_provider)
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		// async-ветка: первый SendSSEEvent с колбэком, логика маппинга и второй SendSSEEvent — в колбэке
		Компонента.НачатьВызовSendSSEEvent(
			Новый ОписаниеОповещения("ПослеОткрытияSSELegacyПотока", ЭтаФорма, ДанныеЗапроса),
			ДанныеЗапроса.id, "", СформироватьSSEЗаголовки(ДанныеЗапроса), "message");
		Возврат;
	КонецЕсли;

	// sync-ветка: оригинальный код
	КомпонентаSendSSEEvent(ДанныеЗапроса.id, "", СформироватьSSEЗаголовки(ДанныеЗапроса));

	// Генерируем transport session_id (hex без дефисов)
	ТрИдСессии = СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", "");

	// Маппинг: transport session → request ID GET-потока
	ЛегасиССЕПотоки.Вставить(ТрИдСессии, ДанныеЗапроса.id);

	// Шлём event: endpoint — клиент будет постить на этот URL
	УРЛ = "/mcp/message?session_id=" + ТрИдСессии;
	КомпонентаSendSSEEvent(ДанныеЗапроса.id, УРЛ, "{}", "endpoint");
КонецПроцедуры

&НаКлиенте
Процедура ПослеОткрытияSSELegacyПотока(РезВызова, ПарВызова, ДопПар) Экспорт
	// ДопПар = ДанныеЗапроса
	// Генерируем transport session_id (hex без дефисов)
	ТрИдСессии = СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", "");

	// Маппинг: transport session → request ID GET-потока
	ЛегасиССЕПотоки.Вставить(ТрИдСессии, ДопПар.id);

	// Шлём event: endpoint — клиент будет постить на этот URL
	УРЛ = "/mcp/message?session_id=" + ТрИдСессии;
	КомпонентаSendSSEEvent(ДопПар.id, УРЛ, "{}", "endpoint");
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьSSELegacyMessage(ДанныеЗапроса)
	// Найти GET-поток по session_id из query
	МассивSessionId = ДанныеЗапроса.query["session_id"];
	Если МассивSessionId = Неопределено ИЛИ МассивSessionId.Количество() = 0 Тогда Возврат КонецЕсли;
	ТрИдСессии = МассивSessionId[0];

	ИдПотока = ЛегасиССЕПотоки.Получить(ТрИдСессии);
	Если ИдПотока = Неопределено Тогда Возврат КонецЕсли; // 202 уже отправлен C++

	// Разобрать JSON тело
	ТелоJSON = ПрочитатьJSON2(ДанныеЗапроса.body);
	Если ТелоJSON = Неопределено Тогда Возврат КонецЕсли;

	Метод = "";
	ТелоJSON.Свойство("method", Метод);
	Ид = Неопределено;
	ТелоJSON.Свойство("id", Ид);

	// Нотификации (нет id или id = null) — ничего не отвечаем
	Если Ид = Неопределено ИЛИ Ид = Null Тогда Возврат КонецЕсли;

	ОтветJSON = Неопределено;
	Если Метод = "initialize" Тогда
		ОтветJSON = ОбработатьLegacyInitialize(ТелоJSON, Ид);
	ИначеЕсли Метод = "tools/list" Тогда
		ОтветJSON = СформироватьJSONRPCОтвет(Ид, ПолучитьСхемыИнструментов());
	ИначеЕсли Метод = "tools/call" Тогда
		РезультатВызова = ОбработатьToolsCall(ДанныеЗапроса, ТелоJSON, Ид, ИдПотока);
		Если РезультатВызова <> Неопределено Тогда
			ОтветJSON = СформироватьJSONRPCОтвет(Ид, РезультатВызова);
		КонецЕсли; // Неопределено = async, диалог пришлёт сам через ИдПотокаSSE
	ИначеЕсли Метод = "ping" Тогда
		ОтветJSON = СформироватьJSONRPCОтвет(Ид, Новый Структура);
	Иначе
		ОтветJSON = СформироватьJSONRPCОшибку(Ид, -32601, "Method not found");
	КонецЕсли;

	Если ОтветJSON <> Неопределено Тогда
		КомпонентаSendSSEEvent(ИдПотока, ОтветJSON, "{}", "message");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Функция ОбработатьLegacyInitialize(ТелоJSON, Ид)
	// Аналог ОбработатьInitialize, но без создания MCP-сессии и без mcp-session-id в ответе.
	// Всегда используем протокол 2024-11-05 (legacy SSE transport)

	Результат = Новый Структура;
	Результат.Вставить("protocolVersion", "2024-11-05");
	Результат.Вставить("capabilities", Новый Структура("tools",
		Новый Структура("listChanged", Ложь)));
	Результат.Вставить("serverInfo", Новый Структура("name, version",
		"1C MCP Toolkit (Native)", "1.0.0"));

	Возврат СформироватьJSONRPCОтвет(Ид, Результат);
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ВАЛИДАЦИЯ ЗАГОЛОВКОВ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьТелоJSONRPCОшибка(Сообщение, КодОшибки)
	
	// Формат ошибок как в streamable_http (python mcp):
	// {"jsonrpc":"2.0","id":"server-error","error":{"code":-32600,"message":"..."}}
	
	Тело = Новый Структура;
	Тело.Вставить("jsonrpc", "2.0");
	Тело.Вставить("id", "server-error");
	Тело.Вставить("error", Новый Структура("code, message", КодОшибки, Сообщение));
	Возврат ЗаписатьJSON2(Тело);
	
КонецФункции

&НаКлиенте
Функция ЗаголовкиJSONMCP(ДанныеЗапроса)
	
	// Как в streamable_http: Content-Type: application/json + (если сессия валидна) mcp-session-id.
	
	Если МСПСессии = Неопределено Тогда
		Возврат ЗаголовкиJSON();
	КонецЕсли;
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/json");
	
	ИдСессии = ДанныеЗапроса.headers["mcp-session-id"];
	Если ИдСессии <> Неопределено
		И ВалидныйФорматСессии(ИдСессии)
		И МСПСессии.Получить(ИдСессии) <> Неопределено Тогда
		Заголовки.Вставить("mcp-session-id", ИдСессии);
	КонецЕсли;
	
	Возврат ЗаписатьJSON2(Заголовки);
	
КонецФункции

&НаКлиенте
Процедура ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, КодОтветаHTTP, Сообщение, КодОшибки = -32600)

	КомпонентаSendResponse(ДанныеЗапроса.id, КодОтветаHTTP,
		ЗаголовкиJSONMCP(ДанныеЗапроса),
		СформироватьТелоJSONRPCОшибка(Сообщение, КодОшибки));

КонецПроцедуры

&НаКлиенте
Функция ПроверитьAcceptPOST(ДанныеЗапроса)
	Акцепт = ДанныеЗапроса.headers["accept"];
	Если Акцепт = Неопределено Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 406,
			"Not Acceptable: Client must accept both application/json and text/event-stream",
			-32600);
		Возврат Ложь;
	КонецЕсли;
	АкцептНР = НРег(СокрЛП(Акцепт));
	Если Найти(АкцептНР, "application/json") = 0
		ИЛИ Найти(АкцептНР, "text/event-stream") = 0 Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 406,
			"Not Acceptable: Client must accept both application/json and text/event-stream",
			-32600);
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ПроверитьAcceptSSE(ДанныеЗапроса)
	Акцепт = ДанныеЗапроса.headers["accept"];
	Если Акцепт = Неопределено
		ИЛИ Найти(НРег(СокрЛП(Акцепт)), "text/event-stream") = 0 Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 406,
			"Not Acceptable: Client must accept text/event-stream",
			-32600);
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ПроверитьПротоколВерсию(ДанныеЗапроса)
	Версия = ДанныеЗапроса.headers["mcp-protocol-version"];
	Если Версия = Неопределено Тогда Возврат Истина КонецЕсли;
	Если Версия <> "2024-11-05" И Версия <> "2025-03-26"
		И Версия <> "2025-06-18" И Версия <> "2025-11-25" Тогда
		ПоддерживаемыеВерсии = "2024-11-05, 2025-03-26, 2025-06-18, 2025-11-25";
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 400,
			"Bad Request: Unsupported protocol version: " + Версия + ". Supported versions: " + ПоддерживаемыеВерсии,
			-32600);
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

&НаКлиенте
Функция ПроверитьСессию(ДанныеЗапроса)
	ИдСессии = ДанныеЗапроса.headers["mcp-session-id"];
	Если ИдСессии = Неопределено Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 400,
			"Bad Request: Missing session ID",
			-32600);
		Возврат Ложь;
	КонецЕсли;
	Если НЕ ВалидныйФорматСессии(ИдСессии) ИЛИ МСПСессии.Получить(ИдСессии) = Неопределено Тогда
		ОтправитьMCPОшибкаJSONRPC(ДанныеЗапроса, 404,
			"Not Found: Invalid or expired session ID",
			-32600);
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ВалидныйФорматСессии(ИдСессии)
	// Только ASCII 0x21-0x7E (видимые, без пробелов)
	Если ПустаяСтрока(ИдСессии) Тогда Возврат Ложь КонецЕсли;
	Для Позиция = 1 По СтрДлина(ИдСессии) Цикл
		Код = КодСимвола(ИдСессии, Позиция);
		Если Код < 33 ИЛИ Код > 126 Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	Возврат Истина;
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// УПРАВЛЕНИЕ СЕССИЯМИ MCP
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция СоздатьМСПСессию()
	// Лимит: 5 сессий, удаляем самую старую
	Если МСПСессии.Количество() >= 5 Тогда
		УдалитьСамуюСтаруюСессию();
	КонецЕсли;

	ИдСессии = Строка(Новый УникальныйИдентификатор);
	МСПСессии.Вставить(ИдСессии, ТекущаяДата());

	Возврат ИдСессии;
КонецФункции

&НаКлиенте
Процедура УдалитьСамуюСтаруюСессию()
	СтарыйКлюч = Неопределено;
	СтараяДата = Неопределено;
	Для Каждого КлючЗначение Из МСПСессии Цикл
		ДатаСоздания = КлючЗначение.Значение;
		Если СтараяДата = Неопределено ИЛИ ДатаСоздания < СтараяДата Тогда
			СтараяДата = ДатаСоздания;
			СтарыйКлюч = КлючЗначение.Ключ;
		КонецЕсли;
	КонецЦикла;

	Если СтарыйКлюч <> Неопределено Тогда
		УдалитьМСПСессию(СтарыйКлюч);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура УдалитьМСПСессию(ИдСессии)
	// Удалить SSE поток синхронно ДО CloseSSEStream
	ИдПотока = SSEПотоки.Получить(ИдСессии);
	SSEПотоки.Удалить(ИдСессии);
	Если ИдПотока <> Неопределено Тогда
		КомпонентаCloseSSEStream(ИдПотока);
	КонецЕсли;
	МСПСессии.Удалить(ИдСессии);
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьВсеМСПСессии()
	SSEПотоки = Новый Соответствие;
	МСПСессии = Новый Соответствие;
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьDeleteСессии(ДанныеЗапроса)
	Если НЕ ПроверитьПротоколВерсию(ДанныеЗапроса) Тогда Возврат КонецЕсли;
	Если НЕ ПроверитьСессию(ДанныеЗапроса) Тогда Возврат КонецЕсли;

	ИдСессии = ДанныеЗапроса.headers["mcp-session-id"];
	УдалитьМСПСессию(ИдСессии);

	// В proxy streamable_http DELETE /mcp возвращает 200 OK с пустым телом.
	КомпонентаSendResponse(ДанныеЗапроса.id, 200, ЗаголовкиJSON(), "");
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// MCP INITIALIZE / TOOLS/LIST
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция ОбработатьInitialize(ТелоJSON, ДанныеЗапроса)

	ИдСессии = СоздатьМСПСессию();

	// protocolVersion: как в MCP SDK — возвращаем запрошенную версию, если поддерживается,
	// иначе отдаём latest.
	ЗапрошеннаяВерсия = Неопределено;
	ПарамИнициализации = Неопределено;
	Если ТелоJSON <> Неопределено
		И ТипЗнч(ТелоJSON) = Тип("Структура")
		И ТелоJSON.Свойство("params", ПарамИнициализации)
		И ТипЗнч(ПарамИнициализации) = Тип("Структура") Тогда
		ПарамИнициализации.Свойство("protocolVersion", ЗапрошеннаяВерсия);
	КонецЕсли;
	
	ЗапрошеннаяВерсия = СокрЛП(Строка(ЗапрошеннаяВерсия));
	Если ПустаяСтрока(ЗапрошеннаяВерсия) Тогда
		ЗапрошеннаяВерсия = Неопределено;
	КонецЕсли;
	
	ВерсияОтвета = "2025-11-25";
	Если ЗапрошеннаяВерсия = "2024-11-05"
		Или ЗапрошеннаяВерсия = "2025-03-26"
		Или ЗапрошеннаяВерсия = "2025-06-18"
		Или ЗапрошеннаяВерсия = "2025-11-25" Тогда
		ВерсияОтвета = ЗапрошеннаяВерсия;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("protocolVersion", ВерсияОтвета);
	Результат.Вставить("capabilities", Новый Структура("tools",
		Новый Структура("listChanged", Ложь)));
	Результат.Вставить("serverInfo", Новый Структура("name, version",
		"1C MCP Toolkit (Native)", "1.0.0"));

	Возврат Новый Структура("Результат, ИдСессии", Результат, ИдСессии);

КонецФункции

&НаКлиенте
Функция ПолучитьСхемыИнструментов()

	Если КэшСхемИнструментов <> Неопределено Тогда
		Возврат КэшСхемИнструментов;
	КонецЕсли;

	Инструменты = Новый Массив;

	// execute_query
	Если ИнструментВключен_ExecuteQuery Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("execute_query",
			ОписаниеToolExecuteQuery(ИспользоватьТочнуюАнонимизацию),
			СхемаExecuteQuery()));
	КонецЕсли;

	// execute_code
	Если ИнструментВключен_ExecuteCode Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("execute_code",
			ОписаниеToolExecuteCode(ИспользоватьТочнуюАнонимизацию),
			СхемаExecuteCode()));
	КонецЕсли;

	// get_metadata
	Если ИнструментВключен_GetMetadata Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("get_metadata",
			ОписаниеToolGetMetadata(),
			СхемаGetMetadata()));
	КонецЕсли;

	// get_event_log
	Если ИнструментВключен_GetEventLog Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("get_event_log",
			ОписаниеToolGetEventLog(ИспользоватьТочнуюАнонимизацию),
			СхемаGetEventLog()));
	КонецЕсли;

	// get_object_by_link
	Если ИнструментВключен_GetObjectByLink Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("get_object_by_link",
			ОписаниеToolGetObjectByLink(ИспользоватьТочнуюАнонимизацию),
			СхемаGetObjectByLink()));
	КонецЕсли;

	// get_link_of_object
	Если ИнструментВключен_GetLinkOfObject Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("get_link_of_object",
			ОписаниеToolGetLinkOfObject(),
			СхемаGetLinkOfObject()));
	КонецЕсли;

	// find_references_to_object
	Если ИнструментВключен_FindReferences Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("find_references_to_object",
			ОписаниеToolFindReferencesToObject(ИспользоватьТочнуюАнонимизацию),
			СхемаFindReferencesToObject()));
	КонецЕсли;

	// get_access_rights
	Если ИнструментВключен_GetAccessRights Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("get_access_rights",
			ОписаниеToolGetAccessRights(ИспользоватьТочнуюАнонимизацию),
			СхемаGetAccessRights()));
	КонецЕсли;

	// get_bsl_syntax_help — только если компонента загружена и инструмент включён
	Если ИнструментВключен_GetBslSyntaxHelp И КомпонентаSyntaxHelp <> Неопределено Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента(
			"get_bsl_syntax_help",
			ОписаниеToolGetBslSyntaxHelp(),
			СхемаGetBslSyntaxHelp()));
	КонецЕсли;

	// submit_for_deanonymization — только при включённой анонимизации и включённом инструменте
	Если ИнструментВключен_SubmitDeanon И ИспользоватьТочнуюАнонимизацию Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("submit_for_deanonymization",
			ОписаниеToolSubmitForDeanonymization(),
			СхемаSubmitForDeanonymization()));
	КонецЕсли;

	// get_screenshot — только если компонента загружена и инструмент включён
	Если ИнструментВключен_GetScreenshot И КомпонентаScreenCapture <> Неопределено Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента(
			"get_screenshot",
			ОписаниеToolGetScreenshot(),
			СхемаGetScreenshot()));
	КонецЕсли;

	// restart_1c_session — только если инструмент включён
	Если ИнструментВключен_Restart1cSession Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("restart_1c_session",
			ОписаниеToolRestart1cSession(),
			СхемаRestart1cSession()));
	КонецЕсли;

	// close_1c_session — только если инструмент включён
	Если ИнструментВключен_Close1cSession Тогда
		Инструменты.Добавить(СоздатьСхемуИнструмента("close_1c_session",
			ОписаниеToolClose1cSession(),
			СхемаClose1cSession()));
	КонецЕсли;

	КэшСхемИнструментов = Новый Структура("tools", Инструменты);
	Возврат КэшСхемИнструментов;

КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// ПЕРЕЗАПУСК СЕССИИ (restart_1c_session)
// ─────────────────────────────────────────────────────────────────────────────────

// Возвращает путь к temp-файлу или "" если маппинги пусты/не нужны
&НаКлиенте
Функция СохранитьМаппингиАнонимизацииВоВременныйФайл(ПутьФайла = "")
	// В proxy mode маппинги живут в Python — передавать нечего
	Если РежимПодключения = 0 Тогда
		Возврат "";
	КонецЕсли;

	Если МаппингАнонимизации = Неопределено Или МаппингАнонимизации.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли;

	МассивМ = Новый Массив;
	Для Каждого П Из МаппингАнонимизации Цикл
		МассивМ.Добавить(Новый Структура("к,т", П.Ключ, П.Значение));
	КонецЦикла;

	МассивС = Новый Массив;
	Если СчетчикиТокенов <> Неопределено Тогда
		Для Каждого П Из СчетчикиТокенов Цикл
			МассивС.Добавить(Новый Структура("к,с", П.Ключ, П.Значение));
		КонецЦикла;
	КонецЕсли;

	Данные = Новый Структура("м,с", МассивМ, МассивС);

	ВременныйФайл = ?(ЗначениеЗаполнено(ПутьФайла), ПутьФайла, ПолучитьИмяВременногоФайла("json"));
	ЗТМапп = Новый ЗаписьТекста(ВременныйФайл, КодировкаТекста.UTF8);
	Попытка
		ЗТМапп.Записать(ЗаписатьJSON2(Данные));
	Исключение
		Попытка ЗТМапп.Закрыть(); Исключение КонецПопытки;
		ВызватьИсключение;
	КонецПопытки;
	ЗТМапп.Закрыть();

	Возврат ВременныйФайл;
КонецФункции

&НаКлиенте
Процедура ВосстановитьМаппингиАнонимизацииИзФайла(Путь)
	Если Путь = "" Тогда
		Возврат;
	КонецЕсли;

	ЧТМапп = Новый ЧтениеТекста(Путь, КодировкаТекста.UTF8);
	Попытка
		ТекстМапп = ЧТМапп.Прочитать();
	Исключение
		Попытка ЧТМапп.Закрыть(); Исключение КонецПопытки;
		ВызватьИсключение;
	КонецПопытки;
	ЧТМапп.Закрыть();
	Данные = ПрочитатьJSON2(ТекстМапп);
	УдалитьФайлы(Путь);

	МаппингАнонимизации   = Новый Соответствие;
	МаппингДеанонимизации = Новый Соответствие;
	Для Каждого З Из Данные.м Цикл
		МаппингАнонимизации[З.к]         = З.т;
		МаппингДеанонимизации[ВРег(З.т)] = З.к;
	КонецЦикла;

	СчетчикиТокенов = Новый Соответствие;
	Для Каждого З Из Данные.с Цикл
		СчетчикиТокенов[З.к] = З.с;
	КонецЦикла;
КонецПроцедуры

// Восстанавливает МСПСессии из плоского массива строк (session ID).
// Восстановленные сессии получают текущее время — корректно, они все "только что" пришли.
&НаКлиенте
Процедура ВосстановитьМСПСессии(МассивИдС)
	Если МассивИдС = Неопределено Или МассивИдС.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	МСПСессии = Новый Соответствие;
	Для Каждого ИдС Из МассивИдС Цикл
		Если ТипЗнч(ИдС) <> Тип("Строка") Или НЕ ВалидныйФорматСессии(ИдС) Тогда
			ВызватьИсключение "Sessions restore error: invalid session ID type or format: " + Строка(ИдС);
		КонецЕсли;
		МСПСессии[ИдС] = ТекущаяДата();
	КонецЦикла;
	ДобавитьВЛог("Восстановлено сессий: " + МассивИдС.Количество() + " → " + СоединитьМассив(МассивИдС, ", "));
КонецПроцедуры

// Побитовый XOR двух байт (0-255) через арифметику
&НаКлиенте
Функция XORБайт(А, Б)
	Рез = 0; Ст = 1;
	Для Индекс = 0 По 7 Цикл
		Рез = Рез + (Цел(А / Ст) % 2 + Цел(Б / Ст) % 2) % 2 * Ст;
		Ст = Ст * 2;
	КонецЦикла;
	Возврат Рез;
КонецФункции

// Шифрует пароль XOR с ключом; результат — Base64. Никаких temp-файлов.
&НаКлиенте
Функция ЗашифроватьПарольПерезапуска(Пароль, КлючШифрования = "")
	Если НЕ ЗначениеЗаполнено(Пароль) Тогда Возврат ""; КонецЕсли;

	ДД_Пароль = ПолучитьДвоичныеДанныеИзСтроки(Пароль, КодировкаТекста.UTF8);
	ДД_Ключ   = ПолучитьДвоичныеДанныеИзСтроки(?(ЗначениеЗаполнено(КлючШифрования), КлючШифрования, ПолучитьКлючШифрования()), КодировкаТекста.UTF8);

	КлючБайты = Новый Массив;
	ЧтК = Новый ЧтениеДанных(ДД_Ключ.ОткрытьПотокДляЧтения());
	Б = ЧтК.ПрочитатьБайт();
	Пока Б <> Неопределено Цикл
		КлючБайты.Добавить(Б);
		Б = ЧтК.ПрочитатьБайт();
	КонецЦикла;
	ЧтК.Закрыть();
	Если КлючБайты.Количество() = 0 Тогда ВызватьИсключение "XOR key is empty"; КонецЕсли;

	ПотокРез = Новый ПотокВПамяти();
	ЗпР = Новый ЗаписьДанных(ПотокРез);
	ЧтП = Новый ЧтениеДанных(ДД_Пароль.ОткрытьПотокДляЧтения());
	КлючИндекс = 0;
	Б = ЧтП.ПрочитатьБайт();
	Пока Б <> Неопределено Цикл
		ЗпР.ЗаписатьБайт(XORБайт(Б, КлючБайты[КлючИндекс % КлючБайты.Количество()]));
		КлючИндекс = КлючИндекс + 1;
		Б = ЧтП.ПрочитатьБайт();
	КонецЦикла;
	ЧтП.Закрыть();
	ЗпР.Закрыть();
	Возврат Base64Строка(ПотокРез.ЗакрытьИПолучитьДвоичныеДанные());
КонецФункции

// Расшифровывает — XOR симметричен, логика идентична шифрованию.
&НаКлиенте
Функция РасшифроватьПарольПерезапуска(ЗашифрованнаяСтрока, КлючШифрования = "")
	Если НЕ ЗначениеЗаполнено(ЗашифрованнаяСтрока) Тогда Возврат ""; КонецЕсли;

	ДД_Шифр = Base64Значение(ЗашифрованнаяСтрока);
	ДД_Ключ = ПолучитьДвоичныеДанныеИзСтроки(?(ЗначениеЗаполнено(КлючШифрования), КлючШифрования, ПолучитьКлючШифрования()), КодировкаТекста.UTF8);

	КлючБайты = Новый Массив;
	ЧтК = Новый ЧтениеДанных(ДД_Ключ.ОткрытьПотокДляЧтения());
	Б = ЧтК.ПрочитатьБайт();
	Пока Б <> Неопределено Цикл
		КлючБайты.Добавить(Б);
		Б = ЧтК.ПрочитатьБайт();
	КонецЦикла;
	ЧтК.Закрыть();
	Если КлючБайты.Количество() = 0 Тогда ВызватьИсключение "XOR key is empty"; КонецЕсли;

	ПотокРез = Новый ПотокВПамяти();
	ЗпР = Новый ЗаписьДанных(ПотокРез);
	ЧтШ = Новый ЧтениеДанных(ДД_Шифр.ОткрытьПотокДляЧтения());
	КлючИндекс = 0;
	Б = ЧтШ.ПрочитатьБайт();
	Пока Б <> Неопределено Цикл
		ЗпР.ЗаписатьБайт(XORБайт(Б, КлючБайты[КлючИндекс % КлючБайты.Количество()]));
		КлючИндекс = КлючИндекс + 1;
		Б = ЧтШ.ПрочитатьБайт();
	КонецЦикла;
	ЧтШ.Закрыть();
	ЗпР.Закрыть();
	Возврат ПолучитьСтрокуИзДвоичныхДанных(ПотокРез.ЗакрытьИПолучитьДвоичныеДанные(), КодировкаТекста.UTF8);
КонецФункции

&НаСервере
Функция ПолучитьПользовательскийКлючИзХранилищаНаСервере()
	Попытка
		ЗначениеИзХранилища = ХранилищеОбщихНастроек.Загрузить("MCPToolkit", "ПользовательскийКлючШифрованияПерезапуска");
		Если ТипЗнч(ЗначениеИзХранилища) = Тип("Строка") Тогда
			Возврат Лев(СокрЛП(ЗначениеИзХранилища), 20);
		КонецЕсли;
	Исключение
	КонецПопытки;
	Возврат "";
КонецФункции

&НаКлиенте
Функция ПолучитьКлючШифрования()
	Возврат "MCPToolkit-RestartKey-v1-x7Kq2mNp" + ПолучитьПользовательскийКлючИзХранилищаНаСервере();
КонецФункции

&НаСервере
Функция СгенерироватьПользовательскийКлюч()
	Алфавит = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
	ГСЧ = Новый ГенераторСлучайныхЧисел();
	Результат = "";
	Для Счетчик = 1 По 20 Цикл
		Результат = Результат + Сред(Алфавит, ГСЧ.СлучайноеЧисло(1, 62), 1);
	КонецЦикла;
	Возврат Результат;
КонецФункции

// Вспомогательная: читает содержимое файла-сигнала
&НаКлиенте
Функция ПрочитатьСигналПерезапуска(Путь)
	Попытка
		ЧТ = Новый ЧтениеТекста(Путь, КодировкаТекста.UTF8);
		Содержимое = СокрЛП(ЧТ.Прочитать());
		ЧТ.Закрыть();
		Возврат Содержимое;
	Исключение
		Возврат "";
	КонецПопытки;
КонецФункции

&НаСервере
Функция ПолучитьПутьКОбработкеНаСервере()
	Возврат РеквизитФормыВЗначение("Объект").ИспользуемоеИмяФайла;
КонецФункции

&НаСервере
Функция ПолучитьИмяТекущегоПользователяНаСервере()
	Возврат ПользователиИнформационнойБазы.ТекущийПользователь().Имя;
КонецФункции

&НаСервере
Функция ПроверитьНастройкиПерезапускаНаСервере(РежимАутентификации, ПарольЗаполнен)
	ТекПользователь = ПользователиИнформационнойБазы.ТекущийПользователь();
	ДоступнаWA     = ТекПользователь.АутентификацияОС;
	ДоступенПароль = ТекПользователь.АутентификацияСтандартная;

	Если НЕ ДоступнаWA И НЕ ДоступенПароль Тогда
		Возврат "Перезапуск невозможен: у пользователя не настроен ни один из поддерживаемых методов аутентификации";
	КонецЕсли;

	Если РежимАутентификации = 0 И НЕ ДоступнаWA Тогда
		Возврат "Выбрана Windows-аутентификация, но она не доступна для текущего пользователя или ОС";
	КонецЕсли;

	Если РежимАутентификации = 1 И НЕ ДоступенПароль Тогда
		Возврат "Выбрана аутентификация по паролю, но для пользователя она не настроена";
	КонецЕсли;

	Если РежимАутентификации = 1 И ТекПользователь.ПарольУстановлен И НЕ ПарольЗаполнен Тогда
		Возврат "Выбрана аутентификация по паролю, но пароль не задан в настройках перезапуска";
	КонецЕсли;

	Возврат "";
КонецФункции

&НаСервере
Функция ПолучитьДоступностьWAПользователяНаСервере()
	Возврат ПользователиИнформационнойБазы.ТекущийПользователь().АутентификацияОС;
КонецФункции

&НаСервере
Функция ПолучитьДоступностьПарольПользователяНаСервере()
	Возврат ПользователиИнформационнойБазы.ТекущийПользователь().АутентификацияСтандартная;
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьРежимАутентификацииПерезапускаНаСервере(Режим)
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "РежимАутентификацииПерезапуска", Режим);
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьПортВстроенногоСервераНаСервере(Порт)
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ПортВстроенногоСервера", Порт);
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура НастроитьДоступностьАутентификацииПерезапуска()
	СисИнфо    = Новый СистемнаяИнформация();
	ЭтоWindows = СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86
		Или СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86_64;

	ДоступнаWA     = ЭтоWindows И ПолучитьДоступностьWAПользователяНаСервере();
	ДоступенПароль = ПолучитьДоступностьПарольПользователяНаСервере();

	Попытка
		Элементы.РежимАутентификацииПерезапуска.СписокВыбора[0].Доступность = ДоступнаWA;
		Элементы.РежимАутентификацииПерезапуска.СписокВыбора[1].Доступность = ДоступенПароль;
	Исключение
	КонецПопытки;

	Если РежимАутентификацииПерезапуска = 0 И НЕ ДоступнаWA Тогда
		РежимАутентификацииПерезапуска = ?(ДоступенПароль, 1, 0);
	ИначеЕсли РежимАутентификацииПерезапуска = 1 И НЕ ДоступенПароль Тогда
		РежимАутентификацииПерезапуска = ?(ДоступнаWA, 0, 1);
	КонецЕсли;

	ОбновитьВидимостьПароляПерезапуска();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьВидимостьПароляПерезапуска()
	Элементы.ПарольПерезапуска.Видимость          = (РежимАутентификацииПерезапуска = 1);
	Элементы.ГруппаДопКлючаПерезапуска.Видимость = (РежимАутентификацииПерезапуска = 1);
КонецПроцедуры

&НаКлиенте
Процедура РежимАутентификацииПерезапускаПриИзменении(Элемент)
	ОбновитьВидимостьПароляПерезапуска();
	СохранитьРежимАутентификацииПерезапускаНаСервере(РежимАутентификацииПерезапуска);
КонецПроцедуры

&НаКлиенте
Процедура СгенерироватьПользовательскийКлючНажатие(Команда)
	НовыйКлюч = СгенерироватьИСохранитьПользовательскийКлючНаСервере();
	ПользовательскийКлючШифрования = НовыйКлюч;
КонецПроцедуры

&НаСервере
Функция СгенерироватьИСохранитьПользовательскийКлючНаСервере()
	НовыйКлюч = СгенерироватьПользовательскийКлюч();
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ПользовательскийКлючШифрованияПерезапуска", НовыйКлюч);
	Исключение
	КонецПопытки;
	Возврат НовыйКлюч;
КонецФункции

&НаКлиенте
Процедура ПользовательскийКлючШифрованияПриИзменении(Элемент)
	ПользовательскийКлючШифрованияСохранитьНаСервере(ПользовательскийКлючШифрования);
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ПользовательскийКлючШифрованияСохранитьНаСервере(Значение)
	Попытка
		ХранилищеОбщихНастроек.Сохранить("MCPToolkit", "ПользовательскийКлючШифрованияПерезапуска", Значение);
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПарольПерезапускаОткрытиеЗначения(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Элементы.ПарольПерезапуска.РежимПароля = НЕ Элементы.ПарольПерезапуска.РежимПароля;
КонецПроцедуры

&НаКлиенте
Функция ПерезапускАктивенИлиЗавершается()
	Возврат ПерезапускВОжидании Или ЗавершениеПослеПерезапускаЗапущено;
КонецФункции

// Сбрасывает только response-контекст (ids, ДанныеЗапроса, флаг REST).
// НЕ трогает ПерезапускВОжидании — guard должен оставаться активным до завершения temp cleanup.
// НЕ трогает temp-file paths — их обнуляет ПроверитьСигналПерезапуска.
// Только для error/timeout/failed paths, где старая сессия продолжает работать.
&НаКлиенте
Процедура СброситьОтветныйКонтекстПерезапуска()
	ЭтоRESTЗапросПерезапуска            = Ложь;
	ИдКомандыПерезапуска                = "";
	ДанныеЗапросаПерезапуска            = Неопределено;
	ИдЗапросаПерезапуска                = "";
	ИдПотокаПерезапуска                = "";
	ИдRESTЗапросаПерезапуска           = "";
	ТаймаутПерезапуска                  = Неопределено;
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьПерезапускСессии(КомандаИД, ДанныеЗапросаEmb = Неопределено, ИдЗапросаEmb = "", ИдПотокаEmb = "", ВызовИзREST = Ложь)

	// Защита от повторного вызова или активного close
	Если ПерезапускАктивенИлиЗавершается() Или ЗакрытиеСессииВПроцессе Тогда
		Если РежимПодключения = 0 Тогда
			ОтправитьРезультатНаСервер(АдресСервера, КомандаИД,
				Новый Структура("success,error", Ложь, "Restart or close already in progress"));
		Иначе
			Сод = Новый Массив;
			Сод.Добавить(Новый Структура("type,text", "text", "Error: Restart or close already in progress"));
			ОтвJSON = СформироватьJSONRPCОтвет(ИдЗапросаEmb,
				Новый Структура("content,isError", Сод, Истина));
			Если ПустаяСтрока(ИдПотокаEmb) Тогда
				ОтправитьSSEОтвет(ДанныеЗапросаEmb, ОтвJSON);
			Иначе
				КомпонентаSendSSEEvent(ИдПотокаEmb, ОтвJSON, "{}", "message");
			КонецЕсли;
		КонецЕсли;
		Возврат;
	КонецЕсли;

	// Сохраняем контекст — режим вызова АТОМАРНО с остальным контекстом,
	// до любых операций которые могут бросить исключение
	ЭтоRESTЗапросПерезапуска    = ВызовИзREST;
	ИдКомандыПерезапуска        = КомандаИД;
	ДанныеЗапросаПерезапуска    = ДанныеЗапросаEmb;
	ИдЗапросаПерезапуска        = ИдЗапросаEmb;
	ИдПотокаПерезапуска         = ИдПотокаEmb;
	// Кэшируем REST request id для безопасного повторного доступа.
	// Попытка — ДанныеЗапросаEmb может не иметь поля id (embedded SSE путь).
	ИдRESTЗапросаПерезапуска = "";
	Если ВызовИзREST Тогда
		Попытка
			Если ДанныеЗапросаEmb <> Неопределено Тогда
				ИдRESTЗапросаПерезапуска = Строка(ДанныеЗапросаEmb.id);
			КонецЕсли;
		Исключение
			ИдRESTЗапросаПерезапуска = "";
		КонецПопытки;
	КонецЕсли;

	// Проверка ОС для Windows-auth
	СисИнфо    = Новый СистемнаяИнформация();
	ЭтоWindows = СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86
		Или СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86_64;
	Если РежимАутентификацииПерезапуска = 0 И НЕ ЭтоWindows Тогда
		ОтправитьОтветПерезапуска(Ложь, "Windows authentication is not available on the current OS");
		Возврат;
	КонецЕсли;

	// Серверная валидация
	ОшибкаВалидации = ПроверитьНастройкиПерезапускаНаСервере(
		РежимАутентификацииПерезапуска,
		ЗначениеЗаполнено(ПарольПерезапуска));
	Если ОшибкаВалидации <> "" Тогда
		ОтправитьОтветПерезапуска(Ложь, ОшибкаВалидации);
		Возврат;
	КонецЕсли;

	// Проверка пути к обработке
	ПутьОбработки = ПолучитьПутьКОбработкеНаСервере();
	Если НЕ ЗначениеЗаполнено(ПутьОбработки) Тогда
		ОтправитьОтветПерезапуска(Ложь, "Cannot restart: data processor path is not available (not running from file?)");
		Возврат;
	КонецЕсли;
	ФайлОбработкиПроверка = Новый Файл(ПутьОбработки);
	Если НЕ ФайлОбработкиПроверка.Существует() Тогда
		ОтправитьОтветПерезапуска(Ложь, "Cannot restart: data processor file not found: " + ПутьОбработки);
		Возврат;
	КонецЕсли;

	// Фаза 1: подготовка + запуск
	ПутьМаппингов  = "";
	ПутьНонса      = "";
	ФайлПараметров = "";
	ПутьСигнала    = "";
	ПутьClaimed    = "";
	Попытка

		ПутьМаппингов  = СохранитьМаппингиАнонимизацииВоВременныйФайл();
		ПутьСигнала    = ПолучитьИмяВременногоФайла("tmp");
		ПутьНонса      = ПолучитьИмяВременногоФайла("nonce");
		ФайлПараметров = ПолучитьИмяВременногоФайла("json");
		ПутьClaimed    = ПолучитьИмяВременногоФайла("claimed");

		ЗТ = Новый ЗаписьТекста(ПутьНонса, КодировкаТекста.UTF8);
		ЗТ.ЗаписатьСтроку("valid");
		ЗТ.Закрыть();

		МСПМассив = Новый Массив;
		Если РежимПодключения = 1 И МСПСессии <> Неопределено Тогда
			Для Каждого П Из МСПСессии Цикл
				МСПМассив.Добавить(П.Ключ);
			КонецЦикла;
		КонецЕсли;

		ЗашифрованныйПароль = ЗашифроватьПарольПерезапуска(
			?(РежимАутентификацииПерезапуска = 1, ПарольПерезапуска, ""));

		ПараметрыЗапуска = Новый Структура("signal_path,mappings_path,mcp_sessions,nonce_path,claimed_path,restart_password",
			ПутьСигнала, ПутьМаппингов, МСПМассив, ПутьНонса, ПутьClaimed, ЗашифрованныйПароль);
		ЗТПарам = Новый ЗаписьТекста(ФайлПараметров, КодировкаТекста.UTF8);
		Попытка
			ЗТПарам.Записать(ЗаписатьJSON2(ПараметрыЗапуска));
		Исключение
			Попытка ЗТПарам.Закрыть(); Исключение КонецПопытки;
			ВызватьИсключение;
		КонецПопытки;
		ЗТПарам.Закрыть();

		#Если ТолстыйКлиентОбычноеПриложение Или ТолстыйКлиентУправляемоеПриложение Тогда
			ИмяExe = "1cv8" + ?(ЭтоWindows, ".exe", "");
		#ИначеЕсли ТонкийКлиент Тогда
			ИмяExe = "1cv8c" + ?(ЭтоWindows, ".exe", "");
		#КонецЕсли

		ПутьExe  = КаталогПрограммы() + ИмяExe;
		СтрСоед  = СтрокаСоединенияИнформационнойБазы();
		ИмяПольз = ПолучитьИмяТекущегоПользователяНаСервере();

		СтрСоедЭкр = СтрЗаменить(СтрСоед, """", """""");

		МеткаВремени = Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
		КомандаЗапуска = """" + ПутьExe + """ ENTERPRISE"
			+ " /IBConnectionString """ + СтрСоедЭкр + """"
			+ " /Execute """            + ПутьОбработки + """"
			+ " /C ""autostart;"        + МеткаВремени + ";" + ФайлПараметров + """";

		Если РежимАутентификацииПерезапуска = 0 Тогда
			КомандаЗапуска = КомандаЗапуска + " /WA";
		Иначе
			ИмяЭкр = СтрЗаменить(ИмяПольз, """", """""");
			КомандаЗапуска = КомандаЗапуска + " /N """ + ИмяЭкр + """";
			Если ЗначениеЗаполнено(ПарольПерезапуска) Тогда
				ПарольЭкр = СтрЗаменить(ПарольПерезапуска, """", """""");
				КомандаЗапуска = КомандаЗапуска + " /P """ + ПарольЭкр + """";
			КонецЕсли;
		КонецЕсли;

		ЗапуститьПриложение(КомандаЗапуска);

	Исключение
		Попытка УдалитьФайлы(ФайлПараметров); Исключение КонецПопытки;
		Попытка УдалитьФайлы(ПутьМаппингов);  Исключение КонецПопытки;
		Попытка УдалитьФайлы(ПутьНонса);      Исключение КонецПопытки;
		Попытка УдалитьФайлы(ПутьClaimed);    Исключение КонецПопытки;
		ОтправитьОтветПерезапуска(Ложь, "Failed to launch new session: " + ОписаниеОшибки());
		Возврат;
	КонецПопытки;

	// Фаза 2: пост-запуск
	ПутьФайлаСигналаПерезапуска    = ПутьСигнала;
	ПутьФайлаПараметровПерезапуска = ФайлПараметров;
	ПутьНонсаПерезапуска           = ПутьНонса;
	ПутьФайлаМаппинговПерезапуска  = ПутьМаппингов;
	ПутьКлеймаПерезапуска          = ПутьClaimed;
	ТаймаутПерезапуска             = ТекущаяДата() + 120;
	ПерезапускВОжидании            = Истина;

	Попытка ОтключитьОбработчикОжидания("ЦиклОпроса"); Исключение КонецПопытки;
	ПодключитьОбработчикОжидания("ПроверитьСигналПерезапуска", 3);

КонецПроцедуры

&НаКлиенте
Функция ОтправитьОтветПерезапуска(Успех, Сообщение)
	Если РежимПодключения = 0 Тогда
		Результат = Новый Структура("success", Успех);
		Если Успех Тогда
			Результат.Вставить("data", Сообщение);
		Иначе
			Результат.Вставить("error", Сообщение);
		КонецЕсли;
		Попытка
			РезОтправки = ОтправитьРезультатНаСервер(АдресСервера, ИдКомандыПерезапуска, Результат);
			Если НЕ РезОтправки.Успех Тогда
				РезОтправки = ОтправитьРезультатНаСервер(АдресСервера, ИдКомандыПерезапуска, Результат);
			КонецЕсли;
			Возврат РезОтправки.Успех;
		Исключение
			Возврат Ложь;
		КонецПопытки;
	Иначе
		Если ЭтоRESTЗапросПерезапуска Тогда
			// Success для REST не должен сюда попадать -- callback shutdown через
			// ОтправитьОтветПерезапускаИЗавершитьEmbedded. Если попал -- ошибка контракта,
			// делегируем в callback-path чтобы не оставлять две сессии.
			Если Успех Тогда
				ОтправитьОтветПерезапускаИЗавершитьEmbedded(Сообщение);
				Возврат Истина;
			КонецЕсли;
			// Error: fire-and-forget, сессия продолжает работать.
			// Попытка покрывает и ЗаписатьJSON2 и SendResponse --
			// в любом случае СброситьОтветныйКонтекстПерезапуска выполнится.
			// Используем ИдRESTЗапросаПерезапуска (кэш) вместо ДанныеЗапроса.id.
			Попытка
				Если ИдRESTЗапросаПерезапуска = "" Тогда
					ДобавитьВЛог("restart REST: error response impossible: request id lost");
				Иначе
					РезТело = ЗаписатьJSON2(Новый Структура("success,error", Ложь, Сообщение));
					КомпонентаSendResponse(ИдRESTЗапросаПерезапуска, 500, ЗаголовкиJSON(), РезТело);
				КонецЕсли;
			Исключение
				ДобавитьВЛог("restart REST: error response exception: " + ОписаниеОшибки());
			КонецПопытки;
			СброситьОтветныйКонтекстПерезапуска();
			Возврат Истина;
		КонецЕсли;
		// Embedded SSE (существующий код без изменений)
		ТекстОтвета = ?(Успех, Сообщение, "Error: " + Сообщение);
		Содержимое  = Новый Массив;
		Содержимое.Добавить(Новый Структура("type,text", "text", ТекстОтвета));
		РезультатMCP = Новый Структура("content,isError", Содержимое, НЕ Успех);
		ОтветJSON = СформироватьJSONRPCОтвет(ИдЗапросаПерезапуска, РезультатMCP);
		Если ПустаяСтрока(ИдПотокаПерезапуска) Тогда
			ОтправитьSSEОтвет(ДанныеЗапросаПерезапуска, ОтветJSON);
		Иначе
			КомпонентаSendSSEEvent(ИдПотокаПерезапуска, ОтветJSON, "{}", "message");
		КонецЕсли;
		Возврат Истина;
	КонецЕсли;
КонецФункции

&НаКлиенте
Процедура ПроверитьСигналПерезапуска()
	Если НЕ ПерезапускВОжидании Тогда
		ОтключитьОбработчикОжидания("ПроверитьСигналПерезапуска");
		Возврат;
	КонецЕсли;

	Файл = Новый Файл(ПутьФайлаСигналаПерезапуска);
	СигналЗначение = ПрочитатьСигналПерезапуска(ПутьФайлаСигналаПерезапуска);

	Если Файл.Существует() И (СигналЗначение = "ready" Или Лев(СигналЗначение, 7) = "failed:") Тогда
		ОтключитьОбработчикОжидания("ПроверитьСигналПерезапуска");
		УдалитьФайлы(ПутьФайлаСигналаПерезапуска);

		Если СигналЗначение <> "ready" Тогда
			ОтправитьОтветПерезапуска(Ложь, "Restart failed in new session: " + Сред(СигналЗначение, 8));
			Попытка УдалитьФайлы(ПутьКлеймаПерезапуска);           Исключение КонецПопытки;
			ПутьКлеймаПерезапуска = "";
			Попытка УдалитьФайлы(ПутьФайлаМаппинговПерезапуска);   Исключение КонецПопытки;
			ПутьФайлаМаппинговПерезапуска = "";
			Попытка УдалитьФайлы(ПутьНонсаПерезапуска);             Исключение КонецПопытки;
			ПутьНонсаПерезапуска = "";
			Попытка УдалитьФайлы(ПутьФайлаПараметровПерезапуска);   Исключение КонецПопытки;
			ПутьФайлаПараметровПерезапуска = "";
			ПутьФайлаСигналаПерезапуска = "";
			ПерезапускВОжидании = Ложь;
			Если РежимПодключения = 0 Тогда
				ПодключитьОбработчикОжидания("ЦиклОпроса", ИнтервалОпроса, Ложь);
			КонецЕсли;
			Возврат;
		КонецЕсли;

		Если РежимПодключения = 0 Тогда
			ПерезапускВОжидании = Ложь;
			ОтправитьОтветПерезапуска(Истина,
			"Session restarted successfully."
			+ " Note: the first MCP request to the new session may fail - retry once if it does.");
			Попытка УдалитьФайлы(ПутьКлеймаПерезапуска); Исключение КонецПопытки;
			ПутьКлеймаПерезапуска          = "";
			Попытка УдалитьФайлы(ПутьФайлаПараметровПерезапуска); Исключение КонецПопытки;
			ПутьФайлаПараметровПерезапуска = "";
			Попытка УдалитьФайлы(ПутьНонсаПерезапуска); Исключение КонецПопытки;
			ПутьНонсаПерезапуска           = "";
			Попытка УдалитьФайлы(ПутьФайлаМаппинговПерезапуска); Исключение КонецПопытки;
			ПутьФайлаМаппинговПерезапуска  = "";
			Попытка ОтключитьсяОтСервера(); Исключение КонецПопытки;
			ЗавершитьРаботуСистемы(Ложь);
		Иначе
			ОтправитьОтветПерезапускаИЗавершитьEmbedded(
			"Session restarted successfully."
			+ " Note: the first MCP request to the new session may fail - retry once if it does.");
		КонецЕсли;

	ИначеЕсли ТекущаяДата() > ТаймаутПерезапуска Тогда
		ОтключитьОбработчикОжидания("ПроверитьСигналПерезапуска");

		ОтправитьОтветПерезапуска(Ложь, "Restart timeout: new session did not start in 120 seconds");

		Попытка УдалитьФайлы(ПутьНонсаПерезапуска);           Исключение КонецПопытки;
		Попытка УдалитьФайлы(ПутьФайлаПараметровПерезапуска); Исключение КонецПопытки;
		Попытка УдалитьФайлы(ПутьФайлаСигналаПерезапуска);    Исключение КонецПопытки;
		Если ПрочитатьСигналПерезапуска(ПутьКлеймаПерезапуска) <> "1" Тогда
			Попытка УдалитьФайлы(ПутьФайлаМаппинговПерезапуска); Исключение КонецПопытки;
		КонецЕсли;
		Попытка УдалитьФайлы(ПутьКлеймаПерезапуска);          Исключение КонецПопытки;
		ПутьФайлаСигналаПерезапуска    = "";
		ПутьФайлаПараметровПерезапуска = "";
		ПутьНонсаПерезапуска           = "";
		ПутьФайлаМаппинговПерезапуска  = "";
		ПутьКлеймаПерезапуска          = "";
		ПерезапускВОжидании = Ложь;

		Если РежимПодключения = 0 Тогда
			ПодключитьОбработчикОжидания("ЦиклОпроса", ИнтервалОпроса, Ложь);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Функция ЭкранироватьPSОдинарнаяКавычка(Строка)
	Возврат СтрЗаменить(Строка, "'", "''");
КонецФункции

&НаКлиенте
Функция ЭкранироватьБашОдинарнаяКавычка(Строка)
	Возврат СтрЗаменить(Строка, "'", "'\''");
КонецФункции

&НаКлиенте
Функция СформироватьТекстЛаунчераХолодногоСтарта(ЭтоWindows, ПутьExe, СтрСоед, ПутьОбработки, ПутьПараметров, ИмяПольз, ЗашифрПароль, РежимАутентификации, ПутьСигнала)

	НуженПароль = (РежимАутентификации = 1) И ЗначениеЗаполнено(ЗашифрПароль);

	Если ЭтоWindows Тогда
		ЕPSExe      = ЭкранироватьPSОдинарнаяКавычка(ПутьExe);
		ЕPSПарамс   = ЭкранироватьPSОдинарнаяКавычка(ПутьПараметров);
		ЕPSСигнал   = ЭкранироватьPSОдинарнаяКавычка(ПутьСигнала);

		Если НЕ НуженПароль Тогда
			Текст =
"$paramsPath = '" + ЕPSПарамс + "'" + Символы.ПС
+ "$signalPath = '" + ЕPSСигнал + "'" + Символы.ПС
+ "try {" + Символы.ПС
+ "    $params = ConvertFrom-Json (Get-Content $paramsPath -Raw -Encoding UTF8 -ErrorAction Stop)" + Символы.ПС
+ "} catch {" + Символы.ПС
+ "    Write-Output ""Failed to read params file: $_""" + Символы.ПС
+ "    Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "    exit 1" + Символы.ПС
+ "}" + Символы.ПС
+ "function EscapeArg($s) { return '" + """" + "' + ($s -replace '" + """" + "', '" + """" + """" + "') + '" + """" + "' }" + Символы.ПС
+ "$connQ = EscapeArg $params.conn_str" + Символы.ПС
+ "$epfQ  = EscapeArg $params.epf_path" + Символы.ПС
+ "$ts = (Get-Date).ToString('yyyyMMddHHmmss')" + Символы.ПС
+ "$coldQ = EscapeArg ('coldstart;' + $ts + ';' + $paramsPath)" + Символы.ПС
+ "if ($params.auth_mode -eq 0) { $authSuffix = '/WA' }" + Символы.ПС
+ "else { $authSuffix = '/N ' + (EscapeArg $params.username) }" + Символы.ПС
+ "$argsStr = 'ENTERPRISE /IBConnectionString ' + $connQ + ' /Execute ' + $epfQ + ' /C ' + $coldQ + ' ' + $authSuffix" + Символы.ПС
+ "try {" + Символы.ПС
+ "    Start-Process -FilePath '" + ЕPSExe + "' -ArgumentList $argsStr -ErrorAction Stop" + Символы.ПС
+ "} catch {" + Символы.ПС
+ "    Write-Output ""Failed to launch 1C: $_""" + Символы.ПС
+ "    Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "    exit 1" + Символы.ПС
+ "}" + Символы.ПС
+ "$timeout = 120; $elapsed = 0" + Символы.ПС
+ "while ($elapsed -lt $timeout) {" + Символы.ПС
+ "    if (Test-Path $signalPath) {" + Символы.ПС
+ "        $content = (Get-Content $signalPath -Raw -Encoding UTF8).Trim()" + Символы.ПС
+ "        Remove-Item $signalPath -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "        Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "        if ($content -eq 'ready') { Write-Output 'Session started successfully. Note: the first MCP request to the new session may fail - retry once if it does.'; exit 0 }" + Символы.ПС
+ "        else { Write-Output ""Session start failed: $content""; exit 1 }" + Символы.ПС
+ "    }" + Символы.ПС
+ "    Start-Sleep -Seconds 3; $elapsed += 3" + Символы.ПС
+ "}" + Символы.ПС
+ "Write-Output ""Session start timeout after $timeout seconds""" + Символы.ПС
+ "Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "exit 1";
		Иначе
			Текст =
"$part2 = $args[0]" + Символы.ПС
+ "$paramsPath = '" + ЕPSПарамс + "'" + Символы.ПС
+ "$signalPath = '" + ЕPSСигнал + "'" + Символы.ПС
+ "try {" + Символы.ПС
+ "    $params = ConvertFrom-Json (Get-Content $paramsPath -Raw -Encoding UTF8 -ErrorAction Stop)" + Символы.ПС
+ "} catch {" + Символы.ПС
+ "    Write-Output ""Failed to read params file: $_""" + Символы.ПС
+ "    Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "    exit 1" + Символы.ПС
+ "}" + Символы.ПС
+ "try {" + Символы.ПС
+ "    $key = 'MCPColdLaunch-v1-n8Tz5rWj-' + $part2" + Символы.ПС
+ "    $bytes = [Convert]::FromBase64String($params.launch_password)" + Символы.ПС
+ "    $keyBytes = [System.Text.Encoding]::UTF8.GetBytes($key)" + Символы.ПС
+ "    $res = New-Object byte[] $bytes.Length" + Символы.ПС
+ "    for ($i = 0; $i -lt $bytes.Length; $i++) { $res[$i] = $bytes[$i] -bxor $keyBytes[$i % $keyBytes.Length] }" + Символы.ПС
+ "    $launchPassword = [System.Text.Encoding]::UTF8.GetString($res)" + Символы.ПС
+ "} catch {" + Символы.ПС
+ "    Write-Output ""Failed to decrypt password: $_""" + Символы.ПС
+ "    Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "    exit 1" + Символы.ПС
+ "}" + Символы.ПС
+ "function EscapeArg($s) { return '" + """" + "' + ($s -replace '" + """" + "', '" + """" + """" + "') + '" + """" + "' }" + Символы.ПС
+ "$connQ = EscapeArg $params.conn_str" + Символы.ПС
+ "$epfQ  = EscapeArg $params.epf_path" + Символы.ПС
+ "$ts = (Get-Date).ToString('yyyyMMddHHmmss')" + Символы.ПС
+ "$coldQ = EscapeArg ('coldstart;' + $ts + ';' + $paramsPath)" + Символы.ПС
+ "$userQ = EscapeArg $params.username" + Символы.ПС
+ "$pwdQ  = EscapeArg $launchPassword" + Символы.ПС
+ "$argsStr = 'ENTERPRISE /IBConnectionString ' + $connQ + ' /Execute ' + $epfQ + ' /C ' + $coldQ + ' /N ' + $userQ + ' /P ' + $pwdQ" + Символы.ПС
+ "try {" + Символы.ПС
+ "    Start-Process -FilePath '" + ЕPSExe + "' -ArgumentList $argsStr -ErrorAction Stop" + Символы.ПС
+ "} catch {" + Символы.ПС
+ "    Write-Output ""Failed to launch 1C: $_""" + Символы.ПС
+ "    Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "    exit 1" + Символы.ПС
+ "}" + Символы.ПС
+ "$timeout = 120; $elapsed = 0" + Символы.ПС
+ "while ($elapsed -lt $timeout) {" + Символы.ПС
+ "    if (Test-Path $signalPath) {" + Символы.ПС
+ "        $content = (Get-Content $signalPath -Raw -Encoding UTF8).Trim()" + Символы.ПС
+ "        Remove-Item $signalPath -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "        Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "        if ($content -eq 'ready') { Write-Output 'Session started successfully. Note: the first MCP request to the new session may fail - retry once if it does.'; exit 0 }" + Символы.ПС
+ "        else { Write-Output ""Session start failed: $content""; exit 1 }" + Символы.ПС
+ "    }" + Символы.ПС
+ "    Start-Sleep -Seconds 3; $elapsed += 3" + Символы.ПС
+ "}" + Символы.ПС
+ "Write-Output ""Session start timeout after $timeout seconds""" + Символы.ПС
+ "Remove-Item ($MyInvocation.MyCommand.Path) -Force -ErrorAction SilentlyContinue" + Символы.ПС
+ "exit 1";
		КонецЕсли;
	Иначе
		// Linux/Mac — bash
		ЕБашExe    = ЭкранироватьБашОдинарнаяКавычка(ПутьExe);
		ЕБашПарамс = ЭкранироватьБашОдинарнаяКавычка(ПутьПараметров);
		ЕБашСигнал = ЭкранироватьБашОдинарнаяКавычка(ПутьСигнала);

		Если РежимАутентификации = 0 Тогда
			AUTH_ARGS = "/WA";
		Иначе
			AUTH_ARGS = "/N '" + ЭкранироватьБашОдинарнаяКавычка(ИмяПольз) + "'";
		КонецЕсли;

		Если НЕ НуженПароль Тогда
			Текст =
"#!/bin/bash" + Символы.ПС
+ "SIGNAL_PATH='" + ЕБашСигнал + "'" + Символы.ПС
+ "SCRIPT_PATH=""$0""" + Символы.ПС
+ "TS=$(date +%Y%m%d%H%M%S)" + Символы.ПС
+ "'" + ЕБашExe + "' ENTERPRISE /IBConnectionString '" + ЭкранироватьБашОдинарнаяКавычка(СтрСоед) + "' /Execute '" + ЭкранироватьБашОдинарнаяКавычка(ПутьОбработки) + "' /C 'coldstart;'" + """" + "$TS" + """" + "';'" + ЕБашПарамс + "' " + AUTH_ARGS + " &" + Символы.ПС
+ "TIMEOUT=120; ELAPSED=0" + Символы.ПС
+ "while [ $ELAPSED -lt $TIMEOUT ]; do" + Символы.ПС
+ "    if [ -f ""$SIGNAL_PATH"" ]; then" + Символы.ПС
+ "        CONTENT=$(cat ""$SIGNAL_PATH""); rm -f ""$SIGNAL_PATH""; rm -f ""$SCRIPT_PATH""" + Символы.ПС
+ "        [ ""$CONTENT"" = ""ready"" ] && echo 'Session started successfully. Note: the first MCP request to the new session may fail - retry once if it does.' && exit 0" + Символы.ПС
+ "        echo ""Session start failed: $CONTENT""; exit 1" + Символы.ПС
+ "    fi" + Символы.ПС
+ "    sleep 3; ELAPSED=$((ELAPSED + 3))" + Символы.ПС
+ "done" + Символы.ПС
+ "echo ""Session start timeout after ${TIMEOUT} seconds""; rm -f ""$SCRIPT_PATH""; exit 1";
		Иначе
			ЕБашПольз = ЭкранироватьБашОдинарнаяКавычка(ИмяПольз);
			Текст =
"#!/bin/bash" + Символы.ПС
+ "SCRIPT_PATH=""$0""" + Символы.ПС
+ "command -v python3 >/dev/null 2>&1 || { echo 'python3 not found on PATH'; rm -f ""$SCRIPT_PATH""; exit 127; }" + Символы.ПС
+ "ENC_PWD='" + ЗашифрПароль + "'" + Символы.ПС
+ "LAUNCH_PASSWORD=$(python3 - ""$ENC_PWD"" ""$1"" <<'PYEOF'" + Символы.ПС
+ "import sys, base64" + Символы.ПС
+ "try:" + Символы.ПС
+ "    data = base64.b64decode(sys.argv[1])" + Символы.ПС
+ "    key = ('MCPColdLaunch-v1-n8Tz5rWj-' + sys.argv[2]).encode()" + Символы.ПС
+ "    print(bytes(b ^ key[i % len(key)] for i,b in enumerate(data)).decode(), end='')" + Символы.ПС
+ "except Exception as e:" + Символы.ПС
+ "    sys.stderr.write(str(e)+'\\n'); sys.exit(1)" + Символы.ПС
+ "PYEOF" + Символы.ПС
+ ")" + Символы.ПС
+ "rc=$?" + Символы.ПС
+ "[ $rc -eq 0 ] || { echo 'password decrypt failed'; rm -f ""$SCRIPT_PATH""; exit 1; }" + Символы.ПС
+ "[ -n ""$LAUNCH_PASSWORD"" ] || { echo 'password decrypt failed: empty result'; rm -f ""$SCRIPT_PATH""; exit 1; }" + Символы.ПС
+ "SIGNAL_PATH='" + ЕБашСигнал + "'" + Символы.ПС
+ "TS=$(date +%Y%m%d%H%M%S)" + Символы.ПС
+ "'" + ЕБашExe + "' ENTERPRISE /IBConnectionString '" + ЭкранироватьБашОдинарнаяКавычка(СтрСоед) + "' /Execute '" + ЭкранироватьБашОдинарнаяКавычка(ПутьОбработки) + "' /C 'coldstart;'" + """" + "$TS" + """" + "';'" + ЕБашПарамс + "' /N '" + ЕБашПольз + "' /P ""$LAUNCH_PASSWORD"" &" + Символы.ПС
+ "TIMEOUT=120; ELAPSED=0" + Символы.ПС
+ "while [ $ELAPSED -lt $TIMEOUT ]; do" + Символы.ПС
+ "    if [ -f ""$SIGNAL_PATH"" ]; then" + Символы.ПС
+ "        CONTENT=$(cat ""$SIGNAL_PATH""); rm -f ""$SIGNAL_PATH""; rm -f ""$SCRIPT_PATH""" + Символы.ПС
+ "        [ ""$CONTENT"" = ""ready"" ] && echo 'Session started successfully. Note: the first MCP request to the new session may fail - retry once if it does.' && exit 0" + Символы.ПС
+ "        echo ""Session start failed: $CONTENT""; exit 1" + Символы.ПС
+ "    fi" + Символы.ПС
+ "    sleep 3; ELAPSED=$((ELAPSED + 3))" + Символы.ПС
+ "done" + Символы.ПС
+ "echo ""Session start timeout after ${TIMEOUT} seconds""; rm -f ""$SCRIPT_PATH""; exit 1";
		КонецЕсли;
	КонецЕсли;

	Возврат Текст;
КонецФункции

&НаКлиенте
Функция ХолодныйВременныйФайл(Назначение)
	Возврат КаталогВременныхФайлов() + "cold_" + Строка(Новый УникальныйИдентификатор) + "_" + Назначение;
КонецФункции

&НаКлиенте
Процедура ВыполнитьЗакрытиеСессии(КомандаИД, ДанныеЗапросаEmb = Неопределено, ИдЗапросаEmb = "", ИдПотокаEmb = "", ВызовИзREST = Ложь)

	// Guard: отвечаем через сырые аргументы ДО установки глобалей — точно как restart
	Если ПерезапускАктивенИлиЗавершается() Или ЗакрытиеСессииВПроцессе Тогда
		Если РежимПодключения = 0 Тогда
			ОтправитьРезультатНаСервер(АдресСервера, КомандаИД,
				Новый Структура("success,error", Ложь, "Restart or close already in progress"));
		ИначеЕсли ВызовИзREST Тогда
			ИдRESTЗапр = "";
			Попытка
				Если ДанныеЗапросаEmb <> Неопределено Тогда
					ИдRESTЗапр = Строка(ДанныеЗапросаEmb.id);
				КонецЕсли;
			Исключение КонецПопытки;
			Если ЗначениеЗаполнено(ИдRESTЗапр) Тогда
				РезТело = ЗаписатьJSON2(Новый Структура("success,error", Ложь, "Restart or close already in progress"));
				Попытка
					КомпонентаSendResponse(ИдRESTЗапр, 500, ЗаголовкиJSON(), РезТело);
				Исключение
					ДобавитьВЛог("close REST guard: SendResponse error: " + ОписаниеОшибки());
				КонецПопытки;
			Иначе
				ДобавитьВЛог("close REST guard: request ID lost, cannot send error response");
			КонецЕсли;
		Иначе
			Сод = Новый Массив;
			Сод.Добавить(Новый Структура("type,text", "text", "Error: Restart or close already in progress"));
			ОтвJSON = СформироватьJSONRPCОтвет(ИдЗапросаEmb,
				Новый Структура("content,isError", Сод, Истина));
			Если ПустаяСтрока(ИдПотокаEmb) Тогда
				ОтправитьSSEОтвет(ДанныеЗапросаEmb, ОтвJSON);
			Иначе
				КомпонентаSendSSEEvent(ИдПотокаEmb, ОтвJSON, "{}", "message");
			КонецЕсли;
		КонецЕсли;
		Возврат;
	КонецЕсли;
	ЗакрытиеСессииВПроцессе = Истина;

	// Устанавливаем глобали ПОСЛЕ guard (совпадает с паттерном restart)
	ЭтоRESTЗапросПерезапуска = ВызовИзREST;
	ИдКомандыПерезапуска     = КомандаИД;
	ДанныеЗапросаПерезапуска = ДанныеЗапросаEmb;
	ИдЗапросаПерезапуска     = ИдЗапросаEmb;
	ИдПотокаПерезапуска      = ИдПотокаEmb;
	ИдRESTЗапросаПерезапуска = "";
	Если ВызовИзREST Тогда
		Попытка
			Если ДанныеЗапросаEmb <> Неопределено Тогда
				ИдRESTЗапросаПерезапуска = Строка(ДанныеЗапросаEmb.id);
			КонецЕсли;
		Исключение КонецПопытки;
	КонецЕсли;

	ПутьПараметров = "";
	ПутьМаппингов  = "";
	ПутьСкрипта    = "";
	ПутьСигнала    = "";

	Попытка

		СисИнфо    = Новый СистемнаяИнформация();
		ЭтоWindows = СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86
			Или СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86_64;
		Если РежимАутентификацииПерезапуска = 0 И НЕ ЭтоWindows Тогда
			ЗакрытиеСессииВПроцессе = Ложь;
			ОтправитьОтветПерезапуска(Ложь, "Windows authentication is not available on the current OS");
			Возврат;
		КонецЕсли;
		ОшибкаВалидации = ПроверитьНастройкиПерезапускаНаСервере(
			РежимАутентификацииПерезапуска, ЗначениеЗаполнено(ПарольПерезапуска));
		Если ОшибкаВалидации <> "" Тогда
			ЗакрытиеСессииВПроцессе = Ложь;
			ОтправитьОтветПерезапуска(Ложь, ОшибкаВалидации);
			Возврат;
		КонецЕсли;
		ПутьОбработки = ПолучитьПутьКОбработкеНаСервере();
		Если НЕ ЗначениеЗаполнено(ПутьОбработки) Тогда
			ЗакрытиеСессииВПроцессе = Ложь;
			ОтправитьОтветПерезапуска(Ложь, "Cannot close: data processor path not available");
			Возврат;
		КонецЕсли;
		ФайлОбработкиПроверка = Новый Файл(ПутьОбработки);
		Если НЕ ФайлОбработкиПроверка.Существует() Тогда
			ЗакрытиеСессииВПроцессе = Ложь;
			ОтправитьОтветПерезапуска(Ложь, "Cannot close: data processor file not found: " + ПутьОбработки);
			Возврат;
		КонецЕсли;

		ПутьПараметров = ХолодныйВременныйФайл("params.json");
		ПутьСигнала    = ХолодныйВременныйФайл("signal.txt");

		МСПМассив = Новый Массив;
		Если РежимПодключения = 1 И МСПСессии <> Неопределено Тогда
			Для Каждого П Из МСПСессии Цикл
				МСПМассив.Добавить(П.Ключ);
			КонецЦикла;
		КонецЕсли;

		ЗашифрованныйПароль = ЗашифроватьПарольПерезапуска(
			?(РежимАутентификацииПерезапуска = 1, ПарольПерезапуска, ""));

		ПутьМаппингов = "";
		Если РежимПодключения = 1 Тогда
			ПутьМаппингов = СохранитьМаппингиАнонимизацииВоВременныйФайл(ХолодныйВременныйФайл("maps.json"));
		КонецЕсли;

		НуженЛаунчерСПаролем = (РежимАутентификацииПерезапуска = 1) И ЗначениеЗаполнено(ПарольПерезапуска);

		ЗашифрПарольЗапуска = "";
		ЧастьКлюча2         = "";
		Если НуженЛаунчерСПаролем Тогда
			Алфавит = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
			ГСЧ = Новый ГенераторСлучайныхЧисел();
			Для Счетчик = 1 По 20 Цикл
				ЧастьКлюча2 = ЧастьКлюча2 + Сред(Алфавит, ГСЧ.СлучайноеЧисло(1, 62), 1);
			КонецЦикла;
			ХолодныйКлюч = "MCPColdLaunch-v1-n8Tz5rWj-" + ЧастьКлюча2;
			ЗашифрПарольЗапуска = ЗашифроватьПарольПерезапуска(ПарольПерезапуска, ХолодныйКлюч);
		КонецЕсли;

		#Если ТолстыйКлиентОбычноеПриложение Или ТолстыйКлиентУправляемоеПриложение Тогда
			ИмяExe = "1cv8" + ?(ЭтоWindows, ".exe", "");
		#ИначеЕсли ТонкийКлиент Тогда
			ИмяExe = "1cv8c" + ?(ЭтоWindows, ".exe", "");
		#КонецЕсли
		ПутьExe  = КаталогПрограммы() + ИмяExe;
		СтрСоед  = СтрокаСоединенияИнформационнойБазы();
		ИмяПольз = ?(РежимАутентификацииПерезапуска = 1, ПолучитьИмяТекущегоПользователяНаСервере(), "");

		ПараметрыЗапуска = Новый Структура(
			"mcp_sessions,restart_password,mappings_path,launch_password,signal_path,conn_str,epf_path,username,auth_mode",
			МСПМассив, ЗашифрованныйПароль, ПутьМаппингов, ЗашифрПарольЗапуска, ПутьСигнала,
			СтрСоед, ПутьОбработки, ИмяПольз, РежимАутентификацииПерезапуска);
		ЗТПараметры = Новый ЗаписьТекста(ПутьПараметров, КодировкаТекста.UTF8);
		Попытка
			ЗТПараметры.Записать(ЗаписатьJSON2(ПараметрыЗапуска));
		Исключение
			Попытка ЗТПараметры.Закрыть(); Исключение КонецПопытки;
			ВызватьИсключение;
		КонецПопытки;
		ЗТПараметры.Закрыть();

		ПутьСкрипта = ХолодныйВременныйФайл(?(ЭтоWindows, "launch.ps1", "launch.sh"));
		ТекстСкрипта = СформироватьТекстЛаунчераХолодногоСтарта(
			ЭтоWindows, ПутьExe, СтрСоед, ПутьОбработки, ПутьПараметров, ИмяПольз,
			ЗашифрПарольЗапуска, РежимАутентификацииПерезапуска, ПутьСигнала);
		ЗТЛаунч = Новый ЗаписьТекста(ПутьСкрипта, КодировкаТекста.UTF8);
		Попытка
			ЗТЛаунч.Записать(ТекстСкрипта);
		Исключение
			Попытка ЗТЛаунч.Закрыть(); Исключение КонецПопытки;
			ВызватьИсключение;
		КонецПопытки;
		ЗТЛаунч.Закрыть();

		ПутьСкриптаEsc = ?(ЭтоWindows,
			СтрЗаменить(ПутьСкрипта, "'", "''"),
			СтрЗаменить(ПутьСкрипта, "'", "'\''"));
		ЧастьКлюча2Esc = "";
		Если НуженЛаунчерСПаролем Тогда
			ЧастьКлюча2Esc = ?(ЭтоWindows,
				СтрЗаменить(ЧастьКлюча2, "'", "''"),
				СтрЗаменить(ЧастьКлюча2, "'", "'\''"));
		КонецЕсли;
		КомандаЗапуска = ?(ЭтоWindows,
			"powershell -ExecutionPolicy Bypass -File '" + ПутьСкриптаEsc + "'"
				+ ?(НуженЛаунчерСПаролем, " '" + ЧастьКлюча2Esc + "'", ""),
			"bash '" + ПутьСкриптаEsc + "'"
				+ ?(НуженЛаунчерСПаролем, " '" + ЧастьКлюча2Esc + "'", ""));

		ТекстОтвета = "Session closed. To start a new session, run:"
			+ Символы.ПС + КомандаЗапуска
			+ Символы.ПС + "Run synchronously. Exit 0 = session ready. Non-zero = startup failed."
			+ " On Windows, run this command in PowerShell (not cmd.exe)."
			+ " On timeout: startup state is unknown — check whether 1C was already started before launching another."
			+ " On non-timeout exit 1: the launcher either did not start 1C, or the failed new instance was closed — safe to retry.";

		Если РежимПодключения = 0 Тогда
			ДобавитьВЛог("close_1c_session launch command: " + КомандаЗапуска);
			ОтправитьОтветПерезапуска(Истина, ТекстОтвета);
			Попытка ОтключитьсяОтСервера(); Исключение КонецПопытки;
			ЗавершитьРаботуСистемы(Ложь);
		Иначе
			ОтправитьОтветПерезапускаИЗавершитьEmbedded(ТекстОтвета);
		КонецЕсли;

	Исключение
		ТекстОшибки = ОписаниеОшибки();
		ДобавитьВЛог("close_1c_session error: " + ТекстОшибки);
		Попытка Если ЗначениеЗаполнено(ПутьСкрипта)    Тогда УдалитьФайлы(ПутьСкрипта);    КонецЕсли; Исключение КонецПопытки;
		Попытка Если ЗначениеЗаполнено(ПутьПараметров) Тогда УдалитьФайлы(ПутьПараметров); КонецЕсли; Исключение КонецПопытки;
		Попытка Если ЗначениеЗаполнено(ПутьМаппингов)  Тогда УдалитьФайлы(ПутьМаппингов);  КонецЕсли; Исключение КонецПопытки;
		Попытка Если ЗначениеЗаполнено(ПутьСигнала)    Тогда УдалитьФайлы(ПутьСигнала);    КонецЕсли; Исключение КонецПопытки;
		ЗакрытиеСессииВПроцессе = Ложь;
		ОтправитьОтветПерезапуска(Ложь, "close_1c_session error: " + ТекстОшибки);
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьСтаруюСессиюПослеОтвета()
	Если ЗавершениеПослеПерезапускаЗапущено Тогда
		Возврат;
	КонецЕсли;
	ЗавершениеПослеПерезапускаЗапущено = Истина;
	ПерезапускВОжидании = Ложь;
	Попытка ОтключитьОбработчикОжидания("АварийноЗавершитьСессиюПослеПерезапуска"); Исключение КонецПопытки;
	Попытка УдалитьФайлы(ПутьКлеймаПерезапуска); Исключение КонецПопытки;
	ПутьКлеймаПерезапуска = "";
	Попытка ОстановитьВстроенныйСервер(); Исключение КонецПопытки;
	ЗавершитьРаботуСистемы(Ложь);
КонецПроцедуры

&НаКлиенте
Процедура АварийноЗавершитьСессиюПослеПерезапуска()
	ЗавершитьСтаруюСессиюПослеОтвета();
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьОтветПерезапускаИЗавершитьEmbedded(Сообщение)
	Если ЭтоRESTЗапросПерезапуска Тогда
		// Guard: проверяем кэшированный ИдRESTЗапросаПерезапуска
		// (установлен при сохранении контекста в ВыполнитьПерезапускСессии).
		// Прямой доступ к ДанныеЗапроса.id не нужен.
		Если ИдRESTЗапросаПерезапуска = "" Тогда
			ДобавитьВЛог("restart REST: request context lost, direct shutdown");
			ЗавершитьСтаруюСессиюПослеОтвета();
			Возврат;
		КонецЕсли;
		ЗавершениеПослеПерезапускаЗапущено = Ложь;
		АсинхроннаяОтправкаЗапущена = Ложь; // флаг: callback зарегистрирован
		// Все операции до send/shutdown -- внутри Попытка.
		// ЗавершитьСтаруюСессиюПослеОтвета() НЕ внутри этой Попытка --
		// idempotency guard заблокирует retry если shutdown уже начался внутри.
		Попытка
			ПодключитьОбработчикОжидания("АварийноЗавершитьСессиюПослеПерезапуска", 30);
			Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
				РезТело = ЗаписатьJSON2(Новый Структура("success,data", Истина, Сообщение));
				// Флаг ДО вызова: если НачатьВызов бросит синхронно после частичной
				// регистрации callback -- catch увидит Истина и не вызовет shutdown
				// напрямую. Shutdown через watchdog (30с). Это безопаснее чем
				// прямой shutdown при потенциально живом callback.
				АсинхроннаяОтправкаЗапущена = Истина;
				Компонента.НачатьВызовSendResponse(
					Новый ОписаниеОповещения("ПослеОтправкиRESTОтветаПерезапуска", ЭтаФорма),
					ИдRESTЗапросаПерезапуска, 200, "{""Content-Type"":""application/json"",""Connection"":""close""}", РезТело);
				// ИНВАРИАНТ: между НачатьВызовSendResponse и Возврат -- никакого кода.
				Возврат;
			Иначе
				РезТело = ЗаписатьJSON2(Новый Структура("success,data", Истина, Сообщение));
				Компонента.SendResponse(ИдRESTЗапросаПерезапуска, 200, ЗаголовкиJSON(), РезТело);
			КонецЕсли;
		Исключение
			ДобавитьВЛог("restart REST: success path exception: " + ОписаниеОшибки());
			Если АсинхроннаяОтправкаЗапущена Тогда
				// Callback зарегистрирован (НачатьВызов прошёл успешно),
				// но исключение произошло после -- shutdown через callback/watchdog.
				// НЕ вызываем ЗавершитьСтаруюСессиюПослеОтвета здесь --
				// иначе shutdown до доставки ответа.
				Возврат;
			КонецЕсли;
			// Исключение ДО НачатьВызовSendResponse -- callback не зарегистрирован.
			// Падаем на ЗавершитьСтаруюСессиюПослеОтвета после КонецПопытки.
		КонецПопытки;
		// Сюда попадаем на sync success ИЛИ sync exception (без зарегистрированного callback).
		// Ответ либо отправлен (sync success), либо отправка невозможна
		// (исключение до/во время SendResponse). В обоих случаях закрываем
		// старую сессию -- новая уже ready, удержание бессмысленно.
		// Async success path вышел через Возврат выше -- shutdown через callback.
		ЗавершитьСтаруюСессиюПослеОтвета();
		Возврат;
	КонецЕсли;
	// ... существующий SSE-код без изменений ...
	Содержимое = Новый Массив;
	Содержимое.Добавить(Новый Структура("type,text", "text", Сообщение));
	РезультатMCP = Новый Структура("content,isError", Содержимое, Ложь);
	ОтветJSON    = СформироватьJSONRPCОтвет(ИдЗапросаПерезапуска, РезультатMCP);

	ЗавершениеПослеПерезапускаЗапущено = Ложь;
	ПодключитьОбработчикОжидания("АварийноЗавершитьСессиюПослеПерезапуска", 30);

	Если ПустаяСтрока(ИдПотокаПерезапуска) Тогда
		ИдПотока = ДанныеЗапросаПерезапуска.id;
		Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
			Компонента.НачатьВызовSendSSEEvent(
				Новый ОписаниеОповещения("ПослеОтправкиSSEОтветаПерезапуска", ЭтаФорма, ИдПотока),
				ИдПотока, ОтветJSON,
				СформироватьSSEЗаголовки(ДанныеЗапросаПерезапуска, Неопределено, Истина), "message");
		Иначе
			Компонента.SendSSEEvent(ИдПотока, ОтветJSON,
				СформироватьSSEЗаголовки(ДанныеЗапросаПерезапуска, Неопределено, Истина), "message");
			Компонента.CloseSSEStream(ИдПотока);
			ЗавершитьСтаруюСессиюПослеОтвета();
		КонецЕсли;
	Иначе
		Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
			Компонента.НачатьВызовSendSSEEvent(
				Новый ОписаниеОповещения("ПослеОтправкиОтветаПерезапускаВПоток", ЭтаФорма),
				ИдПотокаПерезапуска, ОтветJSON, "{}", "message");
		Иначе
			Компонента.SendSSEEvent(ИдПотокаПерезапуска, ОтветJSON, "{}", "message");
			ЗавершитьСтаруюСессиюПослеОтвета();
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеОтправкиRESTОтветаПерезапуска(РезВызова, ПарВызова, ДопПар) Экспорт
	Если РезВызова <> Неопределено И ТипЗнч(РезВызова) = Тип("Строка") И РезВызова <> "" Тогда
		ДобавитьВЛог("restart REST: SendResponse callback error: " + РезВызова);
	КонецЕсли;
	ЗавершитьСтаруюСессиюПослеОтвета();
КонецПроцедуры

&НаКлиенте
Процедура ПослеОтправкиSSEОтветаПерезапуска(РезВызова, ПарВызова, ДопПар) Экспорт
	Если РезВызова <> Неопределено И ТипЗнч(РезВызова) = Тип("Строка") И РезВызова <> "" Тогда
		ДобавитьВЛог("restart: SendSSEEvent error: " + РезВызова);
	КонецЕсли;
	КомпонентаCloseSSEStreamПерезапуска(ДопПар);
КонецПроцедуры

&НаКлиенте
Процедура КомпонентаCloseSSEStreamПерезапуска(РзId)
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		Компонента.НачатьВызовCloseSSEStream(
			Новый ОписаниеОповещения("ПослеЗакрытияПотокаПерезапуска", ЭтаФорма),
			РзId);
	Иначе
		Компонента.CloseSSEStream(РзId);
		ЗавершитьСтаруюСессиюПослеОтвета();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗакрытияПотокаПерезапуска(РезВызова, ПарВызова, ДопПар) Экспорт
	Если РезВызова <> Неопределено И ТипЗнч(РезВызова) = Тип("Строка") И РезВызова <> "" Тогда
		ДобавитьВЛог("restart: CloseSSEStream error: " + РезВызова);
	КонецЕсли;
	ЗавершитьСтаруюСессиюПослеОтвета();
КонецПроцедуры

&НаКлиенте
Процедура ПослеОтправкиОтветаПерезапускаВПоток(РезВызова, ПарВызова, ДопПар) Экспорт
	Если РезВызова <> Неопределено И ТипЗнч(РезВызова) = Тип("Строка") И РезВызова <> "" Тогда
		ДобавитьВЛог("restart: legacy SendSSEEvent error: " + РезВызова);
	КонецЕсли;
	ЗавершитьСтаруюСессиюПослеОтвета();
КонецПроцедуры

// Вызывается из ПослеЗапускаВстроенногоСервера() и ЗапуститьВстроенныйСерверСинхронно()
// сразу после АнонимизаторОчистить(). Вне autostart — немедленный возврат.
&НаКлиенте
Процедура ЗавершитьАвтостартПослеЗапускаВстроенногоСервера()
	Если ПутьФайлаСигналаАвтостарта = Неопределено Или ПутьФайлаСигналаАвтостарта = "" Тогда
		// Ранний выход если это не coldstart — сохраняем прежнее поведение для всех других сценариев
		Если НЕ ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
			Возврат;
		КонецЕсли;

		ОшибкаВосстановления = Ложь;
		Если ЗначениеЗаполнено(МаппингиДляВосстановления) Тогда
			Попытка
				ВосстановитьМаппингиАнонимизацииИзФайла(МаппингиДляВосстановления);
				ОбновитьТаблицуМаппинговВстроенный();
			Исключение
				ДобавитьВЛог("coldstart: mappings restore error: " + ОписаниеОшибки());
				ОшибкаВосстановления = Истина;
			КонецПопытки;
			Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
			МаппингиДляВосстановления = "";
		КонецЕсли;
		Если СессийДляВосстановления <> Неопределено Тогда
			Попытка
				ВосстановитьМСПСессии(СессийДляВосстановления);
			Исключение
				ДобавитьВЛог("coldstart: sessions restore error: " + ОписаниеОшибки());
				ОшибкаВосстановления = Истина;
			КонецПопытки;
			СессийДляВосстановления = Неопределено;
		КонецЕсли;
		Если ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
			Попытка
				СигналСтрока = ?(ОшибкаВосстановления,
					"failed:coldstart restore error (see log)", "ready");
				ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаХолодногоСтарта, КодировкаТекста.UTF8);
				ЗТ.ЗаписатьСтроку(СигналСтрока);
				ЗТ.Закрыть();
			Исключение КонецПопытки;
			ПутьФайлаСигналаХолодногоСтарта = "";
			Если ОшибкаВосстановления Тогда
				Попытка ОстановитьВстроенныйСервер(); Исключение КонецПопытки;
				ЗавершитьРаботуСистемы(Ложь);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		Возврат;
	КонецЕсли;

	ОбщаяОшибка = "";
	МаппингиПуть = "";

	Если МаппингиДляВосстановления <> Неопределено И МаппингиДляВосстановления <> "" Тогда
		МаппингиПуть = МаппингиДляВосстановления;
		Попытка
			ВосстановитьМаппингиАнонимизацииИзФайла(МаппингиДляВосстановления);
			ОбновитьТаблицуМаппинговВстроенный();
		Исключение
			ОбщаяОшибка = "Mappings restore error: " + ОписаниеОшибки();
		КонецПопытки;
		МаппингиДляВосстановления = "";
	КонецЕсли;

	Если ОбщаяОшибка = "" И СессийДляВосстановления <> Неопределено Тогда
		Попытка
			ВосстановитьМСПСессии(СессийДляВосстановления);
		Исключение
			ОбщаяОшибка = "Sessions restore error: " + ОписаниеОшибки();
		КонецПопытки;
		СессийДляВосстановления = Неопределено;
	КонецЕсли;

	Если ОбщаяОшибка <> "" Тогда
		Попытка УдалитьФайлы(МаппингиПуть); Исключение КонецПопытки;
		ОшибкаСтрока = СтрЗаменить(ОбщаяОшибка, Символы.ВК, " ");
		ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.ПС, " ");
		ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.Таб, " ");
		Попытка
			ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
			ЗТ.ЗаписатьСтроку("failed:" + ОшибкаСтрока);
			ЗТ.Закрыть();
		Исключение
		КонецПопытки;
		Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
		ПутьФайлаСигналаАвтостарта = "";
		ПутьКлеймаАвтостарта       = "";
		Попытка ОстановитьВстроенныйСервер(); Исключение КонецПопытки;
		ЗавершитьРаботуСистемы(Ложь);
		Возврат;
	КонецЕсли;

	Попытка
		ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
		ЗТ.ЗаписатьСтроку("ready");
		ЗТ.Закрыть();
		ПутьФайлаСигналаАвтостарта = "";
		Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
		ПутьКлеймаАвтостарта = "";
	Исключение
		ПутьФайлаСигналаАвтостарта = "";
		МаппингиДляВосстановления  = "";
		Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
		ПутьКлеймаАвтостарта       = "";
		СессийДляВосстановления    = Неопределено;
		Попытка УдалитьФайлы(МаппингиПуть); Исключение КонецПопытки;
		Попытка ОстановитьВстроенныйСервер(); Исключение КонецПопытки;
		ЗавершитьРаботуСистемы(Ложь);
	КонецПопытки;
КонецПроцедуры

// Пишет failed:-сигнал, удаляет mappings-файл и закрывает сессию.
// Вне autostart (ПутьФайлаСигналаАвтостарта пуст) — ничего не делает.
&НаКлиенте
Процедура ЗавершитьАвтостартСОшибкой(Сообщение)
	// Coldstart: cleanup и failed-сигнал — всегда, до раннего выхода
	Если ЗначениеЗаполнено(МаппингиДляВосстановления) Тогда
		Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
		МаппингиДляВосстановления = "";
	КонецЕсли;
	СессийДляВосстановления = Неопределено;
	Если ЗначениеЗаполнено(ПутьФайлаСигналаХолодногоСтарта) Тогда
		Попытка
			ОшибкаСтрока = СтрЗаменить(СтрЗаменить(Сообщение, Символы.ВК, " "), Символы.ПС, " ");
			ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаХолодногоСтарта, КодировкаТекста.UTF8);
			ЗТ.ЗаписатьСтроку("failed:" + ОшибкаСтрока);
			ЗТ.Закрыть();
		Исключение КонецПопытки;
		ПутьФайлаСигналаХолодногоСтарта = "";
		Попытка ОстановитьВстроенныйСервер(); Исключение КонецПопытки;
		ЗавершитьРаботуСистемы(Ложь);
		Возврат;
	КонецЕсли;

	Если ПутьФайлаСигналаАвтостарта = Неопределено Или ПутьФайлаСигналаАвтостарта = "" Тогда
		Возврат;
	КонецЕсли;
	Попытка
		ОшибкаСтрока = СтрЗаменить(Сообщение, Символы.ВК, " ");
		ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.ПС, " ");
		ОшибкаСтрока = СтрЗаменить(ОшибкаСтрока, Символы.Таб, " ");
		ЗТ = Новый ЗаписьТекста(ПутьФайлаСигналаАвтостарта, КодировкаТекста.UTF8);
		ЗТ.ЗаписатьСтроку("failed:" + ОшибкаСтрока);
		ЗТ.Закрыть();
	Исключение
	КонецПопытки;
	Если МаппингиДляВосстановления <> Неопределено И МаппингиДляВосстановления <> "" Тогда
		Попытка УдалитьФайлы(МаппингиДляВосстановления); Исключение КонецПопытки;
	КонецЕсли;
	Если ПутьКлеймаАвтостарта <> Неопределено И ПутьКлеймаАвтостарта <> "" Тогда
		Попытка УдалитьФайлы(ПутьКлеймаАвтостарта); Исключение КонецПопытки;
	КонецЕсли;
	ПутьФайлаСигналаАвтостарта = "";
	МаппингиДляВосстановления  = "";
	ПутьКлеймаАвтостарта       = "";
	СессийДляВосстановления    = Неопределено;
	ЗавершитьРаботуСистемы(Ложь);
КонецПроцедуры


// ─────────────────────────────────────────────────────────────────────────────────
// ОПИСАНИЯ ИНСТРУМЕНТОВ (полные docstring из Python-прокси — mcp_handler.py)
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция ПримечаниеОбАнонимизации()
	М = Новый Массив;
	М.Добавить("");
	М.Добавить("NOTE: Anonymization is enabled for this tool.");
	М.Добавить("Some returned values (person names, organization names, TINs, and other");
	М.Добавить("personal data) are replaced with anonymous tokens in the format");
	М.Добавить("[CATEGORY-NNNNN] (e.g. [ORG-00001], [PER-00042], [INN-00001]).");
	М.Добавить("Tokens are stable within the current session.");
	М.Добавить("Do not attempt to interpret these tokens as meaningful data.");
	М.Добавить("Tokens can be used in subsequent requests.");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolExecuteQuery(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Execute a query in 1C query language and return the results.");
	М.Добавить("");
	М.Добавить("This tool executes queries on the 1C:Enterprise database using the 1C query language.");
	М.Добавить("Results are returned as JSON or TOON format.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    query: The query text in 1C query language (e.g., ""ВЫБРАТЬ * ИЗ Справочник.Номенклатура"")");
	М.Добавить("    params: Optional dictionary of query parameters (e.g., {""Param1"": ""value1""}).");
	М.Добавить("           Parameter values can be:");
	М.Добавить("           - Simple values (string, number, boolean, date)");
	М.Добавить("           - Object references in format: {""_objectRef"": true, ""УникальныйИдентификатор"": ""uuid-string"", ""ТипОбъекта"": ""СправочникСсылка.Контрагенты""}");
	М.Добавить("             Object references are automatically converted to actual 1C references before query execution.");
	М.Добавить("    limit: Maximum number of rows to return (default: 100, max: 1000)");
	М.Добавить("    include_schema: Include column type schema in response (default: False)");
	М.Добавить("    ");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the query executed successfully");
	М.Добавить("    - data: Array of result objects in JSON or TOON format (if successful)");
	М.Добавить("    - schema: Column type information (if include_schema=True and successful)");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("    ");
	М.Добавить("Examples:");
	М.Добавить("    # Simple parameter");
	М.Добавить("    execute_query(");
	М.Добавить("        query=""ВЫБРАТЬ Код, Наименование ИЗ Справочник.Номенклатура ГДЕ Код = &КодТовара"",");
	М.Добавить("        params={""КодТовара"": ""001""},");
	М.Добавить("        limit=100,");
	М.Добавить("        include_schema=True");
	М.Добавить("    )");
	М.Добавить("    ");
	М.Добавить("    # Object reference parameter (from previous execute_query result)");
	М.Добавить("    execute_query(");
	М.Добавить("        query=""ВЫБРАТЬ * ИЗ Документ.РеализацияТоваровУслуг ГДЕ Контрагент = &МойКонтрагент"",");
	М.Добавить("        params={");
	М.Добавить("            ""МойКонтрагент"": {");
	М.Добавить("                ""_objectRef"": true,");
	М.Добавить("                ""УникальныйИдентификатор"": ""a1b2c3d4-e5f6-7890-1234-567890abcdef"",");
	М.Добавить("                ""ТипОбъекта"": ""СправочникСсылка.Контрагенты""");
	М.Добавить("            }");
	М.Добавить("        },");
	М.Добавить("        limit=50");
	М.Добавить("    )");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolSubmitForDeanonymization()
	М = Новый Массив;
	М.Добавить("Submit the final user-facing response for de-anonymization display.");
	М.Добавить("");
	М.Добавить("You MUST call this tool if, and only if, your final response to the user contains");
	М.Добавить("anonymization tokens in the form [CATEGORY-NNNNN], for example [ORG-00001],");
	М.Добавить("[PER-00042], [INN-00001].");
	М.Добавить("");
	М.Добавить("Call this tool exactly once, immediately before sending the final response to the user.");
	М.Добавить("Pass the complete final response text in the ""text"" parameter.");
	М.Добавить("Do NOT call this tool for intermediate reasoning, drafts, or raw tool outputs.");
	М.Добавить("");
	М.Добавить("This tool does NOT return de-anonymized text to you.");
	М.Добавить("It only confirms receipt, for example {""received"": true}.");
	М.Добавить("After calling this tool, send your original final response with tokens unchanged.");
	М.Добавить("The user will see the de-anonymized version in their interface.");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolExecuteCode(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Execute arbitrary code in 1C language.");
	М.Добавить("");
	М.Добавить("This tool executes code on the 1C:Enterprise server using the Execute() operator.");
	М.Добавить("The result is returned from the 'Результат' (Result) variable as JSON or TOON format.");
	М.Добавить("");
	М.Добавить("Important limitations (code is executed as a statement block, not a full module):");
	М.Добавить("- Do NOT declare procedures/functions (`Процедура/Функция`) inside the snippet.");
	М.Добавить("- Do NOT use `Возврат` (Return). Instead assign a value to `Результат`.");
	М.Добавить("");
	М.Добавить("WARNING: Some dangerous operations are blocked for safety reasons.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    code: The 1C code to execute. Use 'Результат = ...' to return a value.");
	М.Добавить("    execution_context: Optional['server'|'client'], default='server'.");
	М.Добавить("        'server' — НаСервереБезКонтекста: DB access, 1C objects, no form context.");
	М.Добавить("        'client' — НаКлиенте: form attributes (ЭтаФорма), UI functions, no DB queries.");
	М.Добавить("    ");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the code executed successfully");
	М.Добавить("    - data: The value of 'Результат' variable in JSON or TOON format (if successful)");
	М.Добавить("    - error: Error message with line number (if failed)");
	М.Добавить("    ");
	М.Добавить("Example:");
	М.Добавить("    execute_code(code=""Результат = ТекущаяДата();"")");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetMetadata()
	М = Новый Массив;
	М.Добавить("Get metadata information about 1C database objects.");
	М.Добавить("");
	М.Добавить("This tool returns information about the structure of the 1C database in JSON format.");
	М.Добавить("");
	М.Добавить("Usage modes:");
	М.Добавить("1. Summary (root types): No parameters (filter/meta_type/name_mask not provided)");
	М.Добавить("   Returns:");
	М.Добавить("     - data: list of root metadata types with counts (Тип, Количество)");
	М.Добавить("     - configuration: configuration/platform info (when available) as a top-level field");
	М.Добавить("");
	М.Добавить("2. Filtered list: Use 'meta_type' and/or 'name_mask' parameters");
	М.Добавить("   Example: meta_type=""Документ"", name_mask=""реализ""");
	М.Добавить("   Returns: List of objects matching the criteria.");
	М.Добавить("   Each item includes: ПолноеИмя, Синоним.");
	М.Добавить("   List results are sorted by ПолноеИмя and support pagination via offset.");
	М.Добавить("   List mode response always includes pagination metadata:");
	М.Добавить("     - truncated, limit, returned, count, offset, has_more, next_offset");
	М.Добавить("   Fields truncated/has_more indicate whether more results exist.");
	М.Добавить("");
	М.Добавить("3. Detailed structure: Use 'filter' parameter with exact full name");
	М.Добавить("   Example: filter=""Справочник.Номенклатура""");
	М.Добавить("   Returns: Full structure with attributes, dimensions, resources, tabular sections");
	М.Добавить("");
	М.Добавить("3a. Specific collection element: Use 'filter' with full path to element");
	М.Добавить("   Collection names are in singular (platform ПолноеИмя() format):");
	М.Добавить("     filter=""Справочник.Контрагенты.Реквизит.ИНН""");
	М.Добавить("     filter=""РегистрНакопления.Остатки.Измерение.Номенклатура""");
	М.Добавить("     filter=""РегистрНакопления.Остатки.Ресурс.Количество""");
	М.Добавить("     filter=""Задача.Задача.РеквизитАдресации.Исполнитель""");
	М.Добавить("     filter=""Справочник.Контрагенты.СтандартныйРеквизит.Наименование""");
	М.Добавить("     filter=""Документ.Реализация.ТабличнаяЧасть.Товары""");
	М.Добавить("     filter=""Документ.Реализация.ТабличнаяЧасть.Товары.Реквизит.Номенклатура""");
	М.Добавить("   Use with sections=[""properties""] to get extended element properties");
	М.Добавить("   (ПроверкаЗаполнения, ЗначениеЗаполнения, Ведущее, Использование, etc.)");
	М.Добавить("");
	М.Добавить("4. Extensions support: Use 'extension_name' parameter");
	М.Добавить("   - extension_name not provided: work with main configuration (default)");
	М.Добавить("   - extension_name="""": get list of all connected extensions");
	М.Добавить("   - extension_name=""ExtensionName"": work with objects inside the specified extension");
	М.Добавить("");
	М.Добавить("5. Attribute search: Use 'attribute_mask' parameter");
	М.Добавить("   Find all attributes (реквизиты/измерения/ресурсы/реквизиты ТЧ) whose name or synonym");
	М.Добавить("   contains the given substring (case-insensitive).");
	М.Добавить("   Returns same list contract as mode 2: {success, data, truncated, limit, returned,");
	М.Добавить("     count, offset, has_more, next_offset}");
	М.Добавить("   data: array of {ПолноеИмя, Синоним} — same shape as list mode items, e.g.:");
	М.Добавить("     {ПолноеИмя: ""Документ.Реализация.Реквизит.Контрагент"", Синоним: ""Контрагент""}");
	М.Добавить("     {ПолноеИмя: ""Документ.Реализация.ТабличнаяЧасть.Товары.Реквизит.Номенклатура"", Синоним: ""Номенклатура""}");
	М.Добавить("   ПолноеИмя uses singular segment names: Реквизит/Измерение/Ресурс/ТабличнаяЧасть");
	М.Добавить("   Round-trip: get_metadata(filter=data[0][""ПолноеИмя""], sections=[""properties""])");
	М.Добавить("   Compatible with: meta_type, name_mask, filter (root object), extension_name (specific).");
	М.Добавить("   NOTE: extension_name="""" (list extensions) takes priority, attribute_mask is ignored.");
	М.Добавить("   INCOMPATIBLE with sections (returns error). Use round-trip instead.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    filter: Full name of object (e.g., ""Справочник.Номенклатура"") or full path to collection element");
	М.Добавить("           (e.g., ""Справочник.Контрагенты.Реквизит.ИНН"",");
	М.Добавить("           ""РегистрНакопления.Остатки.Измерение.Номенклатура"",");
	М.Добавить("           ""Документ.Реализация.ТабличнаяЧасть.Товары.Реквизит.Номенклатура"")");
	М.Добавить("    meta_type: Object type filter for list (string or list). Use ""*"" to list across all root types.");
	М.Добавить("    name_mask: Search mask for name/synonym (case-insensitive substring search)");
	М.Добавить("    limit: Maximum number of objects in list (default: 100, max: 1000)");
	М.Добавить("    sections: Detail sections to include (works only with filter). Supported: properties, forms, commands, layouts, predefined, movements, characteristics (movements is only available for Документ objects)");
	М.Добавить("    offset: Offset for pagination in list mode (default: 0)");
	М.Добавить("    extension_name: Extension name (None=main config, """"=list extensions, ""Name""=extension objects).");
	М.Добавить("                   Whitespace-only values are rejected.");
	М.Добавить("    attribute_mask: Search mask for attribute name/synonym (case-insensitive substring).");
	М.Добавить("                   Returns list of {ПолноеИмя, Синоним} items (same shape as list mode).");
	М.Добавить("                   ПолноеИмя uses singular segment names (Реквизит/Измерение/Ресурс/ТабличнаяЧасть), usable as filter value.");
	М.Добавить("                   Compatible with meta_type, name_mask, filter, extension_name.");
	М.Добавить("                   NOTE: extension_name="""" (list extensions) ignores attribute_mask.");
	М.Добавить("                   INCOMPATIBLE with sections (returns error). Round-trip:");
	М.Добавить("                     data[0][""ПолноеИмя""] → filter + sections=[""properties""].");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("     - data: Metadata information in JSON format:");
	М.Добавить("         - Summary: array data of {Тип, Количество} plus optional top-level field configuration");
	М.Добавить("         - List: array of {ПолноеИмя, Синоним}");
	М.Добавить("         - Details: object structure; sections like Реквизиты/ТабличныеЧасти/Измерения/Ресурсы may be omitted if not applicable for the metadata object type (if applicable, they are returned as arrays and may be empty)");
	М.Добавить("         - Extensions list: array of extension properties (ConfigurationExtension), e.g. {Имя, Синоним, Активно, БезопасныйРежим, Версия, УникальныйИдентификатор, Назначение, ОбластьДействия, ...}");
	М.Добавить("    - extension: Extension name (when working with extension objects)");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("");
	М.Добавить("Examples:");
	М.Добавить("    # Get summary of root metadata types (counts)");
	М.Добавить("    get_metadata()");
	М.Добавить("");
	М.Добавить("    # Get only documents");
	М.Добавить("    get_metadata(meta_type=""Документ"")");
	М.Добавить("");
	М.Добавить("    # Search for objects with ""реализ"" in name (across all types)");
	М.Добавить("    get_metadata(name_mask=""реализ"")");
	М.Добавить("");
	М.Добавить("    # Get mixed list across all root types");
	М.Добавить("    get_metadata(meta_type=""*"", limit=50)");
	М.Добавить("");
	М.Добавить("    # Get details about a specific catalog");
	М.Добавить("    get_metadata(filter=""Справочник.Номенклатура"")");
	М.Добавить("");
	М.Добавить("    # Get extended properties of a specific attribute");
	М.Добавить("    get_metadata(filter=""Справочник.Контрагенты.Реквизит.ИНН"", sections=[""properties""])");
	М.Добавить("");
	М.Добавить("    # Get extended properties of a dimension");
	М.Добавить("    get_metadata(filter=""РегистрНакопления.Остатки.Измерение.Номенклатура"", sections=[""properties""])");
	М.Добавить("");
	М.Добавить("    # Get info about a tabular section with its attributes");
	М.Добавить("    get_metadata(filter=""Документ.Реализация.ТабличнаяЧасть.Товары"")");
	М.Добавить("");
	М.Добавить("    # Get extended properties of a tabular section attribute");
	М.Добавить("    get_metadata(filter=""Документ.Реализация.ТабличнаяЧасть.Товары.Реквизит.Номенклатура"", sections=[""properties""])");
	М.Добавить("");
	М.Добавить("    # Get extended properties of a standard attribute");
	М.Добавить("    get_metadata(filter=""Справочник.Контрагенты.СтандартныйРеквизит.Наименование"", sections=[""properties""])");
	М.Добавить("");
	М.Добавить("    # Get list of all connected extensions");
	М.Добавить("    get_metadata(extension_name="""")");
	М.Добавить("");
	М.Добавить("    # Get objects from a specific extension");
	М.Добавить("    get_metadata(extension_name=""MyExtension"", meta_type=""Справочник"")");
	М.Добавить("");
	М.Добавить("    # Get details about an object in extension");
	М.Добавить("    get_metadata(extension_name=""MyExtension"", filter=""Справочник.МойСправочник"")");
	М.Добавить("");
	М.Добавить("    # Find all 'Контрагент' attributes across entire configuration");
	М.Добавить("    get_metadata(attribute_mask=""контраг"")");
	М.Добавить("");
	М.Добавить("    # Only in documents");
	М.Добавить("    get_metadata(attribute_mask=""контраг"", meta_type=""Документ"")");
	М.Добавить("");
	М.Добавить("    # Within one specific object");
	М.Добавить("    get_metadata(attribute_mask=""контраг"", filter=""Справочник.Контрагенты"")");
	М.Добавить("");
	М.Добавить("    # Within extension");
	М.Добавить("    get_metadata(attribute_mask=""контраг"", extension_name=""МоёРасширение"")");
	М.Добавить("");
	М.Добавить("    # Round-trip: use result as filter for details");
	М.Добавить("    matches = get_metadata(attribute_mask=""контраг"")[""data""]");
	М.Добавить("    get_metadata(filter=matches[0][""ПолноеИмя""], sections=[""properties""])");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetEventLog(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Get event log entries from 1C database with filtering options and cursor pagination.");
	М.Добавить("");
	М.Добавить("This tool retrieves entries from the 1C event log (Журнал регистрации)");
	М.Добавить("with optional filtering by date range, importance level, event type,");
	М.Добавить("and additional filters for data object, metadata type, user, session, etc.");
	М.Добавить("Results are returned as JSON or TOON format.");
	М.Добавить("");
	М.Добавить("Supports cursor pagination for iterating through large result sets:");
	М.Добавить("- Response includes last_date, next_same_second_offset, and has_more");
	М.Добавить("- For next page: use last_date as start_date and next_same_second_offset as same_second_offset");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    start_date: Start date in ISO 8601 format (YYYY-MM-DDTHH:MM:SS).");
	М.Добавить("               If not provided, no start date filter is applied.");
	М.Добавить("               For pagination: use last_date from previous response.");
	М.Добавить("    end_date: End date in ISO 8601 format (YYYY-MM-DDTHH:MM:SS).");
	М.Добавить("             If not provided, no end date filter is applied.");
	М.Добавить("    levels: List of importance levels to filter by.");
	М.Добавить("           Valid values: ""Information"", ""Warning"", ""Error"", ""Note"".");
	М.Добавить("           If not provided, all levels are returned.");
	М.Добавить("    events: List of event types to filter by (e.g., ""_$Data$_.New"", ""_$Session$_.Start"").");
	М.Добавить("           If not provided, all event types are returned.");
	М.Добавить("    limit: Maximum number of records to return (default: 100, max: 1000).");
	М.Добавить("    object_description: Object description from execute_query results.");
	М.Добавить("                      Priority: object_description > link > data.");
	М.Добавить("                      Example: {""_objectRef"": true, ""УникальныйИдентификатор"": ""..."", ""ТипОбъекта"": ""...""}");
	М.Добавить("    link: Navigation link in format e1cib/data/Type.Name?ref=HexGUID.");
	М.Добавить("         Priority: object_description > link > data.");
	М.Добавить("         Example: ""e1cib/data/Документ.РеализацияТоваровУслуг?ref=...""");
	М.Добавить("    data: Reference to data object (navigation link) - for backward compatibility.");
	М.Добавить("          Priority: object_description > link > data.");
	М.Добавить("          Example: ""e1cib/data/Документ.РеализацияТоваровУслуг?ref=...""");
	М.Добавить("    metadata_type: List of metadata object types to filter by.");
	М.Добавить("                   Example: [""Документ.РеализацияТоваровУслуг"", ""Справочник.Номенклатура""].");
	М.Добавить("    user: List of user names to filter by.");
	М.Добавить("    session: List of session numbers to filter by.");
	М.Добавить("    application: List of application types to filter by.");
	М.Добавить("                Valid values: ""ThinClient"", ""WebClient"", ""ThickClient"",");
	М.Добавить("                ""BackgroundJob"", ""Designer"", ""COMConnection"",");
	М.Добавить("                ""Server"", ""WebService"", ""HTTPService"", ""ODataInterface"",");
	М.Добавить("                ""MobileAppClient"", ""MobileAppServer"", ""MobileAppBackgroundJob"",");
	М.Добавить("                ""MobileClient"", ""MobileStandaloneServer"",");
	М.Добавить("                ""FileVariantBackgroundJob"", ""FileVariantServerSide"",");
	М.Добавить("                ""WebSocket"", ""FileVariantWebSocket"",");
	М.Добавить("                ""1CV8C"", ""1CV8"".");
	М.Добавить("    computer: Computer name to filter by.");
	М.Добавить("    comment_contains: Substring to search in comment (case-insensitive).");
	М.Добавить("    transaction_status: Transaction status to filter by.");
	М.Добавить("                       Valid values: ""Committed"", ""RolledBack"", ""NotApplicable"", ""Unfinished"".");
	М.Добавить("    same_second_offset: Skip N records with the same second as start_date (for pagination).");
	М.Добавить("                       For pagination: use next_same_second_offset from previous response.");
	М.Добавить("                       Requires start_date to be specified.");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data: Array of event log entries in JSON or TOON format (if successful), each containing:");
	М.Добавить("        - date: Event timestamp");
	М.Добавить("        - level: Importance level (Information, Warning, Error, Note)");
	М.Добавить("        - event: Event type");
	М.Добавить("        - comment: Event description/comment");
	М.Добавить("        - user: User name");
	М.Добавить("        - metadata: Metadata object name");
	М.Добавить("        - data_presentation: Data presentation string");
	М.Добавить("        - session: Session number");
	М.Добавить("        - application: Application type");
	М.Добавить("        - computer: Computer name");
	М.Добавить("        - transaction_status: Transaction status");
	М.Добавить("    - count: Number of returned records");
	М.Добавить("    - last_date: ISO 8601 date of last record (cursor for pagination, only with date filter)");
	М.Добавить("    - next_same_second_offset: Accumulated offset for next page (only with date filter)");
	М.Добавить("    - has_more: Boolean indicating if there are more records (only with date filter)");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("");
	М.Добавить("Example:");
	М.Добавить("    # Get all errors from the last day");
	М.Добавить("    get_event_log(");
	М.Добавить("        start_date=""2024-01-15T00:00:00"",");
	М.Добавить("        end_date=""2024-01-15T23:59:59"",");
	М.Добавить("        levels=[""Error"", ""Warning""],");
	М.Добавить("        limit=100");
	М.Добавить("    )");
	М.Добавить("");
	М.Добавить("    # Pagination example - page 1");
	М.Добавить("    result = get_event_log(");
	М.Добавить("        start_date=""2024-01-01T00:00:00"",");
	М.Добавить("        end_date=""2024-01-31T23:59:59"",");
	М.Добавить("        levels=[""Error""],");
	М.Добавить("        limit=100");
	М.Добавить("    )");
	М.Добавить("    # Response: last_date=""2024-01-15T14:30:45"", next_same_second_offset=3, has_more=true");
	М.Добавить("");
	М.Добавить("    # Pagination example - page 2");
	М.Добавить("    result = get_event_log(");
	М.Добавить("        start_date=""2024-01-15T14:30:45"",  # last_date from page 1");
	М.Добавить("        end_date=""2024-01-31T23:59:59"",");
	М.Добавить("        levels=[""Error""],");
	М.Добавить("        limit=100,");
	М.Добавить("        same_second_offset=3  # next_same_second_offset from page 1");
	М.Добавить("    )");
	М.Добавить("");
	М.Добавить("    # Get entries for specific object using object_description");
	М.Добавить("    get_event_log(");
	М.Добавить("        object_description={");
	М.Добавить("            ""_objectRef"": True,");
	М.Добавить("            ""УникальныйИдентификатор"": ""ba7e5a3d-1234-5678-9abc-def012345678"",");
	М.Добавить("            ""ТипОбъекта"": ""ДокументСсылка.РеализацияТоваровУслуг""");
	М.Добавить("        }");
	М.Добавить("    )");
	М.Добавить("");
	М.Добавить("    # Get entries for specific metadata type and user");
	М.Добавить("    get_event_log(");
	М.Добавить("        metadata_type=[""Документ.РеализацияТоваровУслуг""],");
	М.Добавить("        user=[""Иванов""],");
	М.Добавить("        application=[""ThinClient"", ""WebClient""],");
	М.Добавить("        comment_contains=""ошибка""");
	М.Добавить("    )");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetObjectByLink(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Get 1C object data by navigation link.");
	М.Добавить("");
	М.Добавить("This tool retrieves complete object data from the 1C:Enterprise database");
	М.Добавить("using a navigation link. For standard objects: e1cib/data/Type.Name?ref=HexGUID.");
	М.Добавить("For external data sources:");
	М.Добавить("- Object tables (single key): e1cib/data/ВнешнийИсточникДанных.Source.Таблица.TableName?ref=Value");
	М.Добавить("- Non-object tables (composite key): e1cib/data/ВнешнийИсточникДанных.Source.Таблица.TableName?FieldName=Value");
	М.Добавить("Results are returned as JSON or TOON format.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    link: Navigation link. Standard: e1cib/data/Type.Name?ref=HexGUID (32 hex chars).");
	М.Добавить("          External: e1cib/data/ВнешнийИсточникДанных.Source.Таблица.TableName");
	М.Добавить("          - Object tables: ?ref=Value (e.g., ""...Таблица.Заказы?ref=117953"")");
	М.Добавить("          - Non-object tables: ?FieldName=Value (e.g., ""...Таблица.OrderItems?ID=5&Type=A"")");
	М.Добавить("          ");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data: Object data in JSON or TOON format (if successful), containing:");
	М.Добавить("        - _type: Full metadata type name");
	М.Добавить("        - _presentation: String representation of the object");
	М.Добавить("        - Standard attributes (Код, Наименование, Дата, Номер if applicable)");
	М.Добавить("        - Custom attributes defined in metadata");
	М.Добавить("        - Tabular sections with all rows");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("    ");
	М.Добавить("Example:");
	М.Добавить("    get_object_by_link(");
	М.Добавить("        link=""e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5""");
	М.Добавить("    )");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetLinkOfObject()
	М = Новый Массив;
	М.Добавить("Generate a 1C navigation link from an object description.");
	М.Добавить("");
	М.Добавить("This tool converts an object description (returned by execute_query) into a clickable");
	М.Добавить("navigation link that can be used to open the object directly in 1C.");
	М.Добавить("");
	М.Добавить("Use this tool to provide users with clickable links to 1C objects after finding them");
	М.Добавить("with execute_query.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    object_description: Object description from execute_query results with fields:");
	М.Добавить("                      - _objectRef: must be true");
	М.Добавить("                      - УникальныйИдентификатор: UUID string");
	М.Добавить("                      - ТипОбъекта: object type (e.g., ""СправочникСсылка.Контрагенты"")");
	М.Добавить("                      - Представление: string representation (optional)");
	М.Добавить("                      ");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data: Navigation link string (e.g., ""e1cib/data/Справочник.Контрагенты?ref=..."")");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("    ");
	М.Добавить("Example:");
	М.Добавить("    # After finding an object with execute_query");
	М.Добавить("    result = execute_query(""SELECT Ref FROM Catalog.Customers WHERE Code = '001'"")");
	М.Добавить("    # result.data[0][""Ref""] contains object description");
	М.Добавить("    ");
	М.Добавить("    # Generate link");
	М.Добавить("    link_result = get_link_of_object(result.data[0][""Ref""])");
	М.Добавить("    # link_result.link = ""e1cib/data/Справочник.Контрагенты?ref=80c6cc1a...""");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolFindReferencesToObject(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Find references to a given object across specified metadata collections.");
	М.Добавить("");
	М.Добавить("This tool searches for references to the target object in documents, catalogs,");
	М.Добавить("and registers. It checks all fields whose type can contain a reference to the");
	М.Добавить("target object type, then queries for actual matches.");
	М.Добавить("");
	М.Добавить("The search is performed in two stages:");
	М.Добавить("1. Candidate discovery: metadata traversal to find fields that can hold the target type");
	М.Добавить("2. Query execution: actual queries to find objects/records referencing the target");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    target_object_description: Object description from execute_query results with fields:");
	М.Добавить("                              - _objectRef: must be true");
	М.Добавить("                              - УникальныйИдентификатор: UUID string");
	М.Добавить("                              - ТипОбъекта: object type (e.g., ""СправочникСсылка.Контрагенты"")");
	М.Добавить("                              - Представление: string representation (optional)");
	М.Добавить("    search_scope: List of search scopes to check. Valid values:");
	М.Добавить("                 ""documents"", ""catalogs"", ""information_registers"",");
	М.Добавить("                 ""accumulation_registers"", ""accounting_registers"", ""calculation_registers"".");
	М.Добавить("    meta_filter: Optional metadata filter dict with fields:");
	М.Добавить("                - names: list of exact metadata object names (e.g., [""Документ.РеализацияТоваровУслуг""])");
	М.Добавить("                - name_mask: search mask for name/synonym (case-insensitive)");
	М.Добавить("                If names is provided, name_mask is ignored.");
	М.Добавить("    limit_hits: Maximum total number of hits (default: 200, max: 10000).");
	М.Добавить("    limit_per_meta: Maximum hits per metadata object (default: 20, max: 1000).");
	М.Добавить("    timeout_budget_sec: Time budget in seconds for the search (default: 30, min: 5, max: 300).");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data: Object containing:");
	М.Добавить("        - hits: Array of found references, each with:");
	М.Добавить("            - found_in_meta: metadata object name (e.g., ""Документ.РеализацияТоваровУслуг"")");
	М.Добавить("            - found_in_object: object_description of the owner (for documents/catalogs) or null (for registers)");
	М.Добавить("            - record_key: dimension values (for registers only)");
	М.Добавить("            - path: field path (e.g., ""Контрагент"" or ""Товары.Номенклатура"")");
	М.Добавить("            - match_kind: field type (attribute, tabular_section, dimension, resource, requisite)");
	М.Добавить("            - note: human-readable description");
	М.Добавить("        - total_hits: number of hits found");
	М.Добавить("        - candidates_checked: number of candidate fields checked");
	М.Добавить("        - timeout_exceeded: true if time budget was exhausted");
	М.Добавить("        - skipped_names: names from meta_filter.names that were skipped");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("");
	М.Добавить("Example:");
	М.Добавить("    find_references_to_object(");
	М.Добавить("        target_object_description={");
	М.Добавить("            ""_objectRef"": True,");
	М.Добавить("            ""УникальныйИдентификатор"": ""ba7e5a3d-1234-5678-9abc-def012345678"",");
	М.Добавить("            ""ТипОбъекта"": ""СправочникСсылка.Контрагенты"",");
	М.Добавить("            ""Представление"": ""ООО Рога и Копыта""");
	М.Добавить("        },");
	М.Добавить("        search_scope=[""documents""],");
	М.Добавить("        limit_hits=10");
	М.Добавить("    )");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetAccessRights(АнонимизацияВключена = Ложь)
	М = Новый Массив;
	М.Добавить("Get role permissions for a metadata object and optionally effective rights for a user.");
	М.Добавить("");
	М.Добавить("This tool retrieves information about what rights each role grants for the specified");
	М.Добавить("metadata object. If a user name is provided, it also calculates the effective rights");
	М.Добавить("for that user (sum of all their roles).");
	М.Добавить("");
	М.Добавить("IMPORTANT limitations:");
	М.Добавить("- effective_rights is ""rights by sum of roles"", NOT a guarantee of real access");
	М.Добавить("- RLS (Row Level Security) is NOT taken into account");
	М.Добавить("- Contextual restrictions (by organizations, departments) are NOT taken into account");
	М.Добавить("- This is a check of ""what is allowed by roles"", not ""what the user will actually see""");
	М.Добавить("");
	М.Добавить("Runtime requirements:");
	М.Добавить("- Admin rights are required: AccessRight() with 3rd parameter (Role/User) requires admin rights");
	М.Добавить("- Privileged mode is forbidden: if enabled, AccessRight() always returns True, result is meaningless");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    ctx: MCP Context (injected automatically)");
	М.Добавить("    metadata_object: Full metadata object name (e.g., ""Справочник.Контрагенты"", ""Документ.РеализацияТоваровУслуг"")");
	М.Добавить("    user_name: Optional user name for effective rights calculation (case-insensitive search)");
	М.Добавить("    rights_filter: Optional list of rights to show in result (default: all applicable rights for type)");
	М.Добавить("    roles_filter: Optional list of roles to show (default: all roles with rights)");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data: Object containing:");
	М.Добавить("        - metadata_object: full metadata object name");
	М.Добавить("        - metadata_type: object type (e.g., ""Справочник"", ""Документ"", ""РегистрСведений"")");
	М.Добавить("        - applicable_rights: array of applicable rights for this object type");
	М.Добавить("        - roles: array of roles with their rights, sorted by name");
	М.Добавить("        - total_roles: number of roles in result (after roles_filter)");
	М.Добавить("        - roles_with_rights: number of roles that have at least one right (after rights_filter)");
	М.Добавить("        - user: (if user_name provided) object with:");
	М.Добавить("            - name: user name");
	М.Добавить("            - full_name: user's full name (optional, may not be available)");
	М.Добавить("            - roles: array of role names assigned to the user");
	М.Добавить("            - effective_rights: object with right names as keys and boolean values");
	М.Добавить("    - error: Error message (if failed)");
	М.Добавить("");
	М.Добавить("Example:");
	М.Добавить("    # Get all role permissions for a catalog");
	М.Добавить("    get_access_rights(metadata_object=""Справочник.Контрагенты"")");
	М.Добавить("");
	М.Добавить("    # Get specific rights for a user");
	М.Добавить("    get_access_rights(");
	М.Добавить("        metadata_object=""Справочник.Контрагенты"",");
	М.Добавить("        user_name=""Иванов"",");
	М.Добавить("        rights_filter=[""Чтение"", ""Изменение""]");
	М.Добавить("    )");
	Если АнонимизацияВключена Тогда
		М.Добавить(ПримечаниеОбАнонимизации());
	КонецЕсли;
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция СоздатьСхемуИнструмента(Имя, Описание, ВходнаяСхема)
	Результат = Новый Структура;
	Результат.Вставить("name", Имя);
	Результат.Вставить("description", Описание);
	Результат.Вставить("inputSchema", ВходнаяСхема);
	Возврат Результат;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаExecuteQuery()
	Свойства = Новый Структура;
	Свойства.Вставить("query", Новый Структура("type, description", "string", "The query text in 1C query language (e.g., ""ВЫБРАТЬ * ИЗ Справочник.Номенклатура"")"));
	Свойства.Вставить("params", Новый Структура("type, description", "object", "Optional dictionary of query parameters (e.g., {""Param1"": ""value1""}). Object references use format {""_objectRef"": true, ""УникальныйИдентификатор"": ""uuid"", ""ТипОбъекта"": ""СправочникСсылка.X""}"));
	Свойства.Вставить("limit", Новый Структура("type, description, default", "integer", "Maximum number of rows to return (default: 100, max: 1000)", 100));
	Свойства.Вставить("include_schema", Новый Структура("type, description, default", "boolean", "Include column type schema in response (default: False)", Ложь));

	Требуемые = Новый Массив;
	Требуемые.Добавить("query");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаExecuteCode()
	Свойства = Новый Структура;
	Свойства.Вставить("code", Новый Структура("type, description", "string",
		"The 1C code to execute. Use 'Результат = ...' to return a value."));

	МассивКонтекстов = Новый Массив;
	МассивКонтекстов.Добавить("server");
	МассивКонтекстов.Добавить("client");
	СвойствоКонтекст = Новый Структура("type, description", "string",
		"Execution context: 'server' (default, НаСервереБезКонтекста — DB access, 1C objects) or 'client' (НаКлиенте — form attributes, UI functions, no DB queries)");
	СвойствоКонтекст.Вставить("enum", МассивКонтекстов);
	Свойства.Вставить("execution_context", СвойствоКонтекст);

	Требуемые = Новый Массив;
	Требуемые.Добавить("code");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetMetadata()
	Свойства = Новый Структура;
	Свойства.Вставить("filter", Новый Структура("type, description", "string", "Full name of object (e.g., ""Справочник.Номенклатура"") or full path to collection element (e.g., ""Справочник.Контрагенты.Реквизит.ИНН"", ""РегистрНакопления.Остатки.Измерение.Номенклатура"")"));

	// meta_type: string | array of string (anyOf как у Pydantic v2)
	СвойствоМетаТип = Новый Структура;
	СвойствоМетаТип.Вставить("description", "Object type filter for list (string or list). Use ""*"" to list across all root types.");
	МассивВариантов = Новый Массив;
	МассивВариантов.Добавить(Новый Структура("type", "string"));
	МассивВариантов.Добавить(Новый Структура("type, items", "array", Новый Структура("type", "string")));
	СвойствоМетаТип.Вставить("anyOf", МассивВариантов);
	Свойства.Вставить("meta_type", СвойствоМетаТип);

	Свойства.Вставить("name_mask", Новый Структура("type, description", "string", "Search mask for name/synonym (case-insensitive substring search)"));
	Свойства.Вставить("limit", Новый Структура("type, description, default", "integer", "Maximum number of objects in list (default: 100, max: 1000)", 100));
	Свойства.Вставить("offset", Новый Структура("type, description, default", "integer", "Offset for pagination in list mode (default: 0)", 0));

	// sections: array of string
	СвойствоСекции = Новый Структура;
	СвойствоСекции.Вставить("type", "array");
	СвойствоСекции.Вставить("items", Новый Структура("type", "string"));
	СвойствоСекции.Вставить("description", "Detail sections to include (works only with filter). Supported: properties, forms, commands, layouts, predefined, movements, characteristics (movements is only available for Документ objects)");
	Свойства.Вставить("sections", СвойствоСекции);

	Свойства.Вставить("extension_name", Новый Структура("type, description", "string", "Extension name (None=main config, """"=list extensions, ""Name""=extension objects). Whitespace-only values are rejected."));
	Свойства.Вставить("attribute_mask", Новый Структура("type, description", "string",
		"Search mask for attribute name/synonym (case-insensitive substring). " +
		"Returns list contract: {success, data:[{ПолноеИмя, Синоним}, ...], truncated, limit, " +
		"returned, count, offset, has_more, next_offset}. " +
		"Same item shape as list mode. ПолноеИмя is in singular format (Реквизит, Измерение, etc.) " +
		"and can be used directly as filter value (round-trip: data[0][""ПолноеИмя""]). " +
		"When combined with filter, filter must be Type.Name (not element path). " +
		"Compatible with: meta_type, name_mask, filter, extension_name=Name. " +
		"NOTE: extension_name="""" (list extensions) takes priority over attribute_mask. " +
		"INCOMPATIBLE with sections (returns error)."));

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetEventLog()
	Свойства = Новый Структура;
	ИтемСтр = Новый Структура("type", "string");
	ИтемЦел = Новый Структура("type", "integer");

	Свойства.Вставить("start_date", Новый Структура("type, description", "string", "Start date in ISO 8601 format (YYYY-MM-DDTHH:MM:SS). For pagination: use last_date from previous response."));
	Свойства.Вставить("end_date",   Новый Структура("type, description", "string", "End date in ISO 8601 format (YYYY-MM-DDTHH:MM:SS). If not provided, no end date filter is applied."));
	Свойства.Вставить("limit",      Новый Структура("type, description, default", "integer", "Maximum number of records to return (default: 100, max: 1000).", 100));

	// levels: array of string
	СвЛевелс = Новый Структура;
	СвЛевелс.Вставить("type", "array");
	СвЛевелс.Вставить("items", ИтемСтр);
	СвЛевелс.Вставить("description", "List of importance levels to filter by. Valid values: ""Information"", ""Warning"", ""Error"", ""Note"". If not provided, all levels are returned.");
	Свойства.Вставить("levels", СвЛевелс);

	// events: array of string
	СвЭвентс = Новый Структура;
	СвЭвентс.Вставить("type", "array");
	СвЭвентс.Вставить("items", ИтемСтр);
	СвЭвентс.Вставить("description", "List of event types to filter by (e.g., ""_$Data$_.New"", ""_$Session$_.Start""). If not provided, all event types are returned.");
	Свойства.Вставить("events", СвЭвентс);

	Свойства.Вставить("object_description", Новый Структура("type, description", "object", "Object description from execute_query results. Priority: object_description > link > data. Example: {""_objectRef"": true, ""УникальныйИдентификатор"": ""..."", ""ТипОбъекта"": ""...""}"));
	Свойства.Вставить("link", Новый Структура("type, description", "string", "Navigation link in format e1cib/data/Type.Name?ref=HexGUID. Priority: object_description > link > data."));
	Свойства.Вставить("data", Новый Структура("type, description", "string", "Reference to data object (navigation link) - for backward compatibility. Priority: object_description > link > data."));

	// metadata_type: array of string
	СвМетаТип = Новый Структура;
	СвМетаТип.Вставить("type", "array");
	СвМетаТип.Вставить("items", ИтемСтр);
	СвМетаТип.Вставить("description", "List of metadata object types to filter by. Example: [""Документ.РеализацияТоваровУслуг"", ""Справочник.Номенклатура""].");
	Свойства.Вставить("metadata_type", СвМетаТип);

	// user: array of string
	СвЮзер = Новый Структура;
	СвЮзер.Вставить("type", "array");
	СвЮзер.Вставить("items", ИтемСтр);
	СвЮзер.Вставить("description", "List of user names to filter by.");
	Свойства.Вставить("user", СвЮзер);

	// session: array of integer
	СвСессион = Новый Структура;
	СвСессион.Вставить("type", "array");
	СвСессион.Вставить("items", ИтемЦел);
	СвСессион.Вставить("description", "List of session numbers to filter by.");
	Свойства.Вставить("session", СвСессион);

	// application: array of string
	СвАпп = Новый Структура;
	СвАпп.Вставить("type", "array");
	СвАпп.Вставить("items", ИтемСтр);
	СвАпп.Вставить("description", "List of application types to filter by. Valid values: ""ThinClient"", ""WebClient"", ""ThickClient"", ""BackgroundJob"", ""Designer"", ""COMConnection"", ""Server"", ""WebService"", ""HTTPService"", ""ODataInterface"", ""MobileAppClient"", ""MobileAppServer"", ""1CV8C"", ""1CV8"", etc.");
	Свойства.Вставить("application", СвАпп);

	Свойства.Вставить("computer",          Новый Структура("type, description", "string", "Computer name to filter by."));
	Свойства.Вставить("comment_contains",   Новый Структура("type, description", "string", "Substring to search in comment (case-insensitive)."));
	Свойства.Вставить("transaction_status", Новый Структура("type, description", "string", "Transaction status to filter by. Valid values: ""Committed"", ""RolledBack"", ""NotApplicable"", ""Unfinished""."));
	Свойства.Вставить("same_second_offset", Новый Структура("type, description, default", "integer", "Skip N records with the same second as start_date (for pagination). Use next_same_second_offset from previous response. Requires start_date.", 0));

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetObjectByLink()
	Свойства = Новый Структура;
	Свойства.Вставить("link", Новый Структура("type, description", "string",
		"Navigation link. Standard objects: e1cib/data/Type.Name?ref=HexGUID (32 hex chars). " +
		"External data sources: e1cib/data/ВнешнийИсточникДанных.Source.Таблица.TableName " +
		"- Object tables (single key): ?ref=Value (system parameter) " +
		"- Non-object tables (composite key): ?FieldName=Value (real field names, e.g., ?ID=5&Type=A) " +
		"Examples: ""e1cib/data/Справочник.Контрагенты?ref=80c6cc1a7e58902811ebcda8cb07c0f5"", " +
		"""e1cib/data/ВнешнийИсточникДанных.ВнешняяБД.Таблица.Заказы?ref=117953"", " +
		"""e1cib/data/ВнешнийИсточникДанных.ВнешняяБД.Таблица.OrderItems?ID=5&Type=A"""));

	Требуемые = Новый Массив;
	Требуемые.Добавить("link");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetLinkOfObject()
	Свойства = Новый Структура;
	Свойства.Вставить("object_description", Новый Структура("type, description", "object", "Object description from execute_query results with fields: _objectRef (must be true), УникальныйИдентификатор (UUID), ТипОбъекта (e.g., ""СправочникСсылка.Контрагенты""), Представление (optional)"));

	Требуемые = Новый Массив;
	Требуемые.Добавить("object_description");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаFindReferencesToObject()
	Свойства = Новый Структура;
	ИтемСтр = Новый Структура("type", "string");

	Свойства.Вставить("target_object_description", Новый Структура("type, description", "object", "Object description from execute_query results with fields: _objectRef (must be true), УникальныйИдентификатор (UUID), ТипОбъекта (e.g., ""СправочникСсылка.Контрагенты""), Представление (optional)"));

	// search_scope: array of string (required — ранее отсутствовал в properties, что делало схему невалидной)
	СвСкоп = Новый Структура;
	СвСкоп.Вставить("type", "array");
	СвСкоп.Вставить("items", ИтемСтр);
	СвСкоп.Вставить("description", "List of search scopes to check. Valid values: ""documents"", ""catalogs"", ""information_registers"", ""accumulation_registers"", ""accounting_registers"", ""calculation_registers"".");
	Свойства.Вставить("search_scope", СвСкоп);

	Свойства.Вставить("meta_filter",       Новый Структура("type, description", "object", "Optional metadata filter dict with fields: names (list of exact metadata object names) and name_mask (case-insensitive search mask). If names is provided, name_mask is ignored."));
	Свойства.Вставить("limit_hits",        Новый Структура("type, description, default", "integer", "Maximum total number of hits (default: 200, max: 10000).", 200));
	Свойства.Вставить("limit_per_meta",    Новый Структура("type, description, default", "integer", "Maximum hits per metadata object (default: 20, max: 1000).", 20));
	Свойства.Вставить("timeout_budget_sec", Новый Структура("type, description, default", "integer", "Time budget in seconds for the search (default: 30, min: 5, max: 300).", 30));

	Требуемые = Новый Массив;
	Требуемые.Добавить("target_object_description");
	Требуемые.Добавить("search_scope");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetAccessRights()
	Свойства = Новый Структура;
	ИтемСтр = Новый Структура("type", "string");

	Свойства.Вставить("metadata_object", Новый Структура("type, description", "string", "Full metadata object name (e.g., ""Справочник.Контрагенты"", ""Документ.РеализацияТоваровУслуг"")"));
	Свойства.Вставить("user_name",       Новый Структура("type, description", "string", "Optional user name for effective rights calculation (case-insensitive search)"));

	// rights_filter: array of string
	СвРайтс = Новый Структура;
	СвРайтс.Вставить("type", "array");
	СвРайтс.Вставить("items", ИтемСтр);
	СвРайтс.Вставить("description", "Optional list of rights to show in result (default: all applicable rights for type)");
	Свойства.Вставить("rights_filter", СвРайтс);

	// roles_filter: array of string
	СвРолс = Новый Структура;
	СвРолс.Вставить("type", "array");
	СвРолс.Вставить("items", ИтемСтр);
	СвРолс.Вставить("description", "Optional list of roles to show (default: all roles with rights)");
	Свойства.Вставить("roles_filter", СвРолс);

	Требуемые = Новый Массив;
	Требуемые.Добавить("metadata_object");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetBslSyntaxHelp()
	Свойства = Новый Структура;

	СвойствоKeywords = Новый Структура;
	СвойствоKeywords.Вставить("type", "array");
	СвойствоKeywords.Вставить("items", Новый Структура("type", "string"));
	СвойствоKeywords.Вставить("description",
		"Search keywords or single full breadcrumb path to follow a link");
	Свойства.Вставить("keywords", СвойствоKeywords);

	МассивMatch = Новый Массив;
	МассивMatch.Добавить("all");
	МассивMatch.Добавить("any");
	СвойствоMatch = Новый Структура;
	СвойствоMatch.Вставить("type", "string");
	СвойствоMatch.Вставить("enum", МассивMatch);
	СвойствоMatch.Вставить("default", "all");
	СвойствоMatch.Вставить("description", "Keyword matching: all (default) or any");
	Свойства.Вставить("match", СвойствоMatch);

	СвойствоLimit = Новый Структура;
	СвойствоLimit.Вставить("type", "integer");
	СвойствоLimit.Вставить("default", 100);
	СвойствоLimit.Вставить("description", "Maximum number of candidates to return (1–300, default 100).");
	Свойства.Вставить("limit", СвойствоLimit);

	СвойствоOffset = Новый Структура;
	СвойствоOffset.Вставить("type", "integer");
	СвойствоOffset.Вставить("default", 0);
	СвойствоOffset.Вставить("description", "Number of candidates to skip (default 0). For next page use offset += limit.");
	Свойства.Вставить("offset", СвойствоOffset);

	СвойствоContentPage = Новый Структура;
	СвойствоContentPage.Вставить("type", "integer");
	СвойствоContentPage.Вставить("default", 1);
	СвойствоContentPage.Вставить("description", "Page number of content (1-based, default 1). If content_has_more is true, call again with content_page + 1.");
	Свойства.Вставить("content_page", СвойствоContentPage);

	Требуемые = Новый Массив;
	Требуемые.Добавить("keywords");

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetBslSyntaxHelp()
	М = Новый Массив;
	М.Добавить("Get syntax reference for 1C BSL language.");
	М.Добавить("");
	М.Добавить("Search by keywords to find built-in functions, methods, types and language constructs.");
	М.Добавить("Returns candidates (unique breadcrumb paths) and Markdown content when exactly one breadcrumb matches.");
	М.Добавить("If the single match is a navigation section (no content, has sub-topics), returns its sub-topics as candidates instead.");
	М.Добавить("Candidate paths (e.g. ""Массив/Методы/Найти"") and Markdown link targets from content (format: [Title](topic:Path)) can be used as keywords for exact lookup — pass the full string including the topic: prefix as-is.");
	М.Добавить("When searching for members of a type, include ""Методы"", ""Свойства"", or ""Конструкторы"" in keywords.");
	М.Добавить("Supports pagination: use limit (default 100) and offset (default 0) to page through results.");
	М.Добавить("Response includes total, offset, limit, has_more. If has_more is true, call again with offset += limit.");
	М.Добавить("If multiple candidates are returned — add more specific keywords to narrow down, or use pagination to browse all matches.");
	М.Добавить("When content is returned, it may be paginated. If content_has_more is true, call again with content_page + 1.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    keywords: List of search keywords, or a single candidate path / link from content for exact lookup");
	М.Добавить("    match: ""all"" (default) — all keywords must match; ""any"" — any keyword matches");
	М.Добавить("    limit: Maximum number of candidates to return (1–300, default 100)");
	М.Добавить("    offset: Number of candidates to skip for pagination (default 0)");
	М.Добавить("    content_page: Page number of content to return (1-based, default 1)");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Dictionary with:");
	М.Добавить("    - success: Boolean indicating if the request was successful");
	М.Добавить("    - data.candidates: paged slice of matching breadcrumb paths");
	М.Добавить("    - data.total: total number of matching candidates (before pagination)");
	М.Добавить("    - data.offset: the offset used for this response");
	М.Добавить("    - data.limit: the limit used for this response");
	М.Добавить("    - data.has_more: true if more candidates exist beyond this page");
	М.Добавить("    - data.content: Markdown text page (only when total==1 and offset==0), else null");
	М.Добавить("    - data.content_page: current page of content (only when content is not null)");
	М.Добавить("    - data.content_total_pages: total number of content pages (only when content is not null)");
	М.Добавить("    - data.content_has_more: true if more content pages exist (only when content is not null)");
	М.Добавить("");
	М.Добавить("Example:");
	М.Добавить("    # Search by keyword");
	М.Добавить("    keywords=[""Найти""]");
	М.Добавить("");
	М.Добавить("    # Narrow down with multiple keywords");
	М.Добавить("    keywords=[""Найти"", ""Массив""]");
	М.Добавить("");
	М.Добавить("    # Use a candidate path for exact lookup");
	М.Добавить("    keywords=[""Массив/Методы/Найти""]");
	М.Добавить("");
	М.Добавить("    # Use a link from content for exact lookup");
	М.Добавить("    keywords=[""topic:Массив/Методы/Найти""]");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьНормализоватьПолеРегиона(Рег, ИмяПоля, МинЗначение)
	Если НЕ Рег.Свойство(ИмяПоля) Тогда
		Возврат "Field 'region." + ИмяПоля + "': required";
	КонецЕсли;
	СырЗн = Рег[ИмяПоля];
	Если СырЗн = Null ИЛИ СырЗн = Неопределено Тогда
		Возврат "Field 'region." + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	ЧислоЗнч = Неопределено;
	Если ТипЗнч(СырЗн) = Тип("Число") Тогда
		ЧислоЗнч = СырЗн;
	ИначеЕсли ТипЗнч(СырЗн) = Тип("Строка") Тогда
		СтрЗнч = СокрЛП(СырЗн);
		Если НЕ ПустаяСтрока(СтрЗнч) Тогда
			Попытка ЧислоЗнч = Число(СтрЗнч); Исключение ЧислоЗнч = Неопределено; КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	// Булево явно НЕ принимается (паритет с Python reject_bool; контракт = строгий integer)
	Если ЧислоЗнч = Неопределено ИЛИ ЧислоЗнч <> Цел(ЧислоЗнч) Тогда
		Возврат "Field 'region." + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	ЦелЗнч = Цел(ЧислоЗнч);
	Если ЦелЗнч < МинЗначение Тогда
		Возврат "Field 'region." + ИмяПоля + "': Input should be greater than or equal to " + Строка(МинЗначение);
	КонецЕсли;
	Если ЦелЗнч > 32767 Тогда
		Возврат "Field 'region." + ИмяПоля + "': Input should be less than or equal to 32767";
	КонецЕсли;
	Рег.Вставить(ИмяПоля, ЦелЗнч);
	Возврат Неопределено;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаПолеРегиона(МинЗнч, Описание)
	С = Новый Структура;
	С.Вставить("type", "integer");
	С.Вставить("minimum", МинЗнч);
	С.Вставить("maximum", 32767);
	С.Вставить("description", Описание);
	Возврат С;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаGetScreenshot()
	Свойства = Новый Структура;

	СвойствоFormName = Новый Структура;
	СвойствоFormName.Вставить("type", "string");
	СвойствоFormName.Вставить("description", "1C form name to open before capture (optional). If omitted, captures the current active 1C window.");
	Свойства.Вставить("form_name", СвойствоFormName);

	СвойствоScale = Новый Структура;
	СвойствоScale.Вставить("type", "integer");
	СвойствоScale.Вставить("default", 100);
	СвойствоScale.Вставить("minimum", 10);
	СвойствоScale.Вставить("maximum", 200);
	СвойствоScale.Вставить("description", "Output image scale (10–200, default 100). Values above 100 produce a larger image with more detail (e.g. 200 = 2× size). Values below 100 produce a smaller image (e.g. 50 = half size). Grid coordinate labels always show original window pixel coordinates regardless of scale.");
	Свойства.Вставить("scale_percent", СвойствоScale);

	СвойствоShowGrid = Новый Структура;
	СвойствоShowGrid.Вставить("type", "boolean");
	СвойствоShowGrid.Вставить("default", Ложь);
	СвойствоShowGrid.Вставить("description", "If true, draws a grid on the screenshot (up to 10×10, dynamic density based on image size — small regions get fewer lines or none): red lines, yellow coordinate labels (px) on black background at each axis, red dots at intersections. Coordinate origin (0, 0) is at the top-left corner of the captured image. Grid line coordinates are also returned as text (grid_coords).");
	Свойства.Вставить("show_grid", СвойствоShowGrid);

	СвойстваРег = Новый Структура;
	СвойстваРег.Вставить("x",      СхемаПолеРегиона(0, "X offset from client-area top-left"));
	СвойстваРег.Вставить("y",      СхемаПолеРегиона(0, "Y offset from client-area top-left"));
	СвойстваРег.Вставить("width",  СхемаПолеРегиона(1, "Width of region"));
	СвойстваРег.Вставить("height", СхемаПолеРегиона(1, "Height of region"));
	МассивRequired = Новый Массив;
	МассивRequired.Добавить("x"); МассивRequired.Добавить("y");
	МассивRequired.Добавить("width"); МассивRequired.Добавить("height");
	СхемаОбъект = Новый Структура;
	СхемаОбъект.Вставить("type", "object");
	СхемаОбъект.Вставить("properties", СвойстваРег);
	СхемаОбъект.Вставить("required", МассивRequired);
	АнйОф = Новый Массив;
	АнйОф.Добавить(СхемаОбъект);
	АнйОф.Добавить(Новый Структура("type", "null"));
	СвойствоРегион = Новый Структура;
	СвойствоРегион.Вставить("anyOf", АнйОф);
	СвойствоРегион.Вставить("description", "Optional. Crop to this sub-region before scaling. Omit or set null for full window. Coordinates in original client-area pixels. Returns error if region is outside window bounds.");
	Свойства.Вставить("region", СвойствоРегион);

	СвойстваРектЭлем = Новый Структура;
	СвойстваРектЭлем.Вставить("x",      СхемаПолеРегиона(0, "X offset from client-area top-left"));
	СвойстваРектЭлем.Вставить("y",      СхемаПолеРегиона(0, "Y offset from client-area top-left"));
	СвойстваРектЭлем.Вставить("width",  СхемаПолеРегиона(1, "Width"));
	СвойстваРектЭлем.Вставить("height", СхемаПолеРегиона(1, "Height"));
	МассивRequiredРект = Новый Массив;
	МассивRequiredРект.Добавить("x"); МассивRequiredРект.Добавить("y");
	МассивRequiredРект.Добавить("width"); МассивRequiredРект.Добавить("height");
	СхемаЭлемРект = Новый Структура;
	СхемаЭлемРект.Вставить("type", "object");
	СхемаЭлемРект.Вставить("properties", СвойстваРектЭлем);
	СхемаЭлемРект.Вставить("required", МассивRequiredРект);
	СхемаМассивРект = Новый Структура;
	СхемаМассивРект.Вставить("type", "array");
	СхемаМассивРект.Вставить("maxItems", 20);
	СхемаМассивРект.Вставить("items", СхемаЭлемРект);
	АнйОфРект = Новый Массив;
	АнйОфРект.Добавить(СхемаМассивРект);
	АнйОфРект.Добавить(Новый Структура("type", "null"));
	СвойствоРекты = Новый Структура;
	СвойствоРекты.Вставить("anyOf", АнйОфРект);
	СвойствоРекты.Вставить("description", "Optional. Draw numbered blue rectangles (3 px, RGB 0,120,255) on the screenshot. Each rectangle is labeled with its 1-based index. Rectangles must not overlap — returns error if they do. Coordinates are in original client-area pixels, origin (0, 0) at the top-left corner of the captured image (same coordinate space as grid labels).");
	Свойства.Вставить("highlight_rects", СвойствоРекты);

	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Новый Массив);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolGetScreenshot()
	М = Новый Массив;
	М.Добавить("Take a screenshot of the active 1C application window and return it as base64 PNG.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    form_name: Optional. Full 1C form name (e.g. ""Справочник.Контрагенты.Форма.ФормаЭлемента"").");
	М.Добавить("               If provided, opens the form before capturing the window, then automatically closes the form.");
	М.Добавить("               If omitted, captures the current active 1C window immediately.");
	М.Добавить("    scale_percent: Output image scale (10–200, default 100). >100 = larger image with more detail (200 = 2× size). <100 = smaller image (50 = half size). Grid labels always show original window pixel coordinates.");
	М.Добавить("    show_grid: If true, overlays a grid (up to 10×10, dynamic density based on image size; small regions get fewer lines or none). Red lines, yellow px-coordinate labels on black background, red dots at intersections. Coordinate origin (0, 0) is at the top-left corner of the captured image. Grid line coordinates also returned as text (grid_coords). Default false.");
	М.Добавить("    region: Optional. Crop to a sub-region before scaling. Object with integer fields x, y (≥0), width, height (≥1). Coordinates in original client-area pixels. Omit or null for full window. Returns error if region is outside window bounds.");
	М.Добавить("    highlight_rects: Optional. Array of rectangles to draw on the screenshot as numbered blue frames (3 px, RGB 0,120,255).");
	М.Добавить("                     Each rectangle: {x, y, width, height} in original client-area pixels, origin (0, 0) at the top-left corner of the captured image (same coordinate space as grid labels).");
	М.Добавить("                     Rectangles are labeled 1, 2, 3… in order. Labels appear outside above the top-right corner (inside if not enough space above).");
	М.Добавить("                     Rectangles must not overlap — returns error if they do. Omit or pass null to draw no rectangles. Max 20 items.");
	М.Добавить("");
	М.Добавить("Returns:");
	М.Добавить("    Image content (PNG) on success, or error on failure.");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolRestart1cSession()
	М = Новый Массив;
	М.Добавить("Restart the current 1C session — the session that provides this MCP server and executes 1C tool calls.");
	М.Добавить("A restart is typically required after updating the 1C configuration (metadata, extensions, BSL code)");
	М.Добавить("so the new session picks up the changes. The new session starts automatically with the same database");
	М.Добавить("and connection settings; anonymization state is preserved. The current session shuts down once the new one is ready.");
	М.Добавить("");
	М.Добавить("IMPORTANT: Do NOT call this tool on your own initiative. Only invoke it when explicitly instructed —");
	М.Добавить("either by the user directly or as a defined step in a pipeline or task specification.");
	М.Добавить("Never infer that a restart is needed and call it autonomously.");
	М.Добавить("Note: the first MCP request to the new session may fail - retry once if it does.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    No arguments.");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОписаниеToolClose1cSession()
	М = Новый Массив;
	М.Добавить("Close the current 1C session — the session that provides this MCP server and executes 1C tool calls —");
	М.Добавить("and return a launcher script command to start a new one.");
	М.Добавить("Use this when exclusive database access is needed (e.g. configuration update).");
	М.Добавить("The response text contains the shell command to run the launcher script.");
	М.Добавить("Run it synchronously: exit 0 means the session is ready.");
	М.Добавить("On Windows, run the command in PowerShell (not cmd.exe).");
	М.Добавить("On timeout, check whether a 1C process was started before launching another.");
	М.Добавить("On non-timeout exit 1, read the output: pre-launch errors are safe to retry after fixing the cause;");
	М.Добавить("failed startup errors close the failed new instance automatically.");
	М.Добавить("NOTE: on Linux with password auth, python3 must be available on PATH.");
	М.Добавить("");
	М.Добавить("IMPORTANT: Do NOT call this tool on your own initiative. Only invoke when explicitly instructed.");
	М.Добавить("Note: the first MCP request to the new session may fail - retry once if it does.");
	М.Добавить("");
	М.Добавить("Args:");
	М.Добавить("    No arguments.");
	Возврат СоединитьМассив(М, Символы.ПС);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаRestart1cSession()
	Свойства = Новый Структура;
	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Новый Массив);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаClose1cSession()
	Свойства = Новый Структура;
	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Новый Массив);
	Возврат Схема;
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СхемаSubmitForDeanonymization()
	Свойства = Новый Структура;
	Свойства.Вставить("text", Новый Структура("type, description",
		"string", "The complete final response text containing anonymization tokens to be de-anonymized."));
	Требуемые = Новый Массив;
	Требуемые.Добавить("text");
	Схема = Новый Структура;
	Схема.Вставить("type", "object");
	Схема.Вставить("properties", Свойства);
	Схема.Вставить("required", Требуемые);
	Возврат Схема;
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// TOOLS/CALL
// ─────────────────────────────────────────────────────────────────────────────────

// ─────────────────────────────────────────────────────────────────────────────────
// НОРМАЛИЗАЦИЯ И ВАЛИДАЦИЯ ПАРАМЕТРОВ ДЛЯ ВСТРОЕННОГО HTTP-СЕРВЕРА
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, ИмяПоля, ЗначениеПоУмолчанию, МинЗначение, МаксЗначение)
	
	// Возвращает Неопределено при успехе или строку error_detail при ошибке.
	// error_detail формируем в стиле прокси: "Field '<name>': ..."
	
	Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	
	// Default
	Если НЕ АргументыЗапроса.Свойство(ИмяПоля) Тогда
		АргументыЗапроса.Вставить(ИмяПоля, ЗначениеПоУмолчанию);
		Возврат Неопределено;
	КонецЕсли;
	
	СыройЗначение = АргументыЗапроса[ИмяПоля];
	Если СыройЗначение = Null Или СыройЗначение = Неопределено Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	
	ЧислоЗначение = Неопределено;
	Если ТипЗнч(СыройЗначение) = Тип("Число") Тогда
		ЧислоЗначение = СыройЗначение;
	ИначеЕсли ТипЗнч(СыройЗначение) = Тип("Строка") Тогда
		СтрЗначение = СокрЛП(СыройЗначение);
		Если ПустаяСтрока(СтрЗначение) Тогда
			Возврат "Field '" + ИмяПоля + "': Input should be a valid integer";
		КонецЕсли;
		Попытка
			ЧислоЗначение = Число(СтрЗначение);
		Исключение
			ЧислоЗначение = Неопределено;
		КонецПопытки;
	КонецЕсли;
	
	Если ЧислоЗначение = Неопределено Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	
	Если ЧислоЗначение <> Цел(ЧислоЗначение) Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid integer";
	КонецЕсли;
	
	ЦелоеЗначение = Цел(ЧислоЗначение);
	
	Если ЦелоеЗначение < МинЗначение Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be greater than or equal to " + Строка(МинЗначение);
	КонецЕсли;
	
	Если ЦелоеЗначение > МаксЗначение Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be less than or equal to " + Строка(МаксЗначение);
	КонецЕсли;
	
	АргументыЗапроса.Вставить(ИмяПоля, ЦелоеЗначение);
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьИНормализоватьБулевоПоле(АргументыЗапроса, ИмяПоля, ЗначениеПоУмолчанию)
	
	// StrictBool-логика как в прокси: принимаем ТОЛЬКО JSON boolean (Булево в 1С).
	// Любые строки/числа и т.п. → ошибка валидации (не пытаемся конвертировать).
	// Возвращает Неопределено при успехе или строку error_detail при ошибке.
	
	Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid boolean";
	КонецЕсли;
	
	// Default
	Если НЕ АргументыЗапроса.Свойство(ИмяПоля) Тогда
		АргументыЗапроса.Вставить(ИмяПоля, ЗначениеПоУмолчанию);
		Возврат Неопределено;
	КонецЕсли;
	
	СыройЗначение = АргументыЗапроса[ИмяПоля];
	Если СыройЗначение = Null Или СыройЗначение = Неопределено Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid boolean";
	КонецЕсли;
	
	Если ТипЗнч(СыройЗначение) <> Тип("Булево") Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid boolean";
	КонецЕсли;
	
	АргументыЗапроса.Вставить(ИмяПоля, СыройЗначение);
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяПоля)
	Если АргументыЗапроса = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Если ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
		Возврат;
	КонецЕсли;
	Если НЕ АргументыЗапроса.Свойство(ИмяПоля) Тогда
		Возврат;
	КонецЕсли;
	Попытка
		АргументыЗапроса.Удалить(ИмяПоля);
	Исключение
		// На всякий случай: если Удалить недоступен, оставляем как есть.
	КонецПопытки;
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СтрокаСоответствуетISO8601ДатаВремя(СтрокаДаты)
	
	// Проверяем только формат (как в прокси): YYYY-MM-DDTHH:MM:SS
	Если ТипЗнч(СтрокаДаты) <> Тип("Строка") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если СтрДлина(СтрокаДаты) <> 19 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Сред(СтрокаДаты, 5, 1) <> "-" Или Сред(СтрокаДаты, 8, 1) <> "-"
		Или Сред(СтрокаДаты, 11, 1) <> "T"
		Или Сред(СтрокаДаты, 14, 1) <> ":"
		Или Сред(СтрокаДаты, 17, 1) <> ":" Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// Проверяем что остальные символы - цифры
	ПозицииНеЦифры = Новый Массив;
	ПозицииНеЦифры.Добавить(1);  // YYYY
	ПозицииНеЦифры.Добавить(2);
	ПозицииНеЦифры.Добавить(3);
	ПозицииНеЦифры.Добавить(4);
	ПозицииНеЦифры.Добавить(6);  // MM
	ПозицииНеЦифры.Добавить(7);
	ПозицииНеЦифры.Добавить(9);  // DD
	ПозицииНеЦифры.Добавить(10);
	ПозицииНеЦифры.Добавить(12); // HH
	ПозицииНеЦифры.Добавить(13);
	ПозицииНеЦифры.Добавить(15); // mm
	ПозицииНеЦифры.Добавить(16);
	ПозицииНеЦифры.Добавить(18); // ss
	ПозицииНеЦифры.Добавить(19);
	
	Для Каждого Позиция Из ПозицииНеЦифры Цикл
		Символ = Сред(СтрокаДаты, Позиция, 1);
		Если Символ < "0" Или Символ > "9" Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СтрокаСостоитИзHexСимволов(Текст)
	
	Если ТипЗнч(Текст) <> Тип("Строка") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Для Позиция = 1 По СтрДлина(Текст) Цикл
		Символ = Сред(Текст, Позиция, 1);
		Если НЕ (
			(Символ >= "0" И Символ <= "9")
			Или (Символ >= "a" И Символ <= "f")
			Или (Символ >= "A" И Символ <= "F")
		) Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СтрокаСоответствуетUUID(Текст)
	
	// Формат UUID как в прокси: 8-4-4-4-12
	Если ТипЗнч(Текст) <> Тип("Строка") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Текст = СокрЛП(Текст);
	Если СтрДлина(Текст) <> 36 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Сред(Текст, 9, 1) <> "-" Или Сред(Текст, 14, 1) <> "-"
		Или Сред(Текст, 19, 1) <> "-" Или Сред(Текст, 24, 1) <> "-" Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// Проверяем что остальные символы - hex
	Для Позиция = 1 По 36 Цикл
		Если Позиция = 9 Или Позиция = 14 Или Позиция = 19 Или Позиция = 24 Тогда
			Продолжить;
		КонецЕсли;
		Символ = Сред(Текст, Позиция, 1);
		Если НЕ (
			(Символ >= "0" И Символ <= "9")
			Или (Символ >= "a" И Символ <= "f")
			Или (Символ >= "A" И Символ <= "F")
		) Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьИНормализоватьНепустуюСтроку(АргументыЗапроса, ИмяПоля, СообщениеПустое)
	
	Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
		Возврат "Field '" + ИмяПоля + "': Field required";
	КонецЕсли;
	
	Если НЕ АргументыЗапроса.Свойство(ИмяПоля) Тогда
		Возврат "Field '" + ИмяПоля + "': Field required";
	КонецЕсли;
	
	СыройЗначение = АргументыЗапроса[ИмяПоля];
	Если СыройЗначение = Null Или СыройЗначение = Неопределено Тогда
		Возврат "Field '" + ИмяПоля + "': Input should be a valid string";
	КонецЕсли;
	
	// Pydantic (str) по умолчанию приводит многие типы к строке. Делаем так же.
	НормСтрока = СокрЛП(Строка(СыройЗначение));
	Если ПустаяСтрока(НормСтрока) Тогда
		Возврат "Field '" + ИмяПоля + "': " + СообщениеПустое;
	КонецЕсли;
	
	АргументыЗапроса.Вставить(ИмяПоля, НормСтрока);
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьОписаниеОбъекта(ОписаниеОбъекта)
	
	// Порт validate_object_description_data из прокси (сообщения стараемся сохранить).
	
	Если ОписаниеОбъекта = Неопределено Тогда
		Возврат "object_description must be a dictionary";
	КонецЕсли;
	Если ТипЗнч(ОписаниеОбъекта) <> Тип("Структура") Тогда
		Возврат "object_description must be a dictionary";
	КонецЕсли;
	
	Если НЕ ОписаниеОбъекта.Свойство("_objectRef") Тогда
		Возврат "object_description must contain '_objectRef' field";
	КонецЕсли;
	Если ОписаниеОбъекта._objectRef <> Истина Тогда
		Возврат "object_description._objectRef must be true";
	КонецЕсли;
	
	Если НЕ ОписаниеОбъекта.Свойство("УникальныйИдентификатор") Тогда
		Возврат "object_description must contain 'УникальныйИдентификатор' field";
	КонецЕсли;
	UUIDЗначение = ОписаниеОбъекта.УникальныйИдентификатор;
	Если ТипЗнч(UUIDЗначение) <> Тип("Строка") Или ПустаяСтрока(СокрЛП(UUIDЗначение)) Тогда
		Возврат "УникальныйИдентификатор must be a non-empty string";
	КонецЕсли;
	Если НЕ СтрокаСоответствуетUUID(UUIDЗначение) Тогда
		Возврат "УникальныйИдентификатор must be a valid UUID (format: 8-4-4-4-12), got: '" + UUIDЗначение + "'";
	КонецЕсли;
	
	Если НЕ ОписаниеОбъекта.Свойство("ТипОбъекта") Тогда
		Возврат "object_description must contain 'ТипОбъекта' field";
	КонецЕсли;
	ТипОбъекта = ОписаниеОбъекта.ТипОбъекта;
	Если ТипЗнч(ТипОбъекта) <> Тип("Строка") Или ПустаяСтрока(СокрЛП(ТипОбъекта)) Тогда
		Возврат "ТипОбъекта must be a non-empty string";
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьФорматНавигационнойСсылки(Ссылка, ПрефиксСообщения)
	
	// ПрефиксСообщения: "Link" или "link" (как в сообщениях прокси в разных моделях).
	Ссылка = СокрЛП(Строка(Ссылка));
	
	Префикс = "e1cib/data/";
	Если Лев(Ссылка, СтрДлина(Префикс)) <> Префикс Тогда
		Возврат ПрефиксСообщения + " must start with 'e1cib/data/'";
	КонецЕсли;
	
	ПозицияRef = Найти(Ссылка, "?ref=");
	Если ПозицияRef = 0 Тогда
		Возврат ПрефиксСообщения + " must contain '?ref=' parameter";
	КонецЕсли;
	
	ЧастьRef = Сред(Ссылка, ПозицияRef + 5);
	Если СтрДлина(ЧастьRef) <> 32 Тогда
		Возврат "ref parameter must be exactly 32 hexadecimal characters, got " + Строка(СтрДлина(ЧастьRef));
	КонецЕсли;
	
	Если НЕ СтрокаСостоитИзHexСимволов(ЧастьRef) Тогда
		Возврат "ref parameter must contain only hexadecimal characters";
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьИНормализоватьЛимитыИнструмента(ИмяИнструмента, АргументыЗапроса)
	
	// Возвращает Неопределено или строку error_detail (без двуязычной обёртки).
	
	ИмяИнструмента = СокрЛП(Строка(ИмяИнструмента));
	
	Если ИмяИнструмента = "execute_query" Тогда
		Возврат ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit", 100, 1, 1000);
		
	ИначеЕсли ИмяИнструмента = "get_metadata" Тогда
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit", 100, 1, 1000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		Возврат ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "offset", 0, 0, 1000000);

	ИначеЕсли ИмяИнструмента = "get_bsl_syntax_help" Тогда
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit", 100, 1, 300);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "offset", 0, 0, 1000000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		Возврат ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "content_page", 1, 1, 100000);

	ИначеЕсли ИмяИнструмента = "get_screenshot" Тогда
		// form_name опциональный: проверяем только если передан
		Если АргументыЗапроса.Свойство("form_name") Тогда
			ДетальОшибки = ПроверитьИНормализоватьНепустуюСтроку(
				АргументыЗапроса, "form_name",
				"form_name cannot be empty or whitespace only");
			Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		КонецЕсли;
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "scale_percent", 100, 10, 200);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		// Нормализация show_grid: полный паритет с Pydantic bool
		Если АргументыЗапроса.Свойство("show_grid") Тогда
			Зн = АргументыЗапроса.show_grid;
			Если ТипЗнч(Зн) = Тип("Булево") Тогда
				// уже Булево, ок
			ИначеЕсли ТипЗнч(Зн) = Тип("Число") Тогда
				// Pydantic принимает только 0 и 1; 2, -1, 42 → ValidationError
				Если Зн = 0 Тогда
					АргументыЗапроса.Вставить("show_grid", Ложь);
				ИначеЕсли Зн = 1 Тогда
					АргументыЗапроса.Вставить("show_grid", Истина);
				Иначе
					Возврат "Field 'show_grid': Input should be a valid boolean";
				КонецЕсли;
			ИначеЕсли ТипЗнч(Зн) = Тип("Строка") Тогда
				Сн = НРег(СокрЛП(Зн));
				Если Сн = "true" ИЛИ Сн = "1" ИЛИ Сн = "yes" ИЛИ Сн = "on" Тогда
					АргументыЗапроса.Вставить("show_grid", Истина);
				ИначеЕсли Сн = "false" ИЛИ Сн = "0" ИЛИ Сн = "no" ИЛИ Сн = "off" Тогда
					АргументыЗапроса.Вставить("show_grid", Ложь);
				Иначе
					Возврат "Field 'show_grid': Input should be a valid boolean";
				КонецЕсли;
			Иначе
				Возврат "Field 'show_grid': Input should be a valid boolean";
			КонецЕсли;
		КонецЕсли;
		// Валидация region
		Если АргументыЗапроса.Свойство("region") Тогда
			РегЗн = АргументыЗапроса.region;
			Если РегЗн <> Null И РегЗн <> Неопределено Тогда
				Если ТипЗнч(РегЗн) <> Тип("Структура") Тогда
					Возврат "Field 'region': must be an object with fields x, y, width, height";
				КонецЕсли;
				Рег = РегЗн;
				ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "x",      0);
				Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
				ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "y",      0);
				Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
				ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "width",  1);
				Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
				ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "height", 1);
				Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		// Валидация highlight_rects
		Если АргументыЗапроса.Свойство("highlight_rects") Тогда
			ХРЗн = АргументыЗапроса.highlight_rects;
			Если ХРЗн <> Null И ХРЗн <> Неопределено Тогда
				Если ТипЗнч(ХРЗн) <> Тип("Массив") Тогда
					Возврат "Field 'highlight_rects': must be an array";
				КонецЕсли;
				Если ХРЗн.Количество() > 20 Тогда
					Возврат "highlight_rects must not exceed 20 items";
				КонецЕсли;
				Для ИндексРект = 0 По ХРЗн.Количество() - 1 Цикл
					Рект = ХРЗн[ИндексРект];
					НомерРект = Формат(ИндексРект + 1, "ЧГ=");
					Если ТипЗнч(Рект) <> Тип("Структура") Тогда
						Возврат "Field 'highlight_rects[" + НомерРект + "]': must be an object with fields x, y, width, height";
					КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "x",      0);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "y",      0);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "width",  1);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "height", 1);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
				КонецЦикла;
				// Проверка пересечений
				Для ИндА = 0 По ХРЗн.Количество() - 1 Цикл
					А = ХРЗн[ИндА];
					Для ИндБ = ИндА + 1 По ХРЗн.Количество() - 1 Цикл
						Б = ХРЗн[ИндБ];
						Если НЕ (А.x + А.width <= Б.x ИЛИ Б.x + Б.width <= А.x
							ИЛИ А.y + А.height <= Б.y ИЛИ Б.y + Б.height <= А.y) Тогда
							Возврат "Rectangle " + Формат(ИндА + 1, "ЧГ=") + " and Rectangle " + Формат(ИндБ + 1, "ЧГ=") + " overlap";
						КонецЕсли;
					КонецЦикла;
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		Возврат Неопределено;

	ИначеЕсли ИмяИнструмента = "get_event_log" Тогда
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit", 100, 1, 1000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "same_second_offset", 0, 0, 10000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		
		// same_second_offset requires start_date (как в Python-прокси GetEventLogParams.model_validator)
		СмещениеВСекунде = 0;
		Если АргументыЗапроса <> Неопределено И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
			Если АргументыЗапроса.Свойство("same_second_offset") Тогда
				СмещениеВСекунде = АргументыЗапроса.same_second_offset;
			КонецЕсли;
		КонецЕсли;
		
		Если СмещениеВСекунде > 0 Тогда
			ДатаНачалаСтрока = "";
			Если АргументыЗапроса <> Неопределено И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
				Если АргументыЗапроса.Свойство("start_date") Тогда
					ДатаНачалаСтрока = СокрЛП(Строка(АргументыЗапроса.start_date));
				КонецЕсли;
			КонецЕсли;
			
			Если ПустаяСтрока(ДатаНачалаСтрока) Тогда
				Возврат "Field 'same_second_offset': same_second_offset requires start_date to be specified";
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "find_references_to_object" Тогда
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit_hits", 200, 1, 10000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		
		ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "limit_per_meta", 20, 1, 1000);
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
		
		Возврат ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "timeout_budget_sec", 30, 5, 300);
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПроверитьИНормализоватьАргументыИнструмента(ИмяИнструмента, АргументыЗапроса)
	
	// Централизованная валидация аргументов инструментов для встроенного HTTP-сервера.
	// Возвращает Неопределено или строку error_detail (без двуязычной обёртки).
	
	ДетальОшибки = ПроверитьИНормализоватьЛимитыИнструмента(ИмяИнструмента, АргументыЗапроса);
	Если ДетальОшибки <> Неопределено Тогда
		Возврат ДетальОшибки;
	КонецЕсли;
	
	ИмяИнструмента = СокрЛП(Строка(ИмяИнструмента));
	
	Если ИмяИнструмента = "execute_query" Тогда
		
		// query: обязательная непустая строка
		ДетальОшибки = ПроверитьИНормализоватьНепустуюСтроку(АргументыЗапроса, "query", "Query cannot be empty or whitespace only");
		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;
		
		// params: optional dict
		Если АргументыЗапроса <> Неопределено И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
			Если АргументыЗапроса.Свойство("params") Тогда
				ПарамЗапроса = АргументыЗапроса.params;
				Если ПарамЗапроса = Null Или ПарамЗапроса = Неопределено Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "params");
				ИначеЕсли ТипЗнч(ПарамЗапроса) = Тип("Строка") Тогда
					// Агент может передать params как JSON-строку — пробуем распарсить
					Попытка
						РазобранныеПарамы = ПрочитатьJSON2(ПарамЗапроса);
						Если ТипЗнч(РазобранныеПарамы) = Тип("Структура")
							Или ТипЗнч(РазобранныеПарамы) = Тип("Соответствие") Тогда
							АргументыЗапроса.params = РазобранныеПарамы;
						Иначе
							Возврат "Field 'params': Params must be a dictionary";
						КонецЕсли;
					Исключение
						Возврат "Field 'params': Params must be a dictionary";
					КонецПопытки;
				ИначеЕсли ТипЗнч(ПарамЗапроса) <> Тип("Структура")
					И ТипЗнч(ПарамЗапроса) <> Тип("Соответствие") Тогда
					Возврат "Field 'params': Params must be a dictionary";
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// include_schema: в прокси StrictBool (только boolean), default=False.
		Возврат ПроверитьИНормализоватьБулевоПоле(АргументыЗапроса, "include_schema", Ложь);
		
	ИначеЕсли ИмяИнструмента = "execute_code" Тогда

		// code: обязательная непустая строка
		ДетальОшибки = ПроверитьИНормализоватьНепустуюСтроку(АргументыЗапроса, "code", "Code cannot be empty or whitespace only");
		Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки; КонецЕсли;

		// execution_context: опциональный, только "server" или "client"
		Если АргументыЗапроса <> Неопределено И АргументыЗапроса.Свойство("execution_context") Тогда
			КонтекстЗнч = АргументыЗапроса.execution_context;
			Если КонтекстЗнч <> "server" И КонтекстЗнч <> "client" Тогда
				Возврат "execution_context must be 'server' or 'client'";
			КонецЕсли;
		КонецЕсли;

		Возврат Неопределено;

	ИначеЕсли ИмяИнструмента = "get_metadata" Тогда
		
		Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		// filter: Optional[str], "" или whitespace -> None
		Если АргументыЗапроса.Свойство("filter") Тогда
			ФильтрЗначение = АргументыЗапроса.filter;
			Если ФильтрЗначение = Null Или ФильтрЗначение = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "filter");
			Иначе
				ФильтрЗначение = СокрЛП(Строка(ФильтрЗначение));
				Если ПустаяСтрока(ФильтрЗначение) Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "filter");
				Иначе
					АргументыЗапроса.Вставить("filter", ФильтрЗначение);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// meta_type: Optional[Union[str, List[str]]]
		Если АргументыЗапроса.Свойство("meta_type") Тогда
			ТипыМета = АргументыЗапроса.meta_type;
			Если ТипыМета = Null Или ТипыМета = Неопределено Или ТипыМета = "" Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "meta_type");
			ИначеЕсли ТипЗнч(ТипыМета) = Тип("Строка") Тогда
				ТипыМета = СокрЛП(ТипыМета);
				Если ПустаяСтрока(ТипыМета) Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "meta_type");
				Иначе
					АргументыЗапроса.Вставить("meta_type", ТипыМета);
				КонецЕсли;
			ИначеЕсли ТипЗнч(ТипыМета) = Тип("Массив") Или ТипЗнч(ТипыМета) = Тип("ФиксированныйМассив") Тогда
				Очищенные = Новый Массив;
				Для Каждого Элемент Из ТипыМета Цикл
					Если Элемент = Null Или Элемент = Неопределено Тогда
						Продолжить;
					КонецЕсли;
					ЭлСтрока = СокрЛП(Строка(Элемент));
					Если ПустаяСтрока(ЭлСтрока) Тогда
						Продолжить;
					КонецЕсли;
					Очищенные.Добавить(ЭлСтрока);
				КонецЦикла;
				Если Очищенные.Количество() = 0 Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "meta_type");
				Иначе
					АргументыЗапроса.Вставить("meta_type", Очищенные);
				КонецЕсли;
			Иначе
				// Anything else (e.g. int) -> coerce to string
				ТипыМета = СокрЛП(Строка(ТипыМета));
				Если ПустаяСтрока(ТипыМета) Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "meta_type");
				Иначе
					АргументыЗапроса.Вставить("meta_type", ТипыМета);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// name_mask: Optional[str], whitespace-only запрещён
		Если АргументыЗапроса.Свойство("name_mask") Тогда
			Маска = АргументыЗапроса.name_mask;
			Если Маска = Null Или Маска = Неопределено Или Маска = "" Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "name_mask");
			Иначе
				Маска = СокрЛП(Строка(Маска));
				Если ПустаяСтрока(Маска) Тогда
					Возврат "name_mask cannot be empty or whitespace only";
				КонецЕсли;
				АргументыЗапроса.Вставить("name_mask", Маска);
			КонецЕсли;
		КонецЕсли;
		
		// extension_name: whitespace-only запрещён, "" разрешено, """"/'' -> ""
		Если АргументыЗапроса.Свойство("extension_name") Тогда
			ИмяРасширения = АргументыЗапроса.extension_name;
			Если ИмяРасширения = Null Или ИмяРасширения = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "extension_name");
			ИначеЕсли Строка(ИмяРасширения) = "" Тогда
				// валидно: список расширений
			Иначе
				Стрип = СокрЛП(Строка(ИмяРасширения));
				Если Стрип = """""" Или Стрип = "''" Тогда
					АргументыЗапроса.Вставить("extension_name", "");
				ИначеЕсли Стрип = "" Тогда
					Возврат "extension_name cannot be whitespace-only; use empty string '' for extensions list";
				Иначе
					АргументыЗапроса.Вставить("extension_name", Стрип);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// sections: Optional[List[str]], enum, только вместе с filter
		Если АргументыЗапроса.Свойство("sections") Тогда
			Секции = НормализоватьМассивПараметра(АргументыЗапроса.sections);
			Если Секции = Null Или Секции = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "sections");
			ИначеЕсли ТипЗнч(Секции) <> Тип("Массив") И ТипЗнч(Секции) <> Тип("ФиксированныйМассив") Тогда
				Возврат "Field 'sections': Input should be a valid list";
			Иначе
				// sections requires filter (после нормализации filter)
				ЕстьФильтр = АргументыЗапроса.Свойство("filter") И НЕ ПустаяСтрока(СокрЛП(Строка(АргументыЗапроса.filter)));
				Если НЕ ЕстьФильтр Тогда
					Возврат "sections parameter requires filter parameter";
				КонецЕсли;
				
				Допустимые = Новый Соответствие;
				Допустимые.Вставить("properties", Истина);
				Допустимые.Вставить("forms", Истина);
				Допустимые.Вставить("commands", Истина);
				Допустимые.Вставить("layouts", Истина);
				Допустимые.Вставить("predefined", Истина);
				Допустимые.Вставить("movements", Истина);
				Допустимые.Вставить("characteristics", Истина);
				
				ОчищенныеСекции = Новый Массив;
				НаборУникальных = Новый Соответствие;
				Неверные = Новый Массив;
				
				Для Каждого ЭлементСекции Из Секции Цикл
					Если ЭлементСекции = Null Или ЭлементСекции = Неопределено Тогда
						Продолжить;
					КонецЕсли;
					СекцияСтрока = СокрЛП(Строка(ЭлементСекции));
					СекцияСтрокаНиж = НРег(СекцияСтрока);
					Если ПустаяСтрока(СекцияСтрокаНиж) Тогда
						Продолжить;
					КонецЕсли;
					Если Допустимые.Получить(СекцияСтрокаНиж) = Неопределено Тогда
						Неверные.Добавить(Строка(ЭлементСекции));
						Продолжить;
					КонецЕсли;
					Если НаборУникальных.Получить(СекцияСтрокаНиж) = Неопределено Тогда
						НаборУникальных.Вставить(СекцияСтрокаНиж, Истина);
						ОчищенныеСекции.Добавить(СекцияСтрокаНиж);
					КонецЕсли;
				КонецЦикла;
				
				Если Неверные.Количество() > 0 Тогда
					Возврат "Invalid sections: " + ЗаписатьJSON2(Неверные)
						+ ". Allowed values: properties, forms, commands, layouts, predefined, movements, characteristics";
				КонецЕсли;
				
				Если ОчищенныеСекции.Количество() = 0 Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "sections");
				Иначе
					АргументыЗапроса.Вставить("sections", ОчищенныеСекции);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "get_event_log" Тогда
		
		Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		// start_date/end_date: ISO 8601 strict (YYYY-MM-DDTHH:MM:SS)
		ПоляДаты = Новый Массив;
		ПоляДаты.Добавить("start_date");
		ПоляДаты.Добавить("end_date");
		Для Каждого ИмяПоляДаты Из ПоляДаты Цикл
			Если АргументыЗапроса.Свойство(ИмяПоляДаты) Тогда
				ЗначениеДаты = АргументыЗапроса[ИмяПоляДаты];
				Если ЗначениеДаты = Null Или ЗначениеДаты = Неопределено Или ЗначениеДаты = "" Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяПоляДаты);
				Иначе
					ЗначениеДаты = СокрЛП(Строка(ЗначениеДаты));
					Если ПустаяСтрока(ЗначениеДаты) Тогда
						УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяПоляДаты);
					ИначеЕсли НЕ СтрокаСоответствуетISO8601ДатаВремя(ЗначениеДаты) Тогда
						Возврат "Invalid date format: '" + ЗначениеДаты + "'. Expected ISO 8601 format: YYYY-MM-DDTHH:MM:SS";
					Иначе
						АргументыЗапроса.Вставить(ИмяПоляДаты, ЗначениеДаты);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		// levels: Optional[List[str]] with enum
		Если АргументыЗапроса.Свойство("levels") Тогда
			Уровни = НормализоватьМассивПараметра(АргументыЗапроса.levels);
			Если Уровни = Null Или Уровни = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "levels");
			ИначеЕсли ТипЗнч(Уровни) <> Тип("Массив") И ТипЗнч(Уровни) <> Тип("ФиксированныйМассив") Тогда
				Возврат "Levels must be a list";
			ИначеЕсли Уровни.Количество() = 0 Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "levels");
			Иначе
				ДопустимыеУровни = Новый Соответствие;
				ДопустимыеУровни.Вставить("Information", Истина);
				ДопустимыеУровни.Вставить("Warning", Истина);
				ДопустимыеУровни.Вставить("Error", Истина);
				ДопустимыеУровни.Вставить("Note", Истина);
				
				НеверныеУровни = Новый Массив;
				ОчищенныеУровни = Новый Массив;
				Для Каждого Уровень Из Уровни Цикл
					Если ТипЗнч(Уровень) <> Тип("Строка") Тогда
						Возврат "Level must be a string, got: " + ТипЗнч(Уровень).Имя();
					КонецЕсли;
					УровеньСтрока = СокрЛП(Уровень);
					Если ПустаяСтрока(УровеньСтрока) Тогда
						Продолжить;
					КонецЕсли;
					Если ДопустимыеУровни.Получить(УровеньСтрока) = Неопределено Тогда
						НеверныеУровни.Добавить(УровеньСтрока);
					Иначе
						ОчищенныеУровни.Добавить(УровеньСтрока);
					КонецЕсли;
				КонецЦикла;
				
				Если НеверныеУровни.Количество() > 0 Тогда
					Возврат "Invalid level(s): " + ЗаписатьJSON2(НеверныеУровни)
						+ ". Valid levels are: " + "['Error', 'Information', 'Note', 'Warning']";
				КонецЕсли;
				
				Если ОчищенныеУровни.Количество() = 0 Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "levels");
				Иначе
					АргументыЗапроса.Вставить("levels", ОчищенныеУровни);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// events: Optional[List[str]]
		Если АргументыЗапроса.Свойство("events") Тогда
			События = НормализоватьМассивПараметра(АргументыЗапроса.events);
			Если События = Null Или События = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "events");
			ИначеЕсли ТипЗнч(События) <> Тип("Массив") И ТипЗнч(События) <> Тип("ФиксированныйМассив") Тогда
				Возврат "Events must be a list";
			ИначеЕсли События.Количество() = 0 Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "events");
			Иначе
				ОчищенныеСобытия = Новый Массив;
				Для Каждого Событие Из События Цикл
					Если ТипЗнч(Событие) <> Тип("Строка") Тогда
						Возврат "Event must be a string, got: " + ТипЗнч(Событие).Имя();
					КонецЕсли;
					СобытиеСтрока = СокрЛП(Событие);
					Если ПустаяСтрока(СобытиеСтрока) Тогда
						Продолжить;
					КонецЕсли;
					ОчищенныеСобытия.Добавить(СобытиеСтрока);
				КонецЦикла;
				Если ОчищенныеСобытия.Количество() = 0 Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "events");
				Иначе
					АргументыЗапроса.Вставить("events", ОчищенныеСобытия);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// application: Optional[List[str]] with enum list
		Если АргументыЗапроса.Свойство("application") Тогда
			Приложения = НормализоватьМассивПараметра(АргументыЗапроса.application);
			Если Приложения = Null Или Приложения = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "application");
			ИначеЕсли ТипЗнч(Приложения) <> Тип("Массив") И ТипЗнч(Приложения) <> Тип("ФиксированныйМассив") Тогда
				Возврат "Application must be a list";
			ИначеЕсли Приложения.Количество() = 0 Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "application");
			Иначе
				ДопустимыеПриложения = Новый Соответствие;
				СписокДопустимыхПриложений = Новый Массив;
				СписокДопустимыхПриложений.Добавить("ThinClient");
				СписокДопустимыхПриложений.Добавить("WebClient");
				СписокДопустимыхПриложений.Добавить("ThickClient");
				СписокДопустимыхПриложений.Добавить("BackgroundJob");
				СписокДопустимыхПриложений.Добавить("Designer");
				СписокДопустимыхПриложений.Добавить("COMConnection");
				СписокДопустимыхПриложений.Добавить("Server");
				СписокДопустимыхПриложений.Добавить("WebService");
				СписокДопустимыхПриложений.Добавить("HTTPService");
				СписокДопустимыхПриложений.Добавить("ODataInterface");
				СписокДопустимыхПриложений.Добавить("MobileAppClient");
				СписокДопустимыхПриложений.Добавить("MobileAppServer");
				СписокДопустимыхПриложений.Добавить("MobileAppBackgroundJob");
				СписокДопустимыхПриложений.Добавить("MobileClient");
				СписокДопустимыхПриложений.Добавить("MobileStandaloneServer");
				СписокДопустимыхПриложений.Добавить("FileVariantBackgroundJob");
				СписокДопустимыхПриложений.Добавить("FileVariantServerSide");
				СписокДопустимыхПриложений.Добавить("WebSocket");
				СписокДопустимыхПриложений.Добавить("FileVariantWebSocket");
				СписокДопустимыхПриложений.Добавить("1CV8C");
				СписокДопустимыхПриложений.Добавить("1CV8");
				Для Каждого Доп Из СписокДопустимыхПриложений Цикл
					ДопустимыеПриложения.Вставить(Доп, Истина);
				КонецЦикла;
				
				НеверныеПриложения = Новый Массив;
				ОчищенныеПриложения = Новый Массив;
				Для Каждого Прил Из Приложения Цикл
					Если ТипЗнч(Прил) <> Тип("Строка") Тогда
						Возврат "Application must be a string, got: " + ТипЗнч(Прил).Имя();
					КонецЕсли;
					ПрилСтрока = СокрЛП(Прил);
					Если ПустаяСтрока(ПрилСтрока) Тогда
						Продолжить;
					КонецЕсли;
					Если ДопустимыеПриложения.Получить(ПрилСтрока) = Неопределено Тогда
						НеверныеПриложения.Добавить(ПрилСтрока);
					Иначе
						ОчищенныеПриложения.Добавить(ПрилСтрока);
					КонецЕсли;
				КонецЦикла;
				
				Если НеверныеПриложения.Количество() > 0 Тогда
					Возврат "Invalid application(s): " + ЗаписатьJSON2(НеверныеПриложения)
						+ ". Valid applications are: " + "['ThinClient', 'WebClient', 'ThickClient', 'BackgroundJob', 'Designer', 'COMConnection', 'Server', 'WebService', 'HTTPService', 'ODataInterface', 'MobileAppClient', 'MobileAppServer', 'MobileAppBackgroundJob', 'MobileClient', 'MobileStandaloneServer', 'FileVariantBackgroundJob', 'FileVariantServerSide', 'WebSocket', 'FileVariantWebSocket', '1CV8C', '1CV8']";
				КонецЕсли;
				
				Если ОчищенныеПриложения.Количество() = 0 Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "application");
				Иначе
					АргументыЗапроса.Вставить("application", ОчищенныеПриложения);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// transaction_status: enum
		Если АргументыЗапроса.Свойство("transaction_status") Тогда
			Статус = АргументыЗапроса.transaction_status;
			Если Статус = Null Или Статус = Неопределено Или Статус = "" Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "transaction_status");
			Иначе
				Статус = СокрЛП(Строка(Статус));
				Если Статус <> "Committed" И Статус <> "RolledBack" И Статус <> "NotApplicable" И Статус <> "Unfinished" Тогда
					Возврат "Invalid transaction_status: '" + Статус + "'. Valid statuses are: ['Committed', 'RolledBack', 'NotApplicable', 'Unfinished']";
				КонецЕсли;
				АргументыЗапроса.Вставить("transaction_status", Статус);
			КонецЕсли;
		КонецЕсли;
		
		// metadata_type: normalize string to list (как в прокси)
		Если АргументыЗапроса.Свойство("metadata_type") Тогда
			МетаТип = АргументыЗапроса.metadata_type;
			Если МетаТип = Null Или МетаТип = Неопределено Или МетаТип = "" Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "metadata_type");
			ИначеЕсли ТипЗнч(МетаТип) = Тип("Строка") Тогда
				Сырой = СокрЛП(МетаТип);
				Если ПустаяСтрока(Сырой) Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "metadata_type");
				ИначеЕсли Лев(Сырой, 1) = "[" И Прав(Сырой, 1) = "]" Тогда
					// stringified JSON array
					Попытка
						Разобранный = ПрочитатьJSON2(Сырой);
						Если ТипЗнч(Разобранный) = Тип("Массив") Тогда
							АргументыЗапроса.Вставить("metadata_type", Разобранный);
						Иначе
							ОдинТип = Новый Массив;
							ОдинТип.Добавить(Сырой);
							АргументыЗапроса.Вставить("metadata_type", ОдинТип);
						КонецЕсли;
					Исключение
						ОдинТип = Новый Массив;
						ОдинТип.Добавить(Сырой);
						АргументыЗапроса.Вставить("metadata_type", ОдинТип);
					КонецПопытки;
				Иначе
					ОдинТип = Новый Массив;
					ОдинТип.Добавить(Сырой);
					АргументыЗапроса.Вставить("metadata_type", ОдинТип);
				КонецЕсли;
			ИначеЕсли ТипЗнч(МетаТип) = Тип("Массив") Или ТипЗнч(МетаТип) = Тип("ФиксированныйМассив") Тогда
				// ок
			Иначе
				Возврат "Field 'metadata_type': Input should be a valid list";
			КонецЕсли;
		КонецЕсли;
		
		// object_description / link: validate only used filter (priority)
		Если АргументыЗапроса.Свойство("object_description") И АргументыЗапроса.object_description <> Неопределено И АргументыЗапроса.object_description <> Null Тогда
			ДетальОшибки = ПроверитьОписаниеОбъекта(АргументыЗапроса.object_description);
			Если ДетальОшибки <> Неопределено Тогда
				Возврат ДетальОшибки;
			КонецЕсли;
		ИначеЕсли АргументыЗапроса.Свойство("link") И АргументыЗапроса.link <> Неопределено И АргументыЗапроса.link <> Null Тогда
			Линк = АргументыЗапроса.link;
			Линк = СокрЛП(Строка(Линк));
			Если ПустаяСтрока(Линк) Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "link");
			Иначе
				ДетальОшибки = ПроверитьФорматНавигационнойСсылки(Линк, "link");
				Если ДетальОшибки <> Неопределено Тогда
					Возврат ДетальОшибки;
				КонецЕсли;
				АргументыЗапроса.Вставить("link", Линк);
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "get_object_by_link" Тогда

		ДетальОшибки = ПроверитьИНормализоватьНепустуюСтроку(АргументыЗапроса, "link", "Link cannot be empty");
		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;

		Линк = АргументыЗапроса.link;

		// Тип определяем по пути до '?', независимо от наличия '?'
		Префикс = "e1cib/data/";
		ХвостБезПрефикса = Сред(Линк, СтрДлина(Префикс) + 1);
		ПозВопрос = Найти(ХвостБезПрефикса, "?");
		ПутьОбъекта = ?(ПозВопрос > 0, Лев(ХвостБезПрефикса, ПозВопрос - 1), ХвостБезПрефикса);

		Если НРег(Лев(ПутьОбъекта, СтрДлина("внешнийисточникданных."))) = "внешнийисточникданных." Тогда
			// Внешний источник — своя валидация
			ДетальОшибки = ПроверитьФорматСсылкиВнешнегоИсточника(Линк);
		Иначе
			// Обычный объект — стандартная hex-32 валидация
			ДетальОшибки = ПроверитьФорматНавигационнойСсылки(Линк, "Link");
		КонецЕсли;

		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;

		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "get_link_of_object" Тогда
		
		Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
			Возврат "Field 'object_description': Field required";
		КонецЕсли;
		
		Если НЕ АргументыЗапроса.Свойство("object_description") Тогда
			Возврат "Field 'object_description': Field required";
		КонецЕсли;
		
		ДетальОшибки = ПроверитьОписаниеОбъекта(АргументыЗапроса.object_description);
		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;
		
		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "find_references_to_object" Тогда
		
		Если АргументыЗапроса = Неопределено Или ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
			Возврат "Field 'target_object_description': Field required";
		КонецЕсли;
		
		Если НЕ АргументыЗапроса.Свойство("target_object_description") Тогда
			Возврат "Field 'target_object_description': Field required";
		КонецЕсли;
		
		ДетальОшибки = ПроверитьОписаниеОбъекта(АргументыЗапроса.target_object_description);
		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;
		
		Если НЕ АргументыЗапроса.Свойство("search_scope") Тогда
			Возврат "Field 'search_scope': Field required";
		КонецЕсли;
		
		Области = НормализоватьМассивПараметра(АргументыЗапроса.search_scope);
		АргументыЗапроса.Вставить("search_scope", Области);
		Если ТипЗнч(Области) <> Тип("Массив") И ТипЗнч(Области) <> Тип("ФиксированныйМассив") Тогда
			Возврат "search_scope must be a list";
		КонецЕсли;
		Если Области.Количество() = 0 Тогда
			Возврат "search_scope must contain at least one element";
		КонецЕсли;
		
		ДопустимыеОбласти = Новый Соответствие;
		ДопустимыеОбласти.Вставить("documents", Истина);
		ДопустимыеОбласти.Вставить("catalogs", Истина);
		ДопустимыеОбласти.Вставить("information_registers", Истина);
		ДопустимыеОбласти.Вставить("accumulation_registers", Истина);
		ДопустимыеОбласти.Вставить("accounting_registers", Истина);
		ДопустимыеОбласти.Вставить("calculation_registers", Истина);
		
		НеверныеОбласти = Новый Массив;
		ОчищенныеОбласти = Новый Массив;
		Для Каждого Обл Из Области Цикл
			Если ТипЗнч(Обл) <> Тип("Строка") Тогда
				Возврат "Each scope must be a string, got: " + ТипЗнч(Обл).Имя();
			КонецЕсли;
			ОблСтр = СокрЛП(Обл);
			Если ДопустимыеОбласти.Получить(ОблСтр) = Неопределено Тогда
				НеверныеОбласти.Добавить(ОблСтр);
			Иначе
				ОчищенныеОбласти.Добавить(ОблСтр);
			КонецЕсли;
		КонецЦикла;
		
		Если НеверныеОбласти.Количество() > 0 Тогда
			Возврат "Invalid search scope(s): " + ЗаписатьJSON2(НеверныеОбласти)
				+ ". Valid scopes are: ['documents', 'catalogs', 'information_registers', 'accumulation_registers', 'accounting_registers', 'calculation_registers']";
		КонецЕсли;
		
		АргументыЗапроса.Вставить("search_scope", ОчищенныеОбласти);
		
		// meta_filter: Optional[dict]
		Если АргументыЗапроса.Свойство("meta_filter") Тогда
			ФильтрМета = АргументыЗапроса.meta_filter;
			Если ФильтрМета = Null Или ФильтрМета = Неопределено Тогда
				УдалитьПолеЕслиЕсть(АргументыЗапроса, "meta_filter");
			ИначеЕсли ТипЗнч(ФильтрМета) <> Тип("Структура") Тогда
				Возврат "Input should be a valid dictionary";
			Иначе
				// names
				Если ФильтрМета.Свойство("names") Тогда
					Имена = НормализоватьМассивПараметра(ФильтрМета.names);
					Если Имена = Null Или Имена = Неопределено Тогда
						УдалитьПолеЕслиЕсть(ФильтрМета, "names");
					ИначеЕсли ТипЗнч(Имена) <> Тип("Массив") И ТипЗнч(Имена) <> Тип("ФиксированныйМассив") Тогда
						Возврат "names must be a list";
					ИначеЕсли Имена.Количество() = 0 Тогда
						УдалитьПолеЕслиЕсть(ФильтрМета, "names");
					Иначе
						ДопустимыеПрефиксы = Новый Соответствие;
						СписокПрефиксов = Новый Массив;
						СписокПрефиксов.Добавить("Документ");
						СписокПрефиксов.Добавить("Справочник");
						СписокПрефиксов.Добавить("РегистрСведений");
						СписокПрефиксов.Добавить("РегистрНакопления");
						СписокПрефиксов.Добавить("РегистрБухгалтерии");
						СписокПрефиксов.Добавить("РегистрРасчета");
						СписокПрефиксов.Добавить("ПланВидовХарактеристик");
						СписокПрефиксов.Добавить("ПланСчетов");
						СписокПрефиксов.Добавить("ПланВидовРасчета");
						СписокПрефиксов.Добавить("ПланОбмена");
						СписокПрефиксов.Добавить("БизнесПроцесс");
						СписокПрефиксов.Добавить("Задача");
						СписокПрефиксов.Добавить("Константа");
						СписокПрефиксов.Добавить("Перечисление");
						СписокПрефиксов.Добавить("Отчет");
						СписокПрефиксов.Добавить("Обработка");
						СписокПрефиксов.Добавить("РегламентноеЗадание");
						СписокПрефиксов.Добавить("ПараметрыСеанса");
						Для Каждого Преф Из СписокПрефиксов Цикл
							ДопустимыеПрефиксы.Вставить(Преф, Истина);
						КонецЦикла;
						
						ВалидныеИмена = Новый Массив;
						Для Каждого ИмяМета Из Имена Цикл
							Если ТипЗнч(ИмяМета) <> Тип("Строка") Тогда
								Возврат "Each name must be a string, got: " + ТипЗнч(ИмяМета).Имя();
							КонецЕсли;
							ИмяМетаСтр = СокрЛП(ИмяМета);
							// формат \w+\.\w+ -> упрощённо: ровно одна точка и обе части непустые
							ПозТочка = Найти(ИмяМетаСтр, ".");
							// В 1С 8.2 функция Найти() не поддерживает параметр "начальная позиция",
							// поэтому проверяем наличие второй точки через подстроку после первой.
							Если ПозТочка = 0 Или Найти(Сред(ИмяМетаСтр, ПозТочка + 1), ".") > 0 Тогда
								Возврат "Invalid name format: '" + ИмяМетаСтр + "'. Expected format: 'ТипМетаданных.ИмяОбъекта' (e.g., 'Документ.РеализацияТоваровУслуг')";
							КонецЕсли;
							ПрефиксМета = Лев(ИмяМетаСтр, ПозТочка - 1);
							СуффиксМета = Сред(ИмяМетаСтр, ПозТочка + 1);
							Если ПустаяСтрока(ПрефиксМета) Или ПустаяСтрока(СуффиксМета) Тогда
								Возврат "Invalid name format: '" + ИмяМетаСтр + "'. Expected format: 'ТипМетаданных.ИмяОбъекта' (e.g., 'Документ.РеализацияТоваровУслуг')";
							КонецЕсли;
							Если ДопустимыеПрефиксы.Получить(ПрефиксМета) = Неопределено Тогда
								Возврат "Invalid metadata type prefix: '" + ПрефиксМета + "'. Valid prefixes are: ['Документ', 'Справочник', 'РегистрСведений', 'РегистрНакопления', 'РегистрБухгалтерии', 'РегистрРасчета', 'ПланВидовХарактеристик', 'ПланСчетов', 'ПланВидовРасчета', 'ПланОбмена', 'БизнесПроцесс', 'Задача', 'Константа', 'Перечисление', 'Отчет', 'Обработка', 'РегламентноеЗадание', 'ПараметрыСеанса']";
							КонецЕсли;
							ВалидныеИмена.Добавить(ИмяМетаСтр);
						КонецЦикла;
						Если ВалидныеИмена.Количество() = 0 Тогда
							УдалитьПолеЕслиЕсть(ФильтрМета, "names");
						Иначе
							ФильтрМета.Вставить("names", ВалидныеИмена);
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
				
				// name_mask
				Если ФильтрМета.Свойство("name_mask") Тогда
					МаскаМета = ФильтрМета.name_mask;
					Если МаскаМета = Null Или МаскаМета = Неопределено Или МаскаМета = "" Тогда
						УдалитьПолеЕслиЕсть(ФильтрМета, "name_mask");
					Иначе
						МаскаМета = СокрЛП(Строка(МаскаМета));
						Если ПустаяСтрока(МаскаМета) Тогда
							Возврат "name_mask cannot be empty or whitespace only";
						КонецЕсли;
						ФильтрМета.Вставить("name_mask", МаскаМета);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
		
	ИначеЕсли ИмяИнструмента = "get_access_rights" Тогда
		
		ДетальОшибки = ПроверитьИНормализоватьНепустуюСтроку(АргументыЗапроса, "metadata_object", "metadata_object cannot be empty");
		Если ДетальОшибки <> Неопределено Тогда
			Возврат ДетальОшибки;
		КонецЕсли;
		
		Если Найти(АргументыЗапроса.metadata_object, ".") = 0 Тогда
			Возврат "metadata_object must be in format 'ТипМетаданных.ИмяОбъекта' (e.g., 'Справочник.Контрагенты'), got: '" + АргументыЗапроса.metadata_object + "'";
		КонецЕсли;
		
		// user_name: Optional[str] stripped, whitespace-only -> None
		Если АргументыЗапроса <> Неопределено И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
			Если АргументыЗапроса.Свойство("user_name") Тогда
				Пользователь = АргументыЗапроса.user_name;
				Если Пользователь = Null Или Пользователь = Неопределено Или Пользователь = "" Тогда
					УдалитьПолеЕслиЕсть(АргументыЗапроса, "user_name");
				Иначе
					Пользователь = СокрЛП(Строка(Пользователь));
					Если ПустаяСтрока(Пользователь) Тогда
						УдалитьПолеЕслиЕсть(АргументыЗапроса, "user_name");
					Иначе
						АргументыЗапроса.Вставить("user_name", Пользователь);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// rights_filter, roles_filter: Optional[List[str]]
		ИменаСписков = Новый Массив;
		ИменаСписков.Добавить("rights_filter");
		ИменаСписков.Добавить("roles_filter");
		Для Каждого ИмяСписка Из ИменаСписков Цикл
			Если АргументыЗапроса <> Неопределено И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
				Если АргументыЗапроса.Свойство(ИмяСписка) Тогда
					Список = НормализоватьМассивПараметра(АргументыЗапроса[ИмяСписка]);
					Если Список = Null Или Список = Неопределено Тогда
						УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяСписка);
					ИначеЕсли ТипЗнч(Список) <> Тип("Массив") И ТипЗнч(Список) <> Тип("ФиксированныйМассив") Тогда
						Возврат ИмяСписка + " must be a list";
					ИначеЕсли Список.Количество() = 0 Тогда
						УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяСписка);
					Иначе
						ОчищенныйСписок = Новый Массив;
						Для Каждого ЭлементСписка Из Список Цикл
							Если ТипЗнч(ЭлементСписка) <> Тип("Строка") Тогда
								Возврат "Each " + ?(ИмяСписка="rights_filter","right","role") + " must be a string, got: " + ТипЗнч(ЭлементСписка).Имя();
							КонецЕсли;
							ЭлС = СокрЛП(ЭлементСписка);
							Если ПустаяСтрока(ЭлС) Тогда
								Продолжить;
							КонецЕсли;
							ОчищенныйСписок.Добавить(ЭлС);
						КонецЦикла;
						Если ОчищенныйСписок.Количество() = 0 Тогда
							УдалитьПолеЕслиЕсть(АргументыЗапроса, ИмяСписка);
						Иначе
							АргументыЗапроса.Вставить(ИмяСписка, ОчищенныйСписок);
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;

		Возврат Неопределено;

	ИначеЕсли ИмяИнструмента = "get_bsl_syntax_help" Тогда
		Если АргументыЗапроса = Неопределено ИЛИ ТипЗнч(АргументыЗапроса) <> Тип("Структура") Тогда
			Возврат "'params' must be an object";
		КонецЕсли;
		Если АргументыЗапроса.Свойство("keywords") Тогда
			АргументыЗапроса.Вставить("keywords", НормализоватьМассивПараметра(АргументыЗапроса.keywords));
		КонецЕсли;
		Если НЕ АргументыЗапроса.Свойство("keywords")
			ИЛИ НЕ (ТипЗнч(АргументыЗапроса.keywords) = Тип("Массив") ИЛИ ТипЗнч(АргументыЗапроса.keywords) = Тип("ФиксированныйМассив"))
			ИЛИ АргументыЗапроса.keywords.Количество() = 0 Тогда
			Возврат "'keywords' must be a non-empty array of strings";
		КонецЕсли;
		ОчищенныеКлючи = Новый Массив;
		Для Каждого КС Из АргументыЗапроса.keywords Цикл
			Если ТипЗнч(КС) = Тип("Строка") И ЗначениеЗаполнено(СокрЛП(КС)) Тогда
				ОчищенныеКлючи.Добавить(СокрЛП(КС));
			КонецЕсли;
		КонецЦикла;
		Если ОчищенныеКлючи.Количество() = 0 Тогда
			Возврат "'keywords' must contain at least one non-empty string";
		КонецЕсли;
		АргументыЗапроса.Вставить("keywords", ОчищенныеКлючи);
		Если НЕ АргументыЗапроса.Свойство("match") ИЛИ НЕ ЗначениеЗаполнено(АргументыЗапроса.match) Тогда
			АргументыЗапроса.Вставить("match", "all");
		ИначеЕсли АргументыЗапроса.match <> "all" И АргументыЗапроса.match <> "any" Тогда
			Возврат "'match' must be 'all' or 'any'";
		КонецЕсли;
		Возврат Неопределено;

	ИначеЕсли ИмяИнструмента = "submit_for_deanonymization" Тогда
		// text обязателен и должен быть строкой (пустая строка допустима)
		Если НЕ АргументыЗапроса.Свойство("text") ИЛИ ТипЗнч(АргументыЗапроса.text) <> Тип("Строка") Тогда
			Возврат "Field 'text': Input should be a valid string";
		КонецЕсли;
		Возврат Неопределено; // валидация прошла успешно

	ИначеЕсли ИмяИнструмента = "get_screenshot" Тогда
		Если АргументыЗапроса <> Неопределено
			И ТипЗнч(АргументыЗапроса) = Тип("Структура") Тогда
			// form_name опциональный: проверяем только если передан
			Если АргументыЗапроса.Свойство("form_name") Тогда
				Если ТипЗнч(АргументыЗапроса.form_name) <> Тип("Строка")
					ИЛИ НЕ ЗначениеЗаполнено(СокрЛП(АргументыЗапроса.form_name)) Тогда
					Возврат "form_name cannot be empty or whitespace only";
				КонецЕсли;
				АргументыЗапроса.Вставить("form_name", СокрЛП(АргументыЗапроса.form_name));
			КонецЕсли;
			// scale_percent: нормализация и проверка диапазона (как в REST-валидаторе)
			ДетальОшибки = ПроверитьИНормализоватьЦелоеПоле(АргументыЗапроса, "scale_percent", 100, 10, 200);
			Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
			// Нормализация show_grid: полный паритет с Pydantic bool
			Если АргументыЗапроса.Свойство("show_grid") Тогда
				Зн = АргументыЗапроса.show_grid;
				Если ТипЗнч(Зн) = Тип("Булево") Тогда
					// уже Булево, ок
				ИначеЕсли ТипЗнч(Зн) = Тип("Число") Тогда
					// Pydantic принимает только 0 и 1; 2, -1, 42 → ValidationError
					Если Зн = 0 Тогда
						АргументыЗапроса.Вставить("show_grid", Ложь);
					ИначеЕсли Зн = 1 Тогда
						АргументыЗапроса.Вставить("show_grid", Истина);
					Иначе
						Возврат "Field 'show_grid': Input should be a valid boolean";
					КонецЕсли;
				ИначеЕсли ТипЗнч(Зн) = Тип("Строка") Тогда
					Сн = НРег(СокрЛП(Зн));
					Если Сн = "true" ИЛИ Сн = "1" ИЛИ Сн = "yes" ИЛИ Сн = "on" Тогда
						АргументыЗапроса.Вставить("show_grid", Истина);
					ИначеЕсли Сн = "false" ИЛИ Сн = "0" ИЛИ Сн = "no" ИЛИ Сн = "off" Тогда
						АргументыЗапроса.Вставить("show_grid", Ложь);
					Иначе
						Возврат "Field 'show_grid': Input should be a valid boolean";
					КонецЕсли;
				Иначе
					Возврат "Field 'show_grid': Input should be a valid boolean";
				КонецЕсли;
			КонецЕсли;
			// Валидация region
			Если АргументыЗапроса.Свойство("region") Тогда
				РегЗн = АргументыЗапроса.region;
				Если РегЗн <> Null И РегЗн <> Неопределено Тогда
					Если ТипЗнч(РегЗн) <> Тип("Структура") Тогда
						Возврат "Field 'region': must be an object with fields x, y, width, height";
					КонецЕсли;
					Рег = РегЗн;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "x",      0);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "y",      0);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "width",  1);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рег, "height", 1);
					Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
				КонецЕсли;
			КонецЕсли;
			// Валидация highlight_rects
			Если АргументыЗапроса.Свойство("highlight_rects") Тогда
				ХРЗн = АргументыЗапроса.highlight_rects;
				Если ХРЗн <> Null И ХРЗн <> Неопределено Тогда
					Если ТипЗнч(ХРЗн) <> Тип("Массив") Тогда
						Возврат "Field 'highlight_rects': must be an array";
					КонецЕсли;
					Если ХРЗн.Количество() > 20 Тогда
						Возврат "highlight_rects must not exceed 20 items";
					КонецЕсли;
					Для ИндексРект = 0 По ХРЗн.Количество() - 1 Цикл
						Рект = ХРЗн[ИндексРект];
						НомерРект = Формат(ИндексРект + 1, "ЧГ=");
						Если ТипЗнч(Рект) <> Тип("Структура") Тогда
							Возврат "Field 'highlight_rects[" + НомерРект + "]': must be an object with fields x, y, width, height";
						КонецЕсли;
						ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "x",      0);
						Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
						ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "y",      0);
						Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
						ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "width",  1);
						Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
						ДетальОшибки = ПроверитьНормализоватьПолеРегиона(Рект, "height", 1);
						Если ДетальОшибки <> Неопределено Тогда Возврат ДетальОшибки КонецЕсли;
					КонецЦикла;
					// Проверка пересечений
					Для ИндА = 0 По ХРЗн.Количество() - 1 Цикл
						А = ХРЗн[ИндА];
						Для ИндБ = ИндА + 1 По ХРЗн.Количество() - 1 Цикл
							Б = ХРЗн[ИндБ];
							Если НЕ (А.x + А.width <= Б.x ИЛИ Б.x + Б.width <= А.x
								ИЛИ А.y + А.height <= Б.y ИЛИ Б.y + Б.height <= А.y) Тогда
								Возврат "Rectangle " + Формат(ИндА + 1, "ЧГ=") + " and Rectangle " + Формат(ИндБ + 1, "ЧГ=") + " overlap";
							КонецЕсли;
						КонецЦикла;
					КонецЦикла;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;

	Возврат Неопределено;

КонецФункции

&НаКлиенте
Функция ОбработатьToolsCall(ДанныеЗапроса, ТелоJSON, Ид, ИдПотокаSSE = "")

	// Извлечение params.name и params.arguments
	ПараметрыЗапроса = Неопределено;
	ТелоJSON.Свойство("params", ПараметрыЗапроса);
	Если ПараметрыЗапроса = Неопределено ИЛИ ТипЗнч(ПараметрыЗапроса) <> Тип("Структура") Тогда
		Возврат СформироватьToolsCallОшибку("Missing params");
	КонецЕсли;

	ИмяИнструмента = "";
	ПараметрыЗапроса.Свойство("name", ИмяИнструмента);
	Если ПустаяСтрока(ИмяИнструмента) Тогда
		Возврат СформироватьToolsCallОшибку("Missing tool name");
	КонецЕсли;

	Аргументы = Новый Структура;
	ПараметрыЗапроса.Свойство("arguments", Аргументы);
	Если ТипЗнч(Аргументы) <> Тип("Структура") Тогда
		Аргументы = Новый Структура;
	КонецЕсли;

	// Формируем команду (тот же формат что для proxy)
	Команда = Новый Структура;
	Команда.Вставить("id", Строка(Ид));
	Команда.Вставить("tool", ИмяИнструмента);
	Команда.Вставить("params", Аргументы);

	ДобавитьВЛог("Получена команда: " + ИмяИнструмента + Символы.ПС + ПолучитьТекстКоманды(Команда));

	// restart/close обрабатываются до деанонимизации — отложенный ответ
	Если ИмяИнструмента = "restart_1c_session" Тогда
		ВыполнитьПерезапускСессии("", ДанныеЗапроса, Ид, ИдПотокаSSE);
		Возврат Неопределено;
	КонецЕсли;
	Если ИмяИнструмента = "close_1c_session" Тогда
		ВыполнитьЗакрытиеСессии("", ДанныеЗапроса, Ид, ИдПотокаSSE);
		Возврат Неопределено;
	КонецЕсли;

	// Деанонимизация входных параметров (встроенный сервер, точная анонимизация)
	Если ИспользоватьТочнуюАнонимизацию Тогда
		ДеАнонимизироватьПараметрыКоманды(Команда);
		Аргументы = Команда.params;
	КонецЕсли;

	// execute_query: дефолт/валидация limit как в прокси (default=100, ge=1, le=1000),
	// но только для встроенного HTTP-сервера (tools/call / REST).
	ДетальОшибки = ПроверитьИНормализоватьАргументыИнструмента(ИмяИнструмента, Аргументы);
	Если ДетальОшибки <> Неопределено Тогда
		СообщениеОшибки = "Ошибка валидации параметров: " + ДетальОшибки
			+ " / Parameter validation failed: " + ДетальОшибки;
		Возврат СформироватьToolsCallРезультат(
			Новый Структура("success, error", Ложь, СообщениеОшибки)
		);
	КонецЕсли;

	// Проверка опасных операций для execute_code
	Если ИмяИнструмента = "execute_code" Тогда
		Код = "";
		Аргументы.Свойство("code", Код);
		МассивОпасныхСлов = ПроверитьОпасныеСлова(Код);

		Если МассивОпасныхСлов.Количество() > 0 Тогда
			Если ВсеОперацииАвтоРазрешены(МассивОпасныхСлов) Тогда
				// Автоматически разрешено
				ДобавитьВЛог("Операции автоматически разрешены: " + СоединитьМассив(МассивОпасныхСлов, ", "));
			ИначеЕсли РазрешатьОпасныеОперацииСПодтверждением Тогда
				// Нужен диалог — async
				ОпасныеСлова = СоединитьМассив(МассивОпасныхСлов, ", ");
				ОжидающиеПодтверждения.Вставить(ДанныеЗапроса.id,
					Новый Структура(
						"ДанныеЗапроса, Команда, Ид, ОпасныеКлючевыеСлова, ОпасныеКлючевыеСловаТекст, ИдПотокаSSE",
						ДанныеЗапроса,
						Команда,
						Ид,
						МассивОпасныхСлов,
						ОпасныеСлова,
						ИдПотокаSSE
					));

				// Открываем ту же форму подтверждения, что и в прокси-режиме, чтобы UI/логика были одинаковыми.
				ПараметрыФормы = Новый Структура;
				ПараметрыФормы.Вставить("ОпасныеСлова", ОпасныеСлова);
				ПараметрыФормы.Вставить("Код", Код);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ОбработатьРезультатПодтвержденияВстроенный",
					ЭтаФорма,
					Новый Структура("RequestId", ДанныеЗапроса.id));
				
				ОткрытьФорму(
					"ВнешняяОбработка.MCPToolkit.Форма.ФормаПодтверждения",
					ПараметрыФормы,
					ЭтаФорма,
					,
					,
					,
					ОписаниеОповещения,
					РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс
				);

				Возврат Неопределено; // async
			Иначе
				// Подтверждение опасных операций запрещено — блок (как в прокси block mode)
				КлючевыеСловаТекст = СоединитьМассив(МассивОпасныхСлов, ", ");
				ТекстОшибкиБлок = "Операция запрещена: код содержит опасные ключевые слова: " + КлючевыеСловаТекст
					+ " / Operation not allowed: code contains dangerous keywords: " + КлючевыеСловаТекст;
				ОшибкаБлок = Новый Структура("success, error, dangerous_keywords",
					Ложь, ТекстОшибкиБлок, МассивОпасныхСлов);
				Возврат СформироватьToolsCallРезультат(ОшибкаБлок);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	// Выполнение
	Попытка
		Результат = ВыполнитьКомандуСУчетомКонтекста(Команда);
	Исключение
		Возврат СформироватьToolsCallОшибку("Error: " + ОписаниеОшибки());
	КонецПопытки;
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Результат.Вставить("_cmd_arguments_json", """tool"": """ + ИмяИнструмента + """" + Символы.ПС + ЗаписатьJSON2(Аргументы));
	КонецЕсли;

	ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
	ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Команда);
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И НуженLineageДляРезультата(ИмяИнструмента, ТекстЗапросаLineage, Результат) Тогда
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат, ТекстЗапроса",
			"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат, ТекстЗапросаLineage);
		НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "execute_query" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		Результат = ПрименитьТочнуюPolicyКРезультатуExecuteQuery(Результат, Ложь);
		РегексКонтекст = Новый Структура;
		РегексКонтекст.Вставить("Результат",            Результат);
		РегексКонтекст.Вставить("ВключённыеПравилаJSON", "");
		РегексКонтекст.Вставить("ТекстыJSON",            "");
		РегексКонтекст.Вставить("МаппингИндексов",       Неопределено);
		РегексКонтекст.Вставить("ЕстьТекстыДляRegex",    Ложь);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
			"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		АнонимизироватьРезультатВстроенный(Результат, РегексКонтекст);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_object_by_link" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		Результат = ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат);
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоОбъект",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
			"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "find_references_to_object" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоFindReferences",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
			"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_event_log" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоGetEventLog",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатGetEventLogВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
			"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_access_rights" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		АнонимизироватьРезультатGetAccessRights(Результат);
		ОбновитьТаблицуМаппинговВстроенный();
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "execute_code" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		УжеАнонимизирован = Ложь;
		Если ТипЗнч(Результат) = Тип("Структура") Тогда
			ВремАнон = Неопределено;
			УжеАнонимизирован = Результат.Свойство("_original_data_json", ВремАнон) И ЗначениеЗаполнено(ВремАнон);
		КонецЕсли;
		Если НЕ УжеАнонимизирован Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			РегексКонтекст = Новый Структура(
				"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст);
			КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
				"mcp", ДанныеЗапроса, Ид, ИдПотокаSSE, Результат);
			РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
			ЗапуститьRegexФазу(РегексКонтекст);
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	// Точка перехвата A: синхронный путь
	Если ПроверитьПодтверждениеАнонимизации(Результат, ДанныеЗапроса, Ид, ИдПотокаSSE) Тогда
		Возврат Неопределено;
	КонецЕсли;
	Возврат СформироватьToolsCallРезультат(Результат, ДанныеЗапроса, Ид, ИдПотокаSSE);

КонецФункции

&НаКлиенте
Процедура ОбработатьРезультатПодтвержденияВстроенный(РезультатВопроса, ДополнительныеПараметры) Экспорт

	RequestId = ДополнительныеПараметры.RequestId;
	Контекст = ОжидающиеПодтверждения.Получить(RequestId);
	ОжидающиеПодтверждения.Удалить(RequestId);

	Если Контекст = Неопределено Тогда Возврат КонецЕсли;

	// Безопасное извлечение режима и идентификаторов:
	// REST-контекст не содержит полей Ид и ИдПотокаSSE — прямой доступ к Контекст.Ид даст crash
	РежимКонтекста = "";
	Контекст.Свойство("Режим", РежимКонтекста);
	Если НЕ ЗначениеЗаполнено(РежимКонтекста) Тогда
		РежимКонтекста = "mcp";
	КонецЕсли;
	ИдКонтекст = "";
	Контекст.Свойство("Ид", ИдКонтекст);

	Разрешено = Ложь;
	Если ТипЗнч(РезультатВопроса) = Тип("Булево") Тогда
		Разрешено = РезультатВопроса;
	ИначеЕсли РезультатВопроса = КодВозвратаДиалога.Да Тогда
		Разрешено = Истина;
	КонецЕсли;

	ИдПотокаSSEКонтекст = "";
	Контекст.Свойство("ИдПотокаSSE", ИдПотокаSSEКонтекст);

	Если Разрешено Тогда
		ДобавитьВЛог("Пользователь разрешил выполнение опасной операции");
		Попытка
			Результат = ВыполнитьКомандуСУчетомКонтекста(Контекст.Команда);
		Исключение
			Результат = Новый Структура("success, error", Ложь, "Error: " + ОписаниеОшибки());
		КонецПопытки;
		Если ТипЗнч(Результат) = Тип("Структура") И ТипЗнч(Контекст.Команда) = Тип("Структура") Тогда
			Результат.Вставить("_cmd_arguments_json", """tool"": """ + Контекст.Команда.tool + """" + Символы.ПС + ЗаписатьJSON2(Контекст.Команда.params));
		КонецЕсли;
		ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
		ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Контекст.Команда);
		Если ИспользоватьАсинхронноеПодключениеКомпонент
			И НуженLineageДляРезультата(Контекст.Команда.tool, ТекстЗапросаLineage, Результат) Тогда
			КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат, ТекстЗапроса",
				РежимКонтекста, Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст, Результат, ТекстЗапросаLineage);
			НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
			Возврат;
		КонецЕсли;
		Если ИспользоватьАсинхронноеПодключениеКомпонент
			И Контекст.Команда.tool = "get_object_by_link" И ИспользоватьТочнуюАнонимизацию
			И РежимПодключения <> 0 Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			Результат = ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат);
			РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоОбъект",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст);
			КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
				РежимКонтекста, Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст, Результат);
			РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
			ЗапуститьRegexФазу(РегексКонтекст);
			Возврат;
		КонецЕсли;
		Если ИспользоватьАсинхронноеПодключениеКомпонент
			И Контекст.Команда.tool = "find_references_to_object" И ИспользоватьТочнуюАнонимизацию
			И РежимПодключения <> 0 Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоFindReferences",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст);
			КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
				РежимКонтекста, Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст, Результат);
			РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
			ЗапуститьRegexФазу(РегексКонтекст);
			Возврат;
		КонецЕсли;
		Если ИспользоватьАсинхронноеПодключениеКомпонент
			И Контекст.Команда.tool = "execute_code" И ИспользоватьТочнуюАнонимизацию
			И РежимПодключения <> 0 Тогда
			УжеАнонимизирован = Ложь;
			Если ТипЗнч(Результат) = Тип("Структура") Тогда
				ВремАнон = Неопределено;
				УжеАнонимизирован = Результат.Свойство("_original_data_json", ВремАнон) И ЗначениеЗаполнено(ВремАнон);
			КонецЕсли;
			Если НЕ УжеАнонимизирован Тогда
				Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
					Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
					Результат.Вставить("_original_data_anon_marker", Истина);
				КонецЕсли;
				РегексКонтекст = Новый Структура(
					"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
					Результат, "", "", Неопределено, Ложь, Истина);
				АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст);
				КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Ид, ИдПотока, Результат",
					РежимКонтекста, Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст, Результат);
				РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
				ЗапуститьRegexФазу(РегексКонтекст);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		// Точка перехвата B: после dangerous-op approval
		Если ПроверитьПодтверждениеАнонимизации(Результат, Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст, РежимКонтекста) Тогда
			Возврат;
		КонецЕсли;
		// REST approve: HTTP-ответ вместо SSE (с поддержкой async Toon)
		Если РежимКонтекста = "rest" Тогда
			Результат = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(Результат);
			Если ИспользоватьАсинхронноеПодключениеКомпонент И КомпонентаТун <> Неопределено
				И ФорматОтветаВстроенный = 1 И ТипЗнч(Результат) = Тип("Структура")
				И Результат.Свойство("success") И Результат.success И Результат.Свойство("data") Тогда
				КомпонентаТун.НачатьВызовJsonToToon(
					Новый ОписаниеОповещения("ПослеJsonToToonREST", ЭтаФорма,
						Новый Структура("ДанныеЗапроса, Результат", Контекст.ДанныеЗапроса, Результат)),
					ЗаписатьJSON2(Результат.data));
				Возврат;
			КонецЕсли;
			ПрименитьТунКонвертацию(Результат);
			КомпонентаSendResponse(Контекст.ДанныеЗапроса.id, 200,
				ЗаголовкиJSON(), ЗаписатьJSON2(Результат));
			Возврат;
		КонецЕсли;
		ТулКолРез = СформироватьToolsCallРезультат(Результат,
			Контекст.ДанныеЗапроса, ИдКонтекст, ИдПотокаSSEКонтекст);
		Если ТулКолРез = Неопределено Тогда Возврат КонецЕсли; // async Toon-конвертация сама отправит
		ОтветJSON = СформироватьJSONRPCОтвет(ИдКонтекст, ТулКолРез);
	Иначе
		ДобавитьВЛог("Пользователь отклонил выполнение опасной операции");
		// Возвращаем текст ошибки как в прокси-режиме (RU+EN + список ключевых слов)
		ОпасныеСловаТекст = "";
		Если Контекст.Свойство("ОпасныеКлючевыеСловаТекст") Тогда
			ОпасныеСловаТекст = Контекст.ОпасныеКлючевыеСловаТекст;
		ИначеЕсли Контекст.Свойство("ОпасныеКлючевыеСлова") Тогда
			ОпасныеСловаТекст = СоединитьМассив(Контекст.ОпасныеКлючевыеСлова, ", ");
		КонецЕсли;
		ТекстОшибкиОтклонено = "Операция отклонена пользователем. Код содержит опасные ключевые слова: " + ОпасныеСловаТекст
			+ " / Operation rejected by user. Code contains dangerous keywords: " + ОпасныеСловаТекст;
		ОшибкаОтклонено = Новый Структура("success, error", Ложь, ТекстОшибкиОтклонено);
		// REST reject: HTTP-ответ напрямую (Результат здесь не существует)
		Если РежимКонтекста = "rest" Тогда
			КомпонентаSendResponse(Контекст.ДанныеЗапроса.id, 200,
				ЗаголовкиJSON(), ЗаписатьJSON2(ОшибкаОтклонено));
			Возврат;
		КонецЕсли;
		// success=Ложь → Toon async не нужен, параметры не передаём
		ОтветJSON = СформироватьJSONRPCОтвет(ИдКонтекст,
			СформироватьToolsCallРезультат(ОшибкаОтклонено));
	КонецЕсли;

	// Legacy SSE: ответ через GET-поток, не POST (который уже вернул 202)
	Если ЗначениеЗаполнено(ИдПотокаSSEКонтекст) Тогда
		КомпонентаSendSSEEvent(ИдПотокаSSEКонтекст, ОтветJSON, "{}", "message");
	Иначе
		ОтправитьSSEОтвет(Контекст.ДанныеЗапроса, ОтветJSON); // Streamable HTTP — не трогаем
	КонецЕсли;

КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ДЕТЕКТ ОПАСНЫХ ОПЕРАЦИЙ (execute_code) — токенизация как в Python-прокси
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиенте
Функция НормализоватьКодДляСканированияОпасныхСлов(Текст)
	
	// Минимальная нормализация как в прокси: убираем zero-width символы, которые часто используют для обфускации.
	// Полная Unicode NFKC нормализация в 1С отсутствует, поэтому делаем только этот слой.
	
	Если Текст = Неопределено Или Текст = Null Тогда
		Возврат "";
	КонецЕсли;
	
	Текст = Строка(Текст);
	
	// Zero-width characters: U+200B, U+200C, U+200D, U+FEFF
	Текст = СтрЗаменить(Текст, Символ(8203), "");
	Текст = СтрЗаменить(Текст, Символ(8204), "");
	Текст = СтрЗаменить(Текст, Символ(8205), "");
	Текст = СтрЗаменить(Текст, Символ(65279), "");
	
	Возврат Текст;
	
КонецФункции

&НаКлиенте
Функция ЭтоПробельныйСимволДляСканирования(Символ)
	
	Если Символ = " " Тогда
		Возврат Истина;
	КонецЕсли;
	
	Если Символ = Символы.Таб Или Символ = Символы.ПС Или Символ = Символы.ВК Тогда
		Возврат Истина;
	КонецЕсли;
	
	// NBSP
	Если Символ = Символ(160) Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

&НаКлиенте
Функция ЭтоНачалоИдентификатораДляСканирования(Символ)
	
	Если Символ = "_" Тогда
		Возврат Истина;
	КонецЕсли;
	
	// Для кириллицы/латиницы: буква имеет различие регистра (ВРег <> НРег).
	// Для цифр и знаков препинания это обычно не так.
	Попытка
		Возврат ВРег(Символ) <> НРег(Символ);
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

&НаКлиенте
Функция ЭтоЧастьИдентификатораДляСканирования(Символ)
	
	Если Символ = "_" Тогда
		Возврат Истина;
	КонецЕсли;
	
	Если Символ >= "0" И Символ <= "9" Тогда
		Возврат Истина;
	КонецЕсли;
	
	Попытка
		Возврат ВРег(Символ) <> НРег(Символ);
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

&НаКлиенте
Функция СобратьВызванныеИдентификаторы(КодТекст)
	
	// Возвращает Соответствие, где ключ = НРег(IDENT), значение = Истина.
	// Логика как в прокси: IDENT считается "вызовом", если далее встречается '(',
	// при этом пробелы/переносы/комментарии/строки игнорируются.
	
	Результат = Новый Соответствие;
	
	Если КодТекст = Неопределено Или КодТекст = Null Тогда
		Возврат Результат;
	КонецЕсли;
	
	КодТекст = Строка(КодТекст);
	Длина = СтрДлина(КодТекст);
	Позиция = 1;
	ОжидаемыйИдентификатор = "";
	
	Пока Позиция <= Длина Цикл
		
		Символ = Сред(КодТекст, Позиция, 1);
		
		// whitespace
		Если ЭтоПробельныйСимволДляСканирования(Символ) Тогда
			Позиция = Позиция + 1;
			Продолжить;
		КонецЕсли;
		
		// comments: //... or /*...*/
		Если Символ = "/" И Позиция < Длина Тогда
			Следующий = Сред(КодТекст, Позиция + 1, 1);
			
			Если Следующий = "/" Тогда
				// line comment
				Позиция = Позиция + 2;
				Пока Позиция <= Длина Цикл
					Символ = Сред(КодТекст, Позиция, 1);
					Если Символ = Символы.ПС Или Символ = Символы.ВК Тогда
						Прервать;
					КонецЕсли;
					Позиция = Позиция + 1;
				КонецЦикла;
				Продолжить;
				
			ИначеЕсли Следующий = "*" Тогда
				// block comment
				Позиция = Позиция + 2;
				Пока Позиция <= Длина - 1 Цикл
					Если Сред(КодТекст, Позиция, 1) = "*" И Сред(КодТекст, Позиция + 1, 1) = "/" Тогда
						Позиция = Позиция + 2;
						Прервать;
					КонецЕсли;
					Позиция = Позиция + 1;
				КонецЦикла;
				Продолжить;
			КонецЕсли;
		КонецЕсли;
		
		// 1C string literal: "text", escaped quote is doubled ""
		Если Символ = """" Тогда
			Позиция = Позиция + 1;
			Пока Позиция <= Длина Цикл
				Если Сред(КодТекст, Позиция, 1) = """" Тогда
					Если Позиция < Длина И Сред(КодТекст, Позиция + 1, 1) = """" Тогда
						Позиция = Позиция + 2;
						Продолжить;
					КонецЕсли;
					Позиция = Позиция + 1;
					Прервать;
				КонецЕсли;
				Позиция = Позиция + 1;
			КонецЦикла;
			Продолжить;
		КонецЕсли;
		
		// Single-quoted blocks (как в прокси — просто игнорируем)
		Если Символ = "'" Тогда
			Позиция = Позиция + 1;
			Пока Позиция <= Длина Цикл
				Если Сред(КодТекст, Позиция, 1) = "'" Тогда
					Если Позиция < Длина И Сред(КодТекст, Позиция + 1, 1) = "'" Тогда
						Позиция = Позиция + 2;
						Продолжить;
					КонецЕсли;
					Позиция = Позиция + 1;
					Прервать;
				КонецЕсли;
				Позиция = Позиция + 1;
			КонецЦикла;
			Продолжить;
		КонецЕсли;
		
		// call detection: IDENT + "("
		Если Символ = "(" Тогда
			Если НЕ ПустаяСтрока(ОжидаемыйИдентификатор) Тогда
				Если Результат.Получить(ОжидаемыйИдентификатор) = Неопределено Тогда
					Результат.Вставить(ОжидаемыйИдентификатор, Истина);
				КонецЕсли;
			КонецЕсли;
			ОжидаемыйИдентификатор = "";
			Позиция = Позиция + 1;
			Продолжить;
		КонецЕсли;
		
		// identifier
		Если ЭтоНачалоИдентификатораДляСканирования(Символ) Тогда
			
			НачалоИдентификатора = Позиция;
			Позиция = Позиция + 1;
			Пока Позиция <= Длина Цикл
				Символ = Сред(КодТекст, Позиция, 1);
				Если ЭтоЧастьИдентификатораДляСканирования(Символ) Тогда
					Позиция = Позиция + 1;
				Иначе
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			Идентификатор = Сред(КодТекст, НачалоИдентификатора, Позиция - НачалоИдентификатора);
			ОжидаемыйИдентификатор = НРег(Идентификатор);
			Продолжить;
			
		КонецЕсли;
		
		// Любой другой символ "разрывает" контекст IDENT -> "("
		ОжидаемыйИдентификатор = "";
		Позиция = Позиция + 1;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПроверитьОпасныеСлова(Код)

	ОпасныеСлова = Новый Массив;
	ОпасныеСлова.Добавить("Удалить");       ОпасныеСлова.Добавить("Delete");
	ОпасныеСлова.Добавить("Записать");      ОпасныеСлова.Добавить("Write");
	ОпасныеСлова.Добавить("УстановитьПривилегированныйРежим"); ОпасныеСлова.Добавить("SetPrivilegedMode");
	ОпасныеСлова.Добавить("ПодключитьВнешнююКомпоненту"); ОпасныеСлова.Добавить("AttachAddIn");
	ОпасныеСлова.Добавить("УстановитьВнешнююКомпоненту"); ОпасныеСлова.Добавить("InstallAddIn");
	ОпасныеСлова.Добавить("COMОбъект");     ОпасныеСлова.Добавить("COMObject");
	ОпасныеСлова.Добавить("УстановитьМонопольныйРежим"); ОпасныеСлова.Добавить("SetExclusiveMode");
	ОпасныеСлова.Добавить("УдалитьФайлы");  ОпасныеСлова.Добавить("DeleteFiles");
	ОпасныеСлова.Добавить("КопироватьФайл"); ОпасныеСлова.Добавить("CopyFile");
	ОпасныеСлова.Добавить("ПереместитьФайл"); ОпасныеСлова.Добавить("MoveFile");
	ОпасныеСлова.Добавить("СоздатьКаталог"); ОпасныеСлова.Добавить("CreateDirectory");

	// Встроенный режим: делаем детект как в Python-прокси:
	// - игнорируем строки и комментарии
	// - ищем ВЫЗОВ идентификатора: IDENT + LPAREN (между ними могут быть пробелы/переносы/комментарии)
	// - удаляем zero-width символы (минимальная защита от обфускации)

	КодНормализованный = НормализоватьКодДляСканированияОпасныхСлов(Код);
	ВызванныеИдентификаторы = СобратьВызванныеИдентификаторы(КодНормализованный);

	Найденные = Новый Массив;
	УжеДобавленные = Новый Соответствие;

	Для Каждого Слово Из ОпасныеСлова Цикл
		КаноническоеСлово = НормализоватьКодДляСканированияОпасныхСлов(Слово);
		КаноническоеСлово = НРег(КаноническоеСлово);

		Если ПустаяСтрока(КаноническоеСлово) Тогда
			Продолжить;
		КонецЕсли;

		Если УжеДобавленные.Получить(КаноническоеСлово) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;

		Если ВызванныеИдентификаторы.Получить(КаноническоеСлово) <> Неопределено Тогда
			Найденные.Добавить(Слово);
			УжеДобавленные.Вставить(КаноническоеСлово, Истина);
		КонецЕсли;
	КонецЦикла;

	Возврат Найденные;

КонецФункции

&НаКлиенте
Функция ХотяБыОдинФлажокВключён()
	Возврат АвтоРазрешитьЗаписать ИЛИ АвтоРазрешитьПривилегированныйРежим;
КонецФункции

&НаКлиенте
Функция СформироватьToolsCallРезультат(Результат, ДанныеЗапроса = Неопределено, Ид = Неопределено, ИдПотока = "")

	Содержимое = Новый Массив;
	Элемент = Новый Структура;
	Элемент.Вставить("type", "text");

	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("success") Тогда
		Если Результат.success Тогда
			// Специальная обработка: результат — изображение
			Если Результат.Свойство("image_base64")
				И ЗначениеЗаполнено(Результат.image_base64) Тогда
				ЭлемИзобр = Новый Структура;
				ЭлемИзобр.Вставить("type", "image");
				ЭлемИзобр.Вставить("data", Результат.image_base64);
				ЭлемИзобр.Вставить("mimeType", "image/png");
				Содержимое.Добавить(ЭлемИзобр);
				Если Результат.Свойство("window_rect") И Результат.window_rect <> Неопределено Тогда
					ЭлемРект = Новый Структура;
					ЭлемРект.Вставить("type", "text");
					ЭлемРект.Вставить("text", ЗаписатьJSON2(Результат.window_rect));
					Содержимое.Добавить(ЭлемРект);
				КонецЕсли;
				Если Результат.Свойство("grid_coords") И Результат.grid_coords <> Неопределено Тогда
					ЭлемГрид = Новый Структура;
					ЭлемГрид.Вставить("type", "text");
					ЭлемГрид.Вставить("text", ЗаписатьJSON2(Результат.grid_coords));
					Содержимое.Добавить(ЭлемГрид);
				КонецЕсли;
				Возврат Новый Структура("content", Содержимое);
			КонецЕсли;
			// Собираем полную структуру через whitelist — аналогично ОтправитьРезультатНаСервер()
			РезультатДляВывода = Новый Структура("success", Истина);
			ВсеПоля = Новый Массив;
			ВсеПоля.Добавить("data");
			ВсеПоля.Добавить("schema");
			ВсеПоля.Добавить("truncated");
			ВсеПоля.Добавить("limit");
			ВсеПоля.Добавить("returned");
			ВсеПоля.Добавить("count");
			ВсеПоля.Добавить("offset");
			ВсеПоля.Добавить("has_more");
			ВсеПоля.Добавить("next_offset");
			ВсеПоля.Добавить("last_date");
			ВсеПоля.Добавить("next_same_second_offset");
			ВсеПоля.Добавить("configuration");
			ВсеПоля.Добавить("extension");
			ВсеПоля.Добавить("received");
			Для Каждого ИмяПоля Из ВсеПоля Цикл
				Если Результат.Свойство(ИмяПоля) И Результат[ИмяПоля] <> Неопределено Тогда
					РезультатДляВывода.Вставить(ИмяПоля, Результат[ИмяПоля]);
				КонецЕсли;
			КонецЦикла;
			РезультатДляВывода = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(РезультатДляВывода);

			// Async-ветка для JsonToToon:
			Если ИспользоватьАсинхронноеПодключениеКомпонент И КомпонентаТун <> Неопределено
				И ФорматОтветаВстроенный = 1 И РезультатДляВывода.Свойство("data") Тогда
				КомпонентаТун.НачатьВызовJsonToToon(
					Новый ОписаниеОповещения("ПослеJsonToToonMCP", ЭтаФорма,
						Новый Структура("ДанныеЗапроса, Ид, РезДляВывода, ИдПотока",
							ДанныеЗапроса, Ид, РезультатДляВывода, ИдПотока)),
					ЗаписатьJSON2(РезультатДляВывода.data));
				Возврат Неопределено;
			КонецЕсли;

			ПрименитьТунКонвертацию(РезультатДляВывода);
			Элемент.Вставить("text", ЗаписатьJSON2(РезультатДляВывода));
			Содержимое.Добавить(Элемент);
		Иначе
			// Application-level ошибка: возвращаем JSON-объект без isError (как в прокси)
			РезультатОшибки = Новый Структура("success", Ложь);
			Если Результат.Свойство("error") Тогда
				РезультатОшибки.Вставить("error", Результат.error);
			КонецЕсли;
			Если Результат.Свойство("dangerous_keywords") Тогда
				РезультатОшибки.Вставить("dangerous_keywords", Результат.dangerous_keywords);
			КонецЕсли;
			Элемент.Вставить("text", ЗаписатьJSON2(РезультатОшибки));
			Содержимое.Добавить(Элемент);
		КонецЕсли;
	Иначе
		Элемент.Вставить("text", ЗаписатьJSON2(Результат));
		Содержимое.Добавить(Элемент);
	КонецЕсли;

	Возврат Новый Структура("content", Содержимое);

КонецФункции

&НаКлиенте
Процедура ПослеJsonToToonMCP(РезВызова, ПарВызова, ДопПар) Экспорт
	// ДопПар = {ДанныеЗапроса, Ид, РезДляВывода, ИдПотока}
	Если ЗначениеЗаполнено(РезВызова) Тогда
		ДопПар.РезДляВывода.data = РезВызова;
	КонецЕсли;
	Элемент = Новый Структура("type, text", "text", ЗаписатьJSON2(ДопПар.РезДляВывода));
	Содержимое = Новый Массив;
	Содержимое.Добавить(Элемент);
	ТулКолРез = Новый Структура("content", Содержимое);
	ОтветJSON = СформироватьJSONRPCОтвет(ДопПар.Ид, ТулКолРез);
	// Ветка legacy SSE vs streamable HTTP:
	Если ЗначениеЗаполнено(ДопПар.ИдПотока) Тогда
		КомпонентаSendSSEEvent(ДопПар.ИдПотока, ОтветJSON, "{}", "message"); // Legacy SSE
	Иначе
		ОтправитьSSEОтвет(ДопПар.ДанныеЗапроса, ОтветJSON); // Streamable HTTP
	КонецЕсли;
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьToolsCallОшибку(Сообщение)
	Содержимое = Новый Массив;
	Элемент = Новый Структура;
	Элемент.Вставить("type", "text");
	Элемент.Вставить("text", Сообщение);
	Содержимое.Добавить(Элемент);
	Возврат Новый Структура("content, isError", Содержимое, Истина);
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// REST API
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция РазбитьRESTQueryЗначениеПоЗапятым(Текст)
	
	// Аналог _parse_csv_or_repeated_query_param из Python-прокси: поддержка CSV в одном параметре.
	// Пустые элементы игнорируем (",," -> пропускаем).
	
	Результат = Новый Массив;
	
	Если Текст = Неопределено Или Текст = Null Тогда
		Возврат Результат;
	КонецЕсли;
	
	Остаток = Строка(Текст);
	Пока Истина Цикл
		ПозЗапятая = Найти(Остаток, ",");
		Если ПозЗапятая = 0 Тогда
			Часть = Остаток;
			Остаток = "";
		Иначе
			Часть = Лев(Остаток, ПозЗапятая - 1);
			Остаток = Сред(Остаток, ПозЗапятая + 1);
		КонецЕсли;
		
		Часть = СокрЛП(Часть);
		Если НЕ ПустаяСтрока(Часть) Тогда
			Результат.Добавить(Часть);
		КонецЕсли;
		
		Если ПустаяСтрока(Остаток) Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция НормализоватьRESTQueryСписок(СырыеЗначения)
	
	// Возвращает Массив или Неопределено (если после нормализации нет элементов).
	
	Список = Новый Массив;
	
	Если СырыеЗначения = Неопределено Или СырыеЗначения = Null Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ТипЗнч(СырыеЗначения) = Тип("Массив") Или ТипЗнч(СырыеЗначения) = Тип("ФиксированныйМассив") Тогда
		Для Каждого Сырой Из СырыеЗначения Цикл
			Части = РазбитьRESTQueryЗначениеПоЗапятым(Сырой);
			Для Каждого Элемент Из Части Цикл
				Список.Добавить(Элемент);
			КонецЦикла;
		КонецЦикла;
	Иначе
		Части = РазбитьRESTQueryЗначениеПоЗапятым(СырыеЗначения);
		Для Каждого Элемент Из Части Цикл
			Список.Добавить(Элемент);
		КонецЦикла;
	КонецЕсли;
	
	Если Список.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Список;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьRESTОшибка(КодОтвета, Сообщение, Детали = Неопределено)
	
	Тело = Новый Структура("success, error", Ложь, Сообщение);
	Если Детали <> Неопределено Тогда
		Тело.Вставить("details", Детали);
	КонецЕсли;
	
	Возврат Новый Структура("КодОтвета, Тело", КодОтвета, ЗаписатьJSON2(Тело));
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьRESTGETОшибкаЦелого(ИмяПоля, СыройТекст)
	
	ИмяПоля = СокрЛП(Строка(ИмяПоля));
	СыройТекст = Строка(СыройТекст);
	
	Сообщение = "Ошибка валидации: " + ИмяПоля + " должен быть целым числом, получено '" + СыройТекст + "' / "
		+ "Validation error: " + ИмяПоля + " must be an integer, got '" + СыройТекст + "'";
	
	Лок = Новый Массив;
	Лок.Добавить(ИмяПоля);
	
	Детали = Новый Массив;
	Детали.Добавить(Новый Структура(
		"loc, msg, type, input",
		Лок,
		"value is not a valid integer",
		"int_parsing",
		СыройТекст
	));
	
	Возврат СформироватьRESTОшибка(422, Сообщение, Детали);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, ИмяПоля)
	
	// Если поле отсутствует — ок.
	// Если есть и это строка, но не целое — возвращает структуру REST ошибки (КодОтвета/Тело).
	// Если можно распарсить — заменяет значение на целое и возвращает Неопределено.
	
	Если RESTПараметры = Неопределено Или ТипЗнч(RESTПараметры) <> Тип("Структура") Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ RESTПараметры.Свойство(ИмяПоля) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Сырой = RESTПараметры[ИмяПоля];
	
	// Если пришло несколько значений — берём первое (как request.query_params.get в прокси).
	Если ТипЗнч(Сырой) = Тип("Массив") Или ТипЗнч(Сырой) = Тип("ФиксированныйМассив") Тогда
		Если Сырой.Количество() = 0 Тогда
			Сырой = "";
		Иначе
			Сырой = Сырой[0];
		КонецЕсли;
	КонецЕсли;
	
	Если Сырой = Null Или Сырой = Неопределено Тогда
		Возврат СформироватьRESTGETОшибкаЦелого(ИмяПоля, "");
	КонецЕсли;
	
	Если ТипЗнч(Сырой) = Тип("Число") Тогда
		Если Сырой <> Цел(Сырой) Тогда
			Возврат СформироватьRESTGETОшибкаЦелого(ИмяПоля, Строка(Сырой));
		КонецЕсли;
		RESTПараметры.Вставить(ИмяПоля, Цел(Сырой));
		Возврат Неопределено;
	КонецЕсли;
	
	СыройСтрока = СокрЛП(Строка(Сырой));
	Если ПустаяСтрока(СыройСтрока) Тогда
		Возврат СформироватьRESTGETОшибкаЦелого(ИмяПоля, СыройСтрока);
	КонецЕсли;
	
	ЧислоЗначение = Неопределено;
	Попытка
		ЧислоЗначение = Число(СыройСтрока);
	Исключение
		ЧислоЗначение = Неопределено;
	КонецПопытки;
	
	Если ЧислоЗначение = Неопределено Или ЧислоЗначение <> Цел(ЧислоЗначение) Тогда
		Возврат СформироватьRESTGETОшибкаЦелого(ИмяПоля, СыройСтрока);
	КонецЕсли;
	
	RESTПараметры.Вставить(ИмяПоля, Цел(ЧислоЗначение));
	Возврат Неопределено;
	
КонецФункции

&НаКлиенте
Функция ОбработатьRESTЗапрос(ДанныеЗапроса)

	// path → tool: /api/execute_query → execute_query
	ИмяИнструмента = Сред(ДанныеЗапроса.path, 6); // убираем "/api/"

	// Формируем Команда
	Команда = Новый Структура;
	Команда.Вставить("id", ДанныеЗапроса.id);
	Команда.Вставить("tool", ИмяИнструмента);

	// REST: restart/close -- перехват ДО парсинга body (endpoint без параметров, пустой/любой POST валиден)
	Если ИмяИнструмента = "restart_1c_session" Тогда
		Если ДанныеЗапроса.method <> "POST" Тогда
			Возврат СформироватьRESTОшибка(405, "Method not allowed: restart_1c_session requires POST");
		КонецЕсли;
		// Двойной guard: ПерезапускВОжидании -- during active restart;
		// ЗавершениеПослеПерезапускаЗапущено -- during shutdown (after ПерезапускВОжидании reset)
		Если ПерезапускАктивенИлиЗавершается() Или ЗакрытиеСессииВПроцессе Тогда
			Возврат СформироватьRESTОшибка(500, "Restart or close already in progress");
		КонецЕсли;
		ВыполнитьПерезапускСессии("", ДанныеЗапроса, ДанныеЗапроса.id, "", Истина);
		Возврат Неопределено;
	КонецЕсли;
	Если ИмяИнструмента = "close_1c_session" Тогда
		Если ДанныеЗапроса.method <> "POST" Тогда
			Возврат СформироватьRESTОшибка(405, "Method not allowed: close_1c_session requires POST");
		КонецЕсли;
		Если ПерезапускАктивенИлиЗавершается() Или ЗакрытиеСессииВПроцессе Тогда
			Возврат СформироватьRESTОшибка(500, "Restart or close already in progress");
		КонецЕсли;
		ВыполнитьЗакрытиеСессии("", ДанныеЗапроса, ДанныеЗапроса.id, "", Истина);
		Возврат Неопределено;
	КонецЕсли;

	// POST-only для submit_for_deanonymization
	Если ИмяИнструмента = "submit_for_deanonymization" И ДанныеЗапроса.method <> "POST" Тогда
		Возврат СформироватьRESTОшибка(405, "Method not allowed: submit_for_deanonymization requires POST");
	КонецЕсли;

	// Параметры: GET из query, POST из body
	Если ДанныеЗапроса.method = "GET" Тогда
		RESTПараметры = Новый Структура;
		Попытка
			Для Каждого КлючЗначение Из ДанныеЗапроса.query Цикл
				ИмяПоля = КлючЗначение.Ключ;
				Значения = КлючЗначение.Значение;
				
				// channel — транспортный параметр прокси-режима (маршрутизация на разные БД).
				// Во встроенном сервере не используется и не должен попадать в params инструмента.
				Если ИмяПоля = "channel" Тогда
					Продолжить;
				КонецЕсли;
				
				// Списки: поддерживаем CSV и повторяющиеся параметры как в прокси.
				Если ИмяПоля = "meta_type"
					Или ИмяПоля = "sections"
					Или ИмяПоля = "levels"
					Или ИмяПоля = "events"
					Или ИмяПоля = "rights_filter"
					Или ИмяПоля = "roles_filter"
					Или ИмяПоля = "names"
				Тогда
					СписокЗначений = НормализоватьRESTQueryСписок(Значения);
					Если СписокЗначений <> Неопределено Тогда
						Если СписокЗначений.Количество() = 1 Тогда
							RESTПараметры.Вставить(ИмяПоля, СписокЗначений[0]);
						Иначе
							RESTПараметры.Вставить(ИмяПоля, СписокЗначений);
						КонецЕсли;
					КонецЕсли;
				ИначеЕсли ТипЗнч(Значения) = Тип("Массив") И Значения.Количество() = 1 Тогда
					RESTПараметры.Вставить(ИмяПоля, Значения[0]);
				Иначе
					RESTПараметры.Вставить(ИмяПоля, Значения);
				КонецЕсли;
			КонецЦикла;
		Исключение
			// query может быть пустым
		КонецПопытки;
		
		// Числовые параметры из query: если не парсятся, отдаём ошибку как в прокси (422 + details).
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "limit");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "offset");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "same_second_offset");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "limit_hits");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "limit_per_meta");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		RESTОшибка = ПопробоватьРаспарситьRESTGETЦелоеПоле(RESTПараметры, "timeout_budget_sec");
		Если RESTОшибка <> Неопределено Тогда Возврат RESTОшибка КонецЕсли;
		
		Команда.Вставить("params", RESTПараметры);
	Иначе
		Попытка
			ТелоREST = ПрочитатьJSON2(ДанныеЗапроса.body);
		Исключение
			ТекстОшибки = ОписаниеОшибки();
			Возврат СформироватьRESTОшибка(400,
				"Ошибка разбора JSON: " + ТекстОшибки + " / JSON parse error: " + ТекстОшибки);
		КонецПопытки;
		
		Если ТипЗнч(ТелоREST) <> Тип("Структура") Тогда
			Возврат СформироватьRESTОшибка(400,
				"Тело запроса должно быть JSON объектом / Request body must be a JSON object");
		КонецЕсли;
		
		// channel — транспортный параметр прокси-режима (маршрутизация на разные БД).
		// Во встроенном сервере не используется и не должен попадать в params инструмента.
		УдалитьПолеЕслиЕсть(ТелоREST, "channel");
		
		Команда.Вставить("params", ТелоREST);
	КонецЕсли;

	ДобавитьВЛог("Получена команда: " + ИмяИнструмента + Символы.ПС + ПолучитьТекстКоманды(Команда));

	// Деанонимизация входных параметров (встроенный сервер, точная анонимизация)
	Если ИспользоватьТочнуюАнонимизацию Тогда
		ДеАнонимизироватьПараметрыКоманды(Команда);
	КонецЕсли;

	// Валидация аргументов (строгие типы/дефолты): только для встроенного HTTP-сервера (REST).
	ПараметрыRESTЗапроса = Неопределено;
	Команда.Свойство("params", ПараметрыRESTЗапроса);

	Если ТипЗнч(ПараметрыRESTЗапроса) = Тип("Структура") Тогда
		ДетальОшибки = ПроверитьИНормализоватьАргументыИнструмента(ИмяИнструмента, ПараметрыRESTЗапроса);
		Если ДетальОшибки <> Неопределено Тогда
			СообщениеОшибки = "Ошибка валидации параметров: " + ДетальОшибки
				+ " / Parameter validation failed: " + ДетальОшибки;
			Возврат Новый Структура("КодОтвета, Тело", 422,
				ЗаписатьJSON2(Новый Структура("success, error", Ложь, СообщениеОшибки))
			);
		КонецЕсли;
	Иначе
		// Если параметры не объект, то не пытаемся "валидировать лимиты" по типам,
		// иначе будет неинформативная ошибка на неверный JSON. Лимит-поля всё равно не извлечь.
	КонецЕсли;

	// Проверка опасных операций для execute_code (REST)
	Если ИмяИнструмента = "execute_code" Тогда
		Код = "";
		Если ТипЗнч(Команда.params) = Тип("Структура") Тогда
			Команда.params.Свойство("code", Код);
		КонецЕсли;
		МассивОпасныхСлов = ПроверитьОпасныеСлова(Код);
		Если МассивОпасныхСлов.Количество() > 0 Тогда
			Если ВсеОперацииАвтоРазрешены(МассивОпасныхСлов) Тогда
				ДобавитьВЛог("REST: операции автоматически разрешены: " + СоединитьМассив(МассивОпасныхСлов, ", "));
			ИначеЕсли РазрешатьОпасныеОперацииСПодтверждением Тогда
				ОпасныеСлова = СоединитьМассив(МассивОпасныхСлов, ", ");
				ОжидающиеПодтверждения.Вставить(ДанныеЗапроса.id,
					Новый Структура(
						"Режим, ДанныеЗапроса, Команда, ОпасныеКлючевыеСлова, ОпасныеКлючевыеСловаТекст",
						"rest", ДанныеЗапроса, Команда, МассивОпасныхСлов, ОпасныеСлова));
				ОткрытьФорму(
					"ВнешняяОбработка.MCPToolkit.Форма.ФормаПодтверждения",
					Новый Структура("ОпасныеСлова, Код", ОпасныеСлова, Код),
					ЭтаФорма, , , ,
					Новый ОписаниеОповещения("ОбработатьРезультатПодтвержденияВстроенный",
						ЭтаФорма, Новый Структура("RequestId", ДанныеЗапроса.id)),
					РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
				Возврат Неопределено;
			Иначе
				КлючевыеСловаТекст = СоединитьМассив(МассивОпасныхСлов, ", ");
				Возврат СформироватьRESTОшибка(403,
					"Операция запрещена: код содержит опасные ключевые слова: " + КлючевыеСловаТекст
					+ " / Operation not allowed: code contains dangerous keywords: " + КлючевыеСловаТекст);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;

	// Выполнение
	Попытка
		Результат = ВыполнитьКомандуСУчетомКонтекста(Команда);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		Возврат СформироватьRESTОшибка(500,
			"Необработанное исключение сервера: " + ТекстОшибки
			+ " / Unhandled server exception: " + ТекстОшибки);
	КонецПопытки;

	ДобавитьВЛог("Результат команды отправлен успешно" + Символы.ПС + ПолучитьТекстРезультата(Результат));
	ТекстЗапросаLineage = ПолучитьТекстЗапросаLineageИзКоманды(Команда);
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И НуженLineageДляРезультата(ИмяИнструмента, ТекстЗапросаLineage, Результат) Тогда
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Результат, ТекстЗапроса",
			"rest", ДанныеЗапроса, Результат, ТекстЗапросаLineage);
		НачатьАсинхронноеОбогащениеСхемыИсточниками(КонтекстLineage);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_object_by_link" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		Результат = ПрименитьТочнуюPolicyКРезультатуGetObjectByLink(Результат);
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоОбъект",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьОбъектВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Результат",
			"rest", ДанныеЗапроса, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "find_references_to_object" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоFindReferences",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатFindReferencesВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Результат",
			"rest", ДанныеЗапроса, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_event_log" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		РегексКонтекст = Новый Структура("Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингИндексов, ЕстьТекстыДляRegex, ЭтоGetEventLog",
			Результат, "", "", Неопределено, Ложь, Истина);
		АнонимизироватьРезультатGetEventLogВстроенный(Результат, РегексКонтекст);
		КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Результат",
			"rest", ДанныеЗапроса, Результат);
		РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
		ЗапуститьRegexФазу(РегексКонтекст);
		Возврат Неопределено;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "get_access_rights" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
			Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
			Результат.Вставить("_original_data_anon_marker", Истина);
		КонецЕсли;
		АнонимизироватьРезультатGetAccessRights(Результат);
		ОбновитьТаблицуМаппинговВстроенный();
		Если ПроверитьПодтверждениеАнонимизации(Результат, ДанныеЗапроса, "", "", "rest") Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	Если ИспользоватьАсинхронноеПодключениеКомпонент
		И ИмяИнструмента = "execute_code" И ИспользоватьТочнуюАнонимизацию
		И РежимПодключения <> 0 Тогда
		УжеАнонимизирован = Ложь;
		Если ТипЗнч(Результат) = Тип("Структура") Тогда
			ВремАнон = Неопределено;
			УжеАнонимизирован = Результат.Свойство("_original_data_json", ВремАнон) И ЗначениеЗаполнено(ВремАнон);
		КонецЕсли;
		Если НЕ УжеАнонимизирован Тогда
			Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("data") Тогда
				Результат.Вставить("_original_data_json",        ЗаписатьJSON2(Результат.data));
				Результат.Вставить("_original_data_anon_marker", Истина);
			КонецЕсли;
			РегексКонтекст = Новый Структура(
				"Результат, ВключённыеПравилаJSON, ТекстыJSON, МаппингПутей, ЕстьТекстыДляRegex, ЭтоExecuteCode",
				Результат, "", "", Неопределено, Ложь, Истина);
			АнонимизироватьExecuteCodeВстроенный(Результат, РегексКонтекст);
			КонтекстLineage = Новый Структура("Режим, ДанныеЗапроса, Результат",
				"rest", ДанныеЗапроса, Результат);
			РегексКонтекст.Вставить("КонтекстLineage", КонтекстLineage);
			ЗапуститьRegexФазу(РегексКонтекст);
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;

	// Async-ветка для JsonToToon:
	Если ИспользоватьАсинхронноеПодключениеКомпонент И КомпонентаТун <> Неопределено
		И ФорматОтветаВстроенный = 1 И ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("success") И Результат.success И Результат.Свойство("data") Тогда
		КомпонентаТун.НачатьВызовJsonToToon(
			Новый ОписаниеОповещения("ПослеJsonToToonREST", ЭтаФорма,
				Новый Структура("ДанныеЗапроса, Результат", ДанныеЗапроса, Результат)),
			ЗаписатьJSON2(Результат.data));
		Возврат Неопределено;
	КонецЕсли;

	// sync path: оригинальный вызов ПрименитьТунКонвертацию и Возврат
	Результат = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(Результат);
	ПрименитьТунКонвертацию(Результат);
	Возврат Новый Структура("КодОтвета, Тело", 200, ЗаписатьJSON2(Результат));

КонецФункции

&НаКлиенте
Процедура ПослеJsonToToonREST(РезВызова, ПарВызова, ДопПар) Экспорт
	// ДопПар = Структура("ДанныеЗапроса, Результат", ...)
	Если ЗначениеЗаполнено(РезВызова) Тогда
		ДопПар.Результат.data = РезВызова;
	КонецЕсли;
	ДопПар.Результат = ОчиститьВнутренниеПоляPolicyВРезультатеExecuteQuery(ДопПар.Результат);
	КомпонентаSendResponse(ДопПар.ДанныеЗапроса.id, 200,
		ЗаголовкиJSON(), ЗаписатьJSON2(ДопПар.Результат));
КонецПроцедуры

// ─────────────────────────────────────────────────────────────────────────────────
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ─────────────────────────────────────────────────────────────────────────────────

&НаКлиентеНаСервереБезКонтекста
Функция ЗаголовкиJSON()
	Возврат "{""Content-Type"":""application/json""}";
КонецФункции

&НаКлиенте
Функция СформироватьSSEЗаголовки(ДанныеЗапроса, ИдСессииДляЗаголовка = Неопределено, ЗакрытьСоединение = Ложь)

	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Cache-Control", "no-cache, no-transform");
	Заголовки.Вставить("Connection", ?(ЗакрытьСоединение, "close", "keep-alive"));

	// Источник mcp-session-id
	Если ИдСессииДляЗаголовка <> Неопределено Тогда
		Заголовки.Вставить("mcp-session-id", ИдСессииДляЗаголовка);
	Иначе
		ИдСессии = ДанныеЗапроса.headers["mcp-session-id"];
		Если ИдСессии <> Неопределено Тогда
			Заголовки.Вставить("mcp-session-id", ИдСессии);
		КонецЕсли;
	КонецЕсли;

	Возврат ЗаписатьJSON2(Заголовки);

КонецФункции

&НаКлиенте
Процедура ОтправитьSSEОтвет(ДанныеЗапроса, ОтветJSON, ИдСессииДляЗаголовка = Неопределено)
	Если ИспользоватьАсинхронноеПодключениеКомпонент Тогда
		// CloseSSEStream вызывается в колбэке — гарантированный порядок
		Компонента.НачатьВызовSendSSEEvent(
			Новый ОписаниеОповещения("ПослеОтправкиSSEОтвета", ЭтаФорма, ДанныеЗапроса.id),
			ДанныеЗапроса.id, ОтветJSON,
			СформироватьSSEЗаголовки(ДанныеЗапроса, ИдСессииДляЗаголовка), "message");
	Иначе
		Компонента.SendSSEEvent(ДанныеЗапроса.id, ОтветJSON,
			СформироватьSSEЗаголовки(ДанныеЗапроса, ИдСессииДляЗаголовка));
		Компонента.CloseSSEStream(ДанныеЗапроса.id);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеОтправкиSSEОтвета(РезВызова, ПарВызова, ДопПар) Экспорт
	КомпонентаCloseSSEStream(ДопПар); // ДопПар = ДанныеЗапроса.id
КонецПроцедуры

&НаКлиенте
Функция СформироватьОтветHealth()
	Ответ = Новый Структура;
	Ответ.Вставить("status", "healthy");
	Ответ.Вставить("mode", "native");
	Ответ.Вставить("port", ПортВстроенногоСервера);
	Возврат ЗаписатьJSON2(Ответ);
КонецФункции

&НаКлиенте
Функция СформироватьJSONRPCОтвет(Идентификатор, Результат)
	Ответ = Новый Структура;
	Ответ.Вставить("jsonrpc", "2.0");
	Ответ.Вставить("id", Идентификатор);
	Ответ.Вставить("result", Результат);
	Возврат ЗаписатьJSON2(Ответ);
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СформироватьJSONRPCОшибку(Идентификатор, КодОшибки, Сообщение)
	Ответ = Новый Структура;
	Ответ.Вставить("jsonrpc", "2.0");
	Ответ.Вставить("id", Идентификатор);
	Ответ.Вставить("error", Новый Структура("code, message", КодОшибки, Сообщение));
	Возврат ЗаписатьJSON2(Ответ, Истина);
КонецФункции

// ─────────────────────────────────────────────────────────────────────────────────
// REGEX-АНОНИМИЗАЦИЯ (нативная компонента PCRE2)
// ─────────────────────────────────────────────────────────────────────────────────

// ─── Загрузка компоненты ────────────────────────────────────────────────────────

&НаКлиенте
Процедура ПодключитьКомпонентуRegexСинхронно()
	Попытка
		// Попытка 1: по строке макета
		Если НЕ ПодключитьВнешнююКомпоненту(
				"ВнешняяОбработка.MCPToolkit.Макет.RegexHelper",
				"RegexH", ТипВнешнейКомпоненты.Native) Тогда
			// Попытка 2: через temp-файл
			ДвоичныеДанные = ПолучитьМакетRegexКомпонентыНаСервере();
			ВремФайл = ПолучитьИмяВременногоФайла("dll");
			ДвоичныеДанные.Записать(ВремФайл);
			ПодключитьВнешнююКомпоненту(ВремФайл, "RegexH", ТипВнешнейКомпоненты.Native);
		КонецЕсли;
		КомпонентаRegex = Новый("AddIn.RegexH.RegexHelper");
	Исключение
		КомпонентаRegex = Неопределено;
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ЗапуститьRegexФазу(РегексКонтекст)
	// Fast-path: нет текстов для обработки → сразу продолжить
	Если НЕ РегексКонтекст.ЕстьТекстыДляRegex Тогда
		ПродолжитьПослеRegex(РегексКонтекст);
		Возврат;
	КонецЕсли;
	Если КомпонентаRegexНедоступна = Истина Тогда
		ПродолжитьПослеRegex(РегексКонтекст);
		Возврат;
	КонецЕсли;
	Если КомпонентаRegex <> Неопределено Тогда
		ВызватьRegexКомпоненту(РегексКонтекст);
		Возврат;
	КонецЕсли;
	// Нужна загрузка — только async (эта процедура вызывается только из async-пути)
	НачатьАсинхронноеПодключениеКомпоненты(
		Новый ОписаниеОповещения("ПослеПодключенияМакетаRegex", ЭтаФорма, РегексКонтекст),
		"ВнешняяОбработка.MCPToolkit.Макет.RegexHelper", "RegexH");
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияМакетаRegex(Подключено, ДопПар) Экспорт // ДопПар = РегексКонтекст
	Если Подключено Тогда
		Попытка
			КомпонентаRegex = Новый("AddIn.RegexH.RegexHelper");
			ВызватьRegexКомпоненту(ДопПар);
			Возврат;
		Исключение
		КонецПопытки;
	КонецЕсли;
	// Fallback через temp-файл
	Попытка
		ДвоичныеДанные = ПолучитьМакетRegexКомпонентыНаСервере();
		ВремФайл = ПолучитьИмяВременногоФайла("dll");
		ДвоичныеДанные.Записать(ВремФайл);
		НачатьАсинхронноеПодключениеКомпоненты(
			Новый ОписаниеОповещения("ПослеПодключенияФайлаRegex", ЭтаФорма, ДопПар),
			ВремФайл, "RegexH");
	Исключение
		ПродолжитьПослеRegex(ДопПар);
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодключенияФайлаRegex(Подключено, ДопПар) Экспорт // ДопПар = РегексКонтекст
	Если Подключено Тогда
		Попытка
			КомпонентаRegex = Новый("AddIn.RegexH.RegexHelper");
			ВызватьRegexКомпоненту(ДопПар);
			Возврат;
		Исключение
		КонецПопытки;
	КонецЕсли;
	КомпонентаRegexНедоступна = Истина;
	ПродолжитьПослеRegex(ДопПар);
КонецПроцедуры

&НаКлиенте
Процедура ВызватьRegexКомпоненту(РегексКонтекст)
	КомпонентаRegex.НачатьВызовFindMatchesInTexts(
		Новый ОписаниеОповещения("ПослеRegexАнонимизации", ЭтаФорма, РегексКонтекст),
		РегексКонтекст.ВключённыеПравилаJSON, РегексКонтекст.ТекстыJSON);
КонецПроцедуры

&НаКлиенте
Процедура ПослеRegexАнонимизации(СовпаденияJSON, ПарВызова, РегексКонтекст) Экспорт
	Попытка
		ЭтоОбъект = Ложь;
		РегексКонтекст.Свойство("ЭтоОбъект", ЭтоОбъект);
		ЭтоFindReferences = Ложь;
		РегексКонтекст.Свойство("ЭтоFindReferences", ЭтоFindReferences);
		ЭтоGetEventLog = Ложь;
		РегексКонтекст.Свойство("ЭтоGetEventLog", ЭтоGetEventLog);
		ЭтоExecuteCode = Ложь;
		РегексКонтекст.Свойство("ЭтоExecuteCode", ЭтоExecuteCode);
		Если ЭтоGetEventLog Тогда
			ПрименитьRegexТокеныGetEventLog(РегексКонтекст, СовпаденияJSON);
		ИначеЕсли ЭтоFindReferences Тогда
			ПрименитьRegexТокеныFindReferences(РегексКонтекст, СовпаденияJSON);
		ИначеЕсли ЭтоОбъект Тогда
			ПрименитьRegexТокеныОбъект(РегексКонтекст, СовпаденияJSON);
		ИначеЕсли ЭтоExecuteCode Тогда
			ПрименитьRegexТокеныExecuteCode(РегексКонтекст, СовпаденияJSON);
		Иначе
			ПрименитьRegexТокены(РегексКонтекст, СовпаденияJSON);
		КонецЕсли;
	Исключение
	КонецПопытки;
	ПродолжитьПослеRegex(РегексКонтекст);
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьПослеRegex(РегексКонтекст)
	ОбновитьТаблицуМаппинговВстроенный(); // ПОСЛЕ regex-токенов

	КонтекстLineage = Неопределено;
	РегексКонтекст.Свойство("КонтекстLineage", КонтекстLineage);
	Если КонтекстLineage = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ПродолжитьПослеRegexБезКонтекста(КонтекстLineage);
КонецПроцедуры

&НаСервере
Функция ПолучитьМакетRegexКомпонентыНаСервере()
	Обработка = РеквизитФормыВЗначение("Объект");
	Возврат Обработка.ПолучитьМакет("RegexHelper");
КонецФункции

// ─── Хранение regex-правил ──────────────────────────────────────────────────────

&НаКлиенте
Функция ПолучитьRegexПравилаИзХранилища()
	Если КэшRegexПравил <> Неопределено Тогда
		Возврат КэшRegexПравил;
	КонецЕсли;

	СохранённыеПравила = ПолучитьRegexПравилаИзХранилищаНаСервере();
	Если СохранённыеПравила <> Неопределено И ТипЗнч(СохранённыеПравила) = Тип("Массив") Тогда
		КэшRegexПравил = СохранённыеПравила;
	Иначе
		КэшRegexПравил = ПолучитьRegexПравилаПоУмолчанию();
	КонецЕсли;
	Возврат КэшRegexПравил;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьRegexПравилаИзХранилищаНаСервере()
	Попытка
		ЗначениеJSON = ХранилищеОбщихНастроек.Загрузить("ExecuteQueryRegexRules.v1", "rules");
		Если ЗначениеJSON <> Неопределено И НЕ ПустаяСтрока(ЗначениеJSON) Тогда
			Данные = ПрочитатьJSON2(ЗначениеJSON);
			МассивПравил = Неопределено;
			Если ТипЗнч(Данные) = Тип("Структура") Тогда
				Данные.Свойство("rules", МассивПравил);
			ИначеЕсли ТипЗнч(Данные) = Тип("Соответствие") Тогда
				МассивПравил = Данные["rules"];
			КонецЕсли;
			Если ТипЗнч(МассивПравил) = Тип("Массив") Тогда
				Возврат МассивПравил;
			КонецЕсли;
		КонецЕсли;
	Исключение
	КонецПопытки;
	Возврат Неопределено;
КонецФункции

&НаКлиенте
Процедура СохранитьRegexПравилаВХранилище(Правила)
	СохранитьRegexПравилаВХранилищеНаСервере(ЗаписатьJSON2(Новый Структура("version, rules", 1, Правила)));
	КэшRegexПравил = Неопределено;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьRegexПравилаВХранилищеНаСервере(ЗначениеJSON)
	Попытка
		ХранилищеОбщихНастроек.Сохранить("ExecuteQueryRegexRules.v1", "rules", ЗначениеJSON);
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Функция ПолучитьRegexПравилаПоУмолчанию()
	ДанныеПравил = ПолучитьRegexПравилаПоУмолчаниюНаСервере();
	Если ДанныеПравил = Неопределено Тогда
		Возврат Новый Массив;
	КонецЕсли;
	Возврат ДанныеПравил;
КонецФункции

&НаСервере
Функция ПолучитьRegexПравилаПоУмолчаниюНаСервере()
	Попытка
		Обработка = РеквизитФормыВЗначение("Объект");
		ДвоичныеДанные = Обработка.ПолучитьМакет("НастройкиRegexПоУмолчанию");
		ИмяФайла = ПолучитьИмяВременногоФайла("json");
		ДвоичныеДанные.Записать(ИмяФайла);
		ТекстовыйДокумент = Новый ТекстовыйДокумент;
		ТекстовыйДокумент.Прочитать(ИмяФайла);
		Данные = ПрочитатьJSON2(ТекстовыйДокумент.ПолучитьТекст());
		ИсхМассив = Неопределено;
		Если ТипЗнч(Данные) = Тип("Структура") Тогда
			Данные.Свойство("rules", ИсхМассив);
		ИначеЕсли ТипЗнч(Данные) = Тип("Соответствие") Тогда
			ИсхМассив = Данные["rules"];
		КонецЕсли;
		Если ТипЗнч(ИсхМассив) = Тип("Массив") Тогда
			МассивПравил = Новый Массив;
			Для Каждого Правило Из ИсхМассив Цикл
				Стр = Новый Структура("enabled, pattern, category, description", Ложь, "", "", "");
				Если ТипЗнч(Правило) = Тип("Структура") Или ТипЗнч(Правило) = Тип("Соответствие") Тогда
					ВклЗнч = Неопределено;
					ПатЗнч = Неопределено;
					КатЗнч = Неопределено;
					ОпиЗнч = Неопределено;
					Если ТипЗнч(Правило) = Тип("Структура") Тогда
						Правило.Свойство("enabled",     ВклЗнч);
						Правило.Свойство("pattern",     ПатЗнч);
						Правило.Свойство("category",    КатЗнч);
						Правило.Свойство("description", ОпиЗнч);
					Иначе
						ВклЗнч = Правило["enabled"];
						ПатЗнч = Правило["pattern"];
						КатЗнч = Правило["category"];
						ОпиЗнч = Правило["description"];
					КонецЕсли;
					Если ВклЗнч <> Неопределено Тогда Стр.enabled     = ВклЗнч; КонецЕсли;
					Если ПатЗнч <> Неопределено Тогда Стр.pattern     = ПатЗнч; КонецЕсли;
					Если КатЗнч <> Неопределено Тогда Стр.category    = КатЗнч; КонецЕсли;
					Если ОпиЗнч <> Неопределено Тогда Стр.description = ОпиЗнч; КонецЕсли;
				КонецЕсли;
				МассивПравил.Добавить(Стр);
			КонецЦикла;
			Возврат МассивПравил;
		КонецЕсли;
	Исключение
	КонецПопытки;
	Возврат Неопределено;
КонецФункции

// ─── Фазы regex-анонимизации ────────────────────────────────────────────────────

&НаКлиенте
Процедура СобратьТекстыДляRegex(Результат, РегексКонтекст)
	// Получить включённые правила
	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") И Правило.enabled = Истина Тогда
			ВключённыеПравила.Добавить(Новый Структура("pattern, category", Правило.pattern, Правило.category));
		КонецЕсли;
	КонецЦикла;

	Если ВключённыеПравила.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	// Сериализовать включённые правила в JSON
	МассивПравилJSON = Новый Массив;
	Для Каждого П Из ВключённыеПравила Цикл
		МассивПравилJSON.Добавить(Новый Структура("pattern, category", П.pattern, П.category));
	КонецЦикла;
	РегексКонтекст.ВключённыеПравилаJSON = ЗаписатьJSON2(МассивПравилJSON);

	// Собрать тексты из данных результата
	ТекстыМассив  = Новый Массив;
	МаппингИндексов = Новый Массив; // каждый элемент: Структура("СтрокаИндекс, ИмяКолонки, ТипЗначения")

	Данные = Неопределено;
	Если НЕ (ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("data", Данные)
		И ТипЗнч(Данные) = Тип("Массив")) Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	Схема = Неопределено;
	СоответствиеPolicy = Новый Соответствие;
	Если Результат.Свойство("schema", Схема)
		И ТипЗнч(Схема) = Тип("Структура") Тогда
		Колонки = Неопределено;
		Если Схема.Свойство("columns", Колонки) И ТипЗнч(Колонки) = Тип("Массив") Тогда
			Для Каждого Колонка Из Колонки Цикл
				Если ТипЗнч(Колонка) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
				ИмяКолонки = "";
				Если НЕ Колонка.Свойство("name", ИмяКолонки) Тогда Продолжить; КонецЕсли;
				Режим       = "default";
				РежимСсылки = "default";
				Колонка.Свойство("anonymization_mode",          Режим);
				Колонка.Свойство("reference_presentation_mode", РежимСсылки);
				СоответствиеPolicy.Вставить(ИмяКолонки,
					Новый Структура("mode, ref_mode", Режим, РежимСсылки));
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;

	Для СтрИндекс = 0 По Данные.Количество() - 1 Цикл
		Строка = Данные[СтрИндекс];
		Если ТипЗнч(Строка) <> Тип("Соответствие")
			И ТипЗнч(Строка) <> Тип("Структура") Тогда
			Продолжить;
		КонецЕсли;
		Если ТипЗнч(Строка) = Тип("Структура") Тогда
			МассивКлючей = Новый Массив;
			Для Каждого КвП Из Строка Цикл
				МассивКлючей.Добавить(КвП.Ключ);
			КонецЦикла;
		Иначе
			МассивКлючей = Новый Массив;
			Для Каждого КвП Из Строка Цикл
				МассивКлючей.Добавить(КвП.Ключ);
			КонецЦикла;
		КонецЕсли;
		Для Каждого ИмяКол Из МассивКлючей Цикл
			PolicyКол = СоответствиеPolicy.Получить(ИмяКол);
			МодКол    = "default";
			РефКол    = "default";
			Если ТипЗнч(PolicyКол) = Тип("Структура") Тогда
				PolicyКол.Свойство("mode",     МодКол);
				PolicyКол.Свойство("ref_mode", РефКол);
			КонецЕсли;
			Если МодКол = "force_keep" Тогда Продолжить; КонецЕсли;
			ЗначениеКолонки = Неопределено;
			Если ТипЗнч(Строка) = Тип("Структура") Тогда
				Если НЕ Строка.Свойство(ИмяКол, ЗначениеКолонки) Тогда Продолжить; КонецЕсли;
			Иначе
				ЗначениеКолонки = Строка[ИмяКол];
			КонецЕсли;
			Если ТипЗнч(ЗначениеКолонки) = Тип("Строка") Тогда
				ТекстыМассив.Добавить(ЗначениеКолонки);
				МаппингИндексов.Добавить(Новый Структура("СтрокаИндекс, ИмяКолонки, ТипЗначения",
					СтрИндекс, ИмяКол, "Строка"));
			ИначеЕсли (ТипЗнч(ЗначениеКолонки) = Тип("Структура")
					И ЗначениеКолонки.Свойство("Представление"))
				ИЛИ (ТипЗнч(ЗначениеКолонки) = Тип("Соответствие")
					И ЗначениеКолонки["Представление"] <> Неопределено) Тогда
				// Определить ТипОбъекта для enum-проверки
				ТипОбъектаЗнч = "";
				Если ТипЗнч(ЗначениеКолонки) = Тип("Структура") Тогда
					ЗначениеКолонки.Свойство("ТипОбъекта", ТипОбъектаЗнч);
				Иначе
					ТипОбъектаЗнч = ?(ЗначениеКолонки["ТипОбъекта"] <> Неопределено,
						ЗначениеКолонки["ТипОбъекта"], "");
				КонецЕсли;
				// Enum-ссылки и ref_mode = force_keep не трогаем
				// Планы счетов по умолчанию не анонимизируются, если нет явного force_anonymize
				РазрешеноМенять = НЕ (Найти(ТипОбъектаЗнч, "ПеречислениеСсылка.") = 1)
					И НЕ (Найти(ТипОбъектаЗнч, "ПланСчетовСсылка.") = 1
						И РефКол <> "force_anonymize" И МодКол <> "force_anonymize")
					И НЕ (РефКол = "force_keep");
				ПредставлениеЗнч = ?(ТипЗнч(ЗначениеКолонки) = Тип("Структура"),
					ЗначениеКолонки.Представление,
					ЗначениеКолонки["Представление"]);
				Если РазрешеноМенять И ТипЗнч(ПредставлениеЗнч) = Тип("Строка") Тогда
					ТекстыМассив.Добавить(ПредставлениеЗнч);
					МаппингИндексов.Добавить(Новый Структура("СтрокаИндекс, ИмяКолонки, ТипЗначения",
						СтрИндекс, ИмяКол, "Представление"));
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

	Если ТекстыМассив.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	РегексКонтекст.ТекстыJSON      = ЗаписатьJSON2(ТекстыМассив);
	РегексКонтекст.МаппингИндексов = МаппингИндексов;
	РегексКонтекст.ЕстьТекстыДляRegex = Истина;
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьRegexТокены(РегексКонтекст, СовпаденияJSON)
	Если НЕ ЗначениеЗаполнено(СовпаденияJSON) Тогда Возврат; КонецЕсли;

	РезСовпадений = Неопределено;
	Попытка
		РезСовпадений = ПрочитатьJSON2(СовпаденияJSON);
	Исключение
		Возврат;
	КонецПопытки;
	Если ТипЗнч(РезСовпадений) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	МаппингИндексов = РегексКонтекст.МаппингИндексов;
	Данные = РегексКонтекст.Результат.data;

	Для ТекстИнд = 0 По РезСовпадений.Количество() - 1 Цикл
		Если ТекстИнд >= МаппингИндексов.Количество() Тогда Прервать; КонецЕсли;
		ЭлСовпадений = РезСовпадений[ТекстИнд];
		Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;

		МаппингЭлт = МаппингИндексов[ТекстИнд];
		СтрИндекс  = МаппингЭлт.СтрокаИндекс;
		ИмяКолонки = МаппингЭлт.ИмяКолонки;
		ТипЗнчения = МаппингЭлт.ТипЗначения;

		Строка = Данные[СтрИндекс];

		ТекущийТекст = Неопределено;
		Если ТипЗнч(Строка) = Тип("Структура") Тогда
			Если НЕ Строка.Свойство(ИмяКолонки, ТекущийТекст) Тогда Продолжить; КонецЕсли;
		ИначеЕсли ТипЗнч(Строка) = Тип("Соответствие") Тогда
			ТекущийТекст = Строка[ИмяКолонки];
		Иначе
			Продолжить;
		КонецЕсли;

		Если ТипЗнчения = "Представление" Тогда
			Если ТипЗнч(ТекущийТекст) = Тип("Структура") И ТекущийТекст.Свойство("Представление") Тогда
				ТекущийТекст = ТекущийТекст.Представление;
			ИначеЕсли ТипЗнч(ТекущийТекст) = Тип("Соответствие") Тогда
				ТекущийТекст = ТекущийТекст["Представление"];
			Иначе
				Продолжить;
			КонецЕсли;
		КонецЕсли;
		Если ТипЗнч(ТекущийТекст) <> Тип("Строка") Тогда Продолжить; КонецЕсли;

		// Совпадения отсортированы по start DESC — применяем справа налево
		НовыйТекст = ТекущийТекст;
		Для Каждого С Из ЭлСовпадений Цикл
			Категория = "";
			МатчТекст = "";
			Начало    = 0;
			Длина     = 0;
			Если ТипЗнч(С) = Тип("Структура") Тогда
				С.Свойство("category", Категория);
				С.Свойство("match",    МатчТекст);
				С.Свойство("start",    Начало);
				С.Свойство("length",   Длина);
			ИначеЕсли ТипЗнч(С) = Тип("Соответствие") Тогда
				Категория = С["category"];
				МатчТекст = С["match"];
				Начало    = С["start"];
				Длина     = С["length"];
			КонецЕсли;
			Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
			Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
			Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
			Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
			Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
			Токен = АнонимизаторТокенизировать(МатчТекст, Категория);
			// Замена по позиции (0-based start, 1-based 1С Лев/Сред)
			Если Начало >= 0 И (Начало + Длина) <= СтрДлина(НовыйТекст) Тогда
				НовыйТекст = Лев(НовыйТекст, Начало) + Токен + Сред(НовыйТекст, Начало + Длина + 1);
			КонецЕсли;
		КонецЦикла;

		// Записать обратно
		Если ТипЗнчения = "Представление" Тогда
			// ЗначениеКолонки — Структура/Соответствие с полем "Представление"
			ЗначениеКолонки2 = Неопределено;
			Если ТипЗнч(Строка) = Тип("Структура") Тогда
				Строка.Свойство(ИмяКолонки, ЗначениеКолонки2);
				Если ТипЗнч(ЗначениеКолонки2) = Тип("Структура") Тогда
					ЗначениеКолонки2.Вставить("Представление", НовыйТекст);
				КонецЕсли;
			Иначе
				ЗначениеКолонки2 = Строка[ИмяКолонки];
				Если ТипЗнч(ЗначениеКолонки2) = Тип("Структура") Тогда
					ЗначениеКолонки2.Вставить("Представление", НовыйТекст);
				ИначеЕсли ТипЗнч(ЗначениеКолонки2) = Тип("Соответствие") Тогда
					ЗначениеКолонки2["Представление"] = НовыйТекст;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли ТипЗнч(Строка) = Тип("Структура") Тогда
			Строка.Вставить(ИмяКолонки, НовыйТекст);
		Иначе
			Строка[ИмяКолонки] = НовыйТекст;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура СобратьТекстыДляRegexОбъект(Результат, РегексКонтекст)
	// Получить включённые правила
	ВсеПравила = ПолучитьRegexПравилаИзХранилища();
	ВключённыеПравила = Новый Массив;
	Для Каждого Правило Из ВсеПравила Цикл
		Если ТипЗнч(Правило) = Тип("Структура") И Правило.enabled = Истина Тогда
			ВключённыеПравила.Добавить(Новый Структура("pattern, category", Правило.pattern, Правило.category));
		КонецЕсли;
	КонецЦикла;
	Если ВключённыеПравила.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;
	МассивПравилJSON = Новый Массив;
	Для Каждого П Из ВключённыеПравила Цикл
		МассивПравилJSON.Добавить(Новый Структура("pattern, category", П.pattern, П.category));
	КонецЦикла;
	РегексКонтекст.ВключённыеПравилаJSON = ЗаписатьJSON2(МассивПравилJSON);

	Данные = Неопределено;
	Если НЕ (ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("data", Данные)
		И ТипЗнч(Данные) = Тип("Структура")) Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	Схема = Неопределено;
	Если НЕ (Результат.Свойство("schema", Схема) И ТипЗнч(Схема) = Тип("Структура")) Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	// Строим СоответствиеPolicy для основных полей
	СоответствиеPolicy = Новый Соответствие;
	Колонки = Неопределено;
	Если Схема.Свойство("columns", Колонки) И ТипЗнч(Колонки) = Тип("Массив") Тогда
		Для Каждого Колонка Из Колонки Цикл
			Если ТипЗнч(Колонка) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
			ИмяКолРег = "";
			Если НЕ Колонка.Свойство("name", ИмяКолРег) Тогда Продолжить; КонецЕсли;
			МодРег = "default"; РефРег = "default";
			Колонка.Свойство("anonymization_mode",          МодРег);
			Колонка.Свойство("reference_presentation_mode", РефРег);
			СоответствиеPolicy.Вставить(ИмяКолРег, Новый Структура("mode, ref_mode", МодРег, РефРег));
		КонецЦикла;
	КонецЕсли;

	// Строим СоответствиеPolicyТЧ: имя ТЧ → СоответствиеPolicy колонок ТЧ
	СоответствиеPolicyТЧ = Новый Соответствие;
	ТабЧасти = Неопределено;
	Если Схема.Свойство("tabular_sections", ТабЧасти) И ТипЗнч(ТабЧасти) = Тип("Соответствие") Тогда
		Для Каждого КВПТЧ Из ТабЧасти Цикл
			СхемаТЧ = КВПТЧ.Значение;
			PolicyТЧМп = Новый Соответствие;
			Если ТипЗнч(СхемаТЧ) = Тип("Структура") Тогда
				КолонкиТЧ = Неопределено;
				Если СхемаТЧ.Свойство("columns", КолонкиТЧ) И ТипЗнч(КолонкиТЧ) = Тип("Массив") Тогда
					Для Каждого КолТЧ Из КолонкиТЧ Цикл
						Если ТипЗнч(КолТЧ) <> Тип("Структура") Тогда Продолжить; КонецЕсли;
						ИмяКолТЧРег = "";
						Если НЕ КолТЧ.Свойство("name", ИмяКолТЧРег) Тогда Продолжить; КонецЕсли;
						МодТЧРег = "default"; РефТЧРег = "default";
						КолТЧ.Свойство("anonymization_mode",          МодТЧРег);
						КолТЧ.Свойство("reference_presentation_mode", РефТЧРег);
						PolicyТЧМп.Вставить(ИмяКолТЧРег, Новый Структура("mode, ref_mode", МодТЧРег, РефТЧРег));
					КонецЦикла;
				КонецЕсли;
			КонецЕсли;
			СоответствиеPolicyТЧ.Вставить(КВПТЧ.Ключ, PolicyТЧМп);
		КонецЦикла;
	КонецЕсли;

	ТекстыМассив    = Новый Массив;
	МаппингИндексов = Новый Массив;

	// Обходим поля data (Структура)
	Для Каждого КВП Из Данные Цикл
		ИмяПоля = КВП.Ключ;
		Значение = КВП.Значение;

		// Проверяем: это ТЧ?
		PolicyТЧ = СоответствиеPolicyТЧ.Получить(ИмяПоля);
		Если PolicyТЧ <> Неопределено И ТипЗнч(Значение) = Тип("Массив") Тогда
			// Обход строк ТЧ
			Для ИндексСтроки = 0 По Значение.Количество() - 1 Цикл
				СтрТЧ = Значение[ИндексСтроки];
				Если ТипЗнч(СтрТЧ) <> Тип("Структура") И ТипЗнч(СтрТЧ) <> Тип("Соответствие") Тогда
					Продолжить;
				КонецЕсли;
				Для Каждого КВПСтр Из СтрТЧ Цикл
					ИмяКолТЧ   = КВПСтр.Ключ;
					ЗначениеТЧ = КВПСтр.Значение;
					PolicyКолТЧ = PolicyТЧ.Получить(ИмяКолТЧ);
					МодТЧ = "default"; РефТЧ = "default";
					Если ТипЗнч(PolicyКолТЧ) = Тип("Структура") Тогда
						PolicyКолТЧ.Свойство("mode",     МодТЧ);
						PolicyКолТЧ.Свойство("ref_mode", РефТЧ);
					КонецЕсли;
					Если МодТЧ = "force_keep" Тогда Продолжить; КонецЕсли;
					Если ТипЗнч(ЗначениеТЧ) = Тип("Строка") Тогда
						ТекстыМассив.Добавить(ЗначениеТЧ);
						МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяТЧ, ИндексСтроки, ИмяКолонки, ТипЗначения",
							"тч", ИмяПоля, ИндексСтроки, ИмяКолТЧ, "Строка"));
					ИначеЕсли (ТипЗнч(ЗначениеТЧ) = Тип("Структура") И ЗначениеТЧ.Свойство("Представление"))
						ИЛИ (ТипЗнч(ЗначениеТЧ) = Тип("Соответствие") И ЗначениеТЧ["Представление"] <> Неопределено) Тогда
						ТипОбъектаТЧ = "";
						Если ТипЗнч(ЗначениеТЧ) = Тип("Структура") Тогда
							ЗначениеТЧ.Свойство("ТипОбъекта", ТипОбъектаТЧ);
						Иначе
							ТипОбъектаТЧ = ?(ЗначениеТЧ["ТипОбъекта"] <> Неопределено, ЗначениеТЧ["ТипОбъекта"], "");
						КонецЕсли;
						РазрешеноТЧ = НЕ (Найти(ТипОбъектаТЧ, "ПеречислениеСсылка.") = 1)
							И НЕ (Найти(ТипОбъектаТЧ, "ПланСчетовСсылка.") = 1
								И РефТЧ <> "force_anonymize" И МодТЧ <> "force_anonymize")
							И НЕ (РефТЧ = "force_keep");
						ПредставлениеТЧ = ?(ТипЗнч(ЗначениеТЧ) = Тип("Структура"),
							ЗначениеТЧ.Представление, ЗначениеТЧ["Представление"]);
						Если РазрешеноТЧ И ТипЗнч(ПредставлениеТЧ) = Тип("Строка") Тогда
							ТекстыМассив.Добавить(ПредставлениеТЧ);
							МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяТЧ, ИндексСтроки, ИмяКолонки, ТипЗначения",
								"тч", ИмяПоля, ИндексСтроки, ИмяКолТЧ, "Представление"));
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЦикла;
			Продолжить;
		КонецЕсли;

		// Обычное поле (не ТЧ)
		PolicyПоля = СоответствиеPolicy.Получить(ИмяПоля);
		МодПоля = "default"; РефПоля = "default";
		Если ТипЗнч(PolicyПоля) = Тип("Структура") Тогда
			PolicyПоля.Свойство("mode",     МодПоля);
			PolicyПоля.Свойство("ref_mode", РефПоля);
		КонецЕсли;
		Если МодПоля = "force_keep" Тогда Продолжить; КонецЕсли;

		Если ТипЗнч(Значение) = Тип("Строка") Тогда
			ТекстыМассив.Добавить(Значение);
			МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяПоля, ТипЗначения",
				"поле", ИмяПоля, "Строка"));
		ИначеЕсли (ТипЗнч(Значение) = Тип("Структура") И Значение.Свойство("Представление"))
			ИЛИ (ТипЗнч(Значение) = Тип("Соответствие") И Значение["Представление"] <> Неопределено) Тогда
			ТипОбъектаПоля = "";
			Если ТипЗнч(Значение) = Тип("Структура") Тогда
				Значение.Свойство("ТипОбъекта", ТипОбъектаПоля);
			Иначе
				ТипОбъектаПоля = ?(Значение["ТипОбъекта"] <> Неопределено, Значение["ТипОбъекта"], "");
			КонецЕсли;
			РазрешеноПоле = НЕ (Найти(ТипОбъектаПоля, "ПеречислениеСсылка.") = 1)
				И НЕ (Найти(ТипОбъектаПоля, "ПланСчетовСсылка.") = 1
					И РефПоля <> "force_anonymize" И МодПоля <> "force_anonymize")
				И НЕ (РефПоля = "force_keep");
			ПредставлениеПоля = ?(ТипЗнч(Значение) = Тип("Структура"),
				Значение.Представление, Значение["Представление"]);
			Если РазрешеноПоле И ТипЗнч(ПредставлениеПоля) = Тип("Строка") Тогда
				ТекстыМассив.Добавить(ПредставлениеПоля);
				МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяПоля, ТипЗначения",
					"поле", ИмяПоля, "Представление"));
			КонецЕсли;
		ИначеЕсли ТипЗнч(Значение) = Тип("Массив") И МодПоля <> "force_keep" Тогда
			// Реквизит типа ФиксированныйМассив (строки или ссылки)
			Для ЭлтИнд = 0 По Значение.Количество() - 1 Цикл
				Элт = Значение[ЭлтИнд];
				Если ТипЗнч(Элт) = Тип("Строка") Тогда
					ТекстыМассив.Добавить(Элт);
					МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяПоля, ИндексЭлемента, ТипЗначения",
						"поле_массив", ИмяПоля, ЭлтИнд, "Строка"));
				ИначеЕсли ТипЗнч(Элт) = Тип("Структура") И Элт.Свойство("Представление") Тогда
					ТипОбъектаЭлт = "";
					Элт.Свойство("ТипОбъекта", ТипОбъектаЭлт);
					РазрешеноЭлт = НЕ (Найти(ТипОбъектаЭлт, "ПеречислениеСсылка.") = 1)
						И НЕ (Найти(ТипОбъектаЭлт, "ПланСчетовСсылка.") = 1
							И РефПоля <> "force_anonymize" И МодПоля <> "force_anonymize")
						И НЕ (РефПоля = "force_keep");
					Если РазрешеноЭлт И ТипЗнч(Элт.Представление) = Тип("Строка") Тогда
						ТекстыМассив.Добавить(Элт.Представление);
						МаппингИндексов.Добавить(Новый Структура("Уровень, ИмяПоля, ИндексЭлемента, ТипЗначения",
							"поле_массив", ИмяПоля, ЭлтИнд, "Представление"));
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;

	Если ТекстыМассив.Количество() = 0 Тогда
		РегексКонтекст.ЕстьТекстыДляRegex = Ложь;
		Возврат;
	КонецЕсли;

	РегексКонтекст.ТекстыJSON         = ЗаписатьJSON2(ТекстыМассив);
	РегексКонтекст.МаппингИндексов    = МаппингИндексов;
	РегексКонтекст.ЕстьТекстыДляRegex = Истина;
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьRegexТокеныОбъект(РегексКонтекст, СовпаденияJSON)
	Если НЕ ЗначениеЗаполнено(СовпаденияJSON) Тогда Возврат; КонецЕсли;

	РезСовпадений = Неопределено;
	Попытка
		РезСовпадений = ПрочитатьJSON2(СовпаденияJSON);
	Исключение
		Возврат;
	КонецПопытки;
	Если ТипЗнч(РезСовпадений) <> Тип("Массив") Тогда Возврат; КонецЕсли;

	МаппингИндексов = РегексКонтекст.МаппингИндексов;
	Данные = РегексКонтекст.Результат.data;
	Если ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;

	Для ТекстИнд = 0 По РезСовпадений.Количество() - 1 Цикл
		Если ТекстИнд >= МаппингИндексов.Количество() Тогда Прервать; КонецЕсли;
		ЭлСовпадений = РезСовпадений[ТекстИнд];
		Если ТипЗнч(ЭлСовпадений) <> Тип("Массив") Или ЭлСовпадений.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;

		МаппингЭлт = МаппингИндексов[ТекстИнд];
		Уровень    = "";
		МаппингЭлт.Свойство("Уровень", Уровень);
		ТипЗнчения = МаппингЭлт.ТипЗначения;

		// Получить текущее текстовое значение
		ТекущийТекст = Неопределено;
		Если Уровень = "поле" Тогда
			ЗначениеПоля = Неопределено;
			Если НЕ Данные.Свойство(МаппингЭлт.ИмяПоля, ЗначениеПоля) Тогда Продолжить; КонецЕсли;
			Если ТипЗнчения = "Представление" Тогда
				Если ТипЗнч(ЗначениеПоля) = Тип("Структура") И ЗначениеПоля.Свойство("Представление") Тогда
					ТекущийТекст = ЗначениеПоля.Представление;
				ИначеЕсли ТипЗнч(ЗначениеПоля) = Тип("Соответствие") Тогда
					ТекущийТекст = ЗначениеПоля["Представление"];
				КонецЕсли;
			Иначе
				Если ТипЗнч(ЗначениеПоля) = Тип("Строка") Тогда
					ТекущийТекст = ЗначениеПоля;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли Уровень = "тч" Тогда
			МассивТЧ = Неопределено;
			Если НЕ Данные.Свойство(МаппингЭлт.ИмяТЧ, МассивТЧ) Тогда Продолжить; КонецЕсли;
			Если ТипЗнч(МассивТЧ) <> Тип("Массив") Тогда Продолжить; КонецЕсли;
			Если МаппингЭлт.ИндексСтроки >= МассивТЧ.Количество() Тогда Продолжить; КонецЕсли;
			СтрокаТЧ = МассивТЧ[МаппингЭлт.ИндексСтроки];
			Если ТипЗнч(СтрокаТЧ) = Тип("Структура") Тогда
				ЗначениеТЧ = Неопределено;
				Если НЕ СтрокаТЧ.Свойство(МаппингЭлт.ИмяКолонки, ЗначениеТЧ) Тогда Продолжить; КонецЕсли;
			ИначеЕсли ТипЗнч(СтрокаТЧ) = Тип("Соответствие") Тогда
				ЗначениеТЧ = СтрокаТЧ[МаппингЭлт.ИмяКолонки];
			Иначе
				Продолжить;
			КонецЕсли;
			Если ТипЗнчения = "Представление" Тогда
				Если ТипЗнч(ЗначениеТЧ) = Тип("Структура") И ЗначениеТЧ.Свойство("Представление") Тогда
					ТекущийТекст = ЗначениеТЧ.Представление;
				ИначеЕсли ТипЗнч(ЗначениеТЧ) = Тип("Соответствие") Тогда
					ТекущийТекст = ЗначениеТЧ["Представление"];
				КонецЕсли;
			Иначе
				Если ТипЗнч(ЗначениеТЧ) = Тип("Строка") Тогда
					ТекущийТекст = ЗначениеТЧ;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли Уровень = "поле_массив" Тогда
			МассивПоля = Неопределено;
			Если НЕ Данные.Свойство(МаппингЭлт.ИмяПоля, МассивПоля) Тогда Продолжить; КонецЕсли;
			Если ТипЗнч(МассивПоля) <> Тип("Массив") Тогда Продолжить; КонецЕсли;
			Если МаппингЭлт.ИндексЭлемента >= МассивПоля.Количество() Тогда Продолжить; КонецЕсли;
			Элт = МассивПоля[МаппингЭлт.ИндексЭлемента];
			Если ТипЗнчения = "Представление" Тогда
				Если ТипЗнч(Элт) = Тип("Структура") И Элт.Свойство("Представление") Тогда
					ТекущийТекст = Элт.Представление;
				КонецЕсли;
			Иначе
				Если ТипЗнч(Элт) = Тип("Строка") Тогда ТекущийТекст = Элт; КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Если ТипЗнч(ТекущийТекст) <> Тип("Строка") Тогда Продолжить; КонецЕсли;

		// Применяем совпадения справа налево
		НовыйТекст = ТекущийТекст;
		Для Каждого С Из ЭлСовпадений Цикл
			Категория = ""; МатчТекст = ""; Начало = 0; Длина = 0;
			Если ТипЗнч(С) = Тип("Структура") Тогда
				С.Свойство("category", Категория);
				С.Свойство("match",    МатчТекст);
				С.Свойство("start",    Начало);
				С.Свойство("length",   Длина);
			ИначеЕсли ТипЗнч(С) = Тип("Соответствие") Тогда
				Категория = С["category"];
				МатчТекст = С["match"];
				Начало    = С["start"];
				Длина     = С["length"];
			КонецЕсли;
			Если МатчТекст = Неопределено Тогда МатчТекст = ""; КонецЕсли;
			Если Начало    = Неопределено Тогда Начало    = 0;  КонецЕсли;
			Если Длина     = Неопределено Тогда Длина     = 0;  КонецЕсли;
			Если Категория = Неопределено Тогда Категория = ""; КонецЕсли;
			Если ПустаяСтрока(МатчТекст) Или Длина <= 0 Тогда Продолжить; КонецЕсли;
			Токен = АнонимизаторТокенизировать(МатчТекст, Категория);
			Если Начало >= 0 И (Начало + Длина) <= СтрДлина(НовыйТекст) Тогда
				НовыйТекст = Лев(НовыйТекст, Начало) + Токен + Сред(НовыйТекст, Начало + Длина + 1);
			КонецЕсли;
		КонецЦикла;

		// Записать обратно в объект
		Если Уровень = "поле" Тогда
			Если ТипЗнчения = "Представление" Тогда
				ЗначениеПоля2 = Неопределено;
				Данные.Свойство(МаппингЭлт.ИмяПоля, ЗначениеПоля2);
				Если ТипЗнч(ЗначениеПоля2) = Тип("Структура") Тогда
					ЗначениеПоля2.Вставить("Представление", НовыйТекст);
				ИначеЕсли ТипЗнч(ЗначениеПоля2) = Тип("Соответствие") Тогда
					ЗначениеПоля2["Представление"] = НовыйТекст;
				КонецЕсли;
			Иначе
				Данные.Вставить(МаппингЭлт.ИмяПоля, НовыйТекст);
			КонецЕсли;
		ИначеЕсли Уровень = "тч" Тогда
			МассивТЧОбр = Неопределено;
			Данные.Свойство(МаппингЭлт.ИмяТЧ, МассивТЧОбр);
			Если ТипЗнч(МассивТЧОбр) <> Тип("Массив") Тогда Продолжить; КонецЕсли;
			Если МаппингЭлт.ИндексСтроки >= МассивТЧОбр.Количество() Тогда Продолжить; КонецЕсли;
			СтрокаТЧОбр = МассивТЧОбр[МаппингЭлт.ИндексСтроки];
			Если ТипЗнчения = "Представление" Тогда
				ЗначениеТЧОбр = Неопределено;
				Если ТипЗнч(СтрокаТЧОбр) = Тип("Структура") Тогда
					СтрокаТЧОбр.Свойство(МаппингЭлт.ИмяКолонки, ЗначениеТЧОбр);
					Если ТипЗнч(ЗначениеТЧОбр) = Тип("Структура") Тогда
						ЗначениеТЧОбр.Вставить("Представление", НовыйТекст);
					КонецЕсли;
				ИначеЕсли ТипЗнч(СтрокаТЧОбр) = Тип("Соответствие") Тогда
					ЗначениеТЧОбр = СтрокаТЧОбр[МаппингЭлт.ИмяКолонки];
					Если ТипЗнч(ЗначениеТЧОбр) = Тип("Структура") Тогда
						ЗначениеТЧОбр.Вставить("Представление", НовыйТекст);
					ИначеЕсли ТипЗнч(ЗначениеТЧОбр) = Тип("Соответствие") Тогда
						ЗначениеТЧОбр["Представление"] = НовыйТекст;
					КонецЕсли;
				КонецЕсли;
			Иначе
				Если ТипЗнч(СтрокаТЧОбр) = Тип("Структура") Тогда
					СтрокаТЧОбр.Вставить(МаппингЭлт.ИмяКолонки, НовыйТекст);
				ИначеЕсли ТипЗнч(СтрокаТЧОбр) = Тип("Соответствие") Тогда
					СтрокаТЧОбр[МаппингЭлт.ИмяКолонки] = НовыйТекст;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли Уровень = "поле_массив" Тогда
			МассивПоляОбр = Неопределено;
			Данные.Свойство(МаппингЭлт.ИмяПоля, МассивПоляОбр);
			Если ТипЗнч(МассивПоляОбр) <> Тип("Массив") Тогда Продолжить; КонецЕсли;
			Если МаппингЭлт.ИндексЭлемента >= МассивПоляОбр.Количество() Тогда Продолжить; КонецЕсли;
			ЭлтОбр = МассивПоляОбр[МаппингЭлт.ИндексЭлемента];
			Если ТипЗнчения = "Представление" Тогда
				Если ТипЗнч(ЭлтОбр) = Тип("Структура") И ЭлтОбр.Свойство("Представление") Тогда
					ЭлтОбр.Вставить("Представление", НовыйТекст);
				КонецЕсли;
			Иначе
				МассивПоляОбр[МаппингЭлт.ИндексЭлемента] = НовыйТекст;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры
