# Пакетный запрос «Сводка» формы элемента ХранилищаКонфигураций

Диаграммы построения пакетного запроса из `Catalogs/ХранилищаКонфигураций/Forms/ФормаЭлемента/Ext/Form/Module.bsl:320-412`.

## 1. Диаграмма построения запроса (поток пакета)

```mermaid
flowchart TB
    P0["ВЫБРАТЬ 1 - КОЛИЧЕСТВО РАЗЛИЧНЫХ Ссылка, ГДЕ Владелец = &Хранилище"]
    P1["ВЫБРАТЬ 2 - КОЛИЧЕСТВО, Состояние = ВерсияПомещена"]
    P2["ВЫБРАТЬ 3 - КОЛИЧЕСТВО, Состояние В: МетаданныеЗагружены, НачалоКоммита"]
    P3["ВЫБРАТЬ 4 - КОЛИЧЕСТВО, НЕ Состояние В: Пустая, НачалоКоммита, ВерсияПомещена"]

    subgraph ВТ["Временные таблицы"]
        T1["ПОМЕСТИТЬ ОбщаяСкорость - СостоянияВерсии.СрезПоследних, ВНУТРЕННЕЕ СОЕДИНЕНИЕ по ВерсияХранилища, ГДЕ Состояние = ВерсияПомещена, счётчик + МИН/МАКС Период"]
        T2["ПОМЕСТИТЬ ТекущаяСкорость - тоже + Период >= &ПредыдущаяДата (24ч)"]
    end

    P4["ВЫБРАТЬ 5 - Скорость = Количество / РАЗНОСТЬДАТ Начало-Окончание ЧАС, защита от деления на 0"]
    P5["ВЫБРАТЬ 6 - Скорость за последние сутки"]

    V["Справочник.ВерсииХранилища"]
    R["РегистрСведений.СостоянияВерсии"]

    V --> P0 --> R1["Результат 0 = ВсегоВерсий"]
    V --> P1 --> R2["Результат 1 = КоличествоКоммитов"]
    V --> P2 --> R3["Результат 2 = КоличествоВерсийОбработано"]
    V --> P3 --> R4["Результат 3 = контрольное"]
    V --> T1
    R --> T1
    T1 --> P4 --> R5["Результат 4 = ОбщаяСкорость"]
    V --> T2
    R --> T2
    T2 --> P5 --> R6["Результат 5 = ТекущаяСкорость"]

    Q["Запрос.ВыполнитьПакет()"]
    R1 --> Q
    R2 --> Q
    R3 --> Q
    R4 --> Q
    R5 --> Q
    R6 --> Q
```

## 2. Диаграмма сущность-связь (ER)

```mermaid
erDiagram
    ХранилищаКонфигураций ||--o{ ВерсииХранилища : владелец
    ВерсииХранилища ||--o{ СостоянияВерсии : измерение

    ХранилищаКонфигураций {
        uuid Ссылка PK "идентификатор хранилища"
    }

    ВерсииХранилища {
        uuid Ссылка PK "идентификатор версии"
        string Код "номер версии"
        uuid Владелец FK "ХранилищаКонфигураций"
        СостоянияВерсииПеречисление Состояние "реквизит-состояние"
        string КаталогВременныхФайлов "рабочий каталог"
        uuid Источник FK "Хранилище/Очередь/Копия"
        boolean ВыгрузкаИзменений
        int КоличествоМетаданных
        string Хеш
    }

    СостоянияВерсии {
        uuid ВерсияХранилища PK, FK "измерение"
        date Период PK "секунда - граница состояния"
        СостоянияВерсииПеречисление Состояние "ресурс"
    }
```

## 3. Что связывает запрос с этими сущностями

- Запрос обращается к `Справочник.ВерсииХранилища` (4 счётчика) и к `РегистрСведений.СостоянияВерсии.СрезПоследних` (2 скорости).
- В `СрезПоследних` попадают записи регистра на дату среза (периодичность — секунда), поэтому для каждого момента известно актуальное состояние версии.
- Условие `&ПредыдущаяДата = ТекущаяДатаСеанса() - 86400` ограничивает период «ТекущаяСкорость» последними сутками.
- Защита от деления на ноль: если между началом и окончанием 0 часов (`РАЗНОСТЬДАТ(..., ЧАС) = 0`) — скорость принимается 0.