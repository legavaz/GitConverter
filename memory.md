# Памятка по проекту GitConverter

## Проект
- Каталог: `E:\project\0509 git converter`
- Репозиторий: https://github.com/legavaz/GitConverter.git, ветка `lega-master`
- БД разработки: файловая `E:\file_bases\git_converter` (id `dev` в `.v8-project.json`, default)
- Платформа: `C:\Program Files\1cv8\8.3.27.2214\bin\1cv8.exe`
- Инструменты BSL: `C:\tools\bsl\bsl-language-server.jar` + Java `C:\Program Files\Axiom\AxiomJRE-Pro-25-Full\bin\java.exe`
- MCP `code-index`: repo `main`, root `E:\project\0509 git converter\cfg_src`
- MCP `bsl-language-server` (добавлен 23.09.2026): stdio, `java -jar C:\tools\bsl\bsl-language-server.jar mcp -c <конфиг проекта>`; BSL LS 1.0.7 (последний релиз), Java 25 AxiomJRE. Инструменты: analyze_file, document_symbols, find_references, call_hierarchy, hover, definition, type_info, global_member_info, global_member_search, type_at_position. Рабочие пространства — через MCP roots от клиента. НЕ проверяет синтаксис текстов запросов 1С внутри `Новый Запрос("...")` — ловит только BSL-синтаксис.
- Конфиг opencode: `.opencode/opencode.json` (агенты plan/build, MCP code-index)

## ВАЖНО
- 1С-MCP сервер подключён к ДРУГОЙ базе («ДокументооборотКОРП»), фоновые задания базы `git_converter` через него проверить нельзя.
- BSL-анализ: `java -jar ... analyze -c "E:\project\0509 git converter\.bsl-language-server.json" -s "E:\project\0509 git converter\cfg_src" -r json -o "<outDir>"` (отчёт `bsl-json.json`).
- Расширения (CFE) в `.v8-project.json` НЕ зарегистрированы — при работе с расширением нужно уточнять базу/имя у пользователя.

## Документы в корне проекта
- `BLOCKSCHEME.md` — блок-схема и полное описание пакетных скриптов конвертации (создан). Mermaid-схема + таблица скриптов (CREATEINFOBASE, ConfigurationRepositoryReport, ConfigurationRepositoryUpdateCfg, DumpConfigToFiles, ring edt, git_command_N.bat/.sh, git rev-parse, скрипты инициализации репо/EDT) с координатами кода.
- `QUERY_SVODKA.md` — диаграммы построения самого большого запроса конфигурации (пакет из 8 запросов «Сводка» в `ХранилищаКонфигураций.ФормаЭлемента`): mermaid flowchart + ER-диаграмма сущностей (ВерсииХранилища, СостоянияВерсии).
- `memory.md` — этот файл.

## УСТАНОВЛЕННЫЕ СКИЛЛЫ (opencode, 21.09.2026)
- `.opencode/skills/` — 80 скиллов из `https://github.com/Nikolay-Shirokov/cc-1c-skills`, ветка `port-opencode` (официальный порт под opencode, PowerShell-рантайм).
- **+ `query-optimization` (23.09.2026)** — из `https://github.com/legavaz/skills` (форк msrv-tech/skills, «1C Codex skills»): справочник языка запросов 1С + оптимизация (SKILL.md + references/: details, functions-and-expressions, optimization-and-pitfalls, query-syntax-reference). Загружен вручную (не через restore.ps1), codex-специфичные agents/evals не копировались. Закрывает ошибку класса «СТРОКА() в запросе» (23.09.2026). Другие скиллы того репо НЕ ставить поверх установленных — конфликты имён (другая реализация).
- Остальное из `legavaz/skills` (не установлено): codex-test-bridge (HTTP API к базе 1С), validate/inspect, web-session/playwright-test/test-databases, full-cycle скиллы.
- Состав: cf-*, cfe-*, db-* (13 шт.), epf-*/erf-*, form-*, meta-*, mxl-*, role-* (вкл. role-edit), skd-*, subsystem-*, interface-*, xdto-*, web-*, support-edit, template-*, help-add, img-grid.
- В корневом `.gitignore` добавлено `.opencode/skills/` → скиллы НЕ попадают в репозиторий GitConverter (не смешиваем).
- `.opencode/opencode.json` (агенты plan/build + MCP code-index) НЕ тронут.
- Активация: нужен перезапуск opencode (скиллы сканируются при старте).
- Временный клон (источник) остался в `%TEMP%\opencode\cc-1c-skills` — можно удалить.

## ИСПРАВЛЕНО: аутентификация хранилища для временных баз (22.09.2026)
- **Проблема:** команды работы с хранилищем (`ConfigurationRepositoryReport`, `ConfigurationRepositoryUpdateCfg`) подключались к хранилищу под пользователем **хранилища** (`ХранилищаКонфигураций.ИмяПользователяХранилища`). Из-за этого все временные базы шли под одним пользователем — конфликт «Пользователь существующей связи отличается от текущего», параллельная работа невозможна.
- **Решение:** подключаться под пользователем, указанным в **элементе временной базы** (`ноВременныеБазы.ИмяПользователяХранилища`/`ПарольПользователяХранилища`).
- **Код** (`CommonModules/КонвертацияХранилища/Ext/Module.bsl`):
  - новая процедура `УстановитьПользователяХранилищаВременнойБазы(РеквизитыБазы, Параметры)` (после `ПараметрыПодключенияКХранилищу`);
  - вызов после выбора серверной базы в `ВыполнитьКонвертацию` (отчёт по версиям) и в `ПолучитьВерсиюВБазу` (получение версии).
- **Проверено на реальных данных** (хранилище `E:\rep\task`, 251 версия): отчёт построен базой `1ctmp_vremennaya2`/`user_commit2`; версия 1 получена базой `1ctmp_vremennaya`/`user_commit`. Обе — код 0.
- Загружено частично в базу с обновлением БД.

## ИСПРАВЛЕНО: конвейер конвертации версий (22.09.2026)
- **Строка соединения с серверной временной базой** (`ПолучитьПараметрыОбработкиВерсии`): к адресу сервера добавлялось имя кластера (`localhost\мой кластер`) → «Сервер 1С:Предприятия не обнаружен» при выгрузке. Теперь как в рабочих вызовах: `СтрШаблон("/S %1\%2", АдресСервера, ИмяБазы)`.
- **Пользователь ИБ для временной базы** (`ПолучитьИмяПользователяИБ`): возвращал пользователя СУБД (`postgres`) → «Пользователь ИБ не идентифицирован». Теперь возвращает пользователя кластера или пусто (временные базы — без пользователя ИБ).
- **Данные элемента `E:\rep\task`:** `АдресРепозиторияGit = https://github.com/legavaz/task_commit.git`, `ВыполнятьКоммиты = Да`, `КаталогВыгрузкиВРепозитории = ""` (было абсолютным путём → «Имя каталога содержит недопустимые символы»), `КонвертироватьВФорматEDT = Нет` (1C:EDT/`ring` не установлен).
- **Проверено:** версия 1 получена → выгружена в `src/` → закоммичена (`9b5662f`). **Push отклонён GitHub** (push protection) из-за секрета (Bitbucket PAT) в `src/Reports/jira_SD_DEV*/Ext/ObjectModule.bsl` — нужно разрешить секрет в настройках репо. Состояние версии 1 — «Начало коммита».
- Всего в хранилище 251 версия (все необработаны). Регламентное задание конвертации для элемента не создано (РегламентноеЗадание пустое).

## ТЕКУЩИЕ ОТКРЫТЫЕ ВОПРОСЫ (не завершены)

### 1. Кнопка «Запустить конвертацию» — добавлена проверка пустой ссылки (ЧАСТИЧНО ИСПРАВЛЕНО)
Место: `cfg_src/Catalogs/ХранилищаКонфигураций/Forms/ФормаЭлемента/Ext/Form/Module.bsl`
- `ЗапуститьКонвертацию` теперь проверяет `Объект.Ссылка` (пустая → предупреждение + Возврат) и перед запуском проверяет наличие свободных временных баз (`ПроверитьСвободныеВременныеБазыНаСервере`) — при отсутствии свободных предупреждение, запуск продолжается (fallback на файловый режим).
- НЕ доделано: `Записать()` при модифицированности (в отличие от `СоздатьРепозиторийGit`) и страховка в `ЗапуститьКонвертациюНаСервере`.

## СИНХРОНИЗАЦИЯ БАЗЫ С cfg_src (21.09.2026)
- Выполнена полная выгрузка `E:\file_bases\git_converter` → `cfg_src` (`/DumpConfigToFiles -Format Hierarchical`, `db-dump-xml -Mode Full`).
- В базе обнаружены/приняты ручные правки (Данилин ВИ.15/09/26): реквизиты `Хранилище`, `ИмяПользователяХранилища`, `ПарольПользователяХранилища` у `ноВременныеБазы`; `НайтиСвободную(Кластер, Хранилище)` фильтрует по хранилищу; справочник иерархический; кнопка «(Доработать) подключить к хранилищу» в форме базы.
- Загружено в базу: `db-load-git -Source All -UpdateDB` (12 файлов) — успешно, `/UpdateDBCfg` выполнен.

## ВЫПОЛНЕНО ИЗ «на доработку.txt» (21.09.2026)
1. Кнопка «Освободить временные базы» в форме `ХранилищаКонфигураций` (команда id=10, кнопка id=172 в AutoCommandBar; подтверждение Да/Нет; освобождает базы версий хранилища через `ноВременныеБазы.Освободить`, очищает `ВерсияХранилища.ВременнаяБаза`; сводка «Освобождено / Требуют пересоздания»).
   - **ФИКС (21.09.2026):** `ОсвободитьВременныеБазыНаСервере` искала занятые базы только через версии (`ВерсииХранилища.ВременнаяБаза <> Пустая`) и потому не находила базы, занятые без привязки к версии (например, `Занять(СервернаяБаза)` в КХ:79 при прерванной конвертации). Теперь источник истины — сам справочник `ноВременныеБазы` (`Хранилище = &Хранилище ИЛИ ВерсияХранилища.Владелец = &Хранилище`, `Состояние В (Занята, ТребуетПересоздания)`). Ссылки версий на освобождённую базу очищает новая процедура `ОчиститьВерсииСВременнойБазой`. Загружено частично в базу с обновлением БД (динамически).
2. Проверка свободных баз перед стартом конвертации: клиент (`ЗапуститьКонвертацию`) — предупреждение при отсутствии свободных, запуск продолжается; сервер (`ВыполнитьКонвертацию` и `ПолучитьВерсиюВБазу`) — `ЗаписьЖурналаРегистрации` (Предупреждение) + fallback на файловый режим.

### 2. Распаковка расширения при пустой конфигурации (обсуждалось, НЕ выполнялось)
- Платформа умеет выгружать только расширение: `1cv8 DESIGNER /F <база> /DumpConfigToFiles <каталог> -Extension "<Имя>"` или `ibcmd infobase config export --extension=<Имя> <каталог>` (или `export all-extensions`).
- Исходники ложатся в `<каталог>\Extensions\<Имя>\...`.
- Скилл `db-dump-xml` поддерживает `-Extension` и `-AllExtensions`.
- Нужно уточнить у пользователя: из какой базы, имя расширения, или распаковать `.cfe` через временную пустую базу.

## ВЫПОЛНЕНО ИЗ «на доработку.txt» (23.09.2026, загружено в `localhost\GitConv`)
Целевая база для загрузки — **серверная `localhost\GitConv`** (не файловая dev). Все 5 пунктов внесены в XML/BSL, загружены полной загрузкой + `/UpdateDBCfg` успешно. CheckConfig: 0 ошибок в правках (только предсуществовавшие help-замечания `РегламентныеИФоновыеЗадания`, не трогали).

1. **Кнопка «Сделать пользователя хранилища» + справочник `ноПользователиХранилища`** (новый каталог, реквизиты `Хранилище`, `ИмяПользователя`, `ПарольПользователя` (DontCheck), `Администратор` (Boolean, FillValue=xsi:nil)). Кнопка в командной панели формы `ноВременныеБазы.ФормаЭлемента`, команда id=3 (`Action` добавлен — фикс после form-validate), обработчики `СделатьПользователяХранилища`/`СделатьПользователяХранилищаНаСервере`; в КХ `ДобавитьПользователяХранилища` + регистрация в справочнике. **Фикс загрузки:** у `Администратор` был `<FillValue xsi:type="xs:boolean"/>` (пустой boolean) → «Ошибка XDTO»; заменено на `<FillValue xsi:nil="true"/>`.
2. **Кнопка «Обнулить счётчик временных баз»** в форме `ХранилищаКонфигураций.ФормаЭлемента` (AutoCommandBar кнопка id=174/175, команда id=11, обработчики `ОбнулитьСчетчикВременныхБаз`/`Завершение`/`ОбнулитьСчетчикВременныхБазНаСервере`; запрос по `ноВременныеБазы` где `Хранилище = &Хранилище` И `КоличествоИспользований <> 0` → обнуление + сводка).
3. **Флаг блокировки «Остановить задания конвертации»** — Boolean реквизит `ОстановитьКонвертацию` (uuid `1c30f971-789c-4fbc-9bf0-3592d8f9254e`) у `ХранилищаКонфигураций` + чекбокс id=193/194/195 в форме; в КХ `ВыполнитьКонвертацию` (после проверки заполненности): `ЗначенияРеквизитовОбъекта(Хранилище, "ОстановитьКонвертацию")`, при Истина — `ЗаписьЖурналаРегистрации("КХ.ноВыполнитьКонвертацию.Остановлена", Предупреждение)` + `Возврат` (останавливает и ручной запуск, и регл. задание `КонвертацияХранилища`).
4. **Переименование: «имя пользователя» → «имя пользователя админ хранилища»** — синоним реквизита `ИмяПользователяХранилища` в `ХранилищаКонфигураций.xml` + Title поля на форме (`ФормаЭлемента`) для консистентности UI.
5. **Форма списка «временные базы»: колонка «КоличествоИспользований»** — LabelField (DataPath `Список.КоличествоИспользований`, id 34/35/36) в `ноВременныеБазы.ФормаСписка` после `ДатаПоследнегоОсвобождения`.

UUID-запас (генерились, часть не использована): `2e31437c-6c34-4b35-b935-f677021bb445`, `07b8693a-3ae1-4ec2-8158-2d38dab2a4ea`.
ID на форме хранилища теперь: кнопки до 175, чекбокс 193/194/195 (свободны 176–192 и выше 195).

**ФИКС (23.09.2026):** ошибка «Синтаксическая ошибка "СТРОКА(Пользователи.ИмяПользователя)"» в `ДобавитьПользователяХранилища` (КХ:2125–2127) — в языке запросов нет `НЕ ПУСТАЯ СТРОКА(...)`. Заменено на сравнения `ИмяПользователя <> ""` / `ПарольПользователя <> ""` / `ИмяБазыНаСервере <> ""`. Загружено частично в `localhost\GitConv` (динамическое обновление). NB: `НЕ ПУСТАЯ` в запросах — всегда ошибка; проверка пустой строки в запросе — только `<> ""`.
**Аудит обработчиков (23.09.2026):** все добавленные обработчики/запросы проверены (BSL LS + вручную): 8 запросов корректны. Исправлено: `Знач` у параметров клиент-серверной `СделатьПользователяХранилищаНаСервере`, перенос длинной строки. Остальное — ложные срабатывания (UnusedLocalMethod для form-хендлеров — норма). Загружено в GitConv.
**NB память:** BSL LS не имеет собственной настройки памяти (ни в конфиге, ни в параметрах запуска) — только JVM-флаги. Настройка (23.09.2026): `-Xmx2g -Xms256m -XX:+UseSerialGC` добавлены в команду MCP в `.opencode/opencode.json` и в `C:\tools\bsl\bsl.cmd` (там же зафиксирован полный путь к AxiomJRE java). Дефолт JVM (¼ RAM = до 8 ГБ) приводил к разрастанию java-процесса до ~2.7 ГБ и ошибкам «Недостаточно памяти» у 1cv8 DESIGNER / analyze при параллельной работе. При ошибках памяти: остановить java-процесс MCP (`Stop-Process -Name java`).

## ЗАВЕРШЁННЫЕ ПРАВКИ (загружены в базу `E:\file_bases\git_converter`)

### 1. `СтроковыеФункцииКлиентСервер` — функция `ТранслитерироватьСтроку`
Кириллица → латиница (упрощённо), пробелы/спецсимволы → `_`, результат в нижнем регистре.
Файл: `cfg_src/CommonModules/СтроковыеФункцииКлиентСервер/Ext/Module.bsl` (секция ПрограммныйИнтерфейс).

### 2. Форма `ноВременныеБазы.ФормаЭлемента` — авто-имя базы
- При изменении «Наименование» или «Кластер» автоматически заполняется «Имя базы на сервере» = `Префикс_транслит` (префикс из `ноКластеры.ПрефиксИмениБазы`, транслитерируется, нижний регистр).
- Файлы: `cfg_src/Catalogs/ноВременныеБазы/Forms/ФормаЭлемента/Ext/Form.xml` (+ `OnChange`), `Ext/Form/Module.bsl` (`ЗаполнитьИмяБазыНаСервере`).

### 3. Кнопка «Создать базу» в командной панели
- Перенесена в верхнюю командную панель формы `ноВременныеБазы.ФормаЭлемента` (тип `CommandBarButton` внутри `<AutoCommandBar>`).
- Образец: `Catalogs/ВерсииХранилища/Forms/ФормаЭлемента/Ext/Form.xml`.

### 4. `ноСозданиеБазы` — убрано дублирование префикса
- Удалена `ПолноеИмяБазы`, убрано чтение `ПрефиксИмениБазы`. Префикс теперь в `ИмяБазыНаСервере`, CREATEINFOBASE использует `Параметры.ИмяБазы`.

## Ранее (история)
- Фикс логирования в `ноСозданиеБазы`: `ЗаписьЖурналаРегистрации(...)` (функция).
- `СтрокаСозданияСервернойБазы`: `CrSQLDB="Y"`, `SchJobDn="N"`, `DBMS`/`DB` в кавычках, `/AddInList` — по рабочему примеру `_tmp_create_ib2.ps1` (решало «database wb1 does not exist»).
- `.bsl-language-server.json` создан; `.gitignore` обновлён (`mcp-server.log.*`, `cfg_src/.code-index/`, `_tmp_*.bat`, `*.log`). `.opencode/` не коммитится.
- Коммит `318773f` запушен в `lega-master` (до последних правок).

## Ключевые координаты в `CommonModules/КонвертацияХранилища/Ext/Module.bsl` (КХ)
- `ВыполнитьКонвертацию` — КХ:20; `ОбработатьВерсию` — КХ:1080; `ПолучитьВерсиюВБазу` — КХ:2935.
- `ВыгрузитьВерсию` — КХ:1265; `ЗагрузитьВерсию` — КХ:1473; `ИмпортироватьВерсиюВEDT` — КХ:1495.
- `СформироватьФайлыGitНаСервере` — КХ:2392; `ЗаписатьФайлыКоммитаВерсии` — КХ:3342.
- `ВыполнитьКоммиты` — КХ:566; `ЗапуститьКоммитыВФоне` — КХ:538.
- Пакетные операции: `СоздатьФайловуюИнформационнуюБазу` КХ:2497; `СформироватьОтчетПоВерсиямХранилища` ~КХ:2700; `ОбновитьИнформационнуюБазуИзХранилища` КХ:2770; `ВыгрузитьКонфигурациюВФайлы` КХ:2853.
## [2026-09-23] MCP_Toolkit: автозапуск MCP-сервера при открытии формы
- В `cfg_src\MCP_Toolkit_src` (исходники обработки MCPToolkit перенесены сюда из корня) в конец `ПриОткрытии` (Форма/Ext/Form/Module.bsl, ~L611) добавлен блок `AUTO-START MCP SERVER ON FORM OPEN`: вызов `Подключиться(Неопределено)` — эмуляция кнопки «Подключиться».
- Пересобран `MCP_Toolkit.epf` (epf-build). При открытии обработки в `localhost\GitConv` embedded-сервер стартует сам: `http://localhost:6003/health` → healthy, MCP `initialize`/`tools/list` работают (1C MCP Toolkit Native 1.0.0).
- Ошибка первого варианта: имя флага подключения — `ФлагПодключения` (Перем L12), НЕ `ПодключениеАктивно` (несуществующее имя ломало компиляцию модуля).

## [2026-09-23 21:38] Отменил 4 пункта тех.долга

- ❌ «Заполнить `ИнформацияПользователей` (Email → пользователь)»
- ❌ «Доделать форму ноВременныеБазы (тест «Сделать пользователя хранилища»)»
- ❌ «Включить расписание `ОбработкаОчереди` (period=0 — нужно настроить)»
- ❌ «Настроить EDT-формат (`КонвертироватьВФорматEDT = Ложь`)»
- ❌ «Создать `1ctmp_vremennaya2`/`user_commit2` (нет свободных)»

Pipeline живёт (ОбработкаОчереди включена), слежу только за прогоном.


## [2026-09-24] Сверка `cfg_src` и `localhost\GitConv`

**Методика:** `/S localhost/GitConv /DumpConfigToFiles` во временную папку; `git diff --ignore-cr-at-eol` против `cfg_src/gitConverter/`.

**Результат ДО правки:**
- 1 файл расходился: `CommonModules/КонвертацияХранилища/Ext/Module.bsl`
- В	cfg_src строки L820–824 (старый 816): помечены `//DVI 23/09/26 / //было`, активна строка `Объект.КоличествоМетаданных = Объект.Состав.Количество()`; в базе — старая логика без правки.
- Загружено: `db-load-xml -Mode Partial -Files 'CommonModules/КонвертацияХранилища.xml' -UpdateDB` (конфигурация обновлена **динамически**).
- После загрузки: **`git diff = 0` строк**, 151 файл = 151, база и cfg_src полностью синхронны.

**Вывод:** правка не была загружена в прошлой сессии (ложная пометка «cfg_src синхронизирован» в memory.md). Исправлено.

