# Контекст задачи: серверные временные базы ГитКонвертера

> **Обновлено:** 2026-09-21 (текущее состояние — см. «Статус»)
> Файл-контекст задачи. Дополняет `memory.md` (общие сведения/процедуры) и `modules-analysis.md`
> (разбор модулей). Сюда — только по текущей задаче: цель, решения, сделанное, блокеры, следующий шаг.

> ⚠️ Разделы ниже частично устарели: имена объектов теперь с префиксом «но»
> (ноВременныеБазы, ноКластеры, ноТипыСУБД, ноСостоянияВременныхБаз);
> блокер загрузки cfg_src (раздел 5) РЕШЁН — см. раздел 6/7. Актуальное состояние — в `memory.md`.

---

## 1. Цель задачи

В конфигурации **«ГитКонвертер»** (`cfg_src`) заменить временные **файловые** ИБ на
**серверные** (нет клиентской лицензии) через новые справочники «ВременныеБазы» и «Кластеры»
(СУБД MSSQL/PostgreSQL), с пулом **«только занятие/освобождение»** и поддержкой обоих режимов
(файл/сервер) на уровне хранилища: **и рабочая ИБ версии, и отчётная ИБ (report_db)**,
с fallback на файловый режим.

---

## 2. Решения пользователя

1. Серверную ИБ создавать **пустой**, без шаблона/эталона.
2. ЖЦ пула — **только занятие/освобождение**, без DROP/пересоздания (в окружении нет ibcmd).
3. Объём — оба режима: рабочая ИБ версии + отчётная ИБ.

**Модель пула:** серверные БД **предсозданы** (количество = `КоличествоВременныхБаз` кластера);
конвертер их **не создаёт** (CREATEINFOBASE в серверной ветке не вызывается), только
занимает/освобождает.

---

## 3. Сделанное в `cfg_src`

### Метаданные
- Справочники: `ВременныеБазы`, `Кластеры`.
- Перечисления: `ТипыСУБД` (MSSQL/PostgreSQL), `СостоянияВременныхБаз`
  (Свободна/Занята/ТребуетПересоздания).
- Реквизиты `ХранилищаКонфигураций`: `ИспользоватьСерверныеВременныеБазы`, `КластерВременныхБаз`.
- Реквизит `ВерсииХранилища`: `ВременнаяБаза`.

### Код
- `Catalogs\ВременныеБазы\Ext\ManagerModule.bsl` (новый): `НайтиСвободную`, `Занять`,
  `Освободить` (не освобождает «ТребуетПересоздания»), `ПометитьПересоздание`.
- `CommonModules\КонвертацияХранилища\Ext\Module.bsl`:
  - `ПолучитьКластерВременныхБаз`, `ПараметрыПодключенияККластеру`, `ПолучитьИмяПользователяИБ`,
    `СтрокаСозданияСервернойБазы`;
  - `ПолучитьВерсиюВБазу` — серверная ветка (найти свободную → занять → `/S"..."` → обновить
    из хранилища; CREATEINFOBASE не выполняется), fallback на файловую;
  - отчётная ИБ (`report_db`) в `ВыполнитьКонвертацию` — через пул с занятием/освобождением;
    путь отчёта в серверном режиме `КаталогВременныхФайлов + "Report.mxl"`;
  - `ПолучитьПараметрыОбработкиВерсии`/`ВыгрузитьВерсию` — сохраняют серверную строку соединения
    из реквизита версии;
  - `УдалитьФайлыВерсии`/`ПроверитьПолученныеВерсии` — освобождение базы.

### Формы
- `Кластеры`: ФормаЭлемента + ФормаСписка.
- `ВременныеБазы`: ФормаЭлемента + ФормаСписка.
- `ХранилищаКонфигураций.ФормаЭлемента`: добавлена группа «Серверные временные базы»
  (флаг `ИспользоватьСерверныеВременныеБазы` + поле `КластерВременныхБаз`).
- Дефолты на сервере (`ПриСозданииНаСервере`): Кластеры → ТипСУБД=MSSQL,
  КоличествоВременныхБаз=5, ИспользоватьДляВременныхБаз=Истина; ВременныеБазы → Состояние=Свободна.
- PasswordMode=true у полей паролей в форме Кластеры.

### Прочее
- `.v8-project.json` (корень проекта): `editingAllowedCheck: "off"` — обход `[support-guard]`
  (конфигурация пришла из вендорского `.cf`, возможность изменения выключена).

---

## 4. Валидация (пройдено)

- `cf-validate` — OK.
- `form-validate` по всем изменённым/новым формам — OK (в `ХранилищаКонфигураций.ФормаЭлемента`
  только несвязанный pre-existing warn про AutoCommandBar id=37).

---

## 5. Блокер: загрузка `cfg_src` в базу (НЕ решено)

### Окружение (факты)
- База ГитКонвертера: **`E:\file_bases\git_converter`** (НЕ `C:\1c_base\GK` — такого пути нет).
- Платформа: **`C:\Program Files\1cv8\8.3.27.2214\bin\1cv8.exe`**.
- Эталон из базы: `cfg_ref` (выгружен `/DumpConfigToFiles`), грузится успешно (ExitCode=0).

### Симптомы и найденные причины
- `cfg_src` не грузится (`/LoadConfigFromFiles -Format Hierarchical`, ExitCode=1).
- **Найдено/исправлено:** `Catalogs\ВерсииХранилища.xml` — дублировался тег `<TabularSection>`
  (2 подряд); удалён лишний, файл сбалансирован.
- **Ошибки ссылок на новые объекты:**
  - `Объект не найден - CatalogRef.ВременныеБазы` — `ВерсииХранилища.xml` ссылается на новый
    справочник `ВременныеБазы` (в `Configuration.xml` он стоял после; переставил перед
    `ВерсииХранилища` в `cfg_src\Configuration.xml`).
  - `Объект не найден - CatalogRef.Кластеры` — `ХранилищаКонфигураций.xml` (`КластерВременныхБаз`)
    ссылается на новый `Кластеры`.

### Гипотеза блокера
Ограничение Designer `LoadConfigFromFiles`: ссылка на **новый** объект из существующего в рамках
**одного** прохода не резолвится (в `cfg_ref` cross-ссылки между существующими объектами работают).
**Обход — двухфазная загрузка:**
- Фаза 1: только новые объекты (справочники `ВременныеБазы`/`Кластеры`, перечисления
  `ТипыСУБД`/`СостоянияВременныхБаз`) без правок существующих.
- Фаза 2: полный `cfg_src`.

**Важное расхождение, требующее разбирательства:** тест «ref + новые объекты без правок
существующих» по «чистому» Configuration.xml дал ExitCode=1 (общая «Ошибка преобразования
данных XML»), тогда как ранний тест (test4) с «грязной» Configuration.xml (Set-Content,
литеральные `\n`) давал ExitCode=0. Причина расхождения не установлена; чистая серия тестов
(сравнение полей ID/типов нового справочника, структуры форм, enum InternalInfo) была прервана
пользователем.

---

## 6. Статус

- [x] Метаданные (справочники, перечисления, реквизиты)
- [x] Модули (`ManagerModule.bsl`, `КонвертацияХранилища`)
- [x] Формы (Кластеры, ВременныеБазы, правка ФормаЭлемента ХранилищаКонфигураций)
- [x] Валидация (cf-validate, form-validate)
- [x] Исправление `ВерсииХранилища.xml` (лишний `<TabularSection>`)
- [x] Порядок `<Catalog>` в `Configuration.xml` (ВременныеБазы перед ВерсииХранилища)
- [ ] **Загрузка `cfg_src` в `E:\file_bases\git_converter` + компиляция модулей + проверка** — блокер

---

## 7. Следующий шаг

1. Разобраться с расхождением «чистого» vs «грязного» Configuration.xml при загрузке только
   новых объектов (проверить почленно: один справочник без перечислений → +перечисления → +формы;
   сравнить структуру нового справочника с существующим `КопииХранилищКонфигурации`).
2. Реализовать **двухфазную загрузку** (Фаза 1 = новые объекты, Фаза 2 = полный `cfg_src`).
3. После успешной загрузки — `/UpdateDBCfg`, проверка компиляции модулей
   (`КонвертацияХранилища`, `ManagerModule.bsl`) и открытия новых форм.
4. Почистить отладку: `cfg_ref`, `cfg_phase1`, `cfg_probe`, `cfg_test*`/`cfg_t*`, `load_*.log`,
   `dump_*.log` (после завершения проверки).

---

## 8. Ключевые файлы

- `cfg_src\CommonModules\КонвертацияХранилища\Ext\Module.bsl` — функциональность серверных ИБ.
- `cfg_src\Catalogs\ВременныеБазы\Ext\ManagerModule.bsl` — методы пула.
- `cfg_src\Catalogs\Кластеры.xml`, `cfg_src\Catalogs\ВременныеБазы.xml`,
  `cfg_src\Enums\ТипыСУБД.xml`, `cfg_src\Enums\СостоянияВременныхБаз.xml` — метаданные.
- `cfg_src\Catalogs\ВерсииХранилища.xml` — добавлен реквизит `ВременнаяБаза` (исправлен дубль тега).
- `cfg_src\Catalogs\ХранилищаКонфигураций.xml` + `Forms\ФормаЭлемента\Ext\Form.xml` — группа базы.
- `cfg_src\Configuration.xml` — порядок `<Catalog>` (ВременныеБазы перед ВерсииХранилища).
- `.v8-project.json` — обход support-guard.
- `cfg_ref\` — эталон исходников из базы (для диффа).
