# Анализ модулей конфигурации «1С:ГитКонвертер»

> **Файл-источник:** `ГитКонверер1Cv8.cf` (284 012 байт)
> **База (выгрузка):** `C:\1c_base\GK`
> **Исходники (DumpConfigToFiles):** `C:\project\0509\cfg_src`
> **Дата:** 05.09.2026

---

## 1. Идентификация и назначение

| Свойство | Значение |
|---|---|
| Имя конфигурации | `ГитКонвертер` |
| Синоним | «1С:ГитКонвертер, ред. 1.0» |
| Версия | **1.0.5.8** |
| Поставщик | Фирма «1С» (ООО «1С-Софт») |
| UUID | `a8dd2e89-c043-4f61-ab58-dafb034cd4c1` |
| Совместимость | Версия платформы 8.3.12; совместимость расширений 8.3.27 |
| Лицензия кода | CC BY-SA 4.0 International |

**Назначение:** автоматизация переноса истории хранилища разработки 1С
(configuration storage) в **Git**-репозиторий. В исходниках ядро описано как
«Серверные процедуры и функции общего назначения для "Конвертации хранилища в Git"».

Платформа: управляемые формы (Taxi), синхронные вызовы разрешены, блокировки
управляемые — ядро обращается к внешним процессам (`ЗапуститьПриложение`) и запускает
командные файлы Git.

---

## 2. Структура метаданных (объектный состав)

**Итого объектов:** 38 (включая 1 язык `Русский`, 1 элемент стиля, 2 общие картинки).

| Тип | Кол-во | Объекты |
|---|---|---|
| Общие модули | **14** | КонвертацияХранилища, ОбработкаОчередей + 12 библиотечных (БСП) |
| Справочники | **4** | ВерсииХранилища, ХранилищаКонфигураций, ОчередиВыполнения, КопииХранилищКонфигурации |
| Перечисления | **2** | СостоянияВерсии, ОперацииОчереди |
| Регистры сведений | **2** | ИнформацияПользователей, СостоянияВерсии |
| Регламентные задания | **3** | КонвертацияХранилища, ВыгрузкаВерсииИзКопииХранилища, ОбработкаОчереди |
| Обработки | **2** | КонвертацияВФорматEDT, РегламентныеИФоновыеЗадания |
| Отчёты | **1** | СкоростьКонвертации (СКД, без программного модуля) |
| Функциональные опции | **1** | ИспользоватьОчередиВыполнения |
| Константы | **2** | ИспользоватьОчередиВыполнения, ПутьКВерсиямПлатформыНаСервере |
| Роли | **3** | ПолныеПрава, АдминистраторСистемы, ИнтерактивноеОткрытиеВнешнихОтчетовИОбработок |

> Примечание: конфигурация **не содержит подсистем** (каталог `Subsystems` отсутствует
> в выгрузке) — структура держится на объектной логике и БСП-модулях.
> Документы / регистры накопления / регистры бухгалтерии отсутствуют.

---

## 3. Машина состояний версии (жизненный цикл)

Перечисление `СостоянияВерсии` задаёт конвейер обработки каждой версии хранилища:

```
ПолучениеВерсии → ВерсияПолучена → ВыгрузкаВерсии → ВерсияВыгружена
  → ЗагрузкаМетаданных → МетаданныеЗагружены → НачалоКоммита → ВерсияПомещена
```

Перечисление `ОперацииОчереди`: `ВыгрузкаКонфигурации`, `ЗагрузкаМетаданных`.

---

## 4. Каталог модулей (34 BSL-файла)

Размеры в байтах (`.bsl`, кодировка UTF-8 BOM).

### 4.1. Ядро конфигурации (специфичная логика)

| Модуль | Размер | Назначение |
|---|---|---|
| `CommonModules/КонвертацияХранилища` | **259 098** | Серверное ядро: весь процесс «хранилище → Git» |
| `CommonModules/ОбработкаОчередей` | 11 503 | Пакетная обработка очереди версий (выгрузка/загрузка метаданных) |
| `DataProcessors/КонвертацияВФорматEDT/Форма` | 61 301 | UI ручной конвертации версии в формат 1C:EDT |
| `DataProcessors/РегламентныеИФоновыеЗадания` (3 формы) | 64 014 + 12 808 + 4 102 | UI управления регламентными/фоновыми заданиями (на базе БСП) |
| `Ext/ManagedApplicationModule` | 2 741 | Модуль приложения: `ПередНачаломРаботыСистемы` + глобальные параметры |

### 4.2. Объектные / менеджерские модули

| Модуль | Размер | Экспортные/обработчики |
|---|---|---|
| `Catalogs/ВерсииХранилища/ManagerModule` | 16 666 | `ПоследняяВерсияХранилища`, `ЗагрузитьВерсииИзОтчета`, `УстановитьСостояние` |
| `Catalogs/ВерсииХранилища/ObjectModule` | 3 654 | `ПередЗаписью`, `ПриЗаписи` |
| `Catalogs/ХранилищаКонфигураций/ObjectModule` | 9 632 | `Перед/ПриЗаписи`, `ПриКопировании`, `ПередУдалением`, `ОбработкаПроверкиЗаполнения` |
| `Catalogs/КопииХранилищКонфигурации/ObjectModule` | 6 863 | Модуль объекта копии хранилища |
| `Catalogs/ОчередиВыполнения/ObjectModule` | 6 763 | Модуль объекта очереди (+ команда `ОчередиВыполнения`) |
| `Constants/ПутьКВерсиямПлатформыНаСервере/ValueManagerModule` | 2 112 | Менеджер значения константы |

### 4.3. Формы (полный список)

| Модуль формы | Размер |
|---|---|
| `DataProcessors/РегламентныеИФоновыеЗадания/РегламентныеИФоновыеЗадания` | 64 014 |
| `DataProcessors/КонвертацияВФорматEDT/Форма` | 61 301 |
| `Catalogs/ХранилищаКонфигураций/ФормаЭлемента` | 34 283 |
| `DataProcessors/РегламентныеИФоновыеЗадания/РегламентноеЗадание` | 12 808 |
| `Catalogs/ВерсииХранилища/ФормаСписка` | 10 079 |
| `Catalogs/КопииХранилищКонфигурации/ФормаЭлемента` | 8 323 |
| `Catalogs/ВерсииХранилища/ФормаЭлемента` | 6 340 |
| `Catalogs/ОчередиВыполнения/ФормаЭлемента` | 6 092 |
| `DataProcessors/РегламентныеИФоновыеЗадания/ФоновоеЗадание` | 4 102 |
| `Catalogs/ОчередиВыполнения/ФормаСписка` | 1 974 |
| `Catalogs/ХранилищаКонфигураций/ФормаСписка` | 1 594 |
| `DataProcessors/РегламентныеИФоновыеЗадания/Commands/РегламентныеИФоновыеЗадания` | 1 343 |
| `Catalogs/ОчередиВыполнения/Commands/ОчередиВыполнения` | 1 425 |

### 4.4. Общие модули БСП (библиотечные)

| Модуль | Размер |
|---|---|
| `ОбщегоНазначения` | 21 463 |
| `ОбщегоНазначенияГлобальный` | 1 374 |
| `ОбщегоНазначенияКлиент` | 6 618 |
| `ОбщегоНазначенияКлиентСервер` | 16 378 |
| `ОбщегоНазначенияПовтИсп` | 12 435 |
| `СтандартныеПодсистемыСервер` | 19 779 |
| `РегламентныеЗаданияСервер` | 31 516 |
| `РегламентныеЗаданияСлужебный` | 45 796 |
| `РаботаВБезопасномРежиме` | 14 486 |
| `СтроковыеФункцииКлиентСервер` | 15 812 |
| `ДлительныеОперации` | 17 361 |
| `ДлительныеОперацииКлиент` | 3 487 |
---

## 5. Ядро: общий модуль «КонвертацияХранилища»

Наибольший модуль (259 КБ, ~3 700 строк). Точки входа — регламентные и фоновые задания.
Программный интерфейс (процедуры с `Экспорт`):

### 5.1. Оркестрация конвейера
- `ВыполнитьКонвертацию(Хранилище)` — **главная точка входа** (регламентное задание «КонвертацияХранилища»). Шаги:
  1. `ПроверитьПолученныеВерсии`;  2. `ПроверитьСостоянияЗаданий`;
  3. `ПроверитьУдалениеВременныхДанныхВерсийПослеКоммита`;  4. `СформироватьФайлыGitНаСервере`;
  5. при отсутствии ограничения последней версии — создаёт **временную файловую ИБ**, формирует
     отчёт по версиям хранилища (`СформироватьОтчетПоВерсиямХранилища`) и загружает новые версии
     (`Справочники.ВерсииХранилища.ЗагрузитьВерсииИзОтчета`);
  6. запускает фоновые коммиты (`ЗапуститьКоммитыВФоне`).
- `ЗапуститьКоммитыВФоне(Хранилище)` / `ВыполнитьКоммиты(Хранилище)` — коммиты в Git.
- `ЗапуститьОбработкуВерсииВФоне(ВерсияХранилища)` / `ОбработатьВерсию` — подготовка одной версии.
- `ВыгрузитьВерсиюИзКопииХранилища(Копия)` — регламентное задание «ВыгрузкаВерсииИзКопииХранилища».
- `УдалитьФайлыВерсииВФоне(Версия)` / `УдалитьФайлыВерсии` — очистка временных данных после коммита.

### 5.2. Выгрузка/загрузка версии
- `ВыгрузитьВерсию(ВерсияХранилища, Параметры)` — выгрузка версии в файлы (форматы XML/EDT).
- `ЗагрузитьВерсию(...)` — составление индекса для переименований в Git.
- `ИмпортироватьВерсиюВEDT(...)` / `ПолучитьСписокВерсийEDT()` — конвертация в EDT.
- `ПрочитатьСтруктуруВыгрузкиВерсии(...)` — чтение структуры выгруженных файлов.
- Чтение метаданных: `ПрочитатьСвойстваОбъектаМетаданных(EDT)`, `ПолучитьСписокОбъектовДовыгрузки`.

### 5.3. Взаимодействие с Git (через командные файлы)
- `СоздатьРепозиторийGit(Хранилище)` — генерирует и запускает `.bat`/`.sh` скрипт:
  `git init`, `core.quotepath false`, `gui.encoding utf-8`, `i18n.commitEncoding utf-8`,
  `diff.renameLimit 1`, создание `.gitattributes` (бинарные `*.bin`, `*.axdt`, `*.addin`).
- `УстановитьАдресРепозиторияGit(Хранилище)` — `git remote add origin <URL>` с подстановкой
  `ПользовательСервераGit:ПарольСервераGit@` в URL.
- `СформироватьФайлыКоммитаВерсии(ВерсияХранилища)` / `ЗаписатьФайлыКоммитаВерсии(...)` —
  генерация команд `add/commit`, логов и текста комментария.
- Файлы: команда `git_command_<код>.bat/.sh`, комментарий `git_comment_<код>.txt`, лог `git_log_ver_<код>.txt`.
- После коммита `git rev-parse HEAD` → хеш сохраняется в реквизит `Хеш` версии.

### 5.4. Организационные сервисы
- `ПараметрыКаталогаИсполняемогоФайлаНаСервере`, `ПараметрыПодключенияКХранилищу`.
- `ДобавитьЗаписьВЖурналРегистрации`, `ДобавитьЗаписьВЛог`, `ПрочитатьФайлРезультата`.
- `СкопироватьФайлыРекурсивно`, `КаталогФайловКонфигурации`, работа с UUID-индексами
  (`ПрочитатьТаблицуИндексов`, `СозадатьТаблицуИндексов`, `ЗаписатьТаблицуИндексов`,
  `СформироватьUUIDПоРодителю(EDT)`).
- `СоздатьФайловуюИнформационнуюБазу`, `ОбновитьИнформационнуюБазуИзХранилища`,
  `ВыгрузитьКонфигурациюВФайлы`, `ПолучитьВерсиюВБазу` — работа с промежуточными ИБ.

### 5.5. Временные файловые ИБ в процессе конвертации

В ходе переноса хранилища в Git создаются временные **файловые** информационные базы
двух видов (создание — внешним вызовом `1cv8.exe /@ <файл параметров>` командой `CREATEINFOBASE`):

| Вид ИБ | Путь | Точки входа / процедуры | Когда создаётся | Когда удаляется |
|---|---|---|---|---|
| **Отчётная ИБ** | `<КаталогВыгрузкиВерсий>\report_db\` | `ВыполнитьКонвертацию` (стр. 20) → `СоздатьФайловуюИнформационнуюБазу` (стр. 2376) → `СформироватьОтчетПоВерсиямХранилища` (стр. 2453) → `Справочники.ВерсииХранилища.ЗагрузитьВерсииИзОтчета` | каждый цикл `ВыполнитьКонвертацию`, если `ПоследняяВерсия = 0` | сразу после загрузки отчёта — `УдалитьФайлы(КаталогИБ)` (стр. 79) |
| **Рабочая ИБ версии** | `<КаталогВыгрузкиВерсий>\<Код>\db\` | `ВыполнитьКонвертацию` (стр. 20) / `ВыгрузитьВерсиюИзКопииХранилища` (стр. 363) → `ПолучитьВерсиюВБазу` (стр. 2705) → `СоздатьФайловуюИнформационнуюБазу` (стр. 2376) → `ОбновитьИнформационнуюБазуИзХранилища` (стр. 2540) → `ВыгрузитьКонфигурациюВФайлы` (стр. 2623) → `ОбработатьВерсию` (стр. 1025) | для каждой версии (`/AddInList`, попадает в список баз 1С) | только при включённой опции `УдалятьВременныеДанныеВерсииПослеКоммита` (по умолч. Ложь): `ПроверитьУдалениеВременныхДанныхВерсийПослеКоммита` (стр. 196) → `УдалитьФайлыВерсииВФоне` (стр. 2163) → `УдалитьФайлыВерсии` (стр. 2194); также `ВерсииХранилища.ObjectModule` (ПриЗаписи) |

#### 5.5.1. Экспортные точки входа (программный интерфейс модуля «КонвертацияХранилища»)

| Процедура / Функция | Строка | Назначение |
|---|---|---|
| `ВыполнитьКонвертацию(Хранилище)` | 20 | Главная точка входа (регламентное задание «КонвертацияХранилища») |
| `ПроверитьУдалениеВременныхДанныхВерсийПослеКоммита(Хранилище)` | 196 | Контроль очистки каталогов помещённых версий |
| `ПроверитьСостоянияЗаданий(Хранилище)` | 233 | Контроль фоновых заданий, сброс «упавших» |
| `ВыгрузитьВерсиюИзКопииХранилища(Копия)` | 363 | Регламентное задание «ВыгрузкаВерсииИзКопииХранилища» |
| `ЗапуститьКоммитыВФоне(Хранилище)` | 483 | Запуск коммитов Git в фоне |
| `ВыполнитьКоммиты(Хранилище)` | 511 | Фоновое задание коммитов в Git |
| `ЗапуститьОбработкуВерсииВФоне(ВерсияХранилища)` | 983 | Запуск обработки версии в фоне |
| `ОбработатьВерсию(ВерсияХранилища)` | 1025 | Фоновое задание обработки одной версии |
| `ВыгрузитьВерсию(ВерсияХранилища, Параметры)` | 1172 | Выгрузка версии в файлы (XML) |
| `ЗагрузитьВерсию(ВерсияХранилища, Параметры)` | 1376 | Импорт в EDT / индексация для Git |
| `УдалитьФайлыВерсииВФоне(Версия)` | 2163 | Запуск удаления временных данных версии |
| `УдалитьФайлыВерсии(ВерсияХранилища)` | 2194 | Удаление каталога версии (включая ИБ `db\`) |
| `ПроверитьПолученныеВерсии(Источник)` | 2229 | Сброс «зависших» версий в `ПустаяСсылка` |
| `СоздатьФайловуюИнформационнуюБазу(Параметры)` | 2376 | `CREATEINFOBASE` внешним вызовом 1cv8 |
| `СформироватьОтчетПоВерсиямХранилища(Параметры)` | 2453 | Отчёт по версиям из временной ИБ (`Report.mxl`) |
| `ОбновитьИнформационнуюБазуИзХранилища(Параметры)` | 2540 | `DESIGNER /ConfigurationRepositoryUpdateCfg -force -v` |
| `ВыгрузитьКонфигурациюВФайлы(Параметры)` | 2623 | `DESIGNER /DumpConfigToFiles` |
| `ПолучитьВерсиюВБазу(Параметры, Хранилище, ВерсияХранилища)` | 2705 | Служебная: создание/наполнение рабочей ИБ версии |

#### 5.5.2. Точки входа менеджера и объекта справочника «ВерсииХранилища»

| Процедура / Функция | Модуль | Строка | Назначение |
|---|---|---|---|
| `ПоследняяВерсияХранилища(Хранилище)` | `ManagerModule` | 19 | Максимальный номер версии по хранилищу |
| `ЗагрузитьВерсииИзОтчета(Хранилище, ИмяФайлаОтчета)` | `ManagerModule` | 58 | Чтение `Report.mxl` и загрузка версий в справочник |
| `УстановитьСостояние(ВерсияХранилища, Состояние)` | `ManagerModule` | 288 | Установка состояния версии |
| `ПередЗаписью` / `ПриЗаписи` | `ObjectModule` | ~62 | При записи версии в состоянии `ВерсияПомещена` запускает `УдалитьФайлыВерсииВФоне` |

---

## 6. Очереди: общий модуль «ОбработкаОчередей»

Единственная экспортная процедура `ОбработатьОчередь(Очередь)` (регламентное задание
«ОбработкаОчереди»). Разбирает элементы справочника `ОчередиВыполнения`, выбирает версии
(до 100 шт.) одним пакетным запросом и выполняет операцию очереди:
- `ОперацииОчереди.ВыгрузкаКонфигурации` → состояние `ВерсияПолучена` (фоновое `ВыгрузкаВерсии`);
- `ОперацииОчереди.ЗагрузкаМетаданных` → состояние `ВерсияВыгружена` (фоновое `ЗагрузкаМетаданных`).

Механизм очерёдности агрегирует фоновые задания: версии обрабатываются пакетами, если
включена функциональная опция `ИспользоватьОчередиВыполнения`, иначе — напрямую через
`ЗапуститьОбработкуВерсииВФоне`.

---

## 7. Регламентные задания (сопоставление с обработчиками)

| Регламентное задание | Метод-обработчик |
|---|---|
| `КонвертацияХранилища` | `CommonModule.КонвертацияХранилища.ВыполнитьКонвертацию` |
| `ВыгрузкаВерсииИзКопииХранилища` | `CommonModule.КонвертацияХранилища.ВыгрузитьВерсиюИзКопииХранилища` |
| `ОбработкаОчереди` | `CommonModule.ОбработкаОчередей.ОбработатьОчередь` |

---

## 8. Обработка «КонвертацияВФорматEDT»

Форма (61 КБ) предоставляет ручной режим конвертации одной версии в формат 1C:EDT:
- обработчики выбора хранилища и имени проекта EDT,
- `КонвертироватьВФорматEDT` / `КонвертироватьВФорматEDTНаСервере`,
- `ЗаписатьФайлыКонвертации` — генерация скриптов и структуры каталогов проекта EDT,
- подбор поддерживаемых/не поддерживаемых контейнеров и соответствие типов макетов.

---

## 9. Формат выгрузки версий: XML 1С, а не EDT (проверено по коду)

Проверка утверждения «выгрузка осуществляется не в формат EDT» — **подтверждено**.

**1. Дамп версии всегда выполняется в XML 1С:Предприятия.**
`КонвертацияХранилища.ВыгрузитьКонфигурациюВФайлы()` (стр. 2623–2693) формирует
единственную команду выгрузки — DESIGNER в пакетном режиме:

```
DESIGNER /DisableStartupDialogs /L ru /VL ru <строка соединения ИБ>
| /DumpConfigToFiles "%КаталогФайловКонфигурации%" %ДополнительныеПараметры%
| /DumpResult "%ИмяФайлаРезультатов%" /Out "%ИмяФайлаЛогов%" -NoTruncate
```

Дополнительные параметры (взаимоисключаемые): выгрузка изменений
(`-update -force -getChanges ... -configDumpInfoForChanges ...`), выгрузка по списку
(`-listFile ...`) либо полная выгрузка. «EDT-режима» у команды не существует.

**2. EDT подключается отдельным шагом импорта ПОСЛЕ выгрузки — только по флагу.**
`ЗагрузитьВерсию()` (стр. 1376–1391): при `Параметры.КонвертироватьВФорматEDT`
вызывается `ИмпортироватьВерсиюВEDT()` (стр. 1398–1464), выполняющая утилиту
**Ring EDT** (не платформу 1С):

```
ring edt workspace import --workspace-location "%РабочийКаталог%"
    --configuration-files "%КаталогФайловКонфигурации%"
    --project "%КаталогПроектаEDT%" --version %ВерсияПроектаEDT%
```

Ошибка Ring (код возврата <> 0) → исключение, версия не коммитится.

**3. Флаг — реквизит справочника `ХранилищаКонфигураций` (opt-in, по умолчанию Ложь).**
`КонвертироватьВФорматEDT` (uuid `02dee9a8-9e7c-4ecd-8dc5-46666fa8bb52`).
Единовременно устанавливается обработкой «КонвертацияВФорматEDT»
(`ХранилищеОбъект.КонвертироватьВФорматEDT = Истина`); конвертация необратима —
после неё синхронизация с хранилищем конфигураций 1С невозможна (см. справку обработки).

**4. Если флаг включён, меняется только пост-обработка (не сам дамп):**

| Аспект | XML (флаг выключен) | EDT (флаг включён) |
|---|---|---|
| Проверка файлов коммита | `Configuration.xml` (стр. 705–710) | `Configuration/Configuration.mdo` |
| Разбор структуры версии | парсинг `*.xml` (стр. 1566–1568) | парсинг `*.mdo` — `ПрочитатьСвойстваОбъектаМетаданныхEDT` (стр. 1569–1572) |
| Перемещение в репозиторий | только каталоги дампа (стр. 3325–3345) | весь проект, включая `DT-INF`, `.settings` (стр. 3319–3336) |
| Частичная выгрузка изменений | `getChanges` напрямую | доп. анализ дампа + довыгрузка по списку `dumplist.txt` (стр. 1325–1342) |
| `ConfigDumpInfo.xml` | остаётся в каталоге дампа | переносится во временный каталог (стр. 1297–1308) и копируется в проект EDT (стр. 1456–1462) |

**Вывод:** дамп из хранилища всегда XML (`/DumpConfigToFiles`); формат 1C:EDT
получается последующим импортом (`ring edt workspace import`) и попадает в
репозиторий только при включённом флаге конкретного хранилища. Это согласуется
с `gk-migration-diagram.md`: 1.0.5.8 — последняя версия с форматом XML 1С,
актуальные 1.0.6+ переходят на 1C:EDT.

---

## 10. Ключевые алгоритмы и точки расширения

1. **Конвейер версий** — единый сквозной конвейер `СостоянияВерсии`, управляемый
   регламентными и фоновыми заданиями; те же состояния используются очередями.
2. **Два режима исполнения** — «фон напрямую» и «очереди» (функциональная опция
   `ИспользоватьОчередиВыполнения` + реквизиты справочника `ХранилищаКонфигураций`).
3. **Внешние процессы Git** — вся работа с Git построена на генерации и запуске
   `.bat`/`.sh` командных файлов и чтении логов/файлов результатов; ОС определяется
   через `ТипПлатформы`.
4. **Промежуточные ИБ и отчёты** — для получения истории хранилища создаётся временная
   файловая ИБ и формируется отчёт по версиям; загрузка версий из отчёта через менеджер
   справочника `ВерсииХранилища`.
5. **Соответствие авторов** — определение пользователя версии: регистр
   `ИнформацияПользователей`, общая информация и значения по умолчанию (в запросе коммита).

**Типичные точки расширения:** добавление нового формата выгрузки (аналогично EDT),
новые операции очереди (перечисление `ОперацииОчереди`), дополнительные алгоритмы Git
(команды в `СоздатьРепозиторийGit` / `СформироватьФайлыКоммитаВерсии`).

---

## 11. Наблюдения и выводы

- Конфигурация компактна и узко специализирована: 14 общих модулей, 4 справочника,
  2 обработки. Более ~259 КБ кода сосредоточено в одном модуле `КонвертацияХранилища`.
- Большая часть «обвязки» (регламентные задания, длительные операции, безопасный режим,
  строковые функции, общее назначение) — копия **БСП (Стандартные подсистемы)**, что
  соответствует стандартной схеме поставки инструментов «1С» и лицензии CC BY-SA.
- Отсутствуют подсистемы, документы и большинство типов регистров — конфигурация
  реализует единственную бизнес-задачу (миграцию хранилища в Git) минимальными средствами.
- Версия 1.0.5.8 — последняя в серии «XML 1С» (согласно `gk-migration-diagram.md`: формат
  XML 1С поддерживается до 1.0.5.8, актуальные версии 1.0.6+ переходят на 1C:EDT — подтверждено разбором кода, см. раздел 9).